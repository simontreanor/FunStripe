namespace FunStripe

open FunStripe.Json
open FunStripe.Util
open System

[<System.CodeDom.Compiler.GeneratedCode("FunStripe", "1.0.0")>]
module StripeModel =

    ///A generic type representing a paginated list of Stripe objects, including pagination metadata.
    type StripeList<'T> = {
        ///The list of objects in this page.
        Data: 'T list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Always has the value `list`.
        member _.Object = "list"

        static member New (data: 'T list, hasMore: bool, url: string) =
            {
                StripeList.Data = data
                StripeList.HasMore = hasMore
                StripeList.Url = url
            }

    [<Struct>]
    type WebhookEndpointStatus =
        | Enabled
        | Disabled

    [<Struct>]
    type VerificationSessionRedactionStatus =
        | Processing
        | Redacted

    [<Struct>]
    type UsBankAccountNetworksSupported =
        | Ach
        | UsDomesticWire

    type TreasuryTransactionsResourceFlowDetailsType =
        | CreditReversal
        | DebitReversal
        | InboundTransfer
        | IssuingAuthorization
        | Other
        | OutboundPayment
        | OutboundTransfer
        | ReceivedCredit
        | ReceivedDebit

    ///Change to a FinancialAccount's balance
    type TreasuryTransactionsResourceBalanceImpact = {
        ///The change made to funds the user can spend right now.
        Cash: int
        ///The change made to funds that are not spendable yet, but will become available at a later time.
        InboundPending: int
        ///The change made to funds in the account, but not spendable because they are being held for pending outbound flows.
        OutboundPending: int
    }
    with
        static member New (cash: int, inboundPending: int, outboundPending: int) =
            {
                TreasuryTransactionsResourceBalanceImpact.Cash = cash //required
                TreasuryTransactionsResourceBalanceImpact.InboundPending = inboundPending //required
                TreasuryTransactionsResourceBalanceImpact.OutboundPending = outboundPending //required
            }

    type TreasuryTransactionsResourceAbstractTransactionResourceStatusTransitions = {
        ///Timestamp describing when the Transaction changed status to `posted`.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]PostedAt: DateTime option
        ///Timestamp describing when the Transaction changed status to `void`.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]VoidAt: DateTime option
    }
    with
        static member New (postedAt: DateTime option, voidAt: DateTime option) =
            {
                TreasuryTransactionsResourceAbstractTransactionResourceStatusTransitions.PostedAt = postedAt //required
                TreasuryTransactionsResourceAbstractTransactionResourceStatusTransitions.VoidAt = voidAt //required
            }

    type TreasurySharedResourceInitiatingPaymentMethodDetailsUsBankAccount = {
        ///Bank name.
        BankName: string option
        ///The last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string option
        ///The routing number for the bank account.
        RoutingNumber: string option
    }
    with
        static member New (bankName: string option, last4: string option, routingNumber: string option) =
            {
                TreasurySharedResourceInitiatingPaymentMethodDetailsUsBankAccount.BankName = bankName //required
                TreasurySharedResourceInitiatingPaymentMethodDetailsUsBankAccount.Last4 = last4 //required
                TreasurySharedResourceInitiatingPaymentMethodDetailsUsBankAccount.RoutingNumber = routingNumber //required
            }

    [<Struct>]
    type TreasurySharedResourceInitiatingPaymentMethodDetailsInitiatingPaymentMethodDetailsType =
        | Balance
        | FinancialAccount
        | IssuingCard
        | Stripe
        | UsBankAccount

    type TreasuryReceivedDebitsResourceStatusTransitions = {
        ///Timestamp describing when the DebitReversal changed status to `completed`.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CompletedAt: DateTime option
    }
    with
        static member New (completedAt: DateTime option) =
            {
                TreasuryReceivedDebitsResourceStatusTransitions.CompletedAt = completedAt //required
            }

    [<Struct>]
    type TreasuryReceivedDebitsResourceReversalDetailsRestrictedReason =
        | AlreadyReversed
        | DeadlinePassed
        | NetworkRestricted
        | Other
        | SourceFlowRestricted

    type TreasuryReceivedDebitsResourceLinkedFlows = {
        ///The DebitReversal created as a result of this ReceivedDebit being reversed.
        DebitReversal: string option
        ///Set if the ReceivedDebit is associated with an InboundTransfer's return of funds.
        InboundTransfer: string option
        ///Set if the ReceivedDebit was created due to an [Issuing Authorization](https://api.stripe.com#issuing_authorizations) object.
        IssuingAuthorization: string option
        ///Set if the ReceivedDebit is also viewable as an [Issuing Dispute](https://api.stripe.com#issuing_disputes) object.
        IssuingTransaction: string option
        ///Set if the ReceivedDebit was created due to a [Payout](https://api.stripe.com#payouts) object.
        Payout: string option
        ///Set if the ReceivedDebit was created due to a [Topup](https://api.stripe.com#topups) object.
        Topup: string option
    }
    with
        static member New (debitReversal: string option, inboundTransfer: string option, issuingAuthorization: string option, issuingTransaction: string option, payout: string option, topup: string option) =
            {
                TreasuryReceivedDebitsResourceLinkedFlows.DebitReversal = debitReversal //required
                TreasuryReceivedDebitsResourceLinkedFlows.InboundTransfer = inboundTransfer //required
                TreasuryReceivedDebitsResourceLinkedFlows.IssuingAuthorization = issuingAuthorization //required
                TreasuryReceivedDebitsResourceLinkedFlows.IssuingTransaction = issuingTransaction //required
                TreasuryReceivedDebitsResourceLinkedFlows.Payout = payout //required
                TreasuryReceivedDebitsResourceLinkedFlows.Topup = topup //required
            }

    type TreasuryReceivedDebitsResourceDebitReversalLinkedFlows = {
        ///Set if there is an Issuing dispute associated with the DebitReversal.
        IssuingDispute: string option
    }
    with
        static member New (issuingDispute: string option) =
            {
                TreasuryReceivedDebitsResourceDebitReversalLinkedFlows.IssuingDispute = issuingDispute //required
            }

    type TreasuryReceivedCreditsResourceStatusTransitions = {
        ///Timestamp describing when the CreditReversal changed status to `posted`
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]PostedAt: DateTime option
    }
    with
        static member New (postedAt: DateTime option) =
            {
                TreasuryReceivedCreditsResourceStatusTransitions.PostedAt = postedAt //required
            }

    [<Struct>]
    type TreasuryReceivedCreditsResourceSourceFlowsDetailsType =
        | CreditReversal
        | Other
        | OutboundPayment
        | OutboundTransfer
        | Payout

    [<Struct>]
    type TreasuryReceivedCreditsResourceReversalDetailsRestrictedReason =
        | AlreadyReversed
        | DeadlinePassed
        | NetworkRestricted
        | Other
        | SourceFlowRestricted

    type TreasuryOutboundTransfersResourceUsDomesticWireTrackingDetails = {
        ///CHIPS System Sequence Number (SSN) of the OutboundTransfer for transfers sent over the `us_domestic_wire` network.
        Chips: string option
        ///IMAD of the OutboundTransfer for transfers sent over the `us_domestic_wire` network.
        Imad: string option
        ///OMAD of the OutboundTransfer for transfers sent over the `us_domestic_wire` network.
        Omad: string option
    }
    with
        static member New (chips: string option, imad: string option, omad: string option) =
            {
                TreasuryOutboundTransfersResourceUsDomesticWireTrackingDetails.Chips = chips //required
                TreasuryOutboundTransfersResourceUsDomesticWireTrackingDetails.Imad = imad //required
                TreasuryOutboundTransfersResourceUsDomesticWireTrackingDetails.Omad = omad //required
            }

    type TreasuryOutboundTransfersResourceStatusTransitions = {
        ///Timestamp describing when an OutboundTransfer changed status to `canceled`
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CanceledAt: DateTime option
        ///Timestamp describing when an OutboundTransfer changed status to `failed`
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]FailedAt: DateTime option
        ///Timestamp describing when an OutboundTransfer changed status to `posted`
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]PostedAt: DateTime option
        ///Timestamp describing when an OutboundTransfer changed status to `returned`
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ReturnedAt: DateTime option
    }
    with
        static member New (canceledAt: DateTime option, failedAt: DateTime option, postedAt: DateTime option, returnedAt: DateTime option) =
            {
                TreasuryOutboundTransfersResourceStatusTransitions.CanceledAt = canceledAt //required
                TreasuryOutboundTransfersResourceStatusTransitions.FailedAt = failedAt //required
                TreasuryOutboundTransfersResourceStatusTransitions.PostedAt = postedAt //required
                TreasuryOutboundTransfersResourceStatusTransitions.ReturnedAt = returnedAt //required
            }

    type TreasuryOutboundTransfersResourceReturnedDetailsCode =
        | AccountClosed
        | AccountFrozen
        | BankAccountRestricted
        | BankOwnershipChanged
        | Declined
        | IncorrectAccountHolderName
        | InvalidAccountNumber
        | InvalidCurrency
        | NoAccount
        | Other

    [<Struct>]
    type TreasuryOutboundTransfersResourceOutboundTransferResourceTrackingDetailsType =
        | Ach
        | UsDomesticWire

    type TreasuryOutboundTransfersResourceAchTrackingDetails = {
        ///ACH trace ID of the OutboundTransfer for transfers sent over the `ach` network.
        TraceId: string
    }
    with
        static member New (traceId: string) =
            {
                TreasuryOutboundTransfersResourceAchTrackingDetails.TraceId = traceId //required
            }

    type TreasuryOutboundPaymentsResourceUsDomesticWireTrackingDetails = {
        ///CHIPS System Sequence Number (SSN) of the OutboundPayment for payments sent over the `us_domestic_wire` network.
        Chips: string option
        ///IMAD of the OutboundPayment for payments sent over the `us_domestic_wire` network.
        Imad: string option
        ///OMAD of the OutboundPayment for payments sent over the `us_domestic_wire` network.
        Omad: string option
    }
    with
        static member New (chips: string option, imad: string option, omad: string option) =
            {
                TreasuryOutboundPaymentsResourceUsDomesticWireTrackingDetails.Chips = chips //required
                TreasuryOutboundPaymentsResourceUsDomesticWireTrackingDetails.Imad = imad //required
                TreasuryOutboundPaymentsResourceUsDomesticWireTrackingDetails.Omad = omad //required
            }

    type TreasuryOutboundPaymentsResourceReturnedStatusCode =
        | AccountClosed
        | AccountFrozen
        | BankAccountRestricted
        | BankOwnershipChanged
        | Declined
        | IncorrectAccountHolderName
        | InvalidAccountNumber
        | InvalidCurrency
        | NoAccount
        | Other

    [<Struct>]
    type TreasuryOutboundPaymentsResourceOutboundPaymentResourceTrackingDetailsType =
        | Ach
        | UsDomesticWire

    type TreasuryOutboundPaymentsResourceOutboundPaymentResourceStatusTransitions = {
        ///Timestamp describing when an OutboundPayment changed status to `canceled`.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CanceledAt: DateTime option
        ///Timestamp describing when an OutboundPayment changed status to `failed`.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]FailedAt: DateTime option
        ///Timestamp describing when an OutboundPayment changed status to `posted`.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]PostedAt: DateTime option
        ///Timestamp describing when an OutboundPayment changed status to `returned`.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ReturnedAt: DateTime option
    }
    with
        static member New (canceledAt: DateTime option, failedAt: DateTime option, postedAt: DateTime option, returnedAt: DateTime option) =
            {
                TreasuryOutboundPaymentsResourceOutboundPaymentResourceStatusTransitions.CanceledAt = canceledAt //required
                TreasuryOutboundPaymentsResourceOutboundPaymentResourceStatusTransitions.FailedAt = failedAt //required
                TreasuryOutboundPaymentsResourceOutboundPaymentResourceStatusTransitions.PostedAt = postedAt //required
                TreasuryOutboundPaymentsResourceOutboundPaymentResourceStatusTransitions.ReturnedAt = returnedAt //required
            }

    type TreasuryOutboundPaymentsResourceOutboundPaymentResourceEndUserDetails = {
        ///IP address of the user initiating the OutboundPayment. Set if `present` is set to `true`. IP address collection is required for risk and compliance reasons. This will be used to help determine if the OutboundPayment is authorized or should be blocked.
        IpAddress: string option
        ///`true` if the OutboundPayment creation request is being made on behalf of an end user by a platform. Otherwise, `false`.
        Present: bool
    }
    with
        static member New (ipAddress: string option, present: bool) =
            {
                TreasuryOutboundPaymentsResourceOutboundPaymentResourceEndUserDetails.IpAddress = ipAddress //required
                TreasuryOutboundPaymentsResourceOutboundPaymentResourceEndUserDetails.Present = present //required
            }

    type TreasuryOutboundPaymentsResourceAchTrackingDetails = {
        ///ACH trace ID of the OutboundPayment for payments sent over the `ach` network.
        TraceId: string
    }
    with
        static member New (traceId: string) =
            {
                TreasuryOutboundPaymentsResourceAchTrackingDetails.TraceId = traceId //required
            }

    type TreasuryInboundTransfersResourceInboundTransferResourceStatusTransitions = {
        ///Timestamp describing when an InboundTransfer changed status to `canceled`.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CanceledAt: DateTime option
        ///Timestamp describing when an InboundTransfer changed status to `failed`.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]FailedAt: DateTime option
        ///Timestamp describing when an InboundTransfer changed status to `succeeded`.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]SucceededAt: DateTime option
    }
    with
        static member New (failedAt: DateTime option, succeededAt: DateTime option, ?canceledAt: DateTime option) =
            {
                TreasuryInboundTransfersResourceInboundTransferResourceStatusTransitions.FailedAt = failedAt //required
                TreasuryInboundTransfersResourceInboundTransferResourceStatusTransitions.SucceededAt = succeededAt //required
                TreasuryInboundTransfersResourceInboundTransferResourceStatusTransitions.CanceledAt = canceledAt |> Option.flatten
            }

    type TreasuryInboundTransfersResourceInboundTransferResourceLinkedFlows = {
        ///If funds for this flow were returned after the flow went to the `succeeded` state, this field contains a reference to the ReceivedDebit return.
        ReceivedDebit: string option
    }
    with
        static member New (receivedDebit: string option) =
            {
                TreasuryInboundTransfersResourceInboundTransferResourceLinkedFlows.ReceivedDebit = receivedDebit //required
            }

    type TreasuryInboundTransfersResourceFailureDetailsCode =
        | AccountClosed
        | AccountFrozen
        | BankAccountRestricted
        | BankOwnershipChanged
        | DebitNotAuthorized
        | IncorrectAccountHolderAddress
        | IncorrectAccountHolderName
        | IncorrectAccountHolderTaxId
        | InsufficientFunds
        | InvalidAccountNumber
        | InvalidCurrency
        | NoAccount
        | Other

    [<Struct>]
    type TreasuryFinancialAccountsResourceTogglesSettingStatusDetailsRestriction =
        | InboundFlows
        | OutboundFlows

    [<Struct>]
    type TreasuryFinancialAccountsResourceTogglesSettingStatusDetailsResolution =
        | ContactStripe
        | ProvideInformation
        | RemoveRestriction

    type TreasuryFinancialAccountsResourceTogglesSettingStatusDetailsCode =
        | Activating
        | CapabilityNotRequested
        | FinancialAccountClosed
        | RejectedOther
        | RejectedUnsupportedBusiness
        | RequirementsPastDue
        | RequirementsPendingVerification
        | RestrictedByPlatform
        | RestrictedOther

    [<Struct>]
    type TreasuryFinancialAccountsResourceToggleSettingsStatus =
        | Active
        | Pending
        | Restricted

    [<Struct>]
    type TreasuryFinancialAccountsResourcePlatformRestrictionsOutboundFlows =
        | Restricted
        | Unrestricted

    [<Struct>]
    type TreasuryFinancialAccountsResourcePlatformRestrictionsInboundFlows =
        | Restricted
        | Unrestricted

    [<Struct>]
    type TreasuryFinancialAccountsResourceOutboundAchToggleSettingsStatus =
        | Active
        | Pending
        | Restricted

    [<Struct>]
    type TreasuryFinancialAccountsResourceInboundAchToggleSettingsStatus =
        | Active
        | Pending
        | Restricted

    [<Struct>]
    type TreasuryFinancialAccountsResourceFinancialAddressSupportedNetworks =
        | Ach
        | UsDomesticWire

    [<Struct>]
    type TreasuryFinancialAccountsResourceClosedStatusDetailsReasons =
        | AccountRejected
        | ClosedByPlatform
        | Other

    [<Struct>]
    type TreasuryFinancialAccountsResourceAbaToggleSettingsStatus =
        | Active
        | Pending
        | Restricted

    ///ABA Records contain U.S. bank account details per the ABA format.
    type TreasuryFinancialAccountsResourceAbaRecord = {
        ///The name of the person or business that owns the bank account.
        AccountHolderName: string
        ///The account number.
        AccountNumber: string option
        ///The last four characters of the account number.
        [<JsonField(Name="account_number_last4")>]AccountNumberLast4: string
        ///Name of the bank.
        BankName: string
        ///Routing number for the account.
        RoutingNumber: string
    }
    with
        static member New (accountHolderName: string, accountNumberLast4: string, bankName: string, routingNumber: string, ?accountNumber: string option) =
            {
                TreasuryFinancialAccountsResourceAbaRecord.AccountHolderName = accountHolderName //required
                TreasuryFinancialAccountsResourceAbaRecord.AccountNumberLast4 = accountNumberLast4 //required
                TreasuryFinancialAccountsResourceAbaRecord.BankName = bankName //required
                TreasuryFinancialAccountsResourceAbaRecord.RoutingNumber = routingNumber //required
                TreasuryFinancialAccountsResourceAbaRecord.AccountNumber = accountNumber |> Option.flatten
            }

    type TreasuryTransactionEntryType =
        | CreditReversal
        | CreditReversalPosting
        | DebitReversal
        | InboundTransfer
        | InboundTransferReturn
        | IssuingAuthorizationHold
        | IssuingAuthorizationRelease
        | Other
        | OutboundPayment
        | OutboundPaymentCancellation
        | OutboundPaymentFailure
        | OutboundPaymentPosting
        | OutboundPaymentReturn
        | OutboundTransfer
        | OutboundTransferCancellation
        | OutboundTransferFailure
        | OutboundTransferPosting
        | OutboundTransferReturn
        | ReceivedCredit
        | ReceivedDebit

    type TreasuryTransactionEntryFlowType =
        | CreditReversal
        | DebitReversal
        | InboundTransfer
        | IssuingAuthorization
        | Other
        | OutboundPayment
        | OutboundTransfer
        | ReceivedCredit
        | ReceivedDebit

    [<Struct>]
    type TreasuryTransactionStatus =
        | Open
        | Posted
        | Void

    type TreasuryTransactionFlowType =
        | CreditReversal
        | DebitReversal
        | InboundTransfer
        | IssuingAuthorization
        | Other
        | OutboundPayment
        | OutboundTransfer
        | ReceivedCredit
        | ReceivedDebit

    [<Struct>]
    type TreasuryReceivedDebitStatus =
        | Failed
        | Succeeded

    [<Struct>]
    type TreasuryReceivedDebitNetwork =
        | Ach
        | Card
        | Stripe

    [<Struct>]
    type TreasuryReceivedDebitFailureCode =
        | AccountClosed
        | AccountFrozen
        | InsufficientFunds
        | InternationalTransaction
        | Other

    [<Struct>]
    type TreasuryReceivedCreditStatus =
        | Failed
        | Succeeded

    [<Struct>]
    type TreasuryReceivedCreditNetwork =
        | Ach
        | Card
        | Stripe
        | UsDomesticWire

    [<Struct>]
    type TreasuryReceivedCreditFailureCode =
        | AccountClosed
        | AccountFrozen
        | InternationalTransaction
        | Other

    [<Struct>]
    type TreasuryOutboundTransferStatus =
        | Canceled
        | Failed
        | Posted
        | Processing
        | Returned

    [<Struct>]
    type TreasuryOutboundPaymentStatus =
        | Canceled
        | Failed
        | Posted
        | Processing
        | Returned

    [<Struct>]
    type TreasuryInboundTransferStatus =
        | Canceled
        | Failed
        | Processing
        | Succeeded

    type TreasuryFinancialAccountRestrictedFeatures =
        | CardIssuing
        | DepositInsurance
        | [<JsonUnionCase("financial_addresses.aba")>] FinancialAddressesAba
        | [<JsonUnionCase("financial_addresses.aba.forwarding")>] FinancialAddressesAbaForwarding
        | [<JsonUnionCase("inbound_transfers.ach")>] InboundTransfersAch
        | IntraStripeFlows
        | [<JsonUnionCase("outbound_payments.ach")>] OutboundPaymentsAch
        | [<JsonUnionCase("outbound_payments.us_domestic_wire")>] OutboundPaymentsUsDomesticWire
        | [<JsonUnionCase("outbound_transfers.ach")>] OutboundTransfersAch
        | [<JsonUnionCase("outbound_transfers.us_domestic_wire")>] OutboundTransfersUsDomesticWire
        | RemoteDepositCapture

    type TreasuryFinancialAccountPendingFeatures =
        | CardIssuing
        | DepositInsurance
        | [<JsonUnionCase("financial_addresses.aba")>] FinancialAddressesAba
        | [<JsonUnionCase("financial_addresses.aba.forwarding")>] FinancialAddressesAbaForwarding
        | [<JsonUnionCase("inbound_transfers.ach")>] InboundTransfersAch
        | IntraStripeFlows
        | [<JsonUnionCase("outbound_payments.ach")>] OutboundPaymentsAch
        | [<JsonUnionCase("outbound_payments.us_domestic_wire")>] OutboundPaymentsUsDomesticWire
        | [<JsonUnionCase("outbound_transfers.ach")>] OutboundTransfersAch
        | [<JsonUnionCase("outbound_transfers.us_domestic_wire")>] OutboundTransfersUsDomesticWire
        | RemoteDepositCapture

    type TreasuryFinancialAccountActiveFeatures =
        | CardIssuing
        | DepositInsurance
        | [<JsonUnionCase("financial_addresses.aba")>] FinancialAddressesAba
        | [<JsonUnionCase("financial_addresses.aba.forwarding")>] FinancialAddressesAbaForwarding
        | [<JsonUnionCase("inbound_transfers.ach")>] InboundTransfersAch
        | IntraStripeFlows
        | [<JsonUnionCase("outbound_payments.ach")>] OutboundPaymentsAch
        | [<JsonUnionCase("outbound_payments.us_domestic_wire")>] OutboundPaymentsUsDomesticWire
        | [<JsonUnionCase("outbound_transfers.ach")>] OutboundTransfersAch
        | [<JsonUnionCase("outbound_transfers.us_domestic_wire")>] OutboundTransfersUsDomesticWire
        | RemoteDepositCapture

    [<Struct>]
    type TreasuryFinancialAccountStatus =
        | Closed
        | Open

    [<Struct>]
    type TreasuryDebitReversalStatus =
        | Failed
        | Processing
        | Succeeded

    [<Struct>]
    type TreasuryDebitReversalNetwork =
        | Ach
        | Card

    [<Struct>]
    type TreasuryCreditReversalStatus =
        | Canceled
        | Posted
        | Processing

    [<Struct>]
    type TreasuryCreditReversalNetwork =
        | Ach
        | Stripe

    [<Struct>]
    type TransformUsageRound =
        | Down
        | Up

    [<Struct>]
    type TransformQuantityRound =
        | Down
        | Up

    [<Struct>]
    type TransferScheduleWeeklyPayoutDays =
        | Friday
        | Monday
        | Thursday
        | Tuesday
        | Wednesday

    [<Struct>]
    type TransferScheduleInterval =
        | Manual
        | Daily
        | Weekly
        | Monthly

    [<Struct>]
    type TransferSourceType =
        | Card
        | Fpx
        | BankAccount

    [<Struct>]
    type TopupStatus =
        | Canceled
        | Failed
        | Pending
        | Reversed
        | Succeeded

    type TokenCardNetworks = {
        ///The preferred network for co-branded cards. Can be `cartes_bancaires`, `mastercard`, `visa` or `invalid_preference` if requested network is not valid for the card.
        Preferred: string option
    }
    with
        static member New (preferred: string option) =
            {
                TokenCardNetworks.Preferred = preferred //required
            }

    [<Struct>]
    type TokenType =
        | Account
        | BankAccount
        | Card
        | Pii

    type ThreeDSecureUsage = {
        ///Whether 3D Secure is supported on this card.
        Supported: bool
    }
    with
        static member New (supported: bool) =
            {
                ThreeDSecureUsage.Supported = supported //required
            }

    [<Struct>]
    type ThreeDSecureDetailsChargeVersion =
        | [<JsonUnionCase("1.0.2")>] Numeric102
        | [<JsonUnionCase("2.1.0")>] Numeric210
        | [<JsonUnionCase("2.2.0")>] Numeric220
        | [<JsonUnionCase("2.3.0")>] Numeric230
        | [<JsonUnionCase("2.3.1")>] Numeric231

    type ThreeDSecureDetailsChargeResultReason =
        | Abandoned
        | Bypassed
        | Canceled
        | CardNotEnrolled
        | NetworkNotSupported
        | ProtocolError
        | Rejected

    type ThreeDSecureDetailsChargeResult =
        | AttemptAcknowledged
        | Authenticated
        | Exempted
        | Failed
        | NotSupported
        | ProcessingError

    [<Struct>]
    type ThreeDSecureDetailsChargeExemptionIndicator =
        | LowRisk
        | [<JsonUnionCase("none")>] None'

    [<Struct>]
    type ThreeDSecureDetailsChargeElectronicCommerceIndicator =
        | [<JsonUnionCase("01")>] Numeric01
        | [<JsonUnionCase("02")>] Numeric02
        | [<JsonUnionCase("05")>] Numeric05
        | [<JsonUnionCase("06")>] Numeric06
        | [<JsonUnionCase("07")>] Numeric07

    [<Struct>]
    type ThreeDSecureDetailsChargeAuthenticationFlow =
        | Challenge
        | Frictionless

    [<Struct>]
    type ThreeDSecureDetailsVersion =
        | [<JsonUnionCase("1.0.2")>] Numeric102
        | [<JsonUnionCase("2.1.0")>] Numeric210
        | [<JsonUnionCase("2.2.0")>] Numeric220
        | [<JsonUnionCase("2.3.0")>] Numeric230
        | [<JsonUnionCase("2.3.1")>] Numeric231

    type ThreeDSecureDetailsResultReason =
        | Abandoned
        | Bypassed
        | Canceled
        | CardNotEnrolled
        | NetworkNotSupported
        | ProtocolError
        | Rejected

    type ThreeDSecureDetailsResult =
        | AttemptAcknowledged
        | Authenticated
        | Exempted
        | Failed
        | NotSupported
        | ProcessingError

    [<Struct>]
    type ThreeDSecureDetailsElectronicCommerceIndicator =
        | [<JsonUnionCase("01")>] Numeric01
        | [<JsonUnionCase("02")>] Numeric02
        | [<JsonUnionCase("05")>] Numeric05
        | [<JsonUnionCase("06")>] Numeric06
        | [<JsonUnionCase("07")>] Numeric07

    [<Struct>]
    type ThreeDSecureDetailsAuthenticationFlow =
        | Challenge
        | Frictionless

    [<Struct>]
    type TestHelpersTestClockStatus =
        | Advancing
        | InternalFailure
        | Ready

    [<Struct>]
    type TerminalReaderReaderResourceToggleValue =
        | Disabled
        | Enabled

    [<Struct>]
    type TerminalReaderReaderResourceToggleDefaultValue =
        | Disabled
        | Enabled

    ///Represents a per-transaction tipping configuration
    type TerminalReaderReaderResourceTippingConfig = {
        ///Amount used to calculate tip suggestions on tipping selection screen for this transaction. Must be a positive integer in the smallest currency unit (e.g., 100 cents to represent $1.00 or 100 to represent ¥100, a zero-decimal currency).
        AmountEligible: int option
    }
    with
        static member New (?amountEligible: int) =
            {
                TerminalReaderReaderResourceTippingConfig.AmountEligible = amountEligible
            }

    ///Information about text being collected using a reader
    type TerminalReaderReaderResourceText = {
        ///The collected text value
        Value: string option
    }
    with
        static member New (value: string option) =
            {
                TerminalReaderReaderResourceText.Value = value //required
            }

    ///Information about a signature being collected using a reader
    type TerminalReaderReaderResourceSignature = {
        ///The File ID of a collected signature image
        Value: string option
    }
    with
        static member New (value: string option) =
            {
                TerminalReaderReaderResourceSignature.Value = value //required
            }

    ///Represents a per-transaction override of a reader configuration
    type TerminalReaderReaderResourceRefundPaymentConfig = {
        ///Enable customer-initiated cancellation when refunding this payment.
        EnableCustomerCancellation: bool option
    }
    with
        static member New (?enableCustomerCancellation: bool) =
            {
                TerminalReaderReaderResourceRefundPaymentConfig.EnableCustomerCancellation = enableCustomerCancellation
            }

    [<Struct>]
    type TerminalReaderReaderResourceRefundPaymentActionReason =
        | Duplicate
        | Fraudulent
        | RequestedByCustomer

    type TerminalReaderReaderResourceReaderActionType =
        | CollectInputs
        | CollectPaymentMethod
        | ConfirmPaymentIntent
        | ProcessPaymentIntent
        | ProcessSetupIntent
        | RefundPayment
        | SetReaderDisplay

    [<Struct>]
    type TerminalReaderReaderResourceReaderActionStatus =
        | Failed
        | InProgress
        | Succeeded

    ///Represents a per-setup override of a reader configuration
    type TerminalReaderReaderResourceProcessSetupConfig = {
        ///Enable customer-initiated cancellation when processing this SetupIntent.
        EnableCustomerCancellation: bool option
    }
    with
        static member New (?enableCustomerCancellation: bool) =
            {
                TerminalReaderReaderResourceProcessSetupConfig.EnableCustomerCancellation = enableCustomerCancellation
            }

    ///Information about a phone number being collected using a reader
    type TerminalReaderReaderResourcePhone = {
        ///The collected phone number
        Value: string option
    }
    with
        static member New (value: string option) =
            {
                TerminalReaderReaderResourcePhone.Value = value //required
            }

    ///Information about a number being collected using a reader
    type TerminalReaderReaderResourceNumeric = {
        ///The collected number
        Value: string option
    }
    with
        static member New (value: string option) =
            {
                TerminalReaderReaderResourceNumeric.Value = value //required
            }

    ///Represents a line item to be displayed on the reader
    type TerminalReaderReaderResourceLineItem = {
        ///The amount of the line item. A positive integer in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
        Amount: int
        ///Description of the line item.
        Description: string
        ///The quantity of the line item.
        Quantity: int
    }
    with
        static member New (amount: int, description: string, quantity: int) =
            {
                TerminalReaderReaderResourceLineItem.Amount = amount //required
                TerminalReaderReaderResourceLineItem.Description = description //required
                TerminalReaderReaderResourceLineItem.Quantity = quantity //required
            }

    type TerminalReaderReaderResourceInputType =
        | Email
        | Numeric
        | Phone
        | Selection
        | Signature
        | Text

    ///Information about a email being collected using a reader
    type TerminalReaderReaderResourceEmail = {
        ///The collected email address
        Value: string option
    }
    with
        static member New (value: string option) =
            {
                TerminalReaderReaderResourceEmail.Value = value //required
            }

    ///Represents custom text to be displayed when collecting the input using a reader
    type TerminalReaderReaderResourceCustomText = {
        ///Customize the default description for this input
        Description: string option
        ///Customize the default label for this input's skip button
        SkipButton: string option
        ///Customize the default label for this input's submit button
        SubmitButton: string option
        ///Customize the default title for this input
        Title: string option
    }
    with
        static member New (description: string option, skipButton: string option, submitButton: string option, title: string option) =
            {
                TerminalReaderReaderResourceCustomText.Description = description //required
                TerminalReaderReaderResourceCustomText.SkipButton = skipButton //required
                TerminalReaderReaderResourceCustomText.SubmitButton = submitButton //required
                TerminalReaderReaderResourceCustomText.Title = title //required
            }

    ///Represents a per-transaction override of a reader configuration
    type TerminalReaderReaderResourceConfirmConfig = {
        ///If the customer doesn't abandon authenticating the payment, they're redirected to this URL after completion.
        ReturnUrl: string option
    }
    with
        static member New (?returnUrl: string) =
            {
                TerminalReaderReaderResourceConfirmConfig.ReturnUrl = returnUrl
            }

    [<Struct>]
    type TerminalReaderReaderResourceChoiceStyle =
        | Primary
        | Secondary

    ///Options associated with the Apple Terms and Conditions link type.
    type TerminalOnboardingLinkAppleTermsAndConditions = {
        ///Whether the link should also support users relinking their Apple account.
        AllowRelinking: bool option
        ///The business name of the merchant accepting Apple's Terms and Conditions.
        MerchantDisplayName: string
    }
    with
        static member New (allowRelinking: bool option, merchantDisplayName: string) =
            {
                TerminalOnboardingLinkAppleTermsAndConditions.AllowRelinking = allowRelinking //required
                TerminalOnboardingLinkAppleTermsAndConditions.MerchantDisplayName = merchantDisplayName //required
            }

    [<Struct>]
    type TerminalConfigurationConfigurationResourceWifiConfigType =
        | EnterpriseEapPeap
        | EnterpriseEapTls
        | PersonalPsk

    type TerminalConfigurationConfigurationResourceRebootWindow = {
        ///Integer between 0 to 23 that represents the end hour of the reboot time window. The value must be different than the start_hour.
        EndHour: int
        ///Integer between 0 to 23 that represents the start hour of the reboot time window.
        StartHour: int
    }
    with
        static member New (endHour: int, startHour: int) =
            {
                TerminalConfigurationConfigurationResourceRebootWindow.EndHour = endHour //required
                TerminalConfigurationConfigurationResourceRebootWindow.StartHour = startHour //required
            }

    type TerminalConfigurationConfigurationResourcePersonalPskWifi = {
        ///Password for connecting to the WiFi network
        Password: string
        ///Name of the WiFi network
        Ssid: string
    }
    with
        static member New (password: string, ssid: string) =
            {
                TerminalConfigurationConfigurationResourcePersonalPskWifi.Password = password //required
                TerminalConfigurationConfigurationResourcePersonalPskWifi.Ssid = ssid //required
            }

    type TerminalConfigurationConfigurationResourceOfflineConfig = {
        ///Determines whether to allow transactions to be collected while reader is offline. Defaults to false.
        Enabled: bool option
    }
    with
        static member New (enabled: bool option) =
            {
                TerminalConfigurationConfigurationResourceOfflineConfig.Enabled = enabled //required
            }

    type TerminalConfigurationConfigurationResourceEnterpriseTlsWifi = {
        ///A File ID representing a PEM file containing the server certificate
        CaCertificateFile: string option
        ///A File ID representing a PEM file containing the client certificate
        ClientCertificateFile: string
        ///A File ID representing a PEM file containing the client RSA private key
        PrivateKeyFile: string
        ///Password for the private key file
        PrivateKeyFilePassword: string option
        ///Name of the WiFi network
        Ssid: string
    }
    with
        static member New (clientCertificateFile: string, privateKeyFile: string, ssid: string, ?caCertificateFile: string, ?privateKeyFilePassword: string) =
            {
                TerminalConfigurationConfigurationResourceEnterpriseTlsWifi.ClientCertificateFile = clientCertificateFile //required
                TerminalConfigurationConfigurationResourceEnterpriseTlsWifi.PrivateKeyFile = privateKeyFile //required
                TerminalConfigurationConfigurationResourceEnterpriseTlsWifi.Ssid = ssid //required
                TerminalConfigurationConfigurationResourceEnterpriseTlsWifi.CaCertificateFile = caCertificateFile
                TerminalConfigurationConfigurationResourceEnterpriseTlsWifi.PrivateKeyFilePassword = privateKeyFilePassword
            }

    type TerminalConfigurationConfigurationResourceEnterprisePeapWifi = {
        ///A File ID representing a PEM file containing the server certificate
        CaCertificateFile: string option
        ///Password for connecting to the WiFi network
        Password: string
        ///Name of the WiFi network
        Ssid: string
        ///Username for connecting to the WiFi network
        Username: string
    }
    with
        static member New (password: string, ssid: string, username: string, ?caCertificateFile: string) =
            {
                TerminalConfigurationConfigurationResourceEnterprisePeapWifi.Password = password //required
                TerminalConfigurationConfigurationResourceEnterprisePeapWifi.Ssid = ssid //required
                TerminalConfigurationConfigurationResourceEnterprisePeapWifi.Username = username //required
                TerminalConfigurationConfigurationResourceEnterprisePeapWifi.CaCertificateFile = caCertificateFile
            }

    type TerminalConfigurationConfigurationResourceCellularConfig = {
        ///Whether a cellular-capable reader can connect to the internet over cellular.
        Enabled: bool
    }
    with
        static member New (enabled: bool) =
            {
                TerminalConfigurationConfigurationResourceCellularConfig.Enabled = enabled //required
            }

    [<Struct>]
    type TerminalReaderStatus =
        | Offline
        | Online

    type TerminalReaderDeviceType =
        | BbposChipper2x
        | BbposWisepad3
        | BbposWiseposE
        | MobilePhoneReader
        | SimulatedStripeS700
        | SimulatedStripeS710
        | SimulatedWiseposE
        | StripeM2
        | StripeS700
        | StripeS710
        | VerifoneP400

    ///A Connection Token is used by the Stripe Terminal SDK to connect to a reader.
    ///Related guide: [Fleet management](https://docs.stripe.com/terminal/fleet/locations)
    type TerminalConnectionToken = {
        ///The id of the location that this connection token is scoped to. Note that location scoping only applies to internet-connected readers. For more details, see [the docs on scoping connection tokens](https://docs.stripe.com/terminal/fleet/locations-and-zones?dashboard-or-api=api#connection-tokens).
        Location: string option
        ///Your application should pass this token to the Stripe Terminal SDK.
        Secret: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "terminal.connection_token"

        static member New (secret: string, ?location: string) =
            {
                TerminalConnectionToken.Secret = secret //required
                TerminalConnectionToken.Location = location
            }

    type TaxRateTaxType =
        | AmusementTax
        | CommunicationsTax
        | Gst
        | Hst
        | Igst
        | Jct
        | LeaseTax
        | Pst
        | Qst
        | RetailDeliveryFee
        | Rst
        | SalesTax
        | ServiceTax
        | Vat

    [<Struct>]
    type TaxRateRateType =
        | FlatAmount
        | Percentage

    type TaxRateJurisdictionLevel =
        | City
        | Country
        | County
        | District
        | Multiple
        | State

    [<Struct>]
    type TaxProductResourceTaxTransactionShippingCostTaxBehavior =
        | Exclusive
        | Inclusive

    type TaxProductResourceTaxTransactionResourceReversal = {
        ///The `id` of the reversed `Transaction` object.
        OriginalTransaction: string option
    }
    with
        static member New (originalTransaction: string option) =
            {
                TaxProductResourceTaxTransactionResourceReversal.OriginalTransaction = originalTransaction //required
            }

    type TaxProductResourceTaxTransactionLineItemResourceReversal = {
        ///The `id` of the line item to reverse in the original transaction.
        OriginalLineItem: string
    }
    with
        static member New (originalLineItem: string) =
            {
                TaxProductResourceTaxTransactionLineItemResourceReversal.OriginalLineItem = originalLineItem //required
            }

    type TaxProductResourceTaxSettingsStatusDetailsResourceActive = {
        TaxProductResourceTaxSettingsStatusDetailsResourceActive: string option
    }
    with
        static member New (?taxProductResourceTaxSettingsStatusDetailsResourceActive: string option) =
            {
                TaxProductResourceTaxSettingsStatusDetailsResourceActive.TaxProductResourceTaxSettingsStatusDetailsResourceActive = taxProductResourceTaxSettingsStatusDetailsResourceActive |> Option.flatten
            }

    [<Struct>]
    type TaxProductResourceTaxSettingsDefaultsTaxBehavior =
        | Exclusive
        | Inclusive
        | InferredByCurrency

    [<Struct>]
    type TaxProductResourceTaxSettingsDefaultsProvider =
        | Anrok
        | Avalara
        | Sphere
        | Stripe

    type TaxProductResourceTaxRateDetailsTaxType =
        | AmusementTax
        | CommunicationsTax
        | Gst
        | Hst
        | Igst
        | Jct
        | LeaseTax
        | Pst
        | Qst
        | RetailDeliveryFee
        | Rst
        | SalesTax
        | ServiceTax
        | Vat

    [<Struct>]
    type TaxProductResourceTaxRateDetailsRateType =
        | FlatAmount
        | Percentage

    [<Struct>]
    type TaxProductResourceTaxCalculationShippingCostTaxBehavior =
        | Exclusive
        | Inclusive

    type TaxProductResourceTaxBreakdownTaxabilityReason =
        | CustomerExempt
        | NotCollecting
        | NotSubjectToTax
        | NotSupported
        | PortionProductExempt
        | PortionReducedRated
        | PortionStandardRated
        | ProductExempt
        | ProductExemptHoliday
        | ProportionallyRated
        | ReducedRated
        | ReverseCharge
        | StandardRated
        | TaxableBasisReduced
        | ZeroRated

    [<Struct>]
    type TaxProductResourceTaxAssociationTransactionAttemptsResourceErroredReason =
        | AnotherPaymentAssociatedWithCalculation
        | CalculationExpired
        | CurrencyMismatch
        | OriginalTransactionVoided
        | UniqueReferenceViolation

    type TaxProductResourceTaxAssociationTransactionAttemptsResourceCommitted = {
        ///The [Tax Transaction](https://docs.stripe.com/api/tax/transaction/object)
        Transaction: string
    }
    with
        static member New (transaction: string) =
            {
                TaxProductResourceTaxAssociationTransactionAttemptsResourceCommitted.Transaction = transaction //required
            }

    [<Struct>]
    type TaxProductResourceTaxAssociationTransactionAttemptsStatus =
        | Errored
        | Committed

    type TaxProductResourceLineItemTaxRateDetailsTaxType =
        | AmusementTax
        | CommunicationsTax
        | Gst
        | Hst
        | Igst
        | Jct
        | LeaseTax
        | Pst
        | Qst
        | RetailDeliveryFee
        | Rst
        | SalesTax
        | ServiceTax
        | Vat

    type TaxProductResourceLineItemTaxBreakdownTaxabilityReason =
        | CustomerExempt
        | NotCollecting
        | NotSubjectToTax
        | NotSupported
        | PortionProductExempt
        | PortionReducedRated
        | PortionStandardRated
        | ProductExempt
        | ProductExemptHoliday
        | ProportionallyRated
        | ReducedRated
        | ReverseCharge
        | StandardRated
        | TaxableBasisReduced
        | ZeroRated

    [<Struct>]
    type TaxProductResourceLineItemTaxBreakdownSourcing =
        | Destination
        | Origin

    [<Struct>]
    type TaxProductResourceJurisdictionLevel =
        | City
        | Country
        | County
        | District
        | State

    type TaxProductResourceCustomerDetailsResourceTaxIdType =
        | AdNrt
        | AeTrn
        | AlTin
        | AmTin
        | AoTin
        | ArCuit
        | AuAbn
        | AuArn
        | AwTin
        | AzTin
        | BaTin
        | BbTin
        | BdBin
        | BfIfu
        | BgUic
        | BhVat
        | BjIfu
        | BoTin
        | BrCnpj
        | BrCpf
        | BsTin
        | ByTin
        | CaBn
        | CaGstHst
        | CaPstBc
        | CaPstMb
        | CaPstSk
        | CaQst
        | CdNif
        | ChUid
        | ChVat
        | ClTin
        | CmNiu
        | CnTin
        | CoNit
        | CrTin
        | CvNif
        | DeStn
        | DoRcn
        | EcRuc
        | EgTin
        | EsCif
        | EtTin
        | EuOssVat
        | EuVat
        | FoVat
        | GbVat
        | GeVat
        | GiTin
        | GnNif
        | HkBr
        | HrOib
        | HuTin
        | IdNpwp
        | IlVat
        | InGst
        | IsVat
        | ItCf
        | JpCn
        | JpRn
        | JpTrn
        | KePin
        | KgTin
        | KhTin
        | KrBrn
        | KzBin
        | LaTin
        | LiUid
        | LiVat
        | LkVat
        | MaVat
        | MdVat
        | MePib
        | MkVat
        | MrNif
        | MxRfc
        | MyFrp
        | MyItn
        | MySst
        | NgTin
        | NoVat
        | NoVoec
        | NpPan
        | NzGst
        | OmVat
        | PeRuc
        | PhTin
        | PlNip
        | PyRuc
        | RoTin
        | RsPib
        | RuInn
        | RuKpp
        | SaVat
        | SgGst
        | SgUen
        | SiTin
        | SnNinea
        | SrFin
        | SvNit
        | ThVat
        | TjTin
        | TrTin
        | TwVat
        | TzVat
        | UaVat
        | UgTin
        | Unknown
        | UsEin
        | UyRuc
        | UzTin
        | UzVat
        | VeRif
        | VnTin
        | ZaVat
        | ZmTin
        | ZwTin

    [<Struct>]
    type TaxProductResourceCustomerDetailsTaxabilityOverride =
        | CustomerExempt
        | [<JsonUnionCase("none")>] None'
        | ReverseCharge

    [<Struct>]
    type TaxProductResourceCustomerDetailsAddressSource =
        | Billing
        | Shipping

    [<Struct>]
    type TaxProductRegistrationsResourceCountryOptionsUsStateSalesTaxElectionType =
        | LocalUseTax
        | SimplifiedSellersUseTax
        | SingleLocalUseTax

    type TaxProductRegistrationsResourceCountryOptionsUsLocalLeaseTax = {
        ///A [FIPS code](https://www.census.gov/library/reference/code-lists/ansi.html) representing the local jurisdiction.
        Jurisdiction: string
    }
    with
        static member New (jurisdiction: string) =
            {
                TaxProductRegistrationsResourceCountryOptionsUsLocalLeaseTax.Jurisdiction = jurisdiction //required
            }

    type TaxProductRegistrationsResourceCountryOptionsUsLocalAmusementTax = {
        ///A [FIPS code](https://www.census.gov/library/reference/code-lists/ansi.html) representing the local jurisdiction.
        Jurisdiction: string
    }
    with
        static member New (jurisdiction: string) =
            {
                TaxProductRegistrationsResourceCountryOptionsUsLocalAmusementTax.Jurisdiction = jurisdiction //required
            }

    [<Struct>]
    type TaxProductRegistrationsResourceCountryOptionsUnitedStatesType =
        | LocalAmusementTax
        | LocalLeaseTax
        | StateCommunicationsTax
        | StateRetailDeliveryFee
        | StateSalesTax

    type TaxProductRegistrationsResourceCountryOptionsThailand () = 
        ///Type of registration in `country`.
        member _.Type = "simplified"


    type TaxProductRegistrationsResourceCountryOptionsSimplified () = 
        ///Type of registration in `country`.
        member _.Type = "simplified"


    [<Struct>]
    type TaxProductRegistrationsResourceCountryOptionsEuropeType =
        | Ioss
        | OssNonUnion
        | OssUnion
        | Standard

    [<Struct>]
    type TaxProductRegistrationsResourceCountryOptionsEuStandardPlaceOfSupplyScheme =
        | InboundGoods
        | SmallSeller
        | Standard

    [<Struct>]
    type TaxProductRegistrationsResourceCountryOptionsDefaultStandardPlaceOfSupplyScheme =
        | InboundGoods
        | Standard

    type TaxProductRegistrationsResourceCountryOptionsDefault () = 
        ///Type of registration in `country`.
        member _.Type = "standard"


    [<Struct>]
    type TaxProductRegistrationsResourceCountryOptionsCanadaType =
        | ProvinceStandard
        | Simplified
        | Standard

    type TaxProductRegistrationsResourceCountryOptionsCaProvinceStandard = {
        ///Two-letter CA province code ([ISO 3166-2](https://en.wikipedia.org/wiki/ISO_3166-2)).
        Province: string
    }
    with
        static member New (province: string) =
            {
                TaxProductRegistrationsResourceCountryOptionsCaProvinceStandard.Province = province //required
            }

    [<Struct>]
    type TaxIdVerificationStatus =
        | Pending
        | Unavailable
        | Unverified
        | Verified

    type TaxIdType =
        | AdNrt
        | AeTrn
        | AlTin
        | AmTin
        | AoTin
        | ArCuit
        | AuAbn
        | AuArn
        | AwTin
        | AzTin
        | BaTin
        | BbTin
        | BdBin
        | BfIfu
        | BgUic
        | BhVat
        | BjIfu
        | BoTin
        | BrCnpj
        | BrCpf
        | BsTin
        | ByTin
        | CaBn
        | CaGstHst
        | CaPstBc
        | CaPstMb
        | CaPstSk
        | CaQst
        | CdNif
        | ChUid
        | ChVat
        | ClTin
        | CmNiu
        | CnTin
        | CoNit
        | CrTin
        | CvNif
        | DeStn
        | DoRcn
        | EcRuc
        | EgTin
        | EsCif
        | EtTin
        | EuOssVat
        | EuVat
        | FoVat
        | GbVat
        | GeVat
        | GiTin
        | GnNif
        | HkBr
        | HrOib
        | HuTin
        | IdNpwp
        | IlVat
        | InGst
        | IsVat
        | ItCf
        | JpCn
        | JpRn
        | JpTrn
        | KePin
        | KgTin
        | KhTin
        | KrBrn
        | KzBin
        | LaTin
        | LiUid
        | LiVat
        | LkVat
        | MaVat
        | MdVat
        | MePib
        | MkVat
        | MrNif
        | MxRfc
        | MyFrp
        | MyItn
        | MySst
        | NgTin
        | NoVat
        | NoVoec
        | NpPan
        | NzGst
        | OmVat
        | PeRuc
        | PhTin
        | PlNip
        | PyRuc
        | RoTin
        | RsPib
        | RuInn
        | RuKpp
        | SaVat
        | SgGst
        | SgUen
        | SiTin
        | SnNinea
        | SrFin
        | SvNit
        | ThVat
        | TjTin
        | TrTin
        | TwVat
        | TzVat
        | UaVat
        | UgTin
        | Unknown
        | UsEin
        | UyRuc
        | UzTin
        | UzVat
        | VeRif
        | VnTin
        | ZaVat
        | ZmTin
        | ZwTin

    [<Struct>]
    type TaxIDsOwnerType =
        | Account
        | Application
        | Customer
        | Self

    type TaxDeductedAtSource = {
        ///Unique identifier for the object.
        Id: string
        ///The end of the invoicing period. This TDS applies to Stripe fees collected during this invoicing period.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]PeriodEnd: DateTime
        ///The start of the invoicing period. This TDS applies to Stripe fees collected during this invoicing period.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]PeriodStart: DateTime
        ///The TAN that was supplied to Stripe when TDS was assessed
        TaxDeductionAccountNumber: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "tax_deducted_at_source"

        static member New (id: string, periodEnd: DateTime, periodStart: DateTime, taxDeductionAccountNumber: string) =
            {
                TaxDeductedAtSource.Id = id //required
                TaxDeductedAtSource.PeriodEnd = periodEnd //required
                TaxDeductedAtSource.PeriodStart = periodStart //required
                TaxDeductedAtSource.TaxDeductionAccountNumber = taxDeductionAccountNumber //required
            }

    ///[Tax codes](https://stripe.com/docs/tax/tax-categories) classify goods and services for tax purposes.
    type TaxCode = {
        ///A detailed description of which types of products the tax code represents.
        Description: string
        ///Unique identifier for the object.
        Id: string
        ///A short name for the tax code.
        Name: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "tax_code"

        static member New (description: string, id: string, name: string) =
            {
                TaxCode.Description = description //required
                TaxCode.Id = id //required
                TaxCode.Name = name //required
            }

    [<Struct>]
    type TaxTransactionLineItemType =
        | Reversal
        | Transaction

    [<Struct>]
    type TaxTransactionLineItemTaxBehavior =
        | Exclusive
        | Inclusive

    [<Struct>]
    type TaxTransactionType =
        | Reversal
        | Transaction

    [<Struct>]
    type TaxSettingsStatus =
        | Active
        | Pending

    [<Struct>]
    type TaxRegistrationStatus =
        | Active
        | Expired
        | Scheduled

    [<Struct>]
    type TaxCalculationLineItemTaxBehavior =
        | Exclusive
        | Inclusive

    [<Struct>]
    type SubscriptionsTrialsResourceEndBehaviorMissingPaymentMethod =
        | Cancel
        | CreateInvoice
        | Pause

    [<Struct>]
    type SubscriptionsResourceTrialSettingsEndBehaviorMissingPaymentMethod =
        | Cancel
        | CreateInvoice
        | Pause

    type SubscriptionsResourcePaymentSettingsPaymentMethodTypes =
        | AchCreditTransfer
        | AchDebit
        | AcssDebit
        | Affirm
        | AmazonPay
        | AuBecsDebit
        | BacsDebit
        | Bancontact
        | Boleto
        | Card
        | Cashapp
        | Crypto
        | Custom
        | CustomerBalance
        | Eps
        | Fpx
        | Giropay
        | Grabpay
        | Ideal
        | JpCreditTransfer
        | KakaoPay
        | Klarna
        | Konbini
        | KrCard
        | Link
        | Multibanco
        | NaverPay
        | NzBankAccount
        | P24
        | PayByBank
        | Payco
        | Paynow
        | Paypal
        | Payto
        | Pix
        | Promptpay
        | RevolutPay
        | SepaCreditTransfer
        | SepaDebit
        | Sofort
        | Swish
        | Upi
        | UsBankAccount
        | WechatPay

    [<Struct>]
    type SubscriptionsResourcePaymentSettingsSaveDefaultPaymentMethod =
        | Off
        | OnSubscription

    [<Struct>]
    type SubscriptionsResourcePauseCollectionBehavior =
        | KeepAsDraft
        | MarkUncollectible
        | Void

    [<Struct>]
    type SubscriptionsResourceBillingModeFlexibleProrationDiscounts =
        | Included
        | Itemized

    [<Struct>]
    type SubscriptionsResourceBillingModeType =
        | Classic
        | Flexible

    type SubscriptionsResourceBillingCycleAnchorConfig = {
        ///The day of the month of the billing_cycle_anchor.
        DayOfMonth: int
        ///The hour of the day of the billing_cycle_anchor.
        Hour: int option
        ///The minute of the hour of the billing_cycle_anchor.
        Minute: int option
        ///The month to start full cycle billing periods.
        Month: int option
        ///The second of the minute of the billing_cycle_anchor.
        Second: int option
    }
    with
        static member New (dayOfMonth: int, hour: int option, minute: int option, month: int option, second: int option) =
            {
                SubscriptionsResourceBillingCycleAnchorConfig.DayOfMonth = dayOfMonth //required
                SubscriptionsResourceBillingCycleAnchorConfig.Hour = hour //required
                SubscriptionsResourceBillingCycleAnchorConfig.Minute = minute //required
                SubscriptionsResourceBillingCycleAnchorConfig.Month = month //required
                SubscriptionsResourceBillingCycleAnchorConfig.Second = second //required
            }

    [<Struct>]
    type SubscriptionSchedulesResourceInvoiceItemPeriodResourcePeriodStartType =
        | MaxItemPeriodStart
        | PhaseStart
        | Timestamp

    [<Struct>]
    type SubscriptionSchedulesResourceInvoiceItemPeriodResourcePeriodEndType =
        | MinItemPeriodEnd
        | PhaseEnd
        | Timestamp

    [<Struct>]
    type SubscriptionSchedulesResourceDefaultSettingsCollectionMethod =
        | ChargeAutomatically
        | SendInvoice

    [<Struct>]
    type SubscriptionSchedulesResourceDefaultSettingsBillingCycleAnchor =
        | Automatic
        | PhaseStart

    [<Struct>]
    type SubscriptionSchedulePhaseConfigurationProrationBehavior =
        | AlwaysInvoice
        | CreateProrations
        | [<JsonUnionCase("none")>] None'

    [<Struct>]
    type SubscriptionSchedulePhaseConfigurationCollectionMethod =
        | ChargeAutomatically
        | SendInvoice

    [<Struct>]
    type SubscriptionSchedulePhaseConfigurationBillingCycleAnchor =
        | Automatic
        | PhaseStart

    type SubscriptionScheduleCurrentPhase = {
        ///The end of this phase of the subscription schedule.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]EndDate: DateTime
        ///The start of this phase of the subscription schedule.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]StartDate: DateTime
    }
    with
        static member New (endDate: DateTime, startDate: DateTime) =
            {
                SubscriptionScheduleCurrentPhase.EndDate = endDate //required
                SubscriptionScheduleCurrentPhase.StartDate = startDate //required
            }

    [<Struct>]
    type SubscriptionScheduleStatus =
        | Active
        | Canceled
        | Completed
        | NotStarted
        | Released

    [<Struct>]
    type SubscriptionScheduleEndBehavior =
        | Cancel
        | [<JsonUnionCase("none")>] None'
        | Release
        | Renew

    [<Struct>]
    type SubscriptionPendingInvoiceItemIntervalInterval =
        | Day
        | Month
        | Week
        | Year

    [<Struct>]
    type SubscriptionPaymentMethodOptionsMandateOptionsPixPaymentSchedule =
        | Halfyearly
        | Monthly
        | Quarterly
        | Weekly
        | Yearly

    [<Struct>]
    type SubscriptionPaymentMethodOptionsMandateOptionsPixAmountIncludesIof =
        | Always
        | Never

    [<Struct>]
    type SubscriptionPaymentMethodOptionsCardRequestThreeDSecure =
        | Any
        | Automatic
        | Challenge

    type SubscriptionPaymentMethodOptionsCardNetwork =
        | Amex
        | CartesBancaires
        | Diners
        | Discover
        | EftposAu
        | Girocard
        | Interac
        | Jcb
        | Link
        | Mastercard
        | Unionpay
        | Unknown
        | Visa

    type SubscriptionItemBillingThresholds = {
        ///Usage threshold that triggers the subscription to create an invoice
        UsageGte: int option
    }
    with
        static member New (usageGte: int option) =
            {
                SubscriptionItemBillingThresholds.UsageGte = usageGte //required
            }

    type SubscriptionBillingThresholds = {
        ///Monetary threshold that triggers the subscription to create an invoice
        AmountGte: int option
        ///Indicates if the `billing_cycle_anchor` should be reset when a threshold is reached. If true, `billing_cycle_anchor` will be updated to the date/time the threshold was last reached; otherwise, the value will remain unchanged. This value may not be `true` if the subscription contains items with plans that have `aggregate_usage=last_ever`.
        ResetBillingCycleAnchor: bool option
    }
    with
        static member New (amountGte: int option, resetBillingCycleAnchor: bool option) =
            {
                SubscriptionBillingThresholds.AmountGte = amountGte //required
                SubscriptionBillingThresholds.ResetBillingCycleAnchor = resetBillingCycleAnchor //required
            }

    type SubscriptionStatus =
        | Active
        | Canceled
        | Incomplete
        | IncompleteExpired
        | PastDue
        | Paused
        | Trialing
        | Unpaid

    [<Struct>]
    type SubscriptionCollectionMethod =
        | ChargeAutomatically
        | SendInvoice

    type SourceTypeWechat = {
        PrepayId: string option
        QrCodeUrl: string option
        StatementDescriptor: string option
    }
    with
        static member New (?prepayId: string, ?qrCodeUrl: string option, ?statementDescriptor: string) =
            {
                SourceTypeWechat.PrepayId = prepayId
                SourceTypeWechat.QrCodeUrl = qrCodeUrl |> Option.flatten
                SourceTypeWechat.StatementDescriptor = statementDescriptor
            }

    type SourceTypeP24 = {
        Reference: string option
    }
    with
        static member New (?reference: string option) =
            {
                SourceTypeP24.Reference = reference |> Option.flatten
            }

    type SourceTypeIdeal = {
        Bank: string option
        Bic: string option
        [<JsonField(Name="iban_last4")>]IbanLast4: string option
        StatementDescriptor: string option
    }
    with
        static member New (?bank: string option, ?bic: string option, ?ibanLast4: string option, ?statementDescriptor: string option) =
            {
                SourceTypeIdeal.Bank = bank |> Option.flatten
                SourceTypeIdeal.Bic = bic |> Option.flatten
                SourceTypeIdeal.IbanLast4 = ibanLast4 |> Option.flatten
                SourceTypeIdeal.StatementDescriptor = statementDescriptor |> Option.flatten
            }

    type SourceTypeGiropay = {
        BankCode: string option
        BankName: string option
        Bic: string option
        StatementDescriptor: string option
    }
    with
        static member New (?bankCode: string option, ?bankName: string option, ?bic: string option, ?statementDescriptor: string option) =
            {
                SourceTypeGiropay.BankCode = bankCode |> Option.flatten
                SourceTypeGiropay.BankName = bankName |> Option.flatten
                SourceTypeGiropay.Bic = bic |> Option.flatten
                SourceTypeGiropay.StatementDescriptor = statementDescriptor |> Option.flatten
            }

    type SourceTypeEps = {
        Reference: string option
        StatementDescriptor: string option
    }
    with
        static member New (?reference: string option, ?statementDescriptor: string option) =
            {
                SourceTypeEps.Reference = reference |> Option.flatten
                SourceTypeEps.StatementDescriptor = statementDescriptor |> Option.flatten
            }

    type SourceTypeBancontact = {
        BankCode: string option
        BankName: string option
        Bic: string option
        [<JsonField(Name="iban_last4")>]IbanLast4: string option
        PreferredLanguage: string option
        StatementDescriptor: string option
    }
    with
        static member New (?bankCode: string option, ?bankName: string option, ?bic: string option, ?ibanLast4: string option, ?preferredLanguage: string option, ?statementDescriptor: string option) =
            {
                SourceTypeBancontact.BankCode = bankCode |> Option.flatten
                SourceTypeBancontact.BankName = bankName |> Option.flatten
                SourceTypeBancontact.Bic = bic |> Option.flatten
                SourceTypeBancontact.IbanLast4 = ibanLast4 |> Option.flatten
                SourceTypeBancontact.PreferredLanguage = preferredLanguage |> Option.flatten
                SourceTypeBancontact.StatementDescriptor = statementDescriptor |> Option.flatten
            }

    type SourceTypeAuBecsDebit = {
        BsbNumber: string option
        Fingerprint: string option
        [<JsonField(Name="last4")>]Last4: string option
    }
    with
        static member New (?bsbNumber: string option, ?fingerprint: string option, ?last4: string option) =
            {
                SourceTypeAuBecsDebit.BsbNumber = bsbNumber |> Option.flatten
                SourceTypeAuBecsDebit.Fingerprint = fingerprint |> Option.flatten
                SourceTypeAuBecsDebit.Last4 = last4 |> Option.flatten
            }

    type SourceTypeAlipay = {
        DataString: string option
        NativeUrl: string option
        StatementDescriptor: string option
    }
    with
        static member New (?dataString: string option, ?nativeUrl: string option, ?statementDescriptor: string option) =
            {
                SourceTypeAlipay.DataString = dataString |> Option.flatten
                SourceTypeAlipay.NativeUrl = nativeUrl |> Option.flatten
                SourceTypeAlipay.StatementDescriptor = statementDescriptor |> Option.flatten
            }

    type SourceTypeAchCreditTransfer = {
        AccountNumber: string option
        BankName: string option
        Fingerprint: string option
        RefundAccountHolderName: string option
        RefundAccountHolderType: string option
        RefundRoutingNumber: string option
        RoutingNumber: string option
        SwiftCode: string option
    }
    with
        static member New (?accountNumber: string option, ?bankName: string option, ?fingerprint: string option, ?refundAccountHolderName: string option, ?refundAccountHolderType: string option, ?refundRoutingNumber: string option, ?routingNumber: string option, ?swiftCode: string option) =
            {
                SourceTypeAchCreditTransfer.AccountNumber = accountNumber |> Option.flatten
                SourceTypeAchCreditTransfer.BankName = bankName |> Option.flatten
                SourceTypeAchCreditTransfer.Fingerprint = fingerprint |> Option.flatten
                SourceTypeAchCreditTransfer.RefundAccountHolderName = refundAccountHolderName |> Option.flatten
                SourceTypeAchCreditTransfer.RefundAccountHolderType = refundAccountHolderType |> Option.flatten
                SourceTypeAchCreditTransfer.RefundRoutingNumber = refundRoutingNumber |> Option.flatten
                SourceTypeAchCreditTransfer.RoutingNumber = routingNumber |> Option.flatten
                SourceTypeAchCreditTransfer.SwiftCode = swiftCode |> Option.flatten
            }

    type SourceTransactionSepaCreditTransferData = {
        ///Reference associated with the transfer.
        Reference: string option
        ///Sender's bank account IBAN.
        SenderIban: string option
        ///Sender's name.
        SenderName: string option
    }
    with
        static member New (?reference: string, ?senderIban: string, ?senderName: string) =
            {
                SourceTransactionSepaCreditTransferData.Reference = reference
                SourceTransactionSepaCreditTransferData.SenderIban = senderIban
                SourceTransactionSepaCreditTransferData.SenderName = senderName
            }

    type SourceTransactionPaperCheckData = {
        ///Time at which the deposited funds will be available for use. Measured in seconds since the Unix epoch.
        AvailableAt: string option
        ///Comma-separated list of invoice IDs associated with the paper check.
        Invoices: string option
    }
    with
        static member New (?availableAt: string, ?invoices: string) =
            {
                SourceTransactionPaperCheckData.AvailableAt = availableAt
                SourceTransactionPaperCheckData.Invoices = invoices
            }

    type SourceTransactionGbpCreditTransferData = {
        ///Bank account fingerprint associated with the Stripe owned bank account receiving the transfer.
        Fingerprint: string option
        ///The credit transfer rails the sender used to push this transfer. The possible rails are: Faster Payments, BACS, CHAPS, and wire transfers. Currently only Faster Payments is supported.
        FundingMethod: string option
        ///Last 4 digits of sender account number associated with the transfer.
        [<JsonField(Name="last4")>]Last4: string option
        ///Sender entered arbitrary information about the transfer.
        Reference: string option
        ///Sender account number associated with the transfer.
        SenderAccountNumber: string option
        ///Sender name associated with the transfer.
        SenderName: string option
        ///Sender sort code associated with the transfer.
        SenderSortCode: string option
    }
    with
        static member New (?fingerprint: string, ?fundingMethod: string, ?last4: string, ?reference: string, ?senderAccountNumber: string, ?senderName: string, ?senderSortCode: string) =
            {
                SourceTransactionGbpCreditTransferData.Fingerprint = fingerprint
                SourceTransactionGbpCreditTransferData.FundingMethod = fundingMethod
                SourceTransactionGbpCreditTransferData.Last4 = last4
                SourceTransactionGbpCreditTransferData.Reference = reference
                SourceTransactionGbpCreditTransferData.SenderAccountNumber = senderAccountNumber
                SourceTransactionGbpCreditTransferData.SenderName = senderName
                SourceTransactionGbpCreditTransferData.SenderSortCode = senderSortCode
            }

    type SourceTransactionAchCreditTransferData = {
        ///Customer data associated with the transfer.
        CustomerData: string option
        ///Bank account fingerprint associated with the transfer.
        Fingerprint: string option
        ///Last 4 digits of the account number associated with the transfer.
        [<JsonField(Name="last4")>]Last4: string option
        ///Routing number associated with the transfer.
        RoutingNumber: string option
    }
    with
        static member New (?customerData: string, ?fingerprint: string, ?last4: string, ?routingNumber: string) =
            {
                SourceTransactionAchCreditTransferData.CustomerData = customerData
                SourceTransactionAchCreditTransferData.Fingerprint = fingerprint
                SourceTransactionAchCreditTransferData.Last4 = last4
                SourceTransactionAchCreditTransferData.RoutingNumber = routingNumber
            }

    type SourceTransactionType =
        | AchCreditTransfer
        | AchDebit
        | Alipay
        | Bancontact
        | Card
        | CardPresent
        | Eps
        | Giropay
        | Ideal
        | Klarna
        | Multibanco
        | P24
        | SepaDebit
        | Sofort
        | ThreeDSecure
        | Wechat

    [<Struct>]
    type SourceTransactionStatus =
        | Succeeded
        | Pending
        | Failed

    type SourceRedirectFlow = {
        ///The failure reason for the redirect, either `user_abort` (the customer aborted or dropped out of the redirect flow), `declined` (the authentication failed or the transaction was declined), or `processing_error` (the redirect failed due to a technical error). Present only if the redirect status is `failed`.
        FailureReason: string option
        ///The URL you provide to redirect the customer to after they authenticated their payment.
        ReturnUrl: string
        ///The status of the redirect, either `pending` (ready to be used by your customer to authenticate the transaction), `succeeded` (successful authentication, cannot be reused) or `not_required` (redirect should not be used) or `failed` (failed authentication, cannot be reused).
        Status: string
        ///The URL provided to you to redirect a customer to as part of a `redirect` authentication flow.
        Url: string
    }
    with
        static member New (failureReason: string option, returnUrl: string, status: string, url: string) =
            {
                SourceRedirectFlow.FailureReason = failureReason //required
                SourceRedirectFlow.ReturnUrl = returnUrl //required
                SourceRedirectFlow.Status = status //required
                SourceRedirectFlow.Url = url //required
            }

    [<Struct>]
    type SourceReceiverFlowRefundAttributesStatus =
        | Missing
        | Requested
        | Available

    [<Struct>]
    type SourceReceiverFlowRefundAttributesMethod =
        | Email
        | Manual
        | [<JsonUnionCase("none")>] None'

    [<Struct>]
    type SourceOrderItemType =
        | Sku
        | Tax
        | Shipping

    type SourceMandateNotificationSepaDebitData = {
        ///SEPA creditor ID.
        CreditorIdentifier: string option
        ///Last 4 digits of the account number associated with the debit.
        [<JsonField(Name="last4")>]Last4: string option
        ///Mandate reference associated with the debit.
        MandateReference: string option
    }
    with
        static member New (?creditorIdentifier: string, ?last4: string, ?mandateReference: string) =
            {
                SourceMandateNotificationSepaDebitData.CreditorIdentifier = creditorIdentifier
                SourceMandateNotificationSepaDebitData.Last4 = last4
                SourceMandateNotificationSepaDebitData.MandateReference = mandateReference
            }

    type SourceMandateNotificationBacsDebitData = {
        ///Last 4 digits of the account number associated with the debit.
        [<JsonField(Name="last4")>]Last4: string option
    }
    with
        static member New (?last4: string) =
            {
                SourceMandateNotificationBacsDebitData.Last4 = last4
            }

    type SourceMandateNotificationAcssDebitData = {
        ///The statement descriptor associate with the debit.
        StatementDescriptor: string option
    }
    with
        static member New (?statementDescriptor: string) =
            {
                SourceMandateNotificationAcssDebitData.StatementDescriptor = statementDescriptor
            }

    [<Struct>]
    type SourceMandateNotificationStatus =
        | Pending
        | Submitted

    [<Struct>]
    type SourceMandateNotificationReason =
        | MandateConfirmed
        | DebitInitiated

    type SourceCodeVerificationFlow = {
        ///The number of attempts remaining to authenticate the source object with a verification code.
        AttemptsRemaining: int
        ///The status of the code verification, either `pending` (awaiting verification, `attempts_remaining` should be greater than 0), `succeeded` (successful verification) or `failed` (failed verification, cannot be verified anymore as `attempts_remaining` should be 0).
        Status: string
    }
    with
        static member New (attemptsRemaining: int, status: string) =
            {
                SourceCodeVerificationFlow.AttemptsRemaining = attemptsRemaining //required
                SourceCodeVerificationFlow.Status = status //required
            }

    [<Struct>]
    type SourceUsage =
        | Reusable
        | SingleUse

    type SourceType =
        | AchCreditTransfer
        | AchDebit
        | AcssDebit
        | Alipay
        | AuBecsDebit
        | Bancontact
        | Card
        | CardPresent
        | Eps
        | Giropay
        | Ideal
        | Klarna
        | Multibanco
        | P24
        | SepaCreditTransfer
        | SepaDebit
        | Sofort
        | ThreeDSecure
        | Wechat

    [<Struct>]
    type SourceStatus =
        | Canceled
        | Chargeable
        | Consumed
        | Failed
        | Pending

    [<Struct>]
    type SourceAllowRedisplay =
        | Always
        | Limited
        | Unspecified

    type SmorResourceManagedPayments = {
        ///Set to `true` to enable [Managed Payments](https://docs.stripe.com/payments/managed-payments), Stripe's merchant of record solution, for this session.
        Enabled: bool
    }
    with
        static member New (enabled: bool) =
            {
                SmorResourceManagedPayments.Enabled = enabled //required
            }

    type SigmaScheduledQueryRunError = {
        ///Information about the run failure.
        Message: string
    }
    with
        static member New (message: string) =
            {
                SigmaScheduledQueryRunError.Message = message //required
            }

    [<Struct>]
    type ShippingRateDeliveryEstimateBoundUnit =
        | BusinessDay
        | Day
        | Hour
        | Month
        | Week

    [<Struct>]
    type ShippingRateCurrencyOptionTaxBehavior =
        | Exclusive
        | Inclusive
        | Unspecified

    [<Struct>]
    type ShippingRateTaxBehavior =
        | Exclusive
        | Inclusive
        | Unspecified

    [<Struct>]
    type SetupIntentTypeSpecificPaymentMethodOptionsClientVerificationMethod =
        | Automatic
        | Instant
        | Microdeposits

    [<Struct>]
    type SetupIntentPaymentMethodOptionsUsBankAccountVerificationMethod =
        | Automatic
        | Instant
        | Microdeposits

    type SetupIntentPaymentMethodOptionsPaypal = {
        ///The PayPal Billing Agreement ID (BAID). This is an ID generated by PayPal which represents the mandate between the merchant and the customer.
        BillingAgreementId: string option
    }
    with
        static member New (billingAgreementId: string option) =
            {
                SetupIntentPaymentMethodOptionsPaypal.BillingAgreementId = billingAgreementId //required
            }

    type SetupIntentPaymentMethodOptionsMandateOptionsSepaDebit = {
        ///Prefix used to generate the Mandate reference. Must be at most 12 characters long. Must consist of only uppercase letters, numbers, spaces, or the following special characters: '/', '_', '-', '&', '.'. Cannot begin with 'STRIPE'.
        ReferencePrefix: string option
    }
    with
        static member New (?referencePrefix: string) =
            {
                SetupIntentPaymentMethodOptionsMandateOptionsSepaDebit.ReferencePrefix = referencePrefix
            }

    type SetupIntentPaymentMethodOptionsMandateOptionsPaytoPurpose =
        | DependantSupport
        | Government
        | Loan
        | Mortgage
        | Other
        | Pension
        | Personal
        | Retail
        | Salary
        | Tax
        | Utility

    type SetupIntentPaymentMethodOptionsMandateOptionsPaytoPaymentSchedule =
        | Adhoc
        | Annual
        | Daily
        | Fortnightly
        | Monthly
        | Quarterly
        | SemiAnnual
        | Weekly

    [<Struct>]
    type SetupIntentPaymentMethodOptionsMandateOptionsPaytoAmountType =
        | Fixed
        | Maximum

    type SetupIntentPaymentMethodOptionsMandateOptionsBacsDebit = {
        ///Prefix used to generate the Mandate reference. Must be at most 12 characters long. Must consist of only uppercase letters, numbers, spaces, or the following special characters: '/', '_', '-', '&', '.'. Cannot begin with 'DDIC' or 'STRIPE'.
        ReferencePrefix: string option
    }
    with
        static member New (?referencePrefix: string) =
            {
                SetupIntentPaymentMethodOptionsMandateOptionsBacsDebit.ReferencePrefix = referencePrefix
            }

    [<Struct>]
    type SetupIntentPaymentMethodOptionsMandateOptionsAcssDebitDefaultFor =
        | Invoice
        | Subscription

    [<Struct>]
    type SetupIntentPaymentMethodOptionsMandateOptionsAcssDebitTransactionType =
        | Business
        | Personal

    [<Struct>]
    type SetupIntentPaymentMethodOptionsMandateOptionsAcssDebitPaymentSchedule =
        | Combined
        | Interval
        | Sporadic

    type SetupIntentPaymentMethodOptionsLink = {
        ///[Deprecated] This is a legacy parameter that no longer has any function.
        PersistentToken: string option
    }
    with
        static member New (persistentToken: string option) =
            {
                SetupIntentPaymentMethodOptionsLink.PersistentToken = persistentToken //required
            }

    type SetupIntentPaymentMethodOptionsCardPresent = {
        SetupIntentPaymentMethodOptionsCardPresent: string option
    }
    with
        static member New (?setupIntentPaymentMethodOptionsCardPresent: string option) =
            {
                SetupIntentPaymentMethodOptionsCardPresent.SetupIntentPaymentMethodOptionsCardPresent = setupIntentPaymentMethodOptionsCardPresent |> Option.flatten
            }

    [<Struct>]
    type SetupIntentPaymentMethodOptionsCardMandateOptionsInterval =
        | Day
        | Month
        | Sporadic
        | Week
        | Year

    [<Struct>]
    type SetupIntentPaymentMethodOptionsCardMandateOptionsAmountType =
        | Fixed
        | Maximum

    [<Struct>]
    type SetupIntentPaymentMethodOptionsCardRequestThreeDSecure =
        | Any
        | Automatic
        | Challenge

    type SetupIntentPaymentMethodOptionsCardNetwork =
        | Amex
        | CartesBancaires
        | Diners
        | Discover
        | EftposAu
        | Girocard
        | Interac
        | Jcb
        | Link
        | Mastercard
        | Unionpay
        | Unknown
        | Visa

    type SetupIntentPaymentMethodOptionsAmazonPay = {
        SetupIntentPaymentMethodOptionsAmazonPay: string option
    }
    with
        static member New (?setupIntentPaymentMethodOptionsAmazonPay: string option) =
            {
                SetupIntentPaymentMethodOptionsAmazonPay.SetupIntentPaymentMethodOptionsAmazonPay = setupIntentPaymentMethodOptionsAmazonPay |> Option.flatten
            }

    [<Struct>]
    type SetupIntentPaymentMethodOptionsAcssDebitVerificationMethod =
        | Automatic
        | Instant
        | Microdeposits

    [<Struct>]
    type SetupIntentPaymentMethodOptionsAcssDebitCurrency =
        | Cad
        | Usd

    [<Struct>]
    type SetupIntentNextActionVerifyWithMicrodepositsMicrodepositType =
        | Amounts
        | DescriptorCode

    type SetupIntentNextActionRedirectToUrl = {
        ///If the customer does not exit their browser while authenticating, they will be redirected to this specified URL after completion.
        ReturnUrl: string option
        ///The URL you must redirect your customer to in order to authenticate.
        Url: string option
    }
    with
        static member New (returnUrl: string option, url: string option) =
            {
                SetupIntentNextActionRedirectToUrl.ReturnUrl = returnUrl //required
                SetupIntentNextActionRedirectToUrl.Url = url //required
            }

    type SetupIntentNextActionPixDisplayQrCode = {
        ///The raw data string used to generate QR code, it should be used together with QR code library.
        Data: string
        ///The date (unix timestamp) when the PIX expires.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime
        ///The URL to the hosted pix instructions page, which allows customers to view the pix QR code.
        HostedInstructionsUrl: string
        ///The image_url_png string used to render png QR code
        ImageUrlPng: string
        ///The image_url_svg string used to render svg QR code
        ImageUrlSvg: string
    }
    with
        static member New (data: string, expiresAt: DateTime, hostedInstructionsUrl: string, imageUrlPng: string, imageUrlSvg: string) =
            {
                SetupIntentNextActionPixDisplayQrCode.Data = data //required
                SetupIntentNextActionPixDisplayQrCode.ExpiresAt = expiresAt //required
                SetupIntentNextActionPixDisplayQrCode.HostedInstructionsUrl = hostedInstructionsUrl //required
                SetupIntentNextActionPixDisplayQrCode.ImageUrlPng = imageUrlPng //required
                SetupIntentNextActionPixDisplayQrCode.ImageUrlSvg = imageUrlSvg //required
            }

    [<Struct>]
    type SetupIntentNextActionType =
        | RedirectToUrl
        | UseStripeSdk
        | AlipayHandleRedirect
        | OxxoDisplayDetails
        | VerifyWithMicrodeposits

    [<Struct>]
    type SetupIntentFlowDirections =
        | Inbound
        | Outbound

    type SetupIntentExcludedPaymentMethodTypes =
        | AcssDebit
        | Affirm
        | AfterpayClearpay
        | Alipay
        | Alma
        | AmazonPay
        | AuBecsDebit
        | BacsDebit
        | Bancontact
        | Billie
        | Blik
        | Boleto
        | Card
        | Cashapp
        | Crypto
        | CustomerBalance
        | Eps
        | Fpx
        | Giropay
        | Grabpay
        | Ideal
        | KakaoPay
        | Klarna
        | Konbini
        | KrCard
        | MbWay
        | Mobilepay
        | Multibanco
        | NaverPay
        | NzBankAccount
        | Oxxo
        | P24
        | PayByBank
        | Payco
        | Paynow
        | Paypal
        | Payto
        | Pix
        | Promptpay
        | RevolutPay
        | SamsungPay
        | Satispay
        | SepaDebit
        | Sofort
        | Sunbit
        | Swish
        | Twint
        | Upi
        | UsBankAccount
        | WechatPay
        | Zip

    type SetupIntentStatus =
        | Canceled
        | Processing
        | RequiresAction
        | RequiresConfirmation
        | RequiresPaymentMethod
        | Succeeded

    [<Struct>]
    type SetupIntentCancellationReason =
        | Abandoned
        | Duplicate
        | RequestedByCustomer

    type SetupAttemptPaymentMethodDetailsUsBankAccount = {
        SetupAttemptPaymentMethodDetailsUsBankAccount: string option
    }
    with
        static member New (?setupAttemptPaymentMethodDetailsUsBankAccount: string option) =
            {
                SetupAttemptPaymentMethodDetailsUsBankAccount.SetupAttemptPaymentMethodDetailsUsBankAccount = setupAttemptPaymentMethodDetailsUsBankAccount |> Option.flatten
            }

    type SetupAttemptPaymentMethodDetailsUpi = {
        SetupAttemptPaymentMethodDetailsUpi: string option
    }
    with
        static member New (?setupAttemptPaymentMethodDetailsUpi: string option) =
            {
                SetupAttemptPaymentMethodDetailsUpi.SetupAttemptPaymentMethodDetailsUpi = setupAttemptPaymentMethodDetailsUpi |> Option.flatten
            }

    [<Struct>]
    type SetupAttemptPaymentMethodDetailsSofortPreferredLanguage =
        | De
        | En
        | Fr
        | Nl

    type SetupAttemptPaymentMethodDetailsSepaDebit = {
        SetupAttemptPaymentMethodDetailsSepaDebit: string option
    }
    with
        static member New (?setupAttemptPaymentMethodDetailsSepaDebit: string option) =
            {
                SetupAttemptPaymentMethodDetailsSepaDebit.SetupAttemptPaymentMethodDetailsSepaDebit = setupAttemptPaymentMethodDetailsSepaDebit |> Option.flatten
            }

    type SetupAttemptPaymentMethodDetailsRevolutPay = {
        SetupAttemptPaymentMethodDetailsRevolutPay: string option
    }
    with
        static member New (?setupAttemptPaymentMethodDetailsRevolutPay: string option) =
            {
                SetupAttemptPaymentMethodDetailsRevolutPay.SetupAttemptPaymentMethodDetailsRevolutPay = setupAttemptPaymentMethodDetailsRevolutPay |> Option.flatten
            }

    type SetupAttemptPaymentMethodDetailsPix = {
        SetupAttemptPaymentMethodDetailsPix: string option
    }
    with
        static member New (?setupAttemptPaymentMethodDetailsPix: string option) =
            {
                SetupAttemptPaymentMethodDetailsPix.SetupAttemptPaymentMethodDetailsPix = setupAttemptPaymentMethodDetailsPix |> Option.flatten
            }

    type SetupAttemptPaymentMethodDetailsPayto = {
        SetupAttemptPaymentMethodDetailsPayto: string option
    }
    with
        static member New (?setupAttemptPaymentMethodDetailsPayto: string option) =
            {
                SetupAttemptPaymentMethodDetailsPayto.SetupAttemptPaymentMethodDetailsPayto = setupAttemptPaymentMethodDetailsPayto |> Option.flatten
            }

    type SetupAttemptPaymentMethodDetailsPaypal = {
        SetupAttemptPaymentMethodDetailsPaypal: string option
    }
    with
        static member New (?setupAttemptPaymentMethodDetailsPaypal: string option) =
            {
                SetupAttemptPaymentMethodDetailsPaypal.SetupAttemptPaymentMethodDetailsPaypal = setupAttemptPaymentMethodDetailsPaypal |> Option.flatten
            }

    type SetupAttemptPaymentMethodDetailsNzBankAccount = {
        SetupAttemptPaymentMethodDetailsNzBankAccount: string option
    }
    with
        static member New (?setupAttemptPaymentMethodDetailsNzBankAccount: string option) =
            {
                SetupAttemptPaymentMethodDetailsNzBankAccount.SetupAttemptPaymentMethodDetailsNzBankAccount = setupAttemptPaymentMethodDetailsNzBankAccount |> Option.flatten
            }

    type SetupAttemptPaymentMethodDetailsNaverPay = {
        ///Uniquely identifies this particular Naver Pay account. You can use this attribute to check whether two Naver Pay accounts are the same.
        BuyerId: string option
    }
    with
        static member New (?buyerId: string) =
            {
                SetupAttemptPaymentMethodDetailsNaverPay.BuyerId = buyerId
            }

    type SetupAttemptPaymentMethodDetailsLink = {
        SetupAttemptPaymentMethodDetailsLink: string option
    }
    with
        static member New (?setupAttemptPaymentMethodDetailsLink: string option) =
            {
                SetupAttemptPaymentMethodDetailsLink.SetupAttemptPaymentMethodDetailsLink = setupAttemptPaymentMethodDetailsLink |> Option.flatten
            }

    type SetupAttemptPaymentMethodDetailsKrCard = {
        SetupAttemptPaymentMethodDetailsKrCard: string option
    }
    with
        static member New (?setupAttemptPaymentMethodDetailsKrCard: string option) =
            {
                SetupAttemptPaymentMethodDetailsKrCard.SetupAttemptPaymentMethodDetailsKrCard = setupAttemptPaymentMethodDetailsKrCard |> Option.flatten
            }

    type SetupAttemptPaymentMethodDetailsKlarna = {
        SetupAttemptPaymentMethodDetailsKlarna: string option
    }
    with
        static member New (?setupAttemptPaymentMethodDetailsKlarna: string option) =
            {
                SetupAttemptPaymentMethodDetailsKlarna.SetupAttemptPaymentMethodDetailsKlarna = setupAttemptPaymentMethodDetailsKlarna |> Option.flatten
            }

    type SetupAttemptPaymentMethodDetailsKakaoPay = {
        SetupAttemptPaymentMethodDetailsKakaoPay: string option
    }
    with
        static member New (?setupAttemptPaymentMethodDetailsKakaoPay: string option) =
            {
                SetupAttemptPaymentMethodDetailsKakaoPay.SetupAttemptPaymentMethodDetailsKakaoPay = setupAttemptPaymentMethodDetailsKakaoPay |> Option.flatten
            }

    type SetupAttemptPaymentMethodDetailsIdealBic =
        | [<JsonUnionCase("ABNANL2A")>] ABNANL2A
        | [<JsonUnionCase("ADYBNL2A")>] ADYBNL2A
        | [<JsonUnionCase("ASNBNL21")>] ASNBNL21
        | [<JsonUnionCase("BITSNL2A")>] BITSNL2A
        | [<JsonUnionCase("BUNQNL2A")>] BUNQNL2A
        | [<JsonUnionCase("BUUTNL2A")>] BUUTNL2A
        | [<JsonUnionCase("FNOMNL22")>] FNOMNL22
        | [<JsonUnionCase("FVLBNL22")>] FVLBNL22
        | [<JsonUnionCase("HANDNL2A")>] HANDNL2A
        | [<JsonUnionCase("INGBNL2A")>] INGBNL2A
        | [<JsonUnionCase("KNABNL2H")>] KNABNL2H
        | [<JsonUnionCase("MLLENL2A")>] MLLENL2A
        | [<JsonUnionCase("MOYONL21")>] MOYONL21
        | [<JsonUnionCase("NNBANL2G")>] NNBANL2G
        | [<JsonUnionCase("NTSBDEB1")>] NTSBDEB1
        | [<JsonUnionCase("RABONL2U")>] RABONL2U
        | [<JsonUnionCase("RBRBNL21")>] RBRBNL21
        | [<JsonUnionCase("REVOIE23")>] REVOIE23
        | [<JsonUnionCase("REVOLT21")>] REVOLT21
        | [<JsonUnionCase("SNSBNL2A")>] SNSBNL2A
        | [<JsonUnionCase("TRIONL2U")>] TRIONL2U

    type SetupAttemptPaymentMethodDetailsIdealBank =
        | AbnAmro
        | Adyen
        | AsnBank
        | Bunq
        | Buut
        | Finom
        | Handelsbanken
        | Ing
        | Knab
        | Mollie
        | Moneyou
        | N26
        | Nn
        | Rabobank
        | Regiobank
        | Revolut
        | SnsBank
        | TriodosBank
        | VanLanschot
        | Yoursafe

    type SetupAttemptPaymentMethodDetailsCashapp = {
        SetupAttemptPaymentMethodDetailsCashapp: string option
    }
    with
        static member New (?setupAttemptPaymentMethodDetailsCashapp: string option) =
            {
                SetupAttemptPaymentMethodDetailsCashapp.SetupAttemptPaymentMethodDetailsCashapp = setupAttemptPaymentMethodDetailsCashapp |> Option.flatten
            }

    [<Struct>]
    type SetupAttemptPaymentMethodDetailsCardWalletType =
        | ApplePay
        | GooglePay
        | Link

    [<Struct>]
    type SetupAttemptPaymentMethodDetailsCardChecksCvcCheck =
        | Pass
        | Fail
        | Unavailable
        | Unchecked

    [<Struct>]
    type SetupAttemptPaymentMethodDetailsCardChecksAddressPostalCodeCheck =
        | Pass
        | Fail
        | Unavailable
        | Unchecked

    [<Struct>]
    type SetupAttemptPaymentMethodDetailsCardChecksAddressLine1Check =
        | Pass
        | Fail
        | Unavailable
        | Unchecked

    type SetupAttemptPaymentMethodDetailsCardNetwork =
        | Amex
        | CartesBancaires
        | Diners
        | Discover
        | EftposAu
        | Interac
        | Jcb
        | Link
        | Mastercard
        | Unionpay
        | Visa
        | Unknown

    [<Struct>]
    type SetupAttemptPaymentMethodDetailsCardFunding =
        | Credit
        | Debit
        | Prepaid
        | Unknown

    type SetupAttemptPaymentMethodDetailsCardBrand =
        | Amex
        | CartesBancaires
        | Diners
        | Discover
        | EftposAu
        | Jcb
        | Link
        | Mastercard
        | Unionpay
        | Visa
        | Unknown

    type SetupAttemptPaymentMethodDetailsBoleto = {
        SetupAttemptPaymentMethodDetailsBoleto: string option
    }
    with
        static member New (?setupAttemptPaymentMethodDetailsBoleto: string option) =
            {
                SetupAttemptPaymentMethodDetailsBoleto.SetupAttemptPaymentMethodDetailsBoleto = setupAttemptPaymentMethodDetailsBoleto |> Option.flatten
            }

    [<Struct>]
    type SetupAttemptPaymentMethodDetailsBancontactPreferredLanguage =
        | De
        | En
        | Fr
        | Nl

    type SetupAttemptPaymentMethodDetailsBacsDebit = {
        SetupAttemptPaymentMethodDetailsBacsDebit: string option
    }
    with
        static member New (?setupAttemptPaymentMethodDetailsBacsDebit: string option) =
            {
                SetupAttemptPaymentMethodDetailsBacsDebit.SetupAttemptPaymentMethodDetailsBacsDebit = setupAttemptPaymentMethodDetailsBacsDebit |> Option.flatten
            }

    type SetupAttemptPaymentMethodDetailsAuBecsDebit = {
        SetupAttemptPaymentMethodDetailsAuBecsDebit: string option
    }
    with
        static member New (?setupAttemptPaymentMethodDetailsAuBecsDebit: string option) =
            {
                SetupAttemptPaymentMethodDetailsAuBecsDebit.SetupAttemptPaymentMethodDetailsAuBecsDebit = setupAttemptPaymentMethodDetailsAuBecsDebit |> Option.flatten
            }

    type SetupAttemptPaymentMethodDetailsAmazonPay = {
        SetupAttemptPaymentMethodDetailsAmazonPay: string option
    }
    with
        static member New (?setupAttemptPaymentMethodDetailsAmazonPay: string option) =
            {
                SetupAttemptPaymentMethodDetailsAmazonPay.SetupAttemptPaymentMethodDetailsAmazonPay = setupAttemptPaymentMethodDetailsAmazonPay |> Option.flatten
            }

    type SetupAttemptPaymentMethodDetailsAcssDebit = {
        SetupAttemptPaymentMethodDetailsAcssDebit: string option
    }
    with
        static member New (?setupAttemptPaymentMethodDetailsAcssDebit: string option) =
            {
                SetupAttemptPaymentMethodDetailsAcssDebit.SetupAttemptPaymentMethodDetailsAcssDebit = setupAttemptPaymentMethodDetailsAcssDebit |> Option.flatten
            }

    [<Struct>]
    type SetupAttemptFlowDirections =
        | Inbound
        | Outbound

    [<Struct>]
    type SetupAttemptUsage =
        | OffSession
        | OnSession

    type SetupAttemptStatus =
        | RequiresConfirmation
        | RequiresAction
        | Processing
        | Succeeded
        | Failed
        | Abandoned

    [<Struct>]
    type SecretServiceResourceScopeType =
        | Account
        | User

    type Rule = {
        ///The action taken on the payment.
        Action: string
        ///Unique identifier for the object.
        Id: string
        ///The predicate to evaluate the payment against.
        Predicate: string
    }
    with
        static member New (action: string, id: string, predicate: string) =
            {
                Rule.Action = action //required
                Rule.Id = id //required
                Rule.Predicate = predicate //required
            }

    type ReviewReason =
        | Rule
        | Manual
        | Approved
        | Refunded
        | RefundedAsFraud
        | Disputed
        | Redacted
        | Canceled
        | PaymentNeverSettled
        | Acknowledged

    [<Struct>]
    type ReviewOpenedReason =
        | Manual
        | Rule

    type ReviewClosedReason =
        | Acknowledged
        | Approved
        | Canceled
        | Disputed
        | PaymentNeverSettled
        | Redacted
        | Refunded
        | RefundedAsFraud

    [<Struct>]
    type ReservesReserveReleasesResourcesSourceTransactionType =
        | Dispute
        | Refund

    type ReservesReservePlansResourcesRollingRelease = {
        ///The number of days to reserve funds before releasing.
        DaysAfterCharge: int
        ///The time at which the ReservePlan expires.
        ExpiresOn: int option
    }
    with
        static member New (daysAfterCharge: int, expiresOn: int option) =
            {
                ReservesReservePlansResourcesRollingRelease.DaysAfterCharge = daysAfterCharge //required
                ReservesReservePlansResourcesRollingRelease.ExpiresOn = expiresOn //required
            }

    type ReservesReservePlansResourcesFixedRelease = {
        ///The time after which all reserved funds are requested for release.
        ReleaseAfter: int
        ///The time at which reserved funds are scheduled for release, automatically set to midnight UTC of the day after `release_after`.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ScheduledRelease: DateTime
    }
    with
        static member New (releaseAfter: int, scheduledRelease: DateTime) =
            {
                ReservesReservePlansResourcesFixedRelease.ReleaseAfter = releaseAfter //required
                ReservesReservePlansResourcesFixedRelease.ScheduledRelease = scheduledRelease //required
            }

    type ReservesReserveHoldsResourcesReleaseSchedule = {
        ///The time after which the ReserveHold is requested to be released.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ReleaseAfter: DateTime option
        ///The time at which the ReserveHold is scheduled to be released, automatically set to midnight UTC of the day after `release_after`.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ScheduledRelease: DateTime option
    }
    with
        static member New (releaseAfter: DateTime option, scheduledRelease: DateTime option) =
            {
                ReservesReserveHoldsResourcesReleaseSchedule.ReleaseAfter = releaseAfter //required
                ReservesReserveHoldsResourcesReleaseSchedule.ScheduledRelease = scheduledRelease //required
            }

    [<Struct>]
    type ReserveReleaseReason =
        | BulkHoldExpiry
        | HoldReleasedEarly
        | HoldReversed
        | PlanDisabled

    [<Struct>]
    type ReserveReleaseCreatedBy =
        | Application
        | Stripe

    [<Struct>]
    type ReservePlanType =
        | FixedRelease
        | RollingRelease

    [<Struct>]
    type ReservePlanStatus =
        | Active
        | Disabled
        | Expired

    [<Struct>]
    type ReservePlanCreatedBy =
        | Application
        | Stripe

    [<Struct>]
    type ReserveHoldSourceType =
        | BankAccount
        | Card
        | Fpx

    [<Struct>]
    type ReserveHoldReason =
        | Charge
        | Standalone

    [<Struct>]
    type ReserveHoldCreatedBy =
        | Application
        | Stripe

    [<Struct>]
    type RefundDestinationDetailsUsBankTransferReferenceStatus =
        | Pending
        | Available
        | Unavailable

    [<Struct>]
    type RefundDestinationDetailsThBankTransferReferenceStatus =
        | Pending
        | Available
        | Unavailable

    [<Struct>]
    type RefundDestinationDetailsSwishReferenceStatus =
        | Pending
        | Available
        | Unavailable

    type RefundDestinationDetailsPaypal = {
        ///For refunds declined by the network, a decline code provided by the network which indicates the reason the refund failed.
        NetworkDeclineCode: string option
    }
    with
        static member New (networkDeclineCode: string option) =
            {
                RefundDestinationDetailsPaypal.NetworkDeclineCode = networkDeclineCode //required
            }

    [<Struct>]
    type RefundDestinationDetailsP24ReferenceStatus =
        | Pending
        | Available
        | Unavailable

    [<Struct>]
    type RefundDestinationDetailsMxBankTransferReferenceStatus =
        | Pending
        | Available
        | Unavailable

    [<Struct>]
    type RefundDestinationDetailsMultibancoReferenceStatus =
        | Pending
        | Available
        | Unavailable

    [<Struct>]
    type RefundDestinationDetailsMbWayReferenceStatus =
        | Pending
        | Available
        | Unavailable

    [<Struct>]
    type RefundDestinationDetailsJpBankTransferReferenceStatus =
        | Pending
        | Available
        | Unavailable

    [<Struct>]
    type RefundDestinationDetailsGbBankTransferReferenceStatus =
        | Pending
        | Available
        | Unavailable

    [<Struct>]
    type RefundDestinationDetailsEuBankTransferReferenceStatus =
        | Pending
        | Available
        | Unavailable

    type RefundDestinationDetailsCrypto = {
        ///The transaction hash of the refund.
        Reference: string option
    }
    with
        static member New (reference: string option) =
            {
                RefundDestinationDetailsCrypto.Reference = reference //required
            }

    [<Struct>]
    type RefundDestinationDetailsCardType =
        | Pending
        | Refund
        | Reversal

    [<Struct>]
    type RefundDestinationDetailsCardReferenceStatus =
        | Pending
        | Available
        | Unavailable

    [<Struct>]
    type RefundDestinationDetailsBrBankTransferReferenceStatus =
        | Pending
        | Available
        | Unavailable

    [<Struct>]
    type RefundDestinationDetailsBlikReferenceStatus =
        | Pending
        | Available
        | Unavailable

    [<Struct>]
    type RefundStatus =
        | Pending
        | RequiresAction
        | Succeeded
        | Failed
        | Canceled

    [<Struct>]
    type RefundReason =
        | Duplicate
        | ExpiredUncapturedCharge
        | Fraudulent
        | RequestedByCustomer

    [<Struct>]
    type RefundPendingReason =
        | ChargePending
        | InsufficientFunds
        | Processing

    type RefundFailureReason =
        | LostOrStolenCard
        | ExpiredOrCanceledCard
        | ChargeForPendingRefundDisputed
        | InsufficientFunds
        | Declined
        | MerchantRequest
        | Unknown

    [<Struct>]
    type RecurringUsageType =
        | Licensed
        | Metered

    [<Struct>]
    type RecurringInterval =
        | Day
        | Month
        | Week
        | Year

    type ReceivedPaymentMethodDetailsFinancialAccount = {
        ///The FinancialAccount ID.
        Id: string
    }
    with
        ///The rails the ReceivedCredit was sent over. A FinancialAccount can only send funds over `stripe`.
        member _.Network = "stripe"

        static member New (id: string) =
            {
                ReceivedPaymentMethodDetailsFinancialAccount.Id = id //required
            }

    type RadarReviewResourceSession = {
        ///The browser used in this browser session (e.g., `Chrome`).
        Browser: string option
        ///Information about the device used for the browser session (e.g., `Samsung SM-G930T`).
        Device: string option
        ///The platform for the browser session (e.g., `Macintosh`).
        Platform: string option
        ///The version for the browser session (e.g., `61.0.3163.100`).
        Version: string option
    }
    with
        static member New (browser: string option, device: string option, platform: string option, version: string option) =
            {
                RadarReviewResourceSession.Browser = browser //required
                RadarReviewResourceSession.Device = device //required
                RadarReviewResourceSession.Platform = platform //required
                RadarReviewResourceSession.Version = version //required
            }

    ///Options to configure Radar. See [Radar Session](https://docs.stripe.com/radar/radar-session) for more information.
    type RadarRadarOptions = {
        ///A [Radar Session](https://docs.stripe.com/radar/radar-session) is a snapshot of the browser metadata and device details that help Radar make more accurate predictions on your payments.
        Session: string option
    }
    with
        static member New (?session: string) =
            {
                RadarRadarOptions.Session = session
            }

    ///Value list items allow you to add specific values to a given Radar value list, which can then be used in rules.
    ///Related guide: [Managing list items](https://docs.stripe.com/radar/lists#managing-list-items)
    type RadarValueListItem = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The name or email address of the user who added this item to the value list.
        CreatedBy: string
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The value of the item.
        Value: string
        ///The identifier of the value list this item belongs to.
        ValueList: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "radar.value_list_item"

        static member New (created: DateTime, createdBy: string, id: string, livemode: bool, value: string, valueList: string) =
            {
                RadarValueListItem.Created = created //required
                RadarValueListItem.CreatedBy = createdBy //required
                RadarValueListItem.Id = id //required
                RadarValueListItem.Livemode = livemode //required
                RadarValueListItem.Value = value //required
                RadarValueListItem.ValueList = valueList //required
            }

    type RadarValueListItemType =
        | Account
        | CardBin
        | CardFingerprint
        | CaseSensitiveString
        | Country
        | CryptoFingerprint
        | CustomerId
        | Email
        | IpAddress
        | SepaDebitFingerprint
        | String
        | UsBankAccountFingerprint

    [<Struct>]
    type RadarPaymentEvaluationRecommendedAction =
        | Block
        | Continue

    [<Struct>]
    type QuotesResourceSubscriptionDataBillingModeType =
        | Classic
        | Flexible

    type QuotesResourceStatusTransitions = {
        ///The time that the quote was accepted. Measured in seconds since Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]AcceptedAt: DateTime option
        ///The time that the quote was canceled. Measured in seconds since Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CanceledAt: DateTime option
        ///The time that the quote was finalized. Measured in seconds since Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]FinalizedAt: DateTime option
    }
    with
        static member New (acceptedAt: DateTime option, canceledAt: DateTime option, finalizedAt: DateTime option) =
            {
                QuotesResourceStatusTransitions.AcceptedAt = acceptedAt //required
                QuotesResourceStatusTransitions.CanceledAt = canceledAt //required
                QuotesResourceStatusTransitions.FinalizedAt = finalizedAt //required
            }

    [<Struct>]
    type QuotesResourceRecurringInterval =
        | Day
        | Month
        | Week
        | Year

    [<Struct>]
    type QuotesResourceAutomaticTaxStatus =
        | Complete
        | Failed
        | RequiresLocationInputs

    [<Struct>]
    type QuoteStatus =
        | Accepted
        | Canceled
        | Draft
        | Open

    [<Struct>]
    type QuoteCollectionMethod =
        | ChargeAutomatically
        | SendInvoice

    type PromotionCodeCurrencyOption = {
        ///Minimum amount required to redeem this Promotion Code into a Coupon (e.g., a purchase must be $100 or more to work).
        MinimumAmount: int
    }
    with
        static member New (minimumAmount: int) =
            {
                PromotionCodeCurrencyOption.MinimumAmount = minimumAmount //required
            }

    type ProductMarketingFeature = {
        ///The marketing feature name. Up to 80 characters long.
        Name: string option
    }
    with
        static member New (?name: string) =
            {
                ProductMarketingFeature.Name = name
            }

    [<Struct>]
    type ProductType =
        | Good
        | Service

    type PriceTier = {
        ///Price for the entire tier.
        FlatAmount: int option
        ///Same as `flat_amount`, but contains a decimal value with at most 12 decimal places.
        FlatAmountDecimal: string option
        ///Per unit price for units relevant to the tier.
        UnitAmount: int option
        ///Same as `unit_amount`, but contains a decimal value with at most 12 decimal places.
        UnitAmountDecimal: string option
        ///Up to and including to this quantity will be contained in the tier.
        UpTo: int option
    }
    with
        static member New (flatAmount: int option, flatAmountDecimal: string option, unitAmount: int option, unitAmountDecimal: string option, upTo: int option) =
            {
                PriceTier.FlatAmount = flatAmount //required
                PriceTier.FlatAmountDecimal = flatAmountDecimal //required
                PriceTier.UnitAmount = unitAmount //required
                PriceTier.UnitAmountDecimal = unitAmountDecimal //required
                PriceTier.UpTo = upTo //required
            }

    [<Struct>]
    type PriceType =
        | OneTime
        | Recurring

    [<Struct>]
    type PriceTiersMode =
        | Graduated
        | Volume

    [<Struct>]
    type PriceTaxBehavior =
        | Exclusive
        | Inclusive
        | Unspecified

    [<Struct>]
    type PriceBillingScheme =
        | PerUnit
        | Tiered

    type PortalSubscriptionUpdateProductAdjustableQuantity = {
        ///If true, the quantity can be adjusted to any non-negative integer.
        Enabled: bool
        ///The maximum quantity that can be set for the product.
        Maximum: int option
        ///The minimum quantity that can be set for the product.
        Minimum: int
    }
    with
        static member New (enabled: bool, maximum: int option, minimum: int) =
            {
                PortalSubscriptionUpdateProductAdjustableQuantity.Enabled = enabled //required
                PortalSubscriptionUpdateProductAdjustableQuantity.Maximum = maximum //required
                PortalSubscriptionUpdateProductAdjustableQuantity.Minimum = minimum //required
            }

    [<Struct>]
    type PortalSubscriptionUpdateDefaultAllowedUpdates =
        | Price
        | PromotionCode
        | Quantity

    [<Struct>]
    type PortalSubscriptionUpdateTrialUpdateBehavior =
        | ContinueTrial
        | EndTrial

    [<Struct>]
    type PortalSubscriptionUpdateProrationBehavior =
        | AlwaysInvoice
        | CreateProrations
        | [<JsonUnionCase("none")>] None'

    [<Struct>]
    type PortalSubscriptionUpdateBillingCycleAnchor =
        | Now
        | Unchanged

    type PortalSubscriptionCancellationReasonOptions =
        | CustomerService
        | LowQuality
        | MissingFeatures
        | Other
        | SwitchedService
        | TooComplex
        | TooExpensive
        | Unused

    [<Struct>]
    type PortalSubscriptionCancelProrationBehavior =
        | AlwaysInvoice
        | CreateProrations
        | [<JsonUnionCase("none")>] None'

    [<Struct>]
    type PortalSubscriptionCancelMode =
        | AtPeriodEnd
        | Immediately

    [<Struct>]
    type PortalResourceScheduleUpdateAtPeriodEndConditionType =
        | DecreasingItemAmount
        | ShorteningInterval

    type PortalPaymentMethodUpdate = {
        ///Whether the feature is enabled.
        Enabled: bool
        ///The [Payment Method Configuration](/api/payment_method_configurations) to use for this portal session. When specified, customers will be able to update their payment method to one of the options specified by the payment method configuration. If not set, the default payment method configuration is used.
        PaymentMethodConfiguration: string option
    }
    with
        static member New (enabled: bool, paymentMethodConfiguration: string option) =
            {
                PortalPaymentMethodUpdate.Enabled = enabled //required
                PortalPaymentMethodUpdate.PaymentMethodConfiguration = paymentMethodConfiguration //required
            }

    type PortalLoginPage = {
        ///If `true`, a shareable `url` will be generated that will take your customers to a hosted login page for the customer portal.
        ///If `false`, the previously generated `url`, if any, will be deactivated.
        Enabled: bool
        ///A shareable URL to the hosted portal login page. Your customers will be able to log in with their [email](https://docs.stripe.com/api/customers/object#customer_object-email) and receive a link to their customer portal.
        Url: string option
    }
    with
        static member New (enabled: bool, url: string option) =
            {
                PortalLoginPage.Enabled = enabled //required
                PortalLoginPage.Url = url //required
            }

    type PortalInvoiceList = {
        ///Whether the feature is enabled.
        Enabled: bool
    }
    with
        static member New (enabled: bool) =
            {
                PortalInvoiceList.Enabled = enabled //required
            }

    type PortalFlowsSubscriptionUpdateConfirmItem = {
        ///The ID of the [subscription item](https://docs.stripe.com/api/subscriptions/object#subscription_object-items-data-id) to be updated.
        Id: string option
        ///The price the customer should subscribe to through this flow. The price must also be included in the configuration's [`features.subscription_update.products`](https://docs.stripe.com/api/customer_portal/configuration#portal_configuration_object-features-subscription_update-products).
        Price: string option
        ///[Quantity](https://docs.stripe.com/subscriptions/quantities) for this item that the customer should subscribe to through this flow.
        Quantity: int option
    }
    with
        static member New (id: string option, price: string option, ?quantity: int) =
            {
                PortalFlowsSubscriptionUpdateConfirmItem.Id = id //required
                PortalFlowsSubscriptionUpdateConfirmItem.Price = price //required
                PortalFlowsSubscriptionUpdateConfirmItem.Quantity = quantity
            }

    type PortalFlowsSubscriptionUpdateConfirmDiscount = {
        ///The ID of the coupon to apply to this subscription update.
        Coupon: string option
        ///The ID of a promotion code to apply to this subscription update.
        PromotionCode: string option
    }
    with
        static member New (coupon: string option, promotionCode: string option) =
            {
                PortalFlowsSubscriptionUpdateConfirmDiscount.Coupon = coupon //required
                PortalFlowsSubscriptionUpdateConfirmDiscount.PromotionCode = promotionCode //required
            }

    type PortalFlowsFlowSubscriptionUpdate = {
        ///The ID of the subscription to be updated.
        Subscription: string
    }
    with
        static member New (subscription: string) =
            {
                PortalFlowsFlowSubscriptionUpdate.Subscription = subscription //required
            }

    [<Struct>]
    type PortalFlowsFlowAfterCompletionType =
        | HostedConfirmation
        | PortalHomepage
        | Redirect

    [<Struct>]
    type PortalFlowsFlowType =
        | PaymentMethodUpdate
        | SubscriptionCancel
        | SubscriptionUpdate
        | SubscriptionUpdateConfirm

    type PortalFlowsCouponOffer = {
        ///The ID of the coupon to be offered.
        Coupon: string
    }
    with
        static member New (coupon: string) =
            {
                PortalFlowsCouponOffer.Coupon = coupon //required
            }

    type PortalFlowsAfterCompletionRedirect = {
        ///The URL the customer will be redirected to after the flow is completed.
        ReturnUrl: string
    }
    with
        static member New (returnUrl: string) =
            {
                PortalFlowsAfterCompletionRedirect.ReturnUrl = returnUrl //required
            }

    type PortalFlowsAfterCompletionHostedConfirmation = {
        ///A custom message to display to the customer after the flow is completed.
        CustomMessage: string option
    }
    with
        static member New (customMessage: string option) =
            {
                PortalFlowsAfterCompletionHostedConfirmation.CustomMessage = customMessage //required
            }

    type PortalCustomerUpdateAllowedUpdates =
        | Address
        | Email
        | Name
        | Phone
        | Shipping
        | TaxId

    type PortalBusinessProfile = {
        ///The messaging shown to customers in the portal.
        Headline: string option
        ///A link to the business’s publicly available privacy policy.
        PrivacyPolicyUrl: string option
        ///A link to the business’s publicly available terms of service.
        TermsOfServiceUrl: string option
    }
    with
        static member New (headline: string option, privacyPolicyUrl: string option, termsOfServiceUrl: string option) =
            {
                PortalBusinessProfile.Headline = headline //required
                PortalBusinessProfile.PrivacyPolicyUrl = privacyPolicyUrl //required
                PortalBusinessProfile.TermsOfServiceUrl = termsOfServiceUrl //required
            }

    [<Struct>]
    type PlatformEarningFeeSourceType =
        | Charge
        | Payout

    type PlanTier = {
        ///Price for the entire tier.
        FlatAmount: int option
        ///Same as `flat_amount`, but contains a decimal value with at most 12 decimal places.
        FlatAmountDecimal: string option
        ///Per unit price for units relevant to the tier.
        UnitAmount: int option
        ///Same as `unit_amount`, but contains a decimal value with at most 12 decimal places.
        UnitAmountDecimal: string option
        ///Up to and including to this quantity will be contained in the tier.
        UpTo: int option
    }
    with
        static member New (flatAmount: int option, flatAmountDecimal: string option, unitAmount: int option, unitAmountDecimal: string option, upTo: int option) =
            {
                PlanTier.FlatAmount = flatAmount //required
                PlanTier.FlatAmountDecimal = flatAmountDecimal //required
                PlanTier.UnitAmount = unitAmount //required
                PlanTier.UnitAmountDecimal = unitAmountDecimal //required
                PlanTier.UpTo = upTo //required
            }

    [<Struct>]
    type PlanUsageType =
        | Licensed
        | Metered

    [<Struct>]
    type PlanTiersMode =
        | Graduated
        | Volume

    [<Struct>]
    type PlanInterval =
        | Day
        | Month
        | Week
        | Year

    [<Struct>]
    type PlanBillingScheme =
        | PerUnit
        | Tiered

    type PersonRelationship = {
        ///Whether the person is the authorizer of the account's representative.
        Authorizer: bool option
        ///Whether the person is a director of the account's legal entity. Directors are typically members of the governing board of the company, or responsible for ensuring the company meets its regulatory obligations.
        Director: bool option
        ///Whether the person has significant responsibility to control, manage, or direct the organization.
        Executive: bool option
        ///Whether the person is the legal guardian of the account's representative.
        LegalGuardian: bool option
        ///Whether the person is an owner of the account’s legal entity.
        Owner: bool option
        ///The percent owned by the person of the account's legal entity.
        PercentOwnership: decimal option
        ///Whether the person is authorized as the primary representative of the account. This is the person nominated by the business to provide information about themselves, and general information about the account. There can only be one representative at any given time. At the time the account is created, this person should be set to the person responsible for opening the account.
        Representative: bool option
        ///The person's title (e.g., CEO, Support Engineer).
        Title: string option
    }
    with
        static member New (authorizer: bool option, director: bool option, executive: bool option, legalGuardian: bool option, owner: bool option, percentOwnership: decimal option, representative: bool option, title: string option) =
            {
                PersonRelationship.Authorizer = authorizer //required
                PersonRelationship.Director = director //required
                PersonRelationship.Executive = executive //required
                PersonRelationship.LegalGuardian = legalGuardian //required
                PersonRelationship.Owner = owner //required
                PersonRelationship.PercentOwnership = percentOwnership //required
                PersonRelationship.Representative = representative //required
                PersonRelationship.Title = title //required
            }

    type PersonRaceDetailsRace =
        | AfricanAmerican
        | AmericanIndianOrAlaskaNative
        | Asian
        | AsianIndian
        | BlackOrAfricanAmerican
        | Chinese
        | Ethiopian
        | Filipino
        | GuamanianOrChamorro
        | Haitian
        | Jamaican
        | Japanese
        | Korean
        | NativeHawaiian
        | NativeHawaiianOrOtherPacificIslander
        | Nigerian
        | OtherAsian
        | OtherBlackOrAfricanAmerican
        | OtherPacificIslander
        | PreferNotToAnswer
        | Samoan
        | Somali
        | Vietnamese
        | White

    type PersonEthnicityDetailsEthnicity =
        | Cuban
        | HispanicOrLatino
        | Mexican
        | NotHispanicOrLatino
        | OtherHispanicOrLatino
        | PreferNotToAnswer
        | PuertoRican

    type PersonAdditionalTosAcceptance = {
        ///The Unix timestamp marking when the legal guardian accepted the service agreement.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Date: DateTime option
        ///The IP address from which the legal guardian accepted the service agreement.
        Ip: string option
        ///The user agent of the browser from which the legal guardian accepted the service agreement.
        UserAgent: string option
    }
    with
        static member New (date: DateTime option, ip: string option, userAgent: string option) =
            {
                PersonAdditionalTosAcceptance.Date = date //required
                PersonAdditionalTosAcceptance.Ip = ip //required
                PersonAdditionalTosAcceptance.UserAgent = userAgent //required
            }

    [<Struct>]
    type PersonPoliticalExposure =
        | Existing
        | [<JsonUnionCase("none")>] None'

    [<Struct>]
    type PaypalSellerProtectionDisputeCategories =
        | Fraudulent
        | ProductNotReceived

    [<Struct>]
    type PaypalSellerProtectionStatus =
        | Eligible
        | NotEligible
        | PartiallyEligible

    [<Struct>]
    type PayoutsTraceIdStatus =
        | Pending
        | Supported
        | Unsupported

    [<Struct>]
    type PayoutType =
        | BankAccount
        | Card

    [<Struct>]
    type PayoutStatus =
        | Paid
        | Pending
        | InTransit
        | Canceled
        | Failed

    [<Struct>]
    type PayoutSourceType =
        | Card
        | Fpx
        | BankAccount

    [<Struct>]
    type PayoutReconciliationStatus =
        | Completed
        | InProgress
        | NotApplicable

    [<Struct>]
    type PayoutMethod =
        | Standard
        | Instant

    ///Custom processors represent payment processors not modeled directly in
    ///the Stripe API. This resource consists of details about the custom processor
    ///used for this payment attempt.
    type PaymentsPrimitivesPaymentRecordsResourceProcessorDetailsResourceCustomDetails = {
        ///An opaque string for manual reconciliation of this payment, for example a check number or a payment processor ID.
        PaymentReference: string option
    }
    with
        static member New (paymentReference: string option) =
            {
                PaymentsPrimitivesPaymentRecordsResourceProcessorDetailsResourceCustomDetails.PaymentReference = paymentReference //required
            }

    [<Struct>]
    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodKonbiniDetailsResourceStoreChain =
        | Familymart
        | Lawson
        | Ministop
        | Seicomart

    ///Custom Payment Methods represent Payment Method types not modeled directly in
    ///the Stripe API. This resource consists of details about the custom payment method
    ///used for this payment attempt.
    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCustomDetails = {
        ///Display name for the custom (user-defined) payment method type used to make this payment.
        DisplayName: string
        ///The custom payment method type associated with this payment.
        Type: string option
    }
    with
        static member New (displayName: string, ``type``: string option) =
            {
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCustomDetails.DisplayName = displayName //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCustomDetails.Type = ``type`` //required
            }

    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWalletResourceGooglePay = {
        PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWalletResourceGooglePay: string option
    }
    with
        static member New (?paymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWalletResourceGooglePay: string option) =
            {
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWalletResourceGooglePay.PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWalletResourceGooglePay = paymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWalletResourceGooglePay |> Option.flatten
            }

    [<Struct>]
    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWalletResourceApplePayType =
        | ApplePay
        | ApplePayLater

    [<Struct>]
    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWalletType =
        | ApplePay
        | GooglePay

    [<Struct>]
    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecureVersion =
        | [<JsonUnionCase("1.0.2")>] Numeric102
        | [<JsonUnionCase("2.1.0")>] Numeric210
        | [<JsonUnionCase("2.2.0")>] Numeric220

    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecureResultReason =
        | Abandoned
        | Bypassed
        | Canceled
        | CardNotEnrolled
        | NetworkNotSupported
        | ProtocolError
        | Rejected

    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecureResult =
        | AttemptAcknowledged
        | Authenticated
        | Exempted
        | Failed
        | NotSupported
        | ProcessingError

    [<Struct>]
    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecureExemptionIndicator =
        | LowRisk
        | [<JsonUnionCase("none")>] None'

    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecureElectronicCommerceIndicator =
        | [<JsonUnionCase("01")>] Numeric01
        | [<JsonUnionCase("02")>] Numeric02
        | [<JsonUnionCase("03")>] Numeric03
        | [<JsonUnionCase("04")>] Numeric04
        | [<JsonUnionCase("05")>] Numeric05
        | [<JsonUnionCase("06")>] Numeric06
        | [<JsonUnionCase("07")>] Numeric07

    [<Struct>]
    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecureAuthenticationFlow =
        | Challenge
        | Frictionless

    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceNetworkToken = {
        ///Indicates if Stripe used a network token, either user provided or Stripe managed when processing the transaction.
        Used: bool
    }
    with
        static member New (used: bool) =
            {
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceNetworkToken.Used = used //required
            }

    [<Struct>]
    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceInstallmentPlanType =
        | Bonus
        | FixedCount
        | Revolving

    [<Struct>]
    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceChecksCvcCheck =
        | Fail
        | Pass
        | Unavailable
        | Unchecked

    [<Struct>]
    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceChecksAddressPostalCodeCheck =
        | Fail
        | Pass
        | Unavailable
        | Unchecked

    [<Struct>]
    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceChecksAddressLine1Check =
        | Fail
        | Pass
        | Unavailable
        | Unchecked

    [<Struct>]
    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsStoredCredentialUsage =
        | Recurring
        | Unscheduled

    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsNetwork =
        | Amex
        | CartesBancaires
        | Diners
        | Discover
        | EftposAu
        | Interac
        | Jcb
        | Link
        | Mastercard
        | Unionpay
        | Unknown
        | Visa

    [<Struct>]
    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsFunding =
        | Credit
        | Debit
        | Prepaid
        | Unknown

    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsBrand =
        | Amex
        | CartesBancaires
        | Diners
        | Discover
        | EftposAu
        | Interac
        | Jcb
        | Link
        | Mastercard
        | Unionpay
        | Unknown
        | Visa

    [<Struct>]
    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFundingResourceFundingCardFunding =
        | Credit
        | Debit
        | Prepaid
        | Unknown

    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFundingResourceFundingCardBrand =
        | Amex
        | CartesBancaires
        | Diners
        | Discover
        | EftposAu
        | Jcb
        | Link
        | Mastercard
        | Unionpay
        | Visa
        | Unknown

    type PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAlmaDetailsResourceInstallments = {
        ///The number of installments.
        Count: int option
    }
    with
        static member New (count: int option) =
            {
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAlmaDetailsResourceInstallments.Count = count //required
            }

    ///Information about the customer for this payment.
    type PaymentsPrimitivesPaymentRecordsResourceCustomerDetails = {
        ///ID of the Stripe Customer associated with this payment.
        Customer: string option
        ///The customer's email address.
        Email: string option
        ///The customer's name.
        Name: string option
        ///The customer's phone number.
        Phone: string option
    }
    with
        static member New (customer: string option, email: string option, name: string option, phone: string option) =
            {
                PaymentsPrimitivesPaymentRecordsResourceCustomerDetails.Customer = customer //required
                PaymentsPrimitivesPaymentRecordsResourceCustomerDetails.Email = email //required
                PaymentsPrimitivesPaymentRecordsResourceCustomerDetails.Name = name //required
                PaymentsPrimitivesPaymentRecordsResourceCustomerDetails.Phone = phone //required
            }

    [<Struct>]
    type PaymentRecordReportedBy =
        | Self
        | Stripe

    [<Struct>]
    type PaymentRecordCustomerPresence =
        | OffSession
        | OnSession

    [<Struct>]
    type PaymentPagesPrivateCardPaymentMethodOptionsResourceRestrictionsBrandsBlocked =
        | AmericanExpress
        | DiscoverGlobalNetwork
        | Mastercard
        | Visa

    [<Struct>]
    type PaymentPagesCheckoutSessionTaxIdCollectionRequired =
        | IfSupported
        | Never

    type PaymentPagesCheckoutSessionTaxIdType =
        | AdNrt
        | AeTrn
        | AlTin
        | AmTin
        | AoTin
        | ArCuit
        | AuAbn
        | AuArn
        | AwTin
        | AzTin
        | BaTin
        | BbTin
        | BdBin
        | BfIfu
        | BgUic
        | BhVat
        | BjIfu
        | BoTin
        | BrCnpj
        | BrCpf
        | BsTin
        | ByTin
        | CaBn
        | CaGstHst
        | CaPstBc
        | CaPstMb
        | CaPstSk
        | CaQst
        | CdNif
        | ChUid
        | ChVat
        | ClTin
        | CmNiu
        | CnTin
        | CoNit
        | CrTin
        | CvNif
        | DeStn
        | DoRcn
        | EcRuc
        | EgTin
        | EsCif
        | EtTin
        | EuOssVat
        | EuVat
        | FoVat
        | GbVat
        | GeVat
        | GiTin
        | GnNif
        | HkBr
        | HrOib
        | HuTin
        | IdNpwp
        | IlVat
        | InGst
        | IsVat
        | ItCf
        | JpCn
        | JpRn
        | JpTrn
        | KePin
        | KgTin
        | KhTin
        | KrBrn
        | KzBin
        | LaTin
        | LiUid
        | LiVat
        | LkVat
        | MaVat
        | MdVat
        | MePib
        | MkVat
        | MrNif
        | MxRfc
        | MyFrp
        | MyItn
        | MySst
        | NgTin
        | NoVat
        | NoVoec
        | NpPan
        | NzGst
        | OmVat
        | PeRuc
        | PhTin
        | PlNip
        | PyRuc
        | RoTin
        | RsPib
        | RuInn
        | RuKpp
        | SaVat
        | SgGst
        | SgUen
        | SiTin
        | SnNinea
        | SrFin
        | SvNit
        | ThVat
        | TjTin
        | TrTin
        | TwVat
        | TzVat
        | UaVat
        | UgTin
        | Unknown
        | UsEin
        | UyRuc
        | UzTin
        | UzVat
        | VeRif
        | VnTin
        | ZaVat
        | ZmTin
        | ZwTin

    type PaymentPagesCheckoutSessionShippingAddressCollectionAllowedCountries =
        | [<JsonUnionCase("AC")>] AC
        | [<JsonUnionCase("AD")>] AD
        | [<JsonUnionCase("AE")>] AE
        | [<JsonUnionCase("AF")>] AF
        | [<JsonUnionCase("AG")>] AG
        | [<JsonUnionCase("AI")>] AI
        | [<JsonUnionCase("AL")>] AL
        | [<JsonUnionCase("AM")>] AM
        | [<JsonUnionCase("AO")>] AO
        | [<JsonUnionCase("AQ")>] AQ
        | [<JsonUnionCase("AR")>] AR
        | [<JsonUnionCase("AT")>] AT
        | [<JsonUnionCase("AU")>] AU
        | [<JsonUnionCase("AW")>] AW
        | [<JsonUnionCase("AX")>] AX
        | [<JsonUnionCase("AZ")>] AZ
        | [<JsonUnionCase("BA")>] BA
        | [<JsonUnionCase("BB")>] BB
        | [<JsonUnionCase("BD")>] BD
        | [<JsonUnionCase("BE")>] BE
        | [<JsonUnionCase("BF")>] BF
        | [<JsonUnionCase("BG")>] BG
        | [<JsonUnionCase("BH")>] BH
        | [<JsonUnionCase("BI")>] BI
        | [<JsonUnionCase("BJ")>] BJ
        | [<JsonUnionCase("BL")>] BL
        | [<JsonUnionCase("BM")>] BM
        | [<JsonUnionCase("BN")>] BN
        | [<JsonUnionCase("BO")>] BO
        | [<JsonUnionCase("BQ")>] BQ
        | [<JsonUnionCase("BR")>] BR
        | [<JsonUnionCase("BS")>] BS
        | [<JsonUnionCase("BT")>] BT
        | [<JsonUnionCase("BV")>] BV
        | [<JsonUnionCase("BW")>] BW
        | [<JsonUnionCase("BY")>] BY
        | [<JsonUnionCase("BZ")>] BZ
        | [<JsonUnionCase("CA")>] CA
        | [<JsonUnionCase("CD")>] CD
        | [<JsonUnionCase("CF")>] CF
        | [<JsonUnionCase("CG")>] CG
        | [<JsonUnionCase("CH")>] CH
        | [<JsonUnionCase("CI")>] CI
        | [<JsonUnionCase("CK")>] CK
        | [<JsonUnionCase("CL")>] CL
        | [<JsonUnionCase("CM")>] CM
        | [<JsonUnionCase("CN")>] CN
        | [<JsonUnionCase("CO")>] CO
        | [<JsonUnionCase("CR")>] CR
        | [<JsonUnionCase("CV")>] CV
        | [<JsonUnionCase("CW")>] CW
        | [<JsonUnionCase("CY")>] CY
        | [<JsonUnionCase("CZ")>] CZ
        | [<JsonUnionCase("DE")>] DE
        | [<JsonUnionCase("DJ")>] DJ
        | [<JsonUnionCase("DK")>] DK
        | [<JsonUnionCase("DM")>] DM
        | [<JsonUnionCase("DO")>] DO
        | [<JsonUnionCase("DZ")>] DZ
        | [<JsonUnionCase("EC")>] EC
        | [<JsonUnionCase("EE")>] EE
        | [<JsonUnionCase("EG")>] EG
        | [<JsonUnionCase("EH")>] EH
        | [<JsonUnionCase("ER")>] ER
        | [<JsonUnionCase("ES")>] ES
        | [<JsonUnionCase("ET")>] ET
        | [<JsonUnionCase("FI")>] FI
        | [<JsonUnionCase("FJ")>] FJ
        | [<JsonUnionCase("FK")>] FK
        | [<JsonUnionCase("FO")>] FO
        | [<JsonUnionCase("FR")>] FR
        | [<JsonUnionCase("GA")>] GA
        | [<JsonUnionCase("GB")>] GB
        | [<JsonUnionCase("GD")>] GD
        | [<JsonUnionCase("GE")>] GE
        | [<JsonUnionCase("GF")>] GF
        | [<JsonUnionCase("GG")>] GG
        | [<JsonUnionCase("GH")>] GH
        | [<JsonUnionCase("GI")>] GI
        | [<JsonUnionCase("GL")>] GL
        | [<JsonUnionCase("GM")>] GM
        | [<JsonUnionCase("GN")>] GN
        | [<JsonUnionCase("GP")>] GP
        | [<JsonUnionCase("GQ")>] GQ
        | [<JsonUnionCase("GR")>] GR
        | [<JsonUnionCase("GS")>] GS
        | [<JsonUnionCase("GT")>] GT
        | [<JsonUnionCase("GU")>] GU
        | [<JsonUnionCase("GW")>] GW
        | [<JsonUnionCase("GY")>] GY
        | [<JsonUnionCase("HK")>] HK
        | [<JsonUnionCase("HN")>] HN
        | [<JsonUnionCase("HR")>] HR
        | [<JsonUnionCase("HT")>] HT
        | [<JsonUnionCase("HU")>] HU
        | [<JsonUnionCase("ID")>] ID
        | [<JsonUnionCase("IE")>] IE
        | [<JsonUnionCase("IL")>] IL
        | [<JsonUnionCase("IM")>] IM
        | [<JsonUnionCase("IN")>] IN
        | [<JsonUnionCase("IO")>] IO
        | [<JsonUnionCase("IQ")>] IQ
        | [<JsonUnionCase("IS")>] IS
        | [<JsonUnionCase("IT")>] IT
        | [<JsonUnionCase("JE")>] JE
        | [<JsonUnionCase("JM")>] JM
        | [<JsonUnionCase("JO")>] JO
        | [<JsonUnionCase("JP")>] JP
        | [<JsonUnionCase("KE")>] KE
        | [<JsonUnionCase("KG")>] KG
        | [<JsonUnionCase("KH")>] KH
        | [<JsonUnionCase("KI")>] KI
        | [<JsonUnionCase("KM")>] KM
        | [<JsonUnionCase("KN")>] KN
        | [<JsonUnionCase("KR")>] KR
        | [<JsonUnionCase("KW")>] KW
        | [<JsonUnionCase("KY")>] KY
        | [<JsonUnionCase("KZ")>] KZ
        | [<JsonUnionCase("LA")>] LA
        | [<JsonUnionCase("LB")>] LB
        | [<JsonUnionCase("LC")>] LC
        | [<JsonUnionCase("LI")>] LI
        | [<JsonUnionCase("LK")>] LK
        | [<JsonUnionCase("LR")>] LR
        | [<JsonUnionCase("LS")>] LS
        | [<JsonUnionCase("LT")>] LT
        | [<JsonUnionCase("LU")>] LU
        | [<JsonUnionCase("LV")>] LV
        | [<JsonUnionCase("LY")>] LY
        | [<JsonUnionCase("MA")>] MA
        | [<JsonUnionCase("MC")>] MC
        | [<JsonUnionCase("MD")>] MD
        | [<JsonUnionCase("ME")>] ME
        | [<JsonUnionCase("MF")>] MF
        | [<JsonUnionCase("MG")>] MG
        | [<JsonUnionCase("MK")>] MK
        | [<JsonUnionCase("ML")>] ML
        | [<JsonUnionCase("MM")>] MM
        | [<JsonUnionCase("MN")>] MN
        | [<JsonUnionCase("MO")>] MO
        | [<JsonUnionCase("MQ")>] MQ
        | [<JsonUnionCase("MR")>] MR
        | [<JsonUnionCase("MS")>] MS
        | [<JsonUnionCase("MT")>] MT
        | [<JsonUnionCase("MU")>] MU
        | [<JsonUnionCase("MV")>] MV
        | [<JsonUnionCase("MW")>] MW
        | [<JsonUnionCase("MX")>] MX
        | [<JsonUnionCase("MY")>] MY
        | [<JsonUnionCase("MZ")>] MZ
        | [<JsonUnionCase("NA")>] NA
        | [<JsonUnionCase("NC")>] NC
        | [<JsonUnionCase("NE")>] NE
        | [<JsonUnionCase("NG")>] NG
        | [<JsonUnionCase("NI")>] NI
        | [<JsonUnionCase("NL")>] NL
        | [<JsonUnionCase("NO")>] NO
        | [<JsonUnionCase("NP")>] NP
        | [<JsonUnionCase("NR")>] NR
        | [<JsonUnionCase("NU")>] NU
        | [<JsonUnionCase("NZ")>] NZ
        | [<JsonUnionCase("OM")>] OM
        | [<JsonUnionCase("PA")>] PA
        | [<JsonUnionCase("PE")>] PE
        | [<JsonUnionCase("PF")>] PF
        | [<JsonUnionCase("PG")>] PG
        | [<JsonUnionCase("PH")>] PH
        | [<JsonUnionCase("PK")>] PK
        | [<JsonUnionCase("PL")>] PL
        | [<JsonUnionCase("PM")>] PM
        | [<JsonUnionCase("PN")>] PN
        | [<JsonUnionCase("PR")>] PR
        | [<JsonUnionCase("PS")>] PS
        | [<JsonUnionCase("PT")>] PT
        | [<JsonUnionCase("PY")>] PY
        | [<JsonUnionCase("QA")>] QA
        | [<JsonUnionCase("RE")>] RE
        | [<JsonUnionCase("RO")>] RO
        | [<JsonUnionCase("RS")>] RS
        | [<JsonUnionCase("RU")>] RU
        | [<JsonUnionCase("RW")>] RW
        | [<JsonUnionCase("SA")>] SA
        | [<JsonUnionCase("SB")>] SB
        | [<JsonUnionCase("SC")>] SC
        | [<JsonUnionCase("SD")>] SD
        | [<JsonUnionCase("SE")>] SE
        | [<JsonUnionCase("SG")>] SG
        | [<JsonUnionCase("SH")>] SH
        | [<JsonUnionCase("SI")>] SI
        | [<JsonUnionCase("SJ")>] SJ
        | [<JsonUnionCase("SK")>] SK
        | [<JsonUnionCase("SL")>] SL
        | [<JsonUnionCase("SM")>] SM
        | [<JsonUnionCase("SN")>] SN
        | [<JsonUnionCase("SO")>] SO
        | [<JsonUnionCase("SR")>] SR
        | [<JsonUnionCase("SS")>] SS
        | [<JsonUnionCase("ST")>] ST
        | [<JsonUnionCase("SV")>] SV
        | [<JsonUnionCase("SX")>] SX
        | [<JsonUnionCase("SZ")>] SZ
        | [<JsonUnionCase("TA")>] TA
        | [<JsonUnionCase("TC")>] TC
        | [<JsonUnionCase("TD")>] TD
        | [<JsonUnionCase("TF")>] TF
        | [<JsonUnionCase("TG")>] TG
        | [<JsonUnionCase("TH")>] TH
        | [<JsonUnionCase("TJ")>] TJ
        | [<JsonUnionCase("TK")>] TK
        | [<JsonUnionCase("TL")>] TL
        | [<JsonUnionCase("TM")>] TM
        | [<JsonUnionCase("TN")>] TN
        | [<JsonUnionCase("TO")>] TO
        | [<JsonUnionCase("TR")>] TR
        | [<JsonUnionCase("TT")>] TT
        | [<JsonUnionCase("TV")>] TV
        | [<JsonUnionCase("TW")>] TW
        | [<JsonUnionCase("TZ")>] TZ
        | [<JsonUnionCase("UA")>] UA
        | [<JsonUnionCase("UG")>] UG
        | [<JsonUnionCase("US")>] US
        | [<JsonUnionCase("UY")>] UY
        | [<JsonUnionCase("UZ")>] UZ
        | [<JsonUnionCase("VA")>] VA
        | [<JsonUnionCase("VC")>] VC
        | [<JsonUnionCase("VE")>] VE
        | [<JsonUnionCase("VG")>] VG
        | [<JsonUnionCase("VN")>] VN
        | [<JsonUnionCase("VU")>] VU
        | [<JsonUnionCase("WF")>] WF
        | [<JsonUnionCase("WS")>] WS
        | [<JsonUnionCase("XK")>] XK
        | [<JsonUnionCase("YE")>] YE
        | [<JsonUnionCase("YT")>] YT
        | [<JsonUnionCase("ZA")>] ZA
        | [<JsonUnionCase("ZM")>] ZM
        | [<JsonUnionCase("ZW")>] ZW
        | [<JsonUnionCase("ZZ")>] ZZ

    [<Struct>]
    type PaymentPagesCheckoutSessionSavedPaymentMethodOptionsAllowRedisplayFilters =
        | Always
        | Limited
        | Unspecified

    [<Struct>]
    type PaymentPagesCheckoutSessionSavedPaymentMethodOptionsPaymentMethodSave =
        | Disabled
        | Enabled

    [<Struct>]
    type PaymentPagesCheckoutSessionSavedPaymentMethodOptionsPaymentMethodRemove =
        | Disabled
        | Enabled

    type PaymentPagesCheckoutSessionPhoneNumberCollection = {
        ///Indicates whether phone number collection is enabled for the session
        Enabled: bool
    }
    with
        static member New (enabled: bool) =
            {
                PaymentPagesCheckoutSessionPhoneNumberCollection.Enabled = enabled //required
            }

    [<Struct>]
    type PaymentPagesCheckoutSessionPermissionsUpdateShippingDetails =
        | ClientOnly
        | ServerOnly

    [<Struct>]
    type PaymentPagesCheckoutSessionPaymentMethodReuseAgreementPosition =
        | Auto
        | Hidden

    type PaymentPagesCheckoutSessionOptionalItemAdjustableQuantity = {
        ///Set to true if the quantity can be adjusted to any non-negative integer.
        Enabled: bool
        ///The maximum quantity of this item the customer can purchase. By default this value is 99. You can specify a value up to 999999.
        Maximum: int option
        ///The minimum quantity of this item the customer must purchase, if they choose to purchase it. Because this item is optional, the customer will always be able to remove it from their order, even if the `minimum` configured here is greater than 0. By default this value is 0.
        Minimum: int option
    }
    with
        static member New (enabled: bool, maximum: int option, minimum: int option) =
            {
                PaymentPagesCheckoutSessionOptionalItemAdjustableQuantity.Enabled = enabled //required
                PaymentPagesCheckoutSessionOptionalItemAdjustableQuantity.Maximum = maximum //required
                PaymentPagesCheckoutSessionOptionalItemAdjustableQuantity.Minimum = minimum //required
            }

    type PaymentPagesCheckoutSessionManagedPayments = {
        ///Set to `true` to enable [Managed Payments](https://docs.stripe.com/payments/managed-payments), Stripe's merchant of record solution, for this session.
        Enabled: bool
    }
    with
        static member New (enabled: bool) =
            {
                PaymentPagesCheckoutSessionManagedPayments.Enabled = enabled //required
            }

    type PaymentPagesCheckoutSessionIndividualName = {
        ///Indicates whether individual name collection is enabled for the session
        Enabled: bool
        ///Whether the customer is required to complete the field before completing the Checkout Session. Defaults to `false`.
        Optional: bool
    }
    with
        static member New (enabled: bool, optional: bool) =
            {
                PaymentPagesCheckoutSessionIndividualName.Enabled = enabled //required
                PaymentPagesCheckoutSessionIndividualName.Optional = optional //required
            }

    [<Struct>]
    type PaymentPagesCheckoutSessionCustomerDetailsTaxExempt =
        | Exempt
        | [<JsonUnionCase("none")>] None'
        | Reverse

    type PaymentPagesCheckoutSessionCustomTextPosition = {
        ///Text can be up to 1200 characters in length.
        Message: string
    }
    with
        static member New (message: string) =
            {
                PaymentPagesCheckoutSessionCustomTextPosition.Message = message //required
            }

    type PaymentPagesCheckoutSessionCustomFieldsText = {
        ///The value that pre-fills the field on the payment page.
        DefaultValue: string option
        ///The maximum character length constraint for the customer's input.
        MaximumLength: int option
        ///The minimum character length requirement for the customer's input.
        MinimumLength: int option
        ///The value entered by the customer.
        Value: string option
    }
    with
        static member New (defaultValue: string option, maximumLength: int option, minimumLength: int option, value: string option) =
            {
                PaymentPagesCheckoutSessionCustomFieldsText.DefaultValue = defaultValue //required
                PaymentPagesCheckoutSessionCustomFieldsText.MaximumLength = maximumLength //required
                PaymentPagesCheckoutSessionCustomFieldsText.MinimumLength = minimumLength //required
                PaymentPagesCheckoutSessionCustomFieldsText.Value = value //required
            }

    type PaymentPagesCheckoutSessionCustomFieldsOption = {
        ///The label for the option, displayed to the customer. Up to 100 characters.
        Label: string
        ///The value for this option, not displayed to the customer, used by your integration to reconcile the option selected by the customer. Must be unique to this option, alphanumeric, and up to 100 characters.
        Value: string
    }
    with
        static member New (label: string, value: string) =
            {
                PaymentPagesCheckoutSessionCustomFieldsOption.Label = label //required
                PaymentPagesCheckoutSessionCustomFieldsOption.Value = value //required
            }

    type PaymentPagesCheckoutSessionCustomFieldsNumeric = {
        ///The value that pre-fills the field on the payment page.
        DefaultValue: string option
        ///The maximum character length constraint for the customer's input.
        MaximumLength: int option
        ///The minimum character length requirement for the customer's input.
        MinimumLength: int option
        ///The value entered by the customer, containing only digits.
        Value: string option
    }
    with
        static member New (defaultValue: string option, maximumLength: int option, minimumLength: int option, value: string option) =
            {
                PaymentPagesCheckoutSessionCustomFieldsNumeric.DefaultValue = defaultValue //required
                PaymentPagesCheckoutSessionCustomFieldsNumeric.MaximumLength = maximumLength //required
                PaymentPagesCheckoutSessionCustomFieldsNumeric.MinimumLength = minimumLength //required
                PaymentPagesCheckoutSessionCustomFieldsNumeric.Value = value //required
            }

    type PaymentPagesCheckoutSessionCustomFieldsLabel = {
        ///Custom text for the label, displayed to the customer. Up to 50 characters.
        Custom: string option
    }
    with
        ///The type of the label.
        member _.Type = "custom"

        static member New (custom: string option) =
            {
                PaymentPagesCheckoutSessionCustomFieldsLabel.Custom = custom //required
            }

    [<Struct>]
    type PaymentPagesCheckoutSessionCustomFieldsType =
        | Dropdown
        | Numeric
        | Text

    [<Struct>]
    type PaymentPagesCheckoutSessionConsentCollectionTermsOfService =
        | [<JsonUnionCase("none")>] None'
        | Required

    [<Struct>]
    type PaymentPagesCheckoutSessionConsentCollectionPromotions =
        | Auto
        | [<JsonUnionCase("none")>] None'

    [<Struct>]
    type PaymentPagesCheckoutSessionConsentPromotions =
        | OptIn
        | OptOut

    type PaymentPagesCheckoutSessionBusinessName = {
        ///Indicates whether business name collection is enabled for the session
        Enabled: bool
        ///Whether the customer is required to complete the field before completing the Checkout Session. Defaults to `false`.
        Optional: bool
    }
    with
        static member New (enabled: bool, optional: bool) =
            {
                PaymentPagesCheckoutSessionBusinessName.Enabled = enabled //required
                PaymentPagesCheckoutSessionBusinessName.Optional = optional //required
            }

    [<Struct>]
    type PaymentPagesCheckoutSessionBrandingSettingsLogoType =
        | File
        | Url

    [<Struct>]
    type PaymentPagesCheckoutSessionBrandingSettingsIconType =
        | File
        | Url

    [<Struct>]
    type PaymentPagesCheckoutSessionBrandingSettingsBorderStyle =
        | Pill
        | Rectangular
        | Rounded

    [<Struct>]
    type PaymentPagesCheckoutSessionAutomaticTaxStatus =
        | Complete
        | Failed
        | RequiresLocationInputs

    type PaymentPagesCheckoutSessionAfterExpirationRecovery = {
        ///Enables user redeemable promotion codes on the recovered Checkout Sessions. Defaults to `false`
        AllowPromotionCodes: bool
        ///If `true`, a recovery url will be generated to recover this Checkout Session if it
        ///expires before a transaction is completed. It will be attached to the
        ///Checkout Session object upon expiration.
        Enabled: bool
        ///The timestamp at which the recovery URL will expire.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime option
        ///URL that creates a new Checkout Session when clicked that is a copy of this expired Checkout Session
        Url: string option
    }
    with
        static member New (allowPromotionCodes: bool, enabled: bool, expiresAt: DateTime option, url: string option) =
            {
                PaymentPagesCheckoutSessionAfterExpirationRecovery.AllowPromotionCodes = allowPromotionCodes //required
                PaymentPagesCheckoutSessionAfterExpirationRecovery.Enabled = enabled //required
                PaymentPagesCheckoutSessionAfterExpirationRecovery.ExpiresAt = expiresAt //required
                PaymentPagesCheckoutSessionAfterExpirationRecovery.Url = url //required
            }

    type PaymentPagesCheckoutSessionAdaptivePricing = {
        ///If enabled, Adaptive Pricing is available on [eligible sessions](https://docs.stripe.com/payments/currencies/localize-prices/adaptive-pricing?payment-ui=stripe-hosted#restrictions).
        Enabled: bool
    }
    with
        static member New (enabled: bool) =
            {
                PaymentPagesCheckoutSessionAdaptivePricing.Enabled = enabled //required
            }

    type PaymentMethodZip = {
        PaymentMethodZip: string option
    }
    with
        static member New (?paymentMethodZip: string option) =
            {
                PaymentMethodZip.PaymentMethodZip = paymentMethodZip |> Option.flatten
            }

    type PaymentMethodWechatPay = {
        PaymentMethodWechatPay: string option
    }
    with
        static member New (?paymentMethodWechatPay: string option) =
            {
                PaymentMethodWechatPay.PaymentMethodWechatPay = paymentMethodWechatPay |> Option.flatten
            }

    type PaymentMethodUsBankAccountBlockedReason =
        | BankAccountClosed
        | BankAccountFrozen
        | BankAccountInvalidDetails
        | BankAccountRestricted
        | BankAccountUnusable
        | DebitNotAuthorized
        | TokenizedAccountNumberDeactivated

    type PaymentMethodUsBankAccountBlockedNetworkCode =
        | [<JsonUnionCase("R02")>] R02
        | [<JsonUnionCase("R03")>] R03
        | [<JsonUnionCase("R04")>] R04
        | [<JsonUnionCase("R05")>] R05
        | [<JsonUnionCase("R07")>] R07
        | [<JsonUnionCase("R08")>] R08
        | [<JsonUnionCase("R10")>] R10
        | [<JsonUnionCase("R11")>] R11
        | [<JsonUnionCase("R16")>] R16
        | [<JsonUnionCase("R20")>] R20
        | [<JsonUnionCase("R29")>] R29
        | [<JsonUnionCase("R31")>] R31

    [<Struct>]
    type PaymentMethodUsBankAccountAccountType =
        | Checking
        | Savings

    [<Struct>]
    type PaymentMethodUsBankAccountAccountHolderType =
        | Company
        | Individual

    type PaymentMethodUpi = {
        ///Customer's unique Virtual Payment Address
        Vpa: string option
    }
    with
        static member New (vpa: string option) =
            {
                PaymentMethodUpi.Vpa = vpa //required
            }

    type PaymentMethodTwint = {
        PaymentMethodTwint: string option
    }
    with
        static member New (?paymentMethodTwint: string option) =
            {
                PaymentMethodTwint.PaymentMethodTwint = paymentMethodTwint |> Option.flatten
            }

    type PaymentMethodSwish = {
        PaymentMethodSwish: string option
    }
    with
        static member New (?paymentMethodSwish: string option) =
            {
                PaymentMethodSwish.PaymentMethodSwish = paymentMethodSwish |> Option.flatten
            }

    type PaymentMethodSunbit = {
        PaymentMethodSunbit: string option
    }
    with
        static member New (?paymentMethodSunbit: string option) =
            {
                PaymentMethodSunbit.PaymentMethodSunbit = paymentMethodSunbit |> Option.flatten
            }

    type PaymentMethodSatispay = {
        PaymentMethodSatispay: string option
    }
    with
        static member New (?paymentMethodSatispay: string option) =
            {
                PaymentMethodSatispay.PaymentMethodSatispay = paymentMethodSatispay |> Option.flatten
            }

    type PaymentMethodSamsungPay = {
        PaymentMethodSamsungPay: string option
    }
    with
        static member New (?paymentMethodSamsungPay: string option) =
            {
                PaymentMethodSamsungPay.PaymentMethodSamsungPay = paymentMethodSamsungPay |> Option.flatten
            }

    type PaymentMethodRevolutPay = {
        PaymentMethodRevolutPay: string option
    }
    with
        static member New (?paymentMethodRevolutPay: string option) =
            {
                PaymentMethodRevolutPay.PaymentMethodRevolutPay = paymentMethodRevolutPay |> Option.flatten
            }

    type PaymentMethodPromptpay = {
        PaymentMethodPromptpay: string option
    }
    with
        static member New (?paymentMethodPromptpay: string option) =
            {
                PaymentMethodPromptpay.PaymentMethodPromptpay = paymentMethodPromptpay |> Option.flatten
            }

    type PaymentMethodPix = {
        PaymentMethodPix: string option
    }
    with
        static member New (?paymentMethodPix: string option) =
            {
                PaymentMethodPix.PaymentMethodPix = paymentMethodPix |> Option.flatten
            }

    type PaymentMethodPayto = {
        ///Bank-State-Branch number of the bank account.
        BsbNumber: string option
        ///Last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string option
        ///The PayID alias for the bank account.
        PayId: string option
    }
    with
        static member New (bsbNumber: string option, last4: string option, payId: string option) =
            {
                PaymentMethodPayto.BsbNumber = bsbNumber //required
                PaymentMethodPayto.Last4 = last4 //required
                PaymentMethodPayto.PayId = payId //required
            }

    type PaymentMethodPaynow = {
        PaymentMethodPaynow: string option
    }
    with
        static member New (?paymentMethodPaynow: string option) =
            {
                PaymentMethodPaynow.PaymentMethodPaynow = paymentMethodPaynow |> Option.flatten
            }

    type PaymentMethodPayco = {
        PaymentMethodPayco: string option
    }
    with
        static member New (?paymentMethodPayco: string option) =
            {
                PaymentMethodPayco.PaymentMethodPayco = paymentMethodPayco |> Option.flatten
            }

    type PaymentMethodPayByBank = {
        PaymentMethodPayByBank: string option
    }
    with
        static member New (?paymentMethodPayByBank: string option) =
            {
                PaymentMethodPayByBank.PaymentMethodPayByBank = paymentMethodPayByBank |> Option.flatten
            }

    type PaymentMethodP24Bank =
        | AliorBank
        | BankMillennium
        | BankNowyBfgSa
        | BankPekaoSa
        | BankiSpbdzielcze
        | Blik
        | BnpParibas
        | Boz
        | CitiHandlowy
        | CreditAgricole
        | Envelobank
        | EtransferPocztowy24
        | GetinBank
        | Ideabank
        | Ing
        | Inteligo
        | MbankMtransfer
        | NestPrzelew
        | NoblePay
        | PbacZIpko
        | PlusBank
        | SantanderPrzelew24
        | TmobileUsbugiBankowe
        | ToyotaBank
        | Velobank
        | VolkswagenBank

    type PaymentMethodOxxo = {
        PaymentMethodOxxo: string option
    }
    with
        static member New (?paymentMethodOxxo: string option) =
            {
                PaymentMethodOxxo.PaymentMethodOxxo = paymentMethodOxxo |> Option.flatten
            }

    type PaymentMethodOptionsZip () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    [<Struct>]
    type PaymentMethodOptionsWechatPayClient =
        | Android
        | Ios
        | Web

    type PaymentMethodOptionsUsBankAccountMandateOptions () = 
        ///Mandate collection method
        member _.CollectionMethod = "paper"


    [<Struct>]
    type PaymentMethodOptionsUpiSetupFutureUsage =
        | OffSession
        | OnSession

    type PaymentMethodOptionsTwint () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    [<Struct>]
    type PaymentMethodOptionsSofortSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    type PaymentMethodOptionsSofortPreferredLanguage =
        | De
        | En
        | Es
        | Fr
        | It
        | Nl
        | Pl

    type PaymentMethodOptionsSatispay () = 
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"


    [<Struct>]
    type PaymentMethodOptionsRevolutPaySetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    type PaymentMethodOptionsPromptpay () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    [<Struct>]
    type PaymentMethodOptionsPixSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    [<Struct>]
    type PaymentMethodOptionsPixAmountIncludesIof =
        | Always
        | Never

    [<Struct>]
    type PaymentMethodOptionsPaypalSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    type PaymentMethodOptionsPaynow () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    type PaymentMethodOptionsPayByBank = {
        PaymentMethodOptionsPayByBank: string option
    }
    with
        static member New (?paymentMethodOptionsPayByBank: string option) =
            {
                PaymentMethodOptionsPayByBank.PaymentMethodOptionsPayByBank = paymentMethodOptionsPayByBank |> Option.flatten
            }

    type PaymentMethodOptionsP24 () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    type PaymentMethodOptionsOxxo = {
        ///The number of calendar days before an OXXO invoice expires. For example, if you create an OXXO invoice on Monday and you set expires_after_days to 2, the OXXO invoice will expire on Wednesday at 23:59 America/Mexico_City time.
        ExpiresAfterDays: int
    }
    with
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"

        static member New (expiresAfterDays: int) =
            {
                PaymentMethodOptionsOxxo.ExpiresAfterDays = expiresAfterDays //required
            }

    type PaymentMethodOptionsMultibanco () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    type PaymentMethodOptionsMbWay () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    [<Struct>]
    type PaymentMethodOptionsMandateOptionsUpiAmountType =
        | Fixed
        | Maximum

    [<Struct>]
    type PaymentMethodOptionsMandateOptionsPixPaymentSchedule =
        | Halfyearly
        | Monthly
        | Quarterly
        | Weekly
        | Yearly

    [<Struct>]
    type PaymentMethodOptionsMandateOptionsPixAmountType =
        | Fixed
        | Maximum

    [<Struct>]
    type PaymentMethodOptionsMandateOptionsPixAmountIncludesIof =
        | Always
        | Never

    [<Struct>]
    type PaymentMethodOptionsKrCardSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    type PaymentMethodOptionsKonbini = {
        ///An optional 10 to 11 digit numeric-only string determining the confirmation code at applicable convenience stores.
        ConfirmationNumber: string option
        ///The number of calendar days (between 1 and 60) after which Konbini payment instructions will expire. For example, if a PaymentIntent is confirmed with Konbini and `expires_after_days` set to 2 on Monday JST, the instructions will expire on Wednesday 23:59:59 JST.
        ExpiresAfterDays: int option
        ///The timestamp at which the Konbini payment instructions will expire. Only one of `expires_after_days` or `expires_at` may be set.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime option
        ///A product descriptor of up to 22 characters, which will appear to customers at the convenience store.
        ProductDescription: string option
    }
    with
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"

        static member New (confirmationNumber: string option, expiresAfterDays: int option, expiresAt: DateTime option, productDescription: string option) =
            {
                PaymentMethodOptionsKonbini.ConfirmationNumber = confirmationNumber //required
                PaymentMethodOptionsKonbini.ExpiresAfterDays = expiresAfterDays //required
                PaymentMethodOptionsKonbini.ExpiresAt = expiresAt //required
                PaymentMethodOptionsKonbini.ProductDescription = productDescription //required
            }

    [<Struct>]
    type PaymentMethodOptionsKlarnaSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    type PaymentMethodOptionsInteracPresent = {
        PaymentMethodOptionsInteracPresent: string option
    }
    with
        static member New (?paymentMethodOptionsInteracPresent: string option) =
            {
                PaymentMethodOptionsInteracPresent.PaymentMethodOptionsInteracPresent = paymentMethodOptionsInteracPresent |> Option.flatten
            }

    [<Struct>]
    type PaymentMethodOptionsIdealSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    type PaymentMethodOptionsGrabpay () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    type PaymentMethodOptionsGiropay () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    type PaymentMethodOptionsFpx () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    type PaymentMethodOptionsCustomerBalanceEuBankAccountCountry =
        | [<JsonUnionCase("BE")>] BE
        | [<JsonUnionCase("DE")>] DE
        | [<JsonUnionCase("ES")>] ES
        | [<JsonUnionCase("FR")>] FR
        | [<JsonUnionCase("IE")>] IE
        | [<JsonUnionCase("NL")>] NL

    type PaymentMethodOptionsCustomerBalanceBankTransferRequestedAddressTypes =
        | Aba
        | Iban
        | Sepa
        | SortCode
        | Spei
        | Swift
        | Zengin

    [<Struct>]
    type PaymentMethodOptionsCustomerBalanceBankTransferType =
        | EuBankTransfer
        | GbBankTransfer
        | JpBankTransfer
        | MxBankTransfer
        | UsBankTransfer

    type PaymentMethodOptionsCrypto () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    [<Struct>]
    type PaymentMethodOptionsCashappSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    [<Struct>]
    type PaymentMethodOptionsCardPresentRoutingRequestedPriority =
        | Domestic
        | International

    [<Struct>]
    type PaymentMethodOptionsCardPresentCaptureMethod =
        | Manual
        | ManualPreferred

    [<Struct>]
    type PaymentMethodOptionsCardMandateOptionsInterval =
        | Day
        | Month
        | Sporadic
        | Week
        | Year

    [<Struct>]
    type PaymentMethodOptionsCardMandateOptionsAmountType =
        | Fixed
        | Maximum

    [<Struct>]
    type PaymentMethodOptionsBoletoSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    type PaymentMethodOptionsBillie () = 
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"


    [<Struct>]
    type PaymentMethodOptionsBancontactSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    [<Struct>]
    type PaymentMethodOptionsBancontactPreferredLanguage =
        | De
        | En
        | Fr
        | Nl

    [<Struct>]
    type PaymentMethodOptionsAmazonPaySetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    type PaymentMethodOptionsAlma () = 
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"


    [<Struct>]
    type PaymentMethodOptionsAlipaySetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    type PaymentMethodOptionsAfterpayClearpay = {
        ///An internal identifier or reference that this payment corresponds to. You must limit the identifier to 128 characters, and it can only contain letters, numbers, underscores, backslashes, and dashes.
        ///This field differs from the statement descriptor and item name.
        Reference: string option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"

        static member New (reference: string option) =
            {
                PaymentMethodOptionsAfterpayClearpay.Reference = reference //required
            }

    type PaymentMethodOptionsAffirm = {
        ///Preferred language of the Affirm authorization page that the customer is redirected to.
        PreferredLocale: string option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"

        static member New (?preferredLocale: string) =
            {
                PaymentMethodOptionsAffirm.PreferredLocale = preferredLocale
            }

    type PaymentMethodNzBankAccount = {
        ///The name on the bank account. Only present if the account holder name is different from the name of the authorized signatory collected in the PaymentMethod’s billing details.
        AccountHolderName: string option
        ///The numeric code for the bank account's bank.
        BankCode: string
        ///The name of the bank.
        BankName: string
        ///The numeric code for the bank account's bank branch.
        BranchCode: string
        ///Last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string
        ///The suffix of the bank account number.
        Suffix: string option
    }
    with
        static member New (accountHolderName: string option, bankCode: string, bankName: string, branchCode: string, last4: string, suffix: string option) =
            {
                PaymentMethodNzBankAccount.AccountHolderName = accountHolderName //required
                PaymentMethodNzBankAccount.BankCode = bankCode //required
                PaymentMethodNzBankAccount.BankName = bankName //required
                PaymentMethodNzBankAccount.BranchCode = branchCode //required
                PaymentMethodNzBankAccount.Last4 = last4 //required
                PaymentMethodNzBankAccount.Suffix = suffix //required
            }

    [<Struct>]
    type PaymentMethodNaverPayFunding =
        | Card
        | Points

    type PaymentMethodMultibanco = {
        PaymentMethodMultibanco: string option
    }
    with
        static member New (?paymentMethodMultibanco: string option) =
            {
                PaymentMethodMultibanco.PaymentMethodMultibanco = paymentMethodMultibanco |> Option.flatten
            }

    type PaymentMethodMobilepay = {
        PaymentMethodMobilepay: string option
    }
    with
        static member New (?paymentMethodMobilepay: string option) =
            {
                PaymentMethodMobilepay.PaymentMethodMobilepay = paymentMethodMobilepay |> Option.flatten
            }

    type PaymentMethodMbWay = {
        PaymentMethodMbWay: string option
    }
    with
        static member New (?paymentMethodMbWay: string option) =
            {
                PaymentMethodMbWay.PaymentMethodMbWay = paymentMethodMbWay |> Option.flatten
            }

    type PaymentMethodLink = {
        ///Account owner's email address.
        Email: string option
        ///[Deprecated] This is a legacy parameter that no longer has any function.
        PersistentToken: string option
    }
    with
        static member New (email: string option, ?persistentToken: string) =
            {
                PaymentMethodLink.Email = email //required
                PaymentMethodLink.PersistentToken = persistentToken
            }

    type PaymentMethodKrCardBrand =
        | Bc
        | Citi
        | Hana
        | Hyundai
        | Jeju
        | Jeonbuk
        | Kakaobank
        | Kbank
        | Kdbbank
        | Kookmin
        | Kwangju
        | Lotte
        | Mg
        | Nh
        | Post
        | Samsung
        | Savingsbank
        | Shinhan
        | Shinhyup
        | Suhyup
        | Tossbank
        | Woori

    type PaymentMethodKonbini = {
        PaymentMethodKonbini: string option
    }
    with
        static member New (?paymentMethodKonbini: string option) =
            {
                PaymentMethodKonbini.PaymentMethodKonbini = paymentMethodKonbini |> Option.flatten
            }

    type PaymentMethodKakaoPay = {
        PaymentMethodKakaoPay: string option
    }
    with
        static member New (?paymentMethodKakaoPay: string option) =
            {
                PaymentMethodKakaoPay.PaymentMethodKakaoPay = paymentMethodKakaoPay |> Option.flatten
            }

    [<Struct>]
    type PaymentMethodInteracPresentReadMethod =
        | ContactEmv
        | ContactlessEmv
        | ContactlessMagstripeMode
        | MagneticStripeFallback
        | MagneticStripeTrack2

    [<Struct>]
    type PaymentMethodInteracPresentFunding =
        | Credit
        | Debit
        | Prepaid
        | Unknown

    [<Struct>]
    type PaymentMethodInteracPresentBrand =
        | Interac
        | Mastercard
        | Visa

    type PaymentMethodIdealBic =
        | [<JsonUnionCase("ABNANL2A")>] ABNANL2A
        | [<JsonUnionCase("ADYBNL2A")>] ADYBNL2A
        | [<JsonUnionCase("ASNBNL21")>] ASNBNL21
        | [<JsonUnionCase("BITSNL2A")>] BITSNL2A
        | [<JsonUnionCase("BUNQNL2A")>] BUNQNL2A
        | [<JsonUnionCase("BUUTNL2A")>] BUUTNL2A
        | [<JsonUnionCase("FNOMNL22")>] FNOMNL22
        | [<JsonUnionCase("FVLBNL22")>] FVLBNL22
        | [<JsonUnionCase("HANDNL2A")>] HANDNL2A
        | [<JsonUnionCase("INGBNL2A")>] INGBNL2A
        | [<JsonUnionCase("KNABNL2H")>] KNABNL2H
        | [<JsonUnionCase("MLLENL2A")>] MLLENL2A
        | [<JsonUnionCase("MOYONL21")>] MOYONL21
        | [<JsonUnionCase("NNBANL2G")>] NNBANL2G
        | [<JsonUnionCase("NTSBDEB1")>] NTSBDEB1
        | [<JsonUnionCase("RABONL2U")>] RABONL2U
        | [<JsonUnionCase("RBRBNL21")>] RBRBNL21
        | [<JsonUnionCase("REVOIE23")>] REVOIE23
        | [<JsonUnionCase("REVOLT21")>] REVOLT21
        | [<JsonUnionCase("SNSBNL2A")>] SNSBNL2A
        | [<JsonUnionCase("TRIONL2U")>] TRIONL2U

    type PaymentMethodIdealBank =
        | AbnAmro
        | Adyen
        | AsnBank
        | Bunq
        | Buut
        | Finom
        | Handelsbanken
        | Ing
        | Knab
        | Mollie
        | Moneyou
        | N26
        | Nn
        | Rabobank
        | Regiobank
        | Revolut
        | SnsBank
        | TriodosBank
        | VanLanschot
        | Yoursafe

    type PaymentMethodGrabpay = {
        PaymentMethodGrabpay: string option
    }
    with
        static member New (?paymentMethodGrabpay: string option) =
            {
                PaymentMethodGrabpay.PaymentMethodGrabpay = paymentMethodGrabpay |> Option.flatten
            }

    type PaymentMethodGiropay = {
        PaymentMethodGiropay: string option
    }
    with
        static member New (?paymentMethodGiropay: string option) =
            {
                PaymentMethodGiropay.PaymentMethodGiropay = paymentMethodGiropay |> Option.flatten
            }

    type PaymentMethodFpxBank =
        | AffinBank
        | Agrobank
        | AllianceBank
        | Ambank
        | BankIslam
        | BankMuamalat
        | BankOfChina
        | BankRakyat
        | Bsn
        | Cimb
        | DeutscheBank
        | HongLeongBank
        | Hsbc
        | Kfh
        | Maybank2e
        | Maybank2u
        | Ocbc
        | PbEnterprise
        | PublicBank
        | Rhb
        | StandardChartered
        | Uob

    [<Struct>]
    type PaymentMethodFpxAccountHolderType =
        | Company
        | Individual

    type PaymentMethodEpsBank =
        | ArzteUndApothekerBank
        | AustrianAnadiBankAg
        | BankAustria
        | BankhausCarlSpangler
        | BankhausSchelhammerUndSchatteraAg
        | BawagPskAg
        | BksBankAg
        | BrullKallmusBankAg
        | BtvVierLanderBank
        | CapitalBankGraweGruppeAg
        | DeutscheBankAg
        | Dolomitenbank
        | EasybankAg
        | ErsteBankUndSparkassen
        | HypoAlpeadriabankInternationalAg
        | HypoBankBurgenlandAktiengesellschaft
        | HypoNoeLbFurNiederosterreichUWien
        | HypoOberosterreichSalzburgSteiermark
        | HypoTirolBankAg
        | HypoVorarlbergBankAg
        | MarchfelderBank
        | OberbankAg
        | RaiffeisenBankengruppeOsterreich
        | SchoellerbankAg
        | SpardaBankWien
        | VolksbankGruppe
        | VolkskreditbankAg
        | VrBankBraunau

    ///Contains additional details about the status of a payment method for a specific payment method domain.
    type PaymentMethodDomainResourcePaymentMethodStatusDetails = {
        ///The error message associated with the status of the payment method on the domain.
        ErrorMessage: string
    }
    with
        static member New (errorMessage: string) =
            {
                PaymentMethodDomainResourcePaymentMethodStatusDetails.ErrorMessage = errorMessage //required
            }

    [<Struct>]
    type PaymentMethodDomainResourcePaymentMethodStatusStatus =
        | Active
        | Inactive

    type PaymentMethodDetailsZip = {
        PaymentMethodDetailsZip: string option
    }
    with
        static member New (?paymentMethodDetailsZip: string option) =
            {
                PaymentMethodDetailsZip.PaymentMethodDetailsZip = paymentMethodDetailsZip |> Option.flatten
            }

    type PaymentMethodDetailsWechatPay = {
        ///Uniquely identifies this particular WeChat Pay account. You can use this attribute to check whether two WeChat accounts are the same.
        Fingerprint: string option
        ///ID of the [location](https://docs.stripe.com/api/terminal/locations) that this transaction's reader is assigned to.
        Location: string option
        ///ID of the [reader](https://docs.stripe.com/api/terminal/readers) this transaction was made on.
        Reader: string option
        ///Transaction ID of this particular WeChat Pay transaction.
        TransactionId: string option
    }
    with
        static member New (fingerprint: string option, transactionId: string option, ?location: string, ?reader: string) =
            {
                PaymentMethodDetailsWechatPay.Fingerprint = fingerprint //required
                PaymentMethodDetailsWechatPay.TransactionId = transactionId //required
                PaymentMethodDetailsWechatPay.Location = location
                PaymentMethodDetailsWechatPay.Reader = reader
            }

    type PaymentMethodDetailsWechat = {
        PaymentMethodDetailsWechat: string option
    }
    with
        static member New (?paymentMethodDetailsWechat: string option) =
            {
                PaymentMethodDetailsWechat.PaymentMethodDetailsWechat = paymentMethodDetailsWechat |> Option.flatten
            }

    [<Struct>]
    type PaymentMethodDetailsUsBankAccountAccountType =
        | Checking
        | Savings

    [<Struct>]
    type PaymentMethodDetailsUsBankAccountAccountHolderType =
        | Company
        | Individual

    type PaymentMethodDetailsUpi = {
        ///Customer's unique Virtual Payment Address.
        Vpa: string option
    }
    with
        static member New (vpa: string option) =
            {
                PaymentMethodDetailsUpi.Vpa = vpa //required
            }

    type PaymentMethodDetailsTwint = {
        PaymentMethodDetailsTwint: string option
    }
    with
        static member New (?paymentMethodDetailsTwint: string option) =
            {
                PaymentMethodDetailsTwint.PaymentMethodDetailsTwint = paymentMethodDetailsTwint |> Option.flatten
            }

    type PaymentMethodDetailsSwish = {
        ///Uniquely identifies the payer's Swish account. You can use this attribute to check whether two Swish transactions were paid for by the same payer
        Fingerprint: string option
        ///Payer bank reference number for the payment
        PaymentReference: string option
        ///The last four digits of the Swish account phone number
        [<JsonField(Name="verified_phone_last4")>]VerifiedPhoneLast4: string option
    }
    with
        static member New (fingerprint: string option, paymentReference: string option, verifiedPhoneLast4: string option) =
            {
                PaymentMethodDetailsSwish.Fingerprint = fingerprint //required
                PaymentMethodDetailsSwish.PaymentReference = paymentReference //required
                PaymentMethodDetailsSwish.VerifiedPhoneLast4 = verifiedPhoneLast4 //required
            }

    type PaymentMethodDetailsSunbit = {
        ///The Sunbit transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (transactionId: string option) =
            {
                PaymentMethodDetailsSunbit.TransactionId = transactionId //required
            }

    type PaymentMethodDetailsStripeAccount = {
        PaymentMethodDetailsStripeAccount: string option
    }
    with
        static member New (?paymentMethodDetailsStripeAccount: string option) =
            {
                PaymentMethodDetailsStripeAccount.PaymentMethodDetailsStripeAccount = paymentMethodDetailsStripeAccount |> Option.flatten
            }

    type PaymentMethodDetailsSofortPreferredLanguage =
        | De
        | En
        | Es
        | Fr
        | It
        | Nl
        | Pl

    type PaymentMethodDetailsSepaCreditTransfer = {
        ///Name of the bank associated with the bank account.
        BankName: string option
        ///Bank Identifier Code of the bank associated with the bank account.
        Bic: string option
        ///IBAN of the bank account to transfer funds to.
        Iban: string option
    }
    with
        static member New (bankName: string option, bic: string option, iban: string option) =
            {
                PaymentMethodDetailsSepaCreditTransfer.BankName = bankName //required
                PaymentMethodDetailsSepaCreditTransfer.Bic = bic //required
                PaymentMethodDetailsSepaCreditTransfer.Iban = iban //required
            }

    type PaymentMethodDetailsSatispay = {
        ///The Satispay transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (transactionId: string option) =
            {
                PaymentMethodDetailsSatispay.TransactionId = transactionId //required
            }

    type PaymentMethodDetailsSamsungPay = {
        ///A unique identifier for the buyer as determined by the local payment processor.
        BuyerId: string option
        ///The Samsung Pay transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (buyerId: string option, transactionId: string option) =
            {
                PaymentMethodDetailsSamsungPay.BuyerId = buyerId //required
                PaymentMethodDetailsSamsungPay.TransactionId = transactionId //required
            }

    type PaymentMethodDetailsPromptpay = {
        ///Bill reference generated by PromptPay
        Reference: string option
    }
    with
        static member New (reference: string option) =
            {
                PaymentMethodDetailsPromptpay.Reference = reference //required
            }

    type PaymentMethodDetailsPix = {
        ///Unique transaction id generated by BCB
        BankTransactionId: string option
        ///ID of the multi use Mandate generated by the PaymentIntent
        Mandate: string option
    }
    with
        static member New (?bankTransactionId: string option, ?mandate: string) =
            {
                PaymentMethodDetailsPix.BankTransactionId = bankTransactionId |> Option.flatten
                PaymentMethodDetailsPix.Mandate = mandate
            }

    type PaymentMethodDetailsPayto = {
        ///Bank-State-Branch number of the bank account.
        BsbNumber: string option
        ///Last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string option
        ///ID of the mandate used to make this payment.
        Mandate: string option
        ///The PayID alias for the bank account.
        PayId: string option
    }
    with
        static member New (bsbNumber: string option, last4: string option, payId: string option, ?mandate: string) =
            {
                PaymentMethodDetailsPayto.BsbNumber = bsbNumber //required
                PaymentMethodDetailsPayto.Last4 = last4 //required
                PaymentMethodDetailsPayto.PayId = payId //required
                PaymentMethodDetailsPayto.Mandate = mandate
            }

    type PaymentMethodDetailsPaynow = {
        ///ID of the [location](https://docs.stripe.com/api/terminal/locations) that this transaction's reader is assigned to.
        Location: string option
        ///ID of the [reader](https://docs.stripe.com/api/terminal/readers) this transaction was made on.
        Reader: string option
        ///Reference number associated with this PayNow payment
        Reference: string option
    }
    with
        static member New (reference: string option, ?location: string, ?reader: string) =
            {
                PaymentMethodDetailsPaynow.Reference = reference //required
                PaymentMethodDetailsPaynow.Location = location
                PaymentMethodDetailsPaynow.Reader = reader
            }

    type PaymentMethodDetailsPaymentRecordZip = {
        PaymentMethodDetailsPaymentRecordZip: string option
    }
    with
        static member New (?paymentMethodDetailsPaymentRecordZip: string option) =
            {
                PaymentMethodDetailsPaymentRecordZip.PaymentMethodDetailsPaymentRecordZip = paymentMethodDetailsPaymentRecordZip |> Option.flatten
            }

    type PaymentMethodDetailsPaymentRecordWechatPay = {
        ///Uniquely identifies this particular WeChat Pay account. You can use this attribute to check whether two WeChat accounts are the same.
        Fingerprint: string option
        ///ID of the [location](https://docs.stripe.com/api/terminal/locations) that this transaction's reader is assigned to.
        Location: string option
        ///ID of the [reader](https://docs.stripe.com/api/terminal/readers) this transaction was made on.
        Reader: string option
        ///Transaction ID of this particular WeChat Pay transaction.
        TransactionId: string option
    }
    with
        static member New (fingerprint: string option, transactionId: string option, ?location: string, ?reader: string) =
            {
                PaymentMethodDetailsPaymentRecordWechatPay.Fingerprint = fingerprint //required
                PaymentMethodDetailsPaymentRecordWechatPay.TransactionId = transactionId //required
                PaymentMethodDetailsPaymentRecordWechatPay.Location = location
                PaymentMethodDetailsPaymentRecordWechatPay.Reader = reader
            }

    [<Struct>]
    type PaymentMethodDetailsPaymentRecordUsBankAccountAccountType =
        | Checking
        | Savings

    [<Struct>]
    type PaymentMethodDetailsPaymentRecordUsBankAccountAccountHolderType =
        | Company
        | Individual

    type PaymentMethodDetailsPaymentRecordUpi = {
        ///Customer's unique Virtual Payment Address.
        Vpa: string option
    }
    with
        static member New (vpa: string option) =
            {
                PaymentMethodDetailsPaymentRecordUpi.Vpa = vpa //required
            }

    type PaymentMethodDetailsPaymentRecordTwint = {
        PaymentMethodDetailsPaymentRecordTwint: string option
    }
    with
        static member New (?paymentMethodDetailsPaymentRecordTwint: string option) =
            {
                PaymentMethodDetailsPaymentRecordTwint.PaymentMethodDetailsPaymentRecordTwint = paymentMethodDetailsPaymentRecordTwint |> Option.flatten
            }

    type PaymentMethodDetailsPaymentRecordSwish = {
        ///Uniquely identifies the payer's Swish account. You can use this attribute to check whether two Swish transactions were paid for by the same payer
        Fingerprint: string option
        ///Payer bank reference number for the payment
        PaymentReference: string option
        ///The last four digits of the Swish account phone number
        [<JsonField(Name="verified_phone_last4")>]VerifiedPhoneLast4: string option
    }
    with
        static member New (fingerprint: string option, paymentReference: string option, verifiedPhoneLast4: string option) =
            {
                PaymentMethodDetailsPaymentRecordSwish.Fingerprint = fingerprint //required
                PaymentMethodDetailsPaymentRecordSwish.PaymentReference = paymentReference //required
                PaymentMethodDetailsPaymentRecordSwish.VerifiedPhoneLast4 = verifiedPhoneLast4 //required
            }

    type PaymentMethodDetailsPaymentRecordSofortPreferredLanguage =
        | De
        | En
        | Es
        | Fr
        | It
        | Nl
        | Pl

    type PaymentMethodDetailsPaymentRecordSamsungPay = {
        ///A unique identifier for the buyer as determined by the local payment processor.
        BuyerId: string option
        ///The Samsung Pay transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (buyerId: string option, transactionId: string option) =
            {
                PaymentMethodDetailsPaymentRecordSamsungPay.BuyerId = buyerId //required
                PaymentMethodDetailsPaymentRecordSamsungPay.TransactionId = transactionId //required
            }

    type PaymentMethodDetailsPaymentRecordPromptpay = {
        ///Bill reference generated by PromptPay
        Reference: string option
    }
    with
        static member New (reference: string option) =
            {
                PaymentMethodDetailsPaymentRecordPromptpay.Reference = reference //required
            }

    type PaymentMethodDetailsPaymentRecordPix = {
        ///Unique transaction id generated by BCB
        BankTransactionId: string option
        ///ID of the multi use Mandate generated by the PaymentIntent
        Mandate: string option
    }
    with
        static member New (?bankTransactionId: string option, ?mandate: string) =
            {
                PaymentMethodDetailsPaymentRecordPix.BankTransactionId = bankTransactionId |> Option.flatten
                PaymentMethodDetailsPaymentRecordPix.Mandate = mandate
            }

    type PaymentMethodDetailsPaymentRecordPaynow = {
        ///ID of the [location](https://docs.stripe.com/api/terminal/locations) that this transaction's reader is assigned to.
        Location: string option
        ///ID of the [reader](https://docs.stripe.com/api/terminal/readers) this transaction was made on.
        Reader: string option
        ///Reference number associated with this PayNow payment
        Reference: string option
    }
    with
        static member New (reference: string option, ?location: string, ?reader: string) =
            {
                PaymentMethodDetailsPaymentRecordPaynow.Reference = reference //required
                PaymentMethodDetailsPaymentRecordPaynow.Location = location
                PaymentMethodDetailsPaymentRecordPaynow.Reader = reader
            }

    type PaymentMethodDetailsPaymentRecordPayco = {
        ///A unique identifier for the buyer as determined by the local payment processor.
        BuyerId: string option
        ///The Payco transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (buyerId: string option, transactionId: string option) =
            {
                PaymentMethodDetailsPaymentRecordPayco.BuyerId = buyerId //required
                PaymentMethodDetailsPaymentRecordPayco.TransactionId = transactionId //required
            }

    type PaymentMethodDetailsPaymentRecordPayByBank = {
        PaymentMethodDetailsPaymentRecordPayByBank: string option
    }
    with
        static member New (?paymentMethodDetailsPaymentRecordPayByBank: string option) =
            {
                PaymentMethodDetailsPaymentRecordPayByBank.PaymentMethodDetailsPaymentRecordPayByBank = paymentMethodDetailsPaymentRecordPayByBank |> Option.flatten
            }

    type PaymentMethodDetailsPaymentRecordOxxo = {
        ///OXXO reference number
        Number: string option
    }
    with
        static member New (number: string option) =
            {
                PaymentMethodDetailsPaymentRecordOxxo.Number = number //required
            }

    type PaymentMethodDetailsPaymentRecordNaverPay = {
        ///A unique identifier for the buyer as determined by the local payment processor.
        BuyerId: string option
        ///The Naver Pay transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (buyerId: string option, transactionId: string option) =
            {
                PaymentMethodDetailsPaymentRecordNaverPay.BuyerId = buyerId //required
                PaymentMethodDetailsPaymentRecordNaverPay.TransactionId = transactionId //required
            }

    type PaymentMethodDetailsPaymentRecordMultibanco = {
        ///Entity number associated with this Multibanco payment.
        Entity: string option
        ///Reference number associated with this Multibanco payment.
        Reference: string option
    }
    with
        static member New (entity: string option, reference: string option) =
            {
                PaymentMethodDetailsPaymentRecordMultibanco.Entity = entity //required
                PaymentMethodDetailsPaymentRecordMultibanco.Reference = reference //required
            }

    type PaymentMethodDetailsPaymentRecordMbWay = {
        PaymentMethodDetailsPaymentRecordMbWay: string option
    }
    with
        static member New (?paymentMethodDetailsPaymentRecordMbWay: string option) =
            {
                PaymentMethodDetailsPaymentRecordMbWay.PaymentMethodDetailsPaymentRecordMbWay = paymentMethodDetailsPaymentRecordMbWay |> Option.flatten
            }

    type PaymentMethodDetailsPaymentRecordKakaoPay = {
        ///A unique identifier for the buyer as determined by the local payment processor.
        BuyerId: string option
        ///The Kakao Pay transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (buyerId: string option, transactionId: string option) =
            {
                PaymentMethodDetailsPaymentRecordKakaoPay.BuyerId = buyerId //required
                PaymentMethodDetailsPaymentRecordKakaoPay.TransactionId = transactionId //required
            }

    type PaymentMethodDetailsPaymentRecordIdealBic =
        | [<JsonUnionCase("ABNANL2A")>] ABNANL2A
        | [<JsonUnionCase("ADYBNL2A")>] ADYBNL2A
        | [<JsonUnionCase("ASNBNL21")>] ASNBNL21
        | [<JsonUnionCase("BITSNL2A")>] BITSNL2A
        | [<JsonUnionCase("BUNQNL2A")>] BUNQNL2A
        | [<JsonUnionCase("BUUTNL2A")>] BUUTNL2A
        | [<JsonUnionCase("FNOMNL22")>] FNOMNL22
        | [<JsonUnionCase("FVLBNL22")>] FVLBNL22
        | [<JsonUnionCase("HANDNL2A")>] HANDNL2A
        | [<JsonUnionCase("INGBNL2A")>] INGBNL2A
        | [<JsonUnionCase("KNABNL2H")>] KNABNL2H
        | [<JsonUnionCase("MLLENL2A")>] MLLENL2A
        | [<JsonUnionCase("MOYONL21")>] MOYONL21
        | [<JsonUnionCase("NNBANL2G")>] NNBANL2G
        | [<JsonUnionCase("NTSBDEB1")>] NTSBDEB1
        | [<JsonUnionCase("RABONL2U")>] RABONL2U
        | [<JsonUnionCase("RBRBNL21")>] RBRBNL21
        | [<JsonUnionCase("REVOIE23")>] REVOIE23
        | [<JsonUnionCase("REVOLT21")>] REVOLT21
        | [<JsonUnionCase("SNSBNL2A")>] SNSBNL2A
        | [<JsonUnionCase("TRIONL2U")>] TRIONL2U

    type PaymentMethodDetailsPaymentRecordIdealBank =
        | AbnAmro
        | Adyen
        | AsnBank
        | Bunq
        | Buut
        | Finom
        | Handelsbanken
        | Ing
        | Knab
        | Mollie
        | Moneyou
        | N26
        | Nn
        | Rabobank
        | Regiobank
        | Revolut
        | SnsBank
        | TriodosBank
        | VanLanschot
        | Yoursafe

    type PaymentMethodDetailsPaymentRecordGiropay = {
        ///Bank code of bank associated with the bank account.
        BankCode: string option
        ///Name of the bank associated with the bank account.
        BankName: string option
        ///Bank Identifier Code of the bank associated with the bank account.
        Bic: string option
        ///Owner's verified full name. Values are verified or provided by Giropay directly (if supported) at the time of authorization or settlement. They cannot be set or mutated. Giropay rarely provides this information so the attribute is usually empty.
        VerifiedName: string option
    }
    with
        static member New (bankCode: string option, bankName: string option, bic: string option, verifiedName: string option) =
            {
                PaymentMethodDetailsPaymentRecordGiropay.BankCode = bankCode //required
                PaymentMethodDetailsPaymentRecordGiropay.BankName = bankName //required
                PaymentMethodDetailsPaymentRecordGiropay.Bic = bic //required
                PaymentMethodDetailsPaymentRecordGiropay.VerifiedName = verifiedName //required
            }

    type PaymentMethodDetailsPaymentRecordEpsBank =
        | ArzteUndApothekerBank
        | AustrianAnadiBankAg
        | BankAustria
        | BankhausCarlSpangler
        | BankhausSchelhammerUndSchatteraAg
        | BawagPskAg
        | BksBankAg
        | BrullKallmusBankAg
        | BtvVierLanderBank
        | CapitalBankGraweGruppeAg
        | DeutscheBankAg
        | Dolomitenbank
        | EasybankAg
        | ErsteBankUndSparkassen
        | HypoAlpeadriabankInternationalAg
        | HypoBankBurgenlandAktiengesellschaft
        | HypoNoeLbFurNiederosterreichUWien
        | HypoOberosterreichSalzburgSteiermark
        | HypoTirolBankAg
        | HypoVorarlbergBankAg
        | MarchfelderBank
        | OberbankAg
        | RaiffeisenBankengruppeOsterreich
        | SchoellerbankAg
        | SpardaBankWien
        | VolksbankGruppe
        | VolkskreditbankAg
        | VrBankBraunau

    type PaymentMethodDetailsPaymentRecordCashapp = {
        ///A unique and immutable identifier assigned by Cash App to every buyer.
        BuyerId: string option
        ///A public identifier for buyers using Cash App.
        Cashtag: string option
        ///A unique and immutable identifier of payments assigned by Cash App.
        TransactionId: string option
    }
    with
        static member New (buyerId: string option, cashtag: string option, transactionId: string option) =
            {
                PaymentMethodDetailsPaymentRecordCashapp.BuyerId = buyerId //required
                PaymentMethodDetailsPaymentRecordCashapp.Cashtag = cashtag //required
                PaymentMethodDetailsPaymentRecordCashapp.TransactionId = transactionId //required
            }

    type PaymentMethodDetailsPaymentRecordBoleto = {
        ///The tax ID of the customer (CPF for individuals consumers or CNPJ for businesses consumers)
        TaxId: string option
    }
    with
        static member New (taxId: string option) =
            {
                PaymentMethodDetailsPaymentRecordBoleto.TaxId = taxId //required
            }

    type PaymentMethodDetailsPaymentRecordBlik = {
        ///A unique and immutable identifier assigned by BLIK to every buyer.
        BuyerId: string option
    }
    with
        static member New (buyerId: string option) =
            {
                PaymentMethodDetailsPaymentRecordBlik.BuyerId = buyerId //required
            }

    type PaymentMethodDetailsPaymentRecordBillie = {
        ///The Billie transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (transactionId: string option) =
            {
                PaymentMethodDetailsPaymentRecordBillie.TransactionId = transactionId //required
            }

    [<Struct>]
    type PaymentMethodDetailsPaymentRecordBancontactPreferredLanguage =
        | De
        | En
        | Fr
        | Nl

    type PaymentMethodDetailsPaymentRecordAfterpayClearpay = {
        ///The Afterpay order ID associated with this payment intent.
        OrderId: string option
        ///Order identifier shown to the merchant in Afterpay's online portal.
        Reference: string option
    }
    with
        static member New (orderId: string option, reference: string option) =
            {
                PaymentMethodDetailsPaymentRecordAfterpayClearpay.OrderId = orderId //required
                PaymentMethodDetailsPaymentRecordAfterpayClearpay.Reference = reference //required
            }

    type PaymentMethodDetailsPaymentRecordAffirm = {
        ///ID of the location that this reader is assigned to.
        Location: string option
        ///ID of the reader this transaction was made on.
        Reader: string option
        ///The Affirm transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (transactionId: string option, ?location: string, ?reader: string) =
            {
                PaymentMethodDetailsPaymentRecordAffirm.TransactionId = transactionId //required
                PaymentMethodDetailsPaymentRecordAffirm.Location = location
                PaymentMethodDetailsPaymentRecordAffirm.Reader = reader
            }

    type PaymentMethodDetailsPaymentRecordAcssDebit = {
        ///Name of the bank associated with the bank account.
        BankName: string option
        ///Estimated date to debit the customer's bank account. A date string in YYYY-MM-DD format.
        ExpectedDebitDate: string option
        ///Uniquely identifies this particular bank account. You can use this attribute to check whether two bank accounts are the same.
        Fingerprint: string option
        ///Institution number of the bank account
        InstitutionNumber: string option
        ///Last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string option
        ///ID of the mandate used to make this payment.
        Mandate: string option
        ///Transit number of the bank account.
        TransitNumber: string option
    }
    with
        static member New (bankName: string option, fingerprint: string option, institutionNumber: string option, last4: string option, transitNumber: string option, ?expectedDebitDate: string, ?mandate: string) =
            {
                PaymentMethodDetailsPaymentRecordAcssDebit.BankName = bankName //required
                PaymentMethodDetailsPaymentRecordAcssDebit.Fingerprint = fingerprint //required
                PaymentMethodDetailsPaymentRecordAcssDebit.InstitutionNumber = institutionNumber //required
                PaymentMethodDetailsPaymentRecordAcssDebit.Last4 = last4 //required
                PaymentMethodDetailsPaymentRecordAcssDebit.TransitNumber = transitNumber //required
                PaymentMethodDetailsPaymentRecordAcssDebit.ExpectedDebitDate = expectedDebitDate
                PaymentMethodDetailsPaymentRecordAcssDebit.Mandate = mandate
            }

    type PaymentMethodDetailsPayco = {
        ///A unique identifier for the buyer as determined by the local payment processor.
        BuyerId: string option
        ///The Payco transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (buyerId: string option, transactionId: string option) =
            {
                PaymentMethodDetailsPayco.BuyerId = buyerId //required
                PaymentMethodDetailsPayco.TransactionId = transactionId //required
            }

    type PaymentMethodDetailsPayByBank = {
        PaymentMethodDetailsPayByBank: string option
    }
    with
        static member New (?paymentMethodDetailsPayByBank: string option) =
            {
                PaymentMethodDetailsPayByBank.PaymentMethodDetailsPayByBank = paymentMethodDetailsPayByBank |> Option.flatten
            }

    [<Struct>]
    type PaymentMethodDetailsPassthroughCardFunding =
        | Credit
        | Debit
        | Prepaid
        | Unknown

    type PaymentMethodDetailsPassthroughCardBrand =
        | Amex
        | CartesBancaires
        | Diners
        | Discover
        | EftposAu
        | Jcb
        | Link
        | Mastercard
        | Unionpay
        | Visa
        | Unknown

    type PaymentMethodDetailsP24Bank =
        | AliorBank
        | BankMillennium
        | BankNowyBfgSa
        | BankPekaoSa
        | BankiSpbdzielcze
        | Blik
        | BnpParibas
        | Boz
        | CitiHandlowy
        | CreditAgricole
        | Envelobank
        | EtransferPocztowy24
        | GetinBank
        | Ideabank
        | Ing
        | Inteligo
        | MbankMtransfer
        | NestPrzelew
        | NoblePay
        | PbacZIpko
        | PlusBank
        | SantanderPrzelew24
        | TmobileUsbugiBankowe
        | ToyotaBank
        | Velobank
        | VolkswagenBank

    type PaymentMethodDetailsOxxo = {
        ///OXXO reference number
        Number: string option
    }
    with
        static member New (number: string option) =
            {
                PaymentMethodDetailsOxxo.Number = number //required
            }

    type PaymentMethodDetailsNzBankAccount = {
        ///The name on the bank account. Only present if the account holder name is different from the name of the authorized signatory collected in the PaymentMethod’s billing details.
        AccountHolderName: string option
        ///The numeric code for the bank account's bank.
        BankCode: string
        ///The name of the bank.
        BankName: string
        ///The numeric code for the bank account's bank branch.
        BranchCode: string
        ///Estimated date to debit the customer's bank account. A date string in YYYY-MM-DD format.
        ExpectedDebitDate: string option
        ///Last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string
        ///The suffix of the bank account number.
        Suffix: string option
    }
    with
        static member New (accountHolderName: string option, bankCode: string, bankName: string, branchCode: string, last4: string, suffix: string option, ?expectedDebitDate: string) =
            {
                PaymentMethodDetailsNzBankAccount.AccountHolderName = accountHolderName //required
                PaymentMethodDetailsNzBankAccount.BankCode = bankCode //required
                PaymentMethodDetailsNzBankAccount.BankName = bankName //required
                PaymentMethodDetailsNzBankAccount.BranchCode = branchCode //required
                PaymentMethodDetailsNzBankAccount.Last4 = last4 //required
                PaymentMethodDetailsNzBankAccount.Suffix = suffix //required
                PaymentMethodDetailsNzBankAccount.ExpectedDebitDate = expectedDebitDate
            }

    type PaymentMethodDetailsNaverPay = {
        ///A unique identifier for the buyer as determined by the local payment processor.
        BuyerId: string option
        ///The Naver Pay transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (buyerId: string option, transactionId: string option) =
            {
                PaymentMethodDetailsNaverPay.BuyerId = buyerId //required
                PaymentMethodDetailsNaverPay.TransactionId = transactionId //required
            }

    type PaymentMethodDetailsMultibanco = {
        ///Entity number associated with this Multibanco payment.
        Entity: string option
        ///Reference number associated with this Multibanco payment.
        Reference: string option
    }
    with
        static member New (entity: string option, reference: string option) =
            {
                PaymentMethodDetailsMultibanco.Entity = entity //required
                PaymentMethodDetailsMultibanco.Reference = reference //required
            }

    type PaymentMethodDetailsMbWay = {
        PaymentMethodDetailsMbWay: string option
    }
    with
        static member New (?paymentMethodDetailsMbWay: string option) =
            {
                PaymentMethodDetailsMbWay.PaymentMethodDetailsMbWay = paymentMethodDetailsMbWay |> Option.flatten
            }

    type PaymentMethodDetailsKrCardBrand =
        | Bc
        | Citi
        | Hana
        | Hyundai
        | Jeju
        | Jeonbuk
        | Kakaobank
        | Kbank
        | Kdbbank
        | Kookmin
        | Kwangju
        | Lotte
        | Mg
        | Nh
        | Post
        | Samsung
        | Savingsbank
        | Shinhan
        | Shinhyup
        | Suhyup
        | Tossbank
        | Woori

    [<Struct>]
    type PaymentMethodDetailsKonbiniStoreChain =
        | Familymart
        | Lawson
        | Ministop
        | Seicomart

    type PaymentMethodDetailsKakaoPay = {
        ///A unique identifier for the buyer as determined by the local payment processor.
        BuyerId: string option
        ///The Kakao Pay transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (buyerId: string option, transactionId: string option) =
            {
                PaymentMethodDetailsKakaoPay.BuyerId = buyerId //required
                PaymentMethodDetailsKakaoPay.TransactionId = transactionId //required
            }

    type PaymentMethodDetailsInteracPresentReceiptCardholderVerificationMethod =
        | Approval
        | Failure
        | [<JsonUnionCase("none")>] None'
        | OfflinePin
        | OfflinePinAndSignature
        | OnlinePin
        | Signature

    [<Struct>]
    type PaymentMethodDetailsInteracPresentReceiptAccountType =
        | Checking
        | Savings
        | Unknown

    [<Struct>]
    type PaymentMethodDetailsInteracPresentReadMethod =
        | ContactEmv
        | ContactlessEmv
        | ContactlessMagstripeMode
        | MagneticStripeFallback
        | MagneticStripeTrack2

    type PaymentMethodDetailsInteracPresentNetwork =
        | Amex
        | CartesBancaires
        | Diners
        | Discover
        | EftposAu
        | Interac
        | Jcb
        | Link
        | Mastercard
        | Unionpay
        | Visa
        | Unknown

    [<Struct>]
    type PaymentMethodDetailsInteracPresentFunding =
        | Credit
        | Debit
        | Prepaid
        | Unknown

    [<Struct>]
    type PaymentMethodDetailsInteracPresentBrand =
        | Interac
        | Mastercard
        | Visa

    type PaymentMethodDetailsIdealBic =
        | [<JsonUnionCase("ABNANL2A")>] ABNANL2A
        | [<JsonUnionCase("ADYBNL2A")>] ADYBNL2A
        | [<JsonUnionCase("ASNBNL21")>] ASNBNL21
        | [<JsonUnionCase("BITSNL2A")>] BITSNL2A
        | [<JsonUnionCase("BUNQNL2A")>] BUNQNL2A
        | [<JsonUnionCase("BUUTNL2A")>] BUUTNL2A
        | [<JsonUnionCase("FNOMNL22")>] FNOMNL22
        | [<JsonUnionCase("FVLBNL22")>] FVLBNL22
        | [<JsonUnionCase("HANDNL2A")>] HANDNL2A
        | [<JsonUnionCase("INGBNL2A")>] INGBNL2A
        | [<JsonUnionCase("KNABNL2H")>] KNABNL2H
        | [<JsonUnionCase("MLLENL2A")>] MLLENL2A
        | [<JsonUnionCase("MOYONL21")>] MOYONL21
        | [<JsonUnionCase("NNBANL2G")>] NNBANL2G
        | [<JsonUnionCase("NTSBDEB1")>] NTSBDEB1
        | [<JsonUnionCase("RABONL2U")>] RABONL2U
        | [<JsonUnionCase("RBRBNL21")>] RBRBNL21
        | [<JsonUnionCase("REVOIE23")>] REVOIE23
        | [<JsonUnionCase("REVOLT21")>] REVOLT21
        | [<JsonUnionCase("SNSBNL2A")>] SNSBNL2A
        | [<JsonUnionCase("TRIONL2U")>] TRIONL2U

    type PaymentMethodDetailsIdealBank =
        | AbnAmro
        | Adyen
        | AsnBank
        | Bunq
        | Buut
        | Finom
        | Handelsbanken
        | Ing
        | Knab
        | Mollie
        | Moneyou
        | N26
        | Nn
        | Rabobank
        | Regiobank
        | Revolut
        | SnsBank
        | TriodosBank
        | VanLanschot
        | Yoursafe

    type PaymentMethodDetailsGrabpay = {
        ///Unique transaction id generated by GrabPay
        TransactionId: string option
    }
    with
        static member New (transactionId: string option) =
            {
                PaymentMethodDetailsGrabpay.TransactionId = transactionId //required
            }

    type PaymentMethodDetailsGiropay = {
        ///Bank code of bank associated with the bank account.
        BankCode: string option
        ///Name of the bank associated with the bank account.
        BankName: string option
        ///Bank Identifier Code of the bank associated with the bank account.
        Bic: string option
        ///Owner's verified full name. Values are verified or provided by Giropay directly
        ///(if supported) at the time of authorization or settlement. They cannot be set or mutated.
        ///Giropay rarely provides this information so the attribute is usually empty.
        VerifiedName: string option
    }
    with
        static member New (bankCode: string option, bankName: string option, bic: string option, verifiedName: string option) =
            {
                PaymentMethodDetailsGiropay.BankCode = bankCode //required
                PaymentMethodDetailsGiropay.BankName = bankName //required
                PaymentMethodDetailsGiropay.Bic = bic //required
                PaymentMethodDetailsGiropay.VerifiedName = verifiedName //required
            }

    type PaymentMethodDetailsFpxBank =
        | AffinBank
        | Agrobank
        | AllianceBank
        | Ambank
        | BankIslam
        | BankMuamalat
        | BankOfChina
        | BankRakyat
        | Bsn
        | Cimb
        | DeutscheBank
        | HongLeongBank
        | Hsbc
        | Kfh
        | Maybank2e
        | Maybank2u
        | Ocbc
        | PbEnterprise
        | PublicBank
        | Rhb
        | StandardChartered
        | Uob

    [<Struct>]
    type PaymentMethodDetailsFpxAccountHolderType =
        | Company
        | Individual

    type PaymentMethodDetailsEpsBank =
        | ArzteUndApothekerBank
        | AustrianAnadiBankAg
        | BankAustria
        | BankhausCarlSpangler
        | BankhausSchelhammerUndSchatteraAg
        | BawagPskAg
        | BksBankAg
        | BrullKallmusBankAg
        | BtvVierLanderBank
        | CapitalBankGraweGruppeAg
        | DeutscheBankAg
        | Dolomitenbank
        | EasybankAg
        | ErsteBankUndSparkassen
        | HypoAlpeadriabankInternationalAg
        | HypoBankBurgenlandAktiengesellschaft
        | HypoNoeLbFurNiederosterreichUWien
        | HypoOberosterreichSalzburgSteiermark
        | HypoTirolBankAg
        | HypoVorarlbergBankAg
        | MarchfelderBank
        | OberbankAg
        | RaiffeisenBankengruppeOsterreich
        | SchoellerbankAg
        | SpardaBankWien
        | VolksbankGruppe
        | VolkskreditbankAg
        | VrBankBraunau

    type PaymentMethodDetailsCustomerBalance = {
        PaymentMethodDetailsCustomerBalance: string option
    }
    with
        static member New (?paymentMethodDetailsCustomerBalance: string option) =
            {
                PaymentMethodDetailsCustomerBalance.PaymentMethodDetailsCustomerBalance = paymentMethodDetailsCustomerBalance |> Option.flatten
            }

    [<Struct>]
    type PaymentMethodDetailsCryptoTokenCurrency =
        | PhantomCash
        | Usdc
        | Usdg
        | Usdp
        | Usdt

    [<Struct>]
    type PaymentMethodDetailsCryptoNetwork =
        | Base
        | Ethereum
        | Polygon
        | Solana
        | Tempo

    type PaymentMethodDetailsCashapp = {
        ///A unique and immutable identifier assigned by Cash App to every buyer.
        BuyerId: string option
        ///A public identifier for buyers using Cash App.
        Cashtag: string option
        ///A unique and immutable identifier of payments assigned by Cash App
        TransactionId: string option
    }
    with
        static member New (buyerId: string option, cashtag: string option, transactionId: string option) =
            {
                PaymentMethodDetailsCashapp.BuyerId = buyerId //required
                PaymentMethodDetailsCashapp.Cashtag = cashtag //required
                PaymentMethodDetailsCashapp.TransactionId = transactionId //required
            }

    type PaymentMethodDetailsCardWalletSamsungPay = {
        PaymentMethodDetailsCardWalletSamsungPay: string option
    }
    with
        static member New (?paymentMethodDetailsCardWalletSamsungPay: string option) =
            {
                PaymentMethodDetailsCardWalletSamsungPay.PaymentMethodDetailsCardWalletSamsungPay = paymentMethodDetailsCardWalletSamsungPay |> Option.flatten
            }

    type PaymentMethodDetailsCardWalletLink = {
        PaymentMethodDetailsCardWalletLink: string option
    }
    with
        static member New (?paymentMethodDetailsCardWalletLink: string option) =
            {
                PaymentMethodDetailsCardWalletLink.PaymentMethodDetailsCardWalletLink = paymentMethodDetailsCardWalletLink |> Option.flatten
            }

    type PaymentMethodDetailsCardWalletGooglePay = {
        PaymentMethodDetailsCardWalletGooglePay: string option
    }
    with
        static member New (?paymentMethodDetailsCardWalletGooglePay: string option) =
            {
                PaymentMethodDetailsCardWalletGooglePay.PaymentMethodDetailsCardWalletGooglePay = paymentMethodDetailsCardWalletGooglePay |> Option.flatten
            }

    type PaymentMethodDetailsCardWalletApplePay = {
        PaymentMethodDetailsCardWalletApplePay: string option
    }
    with
        static member New (?paymentMethodDetailsCardWalletApplePay: string option) =
            {
                PaymentMethodDetailsCardWalletApplePay.PaymentMethodDetailsCardWalletApplePay = paymentMethodDetailsCardWalletApplePay |> Option.flatten
            }

    type PaymentMethodDetailsCardWalletAmexExpressCheckout = {
        PaymentMethodDetailsCardWalletAmexExpressCheckout: string option
    }
    with
        static member New (?paymentMethodDetailsCardWalletAmexExpressCheckout: string option) =
            {
                PaymentMethodDetailsCardWalletAmexExpressCheckout.PaymentMethodDetailsCardWalletAmexExpressCheckout = paymentMethodDetailsCardWalletAmexExpressCheckout |> Option.flatten
            }

    type PaymentMethodDetailsCardWalletType =
        | AmexExpressCheckout
        | ApplePay
        | GooglePay
        | Link
        | Masterpass
        | SamsungPay
        | VisaCheckout

    type PaymentMethodDetailsCardPresentReceiptCardholderVerificationMethod =
        | Approval
        | Failure
        | [<JsonUnionCase("none")>] None'
        | OfflinePin
        | OfflinePinAndSignature
        | OnlinePin
        | Signature

    [<Struct>]
    type PaymentMethodDetailsCardPresentReceiptAccountType =
        | Checking
        | Credit
        | Prepaid
        | Unknown

    type PaymentMethodDetailsCardPresentOffline = {
        ///Time at which the payment was collected while offline
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]StoredAt: DateTime option
    }
    with
        ///The method used to process this payment method offline. Only deferred is allowed.
        member _.Type = "deferred"

        static member New (storedAt: DateTime option) =
            {
                PaymentMethodDetailsCardPresentOffline.StoredAt = storedAt //required
            }

    [<Struct>]
    type PaymentMethodDetailsCardPresentReadMethod =
        | ContactEmv
        | ContactlessEmv
        | ContactlessMagstripeMode
        | MagneticStripeFallback
        | MagneticStripeTrack2

    type PaymentMethodDetailsCardPresentNetwork =
        | Amex
        | CartesBancaires
        | Diners
        | Discover
        | EftposAu
        | Interac
        | Jcb
        | Link
        | Mastercard
        | Unionpay
        | Visa
        | Unknown

    [<Struct>]
    type PaymentMethodDetailsCardPresentFunding =
        | Credit
        | Debit
        | Prepaid
        | Unknown

    type PaymentMethodDetailsCardPresentBrand =
        | Amex
        | CartesBancaires
        | Diners
        | Discover
        | EftposAu
        | Jcb
        | Link
        | Mastercard
        | Unionpay
        | Visa
        | Unknown

    type PaymentMethodDetailsCardNetworkToken = {
        ///Indicates if Stripe used a network token, either user provided or Stripe managed when processing the transaction.
        Used: bool
    }
    with
        static member New (used: bool) =
            {
                PaymentMethodDetailsCardNetworkToken.Used = used //required
            }

    [<Struct>]
    type PaymentMethodDetailsCardInstallmentsPlanType =
        | Bonus
        | FixedCount
        | Revolving

    [<Struct>]
    type PaymentMethodDetailsCardChecksCvcCheck =
        | Pass
        | Fail
        | Unavailable
        | Unchecked

    [<Struct>]
    type PaymentMethodDetailsCardChecksAddressPostalCodeCheck =
        | Pass
        | Fail
        | Unavailable
        | Unchecked

    [<Struct>]
    type PaymentMethodDetailsCardChecksAddressLine1Check =
        | Pass
        | Fail
        | Unavailable
        | Unchecked

    [<Struct>]
    type PaymentMethodDetailsCardRegulatedStatus =
        | Regulated
        | Unregulated

    type PaymentMethodDetailsCardNetwork =
        | Amex
        | CartesBancaires
        | Diners
        | Discover
        | EftposAu
        | Interac
        | Jcb
        | Link
        | Mastercard
        | Unionpay
        | Visa
        | Unknown

    [<Struct>]
    type PaymentMethodDetailsCardFunding =
        | Credit
        | Debit
        | Prepaid
        | Unknown

    type PaymentMethodDetailsCardBrand =
        | Amex
        | CartesBancaires
        | Diners
        | Discover
        | EftposAu
        | Jcb
        | Link
        | Mastercard
        | Unionpay
        | Visa
        | Unknown

    type PaymentMethodDetailsBoleto = {
        ///The tax ID of the customer (CPF for individuals consumers or CNPJ for businesses consumers)
        TaxId: string
    }
    with
        static member New (taxId: string) =
            {
                PaymentMethodDetailsBoleto.TaxId = taxId //required
            }

    type PaymentMethodDetailsBlik = {
        ///A unique and immutable identifier assigned by BLIK to every buyer.
        BuyerId: string option
    }
    with
        static member New (buyerId: string option) =
            {
                PaymentMethodDetailsBlik.BuyerId = buyerId //required
            }

    type PaymentMethodDetailsBillie = {
        ///The Billie transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (transactionId: string option) =
            {
                PaymentMethodDetailsBillie.TransactionId = transactionId //required
            }

    [<Struct>]
    type PaymentMethodDetailsBancontactPreferredLanguage =
        | De
        | En
        | Fr
        | Nl

    type PaymentMethodDetailsBacsDebit = {
        ///Estimated date to debit the customer's bank account. A date string in YYYY-MM-DD format.
        ExpectedDebitDate: string option
        ///Uniquely identifies this particular bank account. You can use this attribute to check whether two bank accounts are the same.
        Fingerprint: string option
        ///Last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string option
        ///ID of the mandate used to make this payment.
        Mandate: string option
        ///Sort code of the bank account. (e.g., `10-20-30`)
        SortCode: string option
    }
    with
        static member New (fingerprint: string option, last4: string option, mandate: string option, sortCode: string option, ?expectedDebitDate: string) =
            {
                PaymentMethodDetailsBacsDebit.Fingerprint = fingerprint //required
                PaymentMethodDetailsBacsDebit.Last4 = last4 //required
                PaymentMethodDetailsBacsDebit.Mandate = mandate //required
                PaymentMethodDetailsBacsDebit.SortCode = sortCode //required
                PaymentMethodDetailsBacsDebit.ExpectedDebitDate = expectedDebitDate
            }

    type PaymentMethodDetailsAuBecsDebit = {
        ///Bank-State-Branch number of the bank account.
        BsbNumber: string option
        ///Estimated date to debit the customer's bank account. A date string in YYYY-MM-DD format.
        ExpectedDebitDate: string option
        ///Uniquely identifies this particular bank account. You can use this attribute to check whether two bank accounts are the same.
        Fingerprint: string option
        ///Last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string option
        ///ID of the mandate used to make this payment.
        Mandate: string option
    }
    with
        static member New (bsbNumber: string option, fingerprint: string option, last4: string option, ?expectedDebitDate: string, ?mandate: string) =
            {
                PaymentMethodDetailsAuBecsDebit.BsbNumber = bsbNumber //required
                PaymentMethodDetailsAuBecsDebit.Fingerprint = fingerprint //required
                PaymentMethodDetailsAuBecsDebit.Last4 = last4 //required
                PaymentMethodDetailsAuBecsDebit.ExpectedDebitDate = expectedDebitDate
                PaymentMethodDetailsAuBecsDebit.Mandate = mandate
            }

    type PaymentMethodDetailsAfterpayClearpay = {
        ///The Afterpay order ID associated with this payment intent.
        OrderId: string option
        ///Order identifier shown to the merchant in Afterpay's online portal.
        Reference: string option
    }
    with
        static member New (orderId: string option, reference: string option) =
            {
                PaymentMethodDetailsAfterpayClearpay.OrderId = orderId //required
                PaymentMethodDetailsAfterpayClearpay.Reference = reference //required
            }

    type PaymentMethodDetailsAffirm = {
        ///ID of the location that this reader is assigned to.
        Location: string option
        ///ID of the reader this transaction was made on.
        Reader: string option
        ///The Affirm transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (transactionId: string option, ?location: string, ?reader: string) =
            {
                PaymentMethodDetailsAffirm.TransactionId = transactionId //required
                PaymentMethodDetailsAffirm.Location = location
                PaymentMethodDetailsAffirm.Reader = reader
            }

    type PaymentMethodDetailsAcssDebit = {
        ///Name of the bank associated with the bank account.
        BankName: string option
        ///Estimated date to debit the customer's bank account. A date string in YYYY-MM-DD format.
        ExpectedDebitDate: string option
        ///Uniquely identifies this particular bank account. You can use this attribute to check whether two bank accounts are the same.
        Fingerprint: string option
        ///Institution number of the bank account
        InstitutionNumber: string option
        ///Last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string option
        ///ID of the mandate used to make this payment.
        Mandate: string option
        ///Transit number of the bank account.
        TransitNumber: string option
    }
    with
        static member New (bankName: string option, fingerprint: string option, institutionNumber: string option, last4: string option, transitNumber: string option, ?expectedDebitDate: string, ?mandate: string) =
            {
                PaymentMethodDetailsAcssDebit.BankName = bankName //required
                PaymentMethodDetailsAcssDebit.Fingerprint = fingerprint //required
                PaymentMethodDetailsAcssDebit.InstitutionNumber = institutionNumber //required
                PaymentMethodDetailsAcssDebit.Last4 = last4 //required
                PaymentMethodDetailsAcssDebit.TransitNumber = transitNumber //required
                PaymentMethodDetailsAcssDebit.ExpectedDebitDate = expectedDebitDate
                PaymentMethodDetailsAcssDebit.Mandate = mandate
            }

    [<Struct>]
    type PaymentMethodDetailsAchDebitAccountHolderType =
        | Company
        | Individual

    type PaymentMethodDetailsAchCreditTransfer = {
        ///Account number to transfer funds to.
        AccountNumber: string option
        ///Name of the bank associated with the routing number.
        BankName: string option
        ///Routing transit number for the bank account to transfer funds to.
        RoutingNumber: string option
        ///SWIFT code of the bank associated with the routing number.
        SwiftCode: string option
    }
    with
        static member New (accountNumber: string option, bankName: string option, routingNumber: string option, swiftCode: string option) =
            {
                PaymentMethodDetailsAchCreditTransfer.AccountNumber = accountNumber //required
                PaymentMethodDetailsAchCreditTransfer.BankName = bankName //required
                PaymentMethodDetailsAchCreditTransfer.RoutingNumber = routingNumber //required
                PaymentMethodDetailsAchCreditTransfer.SwiftCode = swiftCode //required
            }

    type PaymentMethodCustomerBalance = {
        PaymentMethodCustomerBalance: string option
    }
    with
        static member New (?paymentMethodCustomerBalance: string option) =
            {
                PaymentMethodCustomerBalance.PaymentMethodCustomerBalance = paymentMethodCustomerBalance |> Option.flatten
            }

    type PaymentMethodCrypto = {
        PaymentMethodCrypto: string option
    }
    with
        static member New (?paymentMethodCrypto: string option) =
            {
                PaymentMethodCrypto.PaymentMethodCrypto = paymentMethodCrypto |> Option.flatten
            }

    [<Struct>]
    type PaymentMethodConfigResourceDisplayPreferenceValue =
        | Off
        | On

    [<Struct>]
    type PaymentMethodConfigResourceDisplayPreferencePreference =
        | [<JsonUnionCase("none")>] None'
        | Off
        | On

    type PaymentMethodConfigBizPaymentMethodConfigurationDetails = {
        ///ID of the payment method configuration used.
        Id: string
        ///ID of the parent payment method configuration used.
        Parent: string option
    }
    with
        static member New (id: string, parent: string option) =
            {
                PaymentMethodConfigBizPaymentMethodConfigurationDetails.Id = id //required
                PaymentMethodConfigBizPaymentMethodConfigurationDetails.Parent = parent //required
            }

    type PaymentMethodCashapp = {
        ///A unique and immutable identifier assigned by Cash App to every buyer.
        BuyerId: string option
        ///A public identifier for buyers using Cash App.
        Cashtag: string option
    }
    with
        static member New (buyerId: string option, cashtag: string option) =
            {
                PaymentMethodCashapp.BuyerId = buyerId //required
                PaymentMethodCashapp.Cashtag = cashtag //required
            }

    type PaymentMethodCardWalletSamsungPay = {
        PaymentMethodCardWalletSamsungPay: string option
    }
    with
        static member New (?paymentMethodCardWalletSamsungPay: string option) =
            {
                PaymentMethodCardWalletSamsungPay.PaymentMethodCardWalletSamsungPay = paymentMethodCardWalletSamsungPay |> Option.flatten
            }

    type PaymentMethodCardWalletLink = {
        PaymentMethodCardWalletLink: string option
    }
    with
        static member New (?paymentMethodCardWalletLink: string option) =
            {
                PaymentMethodCardWalletLink.PaymentMethodCardWalletLink = paymentMethodCardWalletLink |> Option.flatten
            }

    type PaymentMethodCardWalletGooglePay = {
        PaymentMethodCardWalletGooglePay: string option
    }
    with
        static member New (?paymentMethodCardWalletGooglePay: string option) =
            {
                PaymentMethodCardWalletGooglePay.PaymentMethodCardWalletGooglePay = paymentMethodCardWalletGooglePay |> Option.flatten
            }

    type PaymentMethodCardWalletApplePay = {
        PaymentMethodCardWalletApplePay: string option
    }
    with
        static member New (?paymentMethodCardWalletApplePay: string option) =
            {
                PaymentMethodCardWalletApplePay.PaymentMethodCardWalletApplePay = paymentMethodCardWalletApplePay |> Option.flatten
            }

    type PaymentMethodCardWalletAmexExpressCheckout = {
        PaymentMethodCardWalletAmexExpressCheckout: string option
    }
    with
        static member New (?paymentMethodCardWalletAmexExpressCheckout: string option) =
            {
                PaymentMethodCardWalletAmexExpressCheckout.PaymentMethodCardWalletAmexExpressCheckout = paymentMethodCardWalletAmexExpressCheckout |> Option.flatten
            }

    type PaymentMethodCardWalletType =
        | AmexExpressCheckout
        | ApplePay
        | GooglePay
        | Link
        | Masterpass
        | SamsungPay
        | VisaCheckout

    [<Struct>]
    type PaymentMethodCardPresentReadMethod =
        | ContactEmv
        | ContactlessEmv
        | ContactlessMagstripeMode
        | MagneticStripeFallback
        | MagneticStripeTrack2

    [<Struct>]
    type PaymentMethodCardPresentFunding =
        | Credit
        | Debit
        | Prepaid
        | Unknown

    type PaymentMethodCardPresentBrand =
        | Amex
        | CartesBancaires
        | Diners
        | Discover
        | EftposAu
        | Jcb
        | Link
        | Mastercard
        | Unionpay
        | Visa
        | Unknown

    [<Struct>]
    type PaymentMethodCardChecksCvcCheck =
        | Pass
        | Fail
        | Unavailable
        | Unchecked

    [<Struct>]
    type PaymentMethodCardChecksAddressPostalCodeCheck =
        | Pass
        | Fail
        | Unavailable
        | Unchecked

    [<Struct>]
    type PaymentMethodCardChecksAddressLine1Check =
        | Pass
        | Fail
        | Unavailable
        | Unchecked

    [<Struct>]
    type PaymentMethodCardRegulatedStatus =
        | Regulated
        | Unregulated

    [<Struct>]
    type PaymentMethodCardFunding =
        | Credit
        | Debit
        | Prepaid
        | Unknown

    type PaymentMethodCardBrand =
        | Amex
        | CartesBancaires
        | Diners
        | Discover
        | EftposAu
        | Jcb
        | Link
        | Mastercard
        | Unionpay
        | Visa
        | Unknown

    type PaymentMethodBoleto = {
        ///Uniquely identifies the customer tax id (CNPJ or CPF)
        TaxId: string
    }
    with
        static member New (taxId: string) =
            {
                PaymentMethodBoleto.TaxId = taxId //required
            }

    type PaymentMethodBlik = {
        PaymentMethodBlik: string option
    }
    with
        static member New (?paymentMethodBlik: string option) =
            {
                PaymentMethodBlik.PaymentMethodBlik = paymentMethodBlik |> Option.flatten
            }

    type PaymentMethodBillie = {
        PaymentMethodBillie: string option
    }
    with
        static member New (?paymentMethodBillie: string option) =
            {
                PaymentMethodBillie.PaymentMethodBillie = paymentMethodBillie |> Option.flatten
            }

    type PaymentMethodBancontact = {
        PaymentMethodBancontact: string option
    }
    with
        static member New (?paymentMethodBancontact: string option) =
            {
                PaymentMethodBancontact.PaymentMethodBancontact = paymentMethodBancontact |> Option.flatten
            }

    type PaymentMethodBacsDebit = {
        ///Uniquely identifies this particular bank account. You can use this attribute to check whether two bank accounts are the same.
        Fingerprint: string option
        ///Last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string option
        ///Sort code of the bank account. (e.g., `10-20-30`)
        SortCode: string option
    }
    with
        static member New (fingerprint: string option, last4: string option, sortCode: string option) =
            {
                PaymentMethodBacsDebit.Fingerprint = fingerprint //required
                PaymentMethodBacsDebit.Last4 = last4 //required
                PaymentMethodBacsDebit.SortCode = sortCode //required
            }

    type PaymentMethodAuBecsDebit = {
        ///Six-digit number identifying bank and branch associated with this bank account.
        BsbNumber: string option
        ///Uniquely identifies this particular bank account. You can use this attribute to check whether two bank accounts are the same.
        Fingerprint: string option
        ///Last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string option
    }
    with
        static member New (bsbNumber: string option, fingerprint: string option, last4: string option) =
            {
                PaymentMethodAuBecsDebit.BsbNumber = bsbNumber //required
                PaymentMethodAuBecsDebit.Fingerprint = fingerprint //required
                PaymentMethodAuBecsDebit.Last4 = last4 //required
            }

    type PaymentMethodAmazonPay = {
        PaymentMethodAmazonPay: string option
    }
    with
        static member New (?paymentMethodAmazonPay: string option) =
            {
                PaymentMethodAmazonPay.PaymentMethodAmazonPay = paymentMethodAmazonPay |> Option.flatten
            }

    type PaymentMethodAlma = {
        PaymentMethodAlma: string option
    }
    with
        static member New (?paymentMethodAlma: string option) =
            {
                PaymentMethodAlma.PaymentMethodAlma = paymentMethodAlma |> Option.flatten
            }

    type PaymentMethodAfterpayClearpay = {
        PaymentMethodAfterpayClearpay: string option
    }
    with
        static member New (?paymentMethodAfterpayClearpay: string option) =
            {
                PaymentMethodAfterpayClearpay.PaymentMethodAfterpayClearpay = paymentMethodAfterpayClearpay |> Option.flatten
            }

    type PaymentMethodAffirm = {
        PaymentMethodAffirm: string option
    }
    with
        static member New (?paymentMethodAffirm: string option) =
            {
                PaymentMethodAffirm.PaymentMethodAffirm = paymentMethodAffirm |> Option.flatten
            }

    type PaymentMethodAcssDebit = {
        ///Name of the bank associated with the bank account.
        BankName: string option
        ///Uniquely identifies this particular bank account. You can use this attribute to check whether two bank accounts are the same.
        Fingerprint: string option
        ///Institution number of the bank account.
        InstitutionNumber: string option
        ///Last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string option
        ///Transit number of the bank account.
        TransitNumber: string option
    }
    with
        static member New (bankName: string option, fingerprint: string option, institutionNumber: string option, last4: string option, transitNumber: string option) =
            {
                PaymentMethodAcssDebit.BankName = bankName //required
                PaymentMethodAcssDebit.Fingerprint = fingerprint //required
                PaymentMethodAcssDebit.InstitutionNumber = institutionNumber //required
                PaymentMethodAcssDebit.Last4 = last4 //required
                PaymentMethodAcssDebit.TransitNumber = transitNumber //required
            }

    type PaymentMethodType =
        | AcssDebit
        | Affirm
        | AfterpayClearpay
        | Alipay
        | Alma
        | AmazonPay
        | AuBecsDebit
        | BacsDebit
        | Bancontact
        | Billie
        | Blik
        | Boleto
        | Card
        | CardPresent
        | Cashapp
        | Crypto
        | Custom
        | CustomerBalance
        | Eps
        | Fpx
        | Giropay
        | Grabpay
        | Ideal
        | InteracPresent
        | KakaoPay
        | Klarna
        | Konbini
        | KrCard
        | Link
        | MbWay
        | Mobilepay
        | Multibanco
        | NaverPay
        | NzBankAccount
        | Oxxo
        | P24
        | PayByBank
        | Payco
        | Paynow
        | Paypal
        | Payto
        | Pix
        | Promptpay
        | RevolutPay
        | SamsungPay
        | Satispay
        | SepaDebit
        | Sofort
        | Sunbit
        | Swish
        | Twint
        | Upi
        | UsBankAccount
        | WechatPay
        | Zip

    [<Struct>]
    type PaymentMethodAllowRedisplay =
        | Always
        | Limited
        | Unspecified

    [<Struct>]
    type PaymentLinksResourceTaxIdCollectionRequired =
        | IfSupported
        | Never

    type PaymentLinksResourceShippingAddressCollectionAllowedCountries =
        | [<JsonUnionCase("AC")>] AC
        | [<JsonUnionCase("AD")>] AD
        | [<JsonUnionCase("AE")>] AE
        | [<JsonUnionCase("AF")>] AF
        | [<JsonUnionCase("AG")>] AG
        | [<JsonUnionCase("AI")>] AI
        | [<JsonUnionCase("AL")>] AL
        | [<JsonUnionCase("AM")>] AM
        | [<JsonUnionCase("AO")>] AO
        | [<JsonUnionCase("AQ")>] AQ
        | [<JsonUnionCase("AR")>] AR
        | [<JsonUnionCase("AT")>] AT
        | [<JsonUnionCase("AU")>] AU
        | [<JsonUnionCase("AW")>] AW
        | [<JsonUnionCase("AX")>] AX
        | [<JsonUnionCase("AZ")>] AZ
        | [<JsonUnionCase("BA")>] BA
        | [<JsonUnionCase("BB")>] BB
        | [<JsonUnionCase("BD")>] BD
        | [<JsonUnionCase("BE")>] BE
        | [<JsonUnionCase("BF")>] BF
        | [<JsonUnionCase("BG")>] BG
        | [<JsonUnionCase("BH")>] BH
        | [<JsonUnionCase("BI")>] BI
        | [<JsonUnionCase("BJ")>] BJ
        | [<JsonUnionCase("BL")>] BL
        | [<JsonUnionCase("BM")>] BM
        | [<JsonUnionCase("BN")>] BN
        | [<JsonUnionCase("BO")>] BO
        | [<JsonUnionCase("BQ")>] BQ
        | [<JsonUnionCase("BR")>] BR
        | [<JsonUnionCase("BS")>] BS
        | [<JsonUnionCase("BT")>] BT
        | [<JsonUnionCase("BV")>] BV
        | [<JsonUnionCase("BW")>] BW
        | [<JsonUnionCase("BY")>] BY
        | [<JsonUnionCase("BZ")>] BZ
        | [<JsonUnionCase("CA")>] CA
        | [<JsonUnionCase("CD")>] CD
        | [<JsonUnionCase("CF")>] CF
        | [<JsonUnionCase("CG")>] CG
        | [<JsonUnionCase("CH")>] CH
        | [<JsonUnionCase("CI")>] CI
        | [<JsonUnionCase("CK")>] CK
        | [<JsonUnionCase("CL")>] CL
        | [<JsonUnionCase("CM")>] CM
        | [<JsonUnionCase("CN")>] CN
        | [<JsonUnionCase("CO")>] CO
        | [<JsonUnionCase("CR")>] CR
        | [<JsonUnionCase("CV")>] CV
        | [<JsonUnionCase("CW")>] CW
        | [<JsonUnionCase("CY")>] CY
        | [<JsonUnionCase("CZ")>] CZ
        | [<JsonUnionCase("DE")>] DE
        | [<JsonUnionCase("DJ")>] DJ
        | [<JsonUnionCase("DK")>] DK
        | [<JsonUnionCase("DM")>] DM
        | [<JsonUnionCase("DO")>] DO
        | [<JsonUnionCase("DZ")>] DZ
        | [<JsonUnionCase("EC")>] EC
        | [<JsonUnionCase("EE")>] EE
        | [<JsonUnionCase("EG")>] EG
        | [<JsonUnionCase("EH")>] EH
        | [<JsonUnionCase("ER")>] ER
        | [<JsonUnionCase("ES")>] ES
        | [<JsonUnionCase("ET")>] ET
        | [<JsonUnionCase("FI")>] FI
        | [<JsonUnionCase("FJ")>] FJ
        | [<JsonUnionCase("FK")>] FK
        | [<JsonUnionCase("FO")>] FO
        | [<JsonUnionCase("FR")>] FR
        | [<JsonUnionCase("GA")>] GA
        | [<JsonUnionCase("GB")>] GB
        | [<JsonUnionCase("GD")>] GD
        | [<JsonUnionCase("GE")>] GE
        | [<JsonUnionCase("GF")>] GF
        | [<JsonUnionCase("GG")>] GG
        | [<JsonUnionCase("GH")>] GH
        | [<JsonUnionCase("GI")>] GI
        | [<JsonUnionCase("GL")>] GL
        | [<JsonUnionCase("GM")>] GM
        | [<JsonUnionCase("GN")>] GN
        | [<JsonUnionCase("GP")>] GP
        | [<JsonUnionCase("GQ")>] GQ
        | [<JsonUnionCase("GR")>] GR
        | [<JsonUnionCase("GS")>] GS
        | [<JsonUnionCase("GT")>] GT
        | [<JsonUnionCase("GU")>] GU
        | [<JsonUnionCase("GW")>] GW
        | [<JsonUnionCase("GY")>] GY
        | [<JsonUnionCase("HK")>] HK
        | [<JsonUnionCase("HN")>] HN
        | [<JsonUnionCase("HR")>] HR
        | [<JsonUnionCase("HT")>] HT
        | [<JsonUnionCase("HU")>] HU
        | [<JsonUnionCase("ID")>] ID
        | [<JsonUnionCase("IE")>] IE
        | [<JsonUnionCase("IL")>] IL
        | [<JsonUnionCase("IM")>] IM
        | [<JsonUnionCase("IN")>] IN
        | [<JsonUnionCase("IO")>] IO
        | [<JsonUnionCase("IQ")>] IQ
        | [<JsonUnionCase("IS")>] IS
        | [<JsonUnionCase("IT")>] IT
        | [<JsonUnionCase("JE")>] JE
        | [<JsonUnionCase("JM")>] JM
        | [<JsonUnionCase("JO")>] JO
        | [<JsonUnionCase("JP")>] JP
        | [<JsonUnionCase("KE")>] KE
        | [<JsonUnionCase("KG")>] KG
        | [<JsonUnionCase("KH")>] KH
        | [<JsonUnionCase("KI")>] KI
        | [<JsonUnionCase("KM")>] KM
        | [<JsonUnionCase("KN")>] KN
        | [<JsonUnionCase("KR")>] KR
        | [<JsonUnionCase("KW")>] KW
        | [<JsonUnionCase("KY")>] KY
        | [<JsonUnionCase("KZ")>] KZ
        | [<JsonUnionCase("LA")>] LA
        | [<JsonUnionCase("LB")>] LB
        | [<JsonUnionCase("LC")>] LC
        | [<JsonUnionCase("LI")>] LI
        | [<JsonUnionCase("LK")>] LK
        | [<JsonUnionCase("LR")>] LR
        | [<JsonUnionCase("LS")>] LS
        | [<JsonUnionCase("LT")>] LT
        | [<JsonUnionCase("LU")>] LU
        | [<JsonUnionCase("LV")>] LV
        | [<JsonUnionCase("LY")>] LY
        | [<JsonUnionCase("MA")>] MA
        | [<JsonUnionCase("MC")>] MC
        | [<JsonUnionCase("MD")>] MD
        | [<JsonUnionCase("ME")>] ME
        | [<JsonUnionCase("MF")>] MF
        | [<JsonUnionCase("MG")>] MG
        | [<JsonUnionCase("MK")>] MK
        | [<JsonUnionCase("ML")>] ML
        | [<JsonUnionCase("MM")>] MM
        | [<JsonUnionCase("MN")>] MN
        | [<JsonUnionCase("MO")>] MO
        | [<JsonUnionCase("MQ")>] MQ
        | [<JsonUnionCase("MR")>] MR
        | [<JsonUnionCase("MS")>] MS
        | [<JsonUnionCase("MT")>] MT
        | [<JsonUnionCase("MU")>] MU
        | [<JsonUnionCase("MV")>] MV
        | [<JsonUnionCase("MW")>] MW
        | [<JsonUnionCase("MX")>] MX
        | [<JsonUnionCase("MY")>] MY
        | [<JsonUnionCase("MZ")>] MZ
        | [<JsonUnionCase("NA")>] NA
        | [<JsonUnionCase("NC")>] NC
        | [<JsonUnionCase("NE")>] NE
        | [<JsonUnionCase("NG")>] NG
        | [<JsonUnionCase("NI")>] NI
        | [<JsonUnionCase("NL")>] NL
        | [<JsonUnionCase("NO")>] NO
        | [<JsonUnionCase("NP")>] NP
        | [<JsonUnionCase("NR")>] NR
        | [<JsonUnionCase("NU")>] NU
        | [<JsonUnionCase("NZ")>] NZ
        | [<JsonUnionCase("OM")>] OM
        | [<JsonUnionCase("PA")>] PA
        | [<JsonUnionCase("PE")>] PE
        | [<JsonUnionCase("PF")>] PF
        | [<JsonUnionCase("PG")>] PG
        | [<JsonUnionCase("PH")>] PH
        | [<JsonUnionCase("PK")>] PK
        | [<JsonUnionCase("PL")>] PL
        | [<JsonUnionCase("PM")>] PM
        | [<JsonUnionCase("PN")>] PN
        | [<JsonUnionCase("PR")>] PR
        | [<JsonUnionCase("PS")>] PS
        | [<JsonUnionCase("PT")>] PT
        | [<JsonUnionCase("PY")>] PY
        | [<JsonUnionCase("QA")>] QA
        | [<JsonUnionCase("RE")>] RE
        | [<JsonUnionCase("RO")>] RO
        | [<JsonUnionCase("RS")>] RS
        | [<JsonUnionCase("RU")>] RU
        | [<JsonUnionCase("RW")>] RW
        | [<JsonUnionCase("SA")>] SA
        | [<JsonUnionCase("SB")>] SB
        | [<JsonUnionCase("SC")>] SC
        | [<JsonUnionCase("SD")>] SD
        | [<JsonUnionCase("SE")>] SE
        | [<JsonUnionCase("SG")>] SG
        | [<JsonUnionCase("SH")>] SH
        | [<JsonUnionCase("SI")>] SI
        | [<JsonUnionCase("SJ")>] SJ
        | [<JsonUnionCase("SK")>] SK
        | [<JsonUnionCase("SL")>] SL
        | [<JsonUnionCase("SM")>] SM
        | [<JsonUnionCase("SN")>] SN
        | [<JsonUnionCase("SO")>] SO
        | [<JsonUnionCase("SR")>] SR
        | [<JsonUnionCase("SS")>] SS
        | [<JsonUnionCase("ST")>] ST
        | [<JsonUnionCase("SV")>] SV
        | [<JsonUnionCase("SX")>] SX
        | [<JsonUnionCase("SZ")>] SZ
        | [<JsonUnionCase("TA")>] TA
        | [<JsonUnionCase("TC")>] TC
        | [<JsonUnionCase("TD")>] TD
        | [<JsonUnionCase("TF")>] TF
        | [<JsonUnionCase("TG")>] TG
        | [<JsonUnionCase("TH")>] TH
        | [<JsonUnionCase("TJ")>] TJ
        | [<JsonUnionCase("TK")>] TK
        | [<JsonUnionCase("TL")>] TL
        | [<JsonUnionCase("TM")>] TM
        | [<JsonUnionCase("TN")>] TN
        | [<JsonUnionCase("TO")>] TO
        | [<JsonUnionCase("TR")>] TR
        | [<JsonUnionCase("TT")>] TT
        | [<JsonUnionCase("TV")>] TV
        | [<JsonUnionCase("TW")>] TW
        | [<JsonUnionCase("TZ")>] TZ
        | [<JsonUnionCase("UA")>] UA
        | [<JsonUnionCase("UG")>] UG
        | [<JsonUnionCase("US")>] US
        | [<JsonUnionCase("UY")>] UY
        | [<JsonUnionCase("UZ")>] UZ
        | [<JsonUnionCase("VA")>] VA
        | [<JsonUnionCase("VC")>] VC
        | [<JsonUnionCase("VE")>] VE
        | [<JsonUnionCase("VG")>] VG
        | [<JsonUnionCase("VN")>] VN
        | [<JsonUnionCase("VU")>] VU
        | [<JsonUnionCase("WF")>] WF
        | [<JsonUnionCase("WS")>] WS
        | [<JsonUnionCase("XK")>] XK
        | [<JsonUnionCase("YE")>] YE
        | [<JsonUnionCase("YT")>] YT
        | [<JsonUnionCase("ZA")>] ZA
        | [<JsonUnionCase("ZM")>] ZM
        | [<JsonUnionCase("ZW")>] ZW
        | [<JsonUnionCase("ZZ")>] ZZ

    type PaymentLinksResourcePhoneNumberCollection = {
        ///If `true`, a phone number will be collected during checkout.
        Enabled: bool
    }
    with
        static member New (enabled: bool) =
            {
                PaymentLinksResourcePhoneNumberCollection.Enabled = enabled //required
            }

    [<Struct>]
    type PaymentLinksResourcePaymentMethodReuseAgreementPosition =
        | Auto
        | Hidden

    [<Struct>]
    type PaymentLinksResourcePaymentIntentDataSetupFutureUsage =
        | OffSession
        | OnSession

    [<Struct>]
    type PaymentLinksResourcePaymentIntentDataCaptureMethod =
        | Automatic
        | AutomaticAsync
        | Manual

    type PaymentLinksResourceOptionalItemAdjustableQuantity = {
        ///Set to true if the quantity can be adjusted to any non-negative integer.
        Enabled: bool
        ///The maximum quantity of this item the customer can purchase. By default this value is 99.
        Maximum: int option
        ///The minimum quantity of this item the customer must purchase, if they choose to purchase it. Because this item is optional, the customer will always be able to remove it from their order, even if the `minimum` configured here is greater than 0. By default this value is 0.
        Minimum: int option
    }
    with
        static member New (enabled: bool, maximum: int option, minimum: int option) =
            {
                PaymentLinksResourceOptionalItemAdjustableQuantity.Enabled = enabled //required
                PaymentLinksResourceOptionalItemAdjustableQuantity.Maximum = maximum //required
                PaymentLinksResourceOptionalItemAdjustableQuantity.Minimum = minimum //required
            }

    type PaymentLinksResourceIndividualName = {
        ///Indicates whether individual name collection is enabled for the payment link.
        Enabled: bool
        ///Whether the customer is required to complete the field before checking out. Defaults to `false`.
        Optional: bool
    }
    with
        static member New (enabled: bool, optional: bool) =
            {
                PaymentLinksResourceIndividualName.Enabled = enabled //required
                PaymentLinksResourceIndividualName.Optional = optional //required
            }

    type PaymentLinksResourceCustomTextPosition = {
        ///Text can be up to 1200 characters in length.
        Message: string
    }
    with
        static member New (message: string) =
            {
                PaymentLinksResourceCustomTextPosition.Message = message //required
            }

    type PaymentLinksResourceCustomFieldsText = {
        ///The value that pre-fills the field on the payment page.
        DefaultValue: string option
        ///The maximum character length constraint for the customer's input.
        MaximumLength: int option
        ///The minimum character length requirement for the customer's input.
        MinimumLength: int option
    }
    with
        static member New (defaultValue: string option, maximumLength: int option, minimumLength: int option) =
            {
                PaymentLinksResourceCustomFieldsText.DefaultValue = defaultValue //required
                PaymentLinksResourceCustomFieldsText.MaximumLength = maximumLength //required
                PaymentLinksResourceCustomFieldsText.MinimumLength = minimumLength //required
            }

    type PaymentLinksResourceCustomFieldsNumeric = {
        ///The value that pre-fills the field on the payment page.
        DefaultValue: string option
        ///The maximum character length constraint for the customer's input.
        MaximumLength: int option
        ///The minimum character length requirement for the customer's input.
        MinimumLength: int option
    }
    with
        static member New (defaultValue: string option, maximumLength: int option, minimumLength: int option) =
            {
                PaymentLinksResourceCustomFieldsNumeric.DefaultValue = defaultValue //required
                PaymentLinksResourceCustomFieldsNumeric.MaximumLength = maximumLength //required
                PaymentLinksResourceCustomFieldsNumeric.MinimumLength = minimumLength //required
            }

    type PaymentLinksResourceCustomFieldsLabel = {
        ///Custom text for the label, displayed to the customer. Up to 50 characters.
        Custom: string option
    }
    with
        ///The type of the label.
        member _.Type = "custom"

        static member New (custom: string option) =
            {
                PaymentLinksResourceCustomFieldsLabel.Custom = custom //required
            }

    type PaymentLinksResourceCustomFieldsDropdownOption = {
        ///The label for the option, displayed to the customer. Up to 100 characters.
        Label: string
        ///The value for this option, not displayed to the customer, used by your integration to reconcile the option selected by the customer. Must be unique to this option, alphanumeric, and up to 100 characters.
        Value: string
    }
    with
        static member New (label: string, value: string) =
            {
                PaymentLinksResourceCustomFieldsDropdownOption.Label = label //required
                PaymentLinksResourceCustomFieldsDropdownOption.Value = value //required
            }

    [<Struct>]
    type PaymentLinksResourceCustomFieldsType =
        | Dropdown
        | Numeric
        | Text

    [<Struct>]
    type PaymentLinksResourceConsentCollectionTermsOfService =
        | [<JsonUnionCase("none")>] None'
        | Required

    [<Struct>]
    type PaymentLinksResourceConsentCollectionPromotions =
        | Auto
        | [<JsonUnionCase("none")>] None'

    type PaymentLinksResourceCompletionBehaviorRedirect = {
        ///The URL the customer will be redirected to after the purchase is complete.
        Url: string
    }
    with
        static member New (url: string) =
            {
                PaymentLinksResourceCompletionBehaviorRedirect.Url = url //required
            }

    type PaymentLinksResourceCompletionBehaviorConfirmationPage = {
        ///The custom message that is displayed to the customer after the purchase is complete.
        CustomMessage: string option
    }
    with
        static member New (customMessage: string option) =
            {
                PaymentLinksResourceCompletionBehaviorConfirmationPage.CustomMessage = customMessage //required
            }

    type PaymentLinksResourceCompletedSessions = {
        ///The current number of checkout sessions that have been completed on the payment link which count towards the `completed_sessions` restriction to be met.
        Count: int
        ///The maximum number of checkout sessions that can be completed for the `completed_sessions` restriction to be met.
        Limit: int
    }
    with
        static member New (count: int, limit: int) =
            {
                PaymentLinksResourceCompletedSessions.Count = count //required
                PaymentLinksResourceCompletedSessions.Limit = limit //required
            }

    type PaymentLinksResourceBusinessName = {
        ///Indicates whether business name collection is enabled for the payment link.
        Enabled: bool
        ///Whether the customer is required to complete the field before checking out. Defaults to `false`.
        Optional: bool
    }
    with
        static member New (enabled: bool, optional: bool) =
            {
                PaymentLinksResourceBusinessName.Enabled = enabled //required
                PaymentLinksResourceBusinessName.Optional = optional //required
            }

    [<Struct>]
    type PaymentLinksResourceAfterCompletionType =
        | HostedConfirmation
        | Redirect

    type PaymentLinkPaymentMethodTypes =
        | Affirm
        | AfterpayClearpay
        | Alipay
        | Alma
        | AuBecsDebit
        | BacsDebit
        | Bancontact
        | Billie
        | Blik
        | Boleto
        | Card
        | Cashapp
        | Eps
        | Fpx
        | Giropay
        | Grabpay
        | Ideal
        | Klarna
        | Konbini
        | Link
        | MbWay
        | Mobilepay
        | Multibanco
        | Oxxo
        | P24
        | PayByBank
        | Paynow
        | Paypal
        | Payto
        | Pix
        | Promptpay
        | Satispay
        | SepaDebit
        | Sofort
        | Sunbit
        | Swish
        | Twint
        | Upi
        | UsBankAccount
        | WechatPay
        | Zip

    [<Struct>]
    type PaymentLinkSubmitType =
        | Auto
        | Book
        | Donate
        | Pay
        | Subscribe

    [<Struct>]
    type PaymentLinkPaymentMethodCollection =
        | Always
        | IfRequired

    [<Struct>]
    type PaymentLinkCustomerCreation =
        | Always
        | IfRequired

    [<Struct>]
    type PaymentLinkBillingAddressCollection =
        | Auto
        | Required

    [<Struct>]
    type PaymentIntentTypeSpecificPaymentMethodOptionsClientVerificationMethod =
        | Automatic
        | Instant
        | Microdeposits

    [<Struct>]
    type PaymentIntentTypeSpecificPaymentMethodOptionsClientSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    [<Struct>]
    type PaymentIntentTypeSpecificPaymentMethodOptionsClientCaptureMethod =
        | AutomaticDelayed
        | Manual
        | ManualPreferred

    type PaymentIntentProcessingCustomerNotification = {
        ///Whether customer approval has been requested for this payment. For payments greater than INR 15000 or mandate amount, the customer must provide explicit approval of the payment with their bank.
        ApprovalRequested: bool option
        ///If customer approval is required, they need to provide approval before this time.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CompletesAt: DateTime option
    }
    with
        static member New (approvalRequested: bool option, completesAt: DateTime option) =
            {
                PaymentIntentProcessingCustomerNotification.ApprovalRequested = approvalRequested //required
                PaymentIntentProcessingCustomerNotification.CompletesAt = completesAt //required
            }

    [<Struct>]
    type PaymentIntentPaymentMethodOptionsUsBankAccountVerificationMethod =
        | Automatic
        | Instant
        | Microdeposits

    [<Struct>]
    type PaymentIntentPaymentMethodOptionsUsBankAccountTransactionPurpose =
        | Goods
        | Other
        | Services
        | Unspecified

    [<Struct>]
    type PaymentIntentPaymentMethodOptionsUsBankAccountSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    type PaymentIntentPaymentMethodOptionsSwish = {
        ///A reference for this payment to be displayed in the Swish app.
        Reference: string option
    }
    with
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"

        static member New (reference: string option) =
            {
                PaymentIntentPaymentMethodOptionsSwish.Reference = reference //required
            }

    [<Struct>]
    type PaymentIntentPaymentMethodOptionsSepaDebitSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    [<Struct>]
    type PaymentIntentPaymentMethodOptionsPaytoSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    [<Struct>]
    type PaymentIntentPaymentMethodOptionsNzBankAccountSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    type PaymentIntentPaymentMethodOptionsMobilepay () = 
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    type PaymentIntentPaymentMethodOptionsMandateOptionsSepaDebit = {
        ///Prefix used to generate the Mandate reference. Must be at most 12 characters long. Must consist of only uppercase letters, numbers, spaces, or the following special characters: '/', '_', '-', '&', '.'. Cannot begin with 'STRIPE'.
        ReferencePrefix: string option
    }
    with
        static member New (?referencePrefix: string) =
            {
                PaymentIntentPaymentMethodOptionsMandateOptionsSepaDebit.ReferencePrefix = referencePrefix
            }

    type PaymentIntentPaymentMethodOptionsMandateOptionsPaytoPurpose =
        | DependantSupport
        | Government
        | Loan
        | Mortgage
        | Other
        | Pension
        | Personal
        | Retail
        | Salary
        | Tax
        | Utility

    type PaymentIntentPaymentMethodOptionsMandateOptionsPaytoPaymentSchedule =
        | Adhoc
        | Annual
        | Daily
        | Fortnightly
        | Monthly
        | Quarterly
        | SemiAnnual
        | Weekly

    [<Struct>]
    type PaymentIntentPaymentMethodOptionsMandateOptionsPaytoAmountType =
        | Fixed
        | Maximum

    type PaymentIntentPaymentMethodOptionsMandateOptionsBacsDebit = {
        ///Prefix used to generate the Mandate reference. Must be at most 12 characters long. Must consist of only uppercase letters, numbers, spaces, or the following special characters: '/', '_', '-', '&', '.'. Cannot begin with 'DDIC' or 'STRIPE'.
        ReferencePrefix: string option
    }
    with
        static member New (?referencePrefix: string) =
            {
                PaymentIntentPaymentMethodOptionsMandateOptionsBacsDebit.ReferencePrefix = referencePrefix
            }

    [<Struct>]
    type PaymentIntentPaymentMethodOptionsMandateOptionsAcssDebitTransactionType =
        | Business
        | Personal

    [<Struct>]
    type PaymentIntentPaymentMethodOptionsMandateOptionsAcssDebitPaymentSchedule =
        | Combined
        | Interval
        | Sporadic

    [<Struct>]
    type PaymentIntentPaymentMethodOptionsLinkSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    type PaymentIntentPaymentMethodOptionsEps () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    [<Struct>]
    type PaymentIntentPaymentMethodOptionsCardSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    [<Struct>]
    type PaymentIntentPaymentMethodOptionsCardRequestThreeDSecure =
        | Any
        | Automatic
        | Challenge

    [<Struct>]
    type PaymentIntentPaymentMethodOptionsCardRequestOvercapture =
        | IfAvailable
        | Never

    [<Struct>]
    type PaymentIntentPaymentMethodOptionsCardRequestMulticapture =
        | IfAvailable
        | Never

    [<Struct>]
    type PaymentIntentPaymentMethodOptionsCardRequestIncrementalAuthorization =
        | IfAvailable
        | Never

    [<Struct>]
    type PaymentIntentPaymentMethodOptionsCardRequestExtendedAuthorization =
        | IfAvailable
        | Never

    type PaymentIntentPaymentMethodOptionsCardNetwork =
        | Amex
        | CartesBancaires
        | Diners
        | Discover
        | EftposAu
        | Girocard
        | Interac
        | Jcb
        | Link
        | Mastercard
        | Unionpay
        | Unknown
        | Visa

    type PaymentIntentPaymentMethodOptionsBlik () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    [<Struct>]
    type PaymentIntentPaymentMethodOptionsBacsDebitSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    [<Struct>]
    type PaymentIntentPaymentMethodOptionsAuBecsDebitSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    [<Struct>]
    type PaymentIntentPaymentMethodOptionsAcssDebitVerificationMethod =
        | Automatic
        | Instant
        | Microdeposits

    [<Struct>]
    type PaymentIntentPaymentMethodOptionsAcssDebitSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    type PaymentIntentNextActionWechatPayRedirectToIosApp = {
        ///An universal link that redirect to WeChat Pay app
        NativeUrl: string
    }
    with
        static member New (nativeUrl: string) =
            {
                PaymentIntentNextActionWechatPayRedirectToIosApp.NativeUrl = nativeUrl //required
            }

    type PaymentIntentNextActionWechatPayRedirectToAndroidApp = {
        ///app_id is the APP ID registered on WeChat open platform
        AppId: string
        ///nonce_str is a random string
        NonceStr: string
        ///package is static value
        Package: string
        ///an unique merchant ID assigned by WeChat Pay
        PartnerId: string
        ///an unique trading ID assigned by WeChat Pay
        PrepayId: string
        ///A signature
        Sign: string
        ///Specifies the current time in epoch format
        Timestamp: string
    }
    with
        static member New (appId: string, nonceStr: string, package: string, partnerId: string, prepayId: string, sign: string, timestamp: string) =
            {
                PaymentIntentNextActionWechatPayRedirectToAndroidApp.AppId = appId //required
                PaymentIntentNextActionWechatPayRedirectToAndroidApp.NonceStr = nonceStr //required
                PaymentIntentNextActionWechatPayRedirectToAndroidApp.Package = package //required
                PaymentIntentNextActionWechatPayRedirectToAndroidApp.PartnerId = partnerId //required
                PaymentIntentNextActionWechatPayRedirectToAndroidApp.PrepayId = prepayId //required
                PaymentIntentNextActionWechatPayRedirectToAndroidApp.Sign = sign //required
                PaymentIntentNextActionWechatPayRedirectToAndroidApp.Timestamp = timestamp //required
            }

    type PaymentIntentNextActionWechatPayDisplayQrCode = {
        ///The data being used to generate QR code
        Data: string
        ///The URL to the hosted WeChat Pay instructions page, which allows customers to view the WeChat Pay QR code.
        HostedInstructionsUrl: string
        ///The base64 image data for a pre-generated QR code
        ImageDataUrl: string
        ///The image_url_png string used to render QR code
        ImageUrlPng: string
        ///The image_url_svg string used to render QR code
        ImageUrlSvg: string
    }
    with
        static member New (data: string, hostedInstructionsUrl: string, imageDataUrl: string, imageUrlPng: string, imageUrlSvg: string) =
            {
                PaymentIntentNextActionWechatPayDisplayQrCode.Data = data //required
                PaymentIntentNextActionWechatPayDisplayQrCode.HostedInstructionsUrl = hostedInstructionsUrl //required
                PaymentIntentNextActionWechatPayDisplayQrCode.ImageDataUrl = imageDataUrl //required
                PaymentIntentNextActionWechatPayDisplayQrCode.ImageUrlPng = imageUrlPng //required
                PaymentIntentNextActionWechatPayDisplayQrCode.ImageUrlSvg = imageUrlSvg //required
            }

    [<Struct>]
    type PaymentIntentNextActionVerifyWithMicrodepositsMicrodepositType =
        | Amounts
        | DescriptorCode

    type PaymentIntentNextActionUpiqrCode = {
        ///The date (unix timestamp) when the QR code expires.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime
        ///The image_url_png string used to render QR code
        ImageUrlPng: string
        ///The image_url_svg string used to render QR code
        ImageUrlSvg: string
    }
    with
        static member New (expiresAt: DateTime, imageUrlPng: string, imageUrlSvg: string) =
            {
                PaymentIntentNextActionUpiqrCode.ExpiresAt = expiresAt //required
                PaymentIntentNextActionUpiqrCode.ImageUrlPng = imageUrlPng //required
                PaymentIntentNextActionUpiqrCode.ImageUrlSvg = imageUrlSvg //required
            }

    type PaymentIntentNextActionSwishQrCode = {
        ///The raw data string used to generate QR code, it should be used together with QR code library.
        Data: string
        ///The image_url_png string used to render QR code
        ImageUrlPng: string
        ///The image_url_svg string used to render QR code
        ImageUrlSvg: string
    }
    with
        static member New (data: string, imageUrlPng: string, imageUrlSvg: string) =
            {
                PaymentIntentNextActionSwishQrCode.Data = data //required
                PaymentIntentNextActionSwishQrCode.ImageUrlPng = imageUrlPng //required
                PaymentIntentNextActionSwishQrCode.ImageUrlSvg = imageUrlSvg //required
            }

    type PaymentIntentNextActionRedirectToUrl = {
        ///If the customer does not exit their browser while authenticating, they will be redirected to this specified URL after completion.
        ReturnUrl: string option
        ///The URL you must redirect your customer to in order to authenticate the payment.
        Url: string option
    }
    with
        static member New (returnUrl: string option, url: string option) =
            {
                PaymentIntentNextActionRedirectToUrl.ReturnUrl = returnUrl //required
                PaymentIntentNextActionRedirectToUrl.Url = url //required
            }

    type PaymentIntentNextActionPromptpayDisplayQrCode = {
        ///The raw data string used to generate QR code, it should be used together with QR code library.
        Data: string
        ///The URL to the hosted PromptPay instructions page, which allows customers to view the PromptPay QR code.
        HostedInstructionsUrl: string
        ///The PNG path used to render the QR code, can be used as the source in an HTML img tag
        ImageUrlPng: string
        ///The SVG path used to render the QR code, can be used as the source in an HTML img tag
        ImageUrlSvg: string
    }
    with
        static member New (data: string, hostedInstructionsUrl: string, imageUrlPng: string, imageUrlSvg: string) =
            {
                PaymentIntentNextActionPromptpayDisplayQrCode.Data = data //required
                PaymentIntentNextActionPromptpayDisplayQrCode.HostedInstructionsUrl = hostedInstructionsUrl //required
                PaymentIntentNextActionPromptpayDisplayQrCode.ImageUrlPng = imageUrlPng //required
                PaymentIntentNextActionPromptpayDisplayQrCode.ImageUrlSvg = imageUrlSvg //required
            }

    type PaymentIntentNextActionPixDisplayQrCode = {
        ///The raw data string used to generate QR code, it should be used together with QR code library.
        Data: string option
        ///The date (unix timestamp) when the PIX expires.
        ExpiresAt: int option
        ///The URL to the hosted pix instructions page, which allows customers to view the pix QR code.
        HostedInstructionsUrl: string option
        ///The image_url_png string used to render png QR code
        ImageUrlPng: string option
        ///The image_url_svg string used to render svg QR code
        ImageUrlSvg: string option
    }
    with
        static member New (?data: string, ?expiresAt: int, ?hostedInstructionsUrl: string, ?imageUrlPng: string, ?imageUrlSvg: string) =
            {
                PaymentIntentNextActionPixDisplayQrCode.Data = data
                PaymentIntentNextActionPixDisplayQrCode.ExpiresAt = expiresAt
                PaymentIntentNextActionPixDisplayQrCode.HostedInstructionsUrl = hostedInstructionsUrl
                PaymentIntentNextActionPixDisplayQrCode.ImageUrlPng = imageUrlPng
                PaymentIntentNextActionPixDisplayQrCode.ImageUrlSvg = imageUrlSvg
            }

    type PaymentIntentNextActionPaynowDisplayQrCode = {
        ///The raw data string used to generate QR code, it should be used together with QR code library.
        Data: string
        ///The URL to the hosted PayNow instructions page, which allows customers to view the PayNow QR code.
        HostedInstructionsUrl: string option
        ///The image_url_png string used to render QR code
        ImageUrlPng: string
        ///The image_url_svg string used to render QR code
        ImageUrlSvg: string
    }
    with
        static member New (data: string, hostedInstructionsUrl: string option, imageUrlPng: string, imageUrlSvg: string) =
            {
                PaymentIntentNextActionPaynowDisplayQrCode.Data = data //required
                PaymentIntentNextActionPaynowDisplayQrCode.HostedInstructionsUrl = hostedInstructionsUrl //required
                PaymentIntentNextActionPaynowDisplayQrCode.ImageUrlPng = imageUrlPng //required
                PaymentIntentNextActionPaynowDisplayQrCode.ImageUrlSvg = imageUrlSvg //required
            }

    type PaymentIntentNextActionKonbiniSeicomart = {
        ///The confirmation number.
        ConfirmationNumber: string option
        ///The payment code.
        PaymentCode: string
    }
    with
        static member New (paymentCode: string, ?confirmationNumber: string) =
            {
                PaymentIntentNextActionKonbiniSeicomart.PaymentCode = paymentCode //required
                PaymentIntentNextActionKonbiniSeicomart.ConfirmationNumber = confirmationNumber
            }

    type PaymentIntentNextActionKonbiniMinistop = {
        ///The confirmation number.
        ConfirmationNumber: string option
        ///The payment code.
        PaymentCode: string
    }
    with
        static member New (paymentCode: string, ?confirmationNumber: string) =
            {
                PaymentIntentNextActionKonbiniMinistop.PaymentCode = paymentCode //required
                PaymentIntentNextActionKonbiniMinistop.ConfirmationNumber = confirmationNumber
            }

    type PaymentIntentNextActionKonbiniLawson = {
        ///The confirmation number.
        ConfirmationNumber: string option
        ///The payment code.
        PaymentCode: string
    }
    with
        static member New (paymentCode: string, ?confirmationNumber: string) =
            {
                PaymentIntentNextActionKonbiniLawson.PaymentCode = paymentCode //required
                PaymentIntentNextActionKonbiniLawson.ConfirmationNumber = confirmationNumber
            }

    type PaymentIntentNextActionKonbiniFamilymart = {
        ///The confirmation number.
        ConfirmationNumber: string option
        ///The payment code.
        PaymentCode: string
    }
    with
        static member New (paymentCode: string, ?confirmationNumber: string) =
            {
                PaymentIntentNextActionKonbiniFamilymart.PaymentCode = paymentCode //required
                PaymentIntentNextActionKonbiniFamilymart.ConfirmationNumber = confirmationNumber
            }

    type PaymentIntentNextActionKlarnaDisplayQrCode = {
        ///The data being used to generate QR code
        Data: string
        ///The timestamp at which the QR code expires.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime option
        ///The image_url_png string used to render QR code
        ImageUrlPng: string
        ///The image_url_svg string used to render QR code
        ImageUrlSvg: string
    }
    with
        static member New (data: string, expiresAt: DateTime option, imageUrlPng: string, imageUrlSvg: string) =
            {
                PaymentIntentNextActionKlarnaDisplayQrCode.Data = data //required
                PaymentIntentNextActionKlarnaDisplayQrCode.ExpiresAt = expiresAt //required
                PaymentIntentNextActionKlarnaDisplayQrCode.ImageUrlPng = imageUrlPng //required
                PaymentIntentNextActionKlarnaDisplayQrCode.ImageUrlSvg = imageUrlSvg //required
            }

    type PaymentIntentNextActionDisplayOxxoDetails = {
        ///The timestamp after which the OXXO voucher expires.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAfter: DateTime option
        ///The URL for the hosted OXXO voucher page, which allows customers to view and print an OXXO voucher.
        HostedVoucherUrl: string option
        ///OXXO reference number.
        Number: string option
    }
    with
        static member New (expiresAfter: DateTime option, hostedVoucherUrl: string option, number: string option) =
            {
                PaymentIntentNextActionDisplayOxxoDetails.ExpiresAfter = expiresAfter //required
                PaymentIntentNextActionDisplayOxxoDetails.HostedVoucherUrl = hostedVoucherUrl //required
                PaymentIntentNextActionDisplayOxxoDetails.Number = number //required
            }

    type PaymentIntentNextActionDisplayMultibancoDetails = {
        ///Entity number associated with this Multibanco payment.
        Entity: string option
        ///The timestamp at which the Multibanco voucher expires.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime option
        ///The URL for the hosted Multibanco voucher page, which allows customers to view a Multibanco voucher.
        HostedVoucherUrl: string option
        ///Reference number associated with this Multibanco payment.
        Reference: string option
    }
    with
        static member New (entity: string option, expiresAt: DateTime option, hostedVoucherUrl: string option, reference: string option) =
            {
                PaymentIntentNextActionDisplayMultibancoDetails.Entity = entity //required
                PaymentIntentNextActionDisplayMultibancoDetails.ExpiresAt = expiresAt //required
                PaymentIntentNextActionDisplayMultibancoDetails.HostedVoucherUrl = hostedVoucherUrl //required
                PaymentIntentNextActionDisplayMultibancoDetails.Reference = reference //required
            }

    [<Struct>]
    type PaymentIntentNextActionDisplayBankTransferInstructionsType =
        | EuBankTransfer
        | GbBankTransfer
        | JpBankTransfer
        | MxBankTransfer
        | UsBankTransfer

    type PaymentIntentNextActionCashappQrCode = {
        ///The date (unix timestamp) when the QR code expires.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime
        ///The image_url_png string used to render QR code
        ImageUrlPng: string
        ///The image_url_svg string used to render QR code
        ImageUrlSvg: string
    }
    with
        static member New (expiresAt: DateTime, imageUrlPng: string, imageUrlSvg: string) =
            {
                PaymentIntentNextActionCashappQrCode.ExpiresAt = expiresAt //required
                PaymentIntentNextActionCashappQrCode.ImageUrlPng = imageUrlPng //required
                PaymentIntentNextActionCashappQrCode.ImageUrlSvg = imageUrlSvg //required
            }

    type PaymentIntentNextActionCardAwaitNotification = {
        ///The time that payment will be attempted. If customer approval is required, they need to provide approval before this time.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ChargeAttemptAt: DateTime option
        ///For payments greater than INR 15000, the customer must provide explicit approval of the payment with their bank. For payments of lower amount, no customer action is required.
        CustomerApprovalRequired: bool option
    }
    with
        static member New (chargeAttemptAt: DateTime option, customerApprovalRequired: bool option) =
            {
                PaymentIntentNextActionCardAwaitNotification.ChargeAttemptAt = chargeAttemptAt //required
                PaymentIntentNextActionCardAwaitNotification.CustomerApprovalRequired = customerApprovalRequired //required
            }

    type PaymentIntentNextActionBoleto = {
        ///The timestamp after which the boleto expires.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime option
        ///The URL to the hosted boleto voucher page, which allows customers to view the boleto voucher.
        HostedVoucherUrl: string option
        ///The boleto number.
        Number: string option
        ///The URL to the downloadable boleto voucher PDF.
        Pdf: string option
    }
    with
        static member New (expiresAt: DateTime option, hostedVoucherUrl: string option, number: string option, pdf: string option) =
            {
                PaymentIntentNextActionBoleto.ExpiresAt = expiresAt //required
                PaymentIntentNextActionBoleto.HostedVoucherUrl = hostedVoucherUrl //required
                PaymentIntentNextActionBoleto.Number = number //required
                PaymentIntentNextActionBoleto.Pdf = pdf //required
            }

    type PaymentIntentNextActionAlipayHandleRedirect = {
        ///The native data to be used with Alipay SDK you must redirect your customer to in order to authenticate the payment in an Android App.
        NativeData: string option
        ///The native URL you must redirect your customer to in order to authenticate the payment in an iOS App.
        NativeUrl: string option
        ///If the customer does not exit their browser while authenticating, they will be redirected to this specified URL after completion.
        ReturnUrl: string option
        ///The URL you must redirect your customer to in order to authenticate the payment.
        Url: string option
    }
    with
        static member New (nativeData: string option, nativeUrl: string option, returnUrl: string option, url: string option) =
            {
                PaymentIntentNextActionAlipayHandleRedirect.NativeData = nativeData //required
                PaymentIntentNextActionAlipayHandleRedirect.NativeUrl = nativeUrl //required
                PaymentIntentNextActionAlipayHandleRedirect.ReturnUrl = returnUrl //required
                PaymentIntentNextActionAlipayHandleRedirect.Url = url //required
            }

    [<Struct>]
    type PaymentIntentNextActionType =
        | RedirectToUrl
        | UseStripeSdk
        | AlipayHandleRedirect
        | OxxoDisplayDetails
        | VerifyWithMicrodeposits

    type PaymentIntentExcludedPaymentMethodTypes =
        | AcssDebit
        | Affirm
        | AfterpayClearpay
        | Alipay
        | Alma
        | AmazonPay
        | AuBecsDebit
        | BacsDebit
        | Bancontact
        | Billie
        | Blik
        | Boleto
        | Card
        | Cashapp
        | Crypto
        | CustomerBalance
        | Eps
        | Fpx
        | Giropay
        | Grabpay
        | Ideal
        | KakaoPay
        | Klarna
        | Konbini
        | KrCard
        | MbWay
        | Mobilepay
        | Multibanco
        | NaverPay
        | NzBankAccount
        | Oxxo
        | P24
        | PayByBank
        | Payco
        | Paynow
        | Paypal
        | Payto
        | Pix
        | Promptpay
        | RevolutPay
        | SamsungPay
        | Satispay
        | SepaDebit
        | Sofort
        | Sunbit
        | Swish
        | Twint
        | Upi
        | UsBankAccount
        | WechatPay
        | Zip

    type PaymentIntentStatus =
        | Canceled
        | Processing
        | RequiresAction
        | RequiresCapture
        | RequiresConfirmation
        | RequiresPaymentMethod
        | Succeeded

    [<Struct>]
    type PaymentIntentSetupFutureUsage =
        | OffSession
        | OnSession

    [<Struct>]
    type PaymentIntentConfirmationMethod =
        | Automatic
        | Manual

    [<Struct>]
    type PaymentIntentCaptureMethod =
        | Automatic
        | AutomaticAsync
        | Manual

    type PaymentIntentCancellationReason =
        | Abandoned
        | Automatic
        | Duplicate
        | Expired
        | FailedInvoice
        | Fraudulent
        | RequestedByCustomer
        | VoidInvoice

    type PaymentFlowsPrivatePaymentMethodsSamsungPayPaymentMethodOptions () = 
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"


    [<Struct>]
    type PaymentFlowsPrivatePaymentMethodsPaypalAmountDetailsLineItemPaymentMethodOptionsCategory =
        | DigitalGoods
        | Donation
        | PhysicalGoods

    type PaymentFlowsPrivatePaymentMethodsPaycoPaymentMethodOptions () = 
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"


    [<Struct>]
    type PaymentFlowsPrivatePaymentMethodsNaverPayPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    type PaymentFlowsPrivatePaymentMethodsKlarnaPaymentIntentAmountDetailsLineItemPaymentMethodOptions = {
        ImageUrl: string option
        ProductUrl: string option
        Reference: string option
        SubscriptionReference: string option
    }
    with
        static member New (imageUrl: string option, productUrl: string option, reference: string option, subscriptionReference: string option) =
            {
                PaymentFlowsPrivatePaymentMethodsKlarnaPaymentIntentAmountDetailsLineItemPaymentMethodOptions.ImageUrl = imageUrl //required
                PaymentFlowsPrivatePaymentMethodsKlarnaPaymentIntentAmountDetailsLineItemPaymentMethodOptions.ProductUrl = productUrl //required
                PaymentFlowsPrivatePaymentMethodsKlarnaPaymentIntentAmountDetailsLineItemPaymentMethodOptions.Reference = reference //required
                PaymentFlowsPrivatePaymentMethodsKlarnaPaymentIntentAmountDetailsLineItemPaymentMethodOptions.SubscriptionReference = subscriptionReference //required
            }

    type PaymentFlowsPrivatePaymentMethodsKlarnaDob = {
        ///The day of birth, between 1 and 31.
        Day: int option
        ///The month of birth, between 1 and 12.
        Month: int option
        ///The four-digit year of birth.
        Year: int option
    }
    with
        static member New (day: int option, month: int option, year: int option) =
            {
                PaymentFlowsPrivatePaymentMethodsKlarnaDob.Day = day //required
                PaymentFlowsPrivatePaymentMethodsKlarnaDob.Month = month //required
                PaymentFlowsPrivatePaymentMethodsKlarnaDob.Year = year //required
            }

    [<Struct>]
    type PaymentFlowsPrivatePaymentMethodsKakaoPayPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    [<Struct>]
    type PaymentFlowsPrivatePaymentMethodsFinancialConnectionsCommonLinkedAccountOptionsFiltersAccountSubcategories =
        | Checking
        | Savings

    [<Struct>]
    type PaymentFlowsPrivatePaymentMethodsCardPresentCommonWalletType =
        | ApplePay
        | GooglePay
        | SamsungPay
        | Unknown

    type PaymentFlowsPrivatePaymentMethodsCardPresentAmountDetailsLineItemPaymentMethodOptions = {
        CommodityCode: string option
    }
    with
        static member New (commodityCode: string option) =
            {
                PaymentFlowsPrivatePaymentMethodsCardPresentAmountDetailsLineItemPaymentMethodOptions.CommodityCode = commodityCode //required
            }

    type PaymentFlowsPrivatePaymentMethodsCardPaymentIntentAmountDetailsLineItemPaymentMethodOptions = {
        CommodityCode: string option
    }
    with
        static member New (commodityCode: string option) =
            {
                PaymentFlowsPrivatePaymentMethodsCardPaymentIntentAmountDetailsLineItemPaymentMethodOptions.CommodityCode = commodityCode //required
            }

    [<Struct>]
    type PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceMulticaptureStatus =
        | Available
        | Unavailable

    [<Struct>]
    type PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesOvercaptureOvercaptureStatus =
        | Available
        | Unavailable

    [<Struct>]
    type PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesIncrementalAuthorizationIncrementalAuthorizationStatus =
        | Available
        | Unavailable

    [<Struct>]
    type PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesExtendedAuthorizationExtendedAuthorizationStatus =
        | Disabled
        | Enabled

    type PaymentFlowsPrivatePaymentMethodsAlipayDetails = {
        ///Uniquely identifies this particular Alipay account. You can use this attribute to check whether two Alipay accounts are the same.
        BuyerId: string option
        ///Uniquely identifies this particular Alipay account. You can use this attribute to check whether two Alipay accounts are the same.
        Fingerprint: string option
        ///Transaction ID of this particular Alipay transaction.
        TransactionId: string option
    }
    with
        static member New (fingerprint: string option, transactionId: string option, ?buyerId: string) =
            {
                PaymentFlowsPrivatePaymentMethodsAlipayDetails.Fingerprint = fingerprint //required
                PaymentFlowsPrivatePaymentMethodsAlipayDetails.TransactionId = transactionId //required
                PaymentFlowsPrivatePaymentMethodsAlipayDetails.BuyerId = buyerId
            }

    type PaymentFlowsPrivatePaymentMethodsAlipay = {
        PaymentFlowsPrivatePaymentMethodsAlipay: string option
    }
    with
        static member New (?paymentFlowsPrivatePaymentMethodsAlipay: string option) =
            {
                PaymentFlowsPrivatePaymentMethodsAlipay.PaymentFlowsPrivatePaymentMethodsAlipay = paymentFlowsPrivatePaymentMethodsAlipay |> Option.flatten
            }

    type PaymentFlowsPaymentIntentAsyncWorkflowsResourceInputsResourceTax = {
        ///The [TaxCalculation](https://docs.stripe.com/api/tax/calculations) id
        Calculation: string
    }
    with
        static member New (calculation: string) =
            {
                PaymentFlowsPaymentIntentAsyncWorkflowsResourceInputsResourceTax.Calculation = calculation //required
            }

    type PaymentFlowsPaymentDetails = {
        ///A unique value to identify the customer. This field is available only for card payments.
        ///This field is truncated to 25 alphanumeric characters, excluding spaces, before being sent to card networks.
        CustomerReference: string option
        ///A unique value assigned by the business to identify the transaction. Required for L2 and L3 rates.
        ///For Cards, this field is truncated to 25 alphanumeric characters, excluding spaces, before being sent to card networks. For Klarna, this field is truncated to 255 characters and is visible to customers when they view the order in the Klarna app.
        OrderReference: string option
    }
    with
        static member New (customerReference: string option, orderReference: string option) =
            {
                PaymentFlowsPaymentDetails.CustomerReference = customerReference //required
                PaymentFlowsPaymentDetails.OrderReference = orderReference //required
            }

    [<Struct>]
    type PaymentFlowsAutomaticPaymentMethodsSetupIntentAllowRedirects =
        | Always
        | Never

    [<Struct>]
    type PaymentFlowsAutomaticPaymentMethodsPaymentIntentAllowRedirects =
        | Always
        | Never

    type PaymentFlowsAmountDetailsResourceTax = {
        ///The total amount of tax on the transaction represented in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). Required for L2 rates. An integer greater than or equal to 0.
        ///This field is mutually exclusive with the `amount_details[line_items][#][tax][total_tax_amount]` field.
        TotalTaxAmount: int option
    }
    with
        static member New (totalTaxAmount: int option) =
            {
                PaymentFlowsAmountDetailsResourceTax.TotalTaxAmount = totalTaxAmount //required
            }

    type PaymentFlowsAmountDetailsResourceShipping = {
        ///If a physical good is being shipped, the cost of shipping represented in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). An integer greater than or equal to 0.
        Amount: int option
        ///If a physical good is being shipped, the postal code of where it is being shipped from. At most 10 alphanumeric characters long, hyphens are allowed.
        FromPostalCode: string option
        ///If a physical good is being shipped, the postal code of where it is being shipped to. At most 10 alphanumeric characters long, hyphens are allowed.
        ToPostalCode: string option
    }
    with
        static member New (amount: int option, fromPostalCode: string option, toPostalCode: string option) =
            {
                PaymentFlowsAmountDetailsResourceShipping.Amount = amount //required
                PaymentFlowsAmountDetailsResourceShipping.FromPostalCode = fromPostalCode //required
                PaymentFlowsAmountDetailsResourceShipping.ToPostalCode = toPostalCode //required
            }

    type PaymentFlowsAmountDetailsResourceLineItemsListResourceLineItemResourceTax = {
        ///The total amount of tax on the transaction represented in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). Required for L2 rates. An integer greater than or equal to 0.
        ///This field is mutually exclusive with the `amount_details[line_items][#][tax][total_tax_amount]` field.
        TotalTaxAmount: int
    }
    with
        static member New (totalTaxAmount: int) =
            {
                PaymentFlowsAmountDetailsResourceLineItemsListResourceLineItemResourceTax.TotalTaxAmount = totalTaxAmount //required
            }

    [<Struct>]
    type PaymentFlowsAmountDetailsResourceErrorCode =
        | AmountDetailsAmountMismatch
        | AmountDetailsTaxShippingDiscountGreaterThanAmount

    type PaymentFlowsAmountDetailsClientResourceTip = {
        ///Portion of the amount that corresponds to a tip.
        Amount: int option
    }
    with
        static member New (?amount: int) =
            {
                PaymentFlowsAmountDetailsClientResourceTip.Amount = amount
            }

    [<Struct>]
    type PaymentAttemptRecordReportedBy =
        | Self
        | Stripe

    [<Struct>]
    type PaymentAttemptRecordCustomerPresence =
        | OffSession
        | OnSession

    type PackageDimensions = {
        ///Height, in inches.
        Height: decimal
        ///Length, in inches.
        Length: decimal
        ///Weight, in ounces.
        Weight: decimal
        ///Width, in inches.
        Width: decimal
    }
    with
        static member New (height: decimal, length: decimal, weight: decimal, width: decimal) =
            {
                PackageDimensions.Height = height //required
                PackageDimensions.Length = length //required
                PackageDimensions.Weight = weight //required
                PackageDimensions.Width = width //required
            }

    [<Struct>]
    type OutboundTransfersPaymentMethodDetailsUsBankAccountNetwork =
        | Ach
        | UsDomesticWire

    [<Struct>]
    type OutboundTransfersPaymentMethodDetailsUsBankAccountAccountType =
        | Checking
        | Savings

    [<Struct>]
    type OutboundTransfersPaymentMethodDetailsUsBankAccountAccountHolderType =
        | Company
        | Individual

    type OutboundTransfersPaymentMethodDetailsFinancialAccount = {
        ///Token of the FinancialAccount.
        Id: string
    }
    with
        ///The rails used to send funds.
        member _.Network = "stripe"

        static member New (id: string) =
            {
                OutboundTransfersPaymentMethodDetailsFinancialAccount.Id = id //required
            }

    [<Struct>]
    type OutboundTransfersPaymentMethodDetailsType =
        | FinancialAccount
        | UsBankAccount

    [<Struct>]
    type OutboundPaymentsPaymentMethodDetailsUsBankAccountNetwork =
        | Ach
        | UsDomesticWire

    [<Struct>]
    type OutboundPaymentsPaymentMethodDetailsUsBankAccountAccountType =
        | Checking
        | Savings

    [<Struct>]
    type OutboundPaymentsPaymentMethodDetailsUsBankAccountAccountHolderType =
        | Company
        | Individual

    type OutboundPaymentsPaymentMethodDetailsFinancialAccount = {
        ///Token of the FinancialAccount.
        Id: string
    }
    with
        ///The rails used to send funds.
        member _.Network = "stripe"

        static member New (id: string) =
            {
                OutboundPaymentsPaymentMethodDetailsFinancialAccount.Id = id //required
            }

    [<Struct>]
    type OutboundPaymentsPaymentMethodDetailsType =
        | FinancialAccount
        | UsBankAccount

    type OnlineAcceptance = {
        ///The customer accepts the mandate from this IP address.
        IpAddress: string option
        ///The customer accepts the mandate using the user agent of the browser.
        UserAgent: string option
    }
    with
        static member New (ipAddress: string option, userAgent: string option) =
            {
                OnlineAcceptance.IpAddress = ipAddress //required
                OnlineAcceptance.UserAgent = userAgent //required
            }

    type OfflineAcceptance = {
        OfflineAcceptance: string option
    }
    with
        static member New (?offlineAcceptance: string option) =
            {
                OfflineAcceptance.OfflineAcceptance = offlineAcceptance |> Option.flatten
            }

    type NotificationEventRequest = {
        ///ID of the API request that caused the event. If null, the event was automatic (e.g., Stripe's automatic subscription handling). Request logs are available in the [dashboard](https://dashboard.stripe.com/logs), but currently not in the API.
        Id: string option
        ///The idempotency key transmitted during the request, if any. *Note: This property is populated only for events on or after May 23, 2017*.
        IdempotencyKey: string option
    }
    with
        static member New (id: string option, idempotencyKey: string option) =
            {
                NotificationEventRequest.Id = id //required
                NotificationEventRequest.IdempotencyKey = idempotencyKey //required
            }

    type NotificationEventData = {
        ///Object containing the API resource relevant to the event. For example, an `invoice.created` event will have a full [invoice object](https://api.stripe.com#invoice_object) as the value of the object key.
        Object: string
        ///Object containing the names of the updated attributes and their values prior to the event (only included in events of type `*.updated`). If an array attribute has any updated elements, this object contains the entire array. In Stripe API versions 2017-04-06 or earlier, an updated array attribute in this object includes only the updated array elements.
        PreviousAttributes: string option
    }
    with
        static member New (object: string, ?previousAttributes: string) =
            {
                NotificationEventData.Object = object //required
                NotificationEventData.PreviousAttributes = previousAttributes
            }

    type MandateUsBankAccount () = 
        ///Mandate collection method
        member _.CollectionMethod = "paper"


    [<Struct>]
    type MandateUpiAmountType =
        | Fixed
        | Maximum

    type MandateSepaDebit = {
        ///The unique reference of the mandate.
        Reference: string
        ///The URL of the mandate. This URL generally contains sensitive information about the customer and should be shared with them exclusively.
        Url: string
    }
    with
        static member New (reference: string, url: string) =
            {
                MandateSepaDebit.Reference = reference //required
                MandateSepaDebit.Url = url //required
            }

    type MandateRevolutPay = {
        MandateRevolutPay: string option
    }
    with
        static member New (?mandateRevolutPay: string option) =
            {
                MandateRevolutPay.MandateRevolutPay = mandateRevolutPay |> Option.flatten
            }

    [<Struct>]
    type MandatePixPaymentSchedule =
        | Halfyearly
        | Monthly
        | Quarterly
        | Weekly
        | Yearly

    [<Struct>]
    type MandatePixAmountType =
        | Fixed
        | Maximum

    [<Struct>]
    type MandatePixAmountIncludesIof =
        | Always
        | Never

    type MandatePaytoPurpose =
        | DependantSupport
        | Government
        | Loan
        | Mortgage
        | Other
        | Pension
        | Personal
        | Retail
        | Salary
        | Tax
        | Utility

    type MandatePaytoPaymentSchedule =
        | Adhoc
        | Annual
        | Daily
        | Fortnightly
        | Monthly
        | Quarterly
        | SemiAnnual
        | Weekly

    [<Struct>]
    type MandatePaytoAmountType =
        | Fixed
        | Maximum

    type MandatePaypal = {
        ///The PayPal Billing Agreement ID (BAID). This is an ID generated by PayPal which represents the mandate between the merchant and the customer.
        BillingAgreementId: string option
        ///PayPal account PayerID. This identifier uniquely identifies the PayPal customer.
        PayerId: string option
    }
    with
        static member New (billingAgreementId: string option, payerId: string option) =
            {
                MandatePaypal.BillingAgreementId = billingAgreementId //required
                MandatePaypal.PayerId = payerId //required
            }

    [<Struct>]
    type MandateOptionsUpiAmountType =
        | Fixed
        | Maximum

    type MandateOptionsPaytoPurpose =
        | DependantSupport
        | Government
        | Loan
        | Mortgage
        | Other
        | Pension
        | Personal
        | Retail
        | Salary
        | Tax
        | Utility

    type MandateOptionsPaytoPaymentSchedule =
        | Adhoc
        | Annual
        | Daily
        | Fortnightly
        | Monthly
        | Quarterly
        | SemiAnnual
        | Weekly

    [<Struct>]
    type MandateOptionsPaytoAmountType =
        | Fixed
        | Maximum

    type MandateNzBankAccount = {
        MandateNzBankAccount: string option
    }
    with
        static member New (?mandateNzBankAccount: string option) =
            {
                MandateNzBankAccount.MandateNzBankAccount = mandateNzBankAccount |> Option.flatten
            }

    type MandateNaverPay = {
        MandateNaverPay: string option
    }
    with
        static member New (?mandateNaverPay: string option) =
            {
                MandateNaverPay.MandateNaverPay = mandateNaverPay |> Option.flatten
            }

    type MandateLink = {
        MandateLink: string option
    }
    with
        static member New (?mandateLink: string option) =
            {
                MandateLink.MandateLink = mandateLink |> Option.flatten
            }

    type MandateKrCard = {
        MandateKrCard: string option
    }
    with
        static member New (?mandateKrCard: string option) =
            {
                MandateKrCard.MandateKrCard = mandateKrCard |> Option.flatten
            }

    type MandateKlarna = {
        MandateKlarna: string option
    }
    with
        static member New (?mandateKlarna: string option) =
            {
                MandateKlarna.MandateKlarna = mandateKlarna |> Option.flatten
            }

    type MandateKakaoPay = {
        MandateKakaoPay: string option
    }
    with
        static member New (?mandateKakaoPay: string option) =
            {
                MandateKakaoPay.MandateKakaoPay = mandateKakaoPay |> Option.flatten
            }

    type MandateCashapp = {
        MandateCashapp: string option
    }
    with
        static member New (?mandateCashapp: string option) =
            {
                MandateCashapp.MandateCashapp = mandateCashapp |> Option.flatten
            }

    [<Struct>]
    type MandateBacsDebitRevocationReason =
        | AccountClosed
        | BankAccountRestricted
        | BankOwnershipChanged
        | CouldNotProcess
        | DebitNotAuthorized

    [<Struct>]
    type MandateBacsDebitNetworkStatus =
        | Accepted
        | Pending
        | Refused
        | Revoked

    type MandateAuBecsDebit = {
        ///The URL of the mandate. This URL generally contains sensitive information about the customer and should be shared with them exclusively.
        Url: string
    }
    with
        static member New (url: string) =
            {
                MandateAuBecsDebit.Url = url //required
            }

    type MandateAmazonPay = {
        MandateAmazonPay: string option
    }
    with
        static member New (?mandateAmazonPay: string option) =
            {
                MandateAmazonPay.MandateAmazonPay = mandateAmazonPay |> Option.flatten
            }

    [<Struct>]
    type MandateAcssDebitDefaultFor =
        | Invoice
        | Subscription

    [<Struct>]
    type MandateAcssDebitTransactionType =
        | Business
        | Personal

    [<Struct>]
    type MandateAcssDebitPaymentSchedule =
        | Combined
        | Interval
        | Sporadic

    [<Struct>]
    type MandateType =
        | MultiUse
        | SingleUse

    [<Struct>]
    type MandateStatus =
        | Active
        | Inactive
        | Pending

    ///Login Links are single-use URLs that takes an Express account to the login page for their Stripe dashboard.
    ///A Login Link differs from an [Account Link](https://docs.stripe.com/api/account_links) in that it takes the user directly to their [Express dashboard for the specified account](https://docs.stripe.com/connect/integrate-express-dashboard#create-login-link)
    type LoginLink = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The URL for the login link.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "login_link"

        static member New (created: DateTime, url: string) =
            {
                LoginLink.Created = created //required
                LoginLink.Url = url //required
            }

    [<Struct>]
    type LinkedAccountOptionsCommonPrefetch =
        | Balances
        | Ownership
        | Transactions

    [<Struct>]
    type LinkedAccountOptionsCommonPermissions =
        | Balances
        | Ownership
        | PaymentMethod
        | Transactions

    type LineItemsTaxAmountTaxabilityReason =
        | CustomerExempt
        | NotCollecting
        | NotSubjectToTax
        | NotSupported
        | PortionProductExempt
        | PortionReducedRated
        | PortionStandardRated
        | ProductExempt
        | ProductExemptHoliday
        | ProportionallyRated
        | ReducedRated
        | ReverseCharge
        | StandardRated
        | TaxableBasisReduced
        | ZeroRated

    type LineItemsAdjustableQuantity = {
        Enabled: bool
        Maximum: int option
        Minimum: int option
    }
    with
        static member New (enabled: bool, maximum: int option, minimum: int option) =
            {
                LineItemsAdjustableQuantity.Enabled = enabled //required
                LineItemsAdjustableQuantity.Maximum = maximum //required
                LineItemsAdjustableQuantity.Minimum = minimum //required
            }

    type Level3LineItems = {
        DiscountAmount: int option
        ProductCode: string
        ProductDescription: string
        Quantity: int option
        TaxAmount: int option
        UnitCost: int option
    }
    with
        static member New (discountAmount: int option, productCode: string, productDescription: string, quantity: int option, taxAmount: int option, unitCost: int option) =
            {
                Level3LineItems.DiscountAmount = discountAmount //required
                Level3LineItems.ProductCode = productCode //required
                Level3LineItems.ProductDescription = productDescription //required
                Level3LineItems.Quantity = quantity //required
                Level3LineItems.TaxAmount = taxAmount //required
                Level3LineItems.UnitCost = unitCost //required
            }

    type LegalEntityUboDeclaration = {
        ///The Unix timestamp marking when the beneficial owner attestation was made.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Date: DateTime option
        ///The IP address from which the beneficial owner attestation was made.
        Ip: string option
        ///The user-agent string from the browser where the beneficial owner attestation was made.
        UserAgent: string option
    }
    with
        static member New (date: DateTime option, ip: string option, userAgent: string option) =
            {
                LegalEntityUboDeclaration.Date = date //required
                LegalEntityUboDeclaration.Ip = ip //required
                LegalEntityUboDeclaration.UserAgent = userAgent //required
            }

    type LegalEntityRepresentativeDeclaration = {
        ///The Unix timestamp marking when the representative declaration attestation was made.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Date: DateTime option
        ///The IP address from which the representative declaration attestation was made.
        Ip: string option
        ///The user-agent string from the browser where the representative declaration attestation was made.
        UserAgent: string option
    }
    with
        static member New (date: DateTime option, ip: string option, userAgent: string option) =
            {
                LegalEntityRepresentativeDeclaration.Date = date //required
                LegalEntityRepresentativeDeclaration.Ip = ip //required
                LegalEntityRepresentativeDeclaration.UserAgent = userAgent //required
            }

    type LegalEntityRegistrationDate = {
        ///The day of registration, between 1 and 31.
        Day: int option
        ///The month of registration, between 1 and 12.
        Month: int option
        ///The four-digit year of registration.
        Year: int option
    }
    with
        static member New (day: int option, month: int option, year: int option) =
            {
                LegalEntityRegistrationDate.Day = day //required
                LegalEntityRegistrationDate.Month = month //required
                LegalEntityRegistrationDate.Year = year //required
            }

    type LegalEntityPersonVerificationDocumentDetailsCode =
        | DocumentCorrupt
        | DocumentCountryNotSupported
        | DocumentExpired
        | DocumentFailedCopy
        | DocumentFailedOther
        | DocumentFailedTestMode
        | DocumentFraudulent
        | DocumentFailedGreyscale
        | DocumentIncomplete
        | DocumentInvalid
        | DocumentManipulated
        | DocumentMissingBack
        | DocumentMissingFront
        | DocumentNotReadable
        | DocumentNotUploaded
        | DocumentPhotoMismatch
        | DocumentTooLarge
        | DocumentTypeNotSupported

    [<Struct>]
    type LegalEntityPersonVerificationStatus =
        | Unverified
        | Pending
        | Verified

    type LegalEntityPersonVerificationDetailsCode =
        | DocumentAddressMismatch
        | DocumentDobMismatch
        | DocumentDuplicateType
        | DocumentIdNumberMismatch
        | DocumentNameMismatch
        | DocumentNationalityMismatch
        | FailedKeyedIdentity
        | FailedOther

    type LegalEntityDob = {
        ///The day of birth, between 1 and 31.
        Day: int option
        ///The month of birth, between 1 and 12.
        Month: int option
        ///The four-digit year of birth.
        Year: int option
    }
    with
        static member New (day: int option, month: int option, year: int option) =
            {
                LegalEntityDob.Day = day //required
                LegalEntityDob.Month = month //required
                LegalEntityDob.Year = year //required
            }

    type LegalEntityDirectorshipDeclaration = {
        ///The Unix timestamp marking when the directorship declaration attestation was made.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Date: DateTime option
        ///The IP address from which the directorship declaration attestation was made.
        Ip: string option
        ///The user-agent string from the browser where the directorship declaration attestation was made.
        UserAgent: string option
    }
    with
        static member New (date: DateTime option, ip: string option, userAgent: string option) =
            {
                LegalEntityDirectorshipDeclaration.Date = date //required
                LegalEntityDirectorshipDeclaration.Ip = ip //required
                LegalEntityDirectorshipDeclaration.UserAgent = userAgent //required
            }

    type LegalEntityCompanyVerificationDocumentDetailsCode =
        | DocumentCorrupt
        | DocumentExpired
        | DocumentFailedCopy
        | DocumentFailedGreyscale
        | DocumentFailedOther
        | DocumentFailedTestMode
        | DocumentFraudulent
        | DocumentIncomplete
        | DocumentInvalid
        | DocumentManipulated
        | DocumentNotReadable
        | DocumentNotUploaded
        | DocumentTypeNotSupported
        | DocumentTooLarge

    type LegalEntityCompanyStructure =
        | FreeZoneEstablishment
        | FreeZoneLlc
        | GovernmentInstrumentality
        | GovernmentalUnit
        | IncorporatedNonProfit
        | IncorporatedPartnership
        | LimitedLiabilityPartnership
        | Llc
        | MultiMemberLlc
        | PrivateCompany
        | PrivateCorporation
        | PrivatePartnership
        | PublicCompany
        | PublicCorporation
        | PublicPartnership
        | RegisteredCharity
        | SingleMemberLlc
        | SoleEstablishment
        | SoleProprietorship
        | TaxExemptGovernmentInstrumentality
        | UnincorporatedAssociation
        | UnincorporatedNonProfit
        | UnincorporatedPartnership

    [<Struct>]
    type LegalEntityCompanyOwnershipExemptionReason =
        | QualifiedEntityExceedsOwnershipThreshold
        | QualifiesAsFinancialInstitution

    type IssuingTransactionTreasury = {
        ///The Treasury [ReceivedCredit](https://docs.stripe.com/api/treasury/received_credits) representing this Issuing transaction if it is a refund
        ReceivedCredit: string option
        ///The Treasury [ReceivedDebit](https://docs.stripe.com/api/treasury/received_debits) representing this Issuing transaction if it is a capture
        ReceivedDebit: string option
    }
    with
        static member New (receivedCredit: string option, receivedDebit: string option) =
            {
                IssuingTransactionTreasury.ReceivedCredit = receivedCredit //required
                IssuingTransactionTreasury.ReceivedDebit = receivedDebit //required
            }

    type IssuingTransactionReceiptData = {
        ///The description of the item. The maximum length of this field is 26 characters.
        Description: string option
        ///The quantity of the item.
        Quantity: decimal option
        ///The total for this line item in cents.
        Total: int option
        ///The unit cost of the item in cents.
        UnitCost: int option
    }
    with
        static member New (description: string option, quantity: decimal option, total: int option, unitCost: int option) =
            {
                IssuingTransactionReceiptData.Description = description //required
                IssuingTransactionReceiptData.Quantity = quantity //required
                IssuingTransactionReceiptData.Total = total //required
                IssuingTransactionReceiptData.UnitCost = unitCost //required
            }

    type IssuingTransactionNetworkData = {
        ///A code created by Stripe which is shared with the merchant to validate the authorization. This field will be populated if the authorization message was approved. The code typically starts with the letter "S", followed by a six-digit number. For example, "S498162". Please note that the code is not guaranteed to be unique across authorizations.
        AuthorizationCode: string option
        ///The date the transaction was processed by the card network. This can be different from the date the seller recorded the transaction depending on when the acquirer submits the transaction to the network.
        ProcessingDate: string option
        ///Unique identifier for the authorization assigned by the card network used to match subsequent messages, disputes, and transactions.
        TransactionId: string option
    }
    with
        static member New (authorizationCode: string option, processingDate: string option, transactionId: string option) =
            {
                IssuingTransactionNetworkData.AuthorizationCode = authorizationCode //required
                IssuingTransactionNetworkData.ProcessingDate = processingDate //required
                IssuingTransactionNetworkData.TransactionId = transactionId //required
            }

    type IssuingTransactionLodgingData = {
        ///The time of checking into the lodging.
        CheckInAt: int option
        ///The number of nights stayed at the lodging.
        Nights: int option
    }
    with
        static member New (checkInAt: int option, nights: int option) =
            {
                IssuingTransactionLodgingData.CheckInAt = checkInAt //required
                IssuingTransactionLodgingData.Nights = nights //required
            }

    type IssuingTransactionFuelDataUnit =
        | ChargingMinute
        | ImperialGallon
        | Kilogram
        | KilowattHour
        | Liter
        | Pound
        | UsGallon
        | Other

    [<Struct>]
    type IssuingTransactionFuelDataType =
        | Diesel
        | UnleadedPlus
        | UnleadedRegular
        | UnleadedSuper
        | Other

    type IssuingTransactionFlightDataLeg = {
        ///The three-letter IATA airport code of the flight's destination.
        ArrivalAirportCode: string option
        ///The airline carrier code.
        Carrier: string option
        ///The three-letter IATA airport code that the flight departed from.
        DepartureAirportCode: string option
        ///The flight number.
        FlightNumber: string option
        ///The flight's service class.
        ServiceClass: string option
        ///Whether a stopover is allowed on this flight.
        StopoverAllowed: bool option
    }
    with
        static member New (arrivalAirportCode: string option, carrier: string option, departureAirportCode: string option, flightNumber: string option, serviceClass: string option, stopoverAllowed: bool option) =
            {
                IssuingTransactionFlightDataLeg.ArrivalAirportCode = arrivalAirportCode //required
                IssuingTransactionFlightDataLeg.Carrier = carrier //required
                IssuingTransactionFlightDataLeg.DepartureAirportCode = departureAirportCode //required
                IssuingTransactionFlightDataLeg.FlightNumber = flightNumber //required
                IssuingTransactionFlightDataLeg.ServiceClass = serviceClass //required
                IssuingTransactionFlightDataLeg.StopoverAllowed = stopoverAllowed //required
            }

    type IssuingTransactionFleetTaxData = {
        ///Amount of state or provincial Sales Tax included in the transaction amount. Null if not reported by merchant or not subject to tax.
        LocalAmountDecimal: string option
        ///Amount of national Sales Tax or VAT included in the transaction amount. Null if not reported by merchant or not subject to tax.
        NationalAmountDecimal: string option
    }
    with
        static member New (localAmountDecimal: string option, nationalAmountDecimal: string option) =
            {
                IssuingTransactionFleetTaxData.LocalAmountDecimal = localAmountDecimal //required
                IssuingTransactionFleetTaxData.NationalAmountDecimal = nationalAmountDecimal //required
            }

    type IssuingTransactionFleetNonFuelPriceData = {
        ///Gross non-fuel amount that should equal the sum of the line items, inclusive of taxes.
        GrossAmountDecimal: string option
    }
    with
        static member New (grossAmountDecimal: string option) =
            {
                IssuingTransactionFleetNonFuelPriceData.GrossAmountDecimal = grossAmountDecimal //required
            }

    type IssuingTransactionFleetFuelPriceData = {
        ///Gross fuel amount that should equal Fuel Volume multipled by Fuel Unit Cost, inclusive of taxes.
        GrossAmountDecimal: string option
    }
    with
        static member New (grossAmountDecimal: string option) =
            {
                IssuingTransactionFleetFuelPriceData.GrossAmountDecimal = grossAmountDecimal //required
            }

    [<Struct>]
    type IssuingTransactionFleetDataServiceType =
        | NonFuelTransaction
        | FullService
        | SelfService

    [<Struct>]
    type IssuingTransactionFleetDataPurchaseType =
        | FuelPurchase
        | NonFuelPurchase
        | FuelAndNonFuelPurchase

    type IssuingTransactionFleetCardholderPromptData = {
        ///Driver ID.
        DriverId: string option
        ///Odometer reading.
        Odometer: int option
        ///An alphanumeric ID. This field is used when a vehicle ID, driver ID, or generic ID is entered by the cardholder, but the merchant or card network did not specify the prompt type.
        UnspecifiedId: string option
        ///User ID.
        UserId: string option
        ///Vehicle number.
        VehicleNumber: string option
    }
    with
        static member New (driverId: string option, odometer: int option, unspecifiedId: string option, userId: string option, vehicleNumber: string option) =
            {
                IssuingTransactionFleetCardholderPromptData.DriverId = driverId //required
                IssuingTransactionFleetCardholderPromptData.Odometer = odometer //required
                IssuingTransactionFleetCardholderPromptData.UnspecifiedId = unspecifiedId //required
                IssuingTransactionFleetCardholderPromptData.UserId = userId //required
                IssuingTransactionFleetCardholderPromptData.VehicleNumber = vehicleNumber //required
            }

    type IssuingTransactionAmountDetails = {
        ///The fee charged by the ATM for the cash withdrawal.
        AtmFee: int option
        ///The amount of cash requested by the cardholder.
        CashbackAmount: int option
    }
    with
        static member New (atmFee: int option, cashbackAmount: int option) =
            {
                IssuingTransactionAmountDetails.AtmFee = atmFee //required
                IssuingTransactionAmountDetails.CashbackAmount = cashbackAmount //required
            }

    [<Struct>]
    type IssuingPhysicalBundleFeaturesSecondLine =
        | Optional
        | Required
        | Unsupported

    [<Struct>]
    type IssuingPhysicalBundleFeaturesCarrierText =
        | Optional
        | Required
        | Unsupported

    [<Struct>]
    type IssuingPhysicalBundleFeaturesCardLogo =
        | Optional
        | Required
        | Unsupported

    type IssuingPersonalizationDesignRejectionReasonsCarrierText =
        | GeographicLocation
        | Inappropriate
        | NetworkName
        | NonFiatCurrency
        | Other
        | OtherEntity
        | PromotionalMaterial

    type IssuingPersonalizationDesignRejectionReasonsCardLogo =
        | GeographicLocation
        | Inappropriate
        | NetworkName
        | NonBinaryImage
        | NonFiatCurrency
        | Other
        | OtherEntity
        | PromotionalMaterial

    type IssuingPersonalizationDesignPreferences = {
        ///Whether we use this personalization design to create cards when one isn't specified. A connected account uses the Connect platform's default design if no personalization design is set as the default design.
        IsDefault: bool
        ///Whether this personalization design is used to create cards when one is not specified and a default for this connected account does not exist.
        IsPlatformDefault: bool option
    }
    with
        static member New (isDefault: bool, isPlatformDefault: bool option) =
            {
                IssuingPersonalizationDesignPreferences.IsDefault = isDefault //required
                IssuingPersonalizationDesignPreferences.IsPlatformDefault = isPlatformDefault //required
            }

    type IssuingPersonalizationDesignCarrierText = {
        ///The footer body text of the carrier letter.
        FooterBody: string option
        ///The footer title text of the carrier letter.
        FooterTitle: string option
        ///The header body text of the carrier letter.
        HeaderBody: string option
        ///The header title text of the carrier letter.
        HeaderTitle: string option
    }
    with
        static member New (footerBody: string option, footerTitle: string option, headerBody: string option, headerTitle: string option) =
            {
                IssuingPersonalizationDesignCarrierText.FooterBody = footerBody //required
                IssuingPersonalizationDesignCarrierText.FooterTitle = footerTitle //required
                IssuingPersonalizationDesignCarrierText.HeaderBody = headerBody //required
                IssuingPersonalizationDesignCarrierText.HeaderTitle = headerTitle //required
            }

    type IssuingNetworkTokenWalletProviderReasonCodes =
        | AccountCardTooNew
        | AccountRecentlyChanged
        | AccountTooNew
        | AccountTooNewSinceLaunch
        | AdditionalDevice
        | DataExpired
        | DeferIdVDecision
        | DeviceRecentlyLost
        | GoodActivityHistory
        | HasSuspendedTokens
        | HighRisk
        | InactiveAccount
        | LongAccountTenure
        | LowAccountScore
        | LowDeviceScore
        | LowPhoneNumberScore
        | NetworkServiceError
        | OutsideHomeTerritory
        | ProvisioningCardholderMismatch
        | ProvisioningDeviceAndCardholderMismatch
        | ProvisioningDeviceMismatch
        | SameDeviceNoPriorAuthentication
        | SameDeviceSuccessfulPriorAuthentication
        | SoftwareUpdate
        | SuspiciousActivity
        | TooManyDifferentCardholders
        | TooManyRecentAttempts
        | TooManyRecentTokens

    [<Struct>]
    type IssuingNetworkTokenWalletProviderSuggestedDecision =
        | Approve
        | Decline
        | RequireAuth

    [<Struct>]
    type IssuingNetworkTokenWalletProviderCardNumberSource =
        | App
        | Manual
        | OnFile
        | Other

    type IssuingNetworkTokenVisa = {
        ///A unique reference ID from Visa to represent the card account number.
        CardReferenceId: string option
        ///The network-unique identifier for the token.
        TokenReferenceId: string
        ///The ID of the entity requesting tokenization, specific to Visa.
        TokenRequestorId: string
        ///Degree of risk associated with the token between `01` and `99`, with higher number indicating higher risk. A `00` value indicates the token was not scored by Visa.
        TokenRiskScore: string option
    }
    with
        static member New (cardReferenceId: string option, tokenReferenceId: string, tokenRequestorId: string, ?tokenRiskScore: string) =
            {
                IssuingNetworkTokenVisa.CardReferenceId = cardReferenceId //required
                IssuingNetworkTokenVisa.TokenReferenceId = tokenReferenceId //required
                IssuingNetworkTokenVisa.TokenRequestorId = tokenRequestorId //required
                IssuingNetworkTokenVisa.TokenRiskScore = tokenRiskScore
            }

    [<Struct>]
    type IssuingNetworkTokenNetworkDataType =
        | Mastercard
        | Visa

    type IssuingNetworkTokenMastercard = {
        ///A unique reference ID from MasterCard to represent the card account number.
        CardReferenceId: string option
        ///The network-unique identifier for the token.
        TokenReferenceId: string
        ///The ID of the entity requesting tokenization, specific to MasterCard.
        TokenRequestorId: string
        ///The name of the entity requesting tokenization, if known. This is directly provided from MasterCard.
        TokenRequestorName: string option
    }
    with
        static member New (tokenReferenceId: string, tokenRequestorId: string, ?cardReferenceId: string, ?tokenRequestorName: string) =
            {
                IssuingNetworkTokenMastercard.TokenReferenceId = tokenReferenceId //required
                IssuingNetworkTokenMastercard.TokenRequestorId = tokenRequestorId //required
                IssuingNetworkTokenMastercard.CardReferenceId = cardReferenceId
                IssuingNetworkTokenMastercard.TokenRequestorName = tokenRequestorName
            }

    [<Struct>]
    type IssuingNetworkTokenDeviceType =
        | Other
        | Phone
        | Watch

    type IssuingNetworkTokenAddress = {
        ///The street address of the cardholder tokenizing the card.
        [<JsonField(Name="line1")>]Line1: string
        ///The postal code of the cardholder tokenizing the card.
        PostalCode: string
    }
    with
        static member New (line1: string, postalCode: string) =
            {
                IssuingNetworkTokenAddress.Line1 = line1 //required
                IssuingNetworkTokenAddress.PostalCode = postalCode //required
            }

    type IssuingDisputeTreasury = {
        ///The Treasury [DebitReversal](https://docs.stripe.com/api/treasury/debit_reversals) representing this Issuing dispute
        DebitReversal: string option
        ///The Treasury [ReceivedDebit](https://docs.stripe.com/api/treasury/received_debits) that is being disputed.
        ReceivedDebit: string
    }
    with
        static member New (debitReversal: string option, receivedDebit: string) =
            {
                IssuingDisputeTreasury.DebitReversal = debitReversal //required
                IssuingDisputeTreasury.ReceivedDebit = receivedDebit //required
            }

    [<Struct>]
    type IssuingDisputeOtherEvidenceProductType =
        | Merchandise
        | Service

    [<Struct>]
    type IssuingDisputeNotReceivedEvidenceProductType =
        | Merchandise
        | Service

    [<Struct>]
    type IssuingDisputeMerchandiseNotAsDescribedEvidenceReturnStatus =
        | MerchantRejected
        | Successful

    type IssuingDisputeEvidenceReason =
        | Canceled
        | Duplicate
        | Fraudulent
        | MerchandiseNotAsDescribed
        | NoValidAuthorization
        | NotReceived
        | Other
        | ServiceNotAsDescribed

    [<Struct>]
    type IssuingDisputeCanceledEvidenceReturnStatus =
        | MerchantRejected
        | Successful

    [<Struct>]
    type IssuingDisputeCanceledEvidenceProductType =
        | Merchandise
        | Service

    type IssuingCardholderUserTermsAcceptance = {
        ///The Unix timestamp marking when the cardholder accepted the Authorized User Terms.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Date: DateTime option
        ///The IP address from which the cardholder accepted the Authorized User Terms.
        Ip: string option
        ///The user agent of the browser from which the cardholder accepted the Authorized User Terms.
        UserAgent: string option
    }
    with
        static member New (date: DateTime option, ip: string option, userAgent: string option) =
            {
                IssuingCardholderUserTermsAcceptance.Date = date //required
                IssuingCardholderUserTermsAcceptance.Ip = ip //required
                IssuingCardholderUserTermsAcceptance.UserAgent = userAgent //required
            }

    type IssuingCardholderSpendingLimitCategories =
        | AcRefrigerationRepair
        | AccountingBookkeepingServices
        | AdvertisingServices
        | AgriculturalCooperative
        | AirlinesAirCarriers
        | AirportsFlyingFields
        | AmbulanceServices
        | AmusementParksCarnivals
        | AntiqueReproductions
        | AntiqueShops
        | Aquariums
        | ArchitecturalSurveyingServices
        | ArtDealersAndGalleries
        | ArtistsSupplyAndCraftShops
        | AutoAndHomeSupplyStores
        | AutoBodyRepairShops
        | AutoPaintShops
        | AutoServiceShops
        | AutomatedCashDisburse
        | AutomatedFuelDispensers
        | AutomobileAssociations
        | AutomotivePartsAndAccessoriesStores
        | AutomotiveTireStores
        | BailAndBondPayments
        | Bakeries
        | BandsOrchestras
        | BarberAndBeautyShops
        | BettingCasinoGambling
        | BicycleShops
        | BilliardPoolEstablishments
        | BoatDealers
        | BoatRentalsAndLeases
        | BookStores
        | BooksPeriodicalsAndNewspapers
        | BowlingAlleys
        | BusLines
        | BusinessSecretarialSchools
        | BuyingShoppingServices
        | CableSatelliteAndOtherPayTelevisionAndRadio
        | CameraAndPhotographicSupplyStores
        | CandyNutAndConfectioneryStores
        | CarAndTruckDealersNewUsed
        | CarAndTruckDealersUsedOnly
        | CarRentalAgencies
        | CarWashes
        | CarpentryServices
        | CarpetUpholsteryCleaning
        | Caterers
        | CharitableAndSocialServiceOrganizationsFundraising
        | ChemicalsAndAlliedProducts
        | ChildCareServices
        | ChildrensAndInfantsWearStores
        | ChiropodistsPodiatrists
        | Chiropractors
        | CigarStoresAndStands
        | CivicSocialFraternalAssociations
        | CleaningAndMaintenance
        | ClothingRental
        | CollegesUniversities
        | CommercialEquipment
        | CommercialFootwear
        | CommercialPhotographyArtAndGraphics
        | CommuterTransportAndFerries
        | ComputerNetworkServices
        | ComputerProgramming
        | ComputerRepair
        | ComputerSoftwareStores
        | ComputersPeripheralsAndSoftware
        | ConcreteWorkServices
        | ConstructionMaterials
        | ConsultingPublicRelations
        | CorrespondenceSchools
        | CosmeticStores
        | CounselingServices
        | CountryClubs
        | CourierServices
        | CourtCosts
        | CreditReportingAgencies
        | CruiseLines
        | DairyProductsStores
        | DanceHallStudiosSchools
        | DatingEscortServices
        | DentistsOrthodontists
        | DepartmentStores
        | DetectiveAgencies
        | DigitalGoodsApplications
        | DigitalGoodsGames
        | DigitalGoodsLargeVolume
        | DigitalGoodsMedia
        | DirectMarketingCatalogMerchant
        | DirectMarketingCombinationCatalogAndRetailMerchant
        | DirectMarketingInboundTelemarketing
        | DirectMarketingInsuranceServices
        | DirectMarketingOther
        | DirectMarketingOutboundTelemarketing
        | DirectMarketingSubscription
        | DirectMarketingTravel
        | DiscountStores
        | Doctors
        | DoorToDoorSales
        | DraperyWindowCoveringAndUpholsteryStores
        | DrinkingPlaces
        | DrugStoresAndPharmacies
        | DrugsDrugProprietariesAndDruggistSundries
        | DryCleaners
        | DurableGoods
        | DutyFreeStores
        | EatingPlacesRestaurants
        | EducationalServices
        | ElectricRazorStores
        | ElectricVehicleCharging
        | ElectricalPartsAndEquipment
        | ElectricalServices
        | ElectronicsRepairShops
        | ElectronicsStores
        | ElementarySecondarySchools
        | EmergencyServicesGcasVisaUseOnly
        | EmploymentTempAgencies
        | EquipmentRental
        | ExterminatingServices
        | FamilyClothingStores
        | FastFoodRestaurants
        | FinancialInstitutions
        | FinesGovernmentAdministrativeEntities
        | FireplaceFireplaceScreensAndAccessoriesStores
        | FloorCoveringStores
        | Florists
        | FloristsSuppliesNurseryStockAndFlowers
        | FreezerAndLockerMeatProvisioners
        | FuelDealersNonAutomotive
        | FuneralServicesCrematories
        | FurnitureHomeFurnishingsAndEquipmentStoresExceptAppliances
        | FurnitureRepairRefinishing
        | FurriersAndFurShops
        | GeneralServices
        | GiftCardNoveltyAndSouvenirShops
        | GlassPaintAndWallpaperStores
        | GlasswareCrystalStores
        | GolfCoursesPublic
        | GovernmentLicensedHorseDogRacingUsRegionOnly
        | GovernmentLicensedOnlineCasionsOnlineGamblingUsRegionOnly
        | GovernmentOwnedLotteriesNonUsRegion
        | GovernmentOwnedLotteriesUsRegionOnly
        | GovernmentServices
        | GroceryStoresSupermarkets
        | HardwareEquipmentAndSupplies
        | HardwareStores
        | HealthAndBeautySpas
        | HearingAidsSalesAndSupplies
        | HeatingPlumbingAC
        | HobbyToyAndGameShops
        | HomeSupplyWarehouseStores
        | Hospitals
        | HotelsMotelsAndResorts
        | HouseholdApplianceStores
        | IndustrialSupplies
        | InformationRetrievalServices
        | InsuranceDefault
        | InsuranceUnderwritingPremiums
        | IntraCompanyPurchases
        | JewelryStoresWatchesClocksAndSilverwareStores
        | LandscapingServices
        | Laundries
        | LaundryCleaningServices
        | LegalServicesAttorneys
        | LuggageAndLeatherGoodsStores
        | LumberBuildingMaterialsStores
        | ManualCashDisburse
        | MarinasServiceAndSupplies
        | Marketplaces
        | MasonryStoneworkAndPlaster
        | MassageParlors
        | MedicalAndDentalLabs
        | MedicalDentalOphthalmicAndHospitalEquipmentAndSupplies
        | MedicalServices
        | MembershipOrganizations
        | MensAndBoysClothingAndAccessoriesStores
        | MensWomensClothingStores
        | MetalServiceCenters
        | Miscellaneous
        | MiscellaneousApparelAndAccessoryShops
        | MiscellaneousAutoDealers
        | MiscellaneousBusinessServices
        | MiscellaneousFoodStores
        | MiscellaneousGeneralMerchandise
        | MiscellaneousGeneralServices
        | MiscellaneousHomeFurnishingSpecialtyStores
        | MiscellaneousPublishingAndPrinting
        | MiscellaneousRecreationServices
        | MiscellaneousRepairShops
        | MiscellaneousSpecialtyRetail
        | MobileHomeDealers
        | MotionPictureTheaters
        | MotorFreightCarriersAndTrucking
        | MotorHomesDealers
        | MotorVehicleSuppliesAndNewParts
        | MotorcycleShopsAndDealers
        | MotorcycleShopsDealers
        | MusicStoresMusicalInstrumentsPianosAndSheetMusic
        | NewsDealersAndNewsstands
        | NonFiMoneyOrders
        | NonFiStoredValueCardPurchaseLoad
        | NondurableGoods
        | NurseriesLawnAndGardenSupplyStores
        | NursingPersonalCare
        | OfficeAndCommercialFurniture
        | OpticiansEyeglasses
        | OptometristsOphthalmologist
        | OrthopedicGoodsProstheticDevices
        | Osteopaths
        | PackageStoresBeerWineAndLiquor
        | PaintsVarnishesAndSupplies
        | ParkingLotsGarages
        | PassengerRailways
        | PawnShops
        | PetShopsPetFoodAndSupplies
        | PetroleumAndPetroleumProducts
        | PhotoDeveloping
        | PhotographicPhotocopyMicrofilmEquipmentAndSupplies
        | PhotographicStudios
        | PictureVideoProduction
        | PieceGoodsNotionsAndOtherDryGoods
        | PlumbingHeatingEquipmentAndSupplies
        | PoliticalOrganizations
        | PostalServicesGovernmentOnly
        | PreciousStonesAndMetalsWatchesAndJewelry
        | ProfessionalServices
        | PublicWarehousingAndStorage
        | QuickCopyReproAndBlueprint
        | Railroads
        | RealEstateAgentsAndManagersRentals
        | RecordStores
        | RecreationalVehicleRentals
        | ReligiousGoodsStores
        | ReligiousOrganizations
        | RoofingSidingSheetMetal
        | SecretarialSupportServices
        | SecurityBrokersDealers
        | ServiceStations
        | SewingNeedleworkFabricAndPieceGoodsStores
        | ShoeRepairHatCleaning
        | ShoeStores
        | SmallApplianceRepair
        | SnowmobileDealers
        | SpecialTradeServices
        | SpecialtyCleaning
        | SportingGoodsStores
        | SportingRecreationCamps
        | SportsAndRidingApparelStores
        | SportsClubsFields
        | StampAndCoinStores
        | StationaryOfficeSuppliesPrintingAndWritingPaper
        | StationeryStoresOfficeAndSchoolSupplyStores
        | SwimmingPoolsSales
        | TUiTravelGermany
        | TailorsAlterations
        | TaxPaymentsGovernmentAgencies
        | TaxPreparationServices
        | TaxicabsLimousines
        | TelecommunicationEquipmentAndTelephoneSales
        | TelecommunicationServices
        | TelegraphServices
        | TentAndAwningShops
        | TestingLaboratories
        | TheatricalTicketAgencies
        | Timeshares
        | TireRetreadingAndRepair
        | TollsBridgeFees
        | TouristAttractionsAndExhibits
        | TowingServices
        | TrailerParksCampgrounds
        | TransportationServices
        | TravelAgenciesTourOperators
        | TruckStopIteration
        | TruckUtilityTrailerRentals
        | TypesettingPlateMakingAndRelatedServices
        | TypewriterStores
        | USFederalGovernmentAgenciesOrDepartments
        | UniformsCommercialClothing
        | UsedMerchandiseAndSecondhandStores
        | Utilities
        | VarietyStores
        | VeterinaryServices
        | VideoAmusementGameSupplies
        | VideoGameArcades
        | VideoTapeRentalStores
        | VocationalTradeSchools
        | WatchJewelryRepair
        | WeldingRepair
        | WholesaleClubs
        | WigAndToupeeStores
        | WiresMoneyOrders
        | WomensAccessoryAndSpecialtyShops
        | WomensReadyToWearStores
        | WreckingAndSalvageYards

    type IssuingCardholderSpendingLimitInterval =
        | AllTime
        | Daily
        | Monthly
        | PerAuthorization
        | Weekly
        | Yearly

    type IssuingCardholderRequirementsPastDue =
        | [<JsonUnionCase("company.tax_id")>] CompanyTaxId
        | [<JsonUnionCase("individual.card_issuing.user_terms_acceptance.date")>] IndividualCardIssuingUserTermsAcceptanceDate
        | [<JsonUnionCase("individual.card_issuing.user_terms_acceptance.ip")>] IndividualCardIssuingUserTermsAcceptanceIp
        | [<JsonUnionCase("individual.dob.day")>] IndividualDobDay
        | [<JsonUnionCase("individual.dob.month")>] IndividualDobMonth
        | [<JsonUnionCase("individual.dob.year")>] IndividualDobYear
        | [<JsonUnionCase("individual.first_name")>] IndividualFirstName
        | [<JsonUnionCase("individual.last_name")>] IndividualLastName
        | [<JsonUnionCase("individual.verification.document")>] IndividualVerificationDocument

    [<Struct>]
    type IssuingCardholderRequirementsDisabledReason =
        | Listed
        | [<JsonUnionCase("rejected.listed")>] RejectedListed
        | [<JsonUnionCase("requirements.past_due")>] RequirementsPastDue
        | UnderReview

    type IssuingCardholderIndividualDob = {
        ///The day of birth, between 1 and 31.
        Day: int option
        ///The month of birth, between 1 and 12.
        Month: int option
        ///The four-digit year of birth.
        Year: int option
    }
    with
        static member New (day: int option, month: int option, year: int option) =
            {
                IssuingCardholderIndividualDob.Day = day //required
                IssuingCardholderIndividualDob.Month = month //required
                IssuingCardholderIndividualDob.Year = year //required
            }

    type IssuingCardholderCompany = {
        ///Whether the company's business ID number was provided.
        TaxIdProvided: bool
    }
    with
        static member New (taxIdProvided: bool) =
            {
                IssuingCardholderCompany.TaxIdProvided = taxIdProvided //required
            }

    type IssuingCardholderAuthorizationControlsBlockedCategories =
        | AcRefrigerationRepair
        | AccountingBookkeepingServices
        | AdvertisingServices
        | AgriculturalCooperative
        | AirlinesAirCarriers
        | AirportsFlyingFields
        | AmbulanceServices
        | AmusementParksCarnivals
        | AntiqueReproductions
        | AntiqueShops
        | Aquariums
        | ArchitecturalSurveyingServices
        | ArtDealersAndGalleries
        | ArtistsSupplyAndCraftShops
        | AutoAndHomeSupplyStores
        | AutoBodyRepairShops
        | AutoPaintShops
        | AutoServiceShops
        | AutomatedCashDisburse
        | AutomatedFuelDispensers
        | AutomobileAssociations
        | AutomotivePartsAndAccessoriesStores
        | AutomotiveTireStores
        | BailAndBondPayments
        | Bakeries
        | BandsOrchestras
        | BarberAndBeautyShops
        | BettingCasinoGambling
        | BicycleShops
        | BilliardPoolEstablishments
        | BoatDealers
        | BoatRentalsAndLeases
        | BookStores
        | BooksPeriodicalsAndNewspapers
        | BowlingAlleys
        | BusLines
        | BusinessSecretarialSchools
        | BuyingShoppingServices
        | CableSatelliteAndOtherPayTelevisionAndRadio
        | CameraAndPhotographicSupplyStores
        | CandyNutAndConfectioneryStores
        | CarAndTruckDealersNewUsed
        | CarAndTruckDealersUsedOnly
        | CarRentalAgencies
        | CarWashes
        | CarpentryServices
        | CarpetUpholsteryCleaning
        | Caterers
        | CharitableAndSocialServiceOrganizationsFundraising
        | ChemicalsAndAlliedProducts
        | ChildCareServices
        | ChildrensAndInfantsWearStores
        | ChiropodistsPodiatrists
        | Chiropractors
        | CigarStoresAndStands
        | CivicSocialFraternalAssociations
        | CleaningAndMaintenance
        | ClothingRental
        | CollegesUniversities
        | CommercialEquipment
        | CommercialFootwear
        | CommercialPhotographyArtAndGraphics
        | CommuterTransportAndFerries
        | ComputerNetworkServices
        | ComputerProgramming
        | ComputerRepair
        | ComputerSoftwareStores
        | ComputersPeripheralsAndSoftware
        | ConcreteWorkServices
        | ConstructionMaterials
        | ConsultingPublicRelations
        | CorrespondenceSchools
        | CosmeticStores
        | CounselingServices
        | CountryClubs
        | CourierServices
        | CourtCosts
        | CreditReportingAgencies
        | CruiseLines
        | DairyProductsStores
        | DanceHallStudiosSchools
        | DatingEscortServices
        | DentistsOrthodontists
        | DepartmentStores
        | DetectiveAgencies
        | DigitalGoodsApplications
        | DigitalGoodsGames
        | DigitalGoodsLargeVolume
        | DigitalGoodsMedia
        | DirectMarketingCatalogMerchant
        | DirectMarketingCombinationCatalogAndRetailMerchant
        | DirectMarketingInboundTelemarketing
        | DirectMarketingInsuranceServices
        | DirectMarketingOther
        | DirectMarketingOutboundTelemarketing
        | DirectMarketingSubscription
        | DirectMarketingTravel
        | DiscountStores
        | Doctors
        | DoorToDoorSales
        | DraperyWindowCoveringAndUpholsteryStores
        | DrinkingPlaces
        | DrugStoresAndPharmacies
        | DrugsDrugProprietariesAndDruggistSundries
        | DryCleaners
        | DurableGoods
        | DutyFreeStores
        | EatingPlacesRestaurants
        | EducationalServices
        | ElectricRazorStores
        | ElectricVehicleCharging
        | ElectricalPartsAndEquipment
        | ElectricalServices
        | ElectronicsRepairShops
        | ElectronicsStores
        | ElementarySecondarySchools
        | EmergencyServicesGcasVisaUseOnly
        | EmploymentTempAgencies
        | EquipmentRental
        | ExterminatingServices
        | FamilyClothingStores
        | FastFoodRestaurants
        | FinancialInstitutions
        | FinesGovernmentAdministrativeEntities
        | FireplaceFireplaceScreensAndAccessoriesStores
        | FloorCoveringStores
        | Florists
        | FloristsSuppliesNurseryStockAndFlowers
        | FreezerAndLockerMeatProvisioners
        | FuelDealersNonAutomotive
        | FuneralServicesCrematories
        | FurnitureHomeFurnishingsAndEquipmentStoresExceptAppliances
        | FurnitureRepairRefinishing
        | FurriersAndFurShops
        | GeneralServices
        | GiftCardNoveltyAndSouvenirShops
        | GlassPaintAndWallpaperStores
        | GlasswareCrystalStores
        | GolfCoursesPublic
        | GovernmentLicensedHorseDogRacingUsRegionOnly
        | GovernmentLicensedOnlineCasionsOnlineGamblingUsRegionOnly
        | GovernmentOwnedLotteriesNonUsRegion
        | GovernmentOwnedLotteriesUsRegionOnly
        | GovernmentServices
        | GroceryStoresSupermarkets
        | HardwareEquipmentAndSupplies
        | HardwareStores
        | HealthAndBeautySpas
        | HearingAidsSalesAndSupplies
        | HeatingPlumbingAC
        | HobbyToyAndGameShops
        | HomeSupplyWarehouseStores
        | Hospitals
        | HotelsMotelsAndResorts
        | HouseholdApplianceStores
        | IndustrialSupplies
        | InformationRetrievalServices
        | InsuranceDefault
        | InsuranceUnderwritingPremiums
        | IntraCompanyPurchases
        | JewelryStoresWatchesClocksAndSilverwareStores
        | LandscapingServices
        | Laundries
        | LaundryCleaningServices
        | LegalServicesAttorneys
        | LuggageAndLeatherGoodsStores
        | LumberBuildingMaterialsStores
        | ManualCashDisburse
        | MarinasServiceAndSupplies
        | Marketplaces
        | MasonryStoneworkAndPlaster
        | MassageParlors
        | MedicalAndDentalLabs
        | MedicalDentalOphthalmicAndHospitalEquipmentAndSupplies
        | MedicalServices
        | MembershipOrganizations
        | MensAndBoysClothingAndAccessoriesStores
        | MensWomensClothingStores
        | MetalServiceCenters
        | Miscellaneous
        | MiscellaneousApparelAndAccessoryShops
        | MiscellaneousAutoDealers
        | MiscellaneousBusinessServices
        | MiscellaneousFoodStores
        | MiscellaneousGeneralMerchandise
        | MiscellaneousGeneralServices
        | MiscellaneousHomeFurnishingSpecialtyStores
        | MiscellaneousPublishingAndPrinting
        | MiscellaneousRecreationServices
        | MiscellaneousRepairShops
        | MiscellaneousSpecialtyRetail
        | MobileHomeDealers
        | MotionPictureTheaters
        | MotorFreightCarriersAndTrucking
        | MotorHomesDealers
        | MotorVehicleSuppliesAndNewParts
        | MotorcycleShopsAndDealers
        | MotorcycleShopsDealers
        | MusicStoresMusicalInstrumentsPianosAndSheetMusic
        | NewsDealersAndNewsstands
        | NonFiMoneyOrders
        | NonFiStoredValueCardPurchaseLoad
        | NondurableGoods
        | NurseriesLawnAndGardenSupplyStores
        | NursingPersonalCare
        | OfficeAndCommercialFurniture
        | OpticiansEyeglasses
        | OptometristsOphthalmologist
        | OrthopedicGoodsProstheticDevices
        | Osteopaths
        | PackageStoresBeerWineAndLiquor
        | PaintsVarnishesAndSupplies
        | ParkingLotsGarages
        | PassengerRailways
        | PawnShops
        | PetShopsPetFoodAndSupplies
        | PetroleumAndPetroleumProducts
        | PhotoDeveloping
        | PhotographicPhotocopyMicrofilmEquipmentAndSupplies
        | PhotographicStudios
        | PictureVideoProduction
        | PieceGoodsNotionsAndOtherDryGoods
        | PlumbingHeatingEquipmentAndSupplies
        | PoliticalOrganizations
        | PostalServicesGovernmentOnly
        | PreciousStonesAndMetalsWatchesAndJewelry
        | ProfessionalServices
        | PublicWarehousingAndStorage
        | QuickCopyReproAndBlueprint
        | Railroads
        | RealEstateAgentsAndManagersRentals
        | RecordStores
        | RecreationalVehicleRentals
        | ReligiousGoodsStores
        | ReligiousOrganizations
        | RoofingSidingSheetMetal
        | SecretarialSupportServices
        | SecurityBrokersDealers
        | ServiceStations
        | SewingNeedleworkFabricAndPieceGoodsStores
        | ShoeRepairHatCleaning
        | ShoeStores
        | SmallApplianceRepair
        | SnowmobileDealers
        | SpecialTradeServices
        | SpecialtyCleaning
        | SportingGoodsStores
        | SportingRecreationCamps
        | SportsAndRidingApparelStores
        | SportsClubsFields
        | StampAndCoinStores
        | StationaryOfficeSuppliesPrintingAndWritingPaper
        | StationeryStoresOfficeAndSchoolSupplyStores
        | SwimmingPoolsSales
        | TUiTravelGermany
        | TailorsAlterations
        | TaxPaymentsGovernmentAgencies
        | TaxPreparationServices
        | TaxicabsLimousines
        | TelecommunicationEquipmentAndTelephoneSales
        | TelecommunicationServices
        | TelegraphServices
        | TentAndAwningShops
        | TestingLaboratories
        | TheatricalTicketAgencies
        | Timeshares
        | TireRetreadingAndRepair
        | TollsBridgeFees
        | TouristAttractionsAndExhibits
        | TowingServices
        | TrailerParksCampgrounds
        | TransportationServices
        | TravelAgenciesTourOperators
        | TruckStopIteration
        | TruckUtilityTrailerRentals
        | TypesettingPlateMakingAndRelatedServices
        | TypewriterStores
        | USFederalGovernmentAgenciesOrDepartments
        | UniformsCommercialClothing
        | UsedMerchandiseAndSecondhandStores
        | Utilities
        | VarietyStores
        | VeterinaryServices
        | VideoAmusementGameSupplies
        | VideoGameArcades
        | VideoTapeRentalStores
        | VocationalTradeSchools
        | WatchJewelryRepair
        | WeldingRepair
        | WholesaleClubs
        | WigAndToupeeStores
        | WiresMoneyOrders
        | WomensAccessoryAndSpecialtyShops
        | WomensReadyToWearStores
        | WreckingAndSalvageYards

    [<Struct>]
    type IssuingCardholderAuthorizationControlsBlockedCardPresences =
        | NotPresent
        | Present

    type IssuingCardholderAuthorizationControlsAllowedCategories =
        | AcRefrigerationRepair
        | AccountingBookkeepingServices
        | AdvertisingServices
        | AgriculturalCooperative
        | AirlinesAirCarriers
        | AirportsFlyingFields
        | AmbulanceServices
        | AmusementParksCarnivals
        | AntiqueReproductions
        | AntiqueShops
        | Aquariums
        | ArchitecturalSurveyingServices
        | ArtDealersAndGalleries
        | ArtistsSupplyAndCraftShops
        | AutoAndHomeSupplyStores
        | AutoBodyRepairShops
        | AutoPaintShops
        | AutoServiceShops
        | AutomatedCashDisburse
        | AutomatedFuelDispensers
        | AutomobileAssociations
        | AutomotivePartsAndAccessoriesStores
        | AutomotiveTireStores
        | BailAndBondPayments
        | Bakeries
        | BandsOrchestras
        | BarberAndBeautyShops
        | BettingCasinoGambling
        | BicycleShops
        | BilliardPoolEstablishments
        | BoatDealers
        | BoatRentalsAndLeases
        | BookStores
        | BooksPeriodicalsAndNewspapers
        | BowlingAlleys
        | BusLines
        | BusinessSecretarialSchools
        | BuyingShoppingServices
        | CableSatelliteAndOtherPayTelevisionAndRadio
        | CameraAndPhotographicSupplyStores
        | CandyNutAndConfectioneryStores
        | CarAndTruckDealersNewUsed
        | CarAndTruckDealersUsedOnly
        | CarRentalAgencies
        | CarWashes
        | CarpentryServices
        | CarpetUpholsteryCleaning
        | Caterers
        | CharitableAndSocialServiceOrganizationsFundraising
        | ChemicalsAndAlliedProducts
        | ChildCareServices
        | ChildrensAndInfantsWearStores
        | ChiropodistsPodiatrists
        | Chiropractors
        | CigarStoresAndStands
        | CivicSocialFraternalAssociations
        | CleaningAndMaintenance
        | ClothingRental
        | CollegesUniversities
        | CommercialEquipment
        | CommercialFootwear
        | CommercialPhotographyArtAndGraphics
        | CommuterTransportAndFerries
        | ComputerNetworkServices
        | ComputerProgramming
        | ComputerRepair
        | ComputerSoftwareStores
        | ComputersPeripheralsAndSoftware
        | ConcreteWorkServices
        | ConstructionMaterials
        | ConsultingPublicRelations
        | CorrespondenceSchools
        | CosmeticStores
        | CounselingServices
        | CountryClubs
        | CourierServices
        | CourtCosts
        | CreditReportingAgencies
        | CruiseLines
        | DairyProductsStores
        | DanceHallStudiosSchools
        | DatingEscortServices
        | DentistsOrthodontists
        | DepartmentStores
        | DetectiveAgencies
        | DigitalGoodsApplications
        | DigitalGoodsGames
        | DigitalGoodsLargeVolume
        | DigitalGoodsMedia
        | DirectMarketingCatalogMerchant
        | DirectMarketingCombinationCatalogAndRetailMerchant
        | DirectMarketingInboundTelemarketing
        | DirectMarketingInsuranceServices
        | DirectMarketingOther
        | DirectMarketingOutboundTelemarketing
        | DirectMarketingSubscription
        | DirectMarketingTravel
        | DiscountStores
        | Doctors
        | DoorToDoorSales
        | DraperyWindowCoveringAndUpholsteryStores
        | DrinkingPlaces
        | DrugStoresAndPharmacies
        | DrugsDrugProprietariesAndDruggistSundries
        | DryCleaners
        | DurableGoods
        | DutyFreeStores
        | EatingPlacesRestaurants
        | EducationalServices
        | ElectricRazorStores
        | ElectricVehicleCharging
        | ElectricalPartsAndEquipment
        | ElectricalServices
        | ElectronicsRepairShops
        | ElectronicsStores
        | ElementarySecondarySchools
        | EmergencyServicesGcasVisaUseOnly
        | EmploymentTempAgencies
        | EquipmentRental
        | ExterminatingServices
        | FamilyClothingStores
        | FastFoodRestaurants
        | FinancialInstitutions
        | FinesGovernmentAdministrativeEntities
        | FireplaceFireplaceScreensAndAccessoriesStores
        | FloorCoveringStores
        | Florists
        | FloristsSuppliesNurseryStockAndFlowers
        | FreezerAndLockerMeatProvisioners
        | FuelDealersNonAutomotive
        | FuneralServicesCrematories
        | FurnitureHomeFurnishingsAndEquipmentStoresExceptAppliances
        | FurnitureRepairRefinishing
        | FurriersAndFurShops
        | GeneralServices
        | GiftCardNoveltyAndSouvenirShops
        | GlassPaintAndWallpaperStores
        | GlasswareCrystalStores
        | GolfCoursesPublic
        | GovernmentLicensedHorseDogRacingUsRegionOnly
        | GovernmentLicensedOnlineCasionsOnlineGamblingUsRegionOnly
        | GovernmentOwnedLotteriesNonUsRegion
        | GovernmentOwnedLotteriesUsRegionOnly
        | GovernmentServices
        | GroceryStoresSupermarkets
        | HardwareEquipmentAndSupplies
        | HardwareStores
        | HealthAndBeautySpas
        | HearingAidsSalesAndSupplies
        | HeatingPlumbingAC
        | HobbyToyAndGameShops
        | HomeSupplyWarehouseStores
        | Hospitals
        | HotelsMotelsAndResorts
        | HouseholdApplianceStores
        | IndustrialSupplies
        | InformationRetrievalServices
        | InsuranceDefault
        | InsuranceUnderwritingPremiums
        | IntraCompanyPurchases
        | JewelryStoresWatchesClocksAndSilverwareStores
        | LandscapingServices
        | Laundries
        | LaundryCleaningServices
        | LegalServicesAttorneys
        | LuggageAndLeatherGoodsStores
        | LumberBuildingMaterialsStores
        | ManualCashDisburse
        | MarinasServiceAndSupplies
        | Marketplaces
        | MasonryStoneworkAndPlaster
        | MassageParlors
        | MedicalAndDentalLabs
        | MedicalDentalOphthalmicAndHospitalEquipmentAndSupplies
        | MedicalServices
        | MembershipOrganizations
        | MensAndBoysClothingAndAccessoriesStores
        | MensWomensClothingStores
        | MetalServiceCenters
        | Miscellaneous
        | MiscellaneousApparelAndAccessoryShops
        | MiscellaneousAutoDealers
        | MiscellaneousBusinessServices
        | MiscellaneousFoodStores
        | MiscellaneousGeneralMerchandise
        | MiscellaneousGeneralServices
        | MiscellaneousHomeFurnishingSpecialtyStores
        | MiscellaneousPublishingAndPrinting
        | MiscellaneousRecreationServices
        | MiscellaneousRepairShops
        | MiscellaneousSpecialtyRetail
        | MobileHomeDealers
        | MotionPictureTheaters
        | MotorFreightCarriersAndTrucking
        | MotorHomesDealers
        | MotorVehicleSuppliesAndNewParts
        | MotorcycleShopsAndDealers
        | MotorcycleShopsDealers
        | MusicStoresMusicalInstrumentsPianosAndSheetMusic
        | NewsDealersAndNewsstands
        | NonFiMoneyOrders
        | NonFiStoredValueCardPurchaseLoad
        | NondurableGoods
        | NurseriesLawnAndGardenSupplyStores
        | NursingPersonalCare
        | OfficeAndCommercialFurniture
        | OpticiansEyeglasses
        | OptometristsOphthalmologist
        | OrthopedicGoodsProstheticDevices
        | Osteopaths
        | PackageStoresBeerWineAndLiquor
        | PaintsVarnishesAndSupplies
        | ParkingLotsGarages
        | PassengerRailways
        | PawnShops
        | PetShopsPetFoodAndSupplies
        | PetroleumAndPetroleumProducts
        | PhotoDeveloping
        | PhotographicPhotocopyMicrofilmEquipmentAndSupplies
        | PhotographicStudios
        | PictureVideoProduction
        | PieceGoodsNotionsAndOtherDryGoods
        | PlumbingHeatingEquipmentAndSupplies
        | PoliticalOrganizations
        | PostalServicesGovernmentOnly
        | PreciousStonesAndMetalsWatchesAndJewelry
        | ProfessionalServices
        | PublicWarehousingAndStorage
        | QuickCopyReproAndBlueprint
        | Railroads
        | RealEstateAgentsAndManagersRentals
        | RecordStores
        | RecreationalVehicleRentals
        | ReligiousGoodsStores
        | ReligiousOrganizations
        | RoofingSidingSheetMetal
        | SecretarialSupportServices
        | SecurityBrokersDealers
        | ServiceStations
        | SewingNeedleworkFabricAndPieceGoodsStores
        | ShoeRepairHatCleaning
        | ShoeStores
        | SmallApplianceRepair
        | SnowmobileDealers
        | SpecialTradeServices
        | SpecialtyCleaning
        | SportingGoodsStores
        | SportingRecreationCamps
        | SportsAndRidingApparelStores
        | SportsClubsFields
        | StampAndCoinStores
        | StationaryOfficeSuppliesPrintingAndWritingPaper
        | StationeryStoresOfficeAndSchoolSupplyStores
        | SwimmingPoolsSales
        | TUiTravelGermany
        | TailorsAlterations
        | TaxPaymentsGovernmentAgencies
        | TaxPreparationServices
        | TaxicabsLimousines
        | TelecommunicationEquipmentAndTelephoneSales
        | TelecommunicationServices
        | TelegraphServices
        | TentAndAwningShops
        | TestingLaboratories
        | TheatricalTicketAgencies
        | Timeshares
        | TireRetreadingAndRepair
        | TollsBridgeFees
        | TouristAttractionsAndExhibits
        | TowingServices
        | TrailerParksCampgrounds
        | TransportationServices
        | TravelAgenciesTourOperators
        | TruckStopIteration
        | TruckUtilityTrailerRentals
        | TypesettingPlateMakingAndRelatedServices
        | TypewriterStores
        | USFederalGovernmentAgenciesOrDepartments
        | UniformsCommercialClothing
        | UsedMerchandiseAndSecondhandStores
        | Utilities
        | VarietyStores
        | VeterinaryServices
        | VideoAmusementGameSupplies
        | VideoGameArcades
        | VideoTapeRentalStores
        | VocationalTradeSchools
        | WatchJewelryRepair
        | WeldingRepair
        | WholesaleClubs
        | WigAndToupeeStores
        | WiresMoneyOrders
        | WomensAccessoryAndSpecialtyShops
        | WomensReadyToWearStores
        | WreckingAndSalvageYards

    [<Struct>]
    type IssuingCardholderAuthorizationControlsAllowedCardPresences =
        | NotPresent
        | Present

    type IssuingCardSpendingLimitCategories =
        | AcRefrigerationRepair
        | AccountingBookkeepingServices
        | AdvertisingServices
        | AgriculturalCooperative
        | AirlinesAirCarriers
        | AirportsFlyingFields
        | AmbulanceServices
        | AmusementParksCarnivals
        | AntiqueReproductions
        | AntiqueShops
        | Aquariums
        | ArchitecturalSurveyingServices
        | ArtDealersAndGalleries
        | ArtistsSupplyAndCraftShops
        | AutoAndHomeSupplyStores
        | AutoBodyRepairShops
        | AutoPaintShops
        | AutoServiceShops
        | AutomatedCashDisburse
        | AutomatedFuelDispensers
        | AutomobileAssociations
        | AutomotivePartsAndAccessoriesStores
        | AutomotiveTireStores
        | BailAndBondPayments
        | Bakeries
        | BandsOrchestras
        | BarberAndBeautyShops
        | BettingCasinoGambling
        | BicycleShops
        | BilliardPoolEstablishments
        | BoatDealers
        | BoatRentalsAndLeases
        | BookStores
        | BooksPeriodicalsAndNewspapers
        | BowlingAlleys
        | BusLines
        | BusinessSecretarialSchools
        | BuyingShoppingServices
        | CableSatelliteAndOtherPayTelevisionAndRadio
        | CameraAndPhotographicSupplyStores
        | CandyNutAndConfectioneryStores
        | CarAndTruckDealersNewUsed
        | CarAndTruckDealersUsedOnly
        | CarRentalAgencies
        | CarWashes
        | CarpentryServices
        | CarpetUpholsteryCleaning
        | Caterers
        | CharitableAndSocialServiceOrganizationsFundraising
        | ChemicalsAndAlliedProducts
        | ChildCareServices
        | ChildrensAndInfantsWearStores
        | ChiropodistsPodiatrists
        | Chiropractors
        | CigarStoresAndStands
        | CivicSocialFraternalAssociations
        | CleaningAndMaintenance
        | ClothingRental
        | CollegesUniversities
        | CommercialEquipment
        | CommercialFootwear
        | CommercialPhotographyArtAndGraphics
        | CommuterTransportAndFerries
        | ComputerNetworkServices
        | ComputerProgramming
        | ComputerRepair
        | ComputerSoftwareStores
        | ComputersPeripheralsAndSoftware
        | ConcreteWorkServices
        | ConstructionMaterials
        | ConsultingPublicRelations
        | CorrespondenceSchools
        | CosmeticStores
        | CounselingServices
        | CountryClubs
        | CourierServices
        | CourtCosts
        | CreditReportingAgencies
        | CruiseLines
        | DairyProductsStores
        | DanceHallStudiosSchools
        | DatingEscortServices
        | DentistsOrthodontists
        | DepartmentStores
        | DetectiveAgencies
        | DigitalGoodsApplications
        | DigitalGoodsGames
        | DigitalGoodsLargeVolume
        | DigitalGoodsMedia
        | DirectMarketingCatalogMerchant
        | DirectMarketingCombinationCatalogAndRetailMerchant
        | DirectMarketingInboundTelemarketing
        | DirectMarketingInsuranceServices
        | DirectMarketingOther
        | DirectMarketingOutboundTelemarketing
        | DirectMarketingSubscription
        | DirectMarketingTravel
        | DiscountStores
        | Doctors
        | DoorToDoorSales
        | DraperyWindowCoveringAndUpholsteryStores
        | DrinkingPlaces
        | DrugStoresAndPharmacies
        | DrugsDrugProprietariesAndDruggistSundries
        | DryCleaners
        | DurableGoods
        | DutyFreeStores
        | EatingPlacesRestaurants
        | EducationalServices
        | ElectricRazorStores
        | ElectricVehicleCharging
        | ElectricalPartsAndEquipment
        | ElectricalServices
        | ElectronicsRepairShops
        | ElectronicsStores
        | ElementarySecondarySchools
        | EmergencyServicesGcasVisaUseOnly
        | EmploymentTempAgencies
        | EquipmentRental
        | ExterminatingServices
        | FamilyClothingStores
        | FastFoodRestaurants
        | FinancialInstitutions
        | FinesGovernmentAdministrativeEntities
        | FireplaceFireplaceScreensAndAccessoriesStores
        | FloorCoveringStores
        | Florists
        | FloristsSuppliesNurseryStockAndFlowers
        | FreezerAndLockerMeatProvisioners
        | FuelDealersNonAutomotive
        | FuneralServicesCrematories
        | FurnitureHomeFurnishingsAndEquipmentStoresExceptAppliances
        | FurnitureRepairRefinishing
        | FurriersAndFurShops
        | GeneralServices
        | GiftCardNoveltyAndSouvenirShops
        | GlassPaintAndWallpaperStores
        | GlasswareCrystalStores
        | GolfCoursesPublic
        | GovernmentLicensedHorseDogRacingUsRegionOnly
        | GovernmentLicensedOnlineCasionsOnlineGamblingUsRegionOnly
        | GovernmentOwnedLotteriesNonUsRegion
        | GovernmentOwnedLotteriesUsRegionOnly
        | GovernmentServices
        | GroceryStoresSupermarkets
        | HardwareEquipmentAndSupplies
        | HardwareStores
        | HealthAndBeautySpas
        | HearingAidsSalesAndSupplies
        | HeatingPlumbingAC
        | HobbyToyAndGameShops
        | HomeSupplyWarehouseStores
        | Hospitals
        | HotelsMotelsAndResorts
        | HouseholdApplianceStores
        | IndustrialSupplies
        | InformationRetrievalServices
        | InsuranceDefault
        | InsuranceUnderwritingPremiums
        | IntraCompanyPurchases
        | JewelryStoresWatchesClocksAndSilverwareStores
        | LandscapingServices
        | Laundries
        | LaundryCleaningServices
        | LegalServicesAttorneys
        | LuggageAndLeatherGoodsStores
        | LumberBuildingMaterialsStores
        | ManualCashDisburse
        | MarinasServiceAndSupplies
        | Marketplaces
        | MasonryStoneworkAndPlaster
        | MassageParlors
        | MedicalAndDentalLabs
        | MedicalDentalOphthalmicAndHospitalEquipmentAndSupplies
        | MedicalServices
        | MembershipOrganizations
        | MensAndBoysClothingAndAccessoriesStores
        | MensWomensClothingStores
        | MetalServiceCenters
        | Miscellaneous
        | MiscellaneousApparelAndAccessoryShops
        | MiscellaneousAutoDealers
        | MiscellaneousBusinessServices
        | MiscellaneousFoodStores
        | MiscellaneousGeneralMerchandise
        | MiscellaneousGeneralServices
        | MiscellaneousHomeFurnishingSpecialtyStores
        | MiscellaneousPublishingAndPrinting
        | MiscellaneousRecreationServices
        | MiscellaneousRepairShops
        | MiscellaneousSpecialtyRetail
        | MobileHomeDealers
        | MotionPictureTheaters
        | MotorFreightCarriersAndTrucking
        | MotorHomesDealers
        | MotorVehicleSuppliesAndNewParts
        | MotorcycleShopsAndDealers
        | MotorcycleShopsDealers
        | MusicStoresMusicalInstrumentsPianosAndSheetMusic
        | NewsDealersAndNewsstands
        | NonFiMoneyOrders
        | NonFiStoredValueCardPurchaseLoad
        | NondurableGoods
        | NurseriesLawnAndGardenSupplyStores
        | NursingPersonalCare
        | OfficeAndCommercialFurniture
        | OpticiansEyeglasses
        | OptometristsOphthalmologist
        | OrthopedicGoodsProstheticDevices
        | Osteopaths
        | PackageStoresBeerWineAndLiquor
        | PaintsVarnishesAndSupplies
        | ParkingLotsGarages
        | PassengerRailways
        | PawnShops
        | PetShopsPetFoodAndSupplies
        | PetroleumAndPetroleumProducts
        | PhotoDeveloping
        | PhotographicPhotocopyMicrofilmEquipmentAndSupplies
        | PhotographicStudios
        | PictureVideoProduction
        | PieceGoodsNotionsAndOtherDryGoods
        | PlumbingHeatingEquipmentAndSupplies
        | PoliticalOrganizations
        | PostalServicesGovernmentOnly
        | PreciousStonesAndMetalsWatchesAndJewelry
        | ProfessionalServices
        | PublicWarehousingAndStorage
        | QuickCopyReproAndBlueprint
        | Railroads
        | RealEstateAgentsAndManagersRentals
        | RecordStores
        | RecreationalVehicleRentals
        | ReligiousGoodsStores
        | ReligiousOrganizations
        | RoofingSidingSheetMetal
        | SecretarialSupportServices
        | SecurityBrokersDealers
        | ServiceStations
        | SewingNeedleworkFabricAndPieceGoodsStores
        | ShoeRepairHatCleaning
        | ShoeStores
        | SmallApplianceRepair
        | SnowmobileDealers
        | SpecialTradeServices
        | SpecialtyCleaning
        | SportingGoodsStores
        | SportingRecreationCamps
        | SportsAndRidingApparelStores
        | SportsClubsFields
        | StampAndCoinStores
        | StationaryOfficeSuppliesPrintingAndWritingPaper
        | StationeryStoresOfficeAndSchoolSupplyStores
        | SwimmingPoolsSales
        | TUiTravelGermany
        | TailorsAlterations
        | TaxPaymentsGovernmentAgencies
        | TaxPreparationServices
        | TaxicabsLimousines
        | TelecommunicationEquipmentAndTelephoneSales
        | TelecommunicationServices
        | TelegraphServices
        | TentAndAwningShops
        | TestingLaboratories
        | TheatricalTicketAgencies
        | Timeshares
        | TireRetreadingAndRepair
        | TollsBridgeFees
        | TouristAttractionsAndExhibits
        | TowingServices
        | TrailerParksCampgrounds
        | TransportationServices
        | TravelAgenciesTourOperators
        | TruckStopIteration
        | TruckUtilityTrailerRentals
        | TypesettingPlateMakingAndRelatedServices
        | TypewriterStores
        | USFederalGovernmentAgenciesOrDepartments
        | UniformsCommercialClothing
        | UsedMerchandiseAndSecondhandStores
        | Utilities
        | VarietyStores
        | VeterinaryServices
        | VideoAmusementGameSupplies
        | VideoGameArcades
        | VideoTapeRentalStores
        | VocationalTradeSchools
        | WatchJewelryRepair
        | WeldingRepair
        | WholesaleClubs
        | WigAndToupeeStores
        | WiresMoneyOrders
        | WomensAccessoryAndSpecialtyShops
        | WomensReadyToWearStores
        | WreckingAndSalvageYards

    type IssuingCardSpendingLimitInterval =
        | AllTime
        | Daily
        | Monthly
        | PerAuthorization
        | Weekly
        | Yearly

    type IssuingCardShippingCustoms = {
        ///A registration number used for customs in Europe. See [https://www.gov.uk/eori](https://www.gov.uk/eori) for the UK and [https://ec.europa.eu/taxation_customs/business/customs-procedures-import-and-export/customs-procedures/economic-operators-registration-and-identification-number-eori_en](https://ec.europa.eu/taxation_customs/business/customs-procedures-import-and-export/customs-procedures/economic-operators-registration-and-identification-number-eori_en) for the EU.
        EoriNumber: string option
    }
    with
        static member New (eoriNumber: string option) =
            {
                IssuingCardShippingCustoms.EoriNumber = eoriNumber //required
            }

    [<Struct>]
    type IssuingCardShippingAddressValidationResult =
        | Indeterminate
        | LikelyDeliverable
        | LikelyUndeliverable

    [<Struct>]
    type IssuingCardShippingAddressValidationMode =
        | Disabled
        | NormalizationOnly
        | ValidationAndNormalization

    [<Struct>]
    type IssuingCardShippingType =
        | Bulk
        | Individual

    type IssuingCardShippingStatus =
        | Canceled
        | Delivered
        | Failure
        | Pending
        | Returned
        | Shipped
        | Submitted

    [<Struct>]
    type IssuingCardShippingService =
        | Express
        | Priority
        | Standard

    [<Struct>]
    type IssuingCardShippingCarrier =
        | Dhl
        | Fedex
        | RoyalMail
        | Usps

    type IssuingCardLifecycleConditions = {
        ///The card is automatically cancelled when it makes this number of non-zero payment authorizations and transactions. The count includes penny authorizations, but doesn't include non-payment actions, such as authorization advice.
        PaymentCount: int
    }
    with
        static member New (paymentCount: int) =
            {
                IssuingCardLifecycleConditions.PaymentCount = paymentCount //required
            }

    [<Struct>]
    type IssuingCardGooglePayIneligibleReason =
        | MissingAgreement
        | MissingCardholderContact
        | UnsupportedRegion

    [<Struct>]
    type IssuingCardFraudWarningType =
        | CardTestingExposure
        | FraudDisputeFiled
        | ThirdPartyReported
        | UserIndicatedFraud

    type IssuingCardAuthorizationControlsBlockedCategories =
        | AcRefrigerationRepair
        | AccountingBookkeepingServices
        | AdvertisingServices
        | AgriculturalCooperative
        | AirlinesAirCarriers
        | AirportsFlyingFields
        | AmbulanceServices
        | AmusementParksCarnivals
        | AntiqueReproductions
        | AntiqueShops
        | Aquariums
        | ArchitecturalSurveyingServices
        | ArtDealersAndGalleries
        | ArtistsSupplyAndCraftShops
        | AutoAndHomeSupplyStores
        | AutoBodyRepairShops
        | AutoPaintShops
        | AutoServiceShops
        | AutomatedCashDisburse
        | AutomatedFuelDispensers
        | AutomobileAssociations
        | AutomotivePartsAndAccessoriesStores
        | AutomotiveTireStores
        | BailAndBondPayments
        | Bakeries
        | BandsOrchestras
        | BarberAndBeautyShops
        | BettingCasinoGambling
        | BicycleShops
        | BilliardPoolEstablishments
        | BoatDealers
        | BoatRentalsAndLeases
        | BookStores
        | BooksPeriodicalsAndNewspapers
        | BowlingAlleys
        | BusLines
        | BusinessSecretarialSchools
        | BuyingShoppingServices
        | CableSatelliteAndOtherPayTelevisionAndRadio
        | CameraAndPhotographicSupplyStores
        | CandyNutAndConfectioneryStores
        | CarAndTruckDealersNewUsed
        | CarAndTruckDealersUsedOnly
        | CarRentalAgencies
        | CarWashes
        | CarpentryServices
        | CarpetUpholsteryCleaning
        | Caterers
        | CharitableAndSocialServiceOrganizationsFundraising
        | ChemicalsAndAlliedProducts
        | ChildCareServices
        | ChildrensAndInfantsWearStores
        | ChiropodistsPodiatrists
        | Chiropractors
        | CigarStoresAndStands
        | CivicSocialFraternalAssociations
        | CleaningAndMaintenance
        | ClothingRental
        | CollegesUniversities
        | CommercialEquipment
        | CommercialFootwear
        | CommercialPhotographyArtAndGraphics
        | CommuterTransportAndFerries
        | ComputerNetworkServices
        | ComputerProgramming
        | ComputerRepair
        | ComputerSoftwareStores
        | ComputersPeripheralsAndSoftware
        | ConcreteWorkServices
        | ConstructionMaterials
        | ConsultingPublicRelations
        | CorrespondenceSchools
        | CosmeticStores
        | CounselingServices
        | CountryClubs
        | CourierServices
        | CourtCosts
        | CreditReportingAgencies
        | CruiseLines
        | DairyProductsStores
        | DanceHallStudiosSchools
        | DatingEscortServices
        | DentistsOrthodontists
        | DepartmentStores
        | DetectiveAgencies
        | DigitalGoodsApplications
        | DigitalGoodsGames
        | DigitalGoodsLargeVolume
        | DigitalGoodsMedia
        | DirectMarketingCatalogMerchant
        | DirectMarketingCombinationCatalogAndRetailMerchant
        | DirectMarketingInboundTelemarketing
        | DirectMarketingInsuranceServices
        | DirectMarketingOther
        | DirectMarketingOutboundTelemarketing
        | DirectMarketingSubscription
        | DirectMarketingTravel
        | DiscountStores
        | Doctors
        | DoorToDoorSales
        | DraperyWindowCoveringAndUpholsteryStores
        | DrinkingPlaces
        | DrugStoresAndPharmacies
        | DrugsDrugProprietariesAndDruggistSundries
        | DryCleaners
        | DurableGoods
        | DutyFreeStores
        | EatingPlacesRestaurants
        | EducationalServices
        | ElectricRazorStores
        | ElectricVehicleCharging
        | ElectricalPartsAndEquipment
        | ElectricalServices
        | ElectronicsRepairShops
        | ElectronicsStores
        | ElementarySecondarySchools
        | EmergencyServicesGcasVisaUseOnly
        | EmploymentTempAgencies
        | EquipmentRental
        | ExterminatingServices
        | FamilyClothingStores
        | FastFoodRestaurants
        | FinancialInstitutions
        | FinesGovernmentAdministrativeEntities
        | FireplaceFireplaceScreensAndAccessoriesStores
        | FloorCoveringStores
        | Florists
        | FloristsSuppliesNurseryStockAndFlowers
        | FreezerAndLockerMeatProvisioners
        | FuelDealersNonAutomotive
        | FuneralServicesCrematories
        | FurnitureHomeFurnishingsAndEquipmentStoresExceptAppliances
        | FurnitureRepairRefinishing
        | FurriersAndFurShops
        | GeneralServices
        | GiftCardNoveltyAndSouvenirShops
        | GlassPaintAndWallpaperStores
        | GlasswareCrystalStores
        | GolfCoursesPublic
        | GovernmentLicensedHorseDogRacingUsRegionOnly
        | GovernmentLicensedOnlineCasionsOnlineGamblingUsRegionOnly
        | GovernmentOwnedLotteriesNonUsRegion
        | GovernmentOwnedLotteriesUsRegionOnly
        | GovernmentServices
        | GroceryStoresSupermarkets
        | HardwareEquipmentAndSupplies
        | HardwareStores
        | HealthAndBeautySpas
        | HearingAidsSalesAndSupplies
        | HeatingPlumbingAC
        | HobbyToyAndGameShops
        | HomeSupplyWarehouseStores
        | Hospitals
        | HotelsMotelsAndResorts
        | HouseholdApplianceStores
        | IndustrialSupplies
        | InformationRetrievalServices
        | InsuranceDefault
        | InsuranceUnderwritingPremiums
        | IntraCompanyPurchases
        | JewelryStoresWatchesClocksAndSilverwareStores
        | LandscapingServices
        | Laundries
        | LaundryCleaningServices
        | LegalServicesAttorneys
        | LuggageAndLeatherGoodsStores
        | LumberBuildingMaterialsStores
        | ManualCashDisburse
        | MarinasServiceAndSupplies
        | Marketplaces
        | MasonryStoneworkAndPlaster
        | MassageParlors
        | MedicalAndDentalLabs
        | MedicalDentalOphthalmicAndHospitalEquipmentAndSupplies
        | MedicalServices
        | MembershipOrganizations
        | MensAndBoysClothingAndAccessoriesStores
        | MensWomensClothingStores
        | MetalServiceCenters
        | Miscellaneous
        | MiscellaneousApparelAndAccessoryShops
        | MiscellaneousAutoDealers
        | MiscellaneousBusinessServices
        | MiscellaneousFoodStores
        | MiscellaneousGeneralMerchandise
        | MiscellaneousGeneralServices
        | MiscellaneousHomeFurnishingSpecialtyStores
        | MiscellaneousPublishingAndPrinting
        | MiscellaneousRecreationServices
        | MiscellaneousRepairShops
        | MiscellaneousSpecialtyRetail
        | MobileHomeDealers
        | MotionPictureTheaters
        | MotorFreightCarriersAndTrucking
        | MotorHomesDealers
        | MotorVehicleSuppliesAndNewParts
        | MotorcycleShopsAndDealers
        | MotorcycleShopsDealers
        | MusicStoresMusicalInstrumentsPianosAndSheetMusic
        | NewsDealersAndNewsstands
        | NonFiMoneyOrders
        | NonFiStoredValueCardPurchaseLoad
        | NondurableGoods
        | NurseriesLawnAndGardenSupplyStores
        | NursingPersonalCare
        | OfficeAndCommercialFurniture
        | OpticiansEyeglasses
        | OptometristsOphthalmologist
        | OrthopedicGoodsProstheticDevices
        | Osteopaths
        | PackageStoresBeerWineAndLiquor
        | PaintsVarnishesAndSupplies
        | ParkingLotsGarages
        | PassengerRailways
        | PawnShops
        | PetShopsPetFoodAndSupplies
        | PetroleumAndPetroleumProducts
        | PhotoDeveloping
        | PhotographicPhotocopyMicrofilmEquipmentAndSupplies
        | PhotographicStudios
        | PictureVideoProduction
        | PieceGoodsNotionsAndOtherDryGoods
        | PlumbingHeatingEquipmentAndSupplies
        | PoliticalOrganizations
        | PostalServicesGovernmentOnly
        | PreciousStonesAndMetalsWatchesAndJewelry
        | ProfessionalServices
        | PublicWarehousingAndStorage
        | QuickCopyReproAndBlueprint
        | Railroads
        | RealEstateAgentsAndManagersRentals
        | RecordStores
        | RecreationalVehicleRentals
        | ReligiousGoodsStores
        | ReligiousOrganizations
        | RoofingSidingSheetMetal
        | SecretarialSupportServices
        | SecurityBrokersDealers
        | ServiceStations
        | SewingNeedleworkFabricAndPieceGoodsStores
        | ShoeRepairHatCleaning
        | ShoeStores
        | SmallApplianceRepair
        | SnowmobileDealers
        | SpecialTradeServices
        | SpecialtyCleaning
        | SportingGoodsStores
        | SportingRecreationCamps
        | SportsAndRidingApparelStores
        | SportsClubsFields
        | StampAndCoinStores
        | StationaryOfficeSuppliesPrintingAndWritingPaper
        | StationeryStoresOfficeAndSchoolSupplyStores
        | SwimmingPoolsSales
        | TUiTravelGermany
        | TailorsAlterations
        | TaxPaymentsGovernmentAgencies
        | TaxPreparationServices
        | TaxicabsLimousines
        | TelecommunicationEquipmentAndTelephoneSales
        | TelecommunicationServices
        | TelegraphServices
        | TentAndAwningShops
        | TestingLaboratories
        | TheatricalTicketAgencies
        | Timeshares
        | TireRetreadingAndRepair
        | TollsBridgeFees
        | TouristAttractionsAndExhibits
        | TowingServices
        | TrailerParksCampgrounds
        | TransportationServices
        | TravelAgenciesTourOperators
        | TruckStopIteration
        | TruckUtilityTrailerRentals
        | TypesettingPlateMakingAndRelatedServices
        | TypewriterStores
        | USFederalGovernmentAgenciesOrDepartments
        | UniformsCommercialClothing
        | UsedMerchandiseAndSecondhandStores
        | Utilities
        | VarietyStores
        | VeterinaryServices
        | VideoAmusementGameSupplies
        | VideoGameArcades
        | VideoTapeRentalStores
        | VocationalTradeSchools
        | WatchJewelryRepair
        | WeldingRepair
        | WholesaleClubs
        | WigAndToupeeStores
        | WiresMoneyOrders
        | WomensAccessoryAndSpecialtyShops
        | WomensReadyToWearStores
        | WreckingAndSalvageYards

    [<Struct>]
    type IssuingCardAuthorizationControlsBlockedCardPresences =
        | NotPresent
        | Present

    type IssuingCardAuthorizationControlsAllowedCategories =
        | AcRefrigerationRepair
        | AccountingBookkeepingServices
        | AdvertisingServices
        | AgriculturalCooperative
        | AirlinesAirCarriers
        | AirportsFlyingFields
        | AmbulanceServices
        | AmusementParksCarnivals
        | AntiqueReproductions
        | AntiqueShops
        | Aquariums
        | ArchitecturalSurveyingServices
        | ArtDealersAndGalleries
        | ArtistsSupplyAndCraftShops
        | AutoAndHomeSupplyStores
        | AutoBodyRepairShops
        | AutoPaintShops
        | AutoServiceShops
        | AutomatedCashDisburse
        | AutomatedFuelDispensers
        | AutomobileAssociations
        | AutomotivePartsAndAccessoriesStores
        | AutomotiveTireStores
        | BailAndBondPayments
        | Bakeries
        | BandsOrchestras
        | BarberAndBeautyShops
        | BettingCasinoGambling
        | BicycleShops
        | BilliardPoolEstablishments
        | BoatDealers
        | BoatRentalsAndLeases
        | BookStores
        | BooksPeriodicalsAndNewspapers
        | BowlingAlleys
        | BusLines
        | BusinessSecretarialSchools
        | BuyingShoppingServices
        | CableSatelliteAndOtherPayTelevisionAndRadio
        | CameraAndPhotographicSupplyStores
        | CandyNutAndConfectioneryStores
        | CarAndTruckDealersNewUsed
        | CarAndTruckDealersUsedOnly
        | CarRentalAgencies
        | CarWashes
        | CarpentryServices
        | CarpetUpholsteryCleaning
        | Caterers
        | CharitableAndSocialServiceOrganizationsFundraising
        | ChemicalsAndAlliedProducts
        | ChildCareServices
        | ChildrensAndInfantsWearStores
        | ChiropodistsPodiatrists
        | Chiropractors
        | CigarStoresAndStands
        | CivicSocialFraternalAssociations
        | CleaningAndMaintenance
        | ClothingRental
        | CollegesUniversities
        | CommercialEquipment
        | CommercialFootwear
        | CommercialPhotographyArtAndGraphics
        | CommuterTransportAndFerries
        | ComputerNetworkServices
        | ComputerProgramming
        | ComputerRepair
        | ComputerSoftwareStores
        | ComputersPeripheralsAndSoftware
        | ConcreteWorkServices
        | ConstructionMaterials
        | ConsultingPublicRelations
        | CorrespondenceSchools
        | CosmeticStores
        | CounselingServices
        | CountryClubs
        | CourierServices
        | CourtCosts
        | CreditReportingAgencies
        | CruiseLines
        | DairyProductsStores
        | DanceHallStudiosSchools
        | DatingEscortServices
        | DentistsOrthodontists
        | DepartmentStores
        | DetectiveAgencies
        | DigitalGoodsApplications
        | DigitalGoodsGames
        | DigitalGoodsLargeVolume
        | DigitalGoodsMedia
        | DirectMarketingCatalogMerchant
        | DirectMarketingCombinationCatalogAndRetailMerchant
        | DirectMarketingInboundTelemarketing
        | DirectMarketingInsuranceServices
        | DirectMarketingOther
        | DirectMarketingOutboundTelemarketing
        | DirectMarketingSubscription
        | DirectMarketingTravel
        | DiscountStores
        | Doctors
        | DoorToDoorSales
        | DraperyWindowCoveringAndUpholsteryStores
        | DrinkingPlaces
        | DrugStoresAndPharmacies
        | DrugsDrugProprietariesAndDruggistSundries
        | DryCleaners
        | DurableGoods
        | DutyFreeStores
        | EatingPlacesRestaurants
        | EducationalServices
        | ElectricRazorStores
        | ElectricVehicleCharging
        | ElectricalPartsAndEquipment
        | ElectricalServices
        | ElectronicsRepairShops
        | ElectronicsStores
        | ElementarySecondarySchools
        | EmergencyServicesGcasVisaUseOnly
        | EmploymentTempAgencies
        | EquipmentRental
        | ExterminatingServices
        | FamilyClothingStores
        | FastFoodRestaurants
        | FinancialInstitutions
        | FinesGovernmentAdministrativeEntities
        | FireplaceFireplaceScreensAndAccessoriesStores
        | FloorCoveringStores
        | Florists
        | FloristsSuppliesNurseryStockAndFlowers
        | FreezerAndLockerMeatProvisioners
        | FuelDealersNonAutomotive
        | FuneralServicesCrematories
        | FurnitureHomeFurnishingsAndEquipmentStoresExceptAppliances
        | FurnitureRepairRefinishing
        | FurriersAndFurShops
        | GeneralServices
        | GiftCardNoveltyAndSouvenirShops
        | GlassPaintAndWallpaperStores
        | GlasswareCrystalStores
        | GolfCoursesPublic
        | GovernmentLicensedHorseDogRacingUsRegionOnly
        | GovernmentLicensedOnlineCasionsOnlineGamblingUsRegionOnly
        | GovernmentOwnedLotteriesNonUsRegion
        | GovernmentOwnedLotteriesUsRegionOnly
        | GovernmentServices
        | GroceryStoresSupermarkets
        | HardwareEquipmentAndSupplies
        | HardwareStores
        | HealthAndBeautySpas
        | HearingAidsSalesAndSupplies
        | HeatingPlumbingAC
        | HobbyToyAndGameShops
        | HomeSupplyWarehouseStores
        | Hospitals
        | HotelsMotelsAndResorts
        | HouseholdApplianceStores
        | IndustrialSupplies
        | InformationRetrievalServices
        | InsuranceDefault
        | InsuranceUnderwritingPremiums
        | IntraCompanyPurchases
        | JewelryStoresWatchesClocksAndSilverwareStores
        | LandscapingServices
        | Laundries
        | LaundryCleaningServices
        | LegalServicesAttorneys
        | LuggageAndLeatherGoodsStores
        | LumberBuildingMaterialsStores
        | ManualCashDisburse
        | MarinasServiceAndSupplies
        | Marketplaces
        | MasonryStoneworkAndPlaster
        | MassageParlors
        | MedicalAndDentalLabs
        | MedicalDentalOphthalmicAndHospitalEquipmentAndSupplies
        | MedicalServices
        | MembershipOrganizations
        | MensAndBoysClothingAndAccessoriesStores
        | MensWomensClothingStores
        | MetalServiceCenters
        | Miscellaneous
        | MiscellaneousApparelAndAccessoryShops
        | MiscellaneousAutoDealers
        | MiscellaneousBusinessServices
        | MiscellaneousFoodStores
        | MiscellaneousGeneralMerchandise
        | MiscellaneousGeneralServices
        | MiscellaneousHomeFurnishingSpecialtyStores
        | MiscellaneousPublishingAndPrinting
        | MiscellaneousRecreationServices
        | MiscellaneousRepairShops
        | MiscellaneousSpecialtyRetail
        | MobileHomeDealers
        | MotionPictureTheaters
        | MotorFreightCarriersAndTrucking
        | MotorHomesDealers
        | MotorVehicleSuppliesAndNewParts
        | MotorcycleShopsAndDealers
        | MotorcycleShopsDealers
        | MusicStoresMusicalInstrumentsPianosAndSheetMusic
        | NewsDealersAndNewsstands
        | NonFiMoneyOrders
        | NonFiStoredValueCardPurchaseLoad
        | NondurableGoods
        | NurseriesLawnAndGardenSupplyStores
        | NursingPersonalCare
        | OfficeAndCommercialFurniture
        | OpticiansEyeglasses
        | OptometristsOphthalmologist
        | OrthopedicGoodsProstheticDevices
        | Osteopaths
        | PackageStoresBeerWineAndLiquor
        | PaintsVarnishesAndSupplies
        | ParkingLotsGarages
        | PassengerRailways
        | PawnShops
        | PetShopsPetFoodAndSupplies
        | PetroleumAndPetroleumProducts
        | PhotoDeveloping
        | PhotographicPhotocopyMicrofilmEquipmentAndSupplies
        | PhotographicStudios
        | PictureVideoProduction
        | PieceGoodsNotionsAndOtherDryGoods
        | PlumbingHeatingEquipmentAndSupplies
        | PoliticalOrganizations
        | PostalServicesGovernmentOnly
        | PreciousStonesAndMetalsWatchesAndJewelry
        | ProfessionalServices
        | PublicWarehousingAndStorage
        | QuickCopyReproAndBlueprint
        | Railroads
        | RealEstateAgentsAndManagersRentals
        | RecordStores
        | RecreationalVehicleRentals
        | ReligiousGoodsStores
        | ReligiousOrganizations
        | RoofingSidingSheetMetal
        | SecretarialSupportServices
        | SecurityBrokersDealers
        | ServiceStations
        | SewingNeedleworkFabricAndPieceGoodsStores
        | ShoeRepairHatCleaning
        | ShoeStores
        | SmallApplianceRepair
        | SnowmobileDealers
        | SpecialTradeServices
        | SpecialtyCleaning
        | SportingGoodsStores
        | SportingRecreationCamps
        | SportsAndRidingApparelStores
        | SportsClubsFields
        | StampAndCoinStores
        | StationaryOfficeSuppliesPrintingAndWritingPaper
        | StationeryStoresOfficeAndSchoolSupplyStores
        | SwimmingPoolsSales
        | TUiTravelGermany
        | TailorsAlterations
        | TaxPaymentsGovernmentAgencies
        | TaxPreparationServices
        | TaxicabsLimousines
        | TelecommunicationEquipmentAndTelephoneSales
        | TelecommunicationServices
        | TelegraphServices
        | TentAndAwningShops
        | TestingLaboratories
        | TheatricalTicketAgencies
        | Timeshares
        | TireRetreadingAndRepair
        | TollsBridgeFees
        | TouristAttractionsAndExhibits
        | TowingServices
        | TrailerParksCampgrounds
        | TransportationServices
        | TravelAgenciesTourOperators
        | TruckStopIteration
        | TruckUtilityTrailerRentals
        | TypesettingPlateMakingAndRelatedServices
        | TypewriterStores
        | USFederalGovernmentAgenciesOrDepartments
        | UniformsCommercialClothing
        | UsedMerchandiseAndSecondhandStores
        | Utilities
        | VarietyStores
        | VeterinaryServices
        | VideoAmusementGameSupplies
        | VideoGameArcades
        | VideoTapeRentalStores
        | VocationalTradeSchools
        | WatchJewelryRepair
        | WeldingRepair
        | WholesaleClubs
        | WigAndToupeeStores
        | WiresMoneyOrders
        | WomensAccessoryAndSpecialtyShops
        | WomensReadyToWearStores
        | WreckingAndSalvageYards

    [<Struct>]
    type IssuingCardAuthorizationControlsAllowedCardPresences =
        | NotPresent
        | Present

    [<Struct>]
    type IssuingCardApplePayIneligibleReason =
        | MissingAgreement
        | MissingCardholderContact
        | UnsupportedRegion

    [<Struct>]
    type IssuingAuthorizationVerificationDataExpiryCheck =
        | Match
        | Mismatch
        | NotProvided

    [<Struct>]
    type IssuingAuthorizationVerificationDataCvcCheck =
        | Match
        | Mismatch
        | NotProvided

    [<Struct>]
    type IssuingAuthorizationVerificationDataAddressPostalCodeCheck =
        | Match
        | Mismatch
        | NotProvided

    [<Struct>]
    type IssuingAuthorizationVerificationDataAddressLine1Check =
        | Match
        | Mismatch
        | NotProvided

    [<Struct>]
    type IssuingAuthorizationThreeDSecureResult =
        | AttemptAcknowledged
        | Authenticated
        | Failed
        | Required

    type IssuingAuthorizationRequestReason =
        | AccountDisabled
        | CardActive
        | CardCanceled
        | CardExpired
        | CardInactive
        | CardholderBlocked
        | CardholderInactive
        | CardholderVerificationRequired
        | InsecureAuthorizationMethod
        | InsufficientFunds
        | NetworkFallback
        | NotAllowed
        | PinBlocked
        | SpendingControls
        | SuspectedFraud
        | VerificationFailed
        | WebhookApproved
        | WebhookDeclined
        | WebhookError
        | WebhookTimeout

    type IssuingAuthorizationNetworkData = {
        ///Identifier assigned to the acquirer by the card network. Sometimes this value is not provided by the network; in this case, the value will be `null`.
        AcquiringInstitutionId: string option
        ///The System Trace Audit Number (STAN) is a 6-digit identifier assigned by the acquirer. Prefer `network_data.transaction_id` if present, unless you have special requirements.
        SystemTraceAuditNumber: string option
        ///Unique identifier for the authorization assigned by the card network used to match subsequent messages, disputes, and transactions.
        TransactionId: string option
    }
    with
        static member New (acquiringInstitutionId: string option, systemTraceAuditNumber: string option, transactionId: string option) =
            {
                IssuingAuthorizationNetworkData.AcquiringInstitutionId = acquiringInstitutionId //required
                IssuingAuthorizationNetworkData.SystemTraceAuditNumber = systemTraceAuditNumber //required
                IssuingAuthorizationNetworkData.TransactionId = transactionId //required
            }

    type IssuingAuthorizationFuelDataUnit =
        | ChargingMinute
        | ImperialGallon
        | Kilogram
        | KilowattHour
        | Liter
        | Other
        | Pound
        | UsGallon

    [<Struct>]
    type IssuingAuthorizationFuelDataType =
        | Diesel
        | Other
        | UnleadedPlus
        | UnleadedRegular
        | UnleadedSuper

    [<Struct>]
    type IssuingAuthorizationFraudChallengeUndeliverableReason =
        | NoPhoneNumber
        | UnsupportedPhoneNumber

    [<Struct>]
    type IssuingAuthorizationFraudChallengeStatus =
        | Expired
        | Pending
        | Rejected
        | Undeliverable
        | Verified

    type IssuingAuthorizationFleetTaxData = {
        ///Amount of state or provincial Sales Tax included in the transaction amount. `null` if not reported by merchant or not subject to tax.
        LocalAmountDecimal: string option
        ///Amount of national Sales Tax or VAT included in the transaction amount. `null` if not reported by merchant or not subject to tax.
        NationalAmountDecimal: string option
    }
    with
        static member New (localAmountDecimal: string option, nationalAmountDecimal: string option) =
            {
                IssuingAuthorizationFleetTaxData.LocalAmountDecimal = localAmountDecimal //required
                IssuingAuthorizationFleetTaxData.NationalAmountDecimal = nationalAmountDecimal //required
            }

    type IssuingAuthorizationFleetNonFuelPriceData = {
        ///Gross non-fuel amount that should equal the sum of the line items, inclusive of taxes.
        GrossAmountDecimal: string option
    }
    with
        static member New (grossAmountDecimal: string option) =
            {
                IssuingAuthorizationFleetNonFuelPriceData.GrossAmountDecimal = grossAmountDecimal //required
            }

    type IssuingAuthorizationFleetFuelPriceData = {
        ///Gross fuel amount that should equal Fuel Quantity multiplied by Fuel Unit Cost, inclusive of taxes.
        GrossAmountDecimal: string option
    }
    with
        static member New (grossAmountDecimal: string option) =
            {
                IssuingAuthorizationFleetFuelPriceData.GrossAmountDecimal = grossAmountDecimal //required
            }

    [<Struct>]
    type IssuingAuthorizationFleetDataServiceType =
        | FullService
        | NonFuelTransaction
        | SelfService

    [<Struct>]
    type IssuingAuthorizationFleetDataPurchaseType =
        | FuelAndNonFuelPurchase
        | FuelPurchase
        | NonFuelPurchase

    type IssuingAuthorizationFleetCardholderPromptData = {
        ///[Deprecated] An alphanumeric ID, though typical point of sales only support numeric entry. The card program can be configured to prompt for a vehicle ID, driver ID, or generic ID.
        AlphanumericId: string option
        ///Driver ID.
        DriverId: string option
        ///Odometer reading.
        Odometer: int option
        ///An alphanumeric ID. This field is used when a vehicle ID, driver ID, or generic ID is entered by the cardholder, but the merchant or card network did not specify the prompt type.
        UnspecifiedId: string option
        ///User ID.
        UserId: string option
        ///Vehicle number.
        VehicleNumber: string option
    }
    with
        static member New (alphanumericId: string option, driverId: string option, odometer: int option, unspecifiedId: string option, userId: string option, vehicleNumber: string option) =
            {
                IssuingAuthorizationFleetCardholderPromptData.AlphanumericId = alphanumericId //required
                IssuingAuthorizationFleetCardholderPromptData.DriverId = driverId //required
                IssuingAuthorizationFleetCardholderPromptData.Odometer = odometer //required
                IssuingAuthorizationFleetCardholderPromptData.UnspecifiedId = unspecifiedId //required
                IssuingAuthorizationFleetCardholderPromptData.UserId = userId //required
                IssuingAuthorizationFleetCardholderPromptData.VehicleNumber = vehicleNumber //required
            }

    [<Struct>]
    type IssuingAuthorizationAuthenticationExemptionType =
        | LowValueTransaction
        | TransactionRiskAnalysis
        | Unknown

    [<Struct>]
    type IssuingAuthorizationAuthenticationExemptionClaimedBy =
        | Acquirer
        | Issuer

    type IssuingAuthorizationAmountDetails = {
        ///The fee charged by the ATM for the cash withdrawal.
        AtmFee: int option
        ///The amount of cash requested by the cardholder.
        CashbackAmount: int option
    }
    with
        static member New (atmFee: int option, cashbackAmount: int option) =
            {
                IssuingAuthorizationAmountDetails.AtmFee = atmFee //required
                IssuingAuthorizationAmountDetails.CashbackAmount = cashbackAmount //required
            }

    [<Struct>]
    type IssuingTransactionWallet =
        | ApplePay
        | GooglePay
        | SamsungPay

    [<Struct>]
    type IssuingTransactionType =
        | Capture
        | Refund

    [<Struct>]
    type IssuingTokenWalletProvider =
        | ApplePay
        | GooglePay
        | SamsungPay

    [<Struct>]
    type IssuingTokenStatus =
        | Active
        | Deleted
        | Requested
        | Suspended

    [<Struct>]
    type IssuingTokenNetwork =
        | Mastercard
        | Visa

    [<Struct>]
    type IssuingPhysicalBundleType =
        | Custom
        | Standard

    [<Struct>]
    type IssuingPhysicalBundleStatus =
        | Active
        | Inactive
        | Review

    [<Struct>]
    type IssuingPersonalizationDesignStatus =
        | Active
        | Inactive
        | Rejected
        | Review

    [<Struct>]
    type IssuingDisputeStatus =
        | Expired
        | Lost
        | Submitted
        | Unsubmitted
        | Won

    type IssuingDisputeLossReason =
        | CardholderAuthenticationIssuerLiability
        | Eci5TokenTransactionWithTavv
        | ExcessDisputesInTimeframe
        | HasNotMetTheMinimumDisputeAmountRequirements
        | InvalidDuplicateDispute
        | InvalidIncorrectAmountDispute
        | InvalidNoAuthorization
        | InvalidUseOfDisputes
        | MerchandiseDeliveredOrShipped
        | MerchandiseOrServiceAsDescribed
        | NotCancelled
        | Other
        | RefundIssued
        | SubmittedBeyondAllowableTimeLimit
        | Transaction3dsRequired
        | TransactionApprovedAfterPriorFraudDispute
        | TransactionAuthorized
        | TransactionElectronicallyRead
        | TransactionQualifiesForVisaEasyPaymentService
        | TransactionUnattended

    [<Struct>]
    type IssuingCardholderPreferredLocales =
        | De
        | En
        | Es
        | Fr
        | It

    [<Struct>]
    type IssuingCardholderType =
        | Company
        | Individual

    [<Struct>]
    type IssuingCardholderStatus =
        | Active
        | Blocked
        | Inactive

    [<Struct>]
    type IssuingCardType =
        | Physical
        | Virtual

    [<Struct>]
    type IssuingCardStatus =
        | Active
        | Canceled
        | Inactive

    [<Struct>]
    type IssuingCardReplacementReason =
        | Damaged
        | Expired
        | FulfillmentError
        | Lost
        | Stolen

    [<Struct>]
    type IssuingCardCancellationReason =
        | DesignRejected
        | FulfillmentError
        | Lost
        | Stolen

    [<Struct>]
    type IssuingAuthorizationWallet =
        | ApplePay
        | GooglePay
        | SamsungPay

    [<Struct>]
    type IssuingAuthorizationStatus =
        | Closed
        | Expired
        | Pending
        | Reversed

    [<Struct>]
    type IssuingAuthorizationCardPresence =
        | NotPresent
        | Present

    [<Struct>]
    type IssuingAuthorizationAuthorizationMethod =
        | Chip
        | Contactless
        | KeyedIn
        | Online
        | Swipe

    type InvoicesResourceStatusTransitions = {
        ///The time that the invoice draft was finalized.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]FinalizedAt: DateTime option
        ///The time that the invoice was marked uncollectible.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]MarkedUncollectibleAt: DateTime option
        ///The time that the invoice was paid.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]PaidAt: DateTime option
        ///The time that the invoice was voided.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]VoidedAt: DateTime option
    }
    with
        static member New (finalizedAt: DateTime option, markedUncollectibleAt: DateTime option, paidAt: DateTime option, voidedAt: DateTime option) =
            {
                InvoicesResourceStatusTransitions.FinalizedAt = finalizedAt //required
                InvoicesResourceStatusTransitions.MarkedUncollectibleAt = markedUncollectibleAt //required
                InvoicesResourceStatusTransitions.PaidAt = paidAt //required
                InvoicesResourceStatusTransitions.VoidedAt = voidedAt //required
            }

    [<Struct>]
    type InvoicesResourcePretaxCreditAmountType =
        | CreditBalanceTransaction
        | Discount

    type InvoicesResourceInvoiceTaxIdType =
        | AdNrt
        | AeTrn
        | AlTin
        | AmTin
        | AoTin
        | ArCuit
        | AuAbn
        | AuArn
        | AwTin
        | AzTin
        | BaTin
        | BbTin
        | BdBin
        | BfIfu
        | BgUic
        | BhVat
        | BjIfu
        | BoTin
        | BrCnpj
        | BrCpf
        | BsTin
        | ByTin
        | CaBn
        | CaGstHst
        | CaPstBc
        | CaPstMb
        | CaPstSk
        | CaQst
        | CdNif
        | ChUid
        | ChVat
        | ClTin
        | CmNiu
        | CnTin
        | CoNit
        | CrTin
        | CvNif
        | DeStn
        | DoRcn
        | EcRuc
        | EgTin
        | EsCif
        | EtTin
        | EuOssVat
        | EuVat
        | FoVat
        | GbVat
        | GeVat
        | GiTin
        | GnNif
        | HkBr
        | HrOib
        | HuTin
        | IdNpwp
        | IlVat
        | InGst
        | IsVat
        | ItCf
        | JpCn
        | JpRn
        | JpTrn
        | KePin
        | KgTin
        | KhTin
        | KrBrn
        | KzBin
        | LaTin
        | LiUid
        | LiVat
        | LkVat
        | MaVat
        | MdVat
        | MePib
        | MkVat
        | MrNif
        | MxRfc
        | MyFrp
        | MyItn
        | MySst
        | NgTin
        | NoVat
        | NoVoec
        | NpPan
        | NzGst
        | OmVat
        | PeRuc
        | PhTin
        | PlNip
        | PyRuc
        | RoTin
        | RsPib
        | RuInn
        | RuKpp
        | SaVat
        | SgGst
        | SgUen
        | SiTin
        | SnNinea
        | SrFin
        | SvNit
        | ThVat
        | TjTin
        | TrTin
        | TwVat
        | TzVat
        | UaVat
        | UgTin
        | Unknown
        | UsEin
        | UyRuc
        | UzTin
        | UzVat
        | VeRif
        | VnTin
        | ZaVat
        | ZmTin
        | ZwTin

    type InvoicesResourceConfirmationSecret = {
        ///The client_secret of the payment that Stripe creates for the invoice after finalization.
        ClientSecret: string
        ///The type of client_secret. Currently this is always payment_intent, referencing the default payment_intent that Stripe creates during invoice finalization
        Type: string
    }
    with
        static member New (clientSecret: string, ``type``: string) =
            {
                InvoicesResourceConfirmationSecret.ClientSecret = clientSecret //required
                InvoicesResourceConfirmationSecret.Type = ``type`` //required
            }

    type InvoicesPaymentsInvoicePaymentStatusTransitions = {
        ///The time that the payment was canceled.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CanceledAt: DateTime option
        ///The time that the payment succeeded.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]PaidAt: DateTime option
    }
    with
        static member New (canceledAt: DateTime option, paidAt: DateTime option) =
            {
                InvoicesPaymentsInvoicePaymentStatusTransitions.CanceledAt = canceledAt //required
                InvoicesPaymentsInvoicePaymentStatusTransitions.PaidAt = paidAt //required
            }

    [<Struct>]
    type InvoicesPaymentsInvoicePaymentAssociatedPaymentType =
        | Charge
        | PaymentIntent
        | PaymentRecord

    type InvoicesPaymentSettingsPaymentMethodTypes =
        | AchCreditTransfer
        | AchDebit
        | AcssDebit
        | Affirm
        | AmazonPay
        | AuBecsDebit
        | BacsDebit
        | Bancontact
        | Boleto
        | Card
        | Cashapp
        | Crypto
        | Custom
        | CustomerBalance
        | Eps
        | Fpx
        | Giropay
        | Grabpay
        | Ideal
        | JpCreditTransfer
        | KakaoPay
        | Klarna
        | Konbini
        | KrCard
        | Link
        | Multibanco
        | NaverPay
        | NzBankAccount
        | P24
        | PayByBank
        | Payco
        | Paynow
        | Paypal
        | Payto
        | Pix
        | Promptpay
        | RevolutPay
        | SepaCreditTransfer
        | SepaDebit
        | Sofort
        | Swish
        | Upi
        | UsBankAccount
        | WechatPay

    type InvoiceSettingCustomerRenderingOptions = {
        ///How line-item prices and amounts will be displayed with respect to tax on invoice PDFs.
        AmountTaxDisplay: string option
        ///ID of the invoice rendering template to be used for this customer's invoices. If set, the template will be used on all invoices for this customer unless a template is set directly on the invoice.
        Template: string option
    }
    with
        static member New (amountTaxDisplay: string option, template: string option) =
            {
                InvoiceSettingCustomerRenderingOptions.AmountTaxDisplay = amountTaxDisplay //required
                InvoiceSettingCustomerRenderingOptions.Template = template //required
            }

    type InvoiceSettingCustomField = {
        ///The name of the custom field.
        Name: string
        ///The value of the custom field.
        Value: string
    }
    with
        static member New (name: string, value: string) =
            {
                InvoiceSettingCustomField.Name = name //required
                InvoiceSettingCustomField.Value = value //required
            }

    type InvoiceSettingCheckoutRenderingOptions = {
        ///How line-item prices and amounts will be displayed with respect to tax on invoice PDFs.
        AmountTaxDisplay: string option
        ///ID of the invoice rendering template to be used for the generated invoice.
        Template: string option
    }
    with
        static member New (amountTaxDisplay: string option, template: string option) =
            {
                InvoiceSettingCheckoutRenderingOptions.AmountTaxDisplay = amountTaxDisplay //required
                InvoiceSettingCheckoutRenderingOptions.Template = template //required
            }

    [<Struct>]
    type InvoiceRenderingTemplateStatus =
        | Active
        | Archived

    [<Struct>]
    type InvoiceRenderingPdfPageSize =
        | A4
        | Auto
        | Letter

    [<Struct>]
    type InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptionsFiltersAccountSubcategories =
        | Checking
        | Savings

    [<Struct>]
    type InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptionsPrefetch =
        | Balances
        | Ownership
        | Transactions

    [<Struct>]
    type InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptionsPermissions =
        | Balances
        | Ownership
        | PaymentMethod
        | Transactions

    [<Struct>]
    type InvoicePaymentMethodOptionsUsBankAccountVerificationMethod =
        | Automatic
        | Instant
        | Microdeposits

    type InvoicePaymentMethodOptionsSepaDebit = {
        InvoicePaymentMethodOptionsSepaDebit: string option
    }
    with
        static member New (?invoicePaymentMethodOptionsSepaDebit: string option) =
            {
                InvoicePaymentMethodOptionsSepaDebit.InvoicePaymentMethodOptionsSepaDebit = invoicePaymentMethodOptionsSepaDebit |> Option.flatten
            }

    [<Struct>]
    type InvoicePaymentMethodOptionsPixAmountIncludesIof =
        | Always
        | Never

    [<Struct>]
    type InvoicePaymentMethodOptionsMandateOptionsUpiAmountType =
        | Fixed
        | Maximum

    type InvoicePaymentMethodOptionsKonbini = {
        InvoicePaymentMethodOptionsKonbini: string option
    }
    with
        static member New (?invoicePaymentMethodOptionsKonbini: string option) =
            {
                InvoicePaymentMethodOptionsKonbini.InvoicePaymentMethodOptionsKonbini = invoicePaymentMethodOptionsKonbini |> Option.flatten
            }

    type InvoicePaymentMethodOptionsCustomerBalanceBankTransferEuBankTransferCountry =
        | [<JsonUnionCase("BE")>] BE
        | [<JsonUnionCase("DE")>] DE
        | [<JsonUnionCase("ES")>] ES
        | [<JsonUnionCase("FR")>] FR
        | [<JsonUnionCase("IE")>] IE
        | [<JsonUnionCase("NL")>] NL

    [<Struct>]
    type InvoicePaymentMethodOptionsCustomerBalanceBankTransferType =
        | EuBankTransfer
        | GbBankTransfer
        | JpBankTransfer
        | MxBankTransfer
        | UsBankTransfer

    [<Struct>]
    type InvoicePaymentMethodOptionsCardRequestThreeDSecure =
        | Any
        | Automatic
        | Challenge

    [<Struct>]
    type InvoicePaymentMethodOptionsBancontactPreferredLanguage =
        | De
        | En
        | Fr
        | Nl

    [<Struct>]
    type InvoicePaymentMethodOptionsAcssDebitMandateOptionsTransactionType =
        | Business
        | Personal

    [<Struct>]
    type InvoicePaymentMethodOptionsAcssDebitVerificationMethod =
        | Automatic
        | Instant
        | Microdeposits

    [<Struct>]
    type InvoicePaymentStatus =
        | Open
        | Paid
        | Canceled

    type InvoiceMandateOptionsPaytoPurpose =
        | DependantSupport
        | Government
        | Loan
        | Mortgage
        | Other
        | Pension
        | Personal
        | Retail
        | Salary
        | Tax
        | Utility

    [<Struct>]
    type InvoiceMandateOptionsPaytoAmountType =
        | Fixed
        | Maximum

    [<Struct>]
    type InvoiceMandateOptionsCardAmountType =
        | Fixed
        | Maximum

    type InvoiceLineItemPeriod = {
        ///The end of the period, which must be greater than or equal to the start. This value is inclusive.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]End: DateTime
        ///The start of the period. This value is inclusive.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Start: DateTime
    }
    with
        static member New (``end``: DateTime, start: DateTime) =
            {
                InvoiceLineItemPeriod.End = ``end`` //required
                InvoiceLineItemPeriod.Start = start //required
            }

    type InvoiceInstallmentsCard = {
        ///Whether Installments are enabled for this Invoice.
        Enabled: bool option
    }
    with
        static member New (enabled: bool option) =
            {
                InvoiceInstallmentsCard.Enabled = enabled //required
            }

    [<Struct>]
    type InvoiceStatus =
        | Draft
        | Open
        | Paid
        | Uncollectible
        | Void

    [<Struct>]
    type InvoiceCustomerTaxExempt =
        | Exempt
        | [<JsonUnionCase("none")>] None'
        | Reverse

    [<Struct>]
    type InvoiceCollectionMethod =
        | ChargeAutomatically
        | SendInvoice

    type InvoiceBillingReason =
        | AutomaticPendingInvoiceItemInvoice
        | Manual
        | QuoteAccept
        | Subscription
        | SubscriptionCreate
        | SubscriptionCycle
        | SubscriptionThreshold
        | SubscriptionUpdate
        | Upcoming

    [<Struct>]
    type InsightsResourcesPaymentEvaluationUserInterventionResolvedOutcome =
        | Abandoned
        | Failed
        | Passed

    ///User intervention raised custom event details attached to this payment evaluation
    type InsightsResourcesPaymentEvaluationUserInterventionRaisedCustom = {
        ///Custom type of user intervention raised. The string must use a snake case description for the type of intervention performed.
        Type: string
    }
    with
        static member New (``type``: string) =
            {
                InsightsResourcesPaymentEvaluationUserInterventionRaisedCustom.Type = ``type`` //required
            }

    [<Struct>]
    type InsightsResourcesPaymentEvaluationUserInterventionRaisedType =
        | [<JsonUnionCase("3ds")>] Numeric3ds
        | Captcha
        | Custom

    [<Struct>]
    type InsightsResourcesPaymentEvaluationSucceededCardCvcCheck =
        | Fail
        | Pass
        | Unavailable
        | Unchecked

    [<Struct>]
    type InsightsResourcesPaymentEvaluationSucceededCardAddressPostalCodeCheck =
        | Fail
        | Pass
        | Unavailable
        | Unchecked

    [<Struct>]
    type InsightsResourcesPaymentEvaluationSucceededCardAddressLine1Check =
        | Fail
        | Pass
        | Unavailable
        | Unchecked

    type InsightsResourcesPaymentEvaluationSignalV2RiskLevel =
        | Elevated
        | Highest
        | Low
        | Normal
        | NotAssessed
        | Unknown

    type InsightsResourcesPaymentEvaluationRejectedCardReason =
        | AuthenticationFailed
        | DoNotHonor
        | Expired
        | IncorrectCvc
        | IncorrectNumber
        | IncorrectPostalCode
        | InsufficientFunds
        | InvalidAccount
        | LostCard
        | Other
        | ProcessingError
        | ReportedStolen
        | TryAgainLater

    [<Struct>]
    type InsightsResourcesPaymentEvaluationRejectedCardCvcCheck =
        | Fail
        | Pass
        | Unavailable
        | Unchecked

    [<Struct>]
    type InsightsResourcesPaymentEvaluationRejectedCardAddressPostalCodeCheck =
        | Fail
        | Pass
        | Unavailable
        | Unchecked

    [<Struct>]
    type InsightsResourcesPaymentEvaluationRejectedCardAddressLine1Check =
        | Fail
        | Pass
        | Unavailable
        | Unchecked

    [<Struct>]
    type InsightsResourcesPaymentEvaluationRefundedReason =
        | Duplicate
        | Fraudulent
        | Other
        | RequestedByCustomer

    [<Struct>]
    type InsightsResourcesPaymentEvaluationOutcomeType =
        | Failed
        | MerchantBlocked
        | Rejected
        | Succeeded

    [<Struct>]
    type InsightsResourcesPaymentEvaluationMoneyMovementCardPaymentType =
        | OneOff
        | Recurring
        | SetupOneOff
        | SetupRecurring

    [<Struct>]
    type InsightsResourcesPaymentEvaluationMoneyMovementCardCustomerPresence =
        | OffSession
        | OnSession

    [<Struct>]
    type InsightsResourcesPaymentEvaluationMerchantBlockedReason =
        | AuthenticationRequired
        | BlockedForFraud
        | InvalidPayment
        | Other

    [<Struct>]
    type InsightsResourcesPaymentEvaluationEventType =
        | DisputeOpened
        | EarlyFraudWarningReceived
        | Refunded
        | UserInterventionRaised
        | UserInterventionResolved

    [<Struct>]
    type InsightsResourcesPaymentEvaluationEarlyFraudWarningReceivedFraudType =
        | MadeWithLostCard
        | MadeWithStolenCard
        | Other
        | UnauthorizedUseOfCard

    type InsightsResourcesPaymentEvaluationDisputeOpenedReason =
        | AccountNotAvailable
        | CreditNotProcessed
        | CustomerInitiated
        | Duplicate
        | Fraudulent
        | General
        | Noncompliant
        | ProductNotReceived
        | ProductUnacceptable
        | SubscriptionCanceled
        | Unrecognized

    ///Customer details attached to this payment evaluation.
    type InsightsResourcesPaymentEvaluationCustomerDetails = {
        ///The ID of the customer associated with the payment evaluation.
        Customer: string option
        ///The ID of the Account representing the customer associated with the payment evaluation.
        CustomerAccount: string option
        ///The customer's email address.
        Email: string option
        ///The customer's full name or business name.
        Name: string option
        ///The customer's phone number.
        Phone: string option
    }
    with
        static member New (customer: string option, customerAccount: string option, email: string option, name: string option, phone: string option) =
            {
                InsightsResourcesPaymentEvaluationCustomerDetails.Customer = customer //required
                InsightsResourcesPaymentEvaluationCustomerDetails.CustomerAccount = customerAccount //required
                InsightsResourcesPaymentEvaluationCustomerDetails.Email = email //required
                InsightsResourcesPaymentEvaluationCustomerDetails.Name = name //required
                InsightsResourcesPaymentEvaluationCustomerDetails.Phone = phone //required
            }

    ///Client device metadata attached to this payment evaluation.
    type InsightsResourcesPaymentEvaluationClientDeviceMetadata = {
        ///ID for the Radar Session associated with the payment evaluation. A [Radar Session](https://docs.stripe.com/radar/radar-session) is a snapshot of the browser metadata and device details that help Radar make more accurate predictions on your payments.
        RadarSession: string
    }
    with
        static member New (radarSession: string) =
            {
                InsightsResourcesPaymentEvaluationClientDeviceMetadata.RadarSession = radarSession //required
            }

    [<Struct>]
    type InboundTransfersPaymentMethodDetailsUsBankAccountAccountType =
        | Checking
        | Savings

    [<Struct>]
    type InboundTransfersPaymentMethodDetailsUsBankAccountAccountHolderType =
        | Company
        | Individual

    [<Struct>]
    type IdentityVerificationSessionType =
        | Document
        | IdNumber
        | VerificationFlow

    [<Struct>]
    type IdentityVerificationSessionStatus =
        | Canceled
        | Processing
        | RequiresInput
        | Verified

    [<Struct>]
    type IdentityVerificationReportType =
        | Document
        | IdNumber
        | VerificationFlow

    [<Struct>]
    type GelatoVerifiedOutputsSex =
        | [<JsonUnionCase("[redacted]")>] Redacted
        | Female
        | Male
        | Unknown

    [<Struct>]
    type GelatoVerifiedOutputsIdNumberType =
        | BrCpf
        | SgNric
        | UsSsn

    type GelatoSessionPhoneOptions = {
        ///Request one time password verification of `provided_details.phone`.
        RequireVerification: bool option
    }
    with
        static member New (?requireVerification: bool) =
            {
                GelatoSessionPhoneOptions.RequireVerification = requireVerification
            }

    [<Struct>]
    type GelatoSessionMatchingOptionsName =
        | [<JsonUnionCase("none")>] None'
        | Similar

    [<Struct>]
    type GelatoSessionMatchingOptionsDob =
        | [<JsonUnionCase("none")>] None'
        | Similar

    type GelatoSessionLastErrorCode =
        | Abandoned
        | ConsentDeclined
        | CountryNotSupported
        | DeviceNotSupported
        | DocumentExpired
        | DocumentTypeNotSupported
        | DocumentUnverifiedOther
        | EmailUnverifiedOther
        | EmailVerificationDeclined
        | IdNumberInsufficientDocumentData
        | IdNumberMismatch
        | IdNumberUnverifiedOther
        | PhoneUnverifiedOther
        | PhoneVerificationDeclined
        | SelfieDocumentMissingPhoto
        | SelfieFaceMismatch
        | SelfieManipulated
        | SelfieUnverifiedOther
        | UnderSupportedAge

    type GelatoSessionIdNumberOptions = {
        GelatoSessionIdNumberOptions: string option
    }
    with
        static member New (?gelatoSessionIdNumberOptions: string option) =
            {
                GelatoSessionIdNumberOptions.GelatoSessionIdNumberOptions = gelatoSessionIdNumberOptions |> Option.flatten
            }

    type GelatoSessionEmailOptions = {
        ///Request one time password verification of `provided_details.email`.
        RequireVerification: bool option
    }
    with
        static member New (?requireVerification: bool) =
            {
                GelatoSessionEmailOptions.RequireVerification = requireVerification
            }

    [<Struct>]
    type GelatoSessionDocumentOptionsAllowedTypes =
        | DrivingLicense
        | IdCard
        | Passport

    [<Struct>]
    type GelatoSelfieReportErrorCode =
        | SelfieDocumentMissingPhoto
        | SelfieFaceMismatch
        | SelfieManipulated
        | SelfieUnverifiedOther

    [<Struct>]
    type GelatoSelfieReportStatus =
        | Unverified
        | Verified

    type GelatoReportIdNumberOptions = {
        GelatoReportIdNumberOptions: string option
    }
    with
        static member New (?gelatoReportIdNumberOptions: string option) =
            {
                GelatoReportIdNumberOptions.GelatoReportIdNumberOptions = gelatoReportIdNumberOptions |> Option.flatten
            }

    [<Struct>]
    type GelatoReportDocumentOptionsAllowedTypes =
        | DrivingLicense
        | IdCard
        | Passport

    type GelatoRelatedPerson = {
        ///Token referencing the associated Account of the related Person resource.
        Account: string
        ///Token referencing the related Person resource.
        Person: string
    }
    with
        static member New (account: string, person: string) =
            {
                GelatoRelatedPerson.Account = account //required
                GelatoRelatedPerson.Person = person //required
            }

    type GelatoProvidedDetails = {
        ///Email of user being verified
        Email: string option
        ///Phone number of user being verified
        Phone: string option
    }
    with
        static member New (?email: string, ?phone: string) =
            {
                GelatoProvidedDetails.Email = email
                GelatoProvidedDetails.Phone = phone
            }

    [<Struct>]
    type GelatoPhoneReportErrorCode =
        | PhoneUnverifiedOther
        | PhoneVerificationDeclined

    [<Struct>]
    type GelatoPhoneReportStatus =
        | Unverified
        | Verified

    [<Struct>]
    type GelatoIdNumberReportErrorCode =
        | IdNumberInsufficientDocumentData
        | IdNumberMismatch
        | IdNumberUnverifiedOther

    [<Struct>]
    type GelatoIdNumberReportStatus =
        | Unverified
        | Verified

    [<Struct>]
    type GelatoIdNumberReportIdNumberType =
        | BrCpf
        | SgNric
        | UsSsn

    [<Struct>]
    type GelatoEmailReportErrorCode =
        | EmailUnverifiedOther
        | EmailVerificationDeclined

    [<Struct>]
    type GelatoEmailReportStatus =
        | Unverified
        | Verified

    [<Struct>]
    type GelatoDocumentReportErrorCode =
        | DocumentExpired
        | DocumentTypeNotSupported
        | DocumentUnverifiedOther

    [<Struct>]
    type GelatoDocumentReportType =
        | DrivingLicense
        | IdCard
        | Passport

    [<Struct>]
    type GelatoDocumentReportStatus =
        | Unverified
        | Verified

    [<Struct>]
    type GelatoDocumentReportSex =
        | [<JsonUnionCase("[redacted]")>] Redacted
        | Female
        | Male
        | Unknown

    ///Point in Time
    type GelatoDataVerifiedOutputsDate = {
        ///Numerical day between 1 and 31.
        Day: int option
        ///Numerical month between 1 and 12.
        Month: int option
        ///The four-digit year.
        Year: int option
    }
    with
        static member New (day: int option, month: int option, year: int option) =
            {
                GelatoDataVerifiedOutputsDate.Day = day //required
                GelatoDataVerifiedOutputsDate.Month = month //required
                GelatoDataVerifiedOutputsDate.Year = year //required
            }

    ///Point in Time
    type GelatoDataIdNumberReportDate = {
        ///Numerical day between 1 and 31.
        Day: int option
        ///Numerical month between 1 and 12.
        Month: int option
        ///The four-digit year.
        Year: int option
    }
    with
        static member New (day: int option, month: int option, year: int option) =
            {
                GelatoDataIdNumberReportDate.Day = day //required
                GelatoDataIdNumberReportDate.Month = month //required
                GelatoDataIdNumberReportDate.Year = year //required
            }

    ///Point in Time
    type GelatoDataDocumentReportIssuedDate = {
        ///Numerical day between 1 and 31.
        Day: int option
        ///Numerical month between 1 and 12.
        Month: int option
        ///The four-digit year.
        Year: int option
    }
    with
        static member New (day: int option, month: int option, year: int option) =
            {
                GelatoDataDocumentReportIssuedDate.Day = day //required
                GelatoDataDocumentReportIssuedDate.Month = month //required
                GelatoDataDocumentReportIssuedDate.Year = year //required
            }

    ///Point in Time
    type GelatoDataDocumentReportExpirationDate = {
        ///Numerical day between 1 and 31.
        Day: int option
        ///Numerical month between 1 and 12.
        Month: int option
        ///The four-digit year.
        Year: int option
    }
    with
        static member New (day: int option, month: int option, year: int option) =
            {
                GelatoDataDocumentReportExpirationDate.Day = day //required
                GelatoDataDocumentReportExpirationDate.Month = month //required
                GelatoDataDocumentReportExpirationDate.Year = year //required
            }

    ///Point in Time
    type GelatoDataDocumentReportDateOfBirth = {
        ///Numerical day between 1 and 31.
        Day: int option
        ///Numerical month between 1 and 12.
        Month: int option
        ///The four-digit year.
        Year: int option
    }
    with
        static member New (day: int option, month: int option, year: int option) =
            {
                GelatoDataDocumentReportDateOfBirth.Day = day //required
                GelatoDataDocumentReportDateOfBirth.Month = month //required
                GelatoDataDocumentReportDateOfBirth.Year = year //required
            }

    [<Struct>]
    type FundingInstructionsBankTransferZenginRecordAccountType =
        | Futsu
        | Toza

    type FundingInstructionsBankTransferFinancialAddressSupportedNetworks =
        | Ach
        | Bacs
        | DomesticWireUs
        | Fps
        | Sepa
        | Spei
        | Swift
        | Zengin

    type FundingInstructionsBankTransferFinancialAddressType =
        | Aba
        | Iban
        | SortCode
        | Spei
        | Swift
        | Zengin

    [<Struct>]
    type FundingInstructionsBankTransferType =
        | EuBankTransfer
        | JpBankTransfer

    [<Struct>]
    type ForwardingRequestReplacements =
        | CardCvc
        | CardExpiry
        | CardNumber
        | CardholderName
        | RequestSignature

    ///Header data.
    type ForwardedRequestHeader = {
        ///The header name.
        Name: string
        ///The header value.
        Value: string
    }
    with
        static member New (name: string, value: string) =
            {
                ForwardedRequestHeader.Name = name //required
                ForwardedRequestHeader.Value = value //required
            }

    ///Metadata about the forwarded request.
    type ForwardedRequestContext = {
        ///The time it took in milliseconds for the destination endpoint to respond.
        DestinationDuration: int
        ///The IP address of the destination.
        DestinationIpAddress: string
    }
    with
        static member New (destinationDuration: int, destinationIpAddress: string) =
            {
                ForwardedRequestContext.DestinationDuration = destinationDuration //required
                ForwardedRequestContext.DestinationIpAddress = destinationIpAddress //required
            }

    [<Struct>]
    type FinancialReportingFinanceReportRunRunParametersTimezone =
        | IntervalStart
        | IntervalEnd

    [<Struct>]
    type FinancialConnectionsTransactionStatus =
        | Pending
        | Posted
        | Void

    [<Struct>]
    type FinancialConnectionsSessionPrefetch =
        | Balances
        | Ownership
        | Transactions

    [<Struct>]
    type FinancialConnectionsSessionPermissions =
        | Balances
        | Ownership
        | PaymentMethod
        | Transactions

    ///Describes an owner of an account.
    type FinancialConnectionsAccountOwner = {
        ///The email address of the owner.
        Email: string option
        ///Unique identifier for the object.
        Id: string
        ///The full name of the owner.
        Name: string
        ///The ownership object that this owner belongs to.
        Ownership: string
        ///The raw phone number of the owner.
        Phone: string option
        ///The raw physical address of the owner.
        RawAddress: string option
        ///The timestamp of the refresh that updated this owner.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]RefreshedAt: DateTime option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "financial_connections.account_owner"

        static member New (email: string option, id: string, name: string, ownership: string, phone: string option, rawAddress: string option, refreshedAt: DateTime option) =
            {
                FinancialConnectionsAccountOwner.Email = email //required
                FinancialConnectionsAccountOwner.Id = id //required
                FinancialConnectionsAccountOwner.Name = name //required
                FinancialConnectionsAccountOwner.Ownership = ownership //required
                FinancialConnectionsAccountOwner.Phone = phone //required
                FinancialConnectionsAccountOwner.RawAddress = rawAddress //required
                FinancialConnectionsAccountOwner.RefreshedAt = refreshedAt //required
            }

    [<Struct>]
    type FinancialConnectionsAccountSupportedPaymentMethodTypes =
        | Link
        | UsBankAccount

    [<Struct>]
    type FinancialConnectionsAccountPermissions =
        | Balances
        | Ownership
        | PaymentMethod
        | Transactions

    type FinancialConnectionsAccountSubcategory =
        | Checking
        | CreditCard
        | LineOfCredit
        | Mortgage
        | Other
        | Savings

    [<Struct>]
    type FinancialConnectionsAccountStatus =
        | Active
        | Disconnected
        | Inactive

    [<Struct>]
    type FinancialConnectionsAccountCategory =
        | Cash
        | Credit
        | Investment
        | Other

    type FilePurpose =
        | AccountRequirement
        | AdditionalVerification
        | BusinessIcon
        | BusinessLogo
        | CustomerSignature
        | DisputeEvidence
        | DocumentProviderIdentityDocument
        | FinanceReportRun
        | FinancialAccountStatement
        | IdentityDocument
        | IdentityDocumentDownloadable
        | IssuingRegulatoryReporting
        | PciDocument
        | PlatformTermsOfService
        | Selfie
        | SigmaScheduledQuery
        | TaxDocumentUserUpload
        | TerminalAndroidApk
        | TerminalReaderSplashscreen
        | TerminalWifiCertificate
        | TerminalWifiPrivateKey

    [<Struct>]
    type FeeType =
        | ApplicationFee
        | PaymentMethodPassthroughFee
        | StripeFee
        | Tax
        | WithheldTax

    type EventType =
        | [<JsonUnionCase("account.application.authorized")>] AccountApplicationAuthorized
        | [<JsonUnionCase("account.application.deauthorized")>] AccountApplicationDeauthorized
        | [<JsonUnionCase("account.external_account.created")>] AccountExternalAccountCreated
        | [<JsonUnionCase("account.external_account.deleted")>] AccountExternalAccountDeleted
        | [<JsonUnionCase("account.external_account.updated")>] AccountExternalAccountUpdated
        | [<JsonUnionCase("account.updated")>] AccountUpdated
        | [<JsonUnionCase("application_fee.created")>] ApplicationFeeCreated
        | [<JsonUnionCase("application_fee.refund.updated")>] ApplicationFeeRefundUpdated
        | [<JsonUnionCase("application_fee.refunded")>] ApplicationFeeRefunded
        | [<JsonUnionCase("balance.available")>] BalanceAvailable
        | [<JsonUnionCase("balance_settings.updated")>] BalanceSettingsUpdated
        | [<JsonUnionCase("billing.alert.triggered")>] BillingAlertTriggered
        | [<JsonUnionCase("billing.credit_grant.created")>] BillingCreditGrantCreated
        | [<JsonUnionCase("billing_portal.configuration.created")>] BillingPortalConfigurationCreated
        | [<JsonUnionCase("billing_portal.configuration.updated")>] BillingPortalConfigurationUpdated
        | [<JsonUnionCase("billing_portal.session.created")>] BillingPortalSessionCreated
        | [<JsonUnionCase("capability.updated")>] CapabilityUpdated
        | [<JsonUnionCase("cash_balance.funds_available")>] CashBalanceFundsAvailable
        | [<JsonUnionCase("charge.captured")>] ChargeCaptured
        | [<JsonUnionCase("charge.dispute.closed")>] ChargeDisputeClosed
        | [<JsonUnionCase("charge.dispute.created")>] ChargeDisputeCreated
        | [<JsonUnionCase("charge.dispute.funds_reinstated")>] ChargeDisputeFundsReinstated
        | [<JsonUnionCase("charge.dispute.funds_withdrawn")>] ChargeDisputeFundsWithdrawn
        | [<JsonUnionCase("charge.dispute.updated")>] ChargeDisputeUpdated
        | [<JsonUnionCase("charge.expired")>] ChargeExpired
        | [<JsonUnionCase("charge.failed")>] ChargeFailed
        | [<JsonUnionCase("charge.pending")>] ChargePending
        | [<JsonUnionCase("charge.refund.updated")>] ChargeRefundUpdated
        | [<JsonUnionCase("charge.refunded")>] ChargeRefunded
        | [<JsonUnionCase("charge.succeeded")>] ChargeSucceeded
        | [<JsonUnionCase("charge.updated")>] ChargeUpdated
        | [<JsonUnionCase("checkout.session.async_payment_failed")>] CheckoutSessionAsyncPaymentFailed
        | [<JsonUnionCase("checkout.session.async_payment_succeeded")>] CheckoutSessionAsyncPaymentSucceeded
        | [<JsonUnionCase("checkout.session.completed")>] CheckoutSessionCompleted
        | [<JsonUnionCase("checkout.session.expired")>] CheckoutSessionExpired
        | [<JsonUnionCase("climate.order.canceled")>] ClimateOrderCanceled
        | [<JsonUnionCase("climate.order.created")>] ClimateOrderCreated
        | [<JsonUnionCase("climate.order.delayed")>] ClimateOrderDelayed
        | [<JsonUnionCase("climate.order.delivered")>] ClimateOrderDelivered
        | [<JsonUnionCase("climate.order.product_substituted")>] ClimateOrderProductSubstituted
        | [<JsonUnionCase("climate.product.created")>] ClimateProductCreated
        | [<JsonUnionCase("climate.product.pricing_updated")>] ClimateProductPricingUpdated
        | [<JsonUnionCase("coupon.created")>] CouponCreated
        | [<JsonUnionCase("coupon.deleted")>] CouponDeleted
        | [<JsonUnionCase("coupon.updated")>] CouponUpdated
        | [<JsonUnionCase("credit_note.created")>] CreditNoteCreated
        | [<JsonUnionCase("credit_note.updated")>] CreditNoteUpdated
        | [<JsonUnionCase("credit_note.voided")>] CreditNoteVoided
        | [<JsonUnionCase("customer.created")>] CustomerCreated
        | [<JsonUnionCase("customer.deleted")>] CustomerDeleted
        | [<JsonUnionCase("customer.discount.created")>] CustomerDiscountCreated
        | [<JsonUnionCase("customer.discount.deleted")>] CustomerDiscountDeleted
        | [<JsonUnionCase("customer.discount.updated")>] CustomerDiscountUpdated
        | [<JsonUnionCase("customer.source.created")>] CustomerSourceCreated
        | [<JsonUnionCase("customer.source.deleted")>] CustomerSourceDeleted
        | [<JsonUnionCase("customer.source.expiring")>] CustomerSourceExpiring
        | [<JsonUnionCase("customer.source.updated")>] CustomerSourceUpdated
        | [<JsonUnionCase("customer.subscription.created")>] CustomerSubscriptionCreated
        | [<JsonUnionCase("customer.subscription.deleted")>] CustomerSubscriptionDeleted
        | [<JsonUnionCase("customer.subscription.paused")>] CustomerSubscriptionPaused
        | [<JsonUnionCase("customer.subscription.pending_update_applied")>] CustomerSubscriptionPendingUpdateApplied
        | [<JsonUnionCase("customer.subscription.pending_update_expired")>] CustomerSubscriptionPendingUpdateExpired
        | [<JsonUnionCase("customer.subscription.resumed")>] CustomerSubscriptionResumed
        | [<JsonUnionCase("customer.subscription.trial_will_end")>] CustomerSubscriptionTrialWillEnd
        | [<JsonUnionCase("customer.subscription.updated")>] CustomerSubscriptionUpdated
        | [<JsonUnionCase("customer.tax_id.created")>] CustomerTaxIdCreated
        | [<JsonUnionCase("customer.tax_id.deleted")>] CustomerTaxIdDeleted
        | [<JsonUnionCase("customer.tax_id.updated")>] CustomerTaxIdUpdated
        | [<JsonUnionCase("customer.updated")>] CustomerUpdated
        | [<JsonUnionCase("customer_cash_balance_transaction.created")>] CustomerCashBalanceTransactionCreated
        | [<JsonUnionCase("entitlements.active_entitlement_summary.updated")>] EntitlementsActiveEntitlementSummaryUpdated
        | [<JsonUnionCase("file.created")>] FileCreated
        | [<JsonUnionCase("financial_connections.account.account_numbers_updated")>] FinancialConnectionsAccountAccountNumbersUpdated
        | [<JsonUnionCase("financial_connections.account.created")>] FinancialConnectionsAccountCreated
        | [<JsonUnionCase("financial_connections.account.deactivated")>] FinancialConnectionsAccountDeactivated
        | [<JsonUnionCase("financial_connections.account.disconnected")>] FinancialConnectionsAccountDisconnected
        | [<JsonUnionCase("financial_connections.account.reactivated")>] FinancialConnectionsAccountReactivated
        | [<JsonUnionCase("financial_connections.account.refreshed_balance")>] FinancialConnectionsAccountRefreshedBalance
        | [<JsonUnionCase("financial_connections.account.refreshed_ownership")>] FinancialConnectionsAccountRefreshedOwnership
        | [<JsonUnionCase("financial_connections.account.refreshed_transactions")>] FinancialConnectionsAccountRefreshedTransactions
        | [<JsonUnionCase("financial_connections.account.upcoming_account_number_expiry")>] FinancialConnectionsAccountUpcomingAccountNumberExpiry
        | [<JsonUnionCase("identity.verification_session.canceled")>] IdentityVerificationSessionCanceled
        | [<JsonUnionCase("identity.verification_session.created")>] IdentityVerificationSessionCreated
        | [<JsonUnionCase("identity.verification_session.processing")>] IdentityVerificationSessionProcessing
        | [<JsonUnionCase("identity.verification_session.redacted")>] IdentityVerificationSessionRedacted
        | [<JsonUnionCase("identity.verification_session.requires_input")>] IdentityVerificationSessionRequiresInput
        | [<JsonUnionCase("identity.verification_session.verified")>] IdentityVerificationSessionVerified
        | [<JsonUnionCase("invoice.created")>] InvoiceCreated
        | [<JsonUnionCase("invoice.deleted")>] InvoiceDeleted
        | [<JsonUnionCase("invoice.finalization_failed")>] InvoiceFinalizationFailed
        | [<JsonUnionCase("invoice.finalized")>] InvoiceFinalized
        | [<JsonUnionCase("invoice.marked_uncollectible")>] InvoiceMarkedUncollectible
        | [<JsonUnionCase("invoice.overdue")>] InvoiceOverdue
        | [<JsonUnionCase("invoice.overpaid")>] InvoiceOverpaid
        | [<JsonUnionCase("invoice.paid")>] InvoicePaid
        | [<JsonUnionCase("invoice.payment_action_required")>] InvoicePaymentActionRequired
        | [<JsonUnionCase("invoice.payment_attempt_required")>] InvoicePaymentAttemptRequired
        | [<JsonUnionCase("invoice.payment_failed")>] InvoicePaymentFailed
        | [<JsonUnionCase("invoice.payment_succeeded")>] InvoicePaymentSucceeded
        | [<JsonUnionCase("invoice.sent")>] InvoiceSent
        | [<JsonUnionCase("invoice.upcoming")>] InvoiceUpcoming
        | [<JsonUnionCase("invoice.updated")>] InvoiceUpdated
        | [<JsonUnionCase("invoice.voided")>] InvoiceVoided
        | [<JsonUnionCase("invoice.will_be_due")>] InvoiceWillBeDue
        | [<JsonUnionCase("invoice_payment.paid")>] InvoicePaymentPaid
        | [<JsonUnionCase("invoiceitem.created")>] InvoiceitemCreated
        | [<JsonUnionCase("invoiceitem.deleted")>] InvoiceitemDeleted
        | [<JsonUnionCase("issuing_authorization.created")>] IssuingAuthorizationCreated
        | [<JsonUnionCase("issuing_authorization.request")>] IssuingAuthorizationRequest
        | [<JsonUnionCase("issuing_authorization.updated")>] IssuingAuthorizationUpdated
        | [<JsonUnionCase("issuing_card.created")>] IssuingCardCreated
        | [<JsonUnionCase("issuing_card.updated")>] IssuingCardUpdated
        | [<JsonUnionCase("issuing_cardholder.created")>] IssuingCardholderCreated
        | [<JsonUnionCase("issuing_cardholder.updated")>] IssuingCardholderUpdated
        | [<JsonUnionCase("issuing_dispute.closed")>] IssuingDisputeClosed
        | [<JsonUnionCase("issuing_dispute.created")>] IssuingDisputeCreated
        | [<JsonUnionCase("issuing_dispute.funds_reinstated")>] IssuingDisputeFundsReinstated
        | [<JsonUnionCase("issuing_dispute.funds_rescinded")>] IssuingDisputeFundsRescinded
        | [<JsonUnionCase("issuing_dispute.submitted")>] IssuingDisputeSubmitted
        | [<JsonUnionCase("issuing_dispute.updated")>] IssuingDisputeUpdated
        | [<JsonUnionCase("issuing_personalization_design.activated")>] IssuingPersonalizationDesignActivated
        | [<JsonUnionCase("issuing_personalization_design.deactivated")>] IssuingPersonalizationDesignDeactivated
        | [<JsonUnionCase("issuing_personalization_design.rejected")>] IssuingPersonalizationDesignRejected
        | [<JsonUnionCase("issuing_personalization_design.updated")>] IssuingPersonalizationDesignUpdated
        | [<JsonUnionCase("issuing_token.created")>] IssuingTokenCreated
        | [<JsonUnionCase("issuing_token.updated")>] IssuingTokenUpdated
        | [<JsonUnionCase("issuing_transaction.created")>] IssuingTransactionCreated
        | [<JsonUnionCase("issuing_transaction.purchase_details_receipt_updated")>] IssuingTransactionPurchaseDetailsReceiptUpdated
        | [<JsonUnionCase("issuing_transaction.updated")>] IssuingTransactionUpdated
        | [<JsonUnionCase("mandate.updated")>] MandateUpdated
        | [<JsonUnionCase("payment_intent.amount_capturable_updated")>] PaymentIntentAmountCapturableUpdated
        | [<JsonUnionCase("payment_intent.canceled")>] PaymentIntentCanceled
        | [<JsonUnionCase("payment_intent.created")>] PaymentIntentCreated
        | [<JsonUnionCase("payment_intent.partially_funded")>] PaymentIntentPartiallyFunded
        | [<JsonUnionCase("payment_intent.payment_failed")>] PaymentIntentPaymentFailed
        | [<JsonUnionCase("payment_intent.processing")>] PaymentIntentProcessing
        | [<JsonUnionCase("payment_intent.requires_action")>] PaymentIntentRequiresAction
        | [<JsonUnionCase("payment_intent.succeeded")>] PaymentIntentSucceeded
        | [<JsonUnionCase("payment_link.created")>] PaymentLinkCreated
        | [<JsonUnionCase("payment_link.updated")>] PaymentLinkUpdated
        | [<JsonUnionCase("payment_method.attached")>] PaymentMethodAttached
        | [<JsonUnionCase("payment_method.automatically_updated")>] PaymentMethodAutomaticallyUpdated
        | [<JsonUnionCase("payment_method.detached")>] PaymentMethodDetached
        | [<JsonUnionCase("payment_method.updated")>] PaymentMethodUpdated
        | [<JsonUnionCase("payout.canceled")>] PayoutCanceled
        | [<JsonUnionCase("payout.created")>] PayoutCreated
        | [<JsonUnionCase("payout.failed")>] PayoutFailed
        | [<JsonUnionCase("payout.paid")>] PayoutPaid
        | [<JsonUnionCase("payout.reconciliation_completed")>] PayoutReconciliationCompleted
        | [<JsonUnionCase("payout.updated")>] PayoutUpdated
        | [<JsonUnionCase("person.created")>] PersonCreated
        | [<JsonUnionCase("person.deleted")>] PersonDeleted
        | [<JsonUnionCase("person.updated")>] PersonUpdated
        | [<JsonUnionCase("plan.created")>] PlanCreated
        | [<JsonUnionCase("plan.deleted")>] PlanDeleted
        | [<JsonUnionCase("plan.updated")>] PlanUpdated
        | [<JsonUnionCase("price.created")>] PriceCreated
        | [<JsonUnionCase("price.deleted")>] PriceDeleted
        | [<JsonUnionCase("price.updated")>] PriceUpdated
        | [<JsonUnionCase("product.created")>] ProductCreated
        | [<JsonUnionCase("product.deleted")>] ProductDeleted
        | [<JsonUnionCase("product.updated")>] ProductUpdated
        | [<JsonUnionCase("promotion_code.created")>] PromotionCodeCreated
        | [<JsonUnionCase("promotion_code.updated")>] PromotionCodeUpdated
        | [<JsonUnionCase("quote.accepted")>] QuoteAccepted
        | [<JsonUnionCase("quote.canceled")>] QuoteCanceled
        | [<JsonUnionCase("quote.created")>] QuoteCreated
        | [<JsonUnionCase("quote.finalized")>] QuoteFinalized
        | [<JsonUnionCase("radar.early_fraud_warning.created")>] RadarEarlyFraudWarningCreated
        | [<JsonUnionCase("radar.early_fraud_warning.updated")>] RadarEarlyFraudWarningUpdated
        | [<JsonUnionCase("refund.created")>] RefundCreated
        | [<JsonUnionCase("refund.failed")>] RefundFailed
        | [<JsonUnionCase("refund.updated")>] RefundUpdated
        | [<JsonUnionCase("reporting.report_run.failed")>] ReportingReportRunFailed
        | [<JsonUnionCase("reporting.report_run.succeeded")>] ReportingReportRunSucceeded
        | [<JsonUnionCase("reporting.report_type.updated")>] ReportingReportTypeUpdated
        | [<JsonUnionCase("reserve.hold.created")>] ReserveHoldCreated
        | [<JsonUnionCase("reserve.hold.updated")>] ReserveHoldUpdated
        | [<JsonUnionCase("reserve.plan.created")>] ReservePlanCreated
        | [<JsonUnionCase("reserve.plan.disabled")>] ReservePlanDisabled
        | [<JsonUnionCase("reserve.plan.expired")>] ReservePlanExpired
        | [<JsonUnionCase("reserve.plan.updated")>] ReservePlanUpdated
        | [<JsonUnionCase("reserve.release.created")>] ReserveReleaseCreated
        | [<JsonUnionCase("review.closed")>] ReviewClosed
        | [<JsonUnionCase("review.opened")>] ReviewOpened
        | [<JsonUnionCase("setup_intent.canceled")>] SetupIntentCanceled
        | [<JsonUnionCase("setup_intent.created")>] SetupIntentCreated
        | [<JsonUnionCase("setup_intent.requires_action")>] SetupIntentRequiresAction
        | [<JsonUnionCase("setup_intent.setup_failed")>] SetupIntentSetupFailed
        | [<JsonUnionCase("setup_intent.succeeded")>] SetupIntentSucceeded
        | [<JsonUnionCase("sigma.scheduled_query_run.created")>] SigmaScheduledQueryRunCreated
        | [<JsonUnionCase("source.canceled")>] SourceCanceled
        | [<JsonUnionCase("source.chargeable")>] SourceChargeable
        | [<JsonUnionCase("source.failed")>] SourceFailed
        | [<JsonUnionCase("source.mandate_notification")>] SourceMandateNotification
        | [<JsonUnionCase("source.refund_attributes_required")>] SourceRefundAttributesRequired
        | [<JsonUnionCase("source.transaction.created")>] SourceTransactionCreated
        | [<JsonUnionCase("source.transaction.updated")>] SourceTransactionUpdated
        | [<JsonUnionCase("subscription_schedule.aborted")>] SubscriptionScheduleAborted
        | [<JsonUnionCase("subscription_schedule.canceled")>] SubscriptionScheduleCanceled
        | [<JsonUnionCase("subscription_schedule.completed")>] SubscriptionScheduleCompleted
        | [<JsonUnionCase("subscription_schedule.created")>] SubscriptionScheduleCreated
        | [<JsonUnionCase("subscription_schedule.expiring")>] SubscriptionScheduleExpiring
        | [<JsonUnionCase("subscription_schedule.released")>] SubscriptionScheduleReleased
        | [<JsonUnionCase("subscription_schedule.updated")>] SubscriptionScheduleUpdated
        | [<JsonUnionCase("tax.settings.updated")>] TaxSettingsUpdated
        | [<JsonUnionCase("tax_rate.created")>] TaxRateCreated
        | [<JsonUnionCase("tax_rate.updated")>] TaxRateUpdated
        | [<JsonUnionCase("terminal.reader.action_failed")>] TerminalReaderActionFailed
        | [<JsonUnionCase("terminal.reader.action_succeeded")>] TerminalReaderActionSucceeded
        | [<JsonUnionCase("terminal.reader.action_updated")>] TerminalReaderActionUpdated
        | [<JsonUnionCase("test_helpers.test_clock.advancing")>] TestHelpersTestClockAdvancing
        | [<JsonUnionCase("test_helpers.test_clock.created")>] TestHelpersTestClockCreated
        | [<JsonUnionCase("test_helpers.test_clock.deleted")>] TestHelpersTestClockDeleted
        | [<JsonUnionCase("test_helpers.test_clock.internal_failure")>] TestHelpersTestClockInternalFailure
        | [<JsonUnionCase("test_helpers.test_clock.ready")>] TestHelpersTestClockReady
        | [<JsonUnionCase("topup.canceled")>] TopupCanceled
        | [<JsonUnionCase("topup.created")>] TopupCreated
        | [<JsonUnionCase("topup.failed")>] TopupFailed
        | [<JsonUnionCase("topup.reversed")>] TopupReversed
        | [<JsonUnionCase("topup.succeeded")>] TopupSucceeded
        | [<JsonUnionCase("transfer.created")>] TransferCreated
        | [<JsonUnionCase("transfer.reversed")>] TransferReversed
        | [<JsonUnionCase("transfer.updated")>] TransferUpdated
        | [<JsonUnionCase("treasury.credit_reversal.created")>] TreasuryCreditReversalCreated
        | [<JsonUnionCase("treasury.credit_reversal.posted")>] TreasuryCreditReversalPosted
        | [<JsonUnionCase("treasury.debit_reversal.completed")>] TreasuryDebitReversalCompleted
        | [<JsonUnionCase("treasury.debit_reversal.created")>] TreasuryDebitReversalCreated
        | [<JsonUnionCase("treasury.debit_reversal.initial_credit_granted")>] TreasuryDebitReversalInitialCreditGranted
        | [<JsonUnionCase("treasury.financial_account.closed")>] TreasuryFinancialAccountClosed
        | [<JsonUnionCase("treasury.financial_account.created")>] TreasuryFinancialAccountCreated
        | [<JsonUnionCase("treasury.financial_account.features_status_updated")>] TreasuryFinancialAccountFeaturesStatusUpdated
        | [<JsonUnionCase("treasury.inbound_transfer.canceled")>] TreasuryInboundTransferCanceled
        | [<JsonUnionCase("treasury.inbound_transfer.created")>] TreasuryInboundTransferCreated
        | [<JsonUnionCase("treasury.inbound_transfer.failed")>] TreasuryInboundTransferFailed
        | [<JsonUnionCase("treasury.inbound_transfer.succeeded")>] TreasuryInboundTransferSucceeded
        | [<JsonUnionCase("treasury.outbound_payment.canceled")>] TreasuryOutboundPaymentCanceled
        | [<JsonUnionCase("treasury.outbound_payment.created")>] TreasuryOutboundPaymentCreated
        | [<JsonUnionCase("treasury.outbound_payment.expected_arrival_date_updated")>] TreasuryOutboundPaymentExpectedArrivalDateUpdated
        | [<JsonUnionCase("treasury.outbound_payment.failed")>] TreasuryOutboundPaymentFailed
        | [<JsonUnionCase("treasury.outbound_payment.posted")>] TreasuryOutboundPaymentPosted
        | [<JsonUnionCase("treasury.outbound_payment.returned")>] TreasuryOutboundPaymentReturned
        | [<JsonUnionCase("treasury.outbound_payment.tracking_details_updated")>] TreasuryOutboundPaymentTrackingDetailsUpdated
        | [<JsonUnionCase("treasury.outbound_transfer.canceled")>] TreasuryOutboundTransferCanceled
        | [<JsonUnionCase("treasury.outbound_transfer.created")>] TreasuryOutboundTransferCreated
        | [<JsonUnionCase("treasury.outbound_transfer.expected_arrival_date_updated")>] TreasuryOutboundTransferExpectedArrivalDateUpdated
        | [<JsonUnionCase("treasury.outbound_transfer.failed")>] TreasuryOutboundTransferFailed
        | [<JsonUnionCase("treasury.outbound_transfer.posted")>] TreasuryOutboundTransferPosted
        | [<JsonUnionCase("treasury.outbound_transfer.returned")>] TreasuryOutboundTransferReturned
        | [<JsonUnionCase("treasury.outbound_transfer.tracking_details_updated")>] TreasuryOutboundTransferTrackingDetailsUpdated
        | [<JsonUnionCase("treasury.received_credit.created")>] TreasuryReceivedCreditCreated
        | [<JsonUnionCase("treasury.received_credit.failed")>] TreasuryReceivedCreditFailed
        | [<JsonUnionCase("treasury.received_credit.succeeded")>] TreasuryReceivedCreditSucceeded
        | [<JsonUnionCase("treasury.received_debit.created")>] TreasuryReceivedDebitCreated

    type EphemeralKey = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Time at which the key will expire. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Expires: DateTime
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The key's secret. You can use this value to make authorized requests to the Stripe API.
        Secret: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "ephemeral_key"

        static member New (created: DateTime, expires: DateTime, id: string, livemode: bool, ?secret: string) =
            {
                EphemeralKey.Created = created //required
                EphemeralKey.Expires = expires //required
                EphemeralKey.Id = id //required
                EphemeralKey.Livemode = livemode //required
                EphemeralKey.Secret = secret
            }

    type EmailSent = {
        ///The timestamp when the email was sent.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]EmailSentAt: DateTime
        ///The recipient's email address.
        EmailSentTo: string
    }
    with
        static member New (emailSentAt: DateTime, emailSentTo: string) =
            {
                EmailSent.EmailSentAt = emailSentAt //required
                EmailSent.EmailSentTo = emailSentTo //required
            }

    [<Struct>]
    type DisputeVisaCompellingEvidence3DisputedTransactionMerchandiseOrServices =
        | Merchandise
        | Services

    type DisputePaymentMethodDetailsPaypal = {
        ///The ID of the dispute in PayPal.
        CaseId: string option
        ///The reason for the dispute as defined by PayPal
        ReasonCode: string option
    }
    with
        static member New (caseId: string option, reasonCode: string option) =
            {
                DisputePaymentMethodDetailsPaypal.CaseId = caseId //required
                DisputePaymentMethodDetailsPaypal.ReasonCode = reasonCode //required
            }

    type DisputePaymentMethodDetailsKlarna = {
        ///Chargeback loss reason mapped by Stripe from Klarna's chargeback loss reason
        ChargebackLossReasonCode: string option
        ///The reason for the dispute as defined by Klarna
        ReasonCode: string option
    }
    with
        static member New (reasonCode: string option, ?chargebackLossReasonCode: string) =
            {
                DisputePaymentMethodDetailsKlarna.ReasonCode = reasonCode //required
                DisputePaymentMethodDetailsKlarna.ChargebackLossReasonCode = chargebackLossReasonCode
            }

    [<Struct>]
    type DisputePaymentMethodDetailsCardCaseType =
        | Block
        | Chargeback
        | Compliance
        | Inquiry
        | Resolution

    type DisputePaymentMethodDetailsCardBrand =
        | Amex
        | CartesBancaires
        | Diners
        | Discover
        | EftposAu
        | Jcb
        | Link
        | Mastercard
        | Unionpay
        | Visa
        | Unknown

    [<Struct>]
    type DisputePaymentMethodDetailsAmazonPayDisputeType =
        | Chargeback
        | Claim

    [<Struct>]
    type DisputePaymentMethodDetailsType =
        | AmazonPay
        | Card
        | Klarna
        | Paypal

    type DisputeEnhancedEvidenceVisaCompliance = {
        ///A field acknowledging the fee incurred when countering a Visa compliance dispute. If this field is set to true, evidence can be submitted for the compliance dispute. Stripe collects a 500 USD (or local equivalent) amount to cover the network costs associated with resolving compliance disputes. Stripe refunds the 500 USD network fee if you win the dispute.
        FeeAcknowledged: bool
    }
    with
        static member New (feeAcknowledged: bool) =
            {
                DisputeEnhancedEvidenceVisaCompliance.FeeAcknowledged = feeAcknowledged //required
            }

    [<Struct>]
    type DisputeEnhancedEligibilityVisaComplianceStatus =
        | FeeAcknowledged
        | RequiresFeeAcknowledgement

    [<Struct>]
    type DisputeEnhancedEligibilityVisaCompellingEvidence3RequiredActions =
        | MissingCustomerIdentifiers
        | MissingDisputedTransactionDescription
        | MissingMerchandiseOrServices
        | MissingPriorUndisputedTransactionDescription
        | MissingPriorUndisputedTransactions

    [<Struct>]
    type DisputeEnhancedEligibilityVisaCompellingEvidence3Status =
        | NotQualified
        | Qualified
        | RequiresAction

    [<Struct>]
    type DisputeEnhancedEligibilityTypes =
        | VisaCompellingEvidence3
        | VisaCompliance

    type DisputeStatus =
        | Lost
        | NeedsResponse
        | Prevented
        | UnderReview
        | WarningClosed
        | WarningNeedsResponse
        | WarningUnderReview
        | Won

    type DisputeReason =
        | BankCannotProcess
        | CheckReturned
        | CreditNotProcessed
        | CustomerInitiated
        | DebitNotAuthorized
        | Duplicate
        | Fraudulent
        | General
        | IncorrectAccountDetails
        | InsufficientFunds
        | Noncompliant
        | ProductNotReceived
        | ProductUnacceptable
        | SubscriptionCanceled
        | Unrecognized

    type DestinationDetailsUnimplemented = {
        DestinationDetailsUnimplemented: string option
    }
    with
        static member New (?destinationDetailsUnimplemented: string option) =
            {
                DestinationDetailsUnimplemented.DestinationDetailsUnimplemented = destinationDetailsUnimplemented |> Option.flatten
            }

    type DeletedWebhookEndpoint = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "webhook_endpoint"

        static member New (deleted: bool, id: string) =
            {
                DeletedWebhookEndpoint.Deleted = deleted //required
                DeletedWebhookEndpoint.Id = id //required
            }

    type DeletedTestHelpersTestClock = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "test_helpers.test_clock"

        static member New (deleted: bool, id: string) =
            {
                DeletedTestHelpersTestClock.Deleted = deleted //required
                DeletedTestHelpersTestClock.Id = id //required
            }

    type DeletedTerminalReaderDeviceType =
        | BbposChipper2x
        | BbposWisepad3
        | BbposWiseposE
        | MobilePhoneReader
        | SimulatedStripeS700
        | SimulatedStripeS710
        | SimulatedWiseposE
        | StripeM2
        | StripeS700
        | StripeS710
        | VerifoneP400

    type DeletedTerminalLocation = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "terminal.location"

        static member New (deleted: bool, id: string) =
            {
                DeletedTerminalLocation.Deleted = deleted //required
                DeletedTerminalLocation.Id = id //required
            }

    type DeletedTerminalConfiguration = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "terminal.configuration"

        static member New (deleted: bool, id: string) =
            {
                DeletedTerminalConfiguration.Deleted = deleted //required
                DeletedTerminalConfiguration.Id = id //required
            }

    type DeletedTaxId = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "tax_id"

        static member New (deleted: bool, id: string) =
            {
                DeletedTaxId.Deleted = deleted //required
                DeletedTaxId.Id = id //required
            }

    type DeletedSubscriptionItem = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "subscription_item"

        static member New (deleted: bool, id: string) =
            {
                DeletedSubscriptionItem.Deleted = deleted //required
                DeletedSubscriptionItem.Id = id //required
            }

    type DeletedRadarValueListItem = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "radar.value_list_item"

        static member New (deleted: bool, id: string) =
            {
                DeletedRadarValueListItem.Deleted = deleted //required
                DeletedRadarValueListItem.Id = id //required
            }

    type DeletedRadarValueList = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "radar.value_list"

        static member New (deleted: bool, id: string) =
            {
                DeletedRadarValueList.Deleted = deleted //required
                DeletedRadarValueList.Id = id //required
            }

    type DeletedProductFeature = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "product_feature"

        static member New (deleted: bool, id: string) =
            {
                DeletedProductFeature.Deleted = deleted //required
                DeletedProductFeature.Id = id //required
            }

    type DeletedProduct = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "product"

        static member New (deleted: bool, id: string) =
            {
                DeletedProduct.Deleted = deleted //required
                DeletedProduct.Id = id //required
            }

    type DeletedPrice = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "price"

        static member New (deleted: bool, id: string) =
            {
                DeletedPrice.Deleted = deleted //required
                DeletedPrice.Id = id //required
            }

    type DeletedPlan = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "plan"

        static member New (deleted: bool, id: string) =
            {
                DeletedPlan.Deleted = deleted //required
                DeletedPlan.Id = id //required
            }

    type DeletedPerson = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "person"

        static member New (deleted: bool, id: string) =
            {
                DeletedPerson.Deleted = deleted //required
                DeletedPerson.Id = id //required
            }

    type DeletedInvoiceitem = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "invoiceitem"

        static member New (deleted: bool, id: string) =
            {
                DeletedInvoiceitem.Deleted = deleted //required
                DeletedInvoiceitem.Id = id //required
            }

    type DeletedInvoice = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "invoice"

        static member New (deleted: bool, id: string) =
            {
                DeletedInvoice.Deleted = deleted //required
                DeletedInvoice.Id = id //required
            }

    type DeletedCustomer = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "customer"

        static member New (deleted: bool, id: string) =
            {
                DeletedCustomer.Deleted = deleted //required
                DeletedCustomer.Id = id //required
            }

    type DeletedCoupon = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "coupon"

        static member New (deleted: bool, id: string) =
            {
                DeletedCoupon.Deleted = deleted //required
                DeletedCoupon.Id = id //required
            }

    type DeletedApplication = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
        ///The name of the application.
        Name: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "application"

        static member New (deleted: bool, id: string, name: string option) =
            {
                DeletedApplication.Deleted = deleted //required
                DeletedApplication.Id = id //required
                DeletedApplication.Name = name //required
            }

    type DeletedApplePayDomain = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "apple_pay_domain"

        static member New (deleted: bool, id: string) =
            {
                DeletedApplePayDomain.Deleted = deleted //required
                DeletedApplePayDomain.Id = id //required
            }

    type DeletedAccount = {
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "account"

        static member New (deleted: bool, id: string) =
            {
                DeletedAccount.Deleted = deleted //required
                DeletedAccount.Id = id //required
            }

    [<Struct>]
    type CustomerTaxLocationSource =
        | BillingAddress
        | IpAddress
        | PaymentMethod
        | ShippingDestination

    [<Struct>]
    type CustomerTaxProvider =
        | Anrok
        | Avalara
        | Sphere
        | Stripe

    [<Struct>]
    type CustomerTaxAutomaticTax =
        | Failed
        | NotCollecting
        | Supported
        | UnrecognizedLocation

    ///This hash contains whether the pricing table is enabled.
    type CustomerSessionResourceComponentsResourcePricingTable = {
        ///Whether the pricing table is enabled.
        Enabled: bool
    }
    with
        static member New (enabled: bool) =
            {
                CustomerSessionResourceComponentsResourcePricingTable.Enabled = enabled //required
            }

    [<Struct>]
    type CustomerSessionResourceComponentsResourcePaymentElementResourceFeaturesPaymentMethodAllowRedisplayFilters =
        | Always
        | Limited
        | Unspecified

    [<Struct>]
    type CustomerSessionResourceComponentsResourcePaymentElementResourceFeaturesPaymentMethodSaveUsage =
        | OffSession
        | OnSession

    [<Struct>]
    type CustomerSessionResourceComponentsResourcePaymentElementResourceFeaturesPaymentMethodSave =
        | Disabled
        | Enabled

    [<Struct>]
    type CustomerSessionResourceComponentsResourcePaymentElementResourceFeaturesPaymentMethodRemove =
        | Disabled
        | Enabled

    [<Struct>]
    type CustomerSessionResourceComponentsResourcePaymentElementResourceFeaturesPaymentMethodRedisplay =
        | Disabled
        | Enabled

    [<Struct>]
    type CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeaturesPaymentMethodAllowRedisplayFilters =
        | Always
        | Limited
        | Unspecified

    [<Struct>]
    type CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeaturesPaymentMethodSaveAllowRedisplayOverride =
        | Always
        | Limited
        | Unspecified

    [<Struct>]
    type CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeaturesPaymentMethodSave =
        | Disabled
        | Enabled

    [<Struct>]
    type CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeaturesPaymentMethodRemove =
        | Disabled
        | Enabled

    [<Struct>]
    type CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeaturesPaymentMethodRedisplay =
        | Disabled
        | Enabled

    [<Struct>]
    type CustomerSessionResourceComponentsResourceCustomerSheetResourceFeaturesPaymentMethodAllowRedisplayFilters =
        | Always
        | Limited
        | Unspecified

    [<Struct>]
    type CustomerSessionResourceComponentsResourceCustomerSheetResourceFeaturesPaymentMethodRemove =
        | Disabled
        | Enabled

    ///This hash contains whether the buy button is enabled.
    type CustomerSessionResourceComponentsResourceBuyButton = {
        ///Whether the buy button is enabled.
        Enabled: bool
    }
    with
        static member New (enabled: bool) =
            {
                CustomerSessionResourceComponentsResourceBuyButton.Enabled = enabled //required
            }

    type CustomerCashBalanceTransactionType =
        | AdjustedForOverdraft
        | AppliedToPayment
        | Funded
        | FundingReversed
        | RefundedFromPayment
        | ReturnCanceled
        | ReturnInitiated
        | TransferredToBalance
        | UnappliedFromPayment

    type CustomerBalanceTransactionType =
        | Adjustment
        | AppliedToInvoice
        | CheckoutSessionSubscriptionPayment
        | CheckoutSessionSubscriptionPaymentCanceled
        | CreditNote
        | Initial
        | InvoiceOverpaid
        | InvoiceTooLarge
        | InvoiceTooSmall
        | Migration
        | UnappliedFromInvoice
        | UnspentReceiverCredit

    [<Struct>]
    type CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceUsBankTransferNetwork =
        | Ach
        | DomesticWireUs
        | Swift

    type CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceJpBankTransfer = {
        ///The name of the bank of the sender of the funding.
        SenderBank: string option
        ///The name of the bank branch of the sender of the funding.
        SenderBranch: string option
        ///The full name of the sender, as supplied by the sending bank.
        SenderName: string option
    }
    with
        static member New (senderBank: string option, senderBranch: string option, senderName: string option) =
            {
                CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceJpBankTransfer.SenderBank = senderBank //required
                CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceJpBankTransfer.SenderBranch = senderBranch //required
                CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceJpBankTransfer.SenderName = senderName //required
            }

    type CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceGbBankTransfer = {
        ///The last 4 digits of the account number of the sender of the funding.
        [<JsonField(Name="account_number_last4")>]AccountNumberLast4: string option
        ///The full name of the sender, as supplied by the sending bank.
        SenderName: string option
        ///The sort code of the bank of the sender of the funding
        SortCode: string option
    }
    with
        static member New (accountNumberLast4: string option, senderName: string option, sortCode: string option) =
            {
                CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceGbBankTransfer.AccountNumberLast4 = accountNumberLast4 //required
                CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceGbBankTransfer.SenderName = senderName //required
                CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceGbBankTransfer.SortCode = sortCode //required
            }

    type CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceEuBankTransfer = {
        ///The BIC of the bank of the sender of the funding.
        Bic: string option
        ///The last 4 digits of the IBAN of the sender of the funding.
        [<JsonField(Name="iban_last4")>]IbanLast4: string option
        ///The full name of the sender, as supplied by the sending bank.
        SenderName: string option
    }
    with
        static member New (bic: string option, ibanLast4: string option, senderName: string option) =
            {
                CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceEuBankTransfer.Bic = bic //required
                CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceEuBankTransfer.IbanLast4 = ibanLast4 //required
                CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceEuBankTransfer.SenderName = senderName //required
            }

    [<Struct>]
    type CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferType =
        | EuBankTransfer
        | GbBankTransfer
        | JpBankTransfer
        | MxBankTransfer
        | UsBankTransfer

    [<Struct>]
    type CustomerBalanceCustomerBalanceSettingsReconciliationMode =
        | Automatic
        | Manual

    [<Struct>]
    type CustomerAcceptanceType =
        | Offline
        | Online

    [<Struct>]
    type CustomerTaxExempt =
        | Exempt
        | [<JsonUnionCase("none")>] None'
        | Reverse

    type CustomUnitAmount = {
        ///The maximum unit amount the customer can specify for this item.
        Maximum: int option
        ///The minimum unit amount the customer can specify for this item. Must be at least the minimum charge amount.
        Minimum: int option
        ///The starting unit amount which can be updated by the customer.
        Preset: int option
    }
    with
        static member New (maximum: int option, minimum: int option, preset: int option) =
            {
                CustomUnitAmount.Maximum = maximum //required
                CustomUnitAmount.Minimum = minimum //required
                CustomUnitAmount.Preset = preset //required
            }

    type CustomLogo = {
        ///Content type of the Dashboard-only CustomPaymentMethodType logo.
        ContentType: string option
        ///URL of the Dashboard-only CustomPaymentMethodType logo.
        Url: string
    }
    with
        static member New (contentType: string option, url: string) =
            {
                CustomLogo.ContentType = contentType //required
                CustomLogo.Url = url //required
            }

    [<Struct>]
    type CurrencyOptionTaxBehavior =
        | Exclusive
        | Inclusive
        | Unspecified

    [<Struct>]
    type CreditNotesPretaxCreditAmountType =
        | CreditBalanceTransaction
        | Discount

    type CreditNotesPaymentRecordRefund = {
        ///ID of the payment record.
        PaymentRecord: string
        ///ID of the refund group.
        RefundGroup: string
    }
    with
        static member New (paymentRecord: string, refundGroup: string) =
            {
                CreditNotesPaymentRecordRefund.PaymentRecord = paymentRecord //required
                CreditNotesPaymentRecordRefund.RefundGroup = refundGroup //required
            }

    [<Struct>]
    type CreditNoteRefundType =
        | PaymentRecordRefund
        | Refund

    [<Struct>]
    type CreditNoteLineItemType =
        | CustomLineItem
        | InvoiceLineItem

    [<Struct>]
    type CreditNoteType =
        | Mixed
        | PostPayment
        | PrePayment

    [<Struct>]
    type CreditNoteStatus =
        | Issued
        | Void

    [<Struct>]
    type CreditNoteReason =
        | Duplicate
        | Fraudulent
        | OrderChange
        | ProductUnsatisfactory

    type CouponCurrencyOption = {
        ///Amount (in the `currency` specified) that will be taken off the subtotal of any invoices for this customer.
        AmountOff: int
    }
    with
        static member New (amountOff: int) =
            {
                CouponCurrencyOption.AmountOff = amountOff //required
            }

    [<Struct>]
    type CouponDuration =
        | Forever
        | Once
        | Repeating

    type ConnectEmbeddedPayoutsFeatures = {
        ///Whether Stripe user authentication is disabled. This value can only be `true` for accounts where `controller.requirement_collection` is `application` for the account. The default value is the opposite of the `external_account_collection` value. For example, if you don't set `external_account_collection`, it defaults to `true` and `disable_stripe_user_authentication` defaults to `false`.
        DisableStripeUserAuthentication: bool
        ///Whether to allow payout schedule to be changed. Defaults to `true` when `controller.losses.payments` is set to `stripe` for the account, otherwise `false`.
        EditPayoutSchedule: bool
        ///Whether external account collection is enabled. This feature can only be `false` for accounts where you’re responsible for collecting updated information when requirements are due or change, like Custom accounts. The default value for this feature is `true`.
        ExternalAccountCollection: bool
        ///Whether to allow creation of instant payouts. The default value is `enabled` when Stripe is responsible for negative account balances, and `use_dashboard_rules` otherwise.
        InstantPayouts: bool
        ///Whether to allow creation of standard payouts. Defaults to `true` when `controller.losses.payments` is set to `stripe` for the account, otherwise `false`.
        StandardPayouts: bool
    }
    with
        static member New (disableStripeUserAuthentication: bool, editPayoutSchedule: bool, externalAccountCollection: bool, instantPayouts: bool, standardPayouts: bool) =
            {
                ConnectEmbeddedPayoutsFeatures.DisableStripeUserAuthentication = disableStripeUserAuthentication //required
                ConnectEmbeddedPayoutsFeatures.EditPayoutSchedule = editPayoutSchedule //required
                ConnectEmbeddedPayoutsFeatures.ExternalAccountCollection = externalAccountCollection //required
                ConnectEmbeddedPayoutsFeatures.InstantPayouts = instantPayouts //required
                ConnectEmbeddedPayoutsFeatures.StandardPayouts = standardPayouts //required
            }

    type ConnectEmbeddedPaymentsFeatures = {
        ///Whether to allow capturing and cancelling payment intents. This is `true` by default.
        CapturePayments: bool
        ///Whether connected accounts can manage destination charges that are created on behalf of them. This is `false` by default.
        DestinationOnBehalfOfChargeManagement: bool
        ///Whether responding to disputes is enabled, including submitting evidence and accepting disputes. This is `true` by default.
        DisputeManagement: bool
        ///Whether sending refunds is enabled. This is `true` by default.
        RefundManagement: bool
    }
    with
        static member New (capturePayments: bool, destinationOnBehalfOfChargeManagement: bool, disputeManagement: bool, refundManagement: bool) =
            {
                ConnectEmbeddedPaymentsFeatures.CapturePayments = capturePayments //required
                ConnectEmbeddedPaymentsFeatures.DestinationOnBehalfOfChargeManagement = destinationOnBehalfOfChargeManagement //required
                ConnectEmbeddedPaymentsFeatures.DisputeManagement = disputeManagement //required
                ConnectEmbeddedPaymentsFeatures.RefundManagement = refundManagement //required
            }

    type ConnectEmbeddedPaymentDisputesFeatures = {
        ///Whether connected accounts can manage destination charges that are created on behalf of them. This is `false` by default.
        DestinationOnBehalfOfChargeManagement: bool
        ///Whether responding to disputes is enabled, including submitting evidence and accepting disputes. This is `true` by default.
        DisputeManagement: bool
        ///Whether sending refunds is enabled. This is `true` by default.
        RefundManagement: bool
    }
    with
        static member New (destinationOnBehalfOfChargeManagement: bool, disputeManagement: bool, refundManagement: bool) =
            {
                ConnectEmbeddedPaymentDisputesFeatures.DestinationOnBehalfOfChargeManagement = destinationOnBehalfOfChargeManagement //required
                ConnectEmbeddedPaymentDisputesFeatures.DisputeManagement = disputeManagement //required
                ConnectEmbeddedPaymentDisputesFeatures.RefundManagement = refundManagement //required
            }

    type ConnectEmbeddedIssuingCardsListFeatures = {
        ///Whether to allow card management features.
        CardManagement: bool
        ///Whether to allow card spend dispute management features.
        CardSpendDisputeManagement: bool
        ///Whether to allow cardholder management features.
        CardholderManagement: bool
        ///Whether Stripe user authentication is disabled. This value can only be `true` for accounts where `controller.requirement_collection` is `application` for the account. The default value is the opposite of the `external_account_collection` value. For example, if you don't set `external_account_collection`, it defaults to `true` and `disable_stripe_user_authentication` defaults to `false`.
        DisableStripeUserAuthentication: bool
        ///Whether to allow spend control management features.
        SpendControlManagement: bool
    }
    with
        static member New (cardManagement: bool, cardSpendDisputeManagement: bool, cardholderManagement: bool, disableStripeUserAuthentication: bool, spendControlManagement: bool) =
            {
                ConnectEmbeddedIssuingCardsListFeatures.CardManagement = cardManagement //required
                ConnectEmbeddedIssuingCardsListFeatures.CardSpendDisputeManagement = cardSpendDisputeManagement //required
                ConnectEmbeddedIssuingCardsListFeatures.CardholderManagement = cardholderManagement //required
                ConnectEmbeddedIssuingCardsListFeatures.DisableStripeUserAuthentication = disableStripeUserAuthentication //required
                ConnectEmbeddedIssuingCardsListFeatures.SpendControlManagement = spendControlManagement //required
            }

    type ConnectEmbeddedIssuingCardFeatures = {
        ///Whether to allow card management features.
        CardManagement: bool
        ///Whether to allow card spend dispute management features.
        CardSpendDisputeManagement: bool
        ///Whether to allow cardholder management features.
        CardholderManagement: bool
        ///Whether to allow spend control management features.
        SpendControlManagement: bool
    }
    with
        static member New (cardManagement: bool, cardSpendDisputeManagement: bool, cardholderManagement: bool, spendControlManagement: bool) =
            {
                ConnectEmbeddedIssuingCardFeatures.CardManagement = cardManagement //required
                ConnectEmbeddedIssuingCardFeatures.CardSpendDisputeManagement = cardSpendDisputeManagement //required
                ConnectEmbeddedIssuingCardFeatures.CardholderManagement = cardholderManagement //required
                ConnectEmbeddedIssuingCardFeatures.SpendControlManagement = spendControlManagement //required
            }

    type ConnectEmbeddedInstantPayoutsPromotionFeatures = {
        ///Whether Stripe user authentication is disabled. This value can only be `true` for accounts where `controller.requirement_collection` is `application` for the account. The default value is the opposite of the `external_account_collection` value. For example, if you don't set `external_account_collection`, it defaults to `true` and `disable_stripe_user_authentication` defaults to `false`.
        DisableStripeUserAuthentication: bool
        ///Whether external account collection is enabled. This feature can only be `false` for accounts where you’re responsible for collecting updated information when requirements are due or change, like Custom accounts. The default value for this feature is `true`.
        ExternalAccountCollection: bool
        ///Whether to allow creation of instant payouts. The default value is `enabled` when Stripe is responsible for negative account balances, and `use_dashboard_rules` otherwise.
        InstantPayouts: bool
    }
    with
        static member New (disableStripeUserAuthentication: bool, externalAccountCollection: bool, instantPayouts: bool) =
            {
                ConnectEmbeddedInstantPayoutsPromotionFeatures.DisableStripeUserAuthentication = disableStripeUserAuthentication //required
                ConnectEmbeddedInstantPayoutsPromotionFeatures.ExternalAccountCollection = externalAccountCollection //required
                ConnectEmbeddedInstantPayoutsPromotionFeatures.InstantPayouts = instantPayouts //required
            }

    type ConnectEmbeddedFinancialAccountTransactionsFeatures = {
        ///Whether to allow card spend dispute management features.
        CardSpendDisputeManagement: bool
    }
    with
        static member New (cardSpendDisputeManagement: bool) =
            {
                ConnectEmbeddedFinancialAccountTransactionsFeatures.CardSpendDisputeManagement = cardSpendDisputeManagement //required
            }

    type ConnectEmbeddedFinancialAccountFeatures = {
        ///Whether Stripe user authentication is disabled. This value can only be `true` for accounts where `controller.requirement_collection` is `application` for the account. The default value is the opposite of the `external_account_collection` value. For example, if you don't set `external_account_collection`, it defaults to `true` and `disable_stripe_user_authentication` defaults to `false`.
        DisableStripeUserAuthentication: bool
        ///Whether external account collection is enabled. This feature can only be `false` for accounts where you’re responsible for collecting updated information when requirements are due or change, like Custom accounts. The default value for this feature is `true`.
        ExternalAccountCollection: bool
        ///Whether to allow sending money.
        SendMoney: bool
        ///Whether to allow transferring balance.
        TransferBalance: bool
    }
    with
        static member New (disableStripeUserAuthentication: bool, externalAccountCollection: bool, sendMoney: bool, transferBalance: bool) =
            {
                ConnectEmbeddedFinancialAccountFeatures.DisableStripeUserAuthentication = disableStripeUserAuthentication //required
                ConnectEmbeddedFinancialAccountFeatures.ExternalAccountCollection = externalAccountCollection //required
                ConnectEmbeddedFinancialAccountFeatures.SendMoney = sendMoney //required
                ConnectEmbeddedFinancialAccountFeatures.TransferBalance = transferBalance //required
            }

    type ConnectEmbeddedDisputesListFeatures = {
        ///Whether to allow capturing and cancelling payment intents. This is `true` by default.
        CapturePayments: bool
        ///Whether connected accounts can manage destination charges that are created on behalf of them. This is `false` by default.
        DestinationOnBehalfOfChargeManagement: bool
        ///Whether responding to disputes is enabled, including submitting evidence and accepting disputes. This is `true` by default.
        DisputeManagement: bool
        ///Whether sending refunds is enabled. This is `true` by default.
        RefundManagement: bool
    }
    with
        static member New (capturePayments: bool, destinationOnBehalfOfChargeManagement: bool, disputeManagement: bool, refundManagement: bool) =
            {
                ConnectEmbeddedDisputesListFeatures.CapturePayments = capturePayments //required
                ConnectEmbeddedDisputesListFeatures.DestinationOnBehalfOfChargeManagement = destinationOnBehalfOfChargeManagement //required
                ConnectEmbeddedDisputesListFeatures.DisputeManagement = disputeManagement //required
                ConnectEmbeddedDisputesListFeatures.RefundManagement = refundManagement //required
            }

    type ConnectEmbeddedBaseFeatures = {
        ConnectEmbeddedBaseFeatures: string option
    }
    with
        static member New (?connectEmbeddedBaseFeatures: string option) =
            {
                ConnectEmbeddedBaseFeatures.ConnectEmbeddedBaseFeatures = connectEmbeddedBaseFeatures |> Option.flatten
            }

    type ConnectEmbeddedAccountFeaturesClaim = {
        ///Whether Stripe user authentication is disabled. This value can only be `true` for accounts where `controller.requirement_collection` is `application` for the account. The default value is the opposite of the `external_account_collection` value. For example, if you don't set `external_account_collection`, it defaults to `true` and `disable_stripe_user_authentication` defaults to `false`.
        DisableStripeUserAuthentication: bool
        ///Whether external account collection is enabled. This feature can only be `false` for accounts where you’re responsible for collecting updated information when requirements are due or change, like Custom accounts. The default value for this feature is `true`.
        ExternalAccountCollection: bool
    }
    with
        static member New (disableStripeUserAuthentication: bool, externalAccountCollection: bool) =
            {
                ConnectEmbeddedAccountFeaturesClaim.DisableStripeUserAuthentication = disableStripeUserAuthentication //required
                ConnectEmbeddedAccountFeaturesClaim.ExternalAccountCollection = externalAccountCollection //required
            }

    [<Struct>]
    type ConnectAccountReferenceType =
        | Account
        | Self

    type ConfirmationTokensResourcePaymentMethodPreviewType =
        | AcssDebit
        | Affirm
        | AfterpayClearpay
        | Alipay
        | Alma
        | AmazonPay
        | AuBecsDebit
        | BacsDebit
        | Bancontact
        | Billie
        | Blik
        | Boleto
        | Card
        | CardPresent
        | Cashapp
        | Crypto
        | Custom
        | CustomerBalance
        | Eps
        | Fpx
        | Giropay
        | Grabpay
        | Ideal
        | InteracPresent
        | KakaoPay
        | Klarna
        | Konbini
        | KrCard
        | Link
        | MbWay
        | Mobilepay
        | Multibanco
        | NaverPay
        | NzBankAccount
        | Oxxo
        | P24
        | PayByBank
        | Payco
        | Paynow
        | Paypal
        | Payto
        | Pix
        | Promptpay
        | RevolutPay
        | SamsungPay
        | Satispay
        | SepaDebit
        | Sofort
        | Sunbit
        | Swish
        | Twint
        | Upi
        | UsBankAccount
        | WechatPay
        | Zip

    [<Struct>]
    type ConfirmationTokensResourcePaymentMethodPreviewAllowRedisplay =
        | Always
        | Limited
        | Unspecified

    ///This hash contains details about the online acceptance.
    type ConfirmationTokensResourceMandateDataResourceCustomerAcceptanceResourceOnline = {
        ///The IP address from which the Mandate was accepted by the customer.
        IpAddress: string option
        ///The user agent of the browser from which the Mandate was accepted by the customer.
        UserAgent: string option
    }
    with
        static member New (ipAddress: string option, userAgent: string option) =
            {
                ConfirmationTokensResourceMandateDataResourceCustomerAcceptanceResourceOnline.IpAddress = ipAddress //required
                ConfirmationTokensResourceMandateDataResourceCustomerAcceptanceResourceOnline.UserAgent = userAgent //required
            }

    [<Struct>]
    type ConfirmationTokenSetupFutureUsage =
        | OffSession
        | OnSession

    type ClimateRemovalsProductsPrice = {
        ///Fees for one metric ton of carbon removal in the currency's smallest unit.
        AmountFees: int
        ///Subtotal for one metric ton of carbon removal (excluding fees) in the currency's smallest unit.
        AmountSubtotal: int
        ///Total for one metric ton of carbon removal (including fees) in the currency's smallest unit.
        AmountTotal: int
    }
    with
        static member New (amountFees: int, amountSubtotal: int, amountTotal: int) =
            {
                ClimateRemovalsProductsPrice.AmountFees = amountFees //required
                ClimateRemovalsProductsPrice.AmountSubtotal = amountSubtotal //required
                ClimateRemovalsProductsPrice.AmountTotal = amountTotal //required
            }

    type ClimateRemovalsBeneficiary = {
        ///Publicly displayable name for the end beneficiary of carbon removal.
        PublicName: string
    }
    with
        static member New (publicName: string) =
            {
                ClimateRemovalsBeneficiary.PublicName = publicName //required
            }

    [<Struct>]
    type ClimateSupplierRemovalPathway =
        | BiomassCarbonRemovalAndStorage
        | DirectAirCapture
        | EnhancedWeathering
        | MarineCarbonRemoval

    [<Struct>]
    type ClimateOrderStatus =
        | AwaitingFunds
        | Canceled
        | Confirmed
        | Delivered
        | Open

    [<Struct>]
    type ClimateOrderCancellationReason =
        | Expired
        | ProductUnavailable
        | Requested

    [<Struct>]
    type CheckoutUsBankAccountPaymentMethodOptionsVerificationMethod =
        | Automatic
        | Instant

    [<Struct>]
    type CheckoutUsBankAccountPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    [<Struct>]
    type CheckoutUpiPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    type CheckoutTwintPaymentMethodOptions () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    type CheckoutSwishPaymentMethodOptions = {
        ///The order reference that will be displayed to customers in the Swish application. Defaults to the `id` of the Payment Intent.
        Reference: string option
    }
    with
        static member New (reference: string option) =
            {
                CheckoutSwishPaymentMethodOptions.Reference = reference //required
            }

    type CheckoutSofortPaymentMethodOptions () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    [<Struct>]
    type CheckoutSepaDebitPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    type CheckoutSatispayPaymentMethodOptions () = 
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"


    type CheckoutSamsungPayPaymentMethodOptions () = 
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"


    [<Struct>]
    type CheckoutRevolutPayPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    [<Struct>]
    type CheckoutPixPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    [<Struct>]
    type CheckoutPixPaymentMethodOptionsAmountIncludesIof =
        | Always
        | Never

    [<Struct>]
    type CheckoutPaytoPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    [<Struct>]
    type CheckoutPaypalPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    type CheckoutPaynowPaymentMethodOptions () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    type CheckoutPaymentMethodOptionsMandateOptionsSepaDebit = {
        ///Prefix used to generate the Mandate reference. Must be at most 12 characters long. Must consist of only uppercase letters, numbers, spaces, or the following special characters: '/', '_', '-', '&', '.'. Cannot begin with 'STRIPE'.
        ReferencePrefix: string option
    }
    with
        static member New (?referencePrefix: string) =
            {
                CheckoutPaymentMethodOptionsMandateOptionsSepaDebit.ReferencePrefix = referencePrefix
            }

    type CheckoutPaymentMethodOptionsMandateOptionsBacsDebit = {
        ///Prefix used to generate the Mandate reference. Must be at most 12 characters long. Must consist of only uppercase letters, numbers, spaces, or the following special characters: '/', '_', '-', '&', '.'. Cannot begin with 'DDIC' or 'STRIPE'.
        ReferencePrefix: string option
    }
    with
        static member New (?referencePrefix: string) =
            {
                CheckoutPaymentMethodOptionsMandateOptionsBacsDebit.ReferencePrefix = referencePrefix
            }

    type CheckoutPaycoPaymentMethodOptions () = 
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"


    type CheckoutP24PaymentMethodOptions () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    type CheckoutOxxoPaymentMethodOptions = {
        ///The number of calendar days before an OXXO invoice expires. For example, if you create an OXXO invoice on Monday and you set expires_after_days to 2, the OXXO invoice will expire on Wednesday at 23:59 America/Mexico_City time.
        ExpiresAfterDays: int
    }
    with
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"

        static member New (expiresAfterDays: int) =
            {
                CheckoutOxxoPaymentMethodOptions.ExpiresAfterDays = expiresAfterDays //required
            }

    [<Struct>]
    type CheckoutNaverPayPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    type CheckoutMultibancoPaymentMethodOptions () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    type CheckoutMobilepayPaymentMethodOptions () = 
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    [<Struct>]
    type CheckoutLinkWalletOptionsDisplay =
        | Auto
        | Never

    [<Struct>]
    type CheckoutLinkPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    [<Struct>]
    type CheckoutKrCardPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    type CheckoutKonbiniPaymentMethodOptions = {
        ///The number of calendar days (between 1 and 60) after which Konbini payment instructions will expire. For example, if a PaymentIntent is confirmed with Konbini and `expires_after_days` set to 2 on Monday JST, the instructions will expire on Wednesday 23:59:59 JST.
        ExpiresAfterDays: int option
    }
    with
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"

        static member New (expiresAfterDays: int option) =
            {
                CheckoutKonbiniPaymentMethodOptions.ExpiresAfterDays = expiresAfterDays //required
            }

    [<Struct>]
    type CheckoutKlarnaPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    [<Struct>]
    type CheckoutKakaoPayPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    type CheckoutIdealPaymentMethodOptions () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    type CheckoutGrabPayPaymentMethodOptions () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    type CheckoutGiropayPaymentMethodOptions () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    type CheckoutFpxPaymentMethodOptions () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    type CheckoutEpsPaymentMethodOptions () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    type CheckoutCustomerBalanceBankTransferPaymentMethodOptionsRequestedAddressTypes =
        | Aba
        | Iban
        | Sepa
        | SortCode
        | Spei
        | Swift
        | Zengin

    [<Struct>]
    type CheckoutCustomerBalanceBankTransferPaymentMethodOptionsType =
        | EuBankTransfer
        | GbBankTransfer
        | JpBankTransfer
        | MxBankTransfer
        | UsBankTransfer

    type CheckoutCashappPaymentMethodOptions () = 
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    [<Struct>]
    type CheckoutCardPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    [<Struct>]
    type CheckoutCardPaymentMethodOptionsRequestThreeDSecure =
        | Any
        | Automatic
        | Challenge

    [<Struct>]
    type CheckoutCardPaymentMethodOptionsRequestOvercapture =
        | IfAvailable
        | Never

    [<Struct>]
    type CheckoutCardPaymentMethodOptionsRequestMulticapture =
        | IfAvailable
        | Never

    [<Struct>]
    type CheckoutCardPaymentMethodOptionsRequestIncrementalAuthorization =
        | IfAvailable
        | Never

    [<Struct>]
    type CheckoutCardPaymentMethodOptionsRequestExtendedAuthorization =
        | IfAvailable
        | Never

    type CheckoutCardInstallmentsOptions = {
        ///Indicates if installments are enabled
        Enabled: bool option
    }
    with
        static member New (?enabled: bool) =
            {
                CheckoutCardInstallmentsOptions.Enabled = enabled
            }

    [<Struct>]
    type CheckoutBoletoPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    type CheckoutBilliePaymentMethodOptions () = 
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"


    type CheckoutBancontactPaymentMethodOptions () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    [<Struct>]
    type CheckoutBacsDebitPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    type CheckoutAuBecsDebitPaymentMethodOptions = {
        ///Controls when Stripe will attempt to debit the funds from the customer's account. The date must be a string in YYYY-MM-DD format. The date must be in the future and between 3 and 15 calendar days from now.
        TargetDate: string option
    }
    with
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"

        static member New (?targetDate: string) =
            {
                CheckoutAuBecsDebitPaymentMethodOptions.TargetDate = targetDate
            }

    [<Struct>]
    type CheckoutAmazonPayPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession

    type CheckoutAlmaPaymentMethodOptions () = 
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"


    type CheckoutAlipayPaymentMethodOptions () = 
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    type CheckoutAfterpayClearpayPaymentMethodOptions () = 
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    type CheckoutAffirmPaymentMethodOptions () = 
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"


    [<Struct>]
    type CheckoutAcssDebitPaymentMethodOptionsVerificationMethod =
        | Automatic
        | Instant
        | Microdeposits

    [<Struct>]
    type CheckoutAcssDebitPaymentMethodOptionsSetupFutureUsage =
        | [<JsonUnionCase("none")>] None'
        | OffSession
        | OnSession

    [<Struct>]
    type CheckoutAcssDebitPaymentMethodOptionsCurrency =
        | Cad
        | Usd

    [<Struct>]
    type CheckoutAcssDebitMandateOptionsDefaultFor =
        | Invoice
        | Subscription

    [<Struct>]
    type CheckoutAcssDebitMandateOptionsTransactionType =
        | Business
        | Personal

    [<Struct>]
    type CheckoutAcssDebitMandateOptionsPaymentSchedule =
        | Combined
        | Interval
        | Sporadic

    [<Struct>]
    type CheckoutSessionUiMode =
        | Elements
        | EmbeddedPage
        | Form
        | HostedPage

    [<Struct>]
    type CheckoutSessionSubmitType =
        | Auto
        | Book
        | Donate
        | Pay
        | Subscribe

    [<Struct>]
    type CheckoutSessionStatus =
        | Complete
        | Expired
        | Open

    [<Struct>]
    type CheckoutSessionRedirectOnCompletion =
        | Always
        | IfRequired
        | Never

    [<Struct>]
    type CheckoutSessionPaymentStatus =
        | NoPaymentRequired
        | Paid
        | Unpaid

    [<Struct>]
    type CheckoutSessionPaymentMethodCollection =
        | Always
        | IfRequired

    [<Struct>]
    type CheckoutSessionOriginContext =
        | MobileApp
        | Web

    [<Struct>]
    type CheckoutSessionMode =
        | Payment
        | Setup
        | Subscription

    type CheckoutSessionLocale =
        | Auto
        | Bg
        | Cs
        | Da
        | De
        | El
        | En
        | [<JsonUnionCase("en-GB")>] EnGB
        | Es
        | [<JsonUnionCase("es-419")>] Es419
        | Et
        | Fi
        | Fil
        | Fr
        | [<JsonUnionCase("fr-CA")>] FrCA
        | Hr
        | Hu
        | Id
        | It
        | Ja
        | Ko
        | Lt
        | Lv
        | Ms
        | Mt
        | Nb
        | Nl
        | Pl
        | Pt
        | [<JsonUnionCase("pt-BR")>] PtBR
        | Ro
        | Ru
        | Sk
        | Sl
        | Sv
        | Th
        | Tr
        | Vi
        | Zh
        | [<JsonUnionCase("zh-HK")>] ZhHK
        | [<JsonUnionCase("zh-TW")>] ZhTW

    [<Struct>]
    type CheckoutSessionCustomerCreation =
        | Always
        | IfRequired

    [<Struct>]
    type CheckoutSessionBillingAddressCollection =
        | Auto
        | Required

    [<Struct>]
    type ChargeOutcomeType =
        | Authorized
        | ManualReview
        | IssuerDeclined
        | Blocked
        | Invalid

    [<Struct>]
    type ChargeOutcomeNetworkStatus =
        | ApprovedByNetwork
        | DeclinedByNetwork
        | NotSentToNetwork
        | ReversedAfterApproval

    [<Struct>]
    type ChargeOutcomeAdviceCode =
        | ConfirmCardData
        | DoNotTryAgain
        | TryAgainLater

    [<Struct>]
    type ChargeFraudDetailsUserReport =
        | Safe
        | Fraudulent

    [<Struct>]
    type ChargeStatus =
        | Failed
        | Pending
        | Succeeded

    type CardMandatePaymentMethodDetails = {
        CardMandatePaymentMethodDetails: string option
    }
    with
        static member New (?cardMandatePaymentMethodDetails: string option) =
            {
                CardMandatePaymentMethodDetails.CardMandatePaymentMethodDetails = cardMandatePaymentMethodDetails |> Option.flatten
            }

    type CardIssuingAccountTermsOfService = {
        ///The Unix timestamp marking when the account representative accepted the service agreement.
        Date: int option
        ///The IP address from which the account representative accepted the service agreement.
        Ip: string option
        ///The user agent of the browser from which the account representative accepted the service agreement.
        UserAgent: string option
    }
    with
        static member New (date: int option, ip: string option, ?userAgent: string) =
            {
                CardIssuingAccountTermsOfService.Date = date //required
                CardIssuingAccountTermsOfService.Ip = ip //required
                CardIssuingAccountTermsOfService.UserAgent = userAgent
            }

    [<Struct>]
    type CardAvailablePayoutMethods =
        | Instant
        | Standard

    [<Struct>]
    type CardTokenizationMethod =
        | AndroidPay
        | ApplePay
        | Masterpass
        | VisaCheckout

    [<Struct>]
    type CardStatus =
        | New
        | Errored

    [<Struct>]
    type CardRegulatedStatus =
        | Regulated
        | Unregulated

    [<Struct>]
    type CardFunding =
        | Credit
        | Debit
        | Prepaid
        | Unknown

    [<Struct>]
    type CardCvcCheck =
        | Pass
        | Fail
        | Unavailable
        | Unchecked

    type CardBrand =
        | [<JsonUnionCase("American Express")>] AmericanExpress
        | [<JsonUnionCase("Cartes Bancaires")>] CartesBancaires
        | [<JsonUnionCase("Diners Club")>] DinersClub
        | [<JsonUnionCase("Discover")>] Discover
        | [<JsonUnionCase("Eftpos Australia")>] EftposAustralia
        | [<JsonUnionCase("Girocard")>] Girocard
        | [<JsonUnionCase("JCB")>] JCB
        | [<JsonUnionCase("MasterCard")>] MasterCard
        | [<JsonUnionCase("UnionPay")>] UnionPay
        | [<JsonUnionCase("Visa")>] Visa
        | [<JsonUnionCase("Unknown")>] Unknown

    [<Struct>]
    type CardAllowRedisplay =
        | Always
        | Limited
        | Unspecified

    [<Struct>]
    type CardAddressZipCheck =
        | Pass
        | Fail
        | Unavailable
        | Unchecked

    [<Struct>]
    type CardAddressLine1Check =
        | Pass
        | Fail
        | Unavailable
        | Unchecked

    [<Struct>]
    type CapabilityStatus =
        | Active
        | Inactive
        | Pending
        | Unrequested

    [<Struct>]
    type CancellationDetailsReason =
        | CanceledByRetentionPolicy
        | CancellationRequested
        | PaymentDisputed
        | PaymentFailed

    type CancellationDetailsFeedback =
        | CustomerService
        | LowQuality
        | MissingFeatures
        | Other
        | SwitchedService
        | TooComplex
        | TooExpensive
        | Unused

    type BillingPortalSessionLocale =
        | Auto
        | Bg
        | Cs
        | Da
        | De
        | El
        | En
        | [<JsonUnionCase("en-AU")>] EnAU
        | [<JsonUnionCase("en-CA")>] EnCA
        | [<JsonUnionCase("en-GB")>] EnGB
        | [<JsonUnionCase("en-IE")>] EnIE
        | [<JsonUnionCase("en-IN")>] EnIN
        | [<JsonUnionCase("en-NZ")>] EnNZ
        | [<JsonUnionCase("en-SG")>] EnSG
        | Es
        | [<JsonUnionCase("es-419")>] Es419
        | Et
        | Fi
        | Fil
        | Fr
        | [<JsonUnionCase("fr-CA")>] FrCA
        | Hr
        | Hu
        | Id
        | It
        | Ja
        | Ko
        | Lt
        | Lv
        | Ms
        | Mt
        | Nb
        | Nl
        | Pl
        | Pt
        | [<JsonUnionCase("pt-BR")>] PtBR
        | Ro
        | Ru
        | Sk
        | Sl
        | Sv
        | Th
        | Tr
        | Vi
        | Zh
        | [<JsonUnionCase("zh-HK")>] ZhHK
        | [<JsonUnionCase("zh-TW")>] ZhTW

    type BillingMeterResourceCustomerMappingSettings = {
        ///The key in the meter event payload to use for mapping the event to a customer.
        EventPayloadKey: string
    }
    with
        ///The method for mapping a meter event to a customer.
        member _.Type = "by_id"

        static member New (eventPayloadKey: string) =
            {
                BillingMeterResourceCustomerMappingSettings.EventPayloadKey = eventPayloadKey //required
            }

    type BillingMeterResourceBillingMeterValue = {
        ///The key in the meter event payload to use as the value for this meter.
        EventPayloadKey: string
    }
    with
        static member New (eventPayloadKey: string) =
            {
                BillingMeterResourceBillingMeterValue.EventPayloadKey = eventPayloadKey //required
            }

    type BillingMeterResourceBillingMeterStatusTransitions = {
        ///The time the meter was deactivated, if any. Measured in seconds since Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]DeactivatedAt: DateTime option
    }
    with
        static member New (deactivatedAt: DateTime option) =
            {
                BillingMeterResourceBillingMeterStatusTransitions.DeactivatedAt = deactivatedAt //required
            }

    type BillingMeterResourceBillingMeterEventAdjustmentCancel = {
        ///Unique identifier for the event.
        Identifier: string option
    }
    with
        static member New (identifier: string option) =
            {
                BillingMeterResourceBillingMeterEventAdjustmentCancel.Identifier = identifier //required
            }

    [<Struct>]
    type BillingMeterResourceAggregationSettingsFormula =
        | Count
        | Last
        | Sum

    [<Struct>]
    type BillingCreditGrantsResourceBalanceDebitType =
        | CreditsApplied
        | CreditsExpired
        | CreditsVoided

    [<Struct>]
    type BillingCreditGrantsResourceBalanceCreditType =
        | CreditsApplicationInvoiceVoided
        | CreditsGranted

    type BillingCreditGrantsResourceApplicablePrice = {
        ///Unique identifier for the object.
        Id: string option
    }
    with
        static member New (id: string option) =
            {
                BillingCreditGrantsResourceApplicablePrice.Id = id //required
            }

    type BillingClocksResourceStatusDetailsAdvancingStatusDetails = {
        ///The `frozen_time` that the Test Clock is advancing towards.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]TargetFrozenTime: DateTime
    }
    with
        static member New (targetFrozenTime: DateTime) =
            {
                BillingClocksResourceStatusDetailsAdvancingStatusDetails.TargetFrozenTime = targetFrozenTime //required
            }

    type BillingBillResourceInvoicingTaxesTaxTaxabilityReason =
        | CustomerExempt
        | NotAvailable
        | NotCollecting
        | NotSubjectToTax
        | NotSupported
        | PortionProductExempt
        | PortionReducedRated
        | PortionStandardRated
        | ProductExempt
        | ProductExemptHoliday
        | ProportionallyRated
        | ReducedRated
        | ReverseCharge
        | StandardRated
        | TaxableBasisReduced
        | ZeroRated

    [<Struct>]
    type BillingBillResourceInvoicingTaxesTaxTaxBehavior =
        | Exclusive
        | Inclusive

    type BillingBillResourceInvoicingParentsInvoiceQuoteParent = {
        ///The quote that generated this invoice
        Quote: string
    }
    with
        static member New (quote: string) =
            {
                BillingBillResourceInvoicingParentsInvoiceQuoteParent.Quote = quote //required
            }

    [<Struct>]
    type BillingBillResourceInvoicingParentsInvoiceParentType =
        | QuoteDetails
        | SubscriptionDetails

    [<Struct>]
    type BillingBillResourceInvoicingLinesParentsInvoiceLineItemParentType =
        | InvoiceItemDetails
        | SubscriptionItemDetails

    type BillingBillResourceInvoiceItemParentsInvoiceItemSubscriptionParent = {
        ///The subscription that generated this invoice item
        Subscription: string
        ///The subscription item that generated this invoice item
        SubscriptionItem: string option
    }
    with
        static member New (subscription: string, ?subscriptionItem: string) =
            {
                BillingBillResourceInvoiceItemParentsInvoiceItemSubscriptionParent.Subscription = subscription //required
                BillingBillResourceInvoiceItemParentsInvoiceItemSubscriptionParent.SubscriptionItem = subscriptionItem
            }

    ///A billing meter event summary represents an aggregated view of a customer's billing meter events within a specified timeframe. It indicates how much
    ///usage was accrued by a customer for that period.
    ///Note: Meters events are aggregated asynchronously so the meter event summaries provide an eventually consistent view of the reported usage.
    type BillingMeterEventSummary = {
        ///Aggregated value of all the events within `start_time` (inclusive) and `end_time` (inclusive). The aggregation strategy is defined on meter via `default_aggregation`.
        AggregatedValue: decimal
        ///End timestamp for this event summary (exclusive). Must be aligned with minute boundaries.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]EndTime: DateTime
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The meter associated with this event summary.
        Meter: string
        ///Start timestamp for this event summary (inclusive). Must be aligned with minute boundaries.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]StartTime: DateTime
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "billing.meter_event_summary"

        static member New (aggregatedValue: decimal, endTime: DateTime, id: string, livemode: bool, meter: string, startTime: DateTime) =
            {
                BillingMeterEventSummary.AggregatedValue = aggregatedValue //required
                BillingMeterEventSummary.EndTime = endTime //required
                BillingMeterEventSummary.Id = id //required
                BillingMeterEventSummary.Livemode = livemode //required
                BillingMeterEventSummary.Meter = meter //required
                BillingMeterEventSummary.StartTime = startTime //required
            }

    [<Struct>]
    type BillingMeterEventAdjustmentStatus =
        | Complete
        | Pending

    [<Struct>]
    type BillingMeterStatus =
        | Active
        | Inactive

    [<Struct>]
    type BillingMeterEventTimeWindow =
        | Day
        | Hour

    [<Struct>]
    type BillingCreditGrantCategory =
        | Paid
        | Promotional

    [<Struct>]
    type BillingCreditBalanceTransactionType =
        | Credit
        | Debit

    [<Struct>]
    type BillingAlertStatus =
        | Active
        | Archived
        | Inactive

    type BankConnectionsResourceTransactionResourceStatusTransitions = {
        ///Time at which this transaction posted. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]PostedAt: DateTime option
        ///Time at which this transaction was voided. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]VoidAt: DateTime option
    }
    with
        static member New (postedAt: DateTime option, voidAt: DateTime option) =
            {
                BankConnectionsResourceTransactionResourceStatusTransitions.PostedAt = postedAt //required
                BankConnectionsResourceTransactionResourceStatusTransitions.VoidAt = voidAt //required
            }

    [<Struct>]
    type BankConnectionsResourceTransactionRefreshStatus =
        | Failed
        | Pending
        | Succeeded

    [<Struct>]
    type BankConnectionsResourceOwnershipRefreshStatus =
        | Failed
        | Pending
        | Succeeded

    [<Struct>]
    type BankConnectionsResourceLinkAccountSessionFiltersAccountSubcategories =
        | Checking
        | CreditCard
        | LineOfCredit
        | Mortgage
        | Savings

    [<Struct>]
    type BankConnectionsResourceBalanceRefreshStatus =
        | Failed
        | Pending
        | Succeeded

    [<Struct>]
    type BankConnectionsResourceBalanceType =
        | Cash
        | Credit

    [<Struct>]
    type BankConnectionsResourceAccountholderType =
        | Account
        | Customer

    [<Struct>]
    type BankConnectionsResourceAccountNumberDetailsStatus =
        | Deactivated
        | Transactable

    [<Struct>]
    type BankConnectionsResourceAccountNumberDetailsIdentifierType =
        | AccountNumber
        | TokenizedAccountNumber

    [<Struct>]
    type BankAccountAvailablePayoutMethods =
        | Instant
        | Standard

    type BankAccountStatus =
        | New
        | Validated
        | Verified
        | VerificationFailed
        | TokenizedAccountNumberDeactivated
        | Errored

    [<Struct>]
    type BankAccountAccountType =
        | Futsu
        | Toza

    [<Struct>]
    type BankAccountAccountHolderType =
        | Individual
        | Company

    type BalanceTransactionType =
        | Adjustment
        | Advance
        | AdvanceFunding
        | AnticipationRepayment
        | ApplicationFee
        | ApplicationFeeRefund
        | Charge
        | ClimateOrderPurchase
        | ClimateOrderRefund
        | ConnectCollectionTransfer
        | Contribution
        | FeeCreditFunding
        | InboundTransfer
        | InboundTransferReversal
        | IssuingAuthorizationHold
        | IssuingAuthorizationRelease
        | IssuingDispute
        | IssuingTransaction
        | ObligationOutbound
        | ObligationReversalInbound
        | Payment
        | PaymentFailureRefund
        | PaymentNetworkReserveHold
        | PaymentNetworkReserveRelease
        | PaymentRefund
        | PaymentReversal
        | PaymentUnreconciled
        | Payout
        | PayoutCancel
        | PayoutFailure
        | PayoutMinimumBalanceHold
        | PayoutMinimumBalanceRelease
        | Refund
        | RefundFailure
        | ReserveHold
        | ReserveRelease
        | ReserveTransaction
        | ReservedFunds
        | StripeBalancePaymentDebit
        | StripeBalancePaymentDebitReversal
        | StripeFee
        | StripeFxFee
        | TaxFee
        | Topup
        | TopupReversal
        | Transfer
        | TransferCancel
        | TransferFailure
        | TransferRefund

    [<Struct>]
    type BalanceTransactionStatus =
        | Available
        | Pending

    [<Struct>]
    type BalanceTransactionBalanceType =
        | Issuing
        | Payments
        | RefundAndDisputePrefunding
        | RiskReserved

    type BalanceSettingsResourceSettlementTiming = {
        ///The number of days charge funds are held before becoming available.
        DelayDays: int
        ///The number of days charge funds are held before becoming available. If present, overrides the default, or minimum available, for the account.
        DelayDaysOverride: int option
    }
    with
        static member New (delayDays: int, ?delayDaysOverride: int) =
            {
                BalanceSettingsResourceSettlementTiming.DelayDays = delayDays //required
                BalanceSettingsResourceSettlementTiming.DelayDaysOverride = delayDaysOverride
            }

    [<Struct>]
    type BalanceSettingsResourcePayoutsStatus =
        | Disabled
        | Enabled

    [<Struct>]
    type BalanceSettingsResourcePayoutScheduleWeeklyPayoutDays =
        | Friday
        | Monday
        | Thursday
        | Tuesday
        | Wednesday

    [<Struct>]
    type BalanceSettingsResourcePayoutScheduleInterval =
        | Daily
        | Manual
        | Monthly
        | Weekly

    type BalanceAmountBySourceType = {
        ///Amount coming from [legacy US ACH payments](https://docs.stripe.com/ach-deprecated).
        BankAccount: int option
        ///Amount coming from most payment methods, including cards as well as [non-legacy bank debits](https://docs.stripe.com/payments/bank-debits).
        Card: int option
        ///Amount coming from [FPX](https://docs.stripe.com/payments/fpx), a Malaysian payment method.
        Fpx: int option
    }
    with
        static member New (?bankAccount: int, ?card: int, ?fpx: int) =
            {
                BalanceAmountBySourceType.BankAccount = bankAccount
                BalanceAmountBySourceType.Card = card
                BalanceAmountBySourceType.Fpx = fpx
            }

    [<Struct>]
    type AutomaticTaxStatus =
        | Complete
        | Failed
        | RequiresLocationInputs

    [<Struct>]
    type AutomaticTaxDisabledReason =
        | FinalizationRequiresLocationInputs
        | FinalizationSystemError

    type Application = {
        ///Unique identifier for the object.
        Id: string
        ///The name of the application.
        Name: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "application"

        static member New (id: string, name: string option) =
            {
                Application.Id = id //required
                Application.Name = name //required
            }

    type ApplePayDomain = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        DomainName: string
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "apple_pay_domain"

        static member New (created: DateTime, domainName: string, id: string, livemode: bool) =
            {
                ApplePayDomain.Created = created //required
                ApplePayDomain.DomainName = domainName //required
                ApplePayDomain.Id = id //required
                ApplePayDomain.Livemode = livemode //required
            }

    [<Struct>]
    type ApiErrorsType =
        | ApiError
        | CardError
        | IdempotencyError
        | InvalidRequestError

    type ApiErrorsCode =
        | AccountClosed
        | AccountCountryInvalidAddress
        | AccountErrorCountryChangeRequiresAdditionalSteps
        | AccountInformationMismatch
        | AccountInvalid
        | AccountNumberInvalid
        | AccountTokenRequiredForV2Account
        | AcssDebitSessionIncomplete
        | ActionBlocked
        | AlipayUpgradeRequired
        | AmountTooLarge
        | AmountTooSmall
        | ApiKeyExpired
        | ApplicationFeesNotAllowed
        | ApprovalRequired
        | AuthenticationRequired
        | BalanceInsufficient
        | BalanceInvalidParameter
        | BankAccountBadRoutingNumbers
        | BankAccountDeclined
        | BankAccountExists
        | BankAccountRestricted
        | BankAccountUnusable
        | BankAccountUnverified
        | BankAccountVerificationFailed
        | BillingInvalidMandate
        | BitcoinUpgradeRequired
        | CaptureChargeAuthorizationExpired
        | CaptureUnauthorizedPayment
        | CardDeclineRateLimitExceeded
        | CardDeclined
        | CardholderPhoneNumberRequired
        | ChargeAlreadyCaptured
        | ChargeAlreadyRefunded
        | ChargeDisputed
        | ChargeExceedsSourceLimit
        | ChargeExceedsTransactionLimit
        | ChargeExpiredForCapture
        | ChargeInvalidParameter
        | ChargeNotRefundable
        | ClearingCodeUnsupported
        | CountryCodeInvalid
        | CountryUnsupported
        | CouponExpired
        | CustomerMaxPaymentMethods
        | CustomerMaxSubscriptions
        | CustomerSessionExpired
        | CustomerTaxLocationInvalid
        | DebitNotAuthorized
        | EmailInvalid
        | ExpiredCard
        | FinancialConnectionsAccountInactive
        | FinancialConnectionsAccountPendingAccountNumbers
        | FinancialConnectionsAccountUnavailableAccountNumbers
        | FinancialConnectionsNoSuccessfulTransactionRefresh
        | ForwardingApiInactive
        | ForwardingApiInvalidParameter
        | ForwardingApiRetryableUpstreamError
        | ForwardingApiUpstreamConnectionError
        | ForwardingApiUpstreamConnectionTimeout
        | ForwardingApiUpstreamError
        | IdempotencyKeyInUse
        | IncorrectAddress
        | IncorrectCvc
        | IncorrectNumber
        | IncorrectZip
        | IndiaRecurringPaymentMandateCanceled
        | InstantPayoutsConfigDisabled
        | InstantPayoutsCurrencyDisabled
        | InstantPayoutsLimitExceeded
        | InstantPayoutsUnsupported
        | InsufficientFunds
        | IntentInvalidState
        | IntentVerificationMethodMissing
        | InvalidCardType
        | InvalidCharacters
        | InvalidChargeAmount
        | InvalidCvc
        | InvalidExpiryMonth
        | InvalidExpiryYear
        | InvalidMandateReferencePrefixFormat
        | InvalidNumber
        | InvalidSourceUsage
        | InvalidTaxLocation
        | InvoiceNoCustomerLineItems
        | InvoiceNoPaymentMethodTypes
        | InvoiceNoSubscriptionLineItems
        | InvoiceNotEditable
        | InvoiceOnBehalfOfNotEditable
        | InvoicePaymentIntentRequiresAction
        | InvoiceUpcomingNone
        | LivemodeMismatch
        | LockTimeout
        | Missing
        | NoAccount
        | NotAllowedOnStandardAccount
        | OutOfInventory
        | OwnershipDeclarationNotAllowed
        | ParameterInvalidEmpty
        | ParameterInvalidInteger
        | ParameterInvalidStringBlank
        | ParameterInvalidStringEmpty
        | ParameterMissing
        | ParameterUnknown
        | ParametersExclusive
        | PaymentIntentActionRequired
        | PaymentIntentAuthenticationFailure
        | PaymentIntentIncompatiblePaymentMethod
        | PaymentIntentInvalidParameter
        | PaymentIntentKonbiniRejectedConfirmationNumber
        | PaymentIntentMandateInvalid
        | PaymentIntentPaymentAttemptExpired
        | PaymentIntentPaymentAttemptFailed
        | PaymentIntentRateLimitExceeded
        | PaymentIntentUnexpectedState
        | PaymentMethodBankAccountAlreadyVerified
        | PaymentMethodBankAccountBlocked
        | PaymentMethodBillingDetailsAddressMissing
        | PaymentMethodConfigurationFailures
        | PaymentMethodCurrencyMismatch
        | PaymentMethodCustomerDecline
        | PaymentMethodInvalidParameter
        | PaymentMethodInvalidParameterTestmode
        | PaymentMethodMicrodepositFailed
        | PaymentMethodMicrodepositVerificationAmountsInvalid
        | PaymentMethodMicrodepositVerificationAmountsMismatch
        | PaymentMethodMicrodepositVerificationAttemptsExceeded
        | PaymentMethodMicrodepositVerificationDescriptorCodeMismatch
        | PaymentMethodMicrodepositVerificationTimeout
        | PaymentMethodNotAvailable
        | PaymentMethodProviderDecline
        | PaymentMethodProviderTimeout
        | PaymentMethodUnactivated
        | PaymentMethodUnexpectedState
        | PaymentMethodUnsupportedType
        | PayoutReconciliationNotReady
        | PayoutsLimitExceeded
        | PayoutsNotAllowed
        | PlatformAccountRequired
        | PlatformApiKeyExpired
        | PostalCodeInvalid
        | ProcessingError
        | ProductInactive
        | ProgressiveOnboardingLimitExceeded
        | RateLimit
        | ReferToCustomer
        | RefundDisputedPayment
        | RequestBlocked
        | ResourceAlreadyExists
        | ResourceMissing
        | ReturnIntentAlreadyProcessed
        | RoutingNumberInvalid
        | SecretKeyRequired
        | SepaUnsupportedAccount
        | ServicePeriodCouponWithMeteredTieredItemUnsupported
        | SetupAttemptFailed
        | SetupIntentAuthenticationFailure
        | SetupIntentInvalidParameter
        | SetupIntentMandateInvalid
        | SetupIntentMobileWalletUnsupported
        | SetupIntentSetupAttemptExpired
        | SetupIntentUnexpectedState
        | ShippingAddressInvalid
        | ShippingCalculationFailed
        | SkuInactive
        | StateUnsupported
        | StatusTransitionInvalid
        | StorerCapabilityMissing
        | StorerCapabilityNotActive
        | StripeTaxInactive
        | TaxIdInvalid
        | TaxIdProhibited
        | TaxesCalculationFailed
        | TerminalLocationCountryUnsupported
        | TerminalReaderBusy
        | TerminalReaderHardwareFault
        | TerminalReaderInvalidLocationForActivation
        | TerminalReaderInvalidLocationForPayment
        | TerminalReaderOffline
        | TerminalReaderTimeout
        | TestmodeChargesOnly
        | TlsVersionUnsupported
        | TokenAlreadyUsed
        | TokenCardNetworkInvalid
        | TokenInUse
        | TransferSourceBalanceParametersMismatch
        | TransfersNotAllowed
        | UrlInvalid

    type AlmaInstallments = {
        ///The number of installments.
        Count: int
    }
    with
        static member New (count: int) =
            {
                AlmaInstallments.Count = count //required
            }

    [<Struct>]
    type AccountUnificationAccountControllerStripeDashboardType =
        | Express
        | Full
        | [<JsonUnionCase("none")>] None'

    [<Struct>]
    type AccountUnificationAccountControllerLossesPayments =
        | Application
        | Stripe

    [<Struct>]
    type AccountUnificationAccountControllerFeesPayer =
        | Account
        | Application
        | ApplicationCustom
        | ApplicationExpress

    [<Struct>]
    type AccountUnificationAccountControllerType =
        | Account
        | Application

    [<Struct>]
    type AccountUnificationAccountControllerRequirementCollection =
        | Application
        | Stripe

    type AccountTosAcceptance = {
        ///The Unix timestamp marking when the account representative accepted their service agreement
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Date: DateTime option
        ///The IP address from which the account representative accepted their service agreement
        Ip: string option
        ///The user's service agreement type
        ServiceAgreement: string option
        ///The user agent of the browser from which the account representative accepted their service agreement
        UserAgent: string option
    }
    with
        static member New (?date: DateTime option, ?ip: string option, ?serviceAgreement: string, ?userAgent: string option) =
            {
                AccountTosAcceptance.Date = date |> Option.flatten
                AccountTosAcceptance.Ip = ip |> Option.flatten
                AccountTosAcceptance.ServiceAgreement = serviceAgreement
                AccountTosAcceptance.UserAgent = userAgent |> Option.flatten
            }

    type AccountTermsOfService = {
        ///The Unix timestamp marking when the account representative accepted the service agreement.
        Date: int option
        ///The IP address from which the account representative accepted the service agreement.
        Ip: string option
        ///The user agent of the browser from which the account representative accepted the service agreement.
        UserAgent: string option
    }
    with
        static member New (date: int option, ip: string option, ?userAgent: string) =
            {
                AccountTermsOfService.Date = date //required
                AccountTermsOfService.Ip = ip //required
                AccountTermsOfService.UserAgent = userAgent
            }

    type AccountSepaDebitPaymentsSettings = {
        ///SEPA creditor identifier that identifies the company making the payment.
        CreditorId: string option
    }
    with
        static member New (?creditorId: string) =
            {
                AccountSepaDebitPaymentsSettings.CreditorId = creditorId
            }

    type AccountRequirementsErrorCode =
        | ExternalRequest
        | InformationMissing
        | InvalidAddressCityStatePostalCode
        | InvalidAddressHighwayContractBox
        | InvalidAddressPrivateMailbox
        | InvalidBusinessProfileName
        | InvalidBusinessProfileNameDenylisted
        | InvalidCompanyNameDenylisted
        | InvalidDobAgeOverMaximum
        | InvalidDobAgeUnder18
        | InvalidDobAgeUnderMinimum
        | InvalidProductDescriptionLength
        | InvalidProductDescriptionUrlMatch
        | InvalidRepresentativeCountry
        | InvalidSignator
        | InvalidStatementDescriptorBusinessMismatch
        | InvalidStatementDescriptorDenylisted
        | InvalidStatementDescriptorLength
        | InvalidStatementDescriptorPrefixDenylisted
        | InvalidStatementDescriptorPrefixMismatch
        | InvalidStreetAddress
        | InvalidTaxId
        | InvalidTaxIdFormat
        | InvalidTosAcceptance
        | InvalidUrlDenylisted
        | InvalidUrlFormat
        | InvalidUrlLength
        | InvalidUrlWebPresenceDetected
        | InvalidUrlWebsiteBusinessInformationMismatch
        | InvalidUrlWebsiteEmpty
        | InvalidUrlWebsiteInaccessible
        | InvalidUrlWebsiteInaccessibleGeoblocked
        | InvalidUrlWebsiteInaccessiblePasswordProtected
        | InvalidUrlWebsiteIncomplete
        | InvalidUrlWebsiteIncompleteCancellationPolicy
        | InvalidUrlWebsiteIncompleteCustomerServiceDetails
        | InvalidUrlWebsiteIncompleteLegalRestrictions
        | InvalidUrlWebsiteIncompleteRefundPolicy
        | InvalidUrlWebsiteIncompleteReturnPolicy
        | InvalidUrlWebsiteIncompleteTermsAndConditions
        | InvalidUrlWebsiteIncompleteUnderConstruction
        | InvalidUrlWebsiteOther
        | InvalidValueOther
        | UnsupportedBusinessType
        | VerificationDirectorsMismatch
        | VerificationDocumentAddressMismatch
        | VerificationDocumentAddressMissing
        | VerificationDocumentCorrupt
        | VerificationDocumentCountryNotSupported
        | VerificationDocumentDirectorsMismatch
        | VerificationDocumentDobMismatch
        | VerificationDocumentDuplicateType
        | VerificationDocumentExpired
        | VerificationDocumentFailedCopy
        | VerificationDocumentFailedGreyscale
        | VerificationDocumentFailedOther
        | VerificationDocumentFailedTestMode
        | VerificationDocumentFraudulent
        | VerificationDocumentIdNumberMismatch
        | VerificationDocumentIdNumberMissing
        | VerificationDocumentIncomplete
        | VerificationDocumentInvalid
        | VerificationDocumentIssueOrExpiryDateMissing
        | VerificationDocumentManipulated
        | VerificationDocumentMissingBack
        | VerificationDocumentMissingFront
        | VerificationDocumentNameMismatch
        | VerificationDocumentNameMissing
        | VerificationDocumentNationalityMismatch
        | VerificationDocumentNotReadable
        | VerificationDocumentNotSigned
        | VerificationDocumentNotUploaded
        | VerificationDocumentPhotoMismatch
        | VerificationDocumentTooLarge
        | VerificationDocumentTypeNotSupported
        | VerificationExtraneousDirectors
        | VerificationFailedAddressMatch
        | VerificationFailedAuthorizerAuthority
        | VerificationFailedBusinessIecNumber
        | VerificationFailedDocumentMatch
        | VerificationFailedIdNumberMatch
        | VerificationFailedKeyedIdentity
        | VerificationFailedKeyedMatch
        | VerificationFailedNameMatch
        | VerificationFailedOther
        | VerificationFailedRepresentativeAuthority
        | VerificationFailedResidentialAddress
        | VerificationFailedTaxIdMatch
        | VerificationFailedTaxIdNotIssued
        | VerificationLegalEntityStructureMismatch
        | VerificationMissingDirectors
        | VerificationMissingExecutives
        | VerificationMissingOwners
        | VerificationRejectedOwnershipExemptionReason
        | VerificationRequiresAdditionalMemorandumOfAssociations
        | VerificationRequiresAdditionalProofOfRegistration
        | VerificationSupportability

    type AccountRequirementsDisabledReason =
        | [<JsonUnionCase("action_required.requested_capabilities")>] ActionRequiredRequestedCapabilities
        | Listed
        | Other
        | PlatformPaused
        | [<JsonUnionCase("rejected.fraud")>] RejectedFraud
        | [<JsonUnionCase("rejected.incomplete_verification")>] RejectedIncompleteVerification
        | [<JsonUnionCase("rejected.listed")>] RejectedListed
        | [<JsonUnionCase("rejected.other")>] RejectedOther
        | [<JsonUnionCase("rejected.platform_fraud")>] RejectedPlatformFraud
        | [<JsonUnionCase("rejected.platform_other")>] RejectedPlatformOther
        | [<JsonUnionCase("rejected.platform_terms_of_service")>] RejectedPlatformTermsOfService
        | [<JsonUnionCase("rejected.terms_of_service")>] RejectedTermsOfService
        | [<JsonUnionCase("requirements.past_due")>] RequirementsPastDue
        | [<JsonUnionCase("requirements.pending_verification")>] RequirementsPendingVerification
        | UnderReview

    type AccountPaymentsSettings = {
        ///The default text that appears on credit card statements when a charge is made. This field prefixes any dynamic `statement_descriptor` specified on the charge.
        StatementDescriptor: string option
        ///The Kana variation of `statement_descriptor` used for charges in Japan. Japanese statement descriptors have [special requirements](https://docs.stripe.com/get-started/account/statement-descriptors#set-japanese-statement-descriptors).
        StatementDescriptorKana: string option
        ///The Kanji variation of `statement_descriptor` used for charges in Japan. Japanese statement descriptors have [special requirements](https://docs.stripe.com/get-started/account/statement-descriptors#set-japanese-statement-descriptors).
        StatementDescriptorKanji: string option
        ///The Kana variation of `statement_descriptor_prefix` used for card charges in Japan. Japanese statement descriptors have [special requirements](https://docs.stripe.com/get-started/account/statement-descriptors#set-japanese-statement-descriptors).
        StatementDescriptorPrefixKana: string option
        ///The Kanji variation of `statement_descriptor_prefix` used for card charges in Japan. Japanese statement descriptors have [special requirements](https://docs.stripe.com/get-started/account/statement-descriptors#set-japanese-statement-descriptors).
        StatementDescriptorPrefixKanji: string option
    }
    with
        static member New (statementDescriptor: string option, statementDescriptorKana: string option, statementDescriptorKanji: string option, statementDescriptorPrefixKana: string option, statementDescriptorPrefixKanji: string option) =
            {
                AccountPaymentsSettings.StatementDescriptor = statementDescriptor //required
                AccountPaymentsSettings.StatementDescriptorKana = statementDescriptorKana //required
                AccountPaymentsSettings.StatementDescriptorKanji = statementDescriptorKanji //required
                AccountPaymentsSettings.StatementDescriptorPrefixKana = statementDescriptorPrefixKana //required
                AccountPaymentsSettings.StatementDescriptorPrefixKanji = statementDescriptorPrefixKanji //required
            }

    ///Account Links are the means by which a Connect platform grants a connected account permission to access
    ///Stripe-hosted applications, such as Connect Onboarding.
    ///Related guide: [Connect Onboarding](https://docs.stripe.com/connect/custom/hosted-onboarding)
    type AccountLink = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The timestamp at which this account link will expire.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime
        ///The URL for the account link.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "account_link"

        static member New (created: DateTime, expiresAt: DateTime, url: string) =
            {
                AccountLink.Created = created //required
                AccountLink.ExpiresAt = expiresAt //required
                AccountLink.Url = url //required
            }

    [<Struct>]
    type AccountInvoicesSettingsHostedPaymentMethodSave =
        | Always
        | Never
        | Offer

    type AccountGroupMembership = {
        ///The group the account is in to determine their payments pricing, and null if the account is on customized pricing. [See the Platform pricing tool documentation](https://docs.stripe.com/connect/platform-pricing-tools) for details.
        PaymentsPricing: string option
    }
    with
        static member New (paymentsPricing: string option) =
            {
                AccountGroupMembership.PaymentsPricing = paymentsPricing //required
            }

    type AccountFutureRequirementsDisabledReason =
        | [<JsonUnionCase("action_required.requested_capabilities")>] ActionRequiredRequestedCapabilities
        | Listed
        | Other
        | PlatformPaused
        | [<JsonUnionCase("rejected.fraud")>] RejectedFraud
        | [<JsonUnionCase("rejected.incomplete_verification")>] RejectedIncompleteVerification
        | [<JsonUnionCase("rejected.listed")>] RejectedListed
        | [<JsonUnionCase("rejected.other")>] RejectedOther
        | [<JsonUnionCase("rejected.platform_fraud")>] RejectedPlatformFraud
        | [<JsonUnionCase("rejected.platform_other")>] RejectedPlatformOther
        | [<JsonUnionCase("rejected.platform_terms_of_service")>] RejectedPlatformTermsOfService
        | [<JsonUnionCase("rejected.terms_of_service")>] RejectedTermsOfService
        | [<JsonUnionCase("requirements.past_due")>] RequirementsPastDue
        | [<JsonUnionCase("requirements.pending_verification")>] RequirementsPendingVerification
        | UnderReview

    type AccountDeclineChargeOn = {
        ///Whether Stripe automatically declines charges with an incorrect ZIP or postal code. This setting only applies when a ZIP or postal code is provided and they fail bank verification.
        AvsFailure: bool
        ///Whether Stripe automatically declines charges with an incorrect CVC. This setting only applies when a CVC is provided and it fails bank verification.
        CvcFailure: bool
    }
    with
        static member New (avsFailure: bool, cvcFailure: bool) =
            {
                AccountDeclineChargeOn.AvsFailure = avsFailure //required
                AccountDeclineChargeOn.CvcFailure = cvcFailure //required
            }

    type AccountDashboardSettings = {
        ///The display name for this account. This is used on the Stripe Dashboard to differentiate between accounts.
        DisplayName: string option
        ///The timezone used in the Stripe Dashboard for this account. A list of possible time zone values is maintained at the [IANA Time Zone Database](http://www.iana.org/time-zones).
        Timezone: string option
    }
    with
        static member New (displayName: string option, timezone: string option) =
            {
                AccountDashboardSettings.DisplayName = displayName //required
                AccountDashboardSettings.Timezone = timezone //required
            }

    type AccountCapabilityRequirementsDisabledReason =
        | Other
        | [<JsonUnionCase("paused.inactivity")>] PausedInactivity
        | [<JsonUnionCase("pending.onboarding")>] PendingOnboarding
        | [<JsonUnionCase("pending.review")>] PendingReview
        | PlatformDisabled
        | PlatformPaused
        | [<JsonUnionCase("rejected.inactivity")>] RejectedInactivity
        | [<JsonUnionCase("rejected.other")>] RejectedOther
        | [<JsonUnionCase("rejected.unsupported_business")>] RejectedUnsupportedBusiness
        | [<JsonUnionCase("requirements.fields_needed")>] RequirementsFieldsNeeded

    type AccountCapabilityFutureRequirementsDisabledReason =
        | Other
        | [<JsonUnionCase("paused.inactivity")>] PausedInactivity
        | [<JsonUnionCase("pending.onboarding")>] PendingOnboarding
        | [<JsonUnionCase("pending.review")>] PendingReview
        | PlatformDisabled
        | PlatformPaused
        | [<JsonUnionCase("rejected.inactivity")>] RejectedInactivity
        | [<JsonUnionCase("rejected.other")>] RejectedOther
        | [<JsonUnionCase("rejected.unsupported_business")>] RejectedUnsupportedBusiness
        | [<JsonUnionCase("requirements.fields_needed")>] RequirementsFieldsNeeded

    [<Struct>]
    type AccountCapabilitiesZipPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesUsBankTransferPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesUsBankAccountAchPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesUpiPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesTwintPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesTreasury =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesTransfers =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesTaxReportingUs1099Misc =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesTaxReportingUs1099K =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesSwishPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesSunbitPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesSofortPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesSepaDebitPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesSepaBankTransferPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesSatispayPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesSamsungPayPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesRevolutPayPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesPromptpayPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesPixPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesPaytoPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesPaynowPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesPaycoPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesPayByBankPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesP24Payments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesOxxoPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesNzBankAccountBecsDebitPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesNaverPayPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesMxBankTransferPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesMultibancoPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesMobilepayPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesMbWayPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesLinkPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesLegacyPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesKrCardPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesKonbiniPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesKlarnaPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesKakaoPayPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesJpBankTransferPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesJcbPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesIndiaInternationalPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesIdealPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesGrabpayPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesGiropayPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesGbBankTransferPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesFpxPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesEpsPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesCryptoPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesCashappPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesCartesBancairesPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesCardPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesCardIssuing =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesBoletoPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesBlikPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesBilliePayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesBankTransferPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesBancontactPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesBacsDebitPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesAuBecsDebitPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesAppDistribution =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesAmazonPayPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesAlmaPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesAfterpayClearpayPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesAffirmPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountCapabilitiesAcssDebitPayments =
        | Active
        | Inactive
        | Pending

    [<Struct>]
    type AccountBusinessProfileMinorityOwnedBusinessDesignation =
        | LgbtqiOwnedBusiness
        | MinorityOwnedBusiness
        | NoneOfTheseApply
        | PreferNotToAnswer
        | WomenOwnedBusiness

    type AccountBacsDebitPaymentsSettings = {
        ///The Bacs Direct Debit display name for this account. For payments made with Bacs Direct Debit, this name appears on the mandate as the statement descriptor. Mobile banking apps display it as the name of the business. To use custom branding, set the Bacs Direct Debit Display Name during or right after creation. Custom branding incurs an additional monthly fee for the platform. The fee appears 5 business days after requesting Bacs. If you don't set the display name before requesting Bacs capability, it's automatically set as "Stripe" and the account is onboarded to Stripe branding, which is free.
        DisplayName: string option
        ///The Bacs Direct Debit Service user number for this account. For payments made with Bacs Direct Debit, this number is a unique identifier of the account with our banking partners.
        ServiceUserNumber: string option
    }
    with
        static member New (displayName: string option, serviceUserNumber: string option) =
            {
                AccountBacsDebitPaymentsSettings.DisplayName = displayName //required
                AccountBacsDebitPaymentsSettings.ServiceUserNumber = serviceUserNumber //required
            }

    [<Struct>]
    type AccountType =
        | Custom
        | Express
        | [<JsonUnionCase("none")>] None'
        | Standard

    [<Struct>]
    type AccountBusinessType =
        | Company
        | GovernmentEntity
        | Individual
        | NonProfit


    ///For new integrations, we recommend using the [Accounts v2 API](/api/v2/core/accounts), in place of /v1/accounts and /v1/customers to represent a user.
    ///This is an object representing a Stripe account. You can retrieve it to see
    ///properties on the account like its current requirements or if the account is
    ///enabled to make live charges or receive payouts.
    ///For accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection)
    ///is `application`, which includes Custom accounts, the properties below are always
    ///returned.
    ///For accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection)
    ///is `stripe`, which includes Standard and Express accounts, some properties are only returned
    ///until you create an [Account Link](/api/account_links) or [Account Session](/api/account_sessions)
    ///to start Connect Onboarding. Learn about the [differences between accounts](/connect/accounts).
    type Account = {
        ///Business information about the account.
        BusinessProfile: AccountBusinessProfile option
        ///The business type.
        BusinessType: AccountBusinessType option
        Capabilities: AccountCapabilities option
        ///Whether the account can process charges.
        ChargesEnabled: bool option
        Company: LegalEntityCompany option
        Controller: AccountUnificationAccountController option
        ///The account's country.
        Country: IsoTypes.IsoCountryCode option
        ///Time at which the account was connected. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime option
        ///Three-letter ISO currency code representing the default currency for the account. This must be a currency that [Stripe supports in the account's country](https://stripe.com/docs/payouts).
        DefaultCurrency: IsoTypes.IsoCurrencyCode option
        ///Whether account details have been submitted. Accounts with Stripe Dashboard access, which includes Standard accounts, cannot receive payouts before this is true. Accounts where this is false should be directed to [an onboarding flow](/connect/onboarding) to finish submitting account details.
        DetailsSubmitted: bool option
        ///An email address associated with the account. It's not used for authentication and Stripe doesn't market to this field without explicit approval from the platform.
        Email: string option
        ///External accounts (bank accounts and debit cards) currently attached to this account. External accounts are only returned for requests where `controller[is_controller]` is true.
        ExternalAccounts: AccountExternalAccounts option
        FutureRequirements: AccountFutureRequirements option
        ///The groups associated with the account.
        Groups: AccountGroupMembership option
        ///Unique identifier for the object.
        Id: string
        Individual: Person option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///Whether the funds in this account can be paid out.
        PayoutsEnabled: bool option
        Requirements: AccountRequirements option
        ///Options for customizing how the account functions within Stripe.
        Settings: AccountSettings option
        TosAcceptance: AccountTosAcceptance option
        ///The Stripe account type. Can be `standard`, `express`, `custom`, or `none`.
        Type: AccountType option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "account"

        static member New (id: string, ?businessProfile: AccountBusinessProfile option, ?businessType: AccountBusinessType option, ?capabilities: AccountCapabilities, ?chargesEnabled: bool, ?company: LegalEntityCompany, ?controller: AccountUnificationAccountController, ?country: IsoTypes.IsoCountryCode, ?created: DateTime, ?defaultCurrency: IsoTypes.IsoCurrencyCode, ?detailsSubmitted: bool, ?email: string option, ?externalAccounts: AccountExternalAccounts, ?futureRequirements: AccountFutureRequirements, ?groups: AccountGroupMembership option, ?individual: Person, ?metadata: Map<string, string>, ?payoutsEnabled: bool, ?requirements: AccountRequirements, ?settings: AccountSettings option, ?tosAcceptance: AccountTosAcceptance, ?``type``: AccountType) =
            {
                Account.Id = id //required
                Account.BusinessProfile = businessProfile |> Option.flatten
                Account.BusinessType = businessType |> Option.flatten
                Account.Capabilities = capabilities
                Account.ChargesEnabled = chargesEnabled
                Account.Company = company
                Account.Controller = controller
                Account.Country = country
                Account.Created = created
                Account.DefaultCurrency = defaultCurrency
                Account.DetailsSubmitted = detailsSubmitted
                Account.Email = email |> Option.flatten
                Account.ExternalAccounts = externalAccounts
                Account.FutureRequirements = futureRequirements
                Account.Groups = groups |> Option.flatten
                Account.Individual = individual
                Account.Metadata = metadata
                Account.PayoutsEnabled = payoutsEnabled
                Account.Requirements = requirements
                Account.Settings = settings |> Option.flatten
                Account.TosAcceptance = tosAcceptance
                Account.Type = ``type``
            }

    ///External accounts (bank accounts and debit cards) currently attached to this account. External accounts are only returned for requests where `controller[is_controller]` is true.
    and AccountExternalAccounts = {
        ///The list contains all external accounts that have been attached to the Stripe account. These may be bank accounts or cards.
        Data: ExternalAccount list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: ExternalAccount list, hasMore: bool, url: string) =
            {
                AccountExternalAccounts.Data = data //required
                AccountExternalAccounts.HasMore = hasMore //required
                AccountExternalAccounts.Url = url //required
            }

    and AccountAnnualRevenue = {
        ///A non-negative integer representing the amount in the [smallest currency unit](/currencies#zero-decimal).
        Amount: int option
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode option
        ///The close-out date of the preceding fiscal year in ISO 8601 format. E.g. 2023-12-31 for the 31st of December, 2023.
        FiscalYearEnd: string option
    }
    with
        static member New (amount: int option, currency: IsoTypes.IsoCurrencyCode option, fiscalYearEnd: string option) =
            {
                AccountAnnualRevenue.Amount = amount //required
                AccountAnnualRevenue.Currency = currency //required
                AccountAnnualRevenue.FiscalYearEnd = fiscalYearEnd //required
            }

    and AccountBrandingSettings = {
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) An icon for the account. Must be square and at least 128px x 128px.
        Icon: AccountBrandingSettingsIcon'AnyOf option
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) A logo for the account that will be used in Checkout instead of the icon and without the account's name next to it if provided. Must be at least 128px x 128px.
        Logo: AccountBrandingSettingsLogo'AnyOf option
        ///A CSS hex color value representing the primary branding color for this account
        PrimaryColor: string option
        ///A CSS hex color value representing the secondary branding color for this account
        SecondaryColor: string option
    }
    with
        static member New (icon: AccountBrandingSettingsIcon'AnyOf option, logo: AccountBrandingSettingsLogo'AnyOf option, primaryColor: string option, secondaryColor: string option) =
            {
                AccountBrandingSettings.Icon = icon //required
                AccountBrandingSettings.Logo = logo //required
                AccountBrandingSettings.PrimaryColor = primaryColor //required
                AccountBrandingSettings.SecondaryColor = secondaryColor //required
            }

    and AccountBrandingSettingsIcon'AnyOf =
        | String of string
        | File of File

    and AccountBrandingSettingsLogo'AnyOf =
        | String of string
        | File of File

    and AccountBusinessProfile = {
        ///The applicant's gross annual revenue for its preceding fiscal year.
        AnnualRevenue: AccountAnnualRevenue option
        ///An estimated upper bound of employees, contractors, vendors, etc. currently working for the business.
        EstimatedWorkerCount: int option
        ///[The merchant category code for the account](/connect/setting-mcc). MCCs are used to classify businesses based on the goods or services they provide.
        Mcc: string option
        ///Whether the business is a minority-owned, women-owned, and/or LGBTQI+ -owned business.
        MinorityOwnedBusinessDesignation: AccountBusinessProfileMinorityOwnedBusinessDesignation list option
        MonthlyEstimatedRevenue: AccountMonthlyEstimatedRevenue option
        ///The customer-facing business name.
        Name: string option
        ///Internal-only description of the product sold or service provided by the business. It's used by Stripe for risk and underwriting purposes.
        ProductDescription: string option
        ///A publicly available mailing address for sending support issues to.
        SupportAddress: Address option
        ///A publicly available email address for sending support issues to.
        SupportEmail: string option
        ///A publicly available phone number to call with support issues.
        SupportPhone: string option
        ///A publicly available website for handling support issues.
        SupportUrl: string option
        ///The business's publicly available website.
        Url: string option
    }
    with
        static member New (mcc: string option, minorityOwnedBusinessDesignation: AccountBusinessProfileMinorityOwnedBusinessDesignation list option, name: string option, supportAddress: Address option, supportEmail: string option, supportPhone: string option, supportUrl: string option, url: string option, ?annualRevenue: AccountAnnualRevenue option, ?estimatedWorkerCount: int option, ?monthlyEstimatedRevenue: AccountMonthlyEstimatedRevenue, ?productDescription: string option) =
            {
                AccountBusinessProfile.Mcc = mcc //required
                AccountBusinessProfile.MinorityOwnedBusinessDesignation = minorityOwnedBusinessDesignation //required
                AccountBusinessProfile.Name = name //required
                AccountBusinessProfile.SupportAddress = supportAddress //required
                AccountBusinessProfile.SupportEmail = supportEmail //required
                AccountBusinessProfile.SupportPhone = supportPhone //required
                AccountBusinessProfile.SupportUrl = supportUrl //required
                AccountBusinessProfile.Url = url //required
                AccountBusinessProfile.AnnualRevenue = annualRevenue |> Option.flatten
                AccountBusinessProfile.EstimatedWorkerCount = estimatedWorkerCount |> Option.flatten
                AccountBusinessProfile.MonthlyEstimatedRevenue = monthlyEstimatedRevenue
                AccountBusinessProfile.ProductDescription = productDescription |> Option.flatten
            }

    and AccountCapabilities = {
        ///The status of the Canadian pre-authorized debits payments capability of the account, or whether the account can directly process Canadian pre-authorized debits charges.
        AcssDebitPayments: AccountCapabilitiesAcssDebitPayments option
        ///The status of the Affirm capability of the account, or whether the account can directly process Affirm charges.
        AffirmPayments: AccountCapabilitiesAffirmPayments option
        ///The status of the Afterpay Clearpay capability of the account, or whether the account can directly process Afterpay Clearpay charges.
        AfterpayClearpayPayments: AccountCapabilitiesAfterpayClearpayPayments option
        ///The status of the Alma capability of the account, or whether the account can directly process Alma payments.
        AlmaPayments: AccountCapabilitiesAlmaPayments option
        ///The status of the AmazonPay capability of the account, or whether the account can directly process AmazonPay payments.
        AmazonPayPayments: AccountCapabilitiesAmazonPayPayments option
        ///The status of the `app_distribution` capability of the account, or whether the platform can distribute apps to other accounts.
        AppDistribution: AccountCapabilitiesAppDistribution option
        ///The status of the BECS Direct Debit (AU) payments capability of the account, or whether the account can directly process BECS Direct Debit (AU) charges.
        AuBecsDebitPayments: AccountCapabilitiesAuBecsDebitPayments option
        ///The status of the Bacs Direct Debits payments capability of the account, or whether the account can directly process Bacs Direct Debits charges.
        BacsDebitPayments: AccountCapabilitiesBacsDebitPayments option
        ///The status of the Bancontact payments capability of the account, or whether the account can directly process Bancontact charges.
        BancontactPayments: AccountCapabilitiesBancontactPayments option
        ///The status of the customer_balance payments capability of the account, or whether the account can directly process customer_balance charges.
        BankTransferPayments: AccountCapabilitiesBankTransferPayments option
        ///The status of the Billie capability of the account, or whether the account can directly process Billie payments.
        BilliePayments: AccountCapabilitiesBilliePayments option
        ///The status of the blik payments capability of the account, or whether the account can directly process blik charges.
        BlikPayments: AccountCapabilitiesBlikPayments option
        ///The status of the boleto payments capability of the account, or whether the account can directly process boleto charges.
        BoletoPayments: AccountCapabilitiesBoletoPayments option
        ///The status of the card issuing capability of the account, or whether you can use Issuing to distribute funds on cards
        CardIssuing: AccountCapabilitiesCardIssuing option
        ///The status of the card payments capability of the account, or whether the account can directly process credit and debit card charges.
        CardPayments: AccountCapabilitiesCardPayments option
        ///The status of the Cartes Bancaires payments capability of the account, or whether the account can directly process Cartes Bancaires card charges in EUR currency.
        CartesBancairesPayments: AccountCapabilitiesCartesBancairesPayments option
        ///The status of the Cash App Pay capability of the account, or whether the account can directly process Cash App Pay payments.
        CashappPayments: AccountCapabilitiesCashappPayments option
        ///The status of the Crypto capability of the account, or whether the account can directly process Crypto payments.
        CryptoPayments: AccountCapabilitiesCryptoPayments option
        ///The status of the EPS payments capability of the account, or whether the account can directly process EPS charges.
        EpsPayments: AccountCapabilitiesEpsPayments option
        ///The status of the FPX payments capability of the account, or whether the account can directly process FPX charges.
        FpxPayments: AccountCapabilitiesFpxPayments option
        ///The status of the GB customer_balance payments (GBP currency) capability of the account, or whether the account can directly process GB customer_balance charges.
        GbBankTransferPayments: AccountCapabilitiesGbBankTransferPayments option
        ///The status of the giropay payments capability of the account, or whether the account can directly process giropay charges.
        GiropayPayments: AccountCapabilitiesGiropayPayments option
        ///The status of the GrabPay payments capability of the account, or whether the account can directly process GrabPay charges.
        GrabpayPayments: AccountCapabilitiesGrabpayPayments option
        ///The status of the iDEAL payments capability of the account, or whether the account can directly process iDEAL charges.
        IdealPayments: AccountCapabilitiesIdealPayments option
        ///The status of the india_international_payments capability of the account, or whether the account can process international charges (non INR) in India.
        IndiaInternationalPayments: AccountCapabilitiesIndiaInternationalPayments option
        ///The status of the JCB payments capability of the account, or whether the account (Japan only) can directly process JCB credit card charges in JPY currency.
        JcbPayments: AccountCapabilitiesJcbPayments option
        ///The status of the Japanese customer_balance payments (JPY currency) capability of the account, or whether the account can directly process Japanese customer_balance charges.
        JpBankTransferPayments: AccountCapabilitiesJpBankTransferPayments option
        ///The status of the KakaoPay capability of the account, or whether the account can directly process KakaoPay payments.
        KakaoPayPayments: AccountCapabilitiesKakaoPayPayments option
        ///The status of the Klarna payments capability of the account, or whether the account can directly process Klarna charges.
        KlarnaPayments: AccountCapabilitiesKlarnaPayments option
        ///The status of the konbini payments capability of the account, or whether the account can directly process konbini charges.
        KonbiniPayments: AccountCapabilitiesKonbiniPayments option
        ///The status of the KrCard capability of the account, or whether the account can directly process KrCard payments.
        KrCardPayments: AccountCapabilitiesKrCardPayments option
        ///The status of the legacy payments capability of the account.
        LegacyPayments: AccountCapabilitiesLegacyPayments option
        ///The status of the link_payments capability of the account, or whether the account can directly process Link charges.
        LinkPayments: AccountCapabilitiesLinkPayments option
        ///The status of the MB WAY payments capability of the account, or whether the account can directly process MB WAY charges.
        MbWayPayments: AccountCapabilitiesMbWayPayments option
        ///The status of the MobilePay capability of the account, or whether the account can directly process MobilePay charges.
        MobilepayPayments: AccountCapabilitiesMobilepayPayments option
        ///The status of the Multibanco payments capability of the account, or whether the account can directly process Multibanco charges.
        MultibancoPayments: AccountCapabilitiesMultibancoPayments option
        ///The status of the Mexican customer_balance payments (MXN currency) capability of the account, or whether the account can directly process Mexican customer_balance charges.
        MxBankTransferPayments: AccountCapabilitiesMxBankTransferPayments option
        ///The status of the NaverPay capability of the account, or whether the account can directly process NaverPay payments.
        NaverPayPayments: AccountCapabilitiesNaverPayPayments option
        ///The status of the New Zealand BECS Direct Debit payments capability of the account, or whether the account can directly process New Zealand BECS Direct Debit charges.
        NzBankAccountBecsDebitPayments: AccountCapabilitiesNzBankAccountBecsDebitPayments option
        ///The status of the OXXO payments capability of the account, or whether the account can directly process OXXO charges.
        OxxoPayments: AccountCapabilitiesOxxoPayments option
        ///The status of the P24 payments capability of the account, or whether the account can directly process P24 charges.
        [<JsonField(Name="p24_payments")>]P24Payments: AccountCapabilitiesP24Payments option
        ///The status of the pay_by_bank payments capability of the account, or whether the account can directly process pay_by_bank charges.
        PayByBankPayments: AccountCapabilitiesPayByBankPayments option
        ///The status of the Payco capability of the account, or whether the account can directly process Payco payments.
        PaycoPayments: AccountCapabilitiesPaycoPayments option
        ///The status of the paynow payments capability of the account, or whether the account can directly process paynow charges.
        PaynowPayments: AccountCapabilitiesPaynowPayments option
        ///The status of the PayTo capability of the account, or whether the account can directly process PayTo charges.
        PaytoPayments: AccountCapabilitiesPaytoPayments option
        ///The status of the pix payments capability of the account, or whether the account can directly process pix charges.
        PixPayments: AccountCapabilitiesPixPayments option
        ///The status of the promptpay payments capability of the account, or whether the account can directly process promptpay charges.
        PromptpayPayments: AccountCapabilitiesPromptpayPayments option
        ///The status of the RevolutPay capability of the account, or whether the account can directly process RevolutPay payments.
        RevolutPayPayments: AccountCapabilitiesRevolutPayPayments option
        ///The status of the SamsungPay capability of the account, or whether the account can directly process SamsungPay payments.
        SamsungPayPayments: AccountCapabilitiesSamsungPayPayments option
        ///The status of the Satispay capability of the account, or whether the account can directly process Satispay payments.
        SatispayPayments: AccountCapabilitiesSatispayPayments option
        ///The status of the SEPA customer_balance payments (EUR currency) capability of the account, or whether the account can directly process SEPA customer_balance charges.
        SepaBankTransferPayments: AccountCapabilitiesSepaBankTransferPayments option
        ///The status of the SEPA Direct Debits payments capability of the account, or whether the account can directly process SEPA Direct Debits charges.
        SepaDebitPayments: AccountCapabilitiesSepaDebitPayments option
        ///The status of the Sofort payments capability of the account, or whether the account can directly process Sofort charges.
        SofortPayments: AccountCapabilitiesSofortPayments option
        ///The status of the Sunbit capability of the account, or whether the account can directly process Sunbit payments.
        SunbitPayments: AccountCapabilitiesSunbitPayments option
        ///The status of the Swish capability of the account, or whether the account can directly process Swish payments.
        SwishPayments: AccountCapabilitiesSwishPayments option
        ///The status of the tax reporting 1099-K (US) capability of the account.
        [<JsonField(Name="tax_reporting_us_1099_k")>]TaxReportingUs1099K: AccountCapabilitiesTaxReportingUs1099K option
        ///The status of the tax reporting 1099-MISC (US) capability of the account.
        [<JsonField(Name="tax_reporting_us_1099_misc")>]TaxReportingUs1099Misc: AccountCapabilitiesTaxReportingUs1099Misc option
        ///The status of the transfers capability of the account, or whether your platform can transfer funds to the account.
        Transfers: AccountCapabilitiesTransfers option
        ///The status of the banking capability, or whether the account can have bank accounts.
        Treasury: AccountCapabilitiesTreasury option
        ///The status of the TWINT capability of the account, or whether the account can directly process TWINT charges.
        TwintPayments: AccountCapabilitiesTwintPayments option
        ///The status of the upi payments capability of the account, or whether the account can directly process upi charges.
        UpiPayments: AccountCapabilitiesUpiPayments option
        ///The status of the US bank account ACH payments capability of the account, or whether the account can directly process US bank account charges.
        UsBankAccountAchPayments: AccountCapabilitiesUsBankAccountAchPayments option
        ///The status of the US customer_balance payments (USD currency) capability of the account, or whether the account can directly process US customer_balance charges.
        UsBankTransferPayments: AccountCapabilitiesUsBankTransferPayments option
        ///The status of the Zip capability of the account, or whether the account can directly process Zip charges.
        ZipPayments: AccountCapabilitiesZipPayments option
    }
    with
        static member New (?acssDebitPayments: AccountCapabilitiesAcssDebitPayments, ?affirmPayments: AccountCapabilitiesAffirmPayments, ?afterpayClearpayPayments: AccountCapabilitiesAfterpayClearpayPayments, ?almaPayments: AccountCapabilitiesAlmaPayments, ?amazonPayPayments: AccountCapabilitiesAmazonPayPayments, ?appDistribution: AccountCapabilitiesAppDistribution, ?auBecsDebitPayments: AccountCapabilitiesAuBecsDebitPayments, ?bacsDebitPayments: AccountCapabilitiesBacsDebitPayments, ?bancontactPayments: AccountCapabilitiesBancontactPayments, ?bankTransferPayments: AccountCapabilitiesBankTransferPayments, ?billiePayments: AccountCapabilitiesBilliePayments, ?blikPayments: AccountCapabilitiesBlikPayments, ?boletoPayments: AccountCapabilitiesBoletoPayments, ?cardIssuing: AccountCapabilitiesCardIssuing, ?cardPayments: AccountCapabilitiesCardPayments, ?cartesBancairesPayments: AccountCapabilitiesCartesBancairesPayments, ?cashappPayments: AccountCapabilitiesCashappPayments, ?cryptoPayments: AccountCapabilitiesCryptoPayments, ?epsPayments: AccountCapabilitiesEpsPayments, ?fpxPayments: AccountCapabilitiesFpxPayments, ?gbBankTransferPayments: AccountCapabilitiesGbBankTransferPayments, ?giropayPayments: AccountCapabilitiesGiropayPayments, ?grabpayPayments: AccountCapabilitiesGrabpayPayments, ?idealPayments: AccountCapabilitiesIdealPayments, ?indiaInternationalPayments: AccountCapabilitiesIndiaInternationalPayments, ?jcbPayments: AccountCapabilitiesJcbPayments, ?jpBankTransferPayments: AccountCapabilitiesJpBankTransferPayments, ?kakaoPayPayments: AccountCapabilitiesKakaoPayPayments, ?klarnaPayments: AccountCapabilitiesKlarnaPayments, ?konbiniPayments: AccountCapabilitiesKonbiniPayments, ?krCardPayments: AccountCapabilitiesKrCardPayments, ?legacyPayments: AccountCapabilitiesLegacyPayments, ?linkPayments: AccountCapabilitiesLinkPayments, ?mbWayPayments: AccountCapabilitiesMbWayPayments, ?mobilepayPayments: AccountCapabilitiesMobilepayPayments, ?multibancoPayments: AccountCapabilitiesMultibancoPayments, ?mxBankTransferPayments: AccountCapabilitiesMxBankTransferPayments, ?naverPayPayments: AccountCapabilitiesNaverPayPayments, ?nzBankAccountBecsDebitPayments: AccountCapabilitiesNzBankAccountBecsDebitPayments, ?oxxoPayments: AccountCapabilitiesOxxoPayments, ?p24Payments: AccountCapabilitiesP24Payments, ?payByBankPayments: AccountCapabilitiesPayByBankPayments, ?paycoPayments: AccountCapabilitiesPaycoPayments, ?paynowPayments: AccountCapabilitiesPaynowPayments, ?paytoPayments: AccountCapabilitiesPaytoPayments, ?pixPayments: AccountCapabilitiesPixPayments, ?promptpayPayments: AccountCapabilitiesPromptpayPayments, ?revolutPayPayments: AccountCapabilitiesRevolutPayPayments, ?samsungPayPayments: AccountCapabilitiesSamsungPayPayments, ?satispayPayments: AccountCapabilitiesSatispayPayments, ?sepaBankTransferPayments: AccountCapabilitiesSepaBankTransferPayments, ?sepaDebitPayments: AccountCapabilitiesSepaDebitPayments, ?sofortPayments: AccountCapabilitiesSofortPayments, ?sunbitPayments: AccountCapabilitiesSunbitPayments, ?swishPayments: AccountCapabilitiesSwishPayments, ?taxReportingUs1099K: AccountCapabilitiesTaxReportingUs1099K, ?taxReportingUs1099Misc: AccountCapabilitiesTaxReportingUs1099Misc, ?transfers: AccountCapabilitiesTransfers, ?treasury: AccountCapabilitiesTreasury, ?twintPayments: AccountCapabilitiesTwintPayments, ?upiPayments: AccountCapabilitiesUpiPayments, ?usBankAccountAchPayments: AccountCapabilitiesUsBankAccountAchPayments, ?usBankTransferPayments: AccountCapabilitiesUsBankTransferPayments, ?zipPayments: AccountCapabilitiesZipPayments) =
            {
                AccountCapabilities.AcssDebitPayments = acssDebitPayments
                AccountCapabilities.AffirmPayments = affirmPayments
                AccountCapabilities.AfterpayClearpayPayments = afterpayClearpayPayments
                AccountCapabilities.AlmaPayments = almaPayments
                AccountCapabilities.AmazonPayPayments = amazonPayPayments
                AccountCapabilities.AppDistribution = appDistribution
                AccountCapabilities.AuBecsDebitPayments = auBecsDebitPayments
                AccountCapabilities.BacsDebitPayments = bacsDebitPayments
                AccountCapabilities.BancontactPayments = bancontactPayments
                AccountCapabilities.BankTransferPayments = bankTransferPayments
                AccountCapabilities.BilliePayments = billiePayments
                AccountCapabilities.BlikPayments = blikPayments
                AccountCapabilities.BoletoPayments = boletoPayments
                AccountCapabilities.CardIssuing = cardIssuing
                AccountCapabilities.CardPayments = cardPayments
                AccountCapabilities.CartesBancairesPayments = cartesBancairesPayments
                AccountCapabilities.CashappPayments = cashappPayments
                AccountCapabilities.CryptoPayments = cryptoPayments
                AccountCapabilities.EpsPayments = epsPayments
                AccountCapabilities.FpxPayments = fpxPayments
                AccountCapabilities.GbBankTransferPayments = gbBankTransferPayments
                AccountCapabilities.GiropayPayments = giropayPayments
                AccountCapabilities.GrabpayPayments = grabpayPayments
                AccountCapabilities.IdealPayments = idealPayments
                AccountCapabilities.IndiaInternationalPayments = indiaInternationalPayments
                AccountCapabilities.JcbPayments = jcbPayments
                AccountCapabilities.JpBankTransferPayments = jpBankTransferPayments
                AccountCapabilities.KakaoPayPayments = kakaoPayPayments
                AccountCapabilities.KlarnaPayments = klarnaPayments
                AccountCapabilities.KonbiniPayments = konbiniPayments
                AccountCapabilities.KrCardPayments = krCardPayments
                AccountCapabilities.LegacyPayments = legacyPayments
                AccountCapabilities.LinkPayments = linkPayments
                AccountCapabilities.MbWayPayments = mbWayPayments
                AccountCapabilities.MobilepayPayments = mobilepayPayments
                AccountCapabilities.MultibancoPayments = multibancoPayments
                AccountCapabilities.MxBankTransferPayments = mxBankTransferPayments
                AccountCapabilities.NaverPayPayments = naverPayPayments
                AccountCapabilities.NzBankAccountBecsDebitPayments = nzBankAccountBecsDebitPayments
                AccountCapabilities.OxxoPayments = oxxoPayments
                AccountCapabilities.P24Payments = p24Payments
                AccountCapabilities.PayByBankPayments = payByBankPayments
                AccountCapabilities.PaycoPayments = paycoPayments
                AccountCapabilities.PaynowPayments = paynowPayments
                AccountCapabilities.PaytoPayments = paytoPayments
                AccountCapabilities.PixPayments = pixPayments
                AccountCapabilities.PromptpayPayments = promptpayPayments
                AccountCapabilities.RevolutPayPayments = revolutPayPayments
                AccountCapabilities.SamsungPayPayments = samsungPayPayments
                AccountCapabilities.SatispayPayments = satispayPayments
                AccountCapabilities.SepaBankTransferPayments = sepaBankTransferPayments
                AccountCapabilities.SepaDebitPayments = sepaDebitPayments
                AccountCapabilities.SofortPayments = sofortPayments
                AccountCapabilities.SunbitPayments = sunbitPayments
                AccountCapabilities.SwishPayments = swishPayments
                AccountCapabilities.TaxReportingUs1099K = taxReportingUs1099K
                AccountCapabilities.TaxReportingUs1099Misc = taxReportingUs1099Misc
                AccountCapabilities.Transfers = transfers
                AccountCapabilities.Treasury = treasury
                AccountCapabilities.TwintPayments = twintPayments
                AccountCapabilities.UpiPayments = upiPayments
                AccountCapabilities.UsBankAccountAchPayments = usBankAccountAchPayments
                AccountCapabilities.UsBankTransferPayments = usBankTransferPayments
                AccountCapabilities.ZipPayments = zipPayments
            }

    and AccountCapabilityFutureRequirements = {
        ///Fields that are due and can be resolved by providing the corresponding alternative fields instead. Multiple alternatives can reference the same `original_fields_due`. When this happens, any of these alternatives can serve as a pathway for attempting to resolve the fields. Additionally, providing `original_fields_due` again also serves as a pathway for attempting to resolve the fields.
        Alternatives: AccountRequirementsAlternative list option
        ///Date on which `future_requirements` becomes the main `requirements` hash and `future_requirements` becomes empty. After the transition, `currently_due` requirements may immediately become `past_due`, but the account may also be given a grace period depending on the capability's enablement state prior to transitioning.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CurrentDeadline: DateTime option
        ///Fields that need to be resolved to keep the capability enabled. If not resolved by `future_requirements[current_deadline]`, these fields will transition to the main `requirements` hash.
        CurrentlyDue: string list
        ///This is typed as an enum for consistency with `requirements.disabled_reason`, but it safe to assume `future_requirements.disabled_reason` is null because fields in `future_requirements` will never disable the account.
        DisabledReason: AccountCapabilityFutureRequirementsDisabledReason option
        ///Details about validation and verification failures for `due` requirements that must be resolved.
        Errors: AccountRequirementsError list
        ///Fields you must collect when all thresholds are reached. As they become required, they appear in `currently_due` as well.
        EventuallyDue: string list
        ///Fields that haven't been resolved by `requirements.current_deadline`. These fields need to be resolved to enable the capability on the account. `future_requirements.past_due` is a subset of `requirements.past_due`.
        PastDue: string list
        ///Fields that are being reviewed, or might become required depending on the results of a review. If the review fails, these fields can move to `eventually_due`, `currently_due`, `past_due` or `alternatives`. Fields might appear in `eventually_due`, `currently_due`, `past_due` or `alternatives` and in `pending_verification` if one verification fails but another is still pending.
        PendingVerification: string list
    }
    with
        static member New (alternatives: AccountRequirementsAlternative list option, currentDeadline: DateTime option, currentlyDue: string list, disabledReason: AccountCapabilityFutureRequirementsDisabledReason option, errors: AccountRequirementsError list, eventuallyDue: string list, pastDue: string list, pendingVerification: string list) =
            {
                AccountCapabilityFutureRequirements.Alternatives = alternatives //required
                AccountCapabilityFutureRequirements.CurrentDeadline = currentDeadline //required
                AccountCapabilityFutureRequirements.CurrentlyDue = currentlyDue //required
                AccountCapabilityFutureRequirements.DisabledReason = disabledReason //required
                AccountCapabilityFutureRequirements.Errors = errors //required
                AccountCapabilityFutureRequirements.EventuallyDue = eventuallyDue //required
                AccountCapabilityFutureRequirements.PastDue = pastDue //required
                AccountCapabilityFutureRequirements.PendingVerification = pendingVerification //required
            }

    and AccountCapabilityRequirements = {
        ///Fields that are due and can be resolved by providing the corresponding alternative fields instead. Multiple alternatives can reference the same `original_fields_due`. When this happens, any of these alternatives can serve as a pathway for attempting to resolve the fields. Additionally, providing `original_fields_due` again also serves as a pathway for attempting to resolve the fields.
        Alternatives: AccountRequirementsAlternative list option
        ///The date by which all required account information must be both submitted and verified. This includes fields listed in `currently_due` as well as those in `pending_verification`. If any required information is missing or unverified by this date, the account may be disabled. Note that `current_deadline` may change if additional `currently_due` requirements are requested.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CurrentDeadline: DateTime option
        ///Fields that need to be resolved to keep the capability enabled. If not resolved by `current_deadline`, these fields will appear in `past_due` as well, and the capability is disabled.
        CurrentlyDue: string list
        ///Description of why the capability is disabled. [Learn more about handling verification issues](https://docs.stripe.com/connect/handling-api-verification).
        DisabledReason: AccountCapabilityRequirementsDisabledReason option
        ///Details about validation and verification failures for `due` requirements that must be resolved.
        Errors: AccountRequirementsError list
        ///Fields you must collect when all thresholds are reached. As they become required, they appear in `currently_due` as well, and `current_deadline` becomes set.
        EventuallyDue: string list
        ///Fields that haven't been resolved by `current_deadline`. These fields need to be resolved to enable the capability on the account.
        PastDue: string list
        ///Fields that are being reviewed, or might become required depending on the results of a review. If the review fails, these fields can move to `eventually_due`, `currently_due`, `past_due` or `alternatives`. Fields might appear in `eventually_due`, `currently_due`, `past_due` or `alternatives` and in `pending_verification` if one verification fails but another is still pending.
        PendingVerification: string list
    }
    with
        static member New (alternatives: AccountRequirementsAlternative list option, currentDeadline: DateTime option, currentlyDue: string list, disabledReason: AccountCapabilityRequirementsDisabledReason option, errors: AccountRequirementsError list, eventuallyDue: string list, pastDue: string list, pendingVerification: string list) =
            {
                AccountCapabilityRequirements.Alternatives = alternatives //required
                AccountCapabilityRequirements.CurrentDeadline = currentDeadline //required
                AccountCapabilityRequirements.CurrentlyDue = currentlyDue //required
                AccountCapabilityRequirements.DisabledReason = disabledReason //required
                AccountCapabilityRequirements.Errors = errors //required
                AccountCapabilityRequirements.EventuallyDue = eventuallyDue //required
                AccountCapabilityRequirements.PastDue = pastDue //required
                AccountCapabilityRequirements.PendingVerification = pendingVerification //required
            }

    and AccountCardIssuingSettings = {
        TosAcceptance: CardIssuingAccountTermsOfService option
    }
    with
        static member New (?tosAcceptance: CardIssuingAccountTermsOfService) =
            {
                AccountCardIssuingSettings.TosAcceptance = tosAcceptance
            }

    and AccountCardPaymentsSettings = {
        DeclineOn: AccountDeclineChargeOn option
        ///The default text that appears on credit card statements when a charge is made. This field prefixes any dynamic `statement_descriptor` specified on the charge. `statement_descriptor_prefix` is useful for maximizing descriptor space for the dynamic portion.
        StatementDescriptorPrefix: string option
        ///The Kana variation of the default text that appears on credit card statements when a charge is made (Japan only). This field prefixes any dynamic `statement_descriptor_suffix_kana` specified on the charge. `statement_descriptor_prefix_kana` is useful for maximizing descriptor space for the dynamic portion.
        StatementDescriptorPrefixKana: string option
        ///The Kanji variation of the default text that appears on credit card statements when a charge is made (Japan only). This field prefixes any dynamic `statement_descriptor_suffix_kanji` specified on the charge. `statement_descriptor_prefix_kanji` is useful for maximizing descriptor space for the dynamic portion.
        StatementDescriptorPrefixKanji: string option
    }
    with
        static member New (statementDescriptorPrefix: string option, statementDescriptorPrefixKana: string option, statementDescriptorPrefixKanji: string option, ?declineOn: AccountDeclineChargeOn) =
            {
                AccountCardPaymentsSettings.StatementDescriptorPrefix = statementDescriptorPrefix //required
                AccountCardPaymentsSettings.StatementDescriptorPrefixKana = statementDescriptorPrefixKana //required
                AccountCardPaymentsSettings.StatementDescriptorPrefixKanji = statementDescriptorPrefixKanji //required
                AccountCardPaymentsSettings.DeclineOn = declineOn
            }

    and AccountFutureRequirements = {
        ///Fields that are due and can be resolved by providing the corresponding alternative fields instead. Many alternatives can list the same `original_fields_due`, and any of these alternatives can serve as a pathway for attempting to resolve the fields again. Re-providing `original_fields_due` also serves as a pathway for attempting to resolve the fields again.
        Alternatives: AccountRequirementsAlternative list option
        ///Date on which `future_requirements` becomes the main `requirements` hash and `future_requirements` becomes empty. After the transition, `currently_due` requirements may immediately become `past_due`, but the account may also be given a grace period depending on its enablement state prior to transitioning.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CurrentDeadline: DateTime option
        ///Fields that need to be resolved to keep the account enabled. If not resolved by `future_requirements[current_deadline]`, these fields will transition to the main `requirements` hash.
        CurrentlyDue: string list option
        ///This is typed as an enum for consistency with `requirements.disabled_reason`.
        DisabledReason: AccountFutureRequirementsDisabledReason option
        ///Details about validation and verification failures for `due` requirements that must be resolved.
        Errors: AccountRequirementsError list option
        ///Fields you must collect when all thresholds are reached. As they become required, they appear in `currently_due` as well.
        EventuallyDue: string list option
        ///Fields that haven't been resolved by `requirements.current_deadline`. These fields need to be resolved to enable the capability on the account. `future_requirements.past_due` is a subset of `requirements.past_due`.
        PastDue: string list option
        ///Fields that are being reviewed, or might become required depending on the results of a review. If the review fails, these fields can move to `eventually_due`, `currently_due`, `past_due` or `alternatives`. Fields might appear in `eventually_due`, `currently_due`, `past_due` or `alternatives` and in `pending_verification` if one verification fails but another is still pending.
        PendingVerification: string list option
    }
    with
        static member New (alternatives: AccountRequirementsAlternative list option, currentDeadline: DateTime option, currentlyDue: string list option, disabledReason: AccountFutureRequirementsDisabledReason option, errors: AccountRequirementsError list option, eventuallyDue: string list option, pastDue: string list option, pendingVerification: string list option) =
            {
                AccountFutureRequirements.Alternatives = alternatives //required
                AccountFutureRequirements.CurrentDeadline = currentDeadline //required
                AccountFutureRequirements.CurrentlyDue = currentlyDue //required
                AccountFutureRequirements.DisabledReason = disabledReason //required
                AccountFutureRequirements.Errors = errors //required
                AccountFutureRequirements.EventuallyDue = eventuallyDue //required
                AccountFutureRequirements.PastDue = pastDue //required
                AccountFutureRequirements.PendingVerification = pendingVerification //required
            }

    and AccountInvoicesSettings = {
        ///The list of default Account Tax IDs to automatically include on invoices. Account Tax IDs get added when an invoice is finalized.
        DefaultAccountTaxIds: AccountInvoicesSettingsDefaultAccountTaxIds'AnyOf list option
        ///Whether to save the payment method after a payment is completed for a one-time invoice or a subscription invoice when the customer already has a default payment method on the hosted invoice page.
        HostedPaymentMethodSave: AccountInvoicesSettingsHostedPaymentMethodSave option
    }
    with
        static member New (defaultAccountTaxIds: AccountInvoicesSettingsDefaultAccountTaxIds'AnyOf list option, hostedPaymentMethodSave: AccountInvoicesSettingsHostedPaymentMethodSave option) =
            {
                AccountInvoicesSettings.DefaultAccountTaxIds = defaultAccountTaxIds //required
                AccountInvoicesSettings.HostedPaymentMethodSave = hostedPaymentMethodSave //required
            }

    and AccountInvoicesSettingsDefaultAccountTaxIds'AnyOf =
        | String of string
        | TaxId of TaxId

    and AccountMonthlyEstimatedRevenue = {
        ///A non-negative integer representing how much to charge in the [smallest currency unit](/currencies#zero-decimal).
        Amount: int
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
    }
    with
        static member New (amount: int, currency: IsoTypes.IsoCurrencyCode) =
            {
                AccountMonthlyEstimatedRevenue.Amount = amount //required
                AccountMonthlyEstimatedRevenue.Currency = currency //required
            }

    and AccountPayoutSettings = {
        ///A Boolean indicating if Stripe should try to reclaim negative balances from an attached bank account. See [Understanding Connect account balances](/connect/account-balances) for details. The default value is `false` when [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts, otherwise `true`.
        DebitNegativeBalances: bool
        Schedule: TransferSchedule
        ///The text that appears on the bank account statement for payouts. If not set, this defaults to the platform's bank descriptor as set in the Dashboard.
        StatementDescriptor: string option
    }
    with
        static member New (debitNegativeBalances: bool, schedule: TransferSchedule, statementDescriptor: string option) =
            {
                AccountPayoutSettings.DebitNegativeBalances = debitNegativeBalances //required
                AccountPayoutSettings.Schedule = schedule //required
                AccountPayoutSettings.StatementDescriptor = statementDescriptor //required
            }

    and AccountRequirements = {
        ///Fields that are due and can be resolved by providing the corresponding alternative fields instead. Many alternatives can list the same `original_fields_due`, and any of these alternatives can serve as a pathway for attempting to resolve the fields again. Re-providing `original_fields_due` also serves as a pathway for attempting to resolve the fields again.
        Alternatives: AccountRequirementsAlternative list option
        ///Date by which the fields in `currently_due` must be collected to keep the account enabled. These fields may disable the account sooner if the next threshold is reached before they are collected.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CurrentDeadline: DateTime option
        ///Fields that need to be resolved to keep the account enabled. If not resolved by `current_deadline`, these fields will appear in `past_due` as well, and the account is disabled.
        CurrentlyDue: string list option
        ///If the account is disabled, this enum describes why. [Learn more about handling verification issues](https://docs.stripe.com/connect/handling-api-verification).
        DisabledReason: AccountRequirementsDisabledReason option
        ///Details about validation and verification failures for `due` requirements that must be resolved.
        Errors: AccountRequirementsError list option
        ///Fields you must collect when all thresholds are reached. As they become required, they appear in `currently_due` as well, and `current_deadline` becomes set.
        EventuallyDue: string list option
        ///Fields that haven't been resolved by `current_deadline`. These fields need to be resolved to enable the account.
        PastDue: string list option
        ///Fields that are being reviewed, or might become required depending on the results of a review. If the review fails, these fields can move to `eventually_due`, `currently_due`, `past_due` or `alternatives`. Fields might appear in `eventually_due`, `currently_due`, `past_due` or `alternatives` and in `pending_verification` if one verification fails but another is still pending.
        PendingVerification: string list option
    }
    with
        static member New (alternatives: AccountRequirementsAlternative list option, currentDeadline: DateTime option, currentlyDue: string list option, disabledReason: AccountRequirementsDisabledReason option, errors: AccountRequirementsError list option, eventuallyDue: string list option, pastDue: string list option, pendingVerification: string list option) =
            {
                AccountRequirements.Alternatives = alternatives //required
                AccountRequirements.CurrentDeadline = currentDeadline //required
                AccountRequirements.CurrentlyDue = currentlyDue //required
                AccountRequirements.DisabledReason = disabledReason //required
                AccountRequirements.Errors = errors //required
                AccountRequirements.EventuallyDue = eventuallyDue //required
                AccountRequirements.PastDue = pastDue //required
                AccountRequirements.PendingVerification = pendingVerification //required
            }

    and AccountRequirementsAlternative = {
        ///Fields that can be provided to resolve all fields in `original_fields_due`.
        AlternativeFieldsDue: string list
        ///Fields that are due and can be resolved by providing all fields in `alternative_fields_due`.
        OriginalFieldsDue: string list
    }
    with
        static member New (alternativeFieldsDue: string list, originalFieldsDue: string list) =
            {
                AccountRequirementsAlternative.AlternativeFieldsDue = alternativeFieldsDue //required
                AccountRequirementsAlternative.OriginalFieldsDue = originalFieldsDue //required
            }

    and AccountRequirementsError = {
        ///The code for the type of error.
        Code: AccountRequirementsErrorCode
        ///An informative message that indicates the error type and provides additional details about the error.
        Reason: string
        ///The specific user onboarding requirement field (in the requirements hash) that needs to be resolved.
        Requirement: string
    }
    with
        static member New (code: AccountRequirementsErrorCode, reason: string, requirement: string) =
            {
                AccountRequirementsError.Code = code //required
                AccountRequirementsError.Reason = reason //required
                AccountRequirementsError.Requirement = requirement //required
            }

    ///An AccountSession allows a Connect platform to grant access to a connected account in Connect embedded components.
    ///We recommend that you create an AccountSession each time you need to display an embedded component
    ///to your user. Do not save AccountSessions to your database as they expire relatively
    ///quickly, and cannot be used more than once.
    ///Related guide: [Connect embedded components](https://docs.stripe.com/connect/get-started-connect-embedded-components)
    and AccountSession = {
        ///The ID of the account the AccountSession was created for
        Account: string
        ///The client secret of this AccountSession. Used on the client to set up secure access to the given `account`.
        ///The client secret can be used to provide access to `account` from your frontend. It should not be stored, logged, or exposed to anyone other than the connected account. Make sure that you have TLS enabled on any page that includes the client secret.
        ///Refer to our docs to [setup Connect embedded components](https://docs.stripe.com/connect/get-started-connect-embedded-components) and learn about how `client_secret` should be handled.
        ClientSecret: string
        Components: ConnectEmbeddedAccountSessionCreateComponents
        ///The timestamp at which this AccountSession will expire.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "account_session"

        static member New (account: string, clientSecret: string, components: ConnectEmbeddedAccountSessionCreateComponents, expiresAt: DateTime, livemode: bool) =
            {
                AccountSession.Account = account //required
                AccountSession.ClientSecret = clientSecret //required
                AccountSession.Components = components //required
                AccountSession.ExpiresAt = expiresAt //required
                AccountSession.Livemode = livemode //required
            }

    and AccountSettings = {
        BacsDebitPayments: AccountBacsDebitPaymentsSettings option
        Branding: AccountBrandingSettings
        CardIssuing: AccountCardIssuingSettings option
        CardPayments: AccountCardPaymentsSettings
        Dashboard: AccountDashboardSettings
        Invoices: AccountInvoicesSettings option
        Payments: AccountPaymentsSettings
        Payouts: AccountPayoutSettings option
        SepaDebitPayments: AccountSepaDebitPaymentsSettings option
        Treasury: AccountTreasurySettings option
    }
    with
        static member New (branding: AccountBrandingSettings, cardPayments: AccountCardPaymentsSettings, dashboard: AccountDashboardSettings, payments: AccountPaymentsSettings, ?bacsDebitPayments: AccountBacsDebitPaymentsSettings, ?cardIssuing: AccountCardIssuingSettings, ?invoices: AccountInvoicesSettings, ?payouts: AccountPayoutSettings, ?sepaDebitPayments: AccountSepaDebitPaymentsSettings, ?treasury: AccountTreasurySettings) =
            {
                AccountSettings.Branding = branding //required
                AccountSettings.CardPayments = cardPayments //required
                AccountSettings.Dashboard = dashboard //required
                AccountSettings.Payments = payments //required
                AccountSettings.BacsDebitPayments = bacsDebitPayments
                AccountSettings.CardIssuing = cardIssuing
                AccountSettings.Invoices = invoices
                AccountSettings.Payouts = payouts
                AccountSettings.SepaDebitPayments = sepaDebitPayments
                AccountSettings.Treasury = treasury
            }

    and AccountTreasurySettings = {
        TosAcceptance: AccountTermsOfService option
    }
    with
        static member New (?tosAcceptance: AccountTermsOfService) =
            {
                AccountTreasurySettings.TosAcceptance = tosAcceptance
            }

    and AccountUnificationAccountController = {
        Fees: AccountUnificationAccountControllerFees option
        ///`true` if the Connect application retrieving the resource controls the account and can therefore exercise [platform controls](https://docs.stripe.com/connect/platform-controls-for-standard-accounts). Otherwise, this field is null.
        IsController: bool option
        Losses: AccountUnificationAccountControllerLosses option
        ///A value indicating responsibility for collecting requirements on this account. Only returned when the Connect application retrieving the resource controls the account.
        RequirementCollection: AccountUnificationAccountControllerRequirementCollection option
        StripeDashboard: AccountUnificationAccountControllerStripeDashboard option
        ///The controller type. Can be `application`, if a Connect application controls the account, or `account`, if the account controls itself.
        Type: AccountUnificationAccountControllerType
    }
    with
        static member New (``type``: AccountUnificationAccountControllerType, ?fees: AccountUnificationAccountControllerFees, ?isController: bool, ?losses: AccountUnificationAccountControllerLosses, ?requirementCollection: AccountUnificationAccountControllerRequirementCollection, ?stripeDashboard: AccountUnificationAccountControllerStripeDashboard) =
            {
                AccountUnificationAccountController.Type = ``type`` //required
                AccountUnificationAccountController.Fees = fees
                AccountUnificationAccountController.IsController = isController
                AccountUnificationAccountController.Losses = losses
                AccountUnificationAccountController.RequirementCollection = requirementCollection
                AccountUnificationAccountController.StripeDashboard = stripeDashboard
            }

    and AccountUnificationAccountControllerFees = {
        ///A value indicating the responsible payer of a bundle of Stripe fees for pricing-control eligible products on this account. Learn more about [fee behavior on connected accounts](https://docs.stripe.com/connect/direct-charges-fee-payer-behavior).
        Payer: AccountUnificationAccountControllerFeesPayer
    }
    with
        static member New (payer: AccountUnificationAccountControllerFeesPayer) =
            {
                AccountUnificationAccountControllerFees.Payer = payer //required
            }

    and AccountUnificationAccountControllerLosses = {
        ///A value indicating who is liable when this account can't pay back negative balances from payments.
        Payments: AccountUnificationAccountControllerLossesPayments
    }
    with
        static member New (payments: AccountUnificationAccountControllerLossesPayments) =
            {
                AccountUnificationAccountControllerLosses.Payments = payments //required
            }

    and AccountUnificationAccountControllerStripeDashboard = {
        ///A value indicating the Stripe dashboard this account has access to independent of the Connect application.
        Type: AccountUnificationAccountControllerStripeDashboardType
    }
    with
        static member New (``type``: AccountUnificationAccountControllerStripeDashboardType) =
            {
                AccountUnificationAccountControllerStripeDashboard.Type = ``type`` //required
            }

    and Address = {
        ///City, district, suburb, town, or village.
        City: string option
        ///Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
        Country: IsoTypes.IsoCountryCode option
        ///Address line 1, such as the street, PO Box, or company name.
        [<JsonField(Name="line1")>]Line1: string option
        ///Address line 2, such as the apartment, suite, unit, or building.
        [<JsonField(Name="line2")>]Line2: string option
        ///ZIP or postal code.
        PostalCode: string option
        ///State, county, province, or region ([ISO 3166-2](https://en.wikipedia.org/wiki/ISO_3166-2)).
        State: string option
    }
    with
        static member New (city: string option, country: IsoTypes.IsoCountryCode option, line1: string option, line2: string option, postalCode: string option, state: string option) =
            {
                Address.City = city //required
                Address.Country = country //required
                Address.Line1 = line1 //required
                Address.Line2 = line2 //required
                Address.PostalCode = postalCode //required
                Address.State = state //required
            }

    and AmazonPayUnderlyingPaymentMethodFundingDetails = {
        Card: PaymentMethodDetailsPassthroughCard option
    }
    with
        ///funding type of the underlying payment method.
        member _.Type = "card"

        static member New (?card: PaymentMethodDetailsPassthroughCard) =
            {
                AmazonPayUnderlyingPaymentMethodFundingDetails.Card = card
            }

    and ApiErrors = {
        ///For card errors resulting from a card issuer decline, a short string indicating [how to proceed with an error](https://docs.stripe.com/declines#retrying-issuer-declines) if they provide one.
        AdviceCode: string option
        ///For card errors, the ID of the failed charge.
        Charge: string option
        ///For some errors that could be handled programmatically, a short string indicating the [error code](https://docs.stripe.com/error-codes) reported.
        Code: ApiErrorsCode option
        ///For card errors resulting from a card issuer decline, a short string indicating the [card issuer's reason for the decline](https://docs.stripe.com/declines#issuer-declines) if they provide one.
        DeclineCode: string option
        ///A URL to more information about the [error code](https://docs.stripe.com/error-codes) reported.
        DocUrl: string option
        ///A human-readable message providing more details about the error. For card errors, these messages can be shown to your users.
        Message: string option
        ///For card errors resulting from a card issuer decline, a 2 digit code which indicates the advice given to merchant by the card network on how to proceed with an error.
        NetworkAdviceCode: string option
        ///For payments declined by the network, an alphanumeric code which indicates the reason the payment failed.
        NetworkDeclineCode: string option
        ///If the error is parameter-specific, the parameter related to the error. For example, you can use this to display a message near the correct form field.
        Param: string option
        PaymentIntent: PaymentIntent option
        PaymentMethod: PaymentMethod option
        ///If the error is specific to the type of payment method, the payment method type that had a problem. This field is only populated for invoice-related errors.
        PaymentMethodType: string option
        ///A URL to the request log entry in your dashboard.
        RequestLogUrl: string option
        SetupIntent: SetupIntent option
        Source: PaymentSource option
        ///The type of error returned. One of `api_error`, `card_error`, `idempotency_error`, or `invalid_request_error`
        Type: ApiErrorsType
    }
    with
        static member New (``type``: ApiErrorsType, ?adviceCode: string, ?charge: string, ?code: ApiErrorsCode, ?declineCode: string, ?docUrl: string, ?message: string, ?networkAdviceCode: string, ?networkDeclineCode: string, ?param: string, ?paymentIntent: PaymentIntent, ?paymentMethod: PaymentMethod, ?paymentMethodType: string, ?requestLogUrl: string, ?setupIntent: SetupIntent, ?source: PaymentSource) =
            {
                ApiErrors.Type = ``type`` //required
                ApiErrors.AdviceCode = adviceCode
                ApiErrors.Charge = charge
                ApiErrors.Code = code
                ApiErrors.DeclineCode = declineCode
                ApiErrors.DocUrl = docUrl
                ApiErrors.Message = message
                ApiErrors.NetworkAdviceCode = networkAdviceCode
                ApiErrors.NetworkDeclineCode = networkDeclineCode
                ApiErrors.Param = param
                ApiErrors.PaymentIntent = paymentIntent
                ApiErrors.PaymentMethod = paymentMethod
                ApiErrors.PaymentMethodType = paymentMethodType
                ApiErrors.RequestLogUrl = requestLogUrl
                ApiErrors.SetupIntent = setupIntent
                ApiErrors.Source = source
            }

    and ApplicationFee = {
        ///ID of the Stripe account this fee was taken from.
        Account: ApplicationFeeAccount'AnyOf
        ///Amount earned, in cents (or local equivalent).
        Amount: int
        ///Amount in cents (or local equivalent) refunded (can be less than the amount attribute on the fee if a partial refund was issued)
        AmountRefunded: int
        ///ID of the Connect application that earned the fee.
        Application: ApplicationFeeApplication'AnyOf
        ///Balance transaction that describes the impact of this collected application fee on your account balance (not including refunds).
        BalanceTransaction: ApplicationFeeBalanceTransaction'AnyOf option
        ///ID of the charge that the application fee was taken from.
        Charge: ApplicationFeeCharge'AnyOf
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///Polymorphic source of the application fee. Includes the ID of the object the application fee was created from.
        FeeSource: PlatformEarningFeeSource option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///ID of the corresponding charge on the platform account, if this fee was the result of a charge using the `destination` parameter.
        OriginatingTransaction: ApplicationFeeOriginatingTransaction'AnyOf option
        ///Whether the fee has been fully refunded. If the fee is only partially refunded, this attribute will still be false.
        Refunded: bool
        ///A list of refunds that have been applied to the fee.
        Refunds: ApplicationFeeRefunds
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "application_fee"

        static member New (account: ApplicationFeeAccount'AnyOf, amount: int, amountRefunded: int, application: ApplicationFeeApplication'AnyOf, balanceTransaction: ApplicationFeeBalanceTransaction'AnyOf option, charge: ApplicationFeeCharge'AnyOf, created: DateTime, currency: IsoTypes.IsoCurrencyCode, feeSource: PlatformEarningFeeSource option, id: string, livemode: bool, originatingTransaction: ApplicationFeeOriginatingTransaction'AnyOf option, refunded: bool, refunds: ApplicationFeeRefunds) =
            {
                ApplicationFee.Account = account //required
                ApplicationFee.Amount = amount //required
                ApplicationFee.AmountRefunded = amountRefunded //required
                ApplicationFee.Application = application //required
                ApplicationFee.BalanceTransaction = balanceTransaction //required
                ApplicationFee.Charge = charge //required
                ApplicationFee.Created = created //required
                ApplicationFee.Currency = currency //required
                ApplicationFee.FeeSource = feeSource //required
                ApplicationFee.Id = id //required
                ApplicationFee.Livemode = livemode //required
                ApplicationFee.OriginatingTransaction = originatingTransaction //required
                ApplicationFee.Refunded = refunded //required
                ApplicationFee.Refunds = refunds //required
            }

    and ApplicationFeeAccount'AnyOf =
        | String of string
        | Account of Account

    and ApplicationFeeApplication'AnyOf =
        | String of string
        | Application of Application

    and ApplicationFeeBalanceTransaction'AnyOf =
        | String of string
        | BalanceTransaction of BalanceTransaction

    and ApplicationFeeCharge'AnyOf =
        | String of string
        | Charge of Charge

    and ApplicationFeeOriginatingTransaction'AnyOf =
        | String of string
        | Charge of Charge

    ///A list of refunds that have been applied to the fee.
    and ApplicationFeeRefunds = {
        ///Details about each object.
        Data: FeeRefund list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: FeeRefund list, hasMore: bool, url: string) =
            {
                ApplicationFeeRefunds.Data = data //required
                ApplicationFeeRefunds.HasMore = hasMore //required
                ApplicationFeeRefunds.Url = url //required
            }

    ///Secret Store is an API that allows Stripe Apps developers to securely persist secrets for use by UI Extensions and app backends.
    ///The primary resource in Secret Store is a `secret`. Other apps can't view secrets created by an app. Additionally, secrets are scoped to provide further permission control.
    ///All Dashboard users and the app backend share `account` scoped secrets. Use the `account` scope for secrets that don't change per-user, like a third-party API key.
    ///A `user` scoped secret is accessible by the app backend and one specific Dashboard user. Use the `user` scope for per-user secrets like per-user OAuth tokens, where different users might have different permissions.
    ///Related guide: [Store data between page reloads](https://docs.stripe.com/stripe-apps/store-auth-data-custom-objects)
    and AppsSecret = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///If true, indicates that this secret has been deleted
        Deleted: bool option
        ///The Unix timestamp for the expiry time of the secret, after which the secret deletes.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///A name for the secret that's unique within the scope.
        Name: string
        ///The plaintext secret value to be stored.
        Payload: string option
        Scope: SecretServiceResourceScope
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "apps.secret"

        static member New (created: DateTime, expiresAt: DateTime option, id: string, livemode: bool, name: string, scope: SecretServiceResourceScope, ?deleted: bool, ?payload: string option) =
            {
                AppsSecret.Created = created //required
                AppsSecret.ExpiresAt = expiresAt //required
                AppsSecret.Id = id //required
                AppsSecret.Livemode = livemode //required
                AppsSecret.Name = name //required
                AppsSecret.Scope = scope //required
                AppsSecret.Deleted = deleted
                AppsSecret.Payload = payload |> Option.flatten
            }

    and AutomaticTax = {
        ///If Stripe disabled automatic tax, this enum describes why.
        DisabledReason: AutomaticTaxDisabledReason option
        ///Whether Stripe automatically computes tax on this invoice. Note that incompatible invoice items (invoice items with manually specified [tax rates](https://docs.stripe.com/api/tax_rates), negative amounts, or `tax_behavior=unspecified`) cannot be added to automatic tax invoices.
        Enabled: bool
        ///The account that's liable for tax. If set, the business address and tax registrations required to perform the tax calculation are loaded from this account. The tax transaction is returned in the report of the connected account.
        Liability: ConnectAccountReference option
        ///The tax provider powering automatic tax.
        Provider: string option
        ///The status of the most recent automated tax calculation for this invoice.
        Status: AutomaticTaxStatus option
    }
    with
        static member New (disabledReason: AutomaticTaxDisabledReason option, enabled: bool, liability: ConnectAccountReference option, provider: string option, status: AutomaticTaxStatus option) =
            {
                AutomaticTax.DisabledReason = disabledReason //required
                AutomaticTax.Enabled = enabled //required
                AutomaticTax.Liability = liability //required
                AutomaticTax.Provider = provider //required
                AutomaticTax.Status = status //required
            }

    ///This is an object representing your Stripe balance. You can retrieve it to see
    ///the balance currently on your Stripe account.
    ///The top-level `available` and `pending` comprise your "payments balance."
    ///Related guide: [Balances and settlement time](https://docs.stripe.com/payments/balances), [Understanding Connect account balances](https://docs.stripe.com/connect/account-balances)
    and Balance = {
        ///Available funds that you can transfer or pay out automatically by Stripe or explicitly through the [Transfers API](https://api.stripe.com#transfers) or [Payouts API](https://api.stripe.com#payouts). You can find the available balance for each currency and payment type in the `source_types` property.
        Available: BalanceAmount list
        ///Funds held due to negative balances on connected accounts where [account.controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts. You can find the connect reserve balance for each currency and payment type in the `source_types` property.
        ConnectReserved: BalanceAmount list option
        ///Funds that you can pay out using Instant Payouts.
        InstantAvailable: BalanceAmountNet list option
        Issuing: BalanceDetail option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Funds that aren't available in the balance yet. You can find the pending balance for each currency and each payment type in the `source_types` property.
        Pending: BalanceAmount list
        RefundAndDisputePrefunding: BalanceDetailUngated option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "balance"

        static member New (available: BalanceAmount list, livemode: bool, pending: BalanceAmount list, ?connectReserved: BalanceAmount list, ?instantAvailable: BalanceAmountNet list, ?issuing: BalanceDetail, ?refundAndDisputePrefunding: BalanceDetailUngated) =
            {
                Balance.Available = available //required
                Balance.Livemode = livemode //required
                Balance.Pending = pending //required
                Balance.ConnectReserved = connectReserved
                Balance.InstantAvailable = instantAvailable
                Balance.Issuing = issuing
                Balance.RefundAndDisputePrefunding = refundAndDisputePrefunding
            }

    and BalanceAmount = {
        ///Balance amount.
        Amount: int
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        SourceTypes: BalanceAmountBySourceType option
    }
    with
        static member New (amount: int, currency: IsoTypes.IsoCurrencyCode, ?sourceTypes: BalanceAmountBySourceType) =
            {
                BalanceAmount.Amount = amount //required
                BalanceAmount.Currency = currency //required
                BalanceAmount.SourceTypes = sourceTypes
            }

    and BalanceAmountNet = {
        ///Balance amount.
        Amount: int
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///Breakdown of balance by destination.
        NetAvailable: BalanceNetAvailable list option
        SourceTypes: BalanceAmountBySourceType option
    }
    with
        static member New (amount: int, currency: IsoTypes.IsoCurrencyCode, ?netAvailable: BalanceNetAvailable list, ?sourceTypes: BalanceAmountBySourceType) =
            {
                BalanceAmountNet.Amount = amount //required
                BalanceAmountNet.Currency = currency //required
                BalanceAmountNet.NetAvailable = netAvailable
                BalanceAmountNet.SourceTypes = sourceTypes
            }

    and BalanceDetail = {
        ///Funds that are available for use.
        Available: BalanceAmount list
    }
    with
        static member New (available: BalanceAmount list) =
            {
                BalanceDetail.Available = available //required
            }

    and BalanceDetailUngated = {
        ///Funds that are available for use.
        Available: BalanceAmount list
        ///Funds that are pending
        Pending: BalanceAmount list
    }
    with
        static member New (available: BalanceAmount list, pending: BalanceAmount list) =
            {
                BalanceDetailUngated.Available = available //required
                BalanceDetailUngated.Pending = pending //required
            }

    and BalanceNetAvailable = {
        ///Net balance amount, subtracting fees from platform-set pricing.
        Amount: int
        ///ID of the external account for this net balance (not expandable).
        Destination: string
        SourceTypes: BalanceAmountBySourceType option
    }
    with
        static member New (amount: int, destination: string, ?sourceTypes: BalanceAmountBySourceType) =
            {
                BalanceNetAvailable.Amount = amount //required
                BalanceNetAvailable.Destination = destination //required
                BalanceNetAvailable.SourceTypes = sourceTypes
            }

    ///Options for customizing account balances and payout settings for a Stripe platform’s connected accounts.
    and BalanceSettings = {
        Payments: BalanceSettingsResourcePayments
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "balance_settings"

        static member New (payments: BalanceSettingsResourcePayments) =
            {
                BalanceSettings.Payments = payments //required
            }

    and BalanceSettingsResourcePayments = {
        ///A Boolean indicating if Stripe should try to reclaim negative balances from an attached bank account. See [Understanding Connect account balances](/connect/account-balances) for details. The default value is `false` when [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts, otherwise `true`.
        DebitNegativeBalances: bool option
        ///Settings specific to the account's payouts.
        Payouts: BalanceSettingsResourcePayouts option
        SettlementTiming: BalanceSettingsResourceSettlementTiming
    }
    with
        static member New (debitNegativeBalances: bool option, payouts: BalanceSettingsResourcePayouts option, settlementTiming: BalanceSettingsResourceSettlementTiming) =
            {
                BalanceSettingsResourcePayments.DebitNegativeBalances = debitNegativeBalances //required
                BalanceSettingsResourcePayments.Payouts = payouts //required
                BalanceSettingsResourcePayments.SettlementTiming = settlementTiming //required
            }

    and BalanceSettingsResourcePayoutSchedule = {
        ///How frequently funds will be paid out. One of `manual` (payouts only created via API call), `daily`, `weekly`, or `monthly`.
        Interval: BalanceSettingsResourcePayoutScheduleInterval option
        ///The day of the month funds will be paid out. Only shown if `interval` is monthly. Payouts scheduled between the 29th and 31st of the month are sent on the last day of shorter months.
        MonthlyPayoutDays: int list option
        ///The days of the week when available funds are paid out, specified as an array, for example, [`monday`, `tuesday`]. Only shown if `interval` is weekly.
        WeeklyPayoutDays: BalanceSettingsResourcePayoutScheduleWeeklyPayoutDays list option
    }
    with
        static member New (interval: BalanceSettingsResourcePayoutScheduleInterval option, ?monthlyPayoutDays: int list, ?weeklyPayoutDays: BalanceSettingsResourcePayoutScheduleWeeklyPayoutDays list) =
            {
                BalanceSettingsResourcePayoutSchedule.Interval = interval //required
                BalanceSettingsResourcePayoutSchedule.MonthlyPayoutDays = monthlyPayoutDays
                BalanceSettingsResourcePayoutSchedule.WeeklyPayoutDays = weeklyPayoutDays
            }

    and BalanceSettingsResourcePayouts = {
        ///The minimum balance amount to retain per currency after automatic payouts. Only funds that exceed these amounts are paid out. Learn more about the [minimum balances for automatic payouts](/payouts/minimum-balances-for-automatic-payouts).
        MinimumBalanceByCurrency: Map<string, string list> option
        ///Details on when funds from charges are available, and when they are paid out to an external account. See our [Setting Bank and Debit Card Payouts](https://docs.stripe.com/connect/bank-transfers#payout-information) documentation for details.
        Schedule: BalanceSettingsResourcePayoutSchedule option
        ///The text that appears on the bank account statement for payouts. If not set, this defaults to the platform's bank descriptor as set in the Dashboard.
        StatementDescriptor: string option
        ///Whether the funds in this account can be paid out.
        Status: BalanceSettingsResourcePayoutsStatus
    }
    with
        static member New (minimumBalanceByCurrency: Map<string, string list> option, schedule: BalanceSettingsResourcePayoutSchedule option, statementDescriptor: string option, status: BalanceSettingsResourcePayoutsStatus) =
            {
                BalanceSettingsResourcePayouts.MinimumBalanceByCurrency = minimumBalanceByCurrency //required
                BalanceSettingsResourcePayouts.Schedule = schedule //required
                BalanceSettingsResourcePayouts.StatementDescriptor = statementDescriptor //required
                BalanceSettingsResourcePayouts.Status = status //required
            }

    ///Balance transactions represent funds moving through your Stripe account.
    ///Stripe creates them for every type of transaction that enters or leaves your Stripe account balance.
    ///Related guide: [Balance transaction types](https://docs.stripe.com/reports/balance-transaction-types)
    and BalanceTransaction = {
        ///Gross amount of this transaction (in cents (or local equivalent)). A positive value represents funds charged to another party, and a negative value represents funds sent to another party.
        Amount: int
        ///The date that the transaction's net funds become available in the Stripe balance.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]AvailableOn: DateTime
        ///The balance that this transaction impacts.
        BalanceType: BalanceTransactionBalanceType
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///If applicable, this transaction uses an exchange rate. If money converts from currency A to currency B, then the `amount` in currency A, multipled by the `exchange_rate`, equals the `amount` in currency B. For example, if you charge a customer 10.00 EUR, the PaymentIntent's `amount` is `1000` and `currency` is `eur`. If this converts to 12.34 USD in your Stripe account, the BalanceTransaction's `amount` is `1234`, its `currency` is `usd`, and the `exchange_rate` is `1.234`.
        ExchangeRate: decimal option
        ///Fees (in cents (or local equivalent)) paid for this transaction. Represented as a positive integer when assessed.
        Fee: int
        ///Detailed breakdown of fees (in cents (or local equivalent)) paid for this transaction.
        FeeDetails: Fee list
        ///Unique identifier for the object.
        Id: string
        ///Net impact to a Stripe balance (in cents (or local equivalent)). A positive value represents incrementing a Stripe balance, and a negative value decrementing a Stripe balance. You can calculate the net impact of a transaction on a balance by `amount` - `fee`
        Net: int
        ///Learn more about how [reporting categories](https://stripe.com/docs/reports/reporting-categories) can help you understand balance transactions from an accounting perspective.
        ReportingCategory: string
        ///This transaction relates to the Stripe object.
        Source: BalanceTransactionSource'AnyOf option
        ///The transaction's net funds status in the Stripe balance, which are either `available` or `pending`.
        Status: BalanceTransactionStatus
        ///Transaction type: `adjustment`, `advance`, `advance_funding`, `anticipation_repayment`, `application_fee`, `application_fee_refund`, `charge`, `climate_order_purchase`, `climate_order_refund`, `connect_collection_transfer`, `contribution`, `inbound_transfer`, `inbound_transfer_reversal`, `issuing_authorization_hold`, `issuing_authorization_release`, `issuing_dispute`, `issuing_transaction`, `obligation_outbound`, `obligation_reversal_inbound`, `payment`, `payment_failure_refund`, `payment_network_reserve_hold`, `payment_network_reserve_release`, `payment_refund`, `payment_reversal`, `payment_unreconciled`, `payout`, `payout_cancel`, `payout_failure`, `payout_minimum_balance_hold`, `payout_minimum_balance_release`, `refund`, `refund_failure`, `reserve_transaction`, `reserved_funds`, `reserve_hold`, `reserve_release`, `stripe_fee`, `stripe_fx_fee`, `stripe_balance_payment_debit`, `stripe_balance_payment_debit_reversal`, `tax_fee`, `topup`, `topup_reversal`, `transfer`, `transfer_cancel`, `transfer_failure`, `transfer_refund`, or `fee_credit_funding`. Learn more about [balance transaction types and what they represent](https://stripe.com/docs/reports/balance-transaction-types). To classify transactions for accounting purposes, consider `reporting_category` instead.
        Type: BalanceTransactionType
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "balance_transaction"

        static member New (amount: int, availableOn: DateTime, balanceType: BalanceTransactionBalanceType, created: DateTime, currency: IsoTypes.IsoCurrencyCode, description: string option, exchangeRate: decimal option, fee: int, feeDetails: Fee list, id: string, net: int, reportingCategory: string, source: BalanceTransactionSource'AnyOf option, status: BalanceTransactionStatus, ``type``: BalanceTransactionType) =
            {
                BalanceTransaction.Amount = amount //required
                BalanceTransaction.AvailableOn = availableOn //required
                BalanceTransaction.BalanceType = balanceType //required
                BalanceTransaction.Created = created //required
                BalanceTransaction.Currency = currency //required
                BalanceTransaction.Description = description //required
                BalanceTransaction.ExchangeRate = exchangeRate //required
                BalanceTransaction.Fee = fee //required
                BalanceTransaction.FeeDetails = feeDetails //required
                BalanceTransaction.Id = id //required
                BalanceTransaction.Net = net //required
                BalanceTransaction.ReportingCategory = reportingCategory //required
                BalanceTransaction.Source = source //required
                BalanceTransaction.Status = status //required
                BalanceTransaction.Type = ``type`` //required
            }

    and BalanceTransactionSource'AnyOf =
        | String of string
        | BalanceTransactionSource of BalanceTransactionSource

    and BalanceTransactionSource =
        | ApplicationFee of ApplicationFee
        | Charge of Charge
        | ConnectCollectionTransfer of ConnectCollectionTransfer
        | CustomerCashBalanceTransaction of CustomerCashBalanceTransaction
        | Dispute of Dispute
        | FeeRefund of FeeRefund
        | IssuingAuthorization of IssuingAuthorization
        | IssuingDispute of IssuingDispute
        | IssuingTransaction of IssuingTransaction
        | Payout of Payout
        | Refund of Refund
        | ReserveTransaction of ReserveTransaction
        | TaxDeductedAtSource of TaxDeductedAtSource
        | Topup of Topup
        | Transfer of Transfer
        | TransferReversal of TransferReversal

    ///These bank accounts are payment methods on `Customer` objects.
    ///On the other hand [External Accounts](/api#external_accounts) are transfer
    ///destinations on `Account` objects for connected accounts.
    ///They can be bank accounts or debit cards as well, and are documented in the links above.
    ///Related guide: [Bank debits and transfers](/payments/bank-debits-transfers)
    and BankAccount = {
        ///The account this bank account belongs to. Only applicable on Accounts (not customers or recipients) This property is only available when returned as an [External Account](/api/external_account_bank_accounts/object) where [controller.is_controller](/api/accounts/object#account_object-controller-is_controller) is `true`.
        Account: BankAccountAccount'AnyOf option
        ///The name of the person or business that owns the bank account.
        AccountHolderName: string option
        ///The type of entity that holds the account. This can be either `individual` or `company`.
        AccountHolderType: BankAccountAccountHolderType option
        ///The bank account type. This can only be `checking` or `savings` in most countries. In Japan, this can only be `futsu` or `toza`.
        AccountType: BankAccountAccountType option
        ///A set of available payout methods for this bank account. Only values from this set should be passed as the `method` when creating a payout.
        AvailablePayoutMethods: BankAccountAvailablePayoutMethods list option
        ///Name of the bank associated with the routing number (e.g., `WELLS FARGO`).
        BankName: string option
        ///Two-letter ISO code representing the country the bank account is located in.
        Country: IsoTypes.IsoCountryCode
        ///Three-letter [ISO code for the currency](https://stripe.com/docs/payouts) paid out to the bank account.
        Currency: IsoTypes.IsoCurrencyCode
        ///The ID of the customer that the bank account is associated with.
        Customer: BankAccountCustomer'AnyOf option
        ///Whether this bank account is the default external account for its currency.
        DefaultForCurrency: bool option
        ///Uniquely identifies this particular bank account. You can use this attribute to check whether two bank accounts are the same.
        Fingerprint: string option
        ///Information about the [upcoming new requirements for the bank account](https://docs.stripe.com/connect/custom-accounts/future-requirements), including what information needs to be collected, and by when.
        FutureRequirements: ExternalAccountRequirements option
        ///Unique identifier for the object.
        Id: string
        ///The last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///Information about the requirements for the bank account, including what information needs to be collected.
        Requirements: ExternalAccountRequirements option
        ///The routing transit number for the bank account.
        RoutingNumber: string option
        ///For bank accounts, possible values are `new`, `validated`, `verified`, `verification_failed`, `tokenized_account_number_deactivated` or `errored`. A bank account that hasn't had any activity or validation performed is `new`. If Stripe can determine that the bank account exists, its status will be `validated`. Note that there often isn’t enough information to know (e.g., for smaller credit unions), and the validation is not always run. If customer bank account verification has succeeded, the bank account status will be `verified`. If the verification failed for any reason, such as microdeposit failure, the status will be `verification_failed`. If the status is `tokenized_account_number_deactivated`, the account utilizes a tokenized account number which has been deactivated due to expiration or revocation. This account will need to be reverified to continue using it for money movement. If a payout sent to this bank account fails, we'll set the status to `errored` and will not continue to send [scheduled payouts](https://stripe.com/docs/payouts#payout-schedule) until the bank details are updated.
        ///For external accounts, possible values are `new`, `errored`, `verification_failed`, and `tokenized_account_number_deactivated`. If a payout fails, the status is set to `errored` and scheduled payouts are stopped until account details are updated. In the US and India, if we can't [verify the owner of the bank account](https://support.stripe.com/questions/bank-account-ownership-verification), we'll set the status to `verification_failed`. Other validations aren't run against external accounts because they're only used for payouts. This means the other statuses don't apply.
        Status: BankAccountStatus
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "bank_account"

        static member New (accountHolderName: string option, accountHolderType: BankAccountAccountHolderType option, accountType: BankAccountAccountType option, bankName: string option, country: IsoTypes.IsoCountryCode, currency: IsoTypes.IsoCurrencyCode, fingerprint: string option, id: string, last4: string, routingNumber: string option, status: BankAccountStatus, ?account: BankAccountAccount'AnyOf option, ?availablePayoutMethods: BankAccountAvailablePayoutMethods list option, ?customer: BankAccountCustomer'AnyOf option, ?defaultForCurrency: bool option, ?futureRequirements: ExternalAccountRequirements option, ?metadata: Map<string, string> option, ?requirements: ExternalAccountRequirements option) =
            {
                BankAccount.AccountHolderName = accountHolderName //required
                BankAccount.AccountHolderType = accountHolderType //required
                BankAccount.AccountType = accountType //required
                BankAccount.BankName = bankName //required
                BankAccount.Country = country //required
                BankAccount.Currency = currency //required
                BankAccount.Fingerprint = fingerprint //required
                BankAccount.Id = id //required
                BankAccount.Last4 = last4 //required
                BankAccount.RoutingNumber = routingNumber //required
                BankAccount.Status = status //required
                BankAccount.Account = account |> Option.flatten
                BankAccount.AvailablePayoutMethods = availablePayoutMethods |> Option.flatten
                BankAccount.Customer = customer |> Option.flatten
                BankAccount.DefaultForCurrency = defaultForCurrency |> Option.flatten
                BankAccount.FutureRequirements = futureRequirements |> Option.flatten
                BankAccount.Metadata = metadata |> Option.flatten
                BankAccount.Requirements = requirements |> Option.flatten
            }

    and BankAccountAccount'AnyOf =
        | String of string
        | Account of Account

    and BankAccountCustomer'AnyOf =
        | String of string
        | Customer of Customer
        | DeletedCustomer of DeletedCustomer

    and BankConnectionsResourceAccountNumberDetails = {
        ///When the account number is expected to expire, if applicable.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpectedExpiryDate: DateTime option
        ///The type of account number associated with the account.
        IdentifierType: BankConnectionsResourceAccountNumberDetailsIdentifierType
        ///Whether the account number is currently active and usable for transactions.
        Status: BankConnectionsResourceAccountNumberDetailsStatus
        ///The payment networks that the account number can be used for.
        SupportedNetworks: string list
    }
    with
        static member New (expectedExpiryDate: DateTime option, identifierType: BankConnectionsResourceAccountNumberDetailsIdentifierType, status: BankConnectionsResourceAccountNumberDetailsStatus, supportedNetworks: string list) =
            {
                BankConnectionsResourceAccountNumberDetails.ExpectedExpiryDate = expectedExpiryDate //required
                BankConnectionsResourceAccountNumberDetails.IdentifierType = identifierType //required
                BankConnectionsResourceAccountNumberDetails.Status = status //required
                BankConnectionsResourceAccountNumberDetails.SupportedNetworks = supportedNetworks //required
            }

    and BankConnectionsResourceAccountholder = {
        ///The ID of the Stripe account that this account belongs to. Only available when `account_holder.type` is `account`.
        Account: BankConnectionsResourceAccountholderAccount'AnyOf option
        ///The ID for an Account representing a customer that this account belongs to. Only available when `account_holder.type` is `customer`.
        Customer: BankConnectionsResourceAccountholderCustomer'AnyOf option
        CustomerAccount: string option
        ///Type of account holder that this account belongs to.
        Type: BankConnectionsResourceAccountholderType
    }
    with
        static member New (``type``: BankConnectionsResourceAccountholderType, ?account: BankConnectionsResourceAccountholderAccount'AnyOf, ?customer: BankConnectionsResourceAccountholderCustomer'AnyOf, ?customerAccount: string) =
            {
                BankConnectionsResourceAccountholder.Type = ``type`` //required
                BankConnectionsResourceAccountholder.Account = account
                BankConnectionsResourceAccountholder.Customer = customer
                BankConnectionsResourceAccountholder.CustomerAccount = customerAccount
            }

    and BankConnectionsResourceAccountholderAccount'AnyOf =
        | String of string
        | Account of Account

    and BankConnectionsResourceAccountholderCustomer'AnyOf =
        | String of string
        | Customer of Customer

    and BankConnectionsResourceBalance = {
        ///The time that the external institution calculated this balance. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]AsOf: DateTime
        Cash: BankConnectionsResourceBalanceApiResourceCashBalance option
        Credit: BankConnectionsResourceBalanceApiResourceCreditBalance option
        ///The balances owed to (or by) the account holder, before subtracting any outbound pending transactions or adding any inbound pending transactions.
        ///Each key is a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
        ///Each value is a integer amount. A positive amount indicates money owed to the account holder. A negative amount indicates money owed by the account holder.
        Current: Map<string, string list>
        ///The `type` of the balance. An additional hash is included on the balance with a name matching this value.
        Type: BankConnectionsResourceBalanceType
    }
    with
        static member New (asOf: DateTime, current: Map<string, string list>, ``type``: BankConnectionsResourceBalanceType, ?cash: BankConnectionsResourceBalanceApiResourceCashBalance, ?credit: BankConnectionsResourceBalanceApiResourceCreditBalance) =
            {
                BankConnectionsResourceBalance.AsOf = asOf //required
                BankConnectionsResourceBalance.Current = current //required
                BankConnectionsResourceBalance.Type = ``type`` //required
                BankConnectionsResourceBalance.Cash = cash
                BankConnectionsResourceBalance.Credit = credit
            }

    and BankConnectionsResourceBalanceApiResourceCashBalance = {
        ///The funds available to the account holder. Typically this is the current balance after subtracting any outbound pending transactions and adding any inbound pending transactions.
        ///Each key is a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
        ///Each value is a integer amount. A positive amount indicates money owed to the account holder. A negative amount indicates money owed by the account holder.
        Available: Map<string, string list> option
    }
    with
        static member New (available: Map<string, string list> option) =
            {
                BankConnectionsResourceBalanceApiResourceCashBalance.Available = available //required
            }

    and BankConnectionsResourceBalanceApiResourceCreditBalance = {
        ///The credit that has been used by the account holder.
        ///Each key is a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
        ///Each value is a integer amount. A positive amount indicates money owed to the account holder. A negative amount indicates money owed by the account holder.
        Used: Map<string, string list> option
    }
    with
        static member New (used: Map<string, string list> option) =
            {
                BankConnectionsResourceBalanceApiResourceCreditBalance.Used = used //required
            }

    and BankConnectionsResourceBalanceRefresh = {
        ///The time at which the last refresh attempt was initiated. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]LastAttemptedAt: DateTime
        ///Time at which the next balance refresh can be initiated. This value will be `null` when `status` is `pending`. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]NextRefreshAvailableAt: DateTime option
        ///The status of the last refresh attempt.
        Status: BankConnectionsResourceBalanceRefreshStatus
    }
    with
        static member New (lastAttemptedAt: DateTime, nextRefreshAvailableAt: DateTime option, status: BankConnectionsResourceBalanceRefreshStatus) =
            {
                BankConnectionsResourceBalanceRefresh.LastAttemptedAt = lastAttemptedAt //required
                BankConnectionsResourceBalanceRefresh.NextRefreshAvailableAt = nextRefreshAvailableAt //required
                BankConnectionsResourceBalanceRefresh.Status = status //required
            }

    and BankConnectionsResourceLinkAccountSessionFilters = {
        ///Restricts the Session to subcategories of accounts that can be linked. Valid subcategories are: `checking`, `savings`, `mortgage`, `line_of_credit`, `credit_card`.
        AccountSubcategories: BankConnectionsResourceLinkAccountSessionFiltersAccountSubcategories list option
        ///List of countries from which to filter accounts.
        Countries: string list option
    }
    with
        static member New (accountSubcategories: BankConnectionsResourceLinkAccountSessionFiltersAccountSubcategories list option, countries: string list option) =
            {
                BankConnectionsResourceLinkAccountSessionFilters.AccountSubcategories = accountSubcategories //required
                BankConnectionsResourceLinkAccountSessionFilters.Countries = countries //required
            }

    and BankConnectionsResourceOwnershipRefresh = {
        ///The time at which the last refresh attempt was initiated. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]LastAttemptedAt: DateTime
        ///Time at which the next ownership refresh can be initiated. This value will be `null` when `status` is `pending`. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]NextRefreshAvailableAt: DateTime option
        ///The status of the last refresh attempt.
        Status: BankConnectionsResourceOwnershipRefreshStatus
    }
    with
        static member New (lastAttemptedAt: DateTime, nextRefreshAvailableAt: DateTime option, status: BankConnectionsResourceOwnershipRefreshStatus) =
            {
                BankConnectionsResourceOwnershipRefresh.LastAttemptedAt = lastAttemptedAt //required
                BankConnectionsResourceOwnershipRefresh.NextRefreshAvailableAt = nextRefreshAvailableAt //required
                BankConnectionsResourceOwnershipRefresh.Status = status //required
            }

    and BankConnectionsResourceTransactionRefresh = {
        ///Unique identifier for the object.
        Id: string
        ///The time at which the last refresh attempt was initiated. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]LastAttemptedAt: DateTime
        ///Time at which the next transaction refresh can be initiated. This value will be `null` when `status` is `pending`. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]NextRefreshAvailableAt: DateTime option
        ///The status of the last refresh attempt.
        Status: BankConnectionsResourceTransactionRefreshStatus
    }
    with
        static member New (id: string, lastAttemptedAt: DateTime, nextRefreshAvailableAt: DateTime option, status: BankConnectionsResourceTransactionRefreshStatus) =
            {
                BankConnectionsResourceTransactionRefresh.Id = id //required
                BankConnectionsResourceTransactionRefresh.LastAttemptedAt = lastAttemptedAt //required
                BankConnectionsResourceTransactionRefresh.NextRefreshAvailableAt = nextRefreshAvailableAt //required
                BankConnectionsResourceTransactionRefresh.Status = status //required
            }

    ///A billing alert is a resource that notifies you when a certain usage threshold on a meter is crossed. For example, you might create a billing alert to notify you when a certain user made 100 API requests.
    and BillingAlert = {
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Status of the alert. This can be active, inactive or archived.
        Status: BillingAlertStatus option
        ///Title of the alert.
        Title: string
        ///Encapsulates configuration of the alert to monitor usage on a specific [Billing Meter](https://docs.stripe.com/api/billing/meter).
        UsageThreshold: ThresholdsResourceUsageThresholdConfig option
    }
    with
        ///Defines the type of the alert.
        member _.AlertType = "usage_threshold"
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "billing.alert"

        static member New (id: string, livemode: bool, status: BillingAlertStatus option, title: string, usageThreshold: ThresholdsResourceUsageThresholdConfig option) =
            {
                BillingAlert.Id = id //required
                BillingAlert.Livemode = livemode //required
                BillingAlert.Status = status //required
                BillingAlert.Title = title //required
                BillingAlert.UsageThreshold = usageThreshold //required
            }

    and BillingAlertTriggered = {
        Alert: BillingAlert
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///ID of customer for which the alert triggered
        Customer: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The value triggering the alert
        Value: int
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "billing.alert_triggered"

        static member New (alert: BillingAlert, created: DateTime, customer: string, livemode: bool, value: int) =
            {
                BillingAlertTriggered.Alert = alert //required
                BillingAlertTriggered.Created = created //required
                BillingAlertTriggered.Customer = customer //required
                BillingAlertTriggered.Livemode = livemode //required
                BillingAlertTriggered.Value = value //required
            }

    ///Indicates the billing credit balance for billing credits granted to a customer.
    and BillingCreditBalanceSummary = {
        ///The billing credit balances. One entry per credit grant currency. If a customer only has credit grants in a single currency, then this will have a single balance entry.
        Balances: CreditBalance list
        ///The customer the balance is for.
        Customer: BillingCreditBalanceSummaryCustomer'AnyOf
        ///The account the balance is for.
        CustomerAccount: string option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "billing.credit_balance_summary"

        static member New (balances: CreditBalance list, customer: BillingCreditBalanceSummaryCustomer'AnyOf, customerAccount: string option, livemode: bool) =
            {
                BillingCreditBalanceSummary.Balances = balances //required
                BillingCreditBalanceSummary.Customer = customer //required
                BillingCreditBalanceSummary.CustomerAccount = customerAccount //required
                BillingCreditBalanceSummary.Livemode = livemode //required
            }

    and BillingCreditBalanceSummaryCustomer'AnyOf =
        | String of string
        | Customer of Customer
        | DeletedCustomer of DeletedCustomer

    ///A credit balance transaction is a resource representing a transaction (either a credit or a debit) against an existing credit grant.
    and BillingCreditBalanceTransaction = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Credit details for this credit balance transaction. Only present if type is `credit`.
        Credit: BillingCreditGrantsResourceBalanceCredit option
        ///The credit grant associated with this credit balance transaction.
        CreditGrant: BillingCreditBalanceTransactionCreditGrant'AnyOf
        ///Debit details for this credit balance transaction. Only present if type is `debit`.
        Debit: BillingCreditGrantsResourceBalanceDebit option
        ///The effective time of this credit balance transaction.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]EffectiveAt: DateTime
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///ID of the test clock this credit balance transaction belongs to.
        TestClock: BillingCreditBalanceTransactionTestClock'AnyOf option
        ///The type of credit balance transaction (credit or debit).
        Type: BillingCreditBalanceTransactionType option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "billing.credit_balance_transaction"

        static member New (created: DateTime, credit: BillingCreditGrantsResourceBalanceCredit option, creditGrant: BillingCreditBalanceTransactionCreditGrant'AnyOf, debit: BillingCreditGrantsResourceBalanceDebit option, effectiveAt: DateTime, id: string, livemode: bool, testClock: BillingCreditBalanceTransactionTestClock'AnyOf option, ``type``: BillingCreditBalanceTransactionType option) =
            {
                BillingCreditBalanceTransaction.Created = created //required
                BillingCreditBalanceTransaction.Credit = credit //required
                BillingCreditBalanceTransaction.CreditGrant = creditGrant //required
                BillingCreditBalanceTransaction.Debit = debit //required
                BillingCreditBalanceTransaction.EffectiveAt = effectiveAt //required
                BillingCreditBalanceTransaction.Id = id //required
                BillingCreditBalanceTransaction.Livemode = livemode //required
                BillingCreditBalanceTransaction.TestClock = testClock //required
                BillingCreditBalanceTransaction.Type = ``type`` //required
            }

    and BillingCreditBalanceTransactionCreditGrant'AnyOf =
        | String of string
        | BillingCreditGrant of BillingCreditGrant

    and BillingCreditBalanceTransactionTestClock'AnyOf =
        | String of string
        | TestHelpersTestClock of TestHelpersTestClock

    ///A credit grant is an API resource that documents the allocation of some billing credits to a customer.
    ///Related guide: [Billing credits](https://docs.stripe.com/billing/subscriptions/usage-based/billing-credits)
    and BillingCreditGrant = {
        Amount: BillingCreditGrantsResourceAmount
        ApplicabilityConfig: BillingCreditGrantsResourceApplicabilityConfig
        ///The category of this credit grant. This is for tracking purposes and isn't displayed to the customer.
        Category: BillingCreditGrantCategory
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///ID of the customer receiving the billing credits.
        Customer: BillingCreditGrantCustomer'AnyOf
        ///ID of the account representing the customer receiving the billing credits
        CustomerAccount: string option
        ///The time when the billing credits become effective-when they're eligible for use.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]EffectiveAt: DateTime option
        ///The time when the billing credits expire. If not present, the billing credits don't expire.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///A descriptive name shown in dashboard.
        Name: string option
        ///The priority for applying this credit grant. The highest priority is 0 and the lowest is 100.
        Priority: int option
        ///ID of the test clock this credit grant belongs to.
        TestClock: BillingCreditGrantTestClock'AnyOf option
        ///Time at which the object was last updated. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Updated: DateTime
        ///The time when this credit grant was voided. If not present, the credit grant hasn't been voided.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]VoidedAt: DateTime option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "billing.credit_grant"

        static member New (amount: BillingCreditGrantsResourceAmount, applicabilityConfig: BillingCreditGrantsResourceApplicabilityConfig, category: BillingCreditGrantCategory, created: DateTime, customer: BillingCreditGrantCustomer'AnyOf, customerAccount: string option, effectiveAt: DateTime option, expiresAt: DateTime option, id: string, livemode: bool, metadata: Map<string, string>, name: string option, testClock: BillingCreditGrantTestClock'AnyOf option, updated: DateTime, voidedAt: DateTime option, ?priority: int option) =
            {
                BillingCreditGrant.Amount = amount //required
                BillingCreditGrant.ApplicabilityConfig = applicabilityConfig //required
                BillingCreditGrant.Category = category //required
                BillingCreditGrant.Created = created //required
                BillingCreditGrant.Customer = customer //required
                BillingCreditGrant.CustomerAccount = customerAccount //required
                BillingCreditGrant.EffectiveAt = effectiveAt //required
                BillingCreditGrant.ExpiresAt = expiresAt //required
                BillingCreditGrant.Id = id //required
                BillingCreditGrant.Livemode = livemode //required
                BillingCreditGrant.Metadata = metadata //required
                BillingCreditGrant.Name = name //required
                BillingCreditGrant.TestClock = testClock //required
                BillingCreditGrant.Updated = updated //required
                BillingCreditGrant.VoidedAt = voidedAt //required
                BillingCreditGrant.Priority = priority |> Option.flatten
            }

    and BillingCreditGrantCustomer'AnyOf =
        | String of string
        | Customer of Customer
        | DeletedCustomer of DeletedCustomer

    and BillingCreditGrantTestClock'AnyOf =
        | String of string
        | TestHelpersTestClock of TestHelpersTestClock

    ///Meters specify how to aggregate meter events over a billing period. Meter events represent the actions that customers take in your system. Meters attach to prices and form the basis of the bill.
    ///Related guide: [Usage based billing](https://docs.stripe.com/billing/subscriptions/usage-based)
    and BillingMeter = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        CustomerMapping: BillingMeterResourceCustomerMappingSettings
        DefaultAggregation: BillingMeterResourceAggregationSettings
        ///The meter's name.
        DisplayName: string
        ///The name of the meter event to record usage for. Corresponds with the `event_name` field on meter events.
        EventName: string
        ///The time window which meter events have been pre-aggregated for, if any.
        EventTimeWindow: BillingMeterEventTimeWindow option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The meter's status.
        Status: BillingMeterStatus
        StatusTransitions: BillingMeterResourceBillingMeterStatusTransitions
        ///Time at which the object was last updated. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Updated: DateTime
        ValueSettings: BillingMeterResourceBillingMeterValue
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "billing.meter"

        static member New (created: DateTime, customerMapping: BillingMeterResourceCustomerMappingSettings, defaultAggregation: BillingMeterResourceAggregationSettings, displayName: string, eventName: string, eventTimeWindow: BillingMeterEventTimeWindow option, id: string, livemode: bool, status: BillingMeterStatus, statusTransitions: BillingMeterResourceBillingMeterStatusTransitions, updated: DateTime, valueSettings: BillingMeterResourceBillingMeterValue) =
            {
                BillingMeter.Created = created //required
                BillingMeter.CustomerMapping = customerMapping //required
                BillingMeter.DefaultAggregation = defaultAggregation //required
                BillingMeter.DisplayName = displayName //required
                BillingMeter.EventName = eventName //required
                BillingMeter.EventTimeWindow = eventTimeWindow //required
                BillingMeter.Id = id //required
                BillingMeter.Livemode = livemode //required
                BillingMeter.Status = status //required
                BillingMeter.StatusTransitions = statusTransitions //required
                BillingMeter.Updated = updated //required
                BillingMeter.ValueSettings = valueSettings //required
            }

    ///Meter events represent actions that customers take in your system. You can use meter events to bill a customer based on their usage. Meter events are associated with billing meters, which define both the contents of the event’s payload and how to aggregate those events.
    and BillingMeterEvent = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The name of the meter event. Corresponds with the `event_name` field on a meter.
        EventName: string
        ///A unique identifier for the event.
        Identifier: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The payload of the event. This contains the fields corresponding to a meter's `customer_mapping.event_payload_key` (default is `stripe_customer_id`) and `value_settings.event_payload_key` (default is `value`). Read more about the [payload](https://docs.stripe.com/billing/subscriptions/usage-based/meters/configure#meter-configuration-attributes).
        Payload: Map<string, string list>
        ///The timestamp passed in when creating the event. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Timestamp: DateTime
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "billing.meter_event"

        static member New (created: DateTime, eventName: string, identifier: string, livemode: bool, payload: Map<string, string list>, timestamp: DateTime) =
            {
                BillingMeterEvent.Created = created //required
                BillingMeterEvent.EventName = eventName //required
                BillingMeterEvent.Identifier = identifier //required
                BillingMeterEvent.Livemode = livemode //required
                BillingMeterEvent.Payload = payload //required
                BillingMeterEvent.Timestamp = timestamp //required
            }

    ///A billing meter event adjustment is a resource that allows you to cancel a meter event. For example, you might create a billing meter event adjustment to cancel a meter event that was created in error or attached to the wrong customer.
    and BillingMeterEventAdjustment = {
        ///Specifies which event to cancel.
        Cancel: BillingMeterResourceBillingMeterEventAdjustmentCancel option
        ///The name of the meter event. Corresponds with the `event_name` field on a meter.
        EventName: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The meter event adjustment's status.
        Status: BillingMeterEventAdjustmentStatus
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "billing.meter_event_adjustment"
        ///Specifies whether to cancel a single event or a range of events for a time period. Time period cancellation is not supported yet.
        member _.Type = "cancel"

        static member New (cancel: BillingMeterResourceBillingMeterEventAdjustmentCancel option, eventName: string, livemode: bool, status: BillingMeterEventAdjustmentStatus) =
            {
                BillingMeterEventAdjustment.Cancel = cancel //required
                BillingMeterEventAdjustment.EventName = eventName //required
                BillingMeterEventAdjustment.Livemode = livemode //required
                BillingMeterEventAdjustment.Status = status //required
            }

    and BillingBillResourceInvoiceItemParentsInvoiceItemParent = {
        ///Details about the subscription that generated this invoice item
        SubscriptionDetails: BillingBillResourceInvoiceItemParentsInvoiceItemSubscriptionParent option
    }
    with
        ///The type of parent that generated this invoice item
        member _.Type = "subscription_details"

        static member New (subscriptionDetails: BillingBillResourceInvoiceItemParentsInvoiceItemSubscriptionParent option) =
            {
                BillingBillResourceInvoiceItemParentsInvoiceItemParent.SubscriptionDetails = subscriptionDetails //required
            }

    and BillingBillResourceInvoicingLinesCommonCreditedItems = {
        ///Invoice containing the credited invoice line items
        Invoice: string
        ///Credited invoice line items
        InvoiceLineItems: string list
    }
    with
        static member New (invoice: string, invoiceLineItems: string list) =
            {
                BillingBillResourceInvoicingLinesCommonCreditedItems.Invoice = invoice //required
                BillingBillResourceInvoicingLinesCommonCreditedItems.InvoiceLineItems = invoiceLineItems //required
            }

    and BillingBillResourceInvoicingLinesCommonProrationDetails = {
        ///For a credit proration `line_item`, the original debit line_items to which the credit proration applies.
        CreditedItems: BillingBillResourceInvoicingLinesCommonCreditedItems option
    }
    with
        static member New (creditedItems: BillingBillResourceInvoicingLinesCommonCreditedItems option) =
            {
                BillingBillResourceInvoicingLinesCommonProrationDetails.CreditedItems = creditedItems //required
            }

    and BillingBillResourceInvoicingLinesParentsInvoiceLineItemInvoiceItemParent = {
        ///The invoice item that generated this line item
        InvoiceItem: string
        ///Whether this is a proration
        Proration: bool
        ///Additional details for proration line items
        ProrationDetails: BillingBillResourceInvoicingLinesCommonProrationDetails option
        ///The subscription that the invoice item belongs to
        Subscription: string option
    }
    with
        static member New (invoiceItem: string, proration: bool, prorationDetails: BillingBillResourceInvoicingLinesCommonProrationDetails option, subscription: string option) =
            {
                BillingBillResourceInvoicingLinesParentsInvoiceLineItemInvoiceItemParent.InvoiceItem = invoiceItem //required
                BillingBillResourceInvoicingLinesParentsInvoiceLineItemInvoiceItemParent.Proration = proration //required
                BillingBillResourceInvoicingLinesParentsInvoiceLineItemInvoiceItemParent.ProrationDetails = prorationDetails //required
                BillingBillResourceInvoicingLinesParentsInvoiceLineItemInvoiceItemParent.Subscription = subscription //required
            }

    and BillingBillResourceInvoicingLinesParentsInvoiceLineItemParent = {
        ///Details about the invoice item that generated this line item
        InvoiceItemDetails: BillingBillResourceInvoicingLinesParentsInvoiceLineItemInvoiceItemParent option
        ///Details about the subscription item that generated this line item
        SubscriptionItemDetails: BillingBillResourceInvoicingLinesParentsInvoiceLineItemSubscriptionItemParent option
        ///The type of parent that generated this line item
        Type: BillingBillResourceInvoicingLinesParentsInvoiceLineItemParentType
    }
    with
        static member New (invoiceItemDetails: BillingBillResourceInvoicingLinesParentsInvoiceLineItemInvoiceItemParent option, subscriptionItemDetails: BillingBillResourceInvoicingLinesParentsInvoiceLineItemSubscriptionItemParent option, ``type``: BillingBillResourceInvoicingLinesParentsInvoiceLineItemParentType) =
            {
                BillingBillResourceInvoicingLinesParentsInvoiceLineItemParent.InvoiceItemDetails = invoiceItemDetails //required
                BillingBillResourceInvoicingLinesParentsInvoiceLineItemParent.SubscriptionItemDetails = subscriptionItemDetails //required
                BillingBillResourceInvoicingLinesParentsInvoiceLineItemParent.Type = ``type`` //required
            }

    and BillingBillResourceInvoicingLinesParentsInvoiceLineItemSubscriptionItemParent = {
        ///The invoice item that generated this line item
        InvoiceItem: string option
        ///Whether this is a proration
        Proration: bool
        ///Additional details for proration line items
        ProrationDetails: BillingBillResourceInvoicingLinesCommonProrationDetails option
        ///The subscription that the subscription item belongs to
        Subscription: string option
        ///The subscription item that generated this line item
        SubscriptionItem: string
    }
    with
        static member New (invoiceItem: string option, proration: bool, prorationDetails: BillingBillResourceInvoicingLinesCommonProrationDetails option, subscription: string option, subscriptionItem: string) =
            {
                BillingBillResourceInvoicingLinesParentsInvoiceLineItemSubscriptionItemParent.InvoiceItem = invoiceItem //required
                BillingBillResourceInvoicingLinesParentsInvoiceLineItemSubscriptionItemParent.Proration = proration //required
                BillingBillResourceInvoicingLinesParentsInvoiceLineItemSubscriptionItemParent.ProrationDetails = prorationDetails //required
                BillingBillResourceInvoicingLinesParentsInvoiceLineItemSubscriptionItemParent.Subscription = subscription //required
                BillingBillResourceInvoicingLinesParentsInvoiceLineItemSubscriptionItemParent.SubscriptionItem = subscriptionItem //required
            }

    and BillingBillResourceInvoicingParentsInvoiceParent = {
        ///Details about the quote that generated this invoice
        QuoteDetails: BillingBillResourceInvoicingParentsInvoiceQuoteParent option
        ///Details about the subscription that generated this invoice
        SubscriptionDetails: BillingBillResourceInvoicingParentsInvoiceSubscriptionParent option
        ///The type of parent that generated this invoice
        Type: BillingBillResourceInvoicingParentsInvoiceParentType
    }
    with
        static member New (quoteDetails: BillingBillResourceInvoicingParentsInvoiceQuoteParent option, subscriptionDetails: BillingBillResourceInvoicingParentsInvoiceSubscriptionParent option, ``type``: BillingBillResourceInvoicingParentsInvoiceParentType) =
            {
                BillingBillResourceInvoicingParentsInvoiceParent.QuoteDetails = quoteDetails //required
                BillingBillResourceInvoicingParentsInvoiceParent.SubscriptionDetails = subscriptionDetails //required
                BillingBillResourceInvoicingParentsInvoiceParent.Type = ``type`` //required
            }

    and BillingBillResourceInvoicingParentsInvoiceSubscriptionParent = {
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) defined as subscription metadata when an invoice is created. Becomes an immutable snapshot of the subscription metadata at the time of invoice finalization.
        /// *Note: This attribute is populated only for invoices created on or after June 29, 2023.*
        Metadata: Map<string, string> option
        ///The subscription that generated this invoice
        Subscription: BillingBillResourceInvoicingParentsInvoiceSubscriptionParentSubscription'AnyOf
        ///Only set for upcoming invoices that preview prorations. The time used to calculate prorations.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]SubscriptionProrationDate: DateTime option
    }
    with
        static member New (metadata: Map<string, string> option, subscription: BillingBillResourceInvoicingParentsInvoiceSubscriptionParentSubscription'AnyOf, ?subscriptionProrationDate: DateTime) =
            {
                BillingBillResourceInvoicingParentsInvoiceSubscriptionParent.Metadata = metadata //required
                BillingBillResourceInvoicingParentsInvoiceSubscriptionParent.Subscription = subscription //required
                BillingBillResourceInvoicingParentsInvoiceSubscriptionParent.SubscriptionProrationDate = subscriptionProrationDate
            }

    and BillingBillResourceInvoicingParentsInvoiceSubscriptionParentSubscription'AnyOf =
        | String of string
        | Subscription of Subscription

    and BillingBillResourceInvoicingPricingPricing = {
        PriceDetails: BillingBillResourceInvoicingPricingPricingPriceDetails option
        ///The unit amount (in the `currency` specified) of the item which contains a decimal value with at most 12 decimal places.
        UnitAmountDecimal: string option
    }
    with
        ///The type of the pricing details.
        member _.Type = "price_details"

        static member New (unitAmountDecimal: string option, ?priceDetails: BillingBillResourceInvoicingPricingPricingPriceDetails) =
            {
                BillingBillResourceInvoicingPricingPricing.UnitAmountDecimal = unitAmountDecimal //required
                BillingBillResourceInvoicingPricingPricing.PriceDetails = priceDetails
            }

    and BillingBillResourceInvoicingPricingPricingPriceDetails = {
        ///The ID of the price this item is associated with.
        Price: BillingBillResourceInvoicingPricingPricingPriceDetailsPrice'AnyOf
        ///The ID of the product this item is associated with.
        Product: string
    }
    with
        static member New (price: BillingBillResourceInvoicingPricingPricingPriceDetailsPrice'AnyOf, product: string) =
            {
                BillingBillResourceInvoicingPricingPricingPriceDetails.Price = price //required
                BillingBillResourceInvoicingPricingPricingPriceDetails.Product = product //required
            }

    and BillingBillResourceInvoicingPricingPricingPriceDetailsPrice'AnyOf =
        | String of string
        | Price of Price

    and BillingBillResourceInvoicingTaxesTax = {
        ///The amount of the tax, in cents (or local equivalent).
        Amount: int
        ///Whether this tax is inclusive or exclusive.
        TaxBehavior: BillingBillResourceInvoicingTaxesTaxTaxBehavior
        ///Additional details about the tax rate. Only present when `type` is `tax_rate_details`.
        TaxRateDetails: BillingBillResourceInvoicingTaxesTaxRateDetails option
        ///The reasoning behind this tax, for example, if the product is tax exempt. The possible values for this field may be extended as new tax rules are supported.
        TaxabilityReason: BillingBillResourceInvoicingTaxesTaxTaxabilityReason
        ///The amount on which tax is calculated, in cents (or local equivalent).
        TaxableAmount: int option
    }
    with
        ///The type of tax information.
        member _.Type = "tax_rate_details"

        static member New (amount: int, taxBehavior: BillingBillResourceInvoicingTaxesTaxTaxBehavior, taxRateDetails: BillingBillResourceInvoicingTaxesTaxRateDetails option, taxabilityReason: BillingBillResourceInvoicingTaxesTaxTaxabilityReason, taxableAmount: int option) =
            {
                BillingBillResourceInvoicingTaxesTax.Amount = amount //required
                BillingBillResourceInvoicingTaxesTax.TaxBehavior = taxBehavior //required
                BillingBillResourceInvoicingTaxesTax.TaxRateDetails = taxRateDetails //required
                BillingBillResourceInvoicingTaxesTax.TaxabilityReason = taxabilityReason //required
                BillingBillResourceInvoicingTaxesTax.TaxableAmount = taxableAmount //required
            }

    and BillingBillResourceInvoicingTaxesTaxRateDetails = {
        ///ID of the tax rate
        TaxRate: BillingBillResourceInvoicingTaxesTaxRateDetailsTaxRate'AnyOf
    }
    with
        static member New (taxRate: BillingBillResourceInvoicingTaxesTaxRateDetailsTaxRate'AnyOf) =
            {
                BillingBillResourceInvoicingTaxesTaxRateDetails.TaxRate = taxRate //required
            }

    and BillingBillResourceInvoicingTaxesTaxRateDetailsTaxRate'AnyOf =
        | String of string
        | TaxRate of TaxRate

    and BillingClocksResourceStatusDetailsStatusDetails = {
        Advancing: BillingClocksResourceStatusDetailsAdvancingStatusDetails option
    }
    with
        static member New (?advancing: BillingClocksResourceStatusDetailsAdvancingStatusDetails) =
            {
                BillingClocksResourceStatusDetailsStatusDetails.Advancing = advancing
            }

    and BillingCreditGrantsResourceAmount = {
        ///The monetary amount.
        Monetary: BillingCreditGrantsResourceMonetaryAmount option
    }
    with
        ///The type of this amount. We currently only support `monetary` billing credits.
        member _.Type = "monetary"

        static member New (monetary: BillingCreditGrantsResourceMonetaryAmount option) =
            {
                BillingCreditGrantsResourceAmount.Monetary = monetary //required
            }

    and BillingCreditGrantsResourceApplicabilityConfig = {
        Scope: BillingCreditGrantsResourceScope
    }
    with
        static member New (scope: BillingCreditGrantsResourceScope) =
            {
                BillingCreditGrantsResourceApplicabilityConfig.Scope = scope //required
            }

    and BillingCreditGrantsResourceBalanceCredit = {
        Amount: BillingCreditGrantsResourceAmount
        ///Details of the invoice to which the reinstated credits were originally applied. Only present if `type` is `credits_application_invoice_voided`.
        CreditsApplicationInvoiceVoided: BillingCreditGrantsResourceBalanceCreditsApplicationInvoiceVoided option
        ///The type of credit transaction.
        Type: BillingCreditGrantsResourceBalanceCreditType
    }
    with
        static member New (amount: BillingCreditGrantsResourceAmount, creditsApplicationInvoiceVoided: BillingCreditGrantsResourceBalanceCreditsApplicationInvoiceVoided option, ``type``: BillingCreditGrantsResourceBalanceCreditType) =
            {
                BillingCreditGrantsResourceBalanceCredit.Amount = amount //required
                BillingCreditGrantsResourceBalanceCredit.CreditsApplicationInvoiceVoided = creditsApplicationInvoiceVoided //required
                BillingCreditGrantsResourceBalanceCredit.Type = ``type`` //required
            }

    and BillingCreditGrantsResourceBalanceCreditsApplicationInvoiceVoided = {
        ///The invoice to which the reinstated billing credits were originally applied.
        Invoice: BillingCreditGrantsResourceBalanceCreditsApplicationInvoiceVoidedInvoice'AnyOf
        ///The invoice line item to which the reinstated billing credits were originally applied.
        InvoiceLineItem: string
    }
    with
        static member New (invoice: BillingCreditGrantsResourceBalanceCreditsApplicationInvoiceVoidedInvoice'AnyOf, invoiceLineItem: string) =
            {
                BillingCreditGrantsResourceBalanceCreditsApplicationInvoiceVoided.Invoice = invoice //required
                BillingCreditGrantsResourceBalanceCreditsApplicationInvoiceVoided.InvoiceLineItem = invoiceLineItem //required
            }

    and BillingCreditGrantsResourceBalanceCreditsApplicationInvoiceVoidedInvoice'AnyOf =
        | String of string
        | Invoice of Invoice

    and BillingCreditGrantsResourceBalanceCreditsApplied = {
        ///The invoice to which the billing credits were applied.
        Invoice: BillingCreditGrantsResourceBalanceCreditsAppliedInvoice'AnyOf
        ///The invoice line item to which the billing credits were applied.
        InvoiceLineItem: string
    }
    with
        static member New (invoice: BillingCreditGrantsResourceBalanceCreditsAppliedInvoice'AnyOf, invoiceLineItem: string) =
            {
                BillingCreditGrantsResourceBalanceCreditsApplied.Invoice = invoice //required
                BillingCreditGrantsResourceBalanceCreditsApplied.InvoiceLineItem = invoiceLineItem //required
            }

    and BillingCreditGrantsResourceBalanceCreditsAppliedInvoice'AnyOf =
        | String of string
        | Invoice of Invoice

    and BillingCreditGrantsResourceBalanceDebit = {
        Amount: BillingCreditGrantsResourceAmount
        ///Details of how the billing credits were applied to an invoice. Only present if `type` is `credits_applied`.
        CreditsApplied: BillingCreditGrantsResourceBalanceCreditsApplied option
        ///The type of debit transaction.
        Type: BillingCreditGrantsResourceBalanceDebitType
    }
    with
        static member New (amount: BillingCreditGrantsResourceAmount, creditsApplied: BillingCreditGrantsResourceBalanceCreditsApplied option, ``type``: BillingCreditGrantsResourceBalanceDebitType) =
            {
                BillingCreditGrantsResourceBalanceDebit.Amount = amount //required
                BillingCreditGrantsResourceBalanceDebit.CreditsApplied = creditsApplied //required
                BillingCreditGrantsResourceBalanceDebit.Type = ``type`` //required
            }

    and BillingCreditGrantsResourceMonetaryAmount = {
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///A positive integer representing the amount.
        Value: int
    }
    with
        static member New (currency: IsoTypes.IsoCurrencyCode, value: int) =
            {
                BillingCreditGrantsResourceMonetaryAmount.Currency = currency //required
                BillingCreditGrantsResourceMonetaryAmount.Value = value //required
            }

    and BillingCreditGrantsResourceScope = {
        ///The prices that credit grants can apply to. We currently only support `metered` prices. This refers to prices that have a [Billing Meter](https://docs.stripe.com/api/billing/meter) attached to them. Cannot be used in combination with `price_type`.
        Prices: BillingCreditGrantsResourceApplicablePrice list option
    }
    with
        ///The price type that credit grants can apply to. We currently only support the `metered` price type. This refers to prices that have a [Billing Meter](https://docs.stripe.com/api/billing/meter) attached to them. Cannot be used in combination with `prices`.
        member _.PriceType = "metered"

        static member New (?prices: BillingCreditGrantsResourceApplicablePrice list) =
            {
                BillingCreditGrantsResourceScope.Prices = prices
            }

    and BillingDetails = {
        ///Billing address.
        Address: Address option
        ///Email address.
        Email: string option
        ///Full name.
        Name: string option
        ///Billing phone number (including extension).
        Phone: string option
        ///Taxpayer identification number. Used only for transactions between LATAM buyers and non-LATAM sellers.
        TaxId: string option
    }
    with
        static member New (address: Address option, email: string option, name: string option, phone: string option, taxId: string option) =
            {
                BillingDetails.Address = address //required
                BillingDetails.Email = email //required
                BillingDetails.Name = name //required
                BillingDetails.Phone = phone //required
                BillingDetails.TaxId = taxId //required
            }

    and BillingMeterResourceAggregationSettings = {
        ///Specifies how events are aggregated.
        Formula: BillingMeterResourceAggregationSettingsFormula
    }
    with
        static member New (formula: BillingMeterResourceAggregationSettingsFormula) =
            {
                BillingMeterResourceAggregationSettings.Formula = formula //required
            }

    ///A portal configuration describes the functionality and behavior you embed in a portal session. Related guide: [Configure the customer portal](/customer-management/configure-portal).
    and BillingPortalConfiguration = {
        ///Whether the configuration is active and can be used to create portal sessions.
        Active: bool
        ///ID of the Connect Application that created the configuration.
        Application: BillingPortalConfigurationApplication'AnyOf option
        BusinessProfile: PortalBusinessProfile
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The default URL to redirect customers to when they click on the portal's link to return to your website. This can be [overriden](https://docs.stripe.com/api/customer_portal/sessions/create#create_portal_session-return_url) when creating the session.
        DefaultReturnUrl: string option
        Features: PortalFeatures
        ///Unique identifier for the object.
        Id: string
        ///Whether the configuration is the default. If `true`, this configuration can be managed in the Dashboard and portal sessions will use this configuration unless it is overriden when creating the session.
        IsDefault: bool
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        LoginPage: PortalLoginPage
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///The name of the configuration.
        Name: string option
        ///Time at which the object was last updated. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Updated: DateTime
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "billing_portal.configuration"

        static member New (active: bool, application: BillingPortalConfigurationApplication'AnyOf option, businessProfile: PortalBusinessProfile, created: DateTime, defaultReturnUrl: string option, features: PortalFeatures, id: string, isDefault: bool, livemode: bool, loginPage: PortalLoginPage, metadata: Map<string, string> option, name: string option, updated: DateTime) =
            {
                BillingPortalConfiguration.Active = active //required
                BillingPortalConfiguration.Application = application //required
                BillingPortalConfiguration.BusinessProfile = businessProfile //required
                BillingPortalConfiguration.Created = created //required
                BillingPortalConfiguration.DefaultReturnUrl = defaultReturnUrl //required
                BillingPortalConfiguration.Features = features //required
                BillingPortalConfiguration.Id = id //required
                BillingPortalConfiguration.IsDefault = isDefault //required
                BillingPortalConfiguration.Livemode = livemode //required
                BillingPortalConfiguration.LoginPage = loginPage //required
                BillingPortalConfiguration.Metadata = metadata //required
                BillingPortalConfiguration.Name = name //required
                BillingPortalConfiguration.Updated = updated //required
            }

    and BillingPortalConfigurationApplication'AnyOf =
        | String of string
        | Application of Application
        | DeletedApplication of DeletedApplication

    ///The Billing customer portal is a Stripe-hosted UI for subscription and
    ///billing management.
    ///A portal configuration describes the functionality and features that you
    ///want to provide to your customers through the portal.
    ///A portal session describes the instantiation of the customer portal for
    ///a particular customer. By visiting the session's URL, the customer
    ///can manage their subscriptions and billing details. For security reasons,
    ///sessions are short-lived and will expire if the customer does not visit the URL.
    ///Create sessions on-demand when customers intend to manage their subscriptions
    ///and billing details.
    ///Related guide: [Customer management](/customer-management)
    and BillingPortalSession = {
        ///The configuration used by this session, describing the features available.
        Configuration: BillingPortalSessionConfiguration'AnyOf
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The ID of the customer for this session.
        Customer: string
        ///The ID of the account for this session.
        CustomerAccount: string option
        ///Information about a specific flow for the customer to go through. See the [docs](https://docs.stripe.com/customer-management/portal-deep-links) to learn more about using customer portal deep links and flows.
        Flow: PortalFlowsFlow option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The IETF language tag of the locale Customer Portal is displayed in. If blank or auto, the customer’s `preferred_locales` or browser’s locale is used.
        Locale: BillingPortalSessionLocale option
        ///The account for which the session was created on behalf of. When specified, only subscriptions and invoices with this `on_behalf_of` account appear in the portal. For more information, see the [docs](https://docs.stripe.com/connect/separate-charges-and-transfers#settlement-merchant). Use the [Accounts API](https://docs.stripe.com/api/accounts/object#account_object-settings-branding) to modify the `on_behalf_of` account's branding settings, which the portal displays.
        OnBehalfOf: string option
        ///The URL to redirect customers to when they click on the portal's link to return to your website.
        ReturnUrl: string option
        ///The short-lived URL of the session that gives customers access to the customer portal.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "billing_portal.session"

        static member New (configuration: BillingPortalSessionConfiguration'AnyOf, created: DateTime, customer: string, customerAccount: string option, flow: PortalFlowsFlow option, id: string, livemode: bool, locale: BillingPortalSessionLocale option, onBehalfOf: string option, returnUrl: string option, url: string) =
            {
                BillingPortalSession.Configuration = configuration //required
                BillingPortalSession.Created = created //required
                BillingPortalSession.Customer = customer //required
                BillingPortalSession.CustomerAccount = customerAccount //required
                BillingPortalSession.Flow = flow //required
                BillingPortalSession.Id = id //required
                BillingPortalSession.Livemode = livemode //required
                BillingPortalSession.Locale = locale //required
                BillingPortalSession.OnBehalfOf = onBehalfOf //required
                BillingPortalSession.ReturnUrl = returnUrl //required
                BillingPortalSession.Url = url //required
            }

    and BillingPortalSessionConfiguration'AnyOf =
        | String of string
        | BillingPortalConfiguration of BillingPortalConfiguration

    and CancellationDetails = {
        ///Additional comments about why the user canceled the subscription, if the subscription was canceled explicitly by the user.
        Comment: string option
        ///The customer submitted reason for why they canceled, if the subscription was canceled explicitly by the user.
        Feedback: CancellationDetailsFeedback option
        ///Why this subscription was canceled.
        Reason: CancellationDetailsReason option
    }
    with
        static member New (comment: string option, feedback: CancellationDetailsFeedback option, reason: CancellationDetailsReason option) =
            {
                CancellationDetails.Comment = comment //required
                CancellationDetails.Feedback = feedback //required
                CancellationDetails.Reason = reason //required
            }

    ///This is an object representing a capability for a Stripe account.
    ///Related guide: [Account capabilities](https://docs.stripe.com/connect/account-capabilities)
    and Capability = {
        ///The account for which the capability enables functionality.
        Account: CapabilityAccount'AnyOf
        FutureRequirements: AccountCapabilityFutureRequirements option
        ///The identifier for the capability.
        Id: string
        ///Whether the capability has been requested.
        Requested: bool
        ///Time at which the capability was requested. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]RequestedAt: DateTime option
        Requirements: AccountCapabilityRequirements option
        ///The status of the capability.
        Status: CapabilityStatus
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "capability"

        static member New (account: CapabilityAccount'AnyOf, id: string, requested: bool, requestedAt: DateTime option, status: CapabilityStatus, ?futureRequirements: AccountCapabilityFutureRequirements, ?requirements: AccountCapabilityRequirements) =
            {
                Capability.Account = account //required
                Capability.Id = id //required
                Capability.Requested = requested //required
                Capability.RequestedAt = requestedAt //required
                Capability.Status = status //required
                Capability.FutureRequirements = futureRequirements
                Capability.Requirements = requirements
            }

    and CapabilityAccount'AnyOf =
        | String of string
        | Account of Account

    ///You can store multiple cards on a customer in order to charge the customer
    ///later. You can also store multiple debit cards on a recipient in order to
    ///transfer to those cards later.
    ///Related guide: [Card payments with Sources](https://docs.stripe.com/sources/cards)
    and Card = {
        Account: CardAccount'AnyOf option
        ///City/District/Suburb/Town/Village.
        AddressCity: string option
        ///Billing address country, if provided when creating card.
        AddressCountry: IsoTypes.IsoCountryCode option
        ///Address line 1 (Street address/PO Box/Company name).
        [<JsonField(Name="address_line1")>]AddressLine1: string option
        ///If `address_line1` was provided, results of the check: `pass`, `fail`, `unavailable`, or `unchecked`.
        [<JsonField(Name="address_line1_check")>]AddressLine1Check: CardAddressLine1Check option
        ///Address line 2 (Apartment/Suite/Unit/Building).
        [<JsonField(Name="address_line2")>]AddressLine2: string option
        ///State/County/Province/Region.
        AddressState: string option
        ///ZIP or postal code.
        AddressZip: string option
        ///If `address_zip` was provided, results of the check: `pass`, `fail`, `unavailable`, or `unchecked`.
        AddressZipCheck: CardAddressZipCheck option
        ///This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment method in a checkout flow. The field defaults to “unspecified”.
        AllowRedisplay: CardAllowRedisplay option
        ///A set of available payout methods for this card. Only values from this set should be passed as the `method` when creating a payout.
        AvailablePayoutMethods: CardAvailablePayoutMethods list option
        ///Card brand. Can be `American Express`, `Cartes Bancaires`, `Diners Club`, `Discover`, `Eftpos Australia`, `Girocard`, `JCB`, `MasterCard`, `UnionPay`, `Visa`, or `Unknown`.
        Brand: CardBrand
        ///Two-letter ISO code representing the country of the card. You could use this attribute to get a sense of the international breakdown of cards you've collected.
        Country: IsoTypes.IsoCountryCode option
        ///Three-letter [ISO code for currency](https://www.iso.org/iso-4217-currency-codes.html) in lowercase. Must be a [supported currency](https://docs.stripe.com/currencies). Only applicable on accounts (not customers or recipients). The card can be used as a transfer destination for funds in this currency. This property is only available when returned as an [External Account](/api/external_account_cards/object) where [controller.is_controller](/api/accounts/object#account_object-controller-is_controller) is `true`.
        Currency: IsoTypes.IsoCurrencyCode option
        ///The customer that this card belongs to. This attribute will not be in the card object if the card belongs to an account or recipient instead.
        Customer: CardCustomer'AnyOf option
        ///If a CVC was provided, results of the check: `pass`, `fail`, `unavailable`, or `unchecked`. A result of unchecked indicates that CVC was provided but hasn't been checked yet. Checks are typically performed when attaching a card to a Customer object, or when creating a charge. For more details, see [Check if a card is valid without a charge](https://support.stripe.com/questions/check-if-a-card-is-valid-without-a-charge).
        CvcCheck: CardCvcCheck option
        ///Whether this card is the default external account for its currency. This property is only available for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts.
        DefaultForCurrency: bool option
        ///A high-level description of the type of cards issued in this range. (For internal use only and not typically available in standard API requests.)
        Description: string option
        ///(For tokenized numbers only.) The last four digits of the device account number.
        [<JsonField(Name="dynamic_last4")>]DynamicLast4: string option
        ///Two-digit number representing the card's expiration month.
        ExpMonth: int
        ///Four-digit number representing the card's expiration year.
        ExpYear: int
        ///Uniquely identifies this particular card number. You can use this attribute to check whether two customers who’ve signed up with you are using the same card number, for example. For payment methods that tokenize card information (Apple Pay, Google Pay), the tokenized number might be provided instead of the underlying card number.
        ///*As of May 1, 2021, card fingerprint in India for Connect changed to allow two fingerprints for the same card---one for India and one for the rest of the world.*
        Fingerprint: string option
        ///Card funding type. Can be `credit`, `debit`, `prepaid`, or `unknown`.
        Funding: CardFunding
        ///Unique identifier for the object.
        Id: string
        ///Issuer identification number of the card. (For internal use only and not typically available in standard API requests.)
        Iin: string option
        ///The name of the card's issuing bank. (For internal use only and not typically available in standard API requests.)
        Issuer: string option
        ///The last four digits of the card.
        [<JsonField(Name="last4")>]Last4: string
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///Cardholder name.
        Name: string option
        Networks: TokenCardNetworks option
        ///Status of a card based on the card issuer.
        RegulatedStatus: CardRegulatedStatus option
        ///For external accounts that are cards, possible values are `new` and `errored`. If a payout fails, the status is set to `errored` and [scheduled payouts](https://stripe.com/docs/payouts#payout-schedule) are stopped until account details are updated.
        Status: CardStatus option
        ///If the card number is tokenized, this is the method that was used. Can be `android_pay` (includes Google Pay), `apple_pay`, `masterpass`, `visa_checkout`, or null.
        TokenizationMethod: CardTokenizationMethod option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "card"

        static member New (addressCity: string option, addressCountry: IsoTypes.IsoCountryCode option, addressLine1: string option, addressLine1Check: CardAddressLine1Check option, addressLine2: string option, addressState: string option, addressZip: string option, addressZipCheck: CardAddressZipCheck option, brand: CardBrand, country: IsoTypes.IsoCountryCode option, cvcCheck: CardCvcCheck option, dynamicLast4: string option, expMonth: int, expYear: int, funding: CardFunding, id: string, last4: string, metadata: Map<string, string> option, name: string option, regulatedStatus: CardRegulatedStatus option, tokenizationMethod: CardTokenizationMethod option, ?account: CardAccount'AnyOf option, ?allowRedisplay: CardAllowRedisplay option, ?availablePayoutMethods: CardAvailablePayoutMethods list option, ?currency: IsoTypes.IsoCurrencyCode option, ?customer: CardCustomer'AnyOf option, ?defaultForCurrency: bool option, ?description: string, ?fingerprint: string option, ?iin: string, ?issuer: string, ?networks: TokenCardNetworks, ?status: CardStatus option) =
            {
                Card.AddressCity = addressCity //required
                Card.AddressCountry = addressCountry //required
                Card.AddressLine1 = addressLine1 //required
                Card.AddressLine1Check = addressLine1Check //required
                Card.AddressLine2 = addressLine2 //required
                Card.AddressState = addressState //required
                Card.AddressZip = addressZip //required
                Card.AddressZipCheck = addressZipCheck //required
                Card.Brand = brand //required
                Card.Country = country //required
                Card.CvcCheck = cvcCheck //required
                Card.DynamicLast4 = dynamicLast4 //required
                Card.ExpMonth = expMonth //required
                Card.ExpYear = expYear //required
                Card.Funding = funding //required
                Card.Id = id //required
                Card.Last4 = last4 //required
                Card.Metadata = metadata //required
                Card.Name = name //required
                Card.RegulatedStatus = regulatedStatus //required
                Card.TokenizationMethod = tokenizationMethod //required
                Card.Account = account |> Option.flatten
                Card.AllowRedisplay = allowRedisplay |> Option.flatten
                Card.AvailablePayoutMethods = availablePayoutMethods |> Option.flatten
                Card.Currency = currency |> Option.flatten
                Card.Customer = customer |> Option.flatten
                Card.DefaultForCurrency = defaultForCurrency |> Option.flatten
                Card.Description = description
                Card.Fingerprint = fingerprint |> Option.flatten
                Card.Iin = iin
                Card.Issuer = issuer
                Card.Networks = networks
                Card.Status = status |> Option.flatten
            }

    and CardAccount'AnyOf =
        | String of string
        | Account of Account

    and CardCustomer'AnyOf =
        | String of string
        | Customer of Customer
        | DeletedCustomer of DeletedCustomer

    and CardGeneratedFromPaymentMethodDetails = {
        CardPresent: PaymentMethodDetailsCardPresent option
        ///The type of payment method transaction-specific details from the transaction that generated this `card` payment method. Always `card_present`.
        Type: string
    }
    with
        static member New (``type``: string, ?cardPresent: PaymentMethodDetailsCardPresent) =
            {
                CardGeneratedFromPaymentMethodDetails.Type = ``type`` //required
                CardGeneratedFromPaymentMethodDetails.CardPresent = cardPresent
            }

    ///A customer's `Cash balance` represents real funds. Customers can add funds to their cash balance by sending a bank transfer. These funds can be used for payment and can eventually be paid out to your bank account.
    and CashBalance = {
        ///A hash of all cash balances available to this customer. You cannot delete a customer with any cash balances, even if the balance is 0. Amounts are represented in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
        Available: Map<string, string list> option
        ///The ID of the customer whose cash balance this object represents.
        Customer: string
        ///The ID of an Account representing a customer whose cash balance this object represents.
        CustomerAccount: string option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        Settings: CustomerBalanceCustomerBalanceSettings
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "cash_balance"

        static member New (available: Map<string, string list> option, customer: string, customerAccount: string option, livemode: bool, settings: CustomerBalanceCustomerBalanceSettings) =
            {
                CashBalance.Available = available //required
                CashBalance.Customer = customer //required
                CashBalance.CustomerAccount = customerAccount //required
                CashBalance.Livemode = livemode //required
                CashBalance.Settings = settings //required
            }

    ///The `Charge` object represents a single attempt to move money into your Stripe account.
    ///PaymentIntent confirmation is the most common way to create Charges, but [Account Debits](https://docs.stripe.com/connect/account-debits) may also create Charges.
    ///Some legacy payment flows create Charges directly, which is not recommended for new integrations.
    and Charge = {
        ///Amount intended to be collected by this payment. A positive integer representing how much to charge in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal) (e.g., 100 cents to charge $1.00 or 100 to charge ¥100, a zero-decimal currency). The minimum amount is $0.50 US or [equivalent in charge currency](https://docs.stripe.com/currencies#minimum-and-maximum-charge-amounts). The amount value supports up to eight digits (e.g., a value of 99999999 for a USD charge of $999,999.99).
        Amount: int
        ///Amount in cents (or local equivalent) captured (can be less than the amount attribute on the charge if a partial capture was made).
        AmountCaptured: int
        ///Amount in cents (or local equivalent) refunded (can be less than the amount attribute on the charge if a partial refund was issued).
        AmountRefunded: int
        ///ID of the Connect application that created the charge.
        Application: ChargeApplication'AnyOf option
        ///The application fee (if any) for the charge. [See the Connect documentation](https://docs.stripe.com/connect/direct-charges#collect-fees) for details.
        ApplicationFee: ChargeApplicationFee'AnyOf option
        ///The amount of the application fee (if any) requested for the charge. [See the Connect documentation](https://docs.stripe.com/connect/direct-charges#collect-fees) for details.
        ApplicationFeeAmount: int option
        ///Authorization code on the charge.
        AuthorizationCode: string option
        ///ID of the balance transaction that describes the impact of this charge on your account balance (not including refunds or disputes).
        BalanceTransaction: ChargeBalanceTransaction'AnyOf option
        BillingDetails: BillingDetails
        ///The full statement descriptor that is passed to card networks, and that is displayed on your customers' credit card and bank statements. Allows you to see what the statement descriptor looks like after the static and dynamic portions are combined. This value only exists for card payments.
        CalculatedStatementDescriptor: string option
        ///If the charge was created without capturing, this Boolean represents whether it is still uncaptured or has since been captured.
        Captured: bool
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///ID of the customer this charge is for if one exists.
        Customer: ChargeCustomer'AnyOf option
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///Whether the charge has been disputed.
        Disputed: bool
        ///ID of the balance transaction that describes the reversal of the balance on your account due to payment failure.
        FailureBalanceTransaction: ChargeFailureBalanceTransaction'AnyOf option
        ///Error code explaining reason for charge failure if available (see [the errors section](https://docs.stripe.com/error-codes) for a list of codes).
        FailureCode: string option
        ///Message to user further explaining reason for charge failure if available.
        FailureMessage: string option
        ///Information on fraud assessments for the charge.
        FraudDetails: ChargeFraudDetails option
        ///Unique identifier for the object.
        Id: string
        [<JsonField(Name="level3")>]Level3: Level3 option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///The account (if any) the charge was made on behalf of without triggering an automatic transfer. See the [Connect documentation](https://docs.stripe.com/connect/separate-charges-and-transfers) for details.
        OnBehalfOf: ChargeOnBehalfOf'AnyOf option
        ///Details about whether the payment was accepted, and why. See [understanding declines](https://docs.stripe.com/declines) for details.
        Outcome: ChargeOutcome option
        ///`true` if the charge succeeded, or was successfully authorized for later capture.
        Paid: bool
        ///ID of the PaymentIntent associated with this charge, if one exists.
        PaymentIntent: ChargePaymentIntent'AnyOf option
        ///ID of the payment method used in this charge.
        PaymentMethod: string option
        ///Details about the payment method at the time of the transaction.
        PaymentMethodDetails: PaymentMethodDetails option
        PresentmentDetails: PaymentFlowsPaymentIntentPresentmentDetails option
        RadarOptions: RadarRadarOptions option
        ///This is the email address that the receipt for this charge was sent to.
        ReceiptEmail: string option
        ///This is the transaction number that appears on email receipts sent for this charge. This attribute will be `null` until a receipt has been sent.
        ReceiptNumber: string option
        ///This is the URL to view the receipt for this charge. The receipt is kept up-to-date to the latest state of the charge, including any refunds. If the charge is for an Invoice, the receipt will be stylized as an Invoice receipt.
        ReceiptUrl: string option
        ///Whether the charge has been fully refunded. If the charge is only partially refunded, this attribute will still be false.
        Refunded: bool
        ///A list of refunds that have been applied to the charge.
        Refunds: ChargeRefunds option
        ///ID of the review associated with this charge if one exists.
        Review: ChargeReview'AnyOf option
        ///Shipping information for the charge.
        Shipping: Shipping option
        ///This is a legacy field that will be removed in the future. It contains the Source, Card, or BankAccount object used for the charge. For details about the payment method used for this charge, refer to `payment_method` or `payment_method_details` instead.
        Source: PaymentSource option
        ///The transfer ID which created this charge. Only present if the charge came from another Stripe account. [See the Connect documentation](https://docs.stripe.com/connect/destination-charges) for details.
        SourceTransfer: ChargeSourceTransfer'AnyOf option
        ///For a non-card charge, text that appears on the customer's statement as the statement descriptor. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).
        ///For a card charge, this value is ignored unless you don't specify a `statement_descriptor_suffix`, in which case this value is used as the suffix.
        StatementDescriptor: string option
        ///Provides information about a card charge. Concatenated to the account's [statement descriptor prefix](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the customer's statement. If the account has no prefix value, the suffix is concatenated to the account's statement descriptor.
        StatementDescriptorSuffix: string option
        ///The status of the payment is either `succeeded`, `pending`, or `failed`.
        Status: ChargeStatus
        ///ID of the transfer to the `destination` account (only applicable if the charge was created using the `destination` parameter).
        Transfer: ChargeTransfer'AnyOf option
        ///An optional dictionary including the account to automatically transfer to as part of a destination charge. [See the Connect documentation](https://docs.stripe.com/connect/destination-charges) for details.
        TransferData: ChargeTransferData option
        ///A string that identifies this transaction as part of a group. See the [Connect documentation](https://docs.stripe.com/connect/separate-charges-and-transfers#transfer-options) for details.
        TransferGroup: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "charge"

        static member New (amount: int, amountCaptured: int, amountRefunded: int, application: ChargeApplication'AnyOf option, applicationFee: ChargeApplicationFee'AnyOf option, applicationFeeAmount: int option, balanceTransaction: ChargeBalanceTransaction'AnyOf option, billingDetails: BillingDetails, calculatedStatementDescriptor: string option, captured: bool, created: DateTime, currency: IsoTypes.IsoCurrencyCode, customer: ChargeCustomer'AnyOf option, description: string option, disputed: bool, failureBalanceTransaction: ChargeFailureBalanceTransaction'AnyOf option, failureCode: string option, failureMessage: string option, fraudDetails: ChargeFraudDetails option, id: string, livemode: bool, metadata: Map<string, string>, onBehalfOf: ChargeOnBehalfOf'AnyOf option, outcome: ChargeOutcome option, paid: bool, paymentIntent: ChargePaymentIntent'AnyOf option, paymentMethod: string option, paymentMethodDetails: PaymentMethodDetails option, receiptEmail: string option, receiptNumber: string option, receiptUrl: string option, refunded: bool, review: ChargeReview'AnyOf option, shipping: Shipping option, source: PaymentSource option, sourceTransfer: ChargeSourceTransfer'AnyOf option, statementDescriptor: string option, statementDescriptorSuffix: string option, status: ChargeStatus, transferData: ChargeTransferData option, transferGroup: string option, ?authorizationCode: string, ?level3: Level3, ?presentmentDetails: PaymentFlowsPaymentIntentPresentmentDetails, ?radarOptions: RadarRadarOptions, ?refunds: ChargeRefunds option, ?transfer: ChargeTransfer'AnyOf) =
            {
                Charge.Amount = amount //required
                Charge.AmountCaptured = amountCaptured //required
                Charge.AmountRefunded = amountRefunded //required
                Charge.Application = application //required
                Charge.ApplicationFee = applicationFee //required
                Charge.ApplicationFeeAmount = applicationFeeAmount //required
                Charge.BalanceTransaction = balanceTransaction //required
                Charge.BillingDetails = billingDetails //required
                Charge.CalculatedStatementDescriptor = calculatedStatementDescriptor //required
                Charge.Captured = captured //required
                Charge.Created = created //required
                Charge.Currency = currency //required
                Charge.Customer = customer //required
                Charge.Description = description //required
                Charge.Disputed = disputed //required
                Charge.FailureBalanceTransaction = failureBalanceTransaction //required
                Charge.FailureCode = failureCode //required
                Charge.FailureMessage = failureMessage //required
                Charge.FraudDetails = fraudDetails //required
                Charge.Id = id //required
                Charge.Livemode = livemode //required
                Charge.Metadata = metadata //required
                Charge.OnBehalfOf = onBehalfOf //required
                Charge.Outcome = outcome //required
                Charge.Paid = paid //required
                Charge.PaymentIntent = paymentIntent //required
                Charge.PaymentMethod = paymentMethod //required
                Charge.PaymentMethodDetails = paymentMethodDetails //required
                Charge.ReceiptEmail = receiptEmail //required
                Charge.ReceiptNumber = receiptNumber //required
                Charge.ReceiptUrl = receiptUrl //required
                Charge.Refunded = refunded //required
                Charge.Review = review //required
                Charge.Shipping = shipping //required
                Charge.Source = source //required
                Charge.SourceTransfer = sourceTransfer //required
                Charge.StatementDescriptor = statementDescriptor //required
                Charge.StatementDescriptorSuffix = statementDescriptorSuffix //required
                Charge.Status = status //required
                Charge.TransferData = transferData //required
                Charge.TransferGroup = transferGroup //required
                Charge.AuthorizationCode = authorizationCode
                Charge.Level3 = level3
                Charge.PresentmentDetails = presentmentDetails
                Charge.RadarOptions = radarOptions
                Charge.Refunds = refunds |> Option.flatten
                Charge.Transfer = transfer
            }

    and ChargeApplication'AnyOf =
        | String of string
        | Application of Application

    and ChargeApplicationFee'AnyOf =
        | String of string
        | ApplicationFee of ApplicationFee

    and ChargeBalanceTransaction'AnyOf =
        | String of string
        | BalanceTransaction of BalanceTransaction

    and ChargeCustomer'AnyOf =
        | String of string
        | Customer of Customer
        | DeletedCustomer of DeletedCustomer

    and ChargeFailureBalanceTransaction'AnyOf =
        | String of string
        | BalanceTransaction of BalanceTransaction

    and ChargeOnBehalfOf'AnyOf =
        | String of string
        | Account of Account

    and ChargePaymentIntent'AnyOf =
        | String of string
        | PaymentIntent of PaymentIntent

    and ChargeReview'AnyOf =
        | String of string
        | Review of Review

    and ChargeSourceTransfer'AnyOf =
        | String of string
        | Transfer of Transfer

    and ChargeTransfer'AnyOf =
        | String of string
        | Transfer of Transfer

    ///A list of refunds that have been applied to the charge.
    and ChargeRefunds = {
        ///Details about each object.
        Data: Refund list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: Refund list, hasMore: bool, url: string) =
            {
                ChargeRefunds.Data = data //required
                ChargeRefunds.HasMore = hasMore //required
                ChargeRefunds.Url = url //required
            }

    and ChargeFraudDetails = {
        ///Assessments from Stripe. If set, the value is `fraudulent`.
        StripeReport: string option
        ///Assessments reported by you. If set, possible values of are `safe` and `fraudulent`.
        UserReport: ChargeFraudDetailsUserReport option
    }
    with
        static member New (?stripeReport: string, ?userReport: ChargeFraudDetailsUserReport) =
            {
                ChargeFraudDetails.StripeReport = stripeReport
                ChargeFraudDetails.UserReport = userReport
            }

    and ChargeOutcome = {
        ///An enumerated value providing a more detailed explanation on [how to proceed with an error](https://docs.stripe.com/declines#retrying-issuer-declines).
        AdviceCode: ChargeOutcomeAdviceCode option
        ///For charges declined by the network, a 2 digit code which indicates the advice returned by the network on how to proceed with an error.
        NetworkAdviceCode: string option
        ///For charges declined by the network, an alphanumeric code which indicates the reason the charge failed.
        NetworkDeclineCode: string option
        ///Possible values are `approved_by_network`, `declined_by_network`, `not_sent_to_network`, and `reversed_after_approval`. The value `reversed_after_approval` indicates the payment was [blocked by Stripe](https://docs.stripe.com/declines#blocked-payments) after bank authorization, and may temporarily appear as "pending" on a cardholder's statement.
        NetworkStatus: ChargeOutcomeNetworkStatus option
        ///An enumerated value providing a more detailed explanation of the outcome's `type`. Charges blocked by Radar's default block rule have the value `highest_risk_level`. Charges placed in review by Radar's default review rule have the value `elevated_risk_level`. Charges blocked because the payment is unlikely to be authorized have the value `low_probability_of_authorization`. Charges authorized, blocked, or placed in review by custom rules have the value `rule`. See [understanding declines](https://docs.stripe.com/declines) for more details.
        Reason: string option
        ///Stripe Radar's evaluation of the riskiness of the payment. Possible values for evaluated payments are `normal`, `elevated`, `highest`. For non-card payments, and card-based payments predating the public assignment of risk levels, this field will have the value `not_assessed`. In the event of an error in the evaluation, this field will have the value `unknown`. This field is only available with Radar.
        RiskLevel: string option
        ///Stripe Radar's evaluation of the riskiness of the payment. Possible values for evaluated payments are between 0 and 100. For non-card payments, card-based payments predating the public assignment of risk scores, or in the event of an error during evaluation, this field will not be present. This field is only available with Radar for Fraud Teams.
        RiskScore: int option
        ///The ID of the Radar rule that matched the payment, if applicable.
        Rule: ChargeOutcomeRule'AnyOf option
        ///A human-readable description of the outcome type and reason, designed for you (the recipient of the payment), not your customer.
        SellerMessage: string option
        ///Possible values are `authorized`, `manual_review`, `issuer_declined`, `blocked`, and `invalid`. See [understanding declines](https://docs.stripe.com/declines) and [Radar reviews](https://docs.stripe.com/radar/reviews) for details.
        Type: ChargeOutcomeType
    }
    with
        static member New (adviceCode: ChargeOutcomeAdviceCode option, networkAdviceCode: string option, networkDeclineCode: string option, networkStatus: ChargeOutcomeNetworkStatus option, reason: string option, sellerMessage: string option, ``type``: ChargeOutcomeType, ?riskLevel: string, ?riskScore: int, ?rule: ChargeOutcomeRule'AnyOf) =
            {
                ChargeOutcome.AdviceCode = adviceCode //required
                ChargeOutcome.NetworkAdviceCode = networkAdviceCode //required
                ChargeOutcome.NetworkDeclineCode = networkDeclineCode //required
                ChargeOutcome.NetworkStatus = networkStatus //required
                ChargeOutcome.Reason = reason //required
                ChargeOutcome.SellerMessage = sellerMessage //required
                ChargeOutcome.Type = ``type`` //required
                ChargeOutcome.RiskLevel = riskLevel
                ChargeOutcome.RiskScore = riskScore
                ChargeOutcome.Rule = rule
            }

    and ChargeOutcomeRule'AnyOf =
        | String of string
        | Rule of Rule

    and ChargeTransferData = {
        ///The amount transferred to the destination account, if specified. By default, the entire charge amount is transferred to the destination account.
        Amount: int option
        ///ID of an existing, connected Stripe account to transfer funds to if `transfer_data` was specified in the charge request.
        Destination: ChargeTransferDataDestination'AnyOf
    }
    with
        static member New (amount: int option, destination: ChargeTransferDataDestination'AnyOf) =
            {
                ChargeTransferData.Amount = amount //required
                ChargeTransferData.Destination = destination //required
            }

    and ChargeTransferDataDestination'AnyOf =
        | String of string
        | Account of Account

    ///A Checkout Session represents your customer's session as they pay for
    ///one-time purchases or subscriptions through [Checkout](https://docs.stripe.com/payments/checkout)
    ///or [Payment Links](https://docs.stripe.com/payments/payment-links). We recommend creating a
    ///new Session each time your customer attempts to pay.
    ///Once payment is successful, the Checkout Session will contain a reference
    ///to the [Customer](https://docs.stripe.com/api/customers), and either the successful
    ///[PaymentIntent](https://docs.stripe.com/api/payment_intents) or an active
    ///[Subscription](https://docs.stripe.com/api/subscriptions).
    ///You can create a Checkout Session on your server and redirect to its URL
    ///to begin Checkout.
    ///Related guide: [Checkout quickstart](https://docs.stripe.com/checkout/quickstart)
    and CheckoutSession = {
        ///Settings for price localization with [Adaptive Pricing](https://docs.stripe.com/payments/checkout/adaptive-pricing).
        AdaptivePricing: PaymentPagesCheckoutSessionAdaptivePricing option
        ///When set, provides configuration for actions to take if this Checkout Session expires.
        AfterExpiration: PaymentPagesCheckoutSessionAfterExpiration option
        ///Enables user redeemable promotion codes.
        AllowPromotionCodes: bool option
        ///Total of all items before discounts or taxes are applied.
        AmountSubtotal: int option
        ///Total of all items after discounts and taxes are applied.
        AmountTotal: int option
        AutomaticTax: PaymentPagesCheckoutSessionAutomaticTax
        ///Describes whether Checkout should collect the customer's billing address. Defaults to `auto`.
        BillingAddressCollection: CheckoutSessionBillingAddressCollection option
        BrandingSettings: PaymentPagesCheckoutSessionBrandingSettings option
        ///If set, Checkout displays a back button and customers will be directed to this URL if they decide to cancel payment and return to your website.
        CancelUrl: string option
        ///A unique string to reference the Checkout Session. This can be a
        ///customer ID, a cart ID, or similar, and can be used to reconcile the
        ///Session with your internal systems.
        ClientReferenceId: string option
        ///The client secret of your Checkout Session. Applies to Checkout Sessions with `ui_mode: embedded` or `ui_mode: custom`. For `ui_mode: embedded`, the client secret is to be used when initializing Stripe.js embedded checkout.
        /// For `ui_mode: custom`, use the client secret with [initCheckout](https://docs.stripe.com/js/custom_checkout/init) on your front end.
        ClientSecret: string option
        ///Information about the customer collected within the Checkout Session.
        CollectedInformation: PaymentPagesCheckoutSessionCollectedInformation option
        ///Results of `consent_collection` for this session.
        Consent: PaymentPagesCheckoutSessionConsent option
        ///When set, provides configuration for the Checkout Session to gather active consent from customers.
        ConsentCollection: PaymentPagesCheckoutSessionConsentCollection option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode option
        ///Currency conversion details for [Adaptive Pricing](https://docs.stripe.com/payments/checkout/adaptive-pricing) sessions created before 2025-03-31.
        CurrencyConversion: PaymentPagesCheckoutSessionCurrencyConversion option
        ///Collect additional information from your customer using custom fields. Up to 3 fields are supported. You can't set this parameter if `ui_mode` is `custom`.
        CustomFields: PaymentPagesCheckoutSessionCustomFields list
        CustomText: PaymentPagesCheckoutSessionCustomText
        ///The ID of the customer for this Session.
        ///For Checkout Sessions in `subscription` mode or Checkout Sessions with `customer_creation` set as `always` in `payment` mode, Checkout
        ///will create a new customer object based on information provided
        ///during the payment flow unless an existing customer was provided when
        ///the Session was created.
        Customer: CheckoutSessionCustomer'AnyOf option
        ///The ID of the account for this Session.
        CustomerAccount: string option
        ///Configure whether a Checkout Session creates a Customer when the Checkout Session completes.
        CustomerCreation: CheckoutSessionCustomerCreation option
        ///The customer details including the customer's tax exempt status and the customer's tax IDs. Customer's address details are not present on Sessions in `setup` mode.
        CustomerDetails: PaymentPagesCheckoutSessionCustomerDetails option
        ///If provided, this value will be used when the Customer object is created.
        ///If not provided, customers will be asked to enter their email address.
        ///Use this parameter to prefill customer data if you already have an email
        ///on file. To access information about the customer once the payment flow is
        ///complete, use the `customer` attribute.
        CustomerEmail: string option
        ///List of coupons and promotion codes attached to the Checkout Session.
        Discounts: PaymentPagesCheckoutSessionDiscount list option
        ///A list of the types of payment methods (e.g., `card`) that should be excluded from this Checkout Session. This should only be used when payment methods for this Checkout Session are managed through the [Stripe Dashboard](https://dashboard.stripe.com/settings/payment_methods).
        ExcludedPaymentMethodTypes: string list option
        ///The timestamp at which the Checkout Session will expire.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime
        ///Unique identifier for the object.
        Id: string
        ///The integration identifier for this Checkout Session. Multiple Checkout Sessions can have the same integration identifier.
        IntegrationIdentifier: string option
        ///ID of the invoice created by the Checkout Session, if it exists.
        Invoice: CheckoutSessionInvoice'AnyOf option
        ///Details on the state of invoice creation for the Checkout Session.
        InvoiceCreation: PaymentPagesCheckoutSessionInvoiceCreation option
        ///The line items purchased by the customer.
        LineItems: CheckoutSessionLineItems option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The IETF language tag of the locale Checkout is displayed in. If blank or `auto`, the browser's locale is used.
        Locale: CheckoutSessionLocale option
        ///Settings for Managed Payments for this Checkout Session and resulting [PaymentIntents](/api/payment_intents/object), [Invoices](/api/invoices/object), and [Subscriptions](/api/subscriptions/object).
        ManagedPayments: PaymentPagesCheckoutSessionManagedPayments option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///The mode of the Checkout Session.
        Mode: CheckoutSessionMode
        NameCollection: PaymentPagesCheckoutSessionNameCollection option
        ///The optional items presented to the customer at checkout.
        OptionalItems: PaymentPagesCheckoutSessionOptionalItem list option
        ///Where the user is coming from. This informs the optimizations that are applied to the session.
        OriginContext: CheckoutSessionOriginContext option
        ///The ID of the PaymentIntent for Checkout Sessions in `payment` mode. You can't confirm or cancel the PaymentIntent for a Checkout Session. To cancel, [expire the Checkout Session](https://docs.stripe.com/api/checkout/sessions/expire) instead.
        PaymentIntent: CheckoutSessionPaymentIntent'AnyOf option
        ///The ID of the Payment Link that created this Session.
        PaymentLink: CheckoutSessionPaymentLink'AnyOf option
        ///Configure whether a Checkout Session should collect a payment method. Defaults to `always`.
        PaymentMethodCollection: CheckoutSessionPaymentMethodCollection option
        ///Information about the payment method configuration used for this Checkout session if using dynamic payment methods.
        PaymentMethodConfigurationDetails: PaymentMethodConfigBizPaymentMethodConfigurationDetails option
        ///Payment-method-specific configuration for the PaymentIntent or SetupIntent of this CheckoutSession.
        PaymentMethodOptions: CheckoutSessionPaymentMethodOptions option
        ///A list of the types of payment methods (e.g. card) this Checkout
        ///Session is allowed to accept.
        PaymentMethodTypes: string list
        ///The payment status of the Checkout Session, one of `paid`, `unpaid`, or `no_payment_required`.
        ///You can use this value to decide when to fulfill your customer's order.
        PaymentStatus: CheckoutSessionPaymentStatus
        ///This property is used to set up permissions for various actions (e.g., update) on the CheckoutSession object.
        ///For specific permissions, please refer to their dedicated subsections, such as `permissions.update_shipping_details`.
        Permissions: PaymentPagesCheckoutSessionPermissions option
        PhoneNumberCollection: PaymentPagesCheckoutSessionPhoneNumberCollection option
        PresentmentDetails: PaymentFlowsPaymentIntentPresentmentDetails option
        ///The ID of the original expired Checkout Session that triggered the recovery flow.
        RecoveredFrom: string option
        ///This parameter applies to `ui_mode: embedded`. Learn more about the [redirect behavior](https://docs.stripe.com/payments/checkout/custom-success-page?payment-ui=embedded-form) of embedded sessions. Defaults to `always`.
        RedirectOnCompletion: CheckoutSessionRedirectOnCompletion option
        ///Applies to Checkout Sessions with `ui_mode: embedded` or `ui_mode: custom`. The URL to redirect your customer back to after they authenticate or cancel their payment on the payment method's app or site.
        ReturnUrl: string option
        ///Controls saved payment method settings for the session. Only available in `payment` and `subscription` mode.
        SavedPaymentMethodOptions: PaymentPagesCheckoutSessionSavedPaymentMethodOptions option
        ///The ID of the SetupIntent for Checkout Sessions in `setup` mode. You can't confirm or cancel the SetupIntent for a Checkout Session. To cancel, [expire the Checkout Session](https://docs.stripe.com/api/checkout/sessions/expire) instead.
        SetupIntent: CheckoutSessionSetupIntent'AnyOf option
        ///When set, provides configuration for Checkout to collect a shipping address from a customer.
        ShippingAddressCollection: PaymentPagesCheckoutSessionShippingAddressCollection option
        ///The details of the customer cost of shipping, including the customer chosen ShippingRate.
        ShippingCost: PaymentPagesCheckoutSessionShippingCost option
        ///The shipping rate options applied to this Session.
        ShippingOptions: PaymentPagesCheckoutSessionShippingOption list
        ///The status of the Checkout Session, one of `open`, `complete`, or `expired`.
        Status: CheckoutSessionStatus option
        ///Describes the type of transaction being performed by Checkout in order to customize
        ///relevant text on the page, such as the submit button. `submit_type` can only be
        ///specified on Checkout Sessions in `payment` mode. If blank or `auto`, `pay` is used.
        SubmitType: CheckoutSessionSubmitType option
        ///The ID of the [Subscription](https://docs.stripe.com/api/subscriptions) for Checkout Sessions in `subscription` mode.
        Subscription: CheckoutSessionSubscription'AnyOf option
        ///The URL the customer will be directed to after the payment or
        ///subscription creation is successful.
        SuccessUrl: string option
        TaxIdCollection: PaymentPagesCheckoutSessionTaxIdCollection option
        ///Tax and discount details for the computed total amount.
        TotalDetails: PaymentPagesCheckoutSessionTotalDetails option
        ///The UI mode of the Session. Defaults to `hosted_page`.
        UiMode: CheckoutSessionUiMode option
        ///The URL to the Checkout Session. Applies to Checkout Sessions with `ui_mode: hosted`. Redirect customers to this URL to take them to Checkout. If you’re using [Custom Domains](https://docs.stripe.com/payments/checkout/custom-domains), the URL will use your subdomain. Otherwise, it’ll use `checkout.stripe.com.`
        ///This value is only present when the session is active.
        Url: string option
        ///Wallet-specific configuration for this Checkout Session.
        WalletOptions: CheckoutSessionWalletOptions option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "checkout.session"

        static member New (adaptivePricing: PaymentPagesCheckoutSessionAdaptivePricing option, afterExpiration: PaymentPagesCheckoutSessionAfterExpiration option, allowPromotionCodes: bool option, amountSubtotal: int option, amountTotal: int option, automaticTax: PaymentPagesCheckoutSessionAutomaticTax, billingAddressCollection: CheckoutSessionBillingAddressCollection option, cancelUrl: string option, clientReferenceId: string option, clientSecret: string option, collectedInformation: PaymentPagesCheckoutSessionCollectedInformation option, consent: PaymentPagesCheckoutSessionConsent option, consentCollection: PaymentPagesCheckoutSessionConsentCollection option, created: DateTime, currency: IsoTypes.IsoCurrencyCode option, currencyConversion: PaymentPagesCheckoutSessionCurrencyConversion option, customFields: PaymentPagesCheckoutSessionCustomFields list, customText: PaymentPagesCheckoutSessionCustomText, customer: CheckoutSessionCustomer'AnyOf option, customerAccount: string option, customerCreation: CheckoutSessionCustomerCreation option, customerDetails: PaymentPagesCheckoutSessionCustomerDetails option, customerEmail: string option, discounts: PaymentPagesCheckoutSessionDiscount list option, expiresAt: DateTime, id: string, integrationIdentifier: string option, invoice: CheckoutSessionInvoice'AnyOf option, invoiceCreation: PaymentPagesCheckoutSessionInvoiceCreation option, livemode: bool, locale: CheckoutSessionLocale option, managedPayments: PaymentPagesCheckoutSessionManagedPayments option, metadata: Map<string, string> option, mode: CheckoutSessionMode, originContext: CheckoutSessionOriginContext option, paymentIntent: CheckoutSessionPaymentIntent'AnyOf option, paymentLink: CheckoutSessionPaymentLink'AnyOf option, paymentMethodCollection: CheckoutSessionPaymentMethodCollection option, paymentMethodConfigurationDetails: PaymentMethodConfigBizPaymentMethodConfigurationDetails option, paymentMethodOptions: CheckoutSessionPaymentMethodOptions option, paymentMethodTypes: string list, paymentStatus: CheckoutSessionPaymentStatus, permissions: PaymentPagesCheckoutSessionPermissions option, recoveredFrom: string option, savedPaymentMethodOptions: PaymentPagesCheckoutSessionSavedPaymentMethodOptions option, setupIntent: CheckoutSessionSetupIntent'AnyOf option, shippingAddressCollection: PaymentPagesCheckoutSessionShippingAddressCollection option, shippingCost: PaymentPagesCheckoutSessionShippingCost option, shippingOptions: PaymentPagesCheckoutSessionShippingOption list, status: CheckoutSessionStatus option, submitType: CheckoutSessionSubmitType option, subscription: CheckoutSessionSubscription'AnyOf option, successUrl: string option, totalDetails: PaymentPagesCheckoutSessionTotalDetails option, uiMode: CheckoutSessionUiMode option, url: string option, walletOptions: CheckoutSessionWalletOptions option, ?brandingSettings: PaymentPagesCheckoutSessionBrandingSettings, ?excludedPaymentMethodTypes: string list, ?lineItems: CheckoutSessionLineItems, ?nameCollection: PaymentPagesCheckoutSessionNameCollection, ?optionalItems: PaymentPagesCheckoutSessionOptionalItem list option, ?phoneNumberCollection: PaymentPagesCheckoutSessionPhoneNumberCollection, ?presentmentDetails: PaymentFlowsPaymentIntentPresentmentDetails, ?redirectOnCompletion: CheckoutSessionRedirectOnCompletion, ?returnUrl: string, ?taxIdCollection: PaymentPagesCheckoutSessionTaxIdCollection) =
            {
                CheckoutSession.AdaptivePricing = adaptivePricing //required
                CheckoutSession.AfterExpiration = afterExpiration //required
                CheckoutSession.AllowPromotionCodes = allowPromotionCodes //required
                CheckoutSession.AmountSubtotal = amountSubtotal //required
                CheckoutSession.AmountTotal = amountTotal //required
                CheckoutSession.AutomaticTax = automaticTax //required
                CheckoutSession.BillingAddressCollection = billingAddressCollection //required
                CheckoutSession.CancelUrl = cancelUrl //required
                CheckoutSession.ClientReferenceId = clientReferenceId //required
                CheckoutSession.ClientSecret = clientSecret //required
                CheckoutSession.CollectedInformation = collectedInformation //required
                CheckoutSession.Consent = consent //required
                CheckoutSession.ConsentCollection = consentCollection //required
                CheckoutSession.Created = created //required
                CheckoutSession.Currency = currency //required
                CheckoutSession.CurrencyConversion = currencyConversion //required
                CheckoutSession.CustomFields = customFields //required
                CheckoutSession.CustomText = customText //required
                CheckoutSession.Customer = customer //required
                CheckoutSession.CustomerAccount = customerAccount //required
                CheckoutSession.CustomerCreation = customerCreation //required
                CheckoutSession.CustomerDetails = customerDetails //required
                CheckoutSession.CustomerEmail = customerEmail //required
                CheckoutSession.Discounts = discounts //required
                CheckoutSession.ExpiresAt = expiresAt //required
                CheckoutSession.Id = id //required
                CheckoutSession.IntegrationIdentifier = integrationIdentifier //required
                CheckoutSession.Invoice = invoice //required
                CheckoutSession.InvoiceCreation = invoiceCreation //required
                CheckoutSession.Livemode = livemode //required
                CheckoutSession.Locale = locale //required
                CheckoutSession.ManagedPayments = managedPayments //required
                CheckoutSession.Metadata = metadata //required
                CheckoutSession.Mode = mode //required
                CheckoutSession.OriginContext = originContext //required
                CheckoutSession.PaymentIntent = paymentIntent //required
                CheckoutSession.PaymentLink = paymentLink //required
                CheckoutSession.PaymentMethodCollection = paymentMethodCollection //required
                CheckoutSession.PaymentMethodConfigurationDetails = paymentMethodConfigurationDetails //required
                CheckoutSession.PaymentMethodOptions = paymentMethodOptions //required
                CheckoutSession.PaymentMethodTypes = paymentMethodTypes //required
                CheckoutSession.PaymentStatus = paymentStatus //required
                CheckoutSession.Permissions = permissions //required
                CheckoutSession.RecoveredFrom = recoveredFrom //required
                CheckoutSession.SavedPaymentMethodOptions = savedPaymentMethodOptions //required
                CheckoutSession.SetupIntent = setupIntent //required
                CheckoutSession.ShippingAddressCollection = shippingAddressCollection //required
                CheckoutSession.ShippingCost = shippingCost //required
                CheckoutSession.ShippingOptions = shippingOptions //required
                CheckoutSession.Status = status //required
                CheckoutSession.SubmitType = submitType //required
                CheckoutSession.Subscription = subscription //required
                CheckoutSession.SuccessUrl = successUrl //required
                CheckoutSession.TotalDetails = totalDetails //required
                CheckoutSession.UiMode = uiMode //required
                CheckoutSession.Url = url //required
                CheckoutSession.WalletOptions = walletOptions //required
                CheckoutSession.BrandingSettings = brandingSettings
                CheckoutSession.ExcludedPaymentMethodTypes = excludedPaymentMethodTypes
                CheckoutSession.LineItems = lineItems
                CheckoutSession.NameCollection = nameCollection
                CheckoutSession.OptionalItems = optionalItems |> Option.flatten
                CheckoutSession.PhoneNumberCollection = phoneNumberCollection
                CheckoutSession.PresentmentDetails = presentmentDetails
                CheckoutSession.RedirectOnCompletion = redirectOnCompletion
                CheckoutSession.ReturnUrl = returnUrl
                CheckoutSession.TaxIdCollection = taxIdCollection
            }

    and CheckoutSessionCustomer'AnyOf =
        | String of string
        | Customer of Customer
        | DeletedCustomer of DeletedCustomer

    and CheckoutSessionInvoice'AnyOf =
        | String of string
        | Invoice of Invoice

    and CheckoutSessionPaymentIntent'AnyOf =
        | String of string
        | PaymentIntent of PaymentIntent

    and CheckoutSessionPaymentLink'AnyOf =
        | String of string
        | PaymentLink of PaymentLink

    and CheckoutSessionSetupIntent'AnyOf =
        | String of string
        | SetupIntent of SetupIntent

    and CheckoutSessionSubscription'AnyOf =
        | String of string
        | Subscription of Subscription

    ///The line items purchased by the customer.
    and CheckoutSessionLineItems = {
        ///Details about each object.
        Data: Item list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: Item list, hasMore: bool, url: string) =
            {
                CheckoutSessionLineItems.Data = data //required
                CheckoutSessionLineItems.HasMore = hasMore //required
                CheckoutSessionLineItems.Url = url //required
            }

    and CheckoutAcssDebitMandateOptions = {
        ///A URL for custom mandate text
        CustomMandateUrl: string option
        ///List of Stripe products where this mandate can be selected automatically. Returned when the Session is in `setup` mode.
        DefaultFor: CheckoutAcssDebitMandateOptionsDefaultFor list option
        ///Description of the interval. Only required if the 'payment_schedule' parameter is 'interval' or 'combined'.
        IntervalDescription: string option
        ///Payment schedule for the mandate.
        PaymentSchedule: CheckoutAcssDebitMandateOptionsPaymentSchedule option
        ///Transaction type of the mandate.
        TransactionType: CheckoutAcssDebitMandateOptionsTransactionType option
    }
    with
        static member New (intervalDescription: string option, paymentSchedule: CheckoutAcssDebitMandateOptionsPaymentSchedule option, transactionType: CheckoutAcssDebitMandateOptionsTransactionType option, ?customMandateUrl: string, ?defaultFor: CheckoutAcssDebitMandateOptionsDefaultFor list) =
            {
                CheckoutAcssDebitMandateOptions.IntervalDescription = intervalDescription //required
                CheckoutAcssDebitMandateOptions.PaymentSchedule = paymentSchedule //required
                CheckoutAcssDebitMandateOptions.TransactionType = transactionType //required
                CheckoutAcssDebitMandateOptions.CustomMandateUrl = customMandateUrl
                CheckoutAcssDebitMandateOptions.DefaultFor = defaultFor
            }

    and CheckoutAcssDebitPaymentMethodOptions = {
        ///Currency supported by the bank account. Returned when the Session is in `setup` mode.
        Currency: CheckoutAcssDebitPaymentMethodOptionsCurrency option
        MandateOptions: CheckoutAcssDebitMandateOptions option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: CheckoutAcssDebitPaymentMethodOptionsSetupFutureUsage option
        ///Controls when Stripe will attempt to debit the funds from the customer's account. The date must be a string in YYYY-MM-DD format. The date must be in the future and between 3 and 15 calendar days from now.
        TargetDate: string option
        ///Bank account verification method. The default value is `automatic`.
        VerificationMethod: CheckoutAcssDebitPaymentMethodOptionsVerificationMethod option
    }
    with
        static member New (?currency: CheckoutAcssDebitPaymentMethodOptionsCurrency, ?mandateOptions: CheckoutAcssDebitMandateOptions, ?setupFutureUsage: CheckoutAcssDebitPaymentMethodOptionsSetupFutureUsage, ?targetDate: string, ?verificationMethod: CheckoutAcssDebitPaymentMethodOptionsVerificationMethod) =
            {
                CheckoutAcssDebitPaymentMethodOptions.Currency = currency
                CheckoutAcssDebitPaymentMethodOptions.MandateOptions = mandateOptions
                CheckoutAcssDebitPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
                CheckoutAcssDebitPaymentMethodOptions.TargetDate = targetDate
                CheckoutAcssDebitPaymentMethodOptions.VerificationMethod = verificationMethod
            }

    and CheckoutAmazonPayPaymentMethodOptions = {
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: CheckoutAmazonPayPaymentMethodOptionsSetupFutureUsage option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (?setupFutureUsage: CheckoutAmazonPayPaymentMethodOptionsSetupFutureUsage) =
            {
                CheckoutAmazonPayPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
            }

    and CheckoutBacsDebitPaymentMethodOptions = {
        MandateOptions: CheckoutPaymentMethodOptionsMandateOptionsBacsDebit option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: CheckoutBacsDebitPaymentMethodOptionsSetupFutureUsage option
        ///Controls when Stripe will attempt to debit the funds from the customer's account. The date must be a string in YYYY-MM-DD format. The date must be in the future and between 3 and 15 calendar days from now.
        TargetDate: string option
    }
    with
        static member New (?mandateOptions: CheckoutPaymentMethodOptionsMandateOptionsBacsDebit, ?setupFutureUsage: CheckoutBacsDebitPaymentMethodOptionsSetupFutureUsage, ?targetDate: string) =
            {
                CheckoutBacsDebitPaymentMethodOptions.MandateOptions = mandateOptions
                CheckoutBacsDebitPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
                CheckoutBacsDebitPaymentMethodOptions.TargetDate = targetDate
            }

    and CheckoutBoletoPaymentMethodOptions = {
        ///The number of calendar days before a Boleto voucher expires. For example, if you create a Boleto voucher on Monday and you set expires_after_days to 2, the Boleto voucher will expire on Wednesday at 23:59 America/Sao_Paulo time.
        ExpiresAfterDays: int
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: CheckoutBoletoPaymentMethodOptionsSetupFutureUsage option
    }
    with
        static member New (expiresAfterDays: int, ?setupFutureUsage: CheckoutBoletoPaymentMethodOptionsSetupFutureUsage) =
            {
                CheckoutBoletoPaymentMethodOptions.ExpiresAfterDays = expiresAfterDays //required
                CheckoutBoletoPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
            }

    and CheckoutCardPaymentMethodOptions = {
        Installments: CheckoutCardInstallmentsOptions option
        ///Request ability to [capture beyond the standard authorization validity window](/payments/extended-authorization) for this CheckoutSession.
        RequestExtendedAuthorization: CheckoutCardPaymentMethodOptionsRequestExtendedAuthorization option
        ///Request ability to [increment the authorization](/payments/incremental-authorization) for this CheckoutSession.
        RequestIncrementalAuthorization: CheckoutCardPaymentMethodOptionsRequestIncrementalAuthorization option
        ///Request ability to make [multiple captures](/payments/multicapture) for this CheckoutSession.
        RequestMulticapture: CheckoutCardPaymentMethodOptionsRequestMulticapture option
        ///Request ability to [overcapture](/payments/overcapture) for this CheckoutSession.
        RequestOvercapture: CheckoutCardPaymentMethodOptionsRequestOvercapture option
        ///We strongly recommend that you rely on our SCA Engine to automatically prompt your customers for authentication based on risk level and [other requirements](https://docs.stripe.com/strong-customer-authentication). However, if you wish to request 3D Secure based on logic from your own fraud engine, provide this option. If not provided, this value defaults to `automatic`. Read our guide on [manually requesting 3D Secure](https://docs.stripe.com/payments/3d-secure/authentication-flow#manual-three-ds) for more information on how this configuration interacts with Radar and our SCA Engine.
        RequestThreeDSecure: CheckoutCardPaymentMethodOptionsRequestThreeDSecure
        Restrictions: PaymentPagesPrivateCardPaymentMethodOptionsResourceRestrictions option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: CheckoutCardPaymentMethodOptionsSetupFutureUsage option
        ///Provides information about a card payment that customers see on their statements. Concatenated with the Kana prefix (shortened Kana descriptor) or Kana statement descriptor that’s set on the account to form the complete statement descriptor. Maximum 22 characters. On card statements, the *concatenation* of both prefix and suffix (including separators) will appear truncated to 22 characters.
        StatementDescriptorSuffixKana: string option
        ///Provides information about a card payment that customers see on their statements. Concatenated with the Kanji prefix (shortened Kanji descriptor) or Kanji statement descriptor that’s set on the account to form the complete statement descriptor. Maximum 17 characters. On card statements, the *concatenation* of both prefix and suffix (including separators) will appear truncated to 17 characters.
        StatementDescriptorSuffixKanji: string option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (requestThreeDSecure: CheckoutCardPaymentMethodOptionsRequestThreeDSecure, ?installments: CheckoutCardInstallmentsOptions, ?requestExtendedAuthorization: CheckoutCardPaymentMethodOptionsRequestExtendedAuthorization, ?requestIncrementalAuthorization: CheckoutCardPaymentMethodOptionsRequestIncrementalAuthorization, ?requestMulticapture: CheckoutCardPaymentMethodOptionsRequestMulticapture, ?requestOvercapture: CheckoutCardPaymentMethodOptionsRequestOvercapture, ?restrictions: PaymentPagesPrivateCardPaymentMethodOptionsResourceRestrictions, ?setupFutureUsage: CheckoutCardPaymentMethodOptionsSetupFutureUsage, ?statementDescriptorSuffixKana: string, ?statementDescriptorSuffixKanji: string) =
            {
                CheckoutCardPaymentMethodOptions.RequestThreeDSecure = requestThreeDSecure //required
                CheckoutCardPaymentMethodOptions.Installments = installments
                CheckoutCardPaymentMethodOptions.RequestExtendedAuthorization = requestExtendedAuthorization
                CheckoutCardPaymentMethodOptions.RequestIncrementalAuthorization = requestIncrementalAuthorization
                CheckoutCardPaymentMethodOptions.RequestMulticapture = requestMulticapture
                CheckoutCardPaymentMethodOptions.RequestOvercapture = requestOvercapture
                CheckoutCardPaymentMethodOptions.Restrictions = restrictions
                CheckoutCardPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
                CheckoutCardPaymentMethodOptions.StatementDescriptorSuffixKana = statementDescriptorSuffixKana
                CheckoutCardPaymentMethodOptions.StatementDescriptorSuffixKanji = statementDescriptorSuffixKanji
            }

    and CheckoutCustomerBalanceBankTransferPaymentMethodOptions = {
        EuBankTransfer: PaymentMethodOptionsCustomerBalanceEuBankAccount option
        ///List of address types that should be returned in the financial_addresses response. If not specified, all valid types will be returned.
        ///Permitted values include: `sort_code`, `zengin`, `iban`, or `spei`.
        RequestedAddressTypes: CheckoutCustomerBalanceBankTransferPaymentMethodOptionsRequestedAddressTypes list option
        ///The bank transfer type that this PaymentIntent is allowed to use for funding Permitted values include: `eu_bank_transfer`, `gb_bank_transfer`, `jp_bank_transfer`, `mx_bank_transfer`, or `us_bank_transfer`.
        Type: CheckoutCustomerBalanceBankTransferPaymentMethodOptionsType option
    }
    with
        static member New (``type``: CheckoutCustomerBalanceBankTransferPaymentMethodOptionsType option, ?euBankTransfer: PaymentMethodOptionsCustomerBalanceEuBankAccount, ?requestedAddressTypes: CheckoutCustomerBalanceBankTransferPaymentMethodOptionsRequestedAddressTypes list) =
            {
                CheckoutCustomerBalanceBankTransferPaymentMethodOptions.Type = ``type`` //required
                CheckoutCustomerBalanceBankTransferPaymentMethodOptions.EuBankTransfer = euBankTransfer
                CheckoutCustomerBalanceBankTransferPaymentMethodOptions.RequestedAddressTypes = requestedAddressTypes
            }

    and CheckoutCustomerBalancePaymentMethodOptions = {
        BankTransfer: CheckoutCustomerBalanceBankTransferPaymentMethodOptions option
    }
    with
        ///The funding method type to be used when there are not enough funds in the customer balance. Permitted values include: `bank_transfer`.
        member _.FundingType = "bank_transfer"
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"

        static member New (?bankTransfer: CheckoutCustomerBalanceBankTransferPaymentMethodOptions) =
            {
                CheckoutCustomerBalancePaymentMethodOptions.BankTransfer = bankTransfer
            }

    and CheckoutKakaoPayPaymentMethodOptions = {
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: CheckoutKakaoPayPaymentMethodOptionsSetupFutureUsage option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (?setupFutureUsage: CheckoutKakaoPayPaymentMethodOptionsSetupFutureUsage) =
            {
                CheckoutKakaoPayPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
            }

    and CheckoutKlarnaPaymentMethodOptions = {
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: CheckoutKlarnaPaymentMethodOptionsSetupFutureUsage option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (?setupFutureUsage: CheckoutKlarnaPaymentMethodOptionsSetupFutureUsage) =
            {
                CheckoutKlarnaPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
            }

    and CheckoutKrCardPaymentMethodOptions = {
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: CheckoutKrCardPaymentMethodOptionsSetupFutureUsage option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (?setupFutureUsage: CheckoutKrCardPaymentMethodOptionsSetupFutureUsage) =
            {
                CheckoutKrCardPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
            }

    and CheckoutLinkPaymentMethodOptions = {
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: CheckoutLinkPaymentMethodOptionsSetupFutureUsage option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (?setupFutureUsage: CheckoutLinkPaymentMethodOptionsSetupFutureUsage) =
            {
                CheckoutLinkPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
            }

    and CheckoutLinkWalletOptions = {
        ///Describes whether Checkout should display Link. Defaults to `auto`.
        Display: CheckoutLinkWalletOptionsDisplay option
    }
    with
        static member New (?display: CheckoutLinkWalletOptionsDisplay) =
            {
                CheckoutLinkWalletOptions.Display = display
            }

    and CheckoutNaverPayPaymentMethodOptions = {
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: CheckoutNaverPayPaymentMethodOptionsSetupFutureUsage option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (?setupFutureUsage: CheckoutNaverPayPaymentMethodOptionsSetupFutureUsage) =
            {
                CheckoutNaverPayPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
            }

    and CheckoutPaypalPaymentMethodOptions = {
        ///Preferred locale of the PayPal checkout page that the customer is redirected to.
        PreferredLocale: string option
        ///A reference of the PayPal transaction visible to customer which is mapped to PayPal's invoice ID. This must be a globally unique ID if you have configured in your PayPal settings to block multiple payments per invoice ID.
        Reference: string option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: CheckoutPaypalPaymentMethodOptionsSetupFutureUsage option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (preferredLocale: string option, reference: string option, ?setupFutureUsage: CheckoutPaypalPaymentMethodOptionsSetupFutureUsage) =
            {
                CheckoutPaypalPaymentMethodOptions.PreferredLocale = preferredLocale //required
                CheckoutPaypalPaymentMethodOptions.Reference = reference //required
                CheckoutPaypalPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
            }

    and CheckoutPaytoPaymentMethodOptions = {
        MandateOptions: MandateOptionsPayto option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: CheckoutPaytoPaymentMethodOptionsSetupFutureUsage option
    }
    with
        static member New (?mandateOptions: MandateOptionsPayto, ?setupFutureUsage: CheckoutPaytoPaymentMethodOptionsSetupFutureUsage) =
            {
                CheckoutPaytoPaymentMethodOptions.MandateOptions = mandateOptions
                CheckoutPaytoPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
            }

    and CheckoutPixPaymentMethodOptions = {
        ///Determines if the amount includes the IOF tax.
        AmountIncludesIof: CheckoutPixPaymentMethodOptionsAmountIncludesIof option
        ///The number of seconds after which Pix payment will expire.
        ExpiresAfterSeconds: int option
        MandateOptions: PaymentMethodOptionsMandateOptionsPix option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: CheckoutPixPaymentMethodOptionsSetupFutureUsage option
    }
    with
        static member New (expiresAfterSeconds: int option, ?amountIncludesIof: CheckoutPixPaymentMethodOptionsAmountIncludesIof, ?mandateOptions: PaymentMethodOptionsMandateOptionsPix, ?setupFutureUsage: CheckoutPixPaymentMethodOptionsSetupFutureUsage) =
            {
                CheckoutPixPaymentMethodOptions.ExpiresAfterSeconds = expiresAfterSeconds //required
                CheckoutPixPaymentMethodOptions.AmountIncludesIof = amountIncludesIof
                CheckoutPixPaymentMethodOptions.MandateOptions = mandateOptions
                CheckoutPixPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
            }

    and CheckoutRevolutPayPaymentMethodOptions = {
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: CheckoutRevolutPayPaymentMethodOptionsSetupFutureUsage option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (?setupFutureUsage: CheckoutRevolutPayPaymentMethodOptionsSetupFutureUsage) =
            {
                CheckoutRevolutPayPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
            }

    and CheckoutSepaDebitPaymentMethodOptions = {
        MandateOptions: CheckoutPaymentMethodOptionsMandateOptionsSepaDebit option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: CheckoutSepaDebitPaymentMethodOptionsSetupFutureUsage option
        ///Controls when Stripe will attempt to debit the funds from the customer's account. The date must be a string in YYYY-MM-DD format. The date must be in the future and between 3 and 15 calendar days from now.
        TargetDate: string option
    }
    with
        static member New (?mandateOptions: CheckoutPaymentMethodOptionsMandateOptionsSepaDebit, ?setupFutureUsage: CheckoutSepaDebitPaymentMethodOptionsSetupFutureUsage, ?targetDate: string) =
            {
                CheckoutSepaDebitPaymentMethodOptions.MandateOptions = mandateOptions
                CheckoutSepaDebitPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
                CheckoutSepaDebitPaymentMethodOptions.TargetDate = targetDate
            }

    and CheckoutSessionPaymentMethodOptions = {
        AcssDebit: CheckoutAcssDebitPaymentMethodOptions option
        Affirm: CheckoutAffirmPaymentMethodOptions option
        AfterpayClearpay: CheckoutAfterpayClearpayPaymentMethodOptions option
        Alipay: CheckoutAlipayPaymentMethodOptions option
        Alma: CheckoutAlmaPaymentMethodOptions option
        AmazonPay: CheckoutAmazonPayPaymentMethodOptions option
        AuBecsDebit: CheckoutAuBecsDebitPaymentMethodOptions option
        BacsDebit: CheckoutBacsDebitPaymentMethodOptions option
        Bancontact: CheckoutBancontactPaymentMethodOptions option
        Billie: CheckoutBilliePaymentMethodOptions option
        Boleto: CheckoutBoletoPaymentMethodOptions option
        Card: CheckoutCardPaymentMethodOptions option
        Cashapp: CheckoutCashappPaymentMethodOptions option
        CustomerBalance: CheckoutCustomerBalancePaymentMethodOptions option
        Eps: CheckoutEpsPaymentMethodOptions option
        Fpx: CheckoutFpxPaymentMethodOptions option
        Giropay: CheckoutGiropayPaymentMethodOptions option
        Grabpay: CheckoutGrabPayPaymentMethodOptions option
        Ideal: CheckoutIdealPaymentMethodOptions option
        KakaoPay: CheckoutKakaoPayPaymentMethodOptions option
        Klarna: CheckoutKlarnaPaymentMethodOptions option
        Konbini: CheckoutKonbiniPaymentMethodOptions option
        KrCard: CheckoutKrCardPaymentMethodOptions option
        Link: CheckoutLinkPaymentMethodOptions option
        Mobilepay: CheckoutMobilepayPaymentMethodOptions option
        Multibanco: CheckoutMultibancoPaymentMethodOptions option
        NaverPay: CheckoutNaverPayPaymentMethodOptions option
        Oxxo: CheckoutOxxoPaymentMethodOptions option
        [<JsonField(Name="p24")>]P24: CheckoutP24PaymentMethodOptions option
        Payco: CheckoutPaycoPaymentMethodOptions option
        Paynow: CheckoutPaynowPaymentMethodOptions option
        Paypal: CheckoutPaypalPaymentMethodOptions option
        Payto: CheckoutPaytoPaymentMethodOptions option
        Pix: CheckoutPixPaymentMethodOptions option
        RevolutPay: CheckoutRevolutPayPaymentMethodOptions option
        SamsungPay: CheckoutSamsungPayPaymentMethodOptions option
        Satispay: CheckoutSatispayPaymentMethodOptions option
        SepaDebit: CheckoutSepaDebitPaymentMethodOptions option
        Sofort: CheckoutSofortPaymentMethodOptions option
        Swish: CheckoutSwishPaymentMethodOptions option
        Twint: CheckoutTwintPaymentMethodOptions option
        Upi: CheckoutUpiPaymentMethodOptions option
        UsBankAccount: CheckoutUsBankAccountPaymentMethodOptions option
    }
    with
        static member New (?acssDebit: CheckoutAcssDebitPaymentMethodOptions, ?affirm: CheckoutAffirmPaymentMethodOptions, ?afterpayClearpay: CheckoutAfterpayClearpayPaymentMethodOptions, ?alipay: CheckoutAlipayPaymentMethodOptions, ?alma: CheckoutAlmaPaymentMethodOptions, ?amazonPay: CheckoutAmazonPayPaymentMethodOptions, ?auBecsDebit: CheckoutAuBecsDebitPaymentMethodOptions, ?bacsDebit: CheckoutBacsDebitPaymentMethodOptions, ?bancontact: CheckoutBancontactPaymentMethodOptions, ?billie: CheckoutBilliePaymentMethodOptions, ?boleto: CheckoutBoletoPaymentMethodOptions, ?card: CheckoutCardPaymentMethodOptions, ?cashapp: CheckoutCashappPaymentMethodOptions, ?customerBalance: CheckoutCustomerBalancePaymentMethodOptions, ?eps: CheckoutEpsPaymentMethodOptions, ?fpx: CheckoutFpxPaymentMethodOptions, ?giropay: CheckoutGiropayPaymentMethodOptions, ?grabpay: CheckoutGrabPayPaymentMethodOptions, ?ideal: CheckoutIdealPaymentMethodOptions, ?kakaoPay: CheckoutKakaoPayPaymentMethodOptions, ?klarna: CheckoutKlarnaPaymentMethodOptions, ?konbini: CheckoutKonbiniPaymentMethodOptions, ?krCard: CheckoutKrCardPaymentMethodOptions, ?link: CheckoutLinkPaymentMethodOptions, ?mobilepay: CheckoutMobilepayPaymentMethodOptions, ?multibanco: CheckoutMultibancoPaymentMethodOptions, ?naverPay: CheckoutNaverPayPaymentMethodOptions, ?oxxo: CheckoutOxxoPaymentMethodOptions, ?p24: CheckoutP24PaymentMethodOptions, ?payco: CheckoutPaycoPaymentMethodOptions, ?paynow: CheckoutPaynowPaymentMethodOptions, ?paypal: CheckoutPaypalPaymentMethodOptions, ?payto: CheckoutPaytoPaymentMethodOptions, ?pix: CheckoutPixPaymentMethodOptions, ?revolutPay: CheckoutRevolutPayPaymentMethodOptions, ?samsungPay: CheckoutSamsungPayPaymentMethodOptions, ?satispay: CheckoutSatispayPaymentMethodOptions, ?sepaDebit: CheckoutSepaDebitPaymentMethodOptions, ?sofort: CheckoutSofortPaymentMethodOptions, ?swish: CheckoutSwishPaymentMethodOptions, ?twint: CheckoutTwintPaymentMethodOptions, ?upi: CheckoutUpiPaymentMethodOptions, ?usBankAccount: CheckoutUsBankAccountPaymentMethodOptions) =
            {
                CheckoutSessionPaymentMethodOptions.AcssDebit = acssDebit
                CheckoutSessionPaymentMethodOptions.Affirm = affirm
                CheckoutSessionPaymentMethodOptions.AfterpayClearpay = afterpayClearpay
                CheckoutSessionPaymentMethodOptions.Alipay = alipay
                CheckoutSessionPaymentMethodOptions.Alma = alma
                CheckoutSessionPaymentMethodOptions.AmazonPay = amazonPay
                CheckoutSessionPaymentMethodOptions.AuBecsDebit = auBecsDebit
                CheckoutSessionPaymentMethodOptions.BacsDebit = bacsDebit
                CheckoutSessionPaymentMethodOptions.Bancontact = bancontact
                CheckoutSessionPaymentMethodOptions.Billie = billie
                CheckoutSessionPaymentMethodOptions.Boleto = boleto
                CheckoutSessionPaymentMethodOptions.Card = card
                CheckoutSessionPaymentMethodOptions.Cashapp = cashapp
                CheckoutSessionPaymentMethodOptions.CustomerBalance = customerBalance
                CheckoutSessionPaymentMethodOptions.Eps = eps
                CheckoutSessionPaymentMethodOptions.Fpx = fpx
                CheckoutSessionPaymentMethodOptions.Giropay = giropay
                CheckoutSessionPaymentMethodOptions.Grabpay = grabpay
                CheckoutSessionPaymentMethodOptions.Ideal = ideal
                CheckoutSessionPaymentMethodOptions.KakaoPay = kakaoPay
                CheckoutSessionPaymentMethodOptions.Klarna = klarna
                CheckoutSessionPaymentMethodOptions.Konbini = konbini
                CheckoutSessionPaymentMethodOptions.KrCard = krCard
                CheckoutSessionPaymentMethodOptions.Link = link
                CheckoutSessionPaymentMethodOptions.Mobilepay = mobilepay
                CheckoutSessionPaymentMethodOptions.Multibanco = multibanco
                CheckoutSessionPaymentMethodOptions.NaverPay = naverPay
                CheckoutSessionPaymentMethodOptions.Oxxo = oxxo
                CheckoutSessionPaymentMethodOptions.P24 = p24
                CheckoutSessionPaymentMethodOptions.Payco = payco
                CheckoutSessionPaymentMethodOptions.Paynow = paynow
                CheckoutSessionPaymentMethodOptions.Paypal = paypal
                CheckoutSessionPaymentMethodOptions.Payto = payto
                CheckoutSessionPaymentMethodOptions.Pix = pix
                CheckoutSessionPaymentMethodOptions.RevolutPay = revolutPay
                CheckoutSessionPaymentMethodOptions.SamsungPay = samsungPay
                CheckoutSessionPaymentMethodOptions.Satispay = satispay
                CheckoutSessionPaymentMethodOptions.SepaDebit = sepaDebit
                CheckoutSessionPaymentMethodOptions.Sofort = sofort
                CheckoutSessionPaymentMethodOptions.Swish = swish
                CheckoutSessionPaymentMethodOptions.Twint = twint
                CheckoutSessionPaymentMethodOptions.Upi = upi
                CheckoutSessionPaymentMethodOptions.UsBankAccount = usBankAccount
            }

    and CheckoutSessionWalletOptions = {
        Link: CheckoutLinkWalletOptions option
    }
    with
        static member New (?link: CheckoutLinkWalletOptions) =
            {
                CheckoutSessionWalletOptions.Link = link
            }

    and CheckoutUpiPaymentMethodOptions = {
        MandateOptions: MandateOptionsUpi option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: CheckoutUpiPaymentMethodOptionsSetupFutureUsage option
    }
    with
        static member New (?mandateOptions: MandateOptionsUpi, ?setupFutureUsage: CheckoutUpiPaymentMethodOptionsSetupFutureUsage) =
            {
                CheckoutUpiPaymentMethodOptions.MandateOptions = mandateOptions
                CheckoutUpiPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
            }

    and CheckoutUsBankAccountPaymentMethodOptions = {
        FinancialConnections: LinkedAccountOptionsCommon option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: CheckoutUsBankAccountPaymentMethodOptionsSetupFutureUsage option
        ///Controls when Stripe will attempt to debit the funds from the customer's account. The date must be a string in YYYY-MM-DD format. The date must be in the future and between 3 and 15 calendar days from now.
        TargetDate: string option
        ///Bank account verification method. The default value is `automatic`.
        VerificationMethod: CheckoutUsBankAccountPaymentMethodOptionsVerificationMethod option
    }
    with
        static member New (?financialConnections: LinkedAccountOptionsCommon, ?setupFutureUsage: CheckoutUsBankAccountPaymentMethodOptionsSetupFutureUsage, ?targetDate: string, ?verificationMethod: CheckoutUsBankAccountPaymentMethodOptionsVerificationMethod) =
            {
                CheckoutUsBankAccountPaymentMethodOptions.FinancialConnections = financialConnections
                CheckoutUsBankAccountPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
                CheckoutUsBankAccountPaymentMethodOptions.TargetDate = targetDate
                CheckoutUsBankAccountPaymentMethodOptions.VerificationMethod = verificationMethod
            }

    ///Orders represent your intent to purchase a particular Climate product. When you create an order, the
    ///payment is deducted from your merchant balance.
    and ClimateOrder = {
        ///Total amount of [Frontier](https://frontierclimate.com/)'s service fees in the currency's smallest unit.
        AmountFees: int
        ///Total amount of the carbon removal in the currency's smallest unit.
        AmountSubtotal: int
        ///Total amount of the order including fees in the currency's smallest unit.
        AmountTotal: int
        Beneficiary: ClimateRemovalsBeneficiary option
        ///Time at which the order was canceled. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CanceledAt: DateTime option
        ///Reason for the cancellation of this order.
        CancellationReason: ClimateOrderCancellationReason option
        ///For delivered orders, a URL to a delivery certificate for the order.
        Certificate: string option
        ///Time at which the order was confirmed. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ConfirmedAt: DateTime option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase, representing the currency for this order.
        Currency: IsoTypes.IsoCurrencyCode
        ///Time at which the order's expected_delivery_year was delayed. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]DelayedAt: DateTime option
        ///Time at which the order was delivered. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]DeliveredAt: DateTime option
        ///Details about the delivery of carbon removal for this order.
        DeliveryDetails: ClimateRemovalsOrderDeliveries list
        ///The year this order is expected to be delivered.
        ExpectedDeliveryYear: int
        ///Unique identifier for the object.
        Id: string
        ///Has the value `true` if the object exists in live mode or the value `false` if the object exists in test mode.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///Quantity of carbon removal that is included in this order.
        MetricTons: string
        ///Unique ID for the Climate `Product` this order is purchasing.
        Product: ClimateOrderProduct'AnyOf
        ///Time at which the order's product was substituted for a different product. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ProductSubstitutedAt: DateTime option
        ///The current status of this order.
        Status: ClimateOrderStatus
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "climate.order"

        static member New (amountFees: int, amountSubtotal: int, amountTotal: int, canceledAt: DateTime option, cancellationReason: ClimateOrderCancellationReason option, certificate: string option, confirmedAt: DateTime option, created: DateTime, currency: IsoTypes.IsoCurrencyCode, delayedAt: DateTime option, deliveredAt: DateTime option, deliveryDetails: ClimateRemovalsOrderDeliveries list, expectedDeliveryYear: int, id: string, livemode: bool, metadata: Map<string, string>, metricTons: string, product: ClimateOrderProduct'AnyOf, productSubstitutedAt: DateTime option, status: ClimateOrderStatus, ?beneficiary: ClimateRemovalsBeneficiary) =
            {
                ClimateOrder.AmountFees = amountFees //required
                ClimateOrder.AmountSubtotal = amountSubtotal //required
                ClimateOrder.AmountTotal = amountTotal //required
                ClimateOrder.CanceledAt = canceledAt //required
                ClimateOrder.CancellationReason = cancellationReason //required
                ClimateOrder.Certificate = certificate //required
                ClimateOrder.ConfirmedAt = confirmedAt //required
                ClimateOrder.Created = created //required
                ClimateOrder.Currency = currency //required
                ClimateOrder.DelayedAt = delayedAt //required
                ClimateOrder.DeliveredAt = deliveredAt //required
                ClimateOrder.DeliveryDetails = deliveryDetails //required
                ClimateOrder.ExpectedDeliveryYear = expectedDeliveryYear //required
                ClimateOrder.Id = id //required
                ClimateOrder.Livemode = livemode //required
                ClimateOrder.Metadata = metadata //required
                ClimateOrder.MetricTons = metricTons //required
                ClimateOrder.Product = product //required
                ClimateOrder.ProductSubstitutedAt = productSubstitutedAt //required
                ClimateOrder.Status = status //required
                ClimateOrder.Beneficiary = beneficiary
            }

    and ClimateOrderProduct'AnyOf =
        | String of string
        | ClimateProduct of ClimateProduct

    ///A Climate product represents a type of carbon removal unit available for reservation.
    ///You can retrieve it to see the current price and availability.
    and ClimateProduct = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Current prices for a metric ton of carbon removal in a currency's smallest unit.
        CurrentPricesPerMetricTon: Map<string, string list>
        ///The year in which the carbon removal is expected to be delivered.
        DeliveryYear: int option
        ///Unique identifier for the object. For convenience, Climate product IDs are human-readable strings
        ///that start with `climsku_`. See [carbon removal inventory](https://stripe.com/docs/climate/orders/carbon-removal-inventory)
        ///for a list of available carbon removal products.
        Id: string
        ///Has the value `true` if the object exists in live mode or the value `false` if the object exists in test mode.
        Livemode: bool
        ///The quantity of metric tons available for reservation.
        MetricTonsAvailable: string
        ///The Climate product's name.
        Name: string
        ///The carbon removal suppliers that fulfill orders for this Climate product.
        Suppliers: ClimateSupplier list
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "climate.product"

        static member New (created: DateTime, currentPricesPerMetricTon: Map<string, string list>, deliveryYear: int option, id: string, livemode: bool, metricTonsAvailable: string, name: string, suppliers: ClimateSupplier list) =
            {
                ClimateProduct.Created = created //required
                ClimateProduct.CurrentPricesPerMetricTon = currentPricesPerMetricTon //required
                ClimateProduct.DeliveryYear = deliveryYear //required
                ClimateProduct.Id = id //required
                ClimateProduct.Livemode = livemode //required
                ClimateProduct.MetricTonsAvailable = metricTonsAvailable //required
                ClimateProduct.Name = name //required
                ClimateProduct.Suppliers = suppliers //required
            }

    ///A supplier of carbon removal.
    and ClimateSupplier = {
        ///Unique identifier for the object.
        Id: string
        ///Link to a webpage to learn more about the supplier.
        InfoUrl: string
        ///Has the value `true` if the object exists in live mode or the value `false` if the object exists in test mode.
        Livemode: bool
        ///The locations in which this supplier operates.
        Locations: ClimateRemovalsLocation list
        ///Name of this carbon removal supplier.
        Name: string
        ///The scientific pathway used for carbon removal.
        RemovalPathway: ClimateSupplierRemovalPathway
    }
    with
        ///String representing the object’s type. Objects of the same type share the same value.
        member _.Object = "climate.supplier"

        static member New (id: string, infoUrl: string, livemode: bool, locations: ClimateRemovalsLocation list, name: string, removalPathway: ClimateSupplierRemovalPathway) =
            {
                ClimateSupplier.Id = id //required
                ClimateSupplier.InfoUrl = infoUrl //required
                ClimateSupplier.Livemode = livemode //required
                ClimateSupplier.Locations = locations //required
                ClimateSupplier.Name = name //required
                ClimateSupplier.RemovalPathway = removalPathway //required
            }

    and ClimateRemovalsLocation = {
        ///The city where the supplier is located.
        City: string option
        ///Two-letter ISO code representing the country where the supplier is located.
        Country: IsoTypes.IsoCountryCode
        ///The geographic latitude where the supplier is located.
        Latitude: decimal option
        ///The geographic longitude where the supplier is located.
        Longitude: decimal option
        ///The state/county/province/region where the supplier is located.
        Region: string option
    }
    with
        static member New (city: string option, country: IsoTypes.IsoCountryCode, latitude: decimal option, longitude: decimal option, region: string option) =
            {
                ClimateRemovalsLocation.City = city //required
                ClimateRemovalsLocation.Country = country //required
                ClimateRemovalsLocation.Latitude = latitude //required
                ClimateRemovalsLocation.Longitude = longitude //required
                ClimateRemovalsLocation.Region = region //required
            }

    ///The delivery of a specified quantity of carbon for an order.
    and ClimateRemovalsOrderDeliveries = {
        ///Time at which the delivery occurred. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]DeliveredAt: DateTime
        ///Specific location of this delivery.
        Location: ClimateRemovalsLocation option
        ///Quantity of carbon removal supplied by this delivery.
        MetricTons: string
        ///Once retired, a URL to the registry entry for the tons from this delivery.
        RegistryUrl: string option
        Supplier: ClimateSupplier
    }
    with
        static member New (deliveredAt: DateTime, location: ClimateRemovalsLocation option, metricTons: string, registryUrl: string option, supplier: ClimateSupplier) =
            {
                ClimateRemovalsOrderDeliveries.DeliveredAt = deliveredAt //required
                ClimateRemovalsOrderDeliveries.Location = location //required
                ClimateRemovalsOrderDeliveries.MetricTons = metricTons //required
                ClimateRemovalsOrderDeliveries.RegistryUrl = registryUrl //required
                ClimateRemovalsOrderDeliveries.Supplier = supplier //required
            }

    ///ConfirmationTokens help transport client side data collected by Stripe JS over
    ///to your server for confirming a PaymentIntent or SetupIntent. If the confirmation
    ///is successful, values present on the ConfirmationToken are written onto the Intent.
    ///To learn more about how to use ConfirmationToken, visit the related guides:
    ///- [Finalize payments on the server](https://docs.stripe.com/payments/finalize-payments-on-the-server)
    ///- [Build two-step confirmation](https://docs.stripe.com/payments/build-a-two-step-confirmation).
    and ConfirmationToken = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Time at which this ConfirmationToken expires and can no longer be used to confirm a PaymentIntent or SetupIntent.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Data used for generating a Mandate.
        MandateData: ConfirmationTokensResourceMandateData option
        ///ID of the PaymentIntent that this ConfirmationToken was used to confirm, or null if this ConfirmationToken has not yet been used.
        PaymentIntent: string option
        ///Payment-method-specific configuration for this ConfirmationToken.
        PaymentMethodOptions: ConfirmationTokensResourcePaymentMethodOptions option
        ///Payment details collected by the Payment Element, used to create a PaymentMethod when a PaymentIntent or SetupIntent is confirmed with this ConfirmationToken.
        PaymentMethodPreview: ConfirmationTokensResourcePaymentMethodPreview option
        ///Return URL used to confirm the Intent.
        ReturnUrl: string option
        ///Indicates that you intend to make future payments with this ConfirmationToken's payment method.
        ///The presence of this property will [attach the payment method](https://docs.stripe.com/payments/save-during-payment) to the PaymentIntent's Customer, if present, after the PaymentIntent is confirmed and any required actions from the user are complete.
        SetupFutureUsage: ConfirmationTokenSetupFutureUsage option
        ///ID of the SetupIntent that this ConfirmationToken was used to confirm, or null if this ConfirmationToken has not yet been used.
        SetupIntent: string option
        ///Shipping information collected on this ConfirmationToken.
        Shipping: ConfirmationTokensResourceShipping option
        ///Indicates whether the Stripe SDK is used to handle confirmation flow. Defaults to `true` on ConfirmationToken.
        UseStripeSdk: bool
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "confirmation_token"

        static member New (created: DateTime, expiresAt: DateTime option, id: string, livemode: bool, paymentIntent: string option, paymentMethodOptions: ConfirmationTokensResourcePaymentMethodOptions option, paymentMethodPreview: ConfirmationTokensResourcePaymentMethodPreview option, returnUrl: string option, setupFutureUsage: ConfirmationTokenSetupFutureUsage option, setupIntent: string option, shipping: ConfirmationTokensResourceShipping option, useStripeSdk: bool, ?mandateData: ConfirmationTokensResourceMandateData option) =
            {
                ConfirmationToken.Created = created //required
                ConfirmationToken.ExpiresAt = expiresAt //required
                ConfirmationToken.Id = id //required
                ConfirmationToken.Livemode = livemode //required
                ConfirmationToken.PaymentIntent = paymentIntent //required
                ConfirmationToken.PaymentMethodOptions = paymentMethodOptions //required
                ConfirmationToken.PaymentMethodPreview = paymentMethodPreview //required
                ConfirmationToken.ReturnUrl = returnUrl //required
                ConfirmationToken.SetupFutureUsage = setupFutureUsage //required
                ConfirmationToken.SetupIntent = setupIntent //required
                ConfirmationToken.Shipping = shipping //required
                ConfirmationToken.UseStripeSdk = useStripeSdk //required
                ConfirmationToken.MandateData = mandateData |> Option.flatten
            }

    ///Data used for generating a Mandate.
    and ConfirmationTokensResourceMandateData = {
        CustomerAcceptance: ConfirmationTokensResourceMandateDataResourceCustomerAcceptance
    }
    with
        static member New (customerAcceptance: ConfirmationTokensResourceMandateDataResourceCustomerAcceptance) =
            {
                ConfirmationTokensResourceMandateData.CustomerAcceptance = customerAcceptance //required
            }

    ///This hash contains details about the customer acceptance of the Mandate.
    and ConfirmationTokensResourceMandateDataResourceCustomerAcceptance = {
        ///If this is a Mandate accepted online, this hash contains details about the online acceptance.
        Online: ConfirmationTokensResourceMandateDataResourceCustomerAcceptanceResourceOnline option
        ///The type of customer acceptance information included with the Mandate.
        Type: string
    }
    with
        static member New (online: ConfirmationTokensResourceMandateDataResourceCustomerAcceptanceResourceOnline option, ``type``: string) =
            {
                ConfirmationTokensResourceMandateDataResourceCustomerAcceptance.Online = online //required
                ConfirmationTokensResourceMandateDataResourceCustomerAcceptance.Type = ``type`` //required
            }

    ///Payment-method-specific configuration
    and ConfirmationTokensResourcePaymentMethodOptions = {
        ///This hash contains the card payment method options.
        Card: ConfirmationTokensResourcePaymentMethodOptionsResourceCard option
    }
    with
        static member New (card: ConfirmationTokensResourcePaymentMethodOptionsResourceCard option) =
            {
                ConfirmationTokensResourcePaymentMethodOptions.Card = card //required
            }

    ///This hash contains the card payment method options.
    and ConfirmationTokensResourcePaymentMethodOptionsResourceCard = {
        ///The `cvc_update` Token collected from the Payment Element.
        CvcToken: string option
        Installments: ConfirmationTokensResourcePaymentMethodOptionsResourceCardResourceInstallment option
    }
    with
        static member New (cvcToken: string option, ?installments: ConfirmationTokensResourcePaymentMethodOptionsResourceCardResourceInstallment) =
            {
                ConfirmationTokensResourcePaymentMethodOptionsResourceCard.CvcToken = cvcToken //required
                ConfirmationTokensResourcePaymentMethodOptionsResourceCard.Installments = installments
            }

    ///Installment configuration for payments.
    and ConfirmationTokensResourcePaymentMethodOptionsResourceCardResourceInstallment = {
        Plan: PaymentMethodDetailsCardInstallmentsPlan option
    }
    with
        static member New (?plan: PaymentMethodDetailsCardInstallmentsPlan) =
            {
                ConfirmationTokensResourcePaymentMethodOptionsResourceCardResourceInstallment.Plan = plan
            }

    ///Details of the PaymentMethod collected by Payment Element
    and ConfirmationTokensResourcePaymentMethodPreview = {
        AcssDebit: PaymentMethodAcssDebit option
        Affirm: PaymentMethodAffirm option
        AfterpayClearpay: PaymentMethodAfterpayClearpay option
        Alipay: PaymentFlowsPrivatePaymentMethodsAlipay option
        ///This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment method in a checkout flow. The field defaults to “unspecified”.
        AllowRedisplay: ConfirmationTokensResourcePaymentMethodPreviewAllowRedisplay option
        Alma: PaymentMethodAlma option
        AmazonPay: PaymentMethodAmazonPay option
        AuBecsDebit: PaymentMethodAuBecsDebit option
        BacsDebit: PaymentMethodBacsDebit option
        Bancontact: PaymentMethodBancontact option
        Billie: PaymentMethodBillie option
        BillingDetails: BillingDetails
        Blik: PaymentMethodBlik option
        Boleto: PaymentMethodBoleto option
        Card: PaymentMethodCard option
        CardPresent: PaymentMethodCardPresent option
        Cashapp: PaymentMethodCashapp option
        Crypto: PaymentMethodCrypto option
        ///The ID of the Customer to which this PaymentMethod is saved. This will not be set when the PaymentMethod has not been saved to a Customer.
        Customer: ConfirmationTokensResourcePaymentMethodPreviewCustomer'AnyOf option
        CustomerAccount: string option
        CustomerBalance: PaymentMethodCustomerBalance option
        Eps: PaymentMethodEps option
        Fpx: PaymentMethodFpx option
        Giropay: PaymentMethodGiropay option
        Grabpay: PaymentMethodGrabpay option
        Ideal: PaymentMethodIdeal option
        InteracPresent: PaymentMethodInteracPresent option
        KakaoPay: PaymentMethodKakaoPay option
        Klarna: PaymentMethodKlarna option
        Konbini: PaymentMethodKonbini option
        KrCard: PaymentMethodKrCard option
        Link: PaymentMethodLink option
        MbWay: PaymentMethodMbWay option
        Mobilepay: PaymentMethodMobilepay option
        Multibanco: PaymentMethodMultibanco option
        NaverPay: PaymentMethodNaverPay option
        NzBankAccount: PaymentMethodNzBankAccount option
        Oxxo: PaymentMethodOxxo option
        [<JsonField(Name="p24")>]P24: PaymentMethodP24 option
        PayByBank: PaymentMethodPayByBank option
        Payco: PaymentMethodPayco option
        Paynow: PaymentMethodPaynow option
        Paypal: PaymentMethodPaypal option
        Payto: PaymentMethodPayto option
        Pix: PaymentMethodPix option
        Promptpay: PaymentMethodPromptpay option
        RevolutPay: PaymentMethodRevolutPay option
        SamsungPay: PaymentMethodSamsungPay option
        Satispay: PaymentMethodSatispay option
        SepaDebit: PaymentMethodSepaDebit option
        Sofort: PaymentMethodSofort option
        Sunbit: PaymentMethodSunbit option
        Swish: PaymentMethodSwish option
        Twint: PaymentMethodTwint option
        ///The type of the PaymentMethod. An additional hash is included on the PaymentMethod with a name matching this value. It contains additional information specific to the PaymentMethod type.
        Type: ConfirmationTokensResourcePaymentMethodPreviewType
        Upi: PaymentMethodUpi option
        UsBankAccount: PaymentMethodUsBankAccount option
        WechatPay: PaymentMethodWechatPay option
        Zip: PaymentMethodZip option
    }
    with
        static member New (billingDetails: BillingDetails, customer: ConfirmationTokensResourcePaymentMethodPreviewCustomer'AnyOf option, customerAccount: string option, ``type``: ConfirmationTokensResourcePaymentMethodPreviewType, ?acssDebit: PaymentMethodAcssDebit, ?affirm: PaymentMethodAffirm, ?afterpayClearpay: PaymentMethodAfterpayClearpay, ?alipay: PaymentFlowsPrivatePaymentMethodsAlipay, ?allowRedisplay: ConfirmationTokensResourcePaymentMethodPreviewAllowRedisplay, ?alma: PaymentMethodAlma, ?amazonPay: PaymentMethodAmazonPay, ?auBecsDebit: PaymentMethodAuBecsDebit, ?bacsDebit: PaymentMethodBacsDebit, ?bancontact: PaymentMethodBancontact, ?billie: PaymentMethodBillie, ?blik: PaymentMethodBlik, ?boleto: PaymentMethodBoleto, ?card: PaymentMethodCard, ?cardPresent: PaymentMethodCardPresent, ?cashapp: PaymentMethodCashapp, ?crypto: PaymentMethodCrypto, ?customerBalance: PaymentMethodCustomerBalance, ?eps: PaymentMethodEps, ?fpx: PaymentMethodFpx, ?giropay: PaymentMethodGiropay, ?grabpay: PaymentMethodGrabpay, ?ideal: PaymentMethodIdeal, ?interacPresent: PaymentMethodInteracPresent, ?kakaoPay: PaymentMethodKakaoPay, ?klarna: PaymentMethodKlarna, ?konbini: PaymentMethodKonbini, ?krCard: PaymentMethodKrCard, ?link: PaymentMethodLink, ?mbWay: PaymentMethodMbWay, ?mobilepay: PaymentMethodMobilepay, ?multibanco: PaymentMethodMultibanco, ?naverPay: PaymentMethodNaverPay, ?nzBankAccount: PaymentMethodNzBankAccount, ?oxxo: PaymentMethodOxxo, ?p24: PaymentMethodP24, ?payByBank: PaymentMethodPayByBank, ?payco: PaymentMethodPayco, ?paynow: PaymentMethodPaynow, ?paypal: PaymentMethodPaypal, ?payto: PaymentMethodPayto, ?pix: PaymentMethodPix, ?promptpay: PaymentMethodPromptpay, ?revolutPay: PaymentMethodRevolutPay, ?samsungPay: PaymentMethodSamsungPay, ?satispay: PaymentMethodSatispay, ?sepaDebit: PaymentMethodSepaDebit, ?sofort: PaymentMethodSofort, ?sunbit: PaymentMethodSunbit, ?swish: PaymentMethodSwish, ?twint: PaymentMethodTwint, ?upi: PaymentMethodUpi, ?usBankAccount: PaymentMethodUsBankAccount, ?wechatPay: PaymentMethodWechatPay, ?zip: PaymentMethodZip) =
            {
                ConfirmationTokensResourcePaymentMethodPreview.BillingDetails = billingDetails //required
                ConfirmationTokensResourcePaymentMethodPreview.Customer = customer //required
                ConfirmationTokensResourcePaymentMethodPreview.CustomerAccount = customerAccount //required
                ConfirmationTokensResourcePaymentMethodPreview.Type = ``type`` //required
                ConfirmationTokensResourcePaymentMethodPreview.AcssDebit = acssDebit
                ConfirmationTokensResourcePaymentMethodPreview.Affirm = affirm
                ConfirmationTokensResourcePaymentMethodPreview.AfterpayClearpay = afterpayClearpay
                ConfirmationTokensResourcePaymentMethodPreview.Alipay = alipay
                ConfirmationTokensResourcePaymentMethodPreview.AllowRedisplay = allowRedisplay
                ConfirmationTokensResourcePaymentMethodPreview.Alma = alma
                ConfirmationTokensResourcePaymentMethodPreview.AmazonPay = amazonPay
                ConfirmationTokensResourcePaymentMethodPreview.AuBecsDebit = auBecsDebit
                ConfirmationTokensResourcePaymentMethodPreview.BacsDebit = bacsDebit
                ConfirmationTokensResourcePaymentMethodPreview.Bancontact = bancontact
                ConfirmationTokensResourcePaymentMethodPreview.Billie = billie
                ConfirmationTokensResourcePaymentMethodPreview.Blik = blik
                ConfirmationTokensResourcePaymentMethodPreview.Boleto = boleto
                ConfirmationTokensResourcePaymentMethodPreview.Card = card
                ConfirmationTokensResourcePaymentMethodPreview.CardPresent = cardPresent
                ConfirmationTokensResourcePaymentMethodPreview.Cashapp = cashapp
                ConfirmationTokensResourcePaymentMethodPreview.Crypto = crypto
                ConfirmationTokensResourcePaymentMethodPreview.CustomerBalance = customerBalance
                ConfirmationTokensResourcePaymentMethodPreview.Eps = eps
                ConfirmationTokensResourcePaymentMethodPreview.Fpx = fpx
                ConfirmationTokensResourcePaymentMethodPreview.Giropay = giropay
                ConfirmationTokensResourcePaymentMethodPreview.Grabpay = grabpay
                ConfirmationTokensResourcePaymentMethodPreview.Ideal = ideal
                ConfirmationTokensResourcePaymentMethodPreview.InteracPresent = interacPresent
                ConfirmationTokensResourcePaymentMethodPreview.KakaoPay = kakaoPay
                ConfirmationTokensResourcePaymentMethodPreview.Klarna = klarna
                ConfirmationTokensResourcePaymentMethodPreview.Konbini = konbini
                ConfirmationTokensResourcePaymentMethodPreview.KrCard = krCard
                ConfirmationTokensResourcePaymentMethodPreview.Link = link
                ConfirmationTokensResourcePaymentMethodPreview.MbWay = mbWay
                ConfirmationTokensResourcePaymentMethodPreview.Mobilepay = mobilepay
                ConfirmationTokensResourcePaymentMethodPreview.Multibanco = multibanco
                ConfirmationTokensResourcePaymentMethodPreview.NaverPay = naverPay
                ConfirmationTokensResourcePaymentMethodPreview.NzBankAccount = nzBankAccount
                ConfirmationTokensResourcePaymentMethodPreview.Oxxo = oxxo
                ConfirmationTokensResourcePaymentMethodPreview.P24 = p24
                ConfirmationTokensResourcePaymentMethodPreview.PayByBank = payByBank
                ConfirmationTokensResourcePaymentMethodPreview.Payco = payco
                ConfirmationTokensResourcePaymentMethodPreview.Paynow = paynow
                ConfirmationTokensResourcePaymentMethodPreview.Paypal = paypal
                ConfirmationTokensResourcePaymentMethodPreview.Payto = payto
                ConfirmationTokensResourcePaymentMethodPreview.Pix = pix
                ConfirmationTokensResourcePaymentMethodPreview.Promptpay = promptpay
                ConfirmationTokensResourcePaymentMethodPreview.RevolutPay = revolutPay
                ConfirmationTokensResourcePaymentMethodPreview.SamsungPay = samsungPay
                ConfirmationTokensResourcePaymentMethodPreview.Satispay = satispay
                ConfirmationTokensResourcePaymentMethodPreview.SepaDebit = sepaDebit
                ConfirmationTokensResourcePaymentMethodPreview.Sofort = sofort
                ConfirmationTokensResourcePaymentMethodPreview.Sunbit = sunbit
                ConfirmationTokensResourcePaymentMethodPreview.Swish = swish
                ConfirmationTokensResourcePaymentMethodPreview.Twint = twint
                ConfirmationTokensResourcePaymentMethodPreview.Upi = upi
                ConfirmationTokensResourcePaymentMethodPreview.UsBankAccount = usBankAccount
                ConfirmationTokensResourcePaymentMethodPreview.WechatPay = wechatPay
                ConfirmationTokensResourcePaymentMethodPreview.Zip = zip
            }

    and ConfirmationTokensResourcePaymentMethodPreviewCustomer'AnyOf =
        | String of string
        | Customer of Customer

    and ConfirmationTokensResourceShipping = {
        Address: Address
        ///Recipient name.
        Name: string
        ///Recipient phone (including extension).
        Phone: string option
    }
    with
        static member New (address: Address, name: string, phone: string option) =
            {
                ConfirmationTokensResourceShipping.Address = address //required
                ConfirmationTokensResourceShipping.Name = name //required
                ConfirmationTokensResourceShipping.Phone = phone //required
            }

    and ConnectAccountReference = {
        ///The connected account being referenced when `type` is `account`.
        Account: ConnectAccountReferenceAccount'AnyOf option
        ///Type of the account referenced.
        Type: ConnectAccountReferenceType
    }
    with
        static member New (``type``: ConnectAccountReferenceType, ?account: ConnectAccountReferenceAccount'AnyOf) =
            {
                ConnectAccountReference.Type = ``type`` //required
                ConnectAccountReference.Account = account
            }

    and ConnectAccountReferenceAccount'AnyOf =
        | String of string
        | Account of Account

    and ConnectCollectionTransfer = {
        ///Amount transferred, in cents (or local equivalent).
        Amount: int
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///ID of the account that funds are being collected for.
        Destination: ConnectCollectionTransferDestination'AnyOf
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "connect_collection_transfer"

        static member New (amount: int, currency: IsoTypes.IsoCurrencyCode, destination: ConnectCollectionTransferDestination'AnyOf, id: string, livemode: bool) =
            {
                ConnectCollectionTransfer.Amount = amount //required
                ConnectCollectionTransfer.Currency = currency //required
                ConnectCollectionTransfer.Destination = destination //required
                ConnectCollectionTransfer.Id = id //required
                ConnectCollectionTransfer.Livemode = livemode //required
            }

    and ConnectCollectionTransferDestination'AnyOf =
        | String of string
        | Account of Account

    and ConnectEmbeddedAccountConfigClaim = {
        ///Whether the embedded component is enabled.
        Enabled: bool
        Features: ConnectEmbeddedAccountFeaturesClaim
    }
    with
        static member New (enabled: bool, features: ConnectEmbeddedAccountFeaturesClaim) =
            {
                ConnectEmbeddedAccountConfigClaim.Enabled = enabled //required
                ConnectEmbeddedAccountConfigClaim.Features = features //required
            }

    and ConnectEmbeddedAccountSessionCreateComponents = {
        AccountManagement: ConnectEmbeddedAccountConfigClaim
        AccountOnboarding: ConnectEmbeddedAccountConfigClaim
        BalanceReport: ConnectEmbeddedBaseConfigClaim
        Balances: ConnectEmbeddedPayoutsConfig
        DisputesList: ConnectEmbeddedDisputesListConfig
        Documents: ConnectEmbeddedBaseConfigClaim
        FinancialAccount: ConnectEmbeddedFinancialAccountConfigClaim
        FinancialAccountTransactions: ConnectEmbeddedFinancialAccountTransactionsConfigClaim
        InstantPayoutsPromotion: ConnectEmbeddedInstantPayoutsPromotionConfig
        IssuingCard: ConnectEmbeddedIssuingCardConfigClaim
        IssuingCardsList: ConnectEmbeddedIssuingCardsListConfigClaim
        NotificationBanner: ConnectEmbeddedAccountConfigClaim
        PaymentDetails: ConnectEmbeddedPaymentsConfigClaim
        PaymentDisputes: ConnectEmbeddedPaymentDisputesConfig
        Payments: ConnectEmbeddedPaymentsConfigClaim
        PayoutDetails: ConnectEmbeddedBaseConfigClaim
        PayoutReconciliationReport: ConnectEmbeddedBaseConfigClaim
        Payouts: ConnectEmbeddedPayoutsConfig
        PayoutsList: ConnectEmbeddedBaseConfigClaim
        TaxRegistrations: ConnectEmbeddedBaseConfigClaim
        TaxSettings: ConnectEmbeddedBaseConfigClaim
    }
    with
        static member New (accountManagement: ConnectEmbeddedAccountConfigClaim, accountOnboarding: ConnectEmbeddedAccountConfigClaim, balanceReport: ConnectEmbeddedBaseConfigClaim, balances: ConnectEmbeddedPayoutsConfig, disputesList: ConnectEmbeddedDisputesListConfig, documents: ConnectEmbeddedBaseConfigClaim, financialAccount: ConnectEmbeddedFinancialAccountConfigClaim, financialAccountTransactions: ConnectEmbeddedFinancialAccountTransactionsConfigClaim, instantPayoutsPromotion: ConnectEmbeddedInstantPayoutsPromotionConfig, issuingCard: ConnectEmbeddedIssuingCardConfigClaim, issuingCardsList: ConnectEmbeddedIssuingCardsListConfigClaim, notificationBanner: ConnectEmbeddedAccountConfigClaim, paymentDetails: ConnectEmbeddedPaymentsConfigClaim, paymentDisputes: ConnectEmbeddedPaymentDisputesConfig, payments: ConnectEmbeddedPaymentsConfigClaim, payoutDetails: ConnectEmbeddedBaseConfigClaim, payoutReconciliationReport: ConnectEmbeddedBaseConfigClaim, payouts: ConnectEmbeddedPayoutsConfig, payoutsList: ConnectEmbeddedBaseConfigClaim, taxRegistrations: ConnectEmbeddedBaseConfigClaim, taxSettings: ConnectEmbeddedBaseConfigClaim) =
            {
                ConnectEmbeddedAccountSessionCreateComponents.AccountManagement = accountManagement //required
                ConnectEmbeddedAccountSessionCreateComponents.AccountOnboarding = accountOnboarding //required
                ConnectEmbeddedAccountSessionCreateComponents.BalanceReport = balanceReport //required
                ConnectEmbeddedAccountSessionCreateComponents.Balances = balances //required
                ConnectEmbeddedAccountSessionCreateComponents.DisputesList = disputesList //required
                ConnectEmbeddedAccountSessionCreateComponents.Documents = documents //required
                ConnectEmbeddedAccountSessionCreateComponents.FinancialAccount = financialAccount //required
                ConnectEmbeddedAccountSessionCreateComponents.FinancialAccountTransactions = financialAccountTransactions //required
                ConnectEmbeddedAccountSessionCreateComponents.InstantPayoutsPromotion = instantPayoutsPromotion //required
                ConnectEmbeddedAccountSessionCreateComponents.IssuingCard = issuingCard //required
                ConnectEmbeddedAccountSessionCreateComponents.IssuingCardsList = issuingCardsList //required
                ConnectEmbeddedAccountSessionCreateComponents.NotificationBanner = notificationBanner //required
                ConnectEmbeddedAccountSessionCreateComponents.PaymentDetails = paymentDetails //required
                ConnectEmbeddedAccountSessionCreateComponents.PaymentDisputes = paymentDisputes //required
                ConnectEmbeddedAccountSessionCreateComponents.Payments = payments //required
                ConnectEmbeddedAccountSessionCreateComponents.PayoutDetails = payoutDetails //required
                ConnectEmbeddedAccountSessionCreateComponents.PayoutReconciliationReport = payoutReconciliationReport //required
                ConnectEmbeddedAccountSessionCreateComponents.Payouts = payouts //required
                ConnectEmbeddedAccountSessionCreateComponents.PayoutsList = payoutsList //required
                ConnectEmbeddedAccountSessionCreateComponents.TaxRegistrations = taxRegistrations //required
                ConnectEmbeddedAccountSessionCreateComponents.TaxSettings = taxSettings //required
            }

    and ConnectEmbeddedBaseConfigClaim = {
        ///Whether the embedded component is enabled.
        Enabled: bool
        Features: ConnectEmbeddedBaseFeatures
    }
    with
        static member New (enabled: bool, features: ConnectEmbeddedBaseFeatures) =
            {
                ConnectEmbeddedBaseConfigClaim.Enabled = enabled //required
                ConnectEmbeddedBaseConfigClaim.Features = features //required
            }

    and ConnectEmbeddedDisputesListConfig = {
        ///Whether the embedded component is enabled.
        Enabled: bool
        Features: ConnectEmbeddedDisputesListFeatures
    }
    with
        static member New (enabled: bool, features: ConnectEmbeddedDisputesListFeatures) =
            {
                ConnectEmbeddedDisputesListConfig.Enabled = enabled //required
                ConnectEmbeddedDisputesListConfig.Features = features //required
            }

    and ConnectEmbeddedFinancialAccountConfigClaim = {
        ///Whether the embedded component is enabled.
        Enabled: bool
        Features: ConnectEmbeddedFinancialAccountFeatures
    }
    with
        static member New (enabled: bool, features: ConnectEmbeddedFinancialAccountFeatures) =
            {
                ConnectEmbeddedFinancialAccountConfigClaim.Enabled = enabled //required
                ConnectEmbeddedFinancialAccountConfigClaim.Features = features //required
            }

    and ConnectEmbeddedFinancialAccountTransactionsConfigClaim = {
        ///Whether the embedded component is enabled.
        Enabled: bool
        Features: ConnectEmbeddedFinancialAccountTransactionsFeatures
    }
    with
        static member New (enabled: bool, features: ConnectEmbeddedFinancialAccountTransactionsFeatures) =
            {
                ConnectEmbeddedFinancialAccountTransactionsConfigClaim.Enabled = enabled //required
                ConnectEmbeddedFinancialAccountTransactionsConfigClaim.Features = features //required
            }

    and ConnectEmbeddedInstantPayoutsPromotionConfig = {
        ///Whether the embedded component is enabled.
        Enabled: bool
        Features: ConnectEmbeddedInstantPayoutsPromotionFeatures
    }
    with
        static member New (enabled: bool, features: ConnectEmbeddedInstantPayoutsPromotionFeatures) =
            {
                ConnectEmbeddedInstantPayoutsPromotionConfig.Enabled = enabled //required
                ConnectEmbeddedInstantPayoutsPromotionConfig.Features = features //required
            }

    and ConnectEmbeddedIssuingCardConfigClaim = {
        ///Whether the embedded component is enabled.
        Enabled: bool
        Features: ConnectEmbeddedIssuingCardFeatures
    }
    with
        static member New (enabled: bool, features: ConnectEmbeddedIssuingCardFeatures) =
            {
                ConnectEmbeddedIssuingCardConfigClaim.Enabled = enabled //required
                ConnectEmbeddedIssuingCardConfigClaim.Features = features //required
            }

    and ConnectEmbeddedIssuingCardsListConfigClaim = {
        ///Whether the embedded component is enabled.
        Enabled: bool
        Features: ConnectEmbeddedIssuingCardsListFeatures
    }
    with
        static member New (enabled: bool, features: ConnectEmbeddedIssuingCardsListFeatures) =
            {
                ConnectEmbeddedIssuingCardsListConfigClaim.Enabled = enabled //required
                ConnectEmbeddedIssuingCardsListConfigClaim.Features = features //required
            }

    and ConnectEmbeddedPaymentDisputesConfig = {
        ///Whether the embedded component is enabled.
        Enabled: bool
        Features: ConnectEmbeddedPaymentDisputesFeatures
    }
    with
        static member New (enabled: bool, features: ConnectEmbeddedPaymentDisputesFeatures) =
            {
                ConnectEmbeddedPaymentDisputesConfig.Enabled = enabled //required
                ConnectEmbeddedPaymentDisputesConfig.Features = features //required
            }

    and ConnectEmbeddedPaymentsConfigClaim = {
        ///Whether the embedded component is enabled.
        Enabled: bool
        Features: ConnectEmbeddedPaymentsFeatures
    }
    with
        static member New (enabled: bool, features: ConnectEmbeddedPaymentsFeatures) =
            {
                ConnectEmbeddedPaymentsConfigClaim.Enabled = enabled //required
                ConnectEmbeddedPaymentsConfigClaim.Features = features //required
            }

    and ConnectEmbeddedPayoutsConfig = {
        ///Whether the embedded component is enabled.
        Enabled: bool
        Features: ConnectEmbeddedPayoutsFeatures
    }
    with
        static member New (enabled: bool, features: ConnectEmbeddedPayoutsFeatures) =
            {
                ConnectEmbeddedPayoutsConfig.Enabled = enabled //required
                ConnectEmbeddedPayoutsConfig.Features = features //required
            }

    ///Stripe needs to collect certain pieces of information about each account
    ///created. These requirements can differ depending on the account's country. The
    ///Country Specs API makes these rules available to your integration.
    ///You can also view the information from this API call as [an online
    ///guide](/docs/connect/required-verification-information).
    and CountrySpec = {
        ///The default currency for this country. This applies to both payment methods and bank accounts.
        DefaultCurrency: IsoTypes.IsoCurrencyCode
        ///Unique identifier for the object. Represented as the ISO country code for this country.
        Id: string
        ///Currencies that can be accepted in the specific country (for transfers).
        SupportedBankAccountCurrencies: Map<string, string list>
        ///Currencies that can be accepted in the specified country (for payments).
        SupportedPaymentCurrencies: string list
        ///Payment methods available in the specified country. You may need to enable some payment methods (e.g., [ACH](https://stripe.com/docs/ach)) on your account before they appear in this list. The `stripe` payment method refers to [charging through your platform](https://stripe.com/docs/connect/destination-charges).
        SupportedPaymentMethods: string list
        ///Countries that can accept transfers from the specified country.
        SupportedTransferCountries: string list
        VerificationFields: CountrySpecVerificationFields
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "country_spec"

        static member New (defaultCurrency: IsoTypes.IsoCurrencyCode, id: string, supportedBankAccountCurrencies: Map<string, string list>, supportedPaymentCurrencies: string list, supportedPaymentMethods: string list, supportedTransferCountries: string list, verificationFields: CountrySpecVerificationFields) =
            {
                CountrySpec.DefaultCurrency = defaultCurrency //required
                CountrySpec.Id = id //required
                CountrySpec.SupportedBankAccountCurrencies = supportedBankAccountCurrencies //required
                CountrySpec.SupportedPaymentCurrencies = supportedPaymentCurrencies //required
                CountrySpec.SupportedPaymentMethods = supportedPaymentMethods //required
                CountrySpec.SupportedTransferCountries = supportedTransferCountries //required
                CountrySpec.VerificationFields = verificationFields //required
            }

    and CountrySpecVerificationFieldDetails = {
        ///Additional fields which are only required for some users.
        Additional: string list
        ///Fields which every account must eventually provide.
        Minimum: string list
    }
    with
        static member New (additional: string list, minimum: string list) =
            {
                CountrySpecVerificationFieldDetails.Additional = additional //required
                CountrySpecVerificationFieldDetails.Minimum = minimum //required
            }

    and CountrySpecVerificationFields = {
        Company: CountrySpecVerificationFieldDetails
        Individual: CountrySpecVerificationFieldDetails
    }
    with
        static member New (company: CountrySpecVerificationFieldDetails, individual: CountrySpecVerificationFieldDetails) =
            {
                CountrySpecVerificationFields.Company = company //required
                CountrySpecVerificationFields.Individual = individual //required
            }

    ///A coupon contains information about a percent-off or amount-off discount you
    ///might want to apply to a customer. Coupons may be applied to [subscriptions](https://api.stripe.com#subscriptions), [invoices](https://api.stripe.com#invoices),
    ///[checkout sessions](https://docs.stripe.com/api/checkout/sessions), [quotes](https://api.stripe.com#quotes), and more. Coupons do not work with conventional one-off [charges](/api/charges/create) or [payment intents](https://docs.stripe.com/api/payment_intents).
    and Coupon = {
        ///Amount (in the `currency` specified) that will be taken off the subtotal of any invoices for this customer.
        AmountOff: int option
        AppliesTo: CouponAppliesTo option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///If `amount_off` has been set, the three-letter [ISO code for the currency](https://stripe.com/docs/currencies) of the amount to take off.
        Currency: IsoTypes.IsoCurrencyCode option
        ///Coupons defined in each available currency option. Each key must be a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html) and a [supported currency](https://stripe.com/docs/currencies).
        CurrencyOptions: Map<string, string list> option
        ///One of `forever`, `once`, or `repeating`. Describes how long a customer who applies this coupon will get the discount.
        Duration: CouponDuration
        ///If `duration` is `repeating`, the number of months the coupon applies. Null if coupon `duration` is `forever` or `once`.
        DurationInMonths: int option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Maximum number of times this coupon can be redeemed, in total, across all customers, before it is no longer valid.
        MaxRedemptions: int option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///Name of the coupon displayed to customers on for instance invoices or receipts.
        Name: string option
        ///Percent that will be taken off the subtotal of any invoices for this customer for the duration of the coupon. For example, a coupon with percent_off of 50 will make a $ (or local equivalent)100 invoice $ (or local equivalent)50 instead.
        PercentOff: decimal option
        ///Date after which the coupon can no longer be redeemed.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]RedeemBy: DateTime option
        ///Number of times this coupon has been applied to a customer.
        TimesRedeemed: int
        ///Taking account of the above properties, whether this coupon can still be applied to a customer.
        Valid: bool
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "coupon"

        static member New (amountOff: int option, created: DateTime, currency: IsoTypes.IsoCurrencyCode option, duration: CouponDuration, durationInMonths: int option, id: string, livemode: bool, maxRedemptions: int option, metadata: Map<string, string> option, name: string option, percentOff: decimal option, redeemBy: DateTime option, timesRedeemed: int, valid: bool, ?appliesTo: CouponAppliesTo, ?currencyOptions: Map<string, string list>) =
            {
                Coupon.AmountOff = amountOff //required
                Coupon.Created = created //required
                Coupon.Currency = currency //required
                Coupon.Duration = duration //required
                Coupon.DurationInMonths = durationInMonths //required
                Coupon.Id = id //required
                Coupon.Livemode = livemode //required
                Coupon.MaxRedemptions = maxRedemptions //required
                Coupon.Metadata = metadata //required
                Coupon.Name = name //required
                Coupon.PercentOff = percentOff //required
                Coupon.RedeemBy = redeemBy //required
                Coupon.TimesRedeemed = timesRedeemed //required
                Coupon.Valid = valid //required
                Coupon.AppliesTo = appliesTo
                Coupon.CurrencyOptions = currencyOptions
            }

    and CouponAppliesTo = {
        ///A list of product IDs this coupon applies to
        Products: string list
    }
    with
        static member New (products: string list) =
            {
                CouponAppliesTo.Products = products //required
            }

    and CreditBalance = {
        AvailableBalance: BillingCreditGrantsResourceAmount
        LedgerBalance: BillingCreditGrantsResourceAmount
    }
    with
        static member New (availableBalance: BillingCreditGrantsResourceAmount, ledgerBalance: BillingCreditGrantsResourceAmount) =
            {
                CreditBalance.AvailableBalance = availableBalance //required
                CreditBalance.LedgerBalance = ledgerBalance //required
            }

    ///Issue a credit note to adjust an invoice's amount after the invoice is finalized.
    ///Related guide: [Credit notes](https://docs.stripe.com/billing/invoices/credit-notes)
    and CreditNote = {
        ///The integer amount in cents (or local equivalent) representing the total amount of the credit note, including tax.
        Amount: int
        ///This is the sum of all the shipping amounts.
        AmountShipping: int
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///ID of the customer.
        Customer: CreditNoteCustomer'AnyOf
        ///ID of the account representing the customer.
        CustomerAccount: string option
        ///Customer balance transaction related to this credit note.
        CustomerBalanceTransaction: CreditNoteCustomerBalanceTransaction'AnyOf option
        ///The integer amount in cents (or local equivalent) representing the total amount of discount that was credited.
        DiscountAmount: int
        ///The aggregate amounts calculated per discount for all line items.
        DiscountAmounts: DiscountsResourceDiscountAmount list
        ///The date when this credit note is in effect. Same as `created` unless overwritten. When defined, this value replaces the system-generated 'Date of issue' printed on the credit note PDF.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]EffectiveAt: DateTime option
        ///Unique identifier for the object.
        Id: string
        ///ID of the invoice.
        Invoice: CreditNoteInvoice'AnyOf
        ///Line items that make up the credit note
        Lines: CreditNoteLines
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Customer-facing text that appears on the credit note PDF.
        Memo: string option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///A unique number that identifies this particular credit note and appears on the PDF of the credit note and its associated invoice.
        Number: string
        ///Amount that was credited outside of Stripe.
        OutOfBandAmount: int option
        ///The link to download the PDF of the credit note.
        Pdf: string
        ///The amount of the credit note that was refunded to the customer, credited to the customer's balance, credited outside of Stripe, or any combination thereof.
        PostPaymentAmount: int
        ///The amount of the credit note by which the invoice's `amount_remaining` and `amount_due` were reduced.
        PrePaymentAmount: int
        ///The pretax credit amounts (ex: discount, credit grants, etc) for all line items.
        PretaxCreditAmounts: CreditNotesPretaxCreditAmount list
        ///Reason for issuing this credit note, one of `duplicate`, `fraudulent`, `order_change`, or `product_unsatisfactory`
        Reason: CreditNoteReason option
        ///Refunds related to this credit note.
        Refunds: CreditNoteRefund list
        ///The details of the cost of shipping, including the ShippingRate applied to the invoice.
        ShippingCost: InvoicesResourceShippingCost option
        ///Status of this credit note, one of `issued` or `void`. Learn more about [voiding credit notes](https://docs.stripe.com/billing/invoices/credit-notes#voiding).
        Status: CreditNoteStatus
        ///The integer amount in cents (or local equivalent) representing the amount of the credit note, excluding exclusive tax and invoice level discounts.
        Subtotal: int
        ///The integer amount in cents (or local equivalent) representing the amount of the credit note, excluding all tax and invoice level discounts.
        SubtotalExcludingTax: int option
        ///The integer amount in cents (or local equivalent) representing the total amount of the credit note, including tax and all discount.
        Total: int
        ///The integer amount in cents (or local equivalent) representing the total amount of the credit note, excluding tax, but including discounts.
        TotalExcludingTax: int option
        ///The aggregate tax information for all line items.
        TotalTaxes: BillingBillResourceInvoicingTaxesTax list option
        ///Type of this credit note, one of `pre_payment` or `post_payment`. A `pre_payment` credit note means it was issued when the invoice was open. A `post_payment` credit note means it was issued when the invoice was paid.
        Type: CreditNoteType
        ///The time that the credit note was voided.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]VoidedAt: DateTime option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "credit_note"

        static member New (amount: int, amountShipping: int, created: DateTime, currency: IsoTypes.IsoCurrencyCode, customer: CreditNoteCustomer'AnyOf, customerAccount: string option, customerBalanceTransaction: CreditNoteCustomerBalanceTransaction'AnyOf option, discountAmount: int, discountAmounts: DiscountsResourceDiscountAmount list, effectiveAt: DateTime option, id: string, invoice: CreditNoteInvoice'AnyOf, lines: CreditNoteLines, livemode: bool, memo: string option, metadata: Map<string, string> option, number: string, outOfBandAmount: int option, pdf: string, postPaymentAmount: int, prePaymentAmount: int, pretaxCreditAmounts: CreditNotesPretaxCreditAmount list, reason: CreditNoteReason option, refunds: CreditNoteRefund list, shippingCost: InvoicesResourceShippingCost option, status: CreditNoteStatus, subtotal: int, subtotalExcludingTax: int option, total: int, totalExcludingTax: int option, totalTaxes: BillingBillResourceInvoicingTaxesTax list option, ``type``: CreditNoteType, voidedAt: DateTime option) =
            {
                CreditNote.Amount = amount //required
                CreditNote.AmountShipping = amountShipping //required
                CreditNote.Created = created //required
                CreditNote.Currency = currency //required
                CreditNote.Customer = customer //required
                CreditNote.CustomerAccount = customerAccount //required
                CreditNote.CustomerBalanceTransaction = customerBalanceTransaction //required
                CreditNote.DiscountAmount = discountAmount //required
                CreditNote.DiscountAmounts = discountAmounts //required
                CreditNote.EffectiveAt = effectiveAt //required
                CreditNote.Id = id //required
                CreditNote.Invoice = invoice //required
                CreditNote.Lines = lines //required
                CreditNote.Livemode = livemode //required
                CreditNote.Memo = memo //required
                CreditNote.Metadata = metadata //required
                CreditNote.Number = number //required
                CreditNote.OutOfBandAmount = outOfBandAmount //required
                CreditNote.Pdf = pdf //required
                CreditNote.PostPaymentAmount = postPaymentAmount //required
                CreditNote.PrePaymentAmount = prePaymentAmount //required
                CreditNote.PretaxCreditAmounts = pretaxCreditAmounts //required
                CreditNote.Reason = reason //required
                CreditNote.Refunds = refunds //required
                CreditNote.ShippingCost = shippingCost //required
                CreditNote.Status = status //required
                CreditNote.Subtotal = subtotal //required
                CreditNote.SubtotalExcludingTax = subtotalExcludingTax //required
                CreditNote.Total = total //required
                CreditNote.TotalExcludingTax = totalExcludingTax //required
                CreditNote.TotalTaxes = totalTaxes //required
                CreditNote.Type = ``type`` //required
                CreditNote.VoidedAt = voidedAt //required
            }

    and CreditNoteCustomer'AnyOf =
        | String of string
        | Customer of Customer
        | DeletedCustomer of DeletedCustomer

    and CreditNoteCustomerBalanceTransaction'AnyOf =
        | String of string
        | CustomerBalanceTransaction of CustomerBalanceTransaction

    and CreditNoteInvoice'AnyOf =
        | String of string
        | Invoice of Invoice

    ///Line items that make up the credit note
    and CreditNoteLines = {
        ///Details about each object.
        Data: CreditNoteLineItem list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: CreditNoteLineItem list, hasMore: bool, url: string) =
            {
                CreditNoteLines.Data = data //required
                CreditNoteLines.HasMore = hasMore //required
                CreditNoteLines.Url = url //required
            }

    ///The credit note line item object
    and CreditNoteLineItem = {
        ///The integer amount in cents (or local equivalent) representing the gross amount being credited for this line item, excluding (exclusive) tax and discounts.
        Amount: int
        ///Description of the item being credited.
        Description: string option
        ///The integer amount in cents (or local equivalent) representing the discount being credited for this line item.
        DiscountAmount: int
        ///The amount of discount calculated per discount for this line item
        DiscountAmounts: DiscountsResourceDiscountAmount list
        ///Unique identifier for the object.
        Id: string
        ///ID of the invoice line item being credited
        InvoiceLineItem: string option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///The pretax credit amounts (ex: discount, credit grants, etc) for this line item.
        PretaxCreditAmounts: CreditNotesPretaxCreditAmount list
        ///The number of units of product being credited.
        Quantity: int option
        ///The tax rates which apply to the line item.
        TaxRates: TaxRate list
        ///The tax information of the line item.
        Taxes: BillingBillResourceInvoicingTaxesTax list option
        ///The type of the credit note line item, one of `invoice_line_item` or `custom_line_item`. When the type is `invoice_line_item` there is an additional `invoice_line_item` property on the resource the value of which is the id of the credited line item on the invoice.
        Type: CreditNoteLineItemType
        ///The cost of each unit of product being credited.
        UnitAmount: int option
        ///Same as `unit_amount`, but contains a decimal value with at most 12 decimal places.
        UnitAmountDecimal: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "credit_note_line_item"

        static member New (amount: int, description: string option, discountAmount: int, discountAmounts: DiscountsResourceDiscountAmount list, id: string, livemode: bool, metadata: Map<string, string> option, pretaxCreditAmounts: CreditNotesPretaxCreditAmount list, quantity: int option, taxRates: TaxRate list, taxes: BillingBillResourceInvoicingTaxesTax list option, ``type``: CreditNoteLineItemType, unitAmount: int option, unitAmountDecimal: string option, ?invoiceLineItem: string) =
            {
                CreditNoteLineItem.Amount = amount //required
                CreditNoteLineItem.Description = description //required
                CreditNoteLineItem.DiscountAmount = discountAmount //required
                CreditNoteLineItem.DiscountAmounts = discountAmounts //required
                CreditNoteLineItem.Id = id //required
                CreditNoteLineItem.Livemode = livemode //required
                CreditNoteLineItem.Metadata = metadata //required
                CreditNoteLineItem.PretaxCreditAmounts = pretaxCreditAmounts //required
                CreditNoteLineItem.Quantity = quantity //required
                CreditNoteLineItem.TaxRates = taxRates //required
                CreditNoteLineItem.Taxes = taxes //required
                CreditNoteLineItem.Type = ``type`` //required
                CreditNoteLineItem.UnitAmount = unitAmount //required
                CreditNoteLineItem.UnitAmountDecimal = unitAmountDecimal //required
                CreditNoteLineItem.InvoiceLineItem = invoiceLineItem
            }

    and CreditNoteRefund = {
        ///Amount of the refund that applies to this credit note, in cents (or local equivalent).
        AmountRefunded: int
        ///The PaymentRecord refund details associated with this credit note refund.
        PaymentRecordRefund: CreditNotesPaymentRecordRefund option
        ///ID of the refund.
        Refund: CreditNoteRefundRefund'AnyOf
        ///Type of the refund, one of `refund` or `payment_record_refund`.
        Type: CreditNoteRefundType option
    }
    with
        static member New (amountRefunded: int, paymentRecordRefund: CreditNotesPaymentRecordRefund option, refund: CreditNoteRefundRefund'AnyOf, ``type``: CreditNoteRefundType option) =
            {
                CreditNoteRefund.AmountRefunded = amountRefunded //required
                CreditNoteRefund.PaymentRecordRefund = paymentRecordRefund //required
                CreditNoteRefund.Refund = refund //required
                CreditNoteRefund.Type = ``type`` //required
            }

    and CreditNoteRefundRefund'AnyOf =
        | String of string
        | Refund of Refund

    and CreditNotesPretaxCreditAmount = {
        ///The amount, in cents (or local equivalent), of the pretax credit amount.
        Amount: int
        ///The credit balance transaction that was applied to get this pretax credit amount.
        CreditBalanceTransaction: CreditNotesPretaxCreditAmountCreditBalanceTransaction'AnyOf option
        ///The discount that was applied to get this pretax credit amount.
        Discount: CreditNotesPretaxCreditAmountDiscount'AnyOf option
        ///Type of the pretax credit amount referenced.
        Type: CreditNotesPretaxCreditAmountType
    }
    with
        static member New (amount: int, ``type``: CreditNotesPretaxCreditAmountType, ?creditBalanceTransaction: CreditNotesPretaxCreditAmountCreditBalanceTransaction'AnyOf, ?discount: CreditNotesPretaxCreditAmountDiscount'AnyOf) =
            {
                CreditNotesPretaxCreditAmount.Amount = amount //required
                CreditNotesPretaxCreditAmount.Type = ``type`` //required
                CreditNotesPretaxCreditAmount.CreditBalanceTransaction = creditBalanceTransaction
                CreditNotesPretaxCreditAmount.Discount = discount
            }

    and CreditNotesPretaxCreditAmountCreditBalanceTransaction'AnyOf =
        | String of string
        | BillingCreditBalanceTransaction of BillingCreditBalanceTransaction

    and CreditNotesPretaxCreditAmountDiscount'AnyOf =
        | String of string
        | Discount of Discount
        | DeletedDiscount of DeletedDiscount

    and CurrencyOption = {
        ///When set, provides configuration for the amount to be adjusted by the customer during Checkout Sessions and Payment Links.
        CustomUnitAmount: CustomUnitAmount option
        ///Only required if a [default tax behavior](https://docs.stripe.com/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed.
        TaxBehavior: CurrencyOptionTaxBehavior option
        ///Each element represents a pricing tier. This parameter requires `billing_scheme` to be set to `tiered`. See also the documentation for `billing_scheme`.
        Tiers: PriceTier list option
        ///The unit amount in cents (or local equivalent) to be charged, represented as a whole integer if possible. Only set if `billing_scheme=per_unit`.
        UnitAmount: int option
        ///The unit amount in cents (or local equivalent) to be charged, represented as a decimal string with at most 12 decimal places. Only set if `billing_scheme=per_unit`.
        UnitAmountDecimal: string option
    }
    with
        static member New (customUnitAmount: CustomUnitAmount option, taxBehavior: CurrencyOptionTaxBehavior option, unitAmount: int option, unitAmountDecimal: string option, ?tiers: PriceTier list) =
            {
                CurrencyOption.CustomUnitAmount = customUnitAmount //required
                CurrencyOption.TaxBehavior = taxBehavior //required
                CurrencyOption.UnitAmount = unitAmount //required
                CurrencyOption.UnitAmountDecimal = unitAmountDecimal //required
                CurrencyOption.Tiers = tiers
            }

    ///This object represents a customer of your business. Use it to [create recurring charges](https://docs.stripe.com/invoicing/customer), [save payment](https://docs.stripe.com/payments/save-during-payment) and contact information,
    ///and track payments that belong to the same customer.
    and Customer = {
        ///The customer's address.
        Address: Address option
        ///The current balance, if any, that's stored on the customer in their default currency. If negative, the customer has credit to apply to their next invoice. If positive, the customer has an amount owed that's added to their next invoice. The balance only considers amounts that Stripe hasn't successfully applied to any invoice. It doesn't reflect unpaid invoices. This balance is only taken into account after invoices finalize. For multi-currency balances, see [invoice_credit_balance](https://docs.stripe.com/api/customers/object#customer_object-invoice_credit_balance).
        Balance: int option
        ///The customer's business name.
        BusinessName: string option
        ///The current funds being held by Stripe on behalf of the customer. You can apply these funds towards payment intents when the source is "cash_balance". The `settings[reconciliation_mode]` field describes if these funds apply to these payment intents manually or automatically.
        CashBalance: CashBalance option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO code for the currency](https://stripe.com/docs/currencies) the customer can be charged in for recurring billing purposes.
        Currency: IsoTypes.IsoCurrencyCode option
        ///The ID of an Account representing a customer. You can use this ID with any v1 API that accepts a customer_account parameter.
        CustomerAccount: string option
        ///ID of the default payment source for the customer.
        ///If you use payment methods created through the PaymentMethods API, see the [invoice_settings.default_payment_method](https://docs.stripe.com/api/customers/object#customer_object-invoice_settings-default_payment_method) field instead.
        DefaultSource: CustomerDefaultSource'AnyOf option
        ///Tracks the most recent state change on any invoice belonging to the customer. Paying an invoice or marking it uncollectible via the API will set this field to false. An automatic payment failure or passing the `invoice.due_date` will set this field to `true`.
        ///If an invoice becomes uncollectible by [dunning](https://docs.stripe.com/billing/automatic-collection), `delinquent` doesn't reset to `false`.
        ///If you care whether the customer has paid their most recent subscription invoice, use `subscription.status` instead. Paying or marking uncollectible any customer invoice regardless of whether it is the latest invoice for a subscription will always set this field to `false`.
        Delinquent: bool option
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///Describes the current discount active on the customer, if there is one.
        Discount: Discount option
        ///The customer's email address.
        Email: string option
        ///Unique identifier for the object.
        Id: string
        ///The customer's individual name.
        IndividualName: string option
        ///The current multi-currency balances, if any, that's stored on the customer. If positive in a currency, the customer has a credit to apply to their next invoice denominated in that currency. If negative, the customer has an amount owed that's added to their next invoice denominated in that currency. These balances don't apply to unpaid invoices. They solely track amounts that Stripe hasn't successfully applied to any invoice. Stripe only applies a balance in a specific currency to an invoice after that invoice (which is in the same currency) finalizes.
        InvoiceCreditBalance: Map<string, string list> option
        ///The prefix for the customer used to generate unique invoice numbers.
        InvoicePrefix: string option
        InvoiceSettings: InvoiceSettingCustomerSetting option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///The customer's full name or business name.
        Name: string option
        ///The suffix of the customer's next invoice number (for example, 0001). When the account uses account level sequencing, this parameter is ignored in API requests and the field omitted in API responses.
        NextInvoiceSequence: int option
        ///The customer's phone number.
        Phone: string option
        ///The customer's preferred locales (languages), ordered by preference.
        PreferredLocales: string list option
        ///Mailing and shipping address for the customer. Appears on invoices emailed to this customer.
        Shipping: Shipping option
        ///The customer's payment sources, if any.
        Sources: CustomerSources option
        ///The customer's current subscriptions, if any.
        Subscriptions: CustomerSubscriptions option
        Tax: CustomerTax option
        ///Describes the customer's tax exemption status, which is `none`, `exempt`, or `reverse`. When set to `reverse`, invoice and receipt PDFs include the following text: **"Reverse charge"**.
        TaxExempt: CustomerTaxExempt option
        ///The customer's tax IDs.
        TaxIds: CustomerTaxIds option
        ///ID of the test clock that this customer belongs to.
        TestClock: CustomerTestClock'AnyOf option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "customer"

        static member New (created: DateTime, defaultSource: CustomerDefaultSource'AnyOf option, description: string option, email: string option, id: string, livemode: bool, shipping: Shipping option, ?address: Address option, ?balance: int, ?businessName: string, ?cashBalance: CashBalance option, ?currency: IsoTypes.IsoCurrencyCode option, ?customerAccount: string option, ?delinquent: bool option, ?discount: Discount option, ?individualName: string, ?invoiceCreditBalance: Map<string, string list>, ?invoicePrefix: string option, ?invoiceSettings: InvoiceSettingCustomerSetting, ?metadata: Map<string, string>, ?name: string option, ?nextInvoiceSequence: int, ?phone: string option, ?preferredLocales: string list option, ?sources: CustomerSources, ?subscriptions: CustomerSubscriptions, ?tax: CustomerTax, ?taxExempt: CustomerTaxExempt option, ?taxIds: CustomerTaxIds, ?testClock: CustomerTestClock'AnyOf option) =
            {
                Customer.Created = created //required
                Customer.DefaultSource = defaultSource //required
                Customer.Description = description //required
                Customer.Email = email //required
                Customer.Id = id //required
                Customer.Livemode = livemode //required
                Customer.Shipping = shipping //required
                Customer.Address = address |> Option.flatten
                Customer.Balance = balance
                Customer.BusinessName = businessName
                Customer.CashBalance = cashBalance |> Option.flatten
                Customer.Currency = currency |> Option.flatten
                Customer.CustomerAccount = customerAccount |> Option.flatten
                Customer.Delinquent = delinquent |> Option.flatten
                Customer.Discount = discount |> Option.flatten
                Customer.IndividualName = individualName
                Customer.InvoiceCreditBalance = invoiceCreditBalance
                Customer.InvoicePrefix = invoicePrefix |> Option.flatten
                Customer.InvoiceSettings = invoiceSettings
                Customer.Metadata = metadata
                Customer.Name = name |> Option.flatten
                Customer.NextInvoiceSequence = nextInvoiceSequence
                Customer.Phone = phone |> Option.flatten
                Customer.PreferredLocales = preferredLocales |> Option.flatten
                Customer.Sources = sources
                Customer.Subscriptions = subscriptions
                Customer.Tax = tax
                Customer.TaxExempt = taxExempt |> Option.flatten
                Customer.TaxIds = taxIds
                Customer.TestClock = testClock |> Option.flatten
            }

    and CustomerDefaultSource'AnyOf =
        | String of string
        | PaymentSource of PaymentSource

    and CustomerTestClock'AnyOf =
        | String of string
        | TestHelpersTestClock of TestHelpersTestClock

    ///The customer's payment sources, if any.
    and CustomerSources = {
        ///Details about each object.
        Data: PaymentSource list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: PaymentSource list, hasMore: bool, url: string) =
            {
                CustomerSources.Data = data //required
                CustomerSources.HasMore = hasMore //required
                CustomerSources.Url = url //required
            }

    ///The customer's current subscriptions, if any.
    and CustomerSubscriptions = {
        ///Details about each object.
        Data: Subscription list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: Subscription list, hasMore: bool, url: string) =
            {
                CustomerSubscriptions.Data = data //required
                CustomerSubscriptions.HasMore = hasMore //required
                CustomerSubscriptions.Url = url //required
            }

    ///The customer's tax IDs.
    and CustomerTaxIds = {
        ///Details about each object.
        Data: TaxId list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: TaxId list, hasMore: bool, url: string) =
            {
                CustomerTaxIds.Data = data //required
                CustomerTaxIds.HasMore = hasMore //required
                CustomerTaxIds.Url = url //required
            }

    and CustomerAcceptance = {
        ///The time that the customer accepts the mandate.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]AcceptedAt: DateTime option
        Offline: OfflineAcceptance option
        Online: OnlineAcceptance option
        ///The mandate includes the type of customer acceptance information, such as: `online` or `offline`.
        Type: CustomerAcceptanceType
    }
    with
        static member New (acceptedAt: DateTime option, ``type``: CustomerAcceptanceType, ?offline: OfflineAcceptance, ?online: OnlineAcceptance) =
            {
                CustomerAcceptance.AcceptedAt = acceptedAt //required
                CustomerAcceptance.Type = ``type`` //required
                CustomerAcceptance.Offline = offline
                CustomerAcceptance.Online = online
            }

    and CustomerBalanceCustomerBalanceSettings = {
        ///The configuration for how funds that land in the customer cash balance are reconciled.
        ReconciliationMode: CustomerBalanceCustomerBalanceSettingsReconciliationMode
        ///A flag to indicate if reconciliation mode returned is the user's default or is specific to this customer cash balance
        UsingMerchantDefault: bool
    }
    with
        static member New (reconciliationMode: CustomerBalanceCustomerBalanceSettingsReconciliationMode, usingMerchantDefault: bool) =
            {
                CustomerBalanceCustomerBalanceSettings.ReconciliationMode = reconciliationMode //required
                CustomerBalanceCustomerBalanceSettings.UsingMerchantDefault = usingMerchantDefault //required
            }

    and CustomerBalanceResourceCashBalanceTransactionResourceAdjustedForOverdraft = {
        ///The [Balance Transaction](https://docs.stripe.com/api/balance_transactions/object) that corresponds to funds taken out of your Stripe balance.
        BalanceTransaction: CustomerBalanceResourceCashBalanceTransactionResourceAdjustedForOverdraftBalanceTransaction'AnyOf
        ///The [Cash Balance Transaction](https://docs.stripe.com/api/cash_balance_transactions/object) that brought the customer balance negative, triggering the clawback of funds.
        LinkedTransaction: CustomerBalanceResourceCashBalanceTransactionResourceAdjustedForOverdraftLinkedTransaction'AnyOf
    }
    with
        static member New (balanceTransaction: CustomerBalanceResourceCashBalanceTransactionResourceAdjustedForOverdraftBalanceTransaction'AnyOf, linkedTransaction: CustomerBalanceResourceCashBalanceTransactionResourceAdjustedForOverdraftLinkedTransaction'AnyOf) =
            {
                CustomerBalanceResourceCashBalanceTransactionResourceAdjustedForOverdraft.BalanceTransaction = balanceTransaction //required
                CustomerBalanceResourceCashBalanceTransactionResourceAdjustedForOverdraft.LinkedTransaction = linkedTransaction //required
            }

    and CustomerBalanceResourceCashBalanceTransactionResourceAdjustedForOverdraftBalanceTransaction'AnyOf =
        | String of string
        | BalanceTransaction of BalanceTransaction

    and CustomerBalanceResourceCashBalanceTransactionResourceAdjustedForOverdraftLinkedTransaction'AnyOf =
        | String of string
        | CustomerCashBalanceTransaction of CustomerCashBalanceTransaction

    and CustomerBalanceResourceCashBalanceTransactionResourceAppliedToPaymentTransaction = {
        ///The [Payment Intent](https://docs.stripe.com/api/payment_intents/object) that funds were applied to.
        PaymentIntent: CustomerBalanceResourceCashBalanceTransactionResourceAppliedToPaymentTransactionPaymentIntent'AnyOf
    }
    with
        static member New (paymentIntent: CustomerBalanceResourceCashBalanceTransactionResourceAppliedToPaymentTransactionPaymentIntent'AnyOf) =
            {
                CustomerBalanceResourceCashBalanceTransactionResourceAppliedToPaymentTransaction.PaymentIntent = paymentIntent //required
            }

    and CustomerBalanceResourceCashBalanceTransactionResourceAppliedToPaymentTransactionPaymentIntent'AnyOf =
        | String of string
        | PaymentIntent of PaymentIntent

    and CustomerBalanceResourceCashBalanceTransactionResourceFundedTransaction = {
        BankTransfer: CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransfer
    }
    with
        static member New (bankTransfer: CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransfer) =
            {
                CustomerBalanceResourceCashBalanceTransactionResourceFundedTransaction.BankTransfer = bankTransfer //required
            }

    and CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransfer = {
        EuBankTransfer: CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceEuBankTransfer option
        GbBankTransfer: CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceGbBankTransfer option
        JpBankTransfer: CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceJpBankTransfer option
        ///The user-supplied reference field on the bank transfer.
        Reference: string option
        ///The funding method type used to fund the customer balance. Permitted values include: `eu_bank_transfer`, `gb_bank_transfer`, `jp_bank_transfer`, `mx_bank_transfer`, or `us_bank_transfer`.
        Type: CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferType
        UsBankTransfer: CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceUsBankTransfer option
    }
    with
        static member New (reference: string option, ``type``: CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferType, ?euBankTransfer: CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceEuBankTransfer, ?gbBankTransfer: CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceGbBankTransfer, ?jpBankTransfer: CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceJpBankTransfer, ?usBankTransfer: CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceUsBankTransfer) =
            {
                CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransfer.Reference = reference //required
                CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransfer.Type = ``type`` //required
                CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransfer.EuBankTransfer = euBankTransfer
                CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransfer.GbBankTransfer = gbBankTransfer
                CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransfer.JpBankTransfer = jpBankTransfer
                CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransfer.UsBankTransfer = usBankTransfer
            }

    and CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceUsBankTransfer = {
        ///The banking network used for this funding.
        Network: CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceUsBankTransferNetwork option
        ///The full name of the sender, as supplied by the sending bank.
        SenderName: string option
    }
    with
        static member New (senderName: string option, ?network: CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceUsBankTransferNetwork) =
            {
                CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceUsBankTransfer.SenderName = senderName //required
                CustomerBalanceResourceCashBalanceTransactionResourceFundedTransactionResourceBankTransferResourceUsBankTransfer.Network = network
            }

    and CustomerBalanceResourceCashBalanceTransactionResourceRefundedFromPaymentTransaction = {
        ///The [Refund](https://docs.stripe.com/api/refunds/object) that moved these funds into the customer's cash balance.
        Refund: CustomerBalanceResourceCashBalanceTransactionResourceRefundedFromPaymentTransactionRefund'AnyOf
    }
    with
        static member New (refund: CustomerBalanceResourceCashBalanceTransactionResourceRefundedFromPaymentTransactionRefund'AnyOf) =
            {
                CustomerBalanceResourceCashBalanceTransactionResourceRefundedFromPaymentTransaction.Refund = refund //required
            }

    and CustomerBalanceResourceCashBalanceTransactionResourceRefundedFromPaymentTransactionRefund'AnyOf =
        | String of string
        | Refund of Refund

    and CustomerBalanceResourceCashBalanceTransactionResourceTransferredToBalance = {
        ///The [Balance Transaction](https://docs.stripe.com/api/balance_transactions/object) that corresponds to funds transferred to your Stripe balance.
        BalanceTransaction: CustomerBalanceResourceCashBalanceTransactionResourceTransferredToBalanceBalanceTransaction'AnyOf
    }
    with
        static member New (balanceTransaction: CustomerBalanceResourceCashBalanceTransactionResourceTransferredToBalanceBalanceTransaction'AnyOf) =
            {
                CustomerBalanceResourceCashBalanceTransactionResourceTransferredToBalance.BalanceTransaction = balanceTransaction //required
            }

    and CustomerBalanceResourceCashBalanceTransactionResourceTransferredToBalanceBalanceTransaction'AnyOf =
        | String of string
        | BalanceTransaction of BalanceTransaction

    and CustomerBalanceResourceCashBalanceTransactionResourceUnappliedFromPaymentTransaction = {
        ///The [Payment Intent](https://docs.stripe.com/api/payment_intents/object) that funds were unapplied from.
        PaymentIntent: CustomerBalanceResourceCashBalanceTransactionResourceUnappliedFromPaymentTransactionPaymentIntent'AnyOf
    }
    with
        static member New (paymentIntent: CustomerBalanceResourceCashBalanceTransactionResourceUnappliedFromPaymentTransactionPaymentIntent'AnyOf) =
            {
                CustomerBalanceResourceCashBalanceTransactionResourceUnappliedFromPaymentTransaction.PaymentIntent = paymentIntent //required
            }

    and CustomerBalanceResourceCashBalanceTransactionResourceUnappliedFromPaymentTransactionPaymentIntent'AnyOf =
        | String of string
        | PaymentIntent of PaymentIntent

    ///Each customer has a [Balance](https://docs.stripe.com/api/customers/object#customer_object-balance) value,
    ///which denotes a debit or credit that's automatically applied to their next invoice upon finalization.
    ///You may modify the value directly by using the [update customer API](https://docs.stripe.com/api/customers/update),
    ///or by creating a Customer Balance Transaction, which increments or decrements the customer's `balance` by the specified `amount`.
    ///Related guide: [Customer balance](https://docs.stripe.com/billing/customer/balance)
    and CustomerBalanceTransaction = {
        ///The amount of the transaction. A negative value is a credit for the customer's balance, and a positive value is a debit to the customer's `balance`.
        Amount: int
        ///The ID of the checkout session (if any) that created the transaction.
        CheckoutSession: CustomerBalanceTransactionCheckoutSession'AnyOf option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The ID of the credit note (if any) related to the transaction.
        CreditNote: CustomerBalanceTransactionCreditNote'AnyOf option
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///The ID of the customer the transaction belongs to.
        Customer: CustomerBalanceTransactionCustomer'AnyOf
        ///The ID of an Account representing a customer that the transaction belongs to.
        CustomerAccount: string option
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///The customer's `balance` after the transaction was applied. A negative value decreases the amount due on the customer's next invoice. A positive value increases the amount due on the customer's next invoice.
        EndingBalance: int
        ///Unique identifier for the object.
        Id: string
        ///The ID of the invoice (if any) related to the transaction.
        Invoice: CustomerBalanceTransactionInvoice'AnyOf option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///Transaction type: `adjustment`, `applied_to_invoice`, `credit_note`, `initial`, `invoice_overpaid`, `invoice_too_large`, `invoice_too_small`, `unspent_receiver_credit`, `unapplied_from_invoice`, `checkout_session_subscription_payment`, or `checkout_session_subscription_payment_canceled`. See the [Customer Balance page](https://docs.stripe.com/billing/customer/balance#types) to learn more about transaction types.
        Type: CustomerBalanceTransactionType
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "customer_balance_transaction"

        static member New (amount: int, checkoutSession: CustomerBalanceTransactionCheckoutSession'AnyOf option, created: DateTime, creditNote: CustomerBalanceTransactionCreditNote'AnyOf option, currency: IsoTypes.IsoCurrencyCode, customer: CustomerBalanceTransactionCustomer'AnyOf, customerAccount: string option, description: string option, endingBalance: int, id: string, invoice: CustomerBalanceTransactionInvoice'AnyOf option, livemode: bool, metadata: Map<string, string> option, ``type``: CustomerBalanceTransactionType) =
            {
                CustomerBalanceTransaction.Amount = amount //required
                CustomerBalanceTransaction.CheckoutSession = checkoutSession //required
                CustomerBalanceTransaction.Created = created //required
                CustomerBalanceTransaction.CreditNote = creditNote //required
                CustomerBalanceTransaction.Currency = currency //required
                CustomerBalanceTransaction.Customer = customer //required
                CustomerBalanceTransaction.CustomerAccount = customerAccount //required
                CustomerBalanceTransaction.Description = description //required
                CustomerBalanceTransaction.EndingBalance = endingBalance //required
                CustomerBalanceTransaction.Id = id //required
                CustomerBalanceTransaction.Invoice = invoice //required
                CustomerBalanceTransaction.Livemode = livemode //required
                CustomerBalanceTransaction.Metadata = metadata //required
                CustomerBalanceTransaction.Type = ``type`` //required
            }

    and CustomerBalanceTransactionCheckoutSession'AnyOf =
        | String of string
        | CheckoutSession of CheckoutSession

    and CustomerBalanceTransactionCreditNote'AnyOf =
        | String of string
        | CreditNote of CreditNote

    and CustomerBalanceTransactionCustomer'AnyOf =
        | String of string
        | Customer of Customer

    and CustomerBalanceTransactionInvoice'AnyOf =
        | String of string
        | Invoice of Invoice

    ///Customers with certain payments enabled have a cash balance, representing funds that were paid
    ///by the customer to a merchant, but have not yet been allocated to a payment. Cash Balance Transactions
    ///represent when funds are moved into or out of this balance. This includes funding by the customer, allocation
    ///to payments, and refunds to the customer.
    and CustomerCashBalanceTransaction = {
        AdjustedForOverdraft: CustomerBalanceResourceCashBalanceTransactionResourceAdjustedForOverdraft option
        AppliedToPayment: CustomerBalanceResourceCashBalanceTransactionResourceAppliedToPaymentTransaction option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///The customer whose available cash balance changed as a result of this transaction.
        Customer: CustomerCashBalanceTransactionCustomer'AnyOf
        ///The ID of an Account representing a customer whose available cash balance changed as a result of this transaction.
        CustomerAccount: string option
        ///The total available cash balance for the specified currency after this transaction was applied. Represented in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
        EndingBalance: int
        Funded: CustomerBalanceResourceCashBalanceTransactionResourceFundedTransaction option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The amount by which the cash balance changed, represented in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). A positive value represents funds being added to the cash balance, a negative value represents funds being removed from the cash balance.
        NetAmount: int
        RefundedFromPayment: CustomerBalanceResourceCashBalanceTransactionResourceRefundedFromPaymentTransaction option
        TransferredToBalance: CustomerBalanceResourceCashBalanceTransactionResourceTransferredToBalance option
        ///The type of the cash balance transaction. New types may be added in future. See [Customer Balance](https://docs.stripe.com/payments/customer-balance#types) to learn more about these types.
        Type: CustomerCashBalanceTransactionType
        UnappliedFromPayment: CustomerBalanceResourceCashBalanceTransactionResourceUnappliedFromPaymentTransaction option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "customer_cash_balance_transaction"

        static member New (created: DateTime, currency: IsoTypes.IsoCurrencyCode, customer: CustomerCashBalanceTransactionCustomer'AnyOf, customerAccount: string option, endingBalance: int, id: string, livemode: bool, netAmount: int, ``type``: CustomerCashBalanceTransactionType, ?adjustedForOverdraft: CustomerBalanceResourceCashBalanceTransactionResourceAdjustedForOverdraft, ?appliedToPayment: CustomerBalanceResourceCashBalanceTransactionResourceAppliedToPaymentTransaction, ?funded: CustomerBalanceResourceCashBalanceTransactionResourceFundedTransaction, ?refundedFromPayment: CustomerBalanceResourceCashBalanceTransactionResourceRefundedFromPaymentTransaction, ?transferredToBalance: CustomerBalanceResourceCashBalanceTransactionResourceTransferredToBalance, ?unappliedFromPayment: CustomerBalanceResourceCashBalanceTransactionResourceUnappliedFromPaymentTransaction) =
            {
                CustomerCashBalanceTransaction.Created = created //required
                CustomerCashBalanceTransaction.Currency = currency //required
                CustomerCashBalanceTransaction.Customer = customer //required
                CustomerCashBalanceTransaction.CustomerAccount = customerAccount //required
                CustomerCashBalanceTransaction.EndingBalance = endingBalance //required
                CustomerCashBalanceTransaction.Id = id //required
                CustomerCashBalanceTransaction.Livemode = livemode //required
                CustomerCashBalanceTransaction.NetAmount = netAmount //required
                CustomerCashBalanceTransaction.Type = ``type`` //required
                CustomerCashBalanceTransaction.AdjustedForOverdraft = adjustedForOverdraft
                CustomerCashBalanceTransaction.AppliedToPayment = appliedToPayment
                CustomerCashBalanceTransaction.Funded = funded
                CustomerCashBalanceTransaction.RefundedFromPayment = refundedFromPayment
                CustomerCashBalanceTransaction.TransferredToBalance = transferredToBalance
                CustomerCashBalanceTransaction.UnappliedFromPayment = unappliedFromPayment
            }

    and CustomerCashBalanceTransactionCustomer'AnyOf =
        | String of string
        | Customer of Customer

    ///A Customer Session allows you to grant Stripe's frontend SDKs (like Stripe.js) client-side access
    ///control over a Customer.
    ///Related guides: [Customer Session with the Payment Element](/payments/accept-a-payment-deferred?platform=web&type=payment#save-payment-methods),
    ///[Customer Session with the Pricing Table](/payments/checkout/pricing-table#customer-session),
    ///[Customer Session with the Buy Button](/payment-links/buy-button#pass-an-existing-customer).
    and CustomerSession = {
        ///The client secret of this Customer Session. Used on the client to set up secure access to the given `customer`.
        ///The client secret can be used to provide access to `customer` from your frontend. It should not be stored, logged, or exposed to anyone other than the relevant customer. Make sure that you have TLS enabled on any page that includes the client secret.
        ClientSecret: string
        Components: CustomerSessionResourceComponents option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The Customer the Customer Session was created for.
        Customer: CustomerSessionCustomer'AnyOf
        ///The Account that the Customer Session was created for.
        CustomerAccount: string option
        ///The timestamp at which this Customer Session will expire.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "customer_session"

        static member New (clientSecret: string, created: DateTime, customer: CustomerSessionCustomer'AnyOf, customerAccount: string option, expiresAt: DateTime, livemode: bool, ?components: CustomerSessionResourceComponents) =
            {
                CustomerSession.ClientSecret = clientSecret //required
                CustomerSession.Created = created //required
                CustomerSession.Customer = customer //required
                CustomerSession.CustomerAccount = customerAccount //required
                CustomerSession.ExpiresAt = expiresAt //required
                CustomerSession.Livemode = livemode //required
                CustomerSession.Components = components
            }

    and CustomerSessionCustomer'AnyOf =
        | String of string
        | Customer of Customer

    ///Configuration for the components supported by this Customer Session.
    and CustomerSessionResourceComponents = {
        BuyButton: CustomerSessionResourceComponentsResourceBuyButton
        CustomerSheet: CustomerSessionResourceComponentsResourceCustomerSheet
        MobilePaymentElement: CustomerSessionResourceComponentsResourceMobilePaymentElement
        PaymentElement: CustomerSessionResourceComponentsResourcePaymentElement
        PricingTable: CustomerSessionResourceComponentsResourcePricingTable
    }
    with
        static member New (buyButton: CustomerSessionResourceComponentsResourceBuyButton, customerSheet: CustomerSessionResourceComponentsResourceCustomerSheet, mobilePaymentElement: CustomerSessionResourceComponentsResourceMobilePaymentElement, paymentElement: CustomerSessionResourceComponentsResourcePaymentElement, pricingTable: CustomerSessionResourceComponentsResourcePricingTable) =
            {
                CustomerSessionResourceComponents.BuyButton = buyButton //required
                CustomerSessionResourceComponents.CustomerSheet = customerSheet //required
                CustomerSessionResourceComponents.MobilePaymentElement = mobilePaymentElement //required
                CustomerSessionResourceComponents.PaymentElement = paymentElement //required
                CustomerSessionResourceComponents.PricingTable = pricingTable //required
            }

    ///This hash contains whether the customer sheet is enabled and the features it supports.
    and CustomerSessionResourceComponentsResourceCustomerSheet = {
        ///Whether the customer sheet is enabled.
        Enabled: bool
        ///This hash defines whether the customer sheet supports certain features.
        Features: CustomerSessionResourceComponentsResourceCustomerSheetResourceFeatures option
    }
    with
        static member New (enabled: bool, features: CustomerSessionResourceComponentsResourceCustomerSheetResourceFeatures option) =
            {
                CustomerSessionResourceComponentsResourceCustomerSheet.Enabled = enabled //required
                CustomerSessionResourceComponentsResourceCustomerSheet.Features = features //required
            }

    ///This hash contains the features the customer sheet supports.
    and CustomerSessionResourceComponentsResourceCustomerSheetResourceFeatures = {
        ///A list of [`allow_redisplay`](https://docs.stripe.com/api/payment_methods/object#payment_method_object-allow_redisplay) values that controls which saved payment methods the customer sheet displays by filtering to only show payment methods with an `allow_redisplay` value that is present in this list.
        ///If not specified, defaults to ["always"]. In order to display all saved payment methods, specify ["always", "limited", "unspecified"].
        PaymentMethodAllowRedisplayFilters: CustomerSessionResourceComponentsResourceCustomerSheetResourceFeaturesPaymentMethodAllowRedisplayFilters list option
        ///Controls whether the customer sheet displays the option to remove a saved payment method."
        ///Allowing buyers to remove their saved payment methods impacts subscriptions that depend on that payment method. Removing the payment method detaches the [`customer` object](https://docs.stripe.com/api/payment_methods/object#payment_method_object-customer) from that [PaymentMethod](https://docs.stripe.com/api/payment_methods).
        PaymentMethodRemove: CustomerSessionResourceComponentsResourceCustomerSheetResourceFeaturesPaymentMethodRemove option
    }
    with
        static member New (paymentMethodAllowRedisplayFilters: CustomerSessionResourceComponentsResourceCustomerSheetResourceFeaturesPaymentMethodAllowRedisplayFilters list option, paymentMethodRemove: CustomerSessionResourceComponentsResourceCustomerSheetResourceFeaturesPaymentMethodRemove option) =
            {
                CustomerSessionResourceComponentsResourceCustomerSheetResourceFeatures.PaymentMethodAllowRedisplayFilters = paymentMethodAllowRedisplayFilters //required
                CustomerSessionResourceComponentsResourceCustomerSheetResourceFeatures.PaymentMethodRemove = paymentMethodRemove //required
            }

    ///This hash contains whether the mobile payment element is enabled and the features it supports.
    and CustomerSessionResourceComponentsResourceMobilePaymentElement = {
        ///Whether the mobile payment element is enabled.
        Enabled: bool
        ///This hash defines whether the mobile payment element supports certain features.
        Features: CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeatures option
    }
    with
        static member New (enabled: bool, features: CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeatures option) =
            {
                CustomerSessionResourceComponentsResourceMobilePaymentElement.Enabled = enabled //required
                CustomerSessionResourceComponentsResourceMobilePaymentElement.Features = features //required
            }

    ///This hash contains the features the mobile payment element supports.
    and CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeatures = {
        ///A list of [`allow_redisplay`](https://docs.stripe.com/api/payment_methods/object#payment_method_object-allow_redisplay) values that controls which saved payment methods the mobile payment element displays by filtering to only show payment methods with an `allow_redisplay` value that is present in this list.
        ///If not specified, defaults to ["always"]. In order to display all saved payment methods, specify ["always", "limited", "unspecified"].
        PaymentMethodAllowRedisplayFilters: CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeaturesPaymentMethodAllowRedisplayFilters list option
        ///Controls whether or not the mobile payment element shows saved payment methods.
        PaymentMethodRedisplay: CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeaturesPaymentMethodRedisplay option
        ///Controls whether the mobile payment element displays the option to remove a saved payment method."
        ///Allowing buyers to remove their saved payment methods impacts subscriptions that depend on that payment method. Removing the payment method detaches the [`customer` object](https://docs.stripe.com/api/payment_methods/object#payment_method_object-customer) from that [PaymentMethod](https://docs.stripe.com/api/payment_methods).
        PaymentMethodRemove: CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeaturesPaymentMethodRemove option
        ///Controls whether the mobile payment element displays a checkbox offering to save a new payment method.
        ///If a customer checks the box, the [`allow_redisplay`](https://docs.stripe.com/api/payment_methods/object#payment_method_object-allow_redisplay) value on the PaymentMethod is set to `'always'` at confirmation time. For PaymentIntents, the [`setup_future_usage`](https://docs.stripe.com/api/payment_intents/object#payment_intent_object-setup_future_usage) value is also set to the value defined in `payment_method_save_usage`.
        PaymentMethodSave: CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeaturesPaymentMethodSave option
        ///Allows overriding the value of allow_override when saving a new payment method when payment_method_save is set to disabled. Use values: "always", "limited", or "unspecified".
        ///If not specified, defaults to `nil` (no override value).
        PaymentMethodSaveAllowRedisplayOverride: CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeaturesPaymentMethodSaveAllowRedisplayOverride option
    }
    with
        static member New (paymentMethodAllowRedisplayFilters: CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeaturesPaymentMethodAllowRedisplayFilters list option, paymentMethodRedisplay: CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeaturesPaymentMethodRedisplay option, paymentMethodRemove: CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeaturesPaymentMethodRemove option, paymentMethodSave: CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeaturesPaymentMethodSave option, paymentMethodSaveAllowRedisplayOverride: CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeaturesPaymentMethodSaveAllowRedisplayOverride option) =
            {
                CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeatures.PaymentMethodAllowRedisplayFilters = paymentMethodAllowRedisplayFilters //required
                CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeatures.PaymentMethodRedisplay = paymentMethodRedisplay //required
                CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeatures.PaymentMethodRemove = paymentMethodRemove //required
                CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeatures.PaymentMethodSave = paymentMethodSave //required
                CustomerSessionResourceComponentsResourceMobilePaymentElementResourceFeatures.PaymentMethodSaveAllowRedisplayOverride = paymentMethodSaveAllowRedisplayOverride //required
            }

    ///This hash contains whether the Payment Element is enabled and the features it supports.
    and CustomerSessionResourceComponentsResourcePaymentElement = {
        ///Whether the Payment Element is enabled.
        Enabled: bool
        ///This hash defines whether the Payment Element supports certain features.
        Features: CustomerSessionResourceComponentsResourcePaymentElementResourceFeatures option
    }
    with
        static member New (enabled: bool, features: CustomerSessionResourceComponentsResourcePaymentElementResourceFeatures option) =
            {
                CustomerSessionResourceComponentsResourcePaymentElement.Enabled = enabled //required
                CustomerSessionResourceComponentsResourcePaymentElement.Features = features //required
            }

    ///This hash contains the features the Payment Element supports.
    and CustomerSessionResourceComponentsResourcePaymentElementResourceFeatures = {
        ///A list of [`allow_redisplay`](https://docs.stripe.com/api/payment_methods/object#payment_method_object-allow_redisplay) values that controls which saved payment methods the Payment Element displays by filtering to only show payment methods with an `allow_redisplay` value that is present in this list.
        ///If not specified, defaults to ["always"]. In order to display all saved payment methods, specify ["always", "limited", "unspecified"].
        PaymentMethodAllowRedisplayFilters: CustomerSessionResourceComponentsResourcePaymentElementResourceFeaturesPaymentMethodAllowRedisplayFilters list
        ///Controls whether or not the Payment Element shows saved payment methods. This parameter defaults to `disabled`.
        PaymentMethodRedisplay: CustomerSessionResourceComponentsResourcePaymentElementResourceFeaturesPaymentMethodRedisplay
        ///Determines the max number of saved payment methods for the Payment Element to display. This parameter defaults to `3`. The maximum redisplay limit is `10`.
        PaymentMethodRedisplayLimit: int option
        ///Controls whether the Payment Element displays the option to remove a saved payment method. This parameter defaults to `disabled`.
        ///Allowing buyers to remove their saved payment methods impacts subscriptions that depend on that payment method. Removing the payment method detaches the [`customer` object](https://docs.stripe.com/api/payment_methods/object#payment_method_object-customer) from that [PaymentMethod](https://docs.stripe.com/api/payment_methods).
        PaymentMethodRemove: CustomerSessionResourceComponentsResourcePaymentElementResourceFeaturesPaymentMethodRemove
        ///Controls whether the Payment Element displays a checkbox offering to save a new payment method. This parameter defaults to `disabled`.
        ///If a customer checks the box, the [`allow_redisplay`](https://docs.stripe.com/api/payment_methods/object#payment_method_object-allow_redisplay) value on the PaymentMethod is set to `'always'` at confirmation time. For PaymentIntents, the [`setup_future_usage`](https://docs.stripe.com/api/payment_intents/object#payment_intent_object-setup_future_usage) value is also set to the value defined in `payment_method_save_usage`.
        PaymentMethodSave: CustomerSessionResourceComponentsResourcePaymentElementResourceFeaturesPaymentMethodSave
        ///When using PaymentIntents and the customer checks the save checkbox, this field determines the [`setup_future_usage`](https://docs.stripe.com/api/payment_intents/object#payment_intent_object-setup_future_usage) value used to confirm the PaymentIntent.
        ///When using SetupIntents, directly configure the [`usage`](https://docs.stripe.com/api/setup_intents/object#setup_intent_object-usage) value on SetupIntent creation.
        PaymentMethodSaveUsage: CustomerSessionResourceComponentsResourcePaymentElementResourceFeaturesPaymentMethodSaveUsage option
    }
    with
        static member New (paymentMethodAllowRedisplayFilters: CustomerSessionResourceComponentsResourcePaymentElementResourceFeaturesPaymentMethodAllowRedisplayFilters list, paymentMethodRedisplay: CustomerSessionResourceComponentsResourcePaymentElementResourceFeaturesPaymentMethodRedisplay, paymentMethodRedisplayLimit: int option, paymentMethodRemove: CustomerSessionResourceComponentsResourcePaymentElementResourceFeaturesPaymentMethodRemove, paymentMethodSave: CustomerSessionResourceComponentsResourcePaymentElementResourceFeaturesPaymentMethodSave, paymentMethodSaveUsage: CustomerSessionResourceComponentsResourcePaymentElementResourceFeaturesPaymentMethodSaveUsage option) =
            {
                CustomerSessionResourceComponentsResourcePaymentElementResourceFeatures.PaymentMethodAllowRedisplayFilters = paymentMethodAllowRedisplayFilters //required
                CustomerSessionResourceComponentsResourcePaymentElementResourceFeatures.PaymentMethodRedisplay = paymentMethodRedisplay //required
                CustomerSessionResourceComponentsResourcePaymentElementResourceFeatures.PaymentMethodRedisplayLimit = paymentMethodRedisplayLimit //required
                CustomerSessionResourceComponentsResourcePaymentElementResourceFeatures.PaymentMethodRemove = paymentMethodRemove //required
                CustomerSessionResourceComponentsResourcePaymentElementResourceFeatures.PaymentMethodSave = paymentMethodSave //required
                CustomerSessionResourceComponentsResourcePaymentElementResourceFeatures.PaymentMethodSaveUsage = paymentMethodSaveUsage //required
            }

    and CustomerTax = {
        ///Surfaces if automatic tax computation is possible given the current customer location information.
        AutomaticTax: CustomerTaxAutomaticTax
        ///A recent IP address of the customer used for tax reporting and tax location inference.
        IpAddress: string option
        ///The identified tax location of the customer.
        Location: CustomerTaxLocation option
        ///The tax calculation provider used for location resolution. Defaults to `stripe` when not using a [third-party provider](/tax/third-party-apps).
        Provider: CustomerTaxProvider
    }
    with
        static member New (automaticTax: CustomerTaxAutomaticTax, ipAddress: string option, location: CustomerTaxLocation option, provider: CustomerTaxProvider) =
            {
                CustomerTax.AutomaticTax = automaticTax //required
                CustomerTax.IpAddress = ipAddress //required
                CustomerTax.Location = location //required
                CustomerTax.Provider = provider //required
            }

    and CustomerTaxLocation = {
        ///The identified tax country of the customer.
        Country: IsoTypes.IsoCountryCode
        ///The data source used to infer the customer's location.
        Source: CustomerTaxLocationSource
        ///The identified tax state, county, province, or region of the customer.
        State: string option
    }
    with
        static member New (country: IsoTypes.IsoCountryCode, source: CustomerTaxLocationSource, state: string option) =
            {
                CustomerTaxLocation.Country = country //required
                CustomerTaxLocation.Source = source //required
                CustomerTaxLocation.State = state //required
            }

    and DeletedBankAccount = {
        ///Three-letter [ISO code for the currency](https://stripe.com/docs/payouts) paid out to the bank account.
        Currency: IsoTypes.IsoCurrencyCode option
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "bank_account"

        static member New (deleted: bool, id: string, ?currency: IsoTypes.IsoCurrencyCode option) =
            {
                DeletedBankAccount.Deleted = deleted //required
                DeletedBankAccount.Id = id //required
                DeletedBankAccount.Currency = currency |> Option.flatten
            }

    and DeletedCard = {
        ///Three-letter [ISO code for the currency](https://stripe.com/docs/payouts) paid out to the bank account.
        Currency: IsoTypes.IsoCurrencyCode option
        ///Always true for a deleted object
        Deleted: bool
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "card"

        static member New (deleted: bool, id: string, ?currency: IsoTypes.IsoCurrencyCode option) =
            {
                DeletedCard.Deleted = deleted //required
                DeletedCard.Id = id //required
                DeletedCard.Currency = currency |> Option.flatten
            }

    and DeletedDiscount = {
        ///The Checkout session that this coupon is applied to, if it is applied to a particular session in payment mode. Will not be present for subscription mode.
        CheckoutSession: string option
        ///The ID of the customer associated with this discount.
        Customer: DeletedDiscountCustomer'AnyOf option
        ///The ID of the account representing the customer associated with this discount.
        CustomerAccount: string option
        ///Always true for a deleted object
        Deleted: bool
        ///The ID of the discount object. Discounts cannot be fetched by ID. Use `expand[]=discounts` in API calls to expand discount IDs in an array.
        Id: string
        ///The invoice that the discount's coupon was applied to, if it was applied directly to a particular invoice.
        Invoice: string option
        ///The invoice item `id` (or invoice line item `id` for invoice line items of type='subscription') that the discount's coupon was applied to, if it was applied directly to a particular invoice item or invoice line item.
        InvoiceItem: string option
        ///The promotion code applied to create this discount.
        PromotionCode: DeletedDiscountPromotionCode'AnyOf option
        Source: DiscountSource
        ///Date that the coupon was applied.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Start: DateTime
        ///The subscription that this coupon is applied to, if it is applied to a particular subscription.
        Subscription: string option
        ///The subscription item that this coupon is applied to, if it is applied to a particular subscription item.
        SubscriptionItem: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "discount"

        static member New (checkoutSession: string option, customer: DeletedDiscountCustomer'AnyOf option, customerAccount: string option, deleted: bool, id: string, invoice: string option, invoiceItem: string option, promotionCode: DeletedDiscountPromotionCode'AnyOf option, source: DiscountSource, start: DateTime, subscription: string option, subscriptionItem: string option) =
            {
                DeletedDiscount.CheckoutSession = checkoutSession //required
                DeletedDiscount.Customer = customer //required
                DeletedDiscount.CustomerAccount = customerAccount //required
                DeletedDiscount.Deleted = deleted //required
                DeletedDiscount.Id = id //required
                DeletedDiscount.Invoice = invoice //required
                DeletedDiscount.InvoiceItem = invoiceItem //required
                DeletedDiscount.PromotionCode = promotionCode //required
                DeletedDiscount.Source = source //required
                DeletedDiscount.Start = start //required
                DeletedDiscount.Subscription = subscription //required
                DeletedDiscount.SubscriptionItem = subscriptionItem //required
            }

    and DeletedDiscountCustomer'AnyOf =
        | String of string
        | Customer of Customer
        | DeletedCustomer of DeletedCustomer

    and DeletedDiscountPromotionCode'AnyOf =
        | String of string
        | PromotionCode of PromotionCode

    and DeletedExternalAccount =
        | DeletedBankAccount of DeletedBankAccount
        | DeletedCard of DeletedCard

    and DeletedPaymentSource =
        | DeletedBankAccount of DeletedBankAccount
        | DeletedCard of DeletedCard

    and DeletedTerminalReader = {
        ///Always true for a deleted object
        Deleted: bool
        ///Device type of the reader.
        DeviceType: DeletedTerminalReaderDeviceType
        ///Unique identifier for the object.
        Id: string
        ///Serial number of the reader.
        SerialNumber: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "terminal.reader"

        static member New (deleted: bool, deviceType: DeletedTerminalReaderDeviceType, id: string, serialNumber: string) =
            {
                DeletedTerminalReader.Deleted = deleted //required
                DeletedTerminalReader.DeviceType = deviceType //required
                DeletedTerminalReader.Id = id //required
                DeletedTerminalReader.SerialNumber = serialNumber //required
            }

    ///A discount represents the actual application of a [coupon](https://api.stripe.com#coupons) or [promotion code](https://api.stripe.com#promotion_codes).
    ///It contains information about when the discount began, when it will end, and what it is applied to.
    ///Related guide: [Applying discounts to subscriptions](https://docs.stripe.com/billing/subscriptions/discounts)
    and Discount = {
        ///The Checkout session that this coupon is applied to, if it is applied to a particular session in payment mode. Will not be present for subscription mode.
        CheckoutSession: string option
        ///The ID of the customer associated with this discount.
        Customer: DiscountCustomer'AnyOf option
        ///The ID of the account representing the customer associated with this discount.
        CustomerAccount: string option
        ///If the coupon has a duration of `repeating`, the date that this discount will end. If the coupon has a duration of `once` or `forever`, this attribute will be null.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]End: DateTime option
        ///The ID of the discount object. Discounts cannot be fetched by ID. Use `expand[]=discounts` in API calls to expand discount IDs in an array.
        Id: string
        ///The invoice that the discount's coupon was applied to, if it was applied directly to a particular invoice.
        Invoice: string option
        ///The invoice item `id` (or invoice line item `id` for invoice line items of type='subscription') that the discount's coupon was applied to, if it was applied directly to a particular invoice item or invoice line item.
        InvoiceItem: string option
        ///The promotion code applied to create this discount.
        PromotionCode: DiscountPromotionCode'AnyOf option
        Source: DiscountSource
        ///Date that the coupon was applied.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Start: DateTime
        ///The subscription that this coupon is applied to, if it is applied to a particular subscription.
        Subscription: string option
        ///The subscription item that this coupon is applied to, if it is applied to a particular subscription item.
        SubscriptionItem: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "discount"

        static member New (checkoutSession: string option, customer: DiscountCustomer'AnyOf option, customerAccount: string option, ``end``: DateTime option, id: string, invoice: string option, invoiceItem: string option, promotionCode: DiscountPromotionCode'AnyOf option, source: DiscountSource, start: DateTime, subscription: string option, subscriptionItem: string option) =
            {
                Discount.CheckoutSession = checkoutSession //required
                Discount.Customer = customer //required
                Discount.CustomerAccount = customerAccount //required
                Discount.End = ``end`` //required
                Discount.Id = id //required
                Discount.Invoice = invoice //required
                Discount.InvoiceItem = invoiceItem //required
                Discount.PromotionCode = promotionCode //required
                Discount.Source = source //required
                Discount.Start = start //required
                Discount.Subscription = subscription //required
                Discount.SubscriptionItem = subscriptionItem //required
            }

    and DiscountCustomer'AnyOf =
        | String of string
        | Customer of Customer
        | DeletedCustomer of DeletedCustomer

    and DiscountPromotionCode'AnyOf =
        | String of string
        | PromotionCode of PromotionCode

    and DiscountSource = {
        ///The coupon that was redeemed to create this discount.
        Coupon: DiscountSourceCoupon'AnyOf option
    }
    with
        ///The source type of the discount.
        member _.Type = "coupon"

        static member New (coupon: DiscountSourceCoupon'AnyOf option) =
            {
                DiscountSource.Coupon = coupon //required
            }

    and DiscountSourceCoupon'AnyOf =
        | String of string
        | Coupon of Coupon

    and DiscountsResourceDiscountAmount = {
        ///The amount, in cents (or local equivalent), of the discount.
        Amount: int
        ///The discount that was applied to get this discount amount.
        Discount: DiscountsResourceDiscountAmountDiscount'AnyOf
    }
    with
        static member New (amount: int, discount: DiscountsResourceDiscountAmountDiscount'AnyOf) =
            {
                DiscountsResourceDiscountAmount.Amount = amount //required
                DiscountsResourceDiscountAmount.Discount = discount //required
            }

    and DiscountsResourceDiscountAmountDiscount'AnyOf =
        | String of string
        | Discount of Discount
        | DeletedDiscount of DeletedDiscount

    and DiscountsResourceStackableDiscountWithDiscountEnd = {
        ///ID of the coupon to create a new discount for.
        Coupon: DiscountsResourceStackableDiscountWithDiscountEndCoupon'AnyOf option
        ///ID of an existing discount on the object (or one of its ancestors) to reuse.
        Discount: DiscountsResourceStackableDiscountWithDiscountEndDiscount'AnyOf option
        ///ID of the promotion code to create a new discount for.
        PromotionCode: DiscountsResourceStackableDiscountWithDiscountEndPromotionCode'AnyOf option
    }
    with
        static member New (coupon: DiscountsResourceStackableDiscountWithDiscountEndCoupon'AnyOf option, discount: DiscountsResourceStackableDiscountWithDiscountEndDiscount'AnyOf option, promotionCode: DiscountsResourceStackableDiscountWithDiscountEndPromotionCode'AnyOf option) =
            {
                DiscountsResourceStackableDiscountWithDiscountEnd.Coupon = coupon //required
                DiscountsResourceStackableDiscountWithDiscountEnd.Discount = discount //required
                DiscountsResourceStackableDiscountWithDiscountEnd.PromotionCode = promotionCode //required
            }

    and DiscountsResourceStackableDiscountWithDiscountEndCoupon'AnyOf =
        | String of string
        | Coupon of Coupon

    and DiscountsResourceStackableDiscountWithDiscountEndDiscount'AnyOf =
        | String of string
        | Discount of Discount

    and DiscountsResourceStackableDiscountWithDiscountEndPromotionCode'AnyOf =
        | String of string
        | PromotionCode of PromotionCode

    ///A dispute occurs when a customer questions your charge with their card issuer.
    ///When this happens, you have the opportunity to respond to the dispute with
    ///evidence that shows that the charge is legitimate.
    ///Related guide: [Disputes and fraud](https://docs.stripe.com/disputes)
    and Dispute = {
        ///Disputed amount. Usually the amount of the charge, but it can differ (usually because of currency fluctuation or because only part of the order is disputed).
        Amount: int
        ///List of zero, one, or two balance transactions that show funds withdrawn and reinstated to your Stripe account as a result of this dispute.
        BalanceTransactions: BalanceTransaction list
        ///ID of the charge that's disputed.
        Charge: DisputeCharge'AnyOf
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///List of eligibility types that are included in `enhanced_evidence`.
        EnhancedEligibilityTypes: DisputeEnhancedEligibilityTypes list
        Evidence: DisputeEvidence
        EvidenceDetails: DisputeEvidenceDetails
        ///Unique identifier for the object.
        Id: string
        ///If true, it's still possible to refund the disputed payment. After the payment has been fully refunded, no further funds are withdrawn from your Stripe account as a result of this dispute.
        IsChargeRefundable: bool
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///Network-dependent reason code for the dispute.
        NetworkReasonCode: string option
        ///ID of the PaymentIntent that's disputed.
        PaymentIntent: DisputePaymentIntent'AnyOf option
        PaymentMethodDetails: DisputePaymentMethodDetails option
        ///Reason given by cardholder for dispute. Possible values are `bank_cannot_process`, `check_returned`, `credit_not_processed`, `customer_initiated`, `debit_not_authorized`, `duplicate`, `fraudulent`, `general`, `incorrect_account_details`, `insufficient_funds`, `noncompliant`, `product_not_received`, `product_unacceptable`, `subscription_canceled`, or `unrecognized`. Learn more about [dispute reasons](https://docs.stripe.com/disputes/categories).
        Reason: DisputeReason
        ///The current status of a dispute. Possible values include:`warning_needs_response`, `warning_under_review`, `warning_closed`, `needs_response`, `under_review`, `won`, `lost`, or `prevented`.
        Status: DisputeStatus
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "dispute"

        static member New (amount: int, balanceTransactions: BalanceTransaction list, charge: DisputeCharge'AnyOf, created: DateTime, currency: IsoTypes.IsoCurrencyCode, enhancedEligibilityTypes: DisputeEnhancedEligibilityTypes list, evidence: DisputeEvidence, evidenceDetails: DisputeEvidenceDetails, id: string, isChargeRefundable: bool, livemode: bool, metadata: Map<string, string>, paymentIntent: DisputePaymentIntent'AnyOf option, reason: DisputeReason, status: DisputeStatus, ?networkReasonCode: string option, ?paymentMethodDetails: DisputePaymentMethodDetails) =
            {
                Dispute.Amount = amount //required
                Dispute.BalanceTransactions = balanceTransactions //required
                Dispute.Charge = charge //required
                Dispute.Created = created //required
                Dispute.Currency = currency //required
                Dispute.EnhancedEligibilityTypes = enhancedEligibilityTypes //required
                Dispute.Evidence = evidence //required
                Dispute.EvidenceDetails = evidenceDetails //required
                Dispute.Id = id //required
                Dispute.IsChargeRefundable = isChargeRefundable //required
                Dispute.Livemode = livemode //required
                Dispute.Metadata = metadata //required
                Dispute.PaymentIntent = paymentIntent //required
                Dispute.Reason = reason //required
                Dispute.Status = status //required
                Dispute.NetworkReasonCode = networkReasonCode |> Option.flatten
                Dispute.PaymentMethodDetails = paymentMethodDetails
            }

    and DisputeCharge'AnyOf =
        | String of string
        | Charge of Charge

    and DisputePaymentIntent'AnyOf =
        | String of string
        | PaymentIntent of PaymentIntent

    and DisputeEnhancedEligibility = {
        VisaCompellingEvidence3: DisputeEnhancedEligibilityVisaCompellingEvidence3 option
        VisaCompliance: DisputeEnhancedEligibilityVisaCompliance option
    }
    with
        static member New (?visaCompellingEvidence3: DisputeEnhancedEligibilityVisaCompellingEvidence3, ?visaCompliance: DisputeEnhancedEligibilityVisaCompliance) =
            {
                DisputeEnhancedEligibility.VisaCompellingEvidence3 = visaCompellingEvidence3
                DisputeEnhancedEligibility.VisaCompliance = visaCompliance
            }

    and DisputeEnhancedEligibilityVisaCompellingEvidence3 = {
        ///List of actions required to qualify dispute for Visa Compelling Evidence 3.0 evidence submission.
        RequiredActions: DisputeEnhancedEligibilityVisaCompellingEvidence3RequiredActions list
        ///Visa Compelling Evidence 3.0 eligibility status.
        Status: DisputeEnhancedEligibilityVisaCompellingEvidence3Status
    }
    with
        static member New (requiredActions: DisputeEnhancedEligibilityVisaCompellingEvidence3RequiredActions list, status: DisputeEnhancedEligibilityVisaCompellingEvidence3Status) =
            {
                DisputeEnhancedEligibilityVisaCompellingEvidence3.RequiredActions = requiredActions //required
                DisputeEnhancedEligibilityVisaCompellingEvidence3.Status = status //required
            }

    and DisputeEnhancedEligibilityVisaCompliance = {
        ///Visa compliance eligibility status.
        Status: DisputeEnhancedEligibilityVisaComplianceStatus
    }
    with
        static member New (status: DisputeEnhancedEligibilityVisaComplianceStatus) =
            {
                DisputeEnhancedEligibilityVisaCompliance.Status = status //required
            }

    and DisputeEnhancedEvidence = {
        VisaCompellingEvidence3: DisputeEnhancedEvidenceVisaCompellingEvidence3 option
        VisaCompliance: DisputeEnhancedEvidenceVisaCompliance option
    }
    with
        static member New (?visaCompellingEvidence3: DisputeEnhancedEvidenceVisaCompellingEvidence3, ?visaCompliance: DisputeEnhancedEvidenceVisaCompliance) =
            {
                DisputeEnhancedEvidence.VisaCompellingEvidence3 = visaCompellingEvidence3
                DisputeEnhancedEvidence.VisaCompliance = visaCompliance
            }

    and DisputeEnhancedEvidenceVisaCompellingEvidence3 = {
        ///Disputed transaction details for Visa Compelling Evidence 3.0 evidence submission.
        DisputedTransaction: DisputeVisaCompellingEvidence3DisputedTransaction option
        ///List of exactly two prior undisputed transaction objects for Visa Compelling Evidence 3.0 evidence submission.
        PriorUndisputedTransactions: DisputeVisaCompellingEvidence3PriorUndisputedTransaction list
    }
    with
        static member New (disputedTransaction: DisputeVisaCompellingEvidence3DisputedTransaction option, priorUndisputedTransactions: DisputeVisaCompellingEvidence3PriorUndisputedTransaction list) =
            {
                DisputeEnhancedEvidenceVisaCompellingEvidence3.DisputedTransaction = disputedTransaction //required
                DisputeEnhancedEvidenceVisaCompellingEvidence3.PriorUndisputedTransactions = priorUndisputedTransactions //required
            }

    and DisputeEvidence = {
        ///Any server or activity logs showing proof that the customer accessed or downloaded the purchased digital product. This information should include IP addresses, corresponding timestamps, and any detailed recorded activity.
        AccessActivityLog: string option
        ///The billing address provided by the customer.
        BillingAddress: string option
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Your subscription cancellation policy, as shown to the customer.
        CancellationPolicy: DisputeEvidenceCancellationPolicy'AnyOf option
        ///An explanation of how and when the customer was shown your refund policy prior to purchase.
        CancellationPolicyDisclosure: string option
        ///A justification for why the customer's subscription was not canceled.
        CancellationRebuttal: string option
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Any communication with the customer that you feel is relevant to your case. Examples include emails proving that the customer received the product or service, or demonstrating their use of or satisfaction with the product or service.
        CustomerCommunication: DisputeEvidenceCustomerCommunication'AnyOf option
        ///The email address of the customer.
        CustomerEmailAddress: string option
        ///The name of the customer.
        CustomerName: string option
        ///The IP address that the customer used when making the purchase.
        CustomerPurchaseIp: string option
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) A relevant document or contract showing the customer's signature.
        CustomerSignature: DisputeEvidenceCustomerSignature'AnyOf option
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Documentation for the prior charge that can uniquely identify the charge, such as a receipt, shipping label, work order, etc. This document should be paired with a similar document from the disputed payment that proves the two payments are separate.
        DuplicateChargeDocumentation: DisputeEvidenceDuplicateChargeDocumentation'AnyOf option
        ///An explanation of the difference between the disputed charge versus the prior charge that appears to be a duplicate.
        DuplicateChargeExplanation: string option
        ///The Stripe ID for the prior charge which appears to be a duplicate of the disputed charge.
        DuplicateChargeId: string option
        EnhancedEvidence: DisputeEnhancedEvidence
        ///A description of the product or service that was sold.
        ProductDescription: string option
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Any receipt or message sent to the customer notifying them of the charge.
        Receipt: DisputeEvidenceReceipt'AnyOf option
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Your refund policy, as shown to the customer.
        RefundPolicy: DisputeEvidenceRefundPolicy'AnyOf option
        ///Documentation demonstrating that the customer was shown your refund policy prior to purchase.
        RefundPolicyDisclosure: string option
        ///A justification for why the customer is not entitled to a refund.
        RefundRefusalExplanation: string option
        ///The date on which the customer received or began receiving the purchased service, in a clear human-readable format.
        ServiceDate: string option
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Documentation showing proof that a service was provided to the customer. This could include a copy of a signed contract, work order, or other form of written agreement.
        ServiceDocumentation: DisputeEvidenceServiceDocumentation'AnyOf option
        ///The address to which a physical product was shipped. You should try to include as complete address information as possible.
        ShippingAddress: string option
        ///The delivery service that shipped a physical product, such as Fedex, UPS, USPS, etc. If multiple carriers were used for this purchase, please separate them with commas.
        ShippingCarrier: string option
        ///The date on which a physical product began its route to the shipping address, in a clear human-readable format.
        ShippingDate: string option
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Documentation showing proof that a product was shipped to the customer at the same address the customer provided to you. This could include a copy of the shipment receipt, shipping label, etc. It should show the customer's full shipping address, if possible.
        ShippingDocumentation: DisputeEvidenceShippingDocumentation'AnyOf option
        ///The tracking number for a physical product, obtained from the delivery service. If multiple tracking numbers were generated for this purchase, please separate them with commas.
        ShippingTrackingNumber: string option
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Any additional evidence or statements.
        UncategorizedFile: DisputeEvidenceUncategorizedFile'AnyOf option
        ///Any additional evidence or statements.
        UncategorizedText: string option
    }
    with
        static member New (accessActivityLog: string option, billingAddress: string option, cancellationPolicy: DisputeEvidenceCancellationPolicy'AnyOf option, cancellationPolicyDisclosure: string option, cancellationRebuttal: string option, customerCommunication: DisputeEvidenceCustomerCommunication'AnyOf option, customerEmailAddress: string option, customerName: string option, customerPurchaseIp: string option, customerSignature: DisputeEvidenceCustomerSignature'AnyOf option, duplicateChargeDocumentation: DisputeEvidenceDuplicateChargeDocumentation'AnyOf option, duplicateChargeExplanation: string option, duplicateChargeId: string option, enhancedEvidence: DisputeEnhancedEvidence, productDescription: string option, receipt: DisputeEvidenceReceipt'AnyOf option, refundPolicy: DisputeEvidenceRefundPolicy'AnyOf option, refundPolicyDisclosure: string option, refundRefusalExplanation: string option, serviceDate: string option, serviceDocumentation: DisputeEvidenceServiceDocumentation'AnyOf option, shippingAddress: string option, shippingCarrier: string option, shippingDate: string option, shippingDocumentation: DisputeEvidenceShippingDocumentation'AnyOf option, shippingTrackingNumber: string option, uncategorizedFile: DisputeEvidenceUncategorizedFile'AnyOf option, uncategorizedText: string option) =
            {
                DisputeEvidence.AccessActivityLog = accessActivityLog //required
                DisputeEvidence.BillingAddress = billingAddress //required
                DisputeEvidence.CancellationPolicy = cancellationPolicy //required
                DisputeEvidence.CancellationPolicyDisclosure = cancellationPolicyDisclosure //required
                DisputeEvidence.CancellationRebuttal = cancellationRebuttal //required
                DisputeEvidence.CustomerCommunication = customerCommunication //required
                DisputeEvidence.CustomerEmailAddress = customerEmailAddress //required
                DisputeEvidence.CustomerName = customerName //required
                DisputeEvidence.CustomerPurchaseIp = customerPurchaseIp //required
                DisputeEvidence.CustomerSignature = customerSignature //required
                DisputeEvidence.DuplicateChargeDocumentation = duplicateChargeDocumentation //required
                DisputeEvidence.DuplicateChargeExplanation = duplicateChargeExplanation //required
                DisputeEvidence.DuplicateChargeId = duplicateChargeId //required
                DisputeEvidence.EnhancedEvidence = enhancedEvidence //required
                DisputeEvidence.ProductDescription = productDescription //required
                DisputeEvidence.Receipt = receipt //required
                DisputeEvidence.RefundPolicy = refundPolicy //required
                DisputeEvidence.RefundPolicyDisclosure = refundPolicyDisclosure //required
                DisputeEvidence.RefundRefusalExplanation = refundRefusalExplanation //required
                DisputeEvidence.ServiceDate = serviceDate //required
                DisputeEvidence.ServiceDocumentation = serviceDocumentation //required
                DisputeEvidence.ShippingAddress = shippingAddress //required
                DisputeEvidence.ShippingCarrier = shippingCarrier //required
                DisputeEvidence.ShippingDate = shippingDate //required
                DisputeEvidence.ShippingDocumentation = shippingDocumentation //required
                DisputeEvidence.ShippingTrackingNumber = shippingTrackingNumber //required
                DisputeEvidence.UncategorizedFile = uncategorizedFile //required
                DisputeEvidence.UncategorizedText = uncategorizedText //required
            }

    and DisputeEvidenceCancellationPolicy'AnyOf =
        | String of string
        | File of File

    and DisputeEvidenceCustomerCommunication'AnyOf =
        | String of string
        | File of File

    and DisputeEvidenceCustomerSignature'AnyOf =
        | String of string
        | File of File

    and DisputeEvidenceDuplicateChargeDocumentation'AnyOf =
        | String of string
        | File of File

    and DisputeEvidenceReceipt'AnyOf =
        | String of string
        | File of File

    and DisputeEvidenceRefundPolicy'AnyOf =
        | String of string
        | File of File

    and DisputeEvidenceServiceDocumentation'AnyOf =
        | String of string
        | File of File

    and DisputeEvidenceShippingDocumentation'AnyOf =
        | String of string
        | File of File

    and DisputeEvidenceUncategorizedFile'AnyOf =
        | String of string
        | File of File

    and DisputeEvidenceDetails = {
        ///Date by which evidence must be submitted in order to successfully challenge dispute. Will be 0 if the customer's bank or credit card company doesn't allow a response for this particular dispute.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]DueBy: DateTime option
        EnhancedEligibility: DisputeEnhancedEligibility
        ///Whether evidence has been staged for this dispute.
        HasEvidence: bool
        ///Whether the last evidence submission was submitted past the due date. Defaults to `false` if no evidence submissions have occurred. If `true`, then delivery of the latest evidence is *not* guaranteed.
        PastDue: bool
        ///The number of times evidence has been submitted. Typically, you may only submit evidence once.
        SubmissionCount: int
    }
    with
        static member New (dueBy: DateTime option, enhancedEligibility: DisputeEnhancedEligibility, hasEvidence: bool, pastDue: bool, submissionCount: int) =
            {
                DisputeEvidenceDetails.DueBy = dueBy //required
                DisputeEvidenceDetails.EnhancedEligibility = enhancedEligibility //required
                DisputeEvidenceDetails.HasEvidence = hasEvidence //required
                DisputeEvidenceDetails.PastDue = pastDue //required
                DisputeEvidenceDetails.SubmissionCount = submissionCount //required
            }

    and DisputePaymentMethodDetails = {
        AmazonPay: DisputePaymentMethodDetailsAmazonPay option
        Card: DisputePaymentMethodDetailsCard option
        Klarna: DisputePaymentMethodDetailsKlarna option
        Paypal: DisputePaymentMethodDetailsPaypal option
        ///Payment method type.
        Type: DisputePaymentMethodDetailsType
    }
    with
        static member New (``type``: DisputePaymentMethodDetailsType, ?amazonPay: DisputePaymentMethodDetailsAmazonPay, ?card: DisputePaymentMethodDetailsCard, ?klarna: DisputePaymentMethodDetailsKlarna, ?paypal: DisputePaymentMethodDetailsPaypal) =
            {
                DisputePaymentMethodDetails.Type = ``type`` //required
                DisputePaymentMethodDetails.AmazonPay = amazonPay
                DisputePaymentMethodDetails.Card = card
                DisputePaymentMethodDetails.Klarna = klarna
                DisputePaymentMethodDetails.Paypal = paypal
            }

    and DisputePaymentMethodDetailsAmazonPay = {
        ///The AmazonPay dispute type, chargeback or claim
        DisputeType: DisputePaymentMethodDetailsAmazonPayDisputeType option
    }
    with
        static member New (disputeType: DisputePaymentMethodDetailsAmazonPayDisputeType option) =
            {
                DisputePaymentMethodDetailsAmazonPay.DisputeType = disputeType //required
            }

    and DisputePaymentMethodDetailsCard = {
        ///Card brand. Can be `amex`, `cartes_bancaires`, `diners`, `discover`, `eftpos_au`, `jcb`, `link`, `mastercard`, `unionpay`, `visa` or `unknown`.
        Brand: DisputePaymentMethodDetailsCardBrand
        ///The type of dispute opened. Different case types may have varying fees and financial impact.
        CaseType: DisputePaymentMethodDetailsCardCaseType
        ///The card network's specific dispute reason code, which maps to one of Stripe's primary dispute categories to simplify response guidance. The [Network code map](https://stripe.com/docs/disputes/categories#network-code-map) lists all available dispute reason codes by network.
        NetworkReasonCode: string option
    }
    with
        static member New (brand: DisputePaymentMethodDetailsCardBrand, caseType: DisputePaymentMethodDetailsCardCaseType, networkReasonCode: string option) =
            {
                DisputePaymentMethodDetailsCard.Brand = brand //required
                DisputePaymentMethodDetailsCard.CaseType = caseType //required
                DisputePaymentMethodDetailsCard.NetworkReasonCode = networkReasonCode //required
            }

    and DisputeTransactionShippingAddress = {
        ///City, district, suburb, town, or village.
        City: string option
        ///Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
        Country: IsoTypes.IsoCountryCode option
        ///Address line 1, such as the street, PO Box, or company name.
        [<JsonField(Name="line1")>]Line1: string option
        ///Address line 2, such as the apartment, suite, unit, or building.
        [<JsonField(Name="line2")>]Line2: string option
        ///ZIP or postal code.
        PostalCode: string option
        ///State, county, province, or region ([ISO 3166-2](https://en.wikipedia.org/wiki/ISO_3166-2)).
        State: string option
    }
    with
        static member New (city: string option, country: IsoTypes.IsoCountryCode option, line1: string option, line2: string option, postalCode: string option, state: string option) =
            {
                DisputeTransactionShippingAddress.City = city //required
                DisputeTransactionShippingAddress.Country = country //required
                DisputeTransactionShippingAddress.Line1 = line1 //required
                DisputeTransactionShippingAddress.Line2 = line2 //required
                DisputeTransactionShippingAddress.PostalCode = postalCode //required
                DisputeTransactionShippingAddress.State = state //required
            }

    and DisputeVisaCompellingEvidence3DisputedTransaction = {
        ///User Account ID used to log into business platform. Must be recognizable by the user.
        CustomerAccountId: string option
        ///Unique identifier of the cardholder’s device derived from a combination of at least two hardware and software attributes. Must be at least 20 characters.
        CustomerDeviceFingerprint: string option
        ///Unique identifier of the cardholder’s device such as a device serial number (e.g., International Mobile Equipment Identity [IMEI]). Must be at least 15 characters.
        CustomerDeviceId: string option
        ///The email address of the customer.
        CustomerEmailAddress: string option
        ///The IP address that the customer used when making the purchase.
        CustomerPurchaseIp: string option
        ///Categorization of disputed payment.
        MerchandiseOrServices: DisputeVisaCompellingEvidence3DisputedTransactionMerchandiseOrServices option
        ///A description of the product or service that was sold.
        ProductDescription: string option
        ///The address to which a physical product was shipped. All fields are required for Visa Compelling Evidence 3.0 evidence submission.
        ShippingAddress: DisputeTransactionShippingAddress option
    }
    with
        static member New (customerAccountId: string option, customerDeviceFingerprint: string option, customerDeviceId: string option, customerEmailAddress: string option, customerPurchaseIp: string option, merchandiseOrServices: DisputeVisaCompellingEvidence3DisputedTransactionMerchandiseOrServices option, productDescription: string option, shippingAddress: DisputeTransactionShippingAddress option) =
            {
                DisputeVisaCompellingEvidence3DisputedTransaction.CustomerAccountId = customerAccountId //required
                DisputeVisaCompellingEvidence3DisputedTransaction.CustomerDeviceFingerprint = customerDeviceFingerprint //required
                DisputeVisaCompellingEvidence3DisputedTransaction.CustomerDeviceId = customerDeviceId //required
                DisputeVisaCompellingEvidence3DisputedTransaction.CustomerEmailAddress = customerEmailAddress //required
                DisputeVisaCompellingEvidence3DisputedTransaction.CustomerPurchaseIp = customerPurchaseIp //required
                DisputeVisaCompellingEvidence3DisputedTransaction.MerchandiseOrServices = merchandiseOrServices //required
                DisputeVisaCompellingEvidence3DisputedTransaction.ProductDescription = productDescription //required
                DisputeVisaCompellingEvidence3DisputedTransaction.ShippingAddress = shippingAddress //required
            }

    and DisputeVisaCompellingEvidence3PriorUndisputedTransaction = {
        ///Stripe charge ID for the Visa Compelling Evidence 3.0 eligible prior charge.
        Charge: string
        ///User Account ID used to log into business platform. Must be recognizable by the user.
        CustomerAccountId: string option
        ///Unique identifier of the cardholder’s device derived from a combination of at least two hardware and software attributes. Must be at least 20 characters.
        CustomerDeviceFingerprint: string option
        ///Unique identifier of the cardholder’s device such as a device serial number (e.g., International Mobile Equipment Identity [IMEI]). Must be at least 15 characters.
        CustomerDeviceId: string option
        ///The email address of the customer.
        CustomerEmailAddress: string option
        ///The IP address that the customer used when making the purchase.
        CustomerPurchaseIp: string option
        ///A description of the product or service that was sold.
        ProductDescription: string option
        ///The address to which a physical product was shipped. All fields are required for Visa Compelling Evidence 3.0 evidence submission.
        ShippingAddress: DisputeTransactionShippingAddress option
    }
    with
        static member New (charge: string, customerAccountId: string option, customerDeviceFingerprint: string option, customerDeviceId: string option, customerEmailAddress: string option, customerPurchaseIp: string option, productDescription: string option, shippingAddress: DisputeTransactionShippingAddress option) =
            {
                DisputeVisaCompellingEvidence3PriorUndisputedTransaction.Charge = charge //required
                DisputeVisaCompellingEvidence3PriorUndisputedTransaction.CustomerAccountId = customerAccountId //required
                DisputeVisaCompellingEvidence3PriorUndisputedTransaction.CustomerDeviceFingerprint = customerDeviceFingerprint //required
                DisputeVisaCompellingEvidence3PriorUndisputedTransaction.CustomerDeviceId = customerDeviceId //required
                DisputeVisaCompellingEvidence3PriorUndisputedTransaction.CustomerEmailAddress = customerEmailAddress //required
                DisputeVisaCompellingEvidence3PriorUndisputedTransaction.CustomerPurchaseIp = customerPurchaseIp //required
                DisputeVisaCompellingEvidence3PriorUndisputedTransaction.ProductDescription = productDescription //required
                DisputeVisaCompellingEvidence3PriorUndisputedTransaction.ShippingAddress = shippingAddress //required
            }

    ///An active entitlement describes access to a feature for a customer.
    and EntitlementsActiveEntitlement = {
        ///The [Feature](https://docs.stripe.com/api/entitlements/feature) that the customer is entitled to.
        Feature: EntitlementsActiveEntitlementFeature'AnyOf
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///A unique key you provide as your own system identifier. This may be up to 80 characters.
        LookupKey: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "entitlements.active_entitlement"

        static member New (feature: EntitlementsActiveEntitlementFeature'AnyOf, id: string, livemode: bool, lookupKey: string) =
            {
                EntitlementsActiveEntitlement.Feature = feature //required
                EntitlementsActiveEntitlement.Id = id //required
                EntitlementsActiveEntitlement.Livemode = livemode //required
                EntitlementsActiveEntitlement.LookupKey = lookupKey //required
            }

    and EntitlementsActiveEntitlementFeature'AnyOf =
        | String of string
        | EntitlementsFeature of EntitlementsFeature

    ///A summary of a customer's active entitlements.
    and EntitlementsActiveEntitlementSummary = {
        ///The customer that is entitled to this feature.
        Customer: string
        ///The list of entitlements this customer has.
        Entitlements: EntitlementsActiveEntitlementSummaryEntitlements
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "entitlements.active_entitlement_summary"

        static member New (customer: string, entitlements: EntitlementsActiveEntitlementSummaryEntitlements, livemode: bool) =
            {
                EntitlementsActiveEntitlementSummary.Customer = customer //required
                EntitlementsActiveEntitlementSummary.Entitlements = entitlements //required
                EntitlementsActiveEntitlementSummary.Livemode = livemode //required
            }

    ///The list of entitlements this customer has.
    and EntitlementsActiveEntitlementSummaryEntitlements = {
        Data: EntitlementsActiveEntitlement list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: EntitlementsActiveEntitlement list, hasMore: bool, url: string) =
            {
                EntitlementsActiveEntitlementSummaryEntitlements.Data = data //required
                EntitlementsActiveEntitlementSummaryEntitlements.HasMore = hasMore //required
                EntitlementsActiveEntitlementSummaryEntitlements.Url = url //required
            }

    ///A feature represents a monetizable ability or functionality in your system.
    ///Features can be assigned to products, and when those products are purchased, Stripe will create an entitlement to the feature for the purchasing customer.
    and EntitlementsFeature = {
        ///Inactive features cannot be attached to new products and will not be returned from the features list endpoint.
        Active: bool
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///A unique key you provide as your own system identifier. This may be up to 80 characters.
        LookupKey: string
        ///Set of key-value pairs that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///The feature's name, for your own purpose, not meant to be displayable to the customer.
        Name: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "entitlements.feature"

        static member New (active: bool, id: string, livemode: bool, lookupKey: string, metadata: Map<string, string>, name: string) =
            {
                EntitlementsFeature.Active = active //required
                EntitlementsFeature.Id = id //required
                EntitlementsFeature.Livemode = livemode //required
                EntitlementsFeature.LookupKey = lookupKey //required
                EntitlementsFeature.Metadata = metadata //required
                EntitlementsFeature.Name = name //required
            }

    ///An error response from the Stripe API
    and Error = {
        Error: ApiErrors
    }
    with
        static member New (error: ApiErrors) =
            {
                Error.Error = error //required
            }

    ///Snapshot events allow you to track and react to activity in your Stripe integration. When
    ///the state of another API resource changes, Stripe creates an `Event` object that contains
    ///all the relevant information associated with that action, including the affected API
    ///resource. For example, a successful payment triggers a `charge.succeeded` event, which
    ///contains the `Charge` in the event's data property. Some actions trigger multiple events.
    ///For example, if you create a new subscription for a customer, it triggers both a
    ///`customer.subscription.created` event and a `charge.succeeded` event.
    ///Configure an event destination in your account to listen for events that represent actions
    ///your integration needs to respond to. Additionally, you can retrieve an individual event or
    ///a list of events from the API.
    ///[Connect](https://docs.stripe.com/connect) platforms can also receive event notifications
    ///that occur in their connected accounts. These events include an account attribute that
    ///identifies the relevant connected account.
    ///You can access events through the [Retrieve Event API](https://docs.stripe.com/api/events#retrieve_event)
    ///for 30 days.
    and Event = {
        ///The connected account that originates the event.
        Account: string option
        ///The Stripe API version used to render `data` when the event was created. The contents of `data` never change, so this value remains static regardless of the API version currently in use. This property is populated only for events created on or after October 31, 2014.
        ApiVersion: string option
        ///Authentication context needed to fetch the event or related object.
        Context: string option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        Data: NotificationEventData
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Number of webhooks that haven't been successfully delivered (for example, to return a 20x response) to the URLs you specify.
        PendingWebhooks: int
        ///Information on the API request that triggers the event.
        Request: NotificationEventRequest option
        ///Description of the event (for example, `invoice.created` or `charge.refunded`).
        Type: EventType
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "event"

        static member New (apiVersion: string option, created: DateTime, data: NotificationEventData, id: string, livemode: bool, pendingWebhooks: int, request: NotificationEventRequest option, ``type``: EventType, ?account: string, ?context: string) =
            {
                Event.ApiVersion = apiVersion //required
                Event.Created = created //required
                Event.Data = data //required
                Event.Id = id //required
                Event.Livemode = livemode //required
                Event.PendingWebhooks = pendingWebhooks //required
                Event.Request = request //required
                Event.Type = ``type`` //required
                Event.Account = account
                Event.Context = context
            }

    ///[Deprecated] The `ExchangeRate` APIs are deprecated. Please use the [FX Quotes API](https://docs.stripe.com/payments/currencies/localize-prices/fx-quotes-api) instead.
    ///`ExchangeRate` objects allow you to determine the rates that Stripe is currently
    ///using to convert from one currency to another. Since this number is variable
    ///throughout the day, there are various reasons why you might want to know the current
    ///rate (for example, to dynamically price an item for a user with a default
    ///payment in a foreign currency).
    ///Please refer to our [Exchange Rates API](https://docs.stripe.com/fx-rates) guide for more details.
    ///*[Note: this integration path is supported but no longer recommended]* Additionally,
    ///you can guarantee that a charge is made with an exchange rate that you expect is
    ///current. To do so, you must pass in the exchange_rate to charges endpoints. If the
    ///value is no longer up to date, the charge won't go through. Please refer to our
    ///[Using with charges](https://docs.stripe.com/exchange-rates) guide for more details.
    ///-----
    ///&nbsp;
    ///*This Exchange Rates API is a Beta Service and is subject to Stripe's terms of service. You may use the API solely for the purpose of transacting on Stripe. For example, the API may be queried in order to:*
    ///- *localize prices for processing payments on Stripe*
    ///- *reconcile Stripe transactions*
    ///- *determine how much money to send to a connected account*
    ///- *determine app fees to charge a connected account*
    ///*Using this Exchange Rates API beta for any purpose other than to transact on Stripe is strictly prohibited and constitutes a violation of Stripe's terms of service.*
    and ExchangeRate = {
        ///Unique identifier for the object. Represented as the three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html) in lowercase.
        Id: string
        ///Hash where the keys are supported currencies and the values are the exchange rate at which the base id currency converts to the key currency.
        Rates: Map<string, string list>
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "exchange_rate"

        static member New (id: string, rates: Map<string, string list>) =
            {
                ExchangeRate.Id = id //required
                ExchangeRate.Rates = rates //required
            }

    and ExternalAccount =
        | BankAccount of BankAccount
        | Card of Card

    and ExternalAccountRequirements = {
        ///Fields that need to be resolved to keep the external account enabled. If not resolved by `current_deadline`, these fields will appear in `past_due` as well, and the account is disabled.
        CurrentlyDue: string list option
        ///Details about validation and verification failures for `due` requirements that must be resolved.
        Errors: AccountRequirementsError list option
        ///Fields that haven't been resolved by `current_deadline`. These fields need to be resolved to enable the external account.
        PastDue: string list option
        ///Fields that are being reviewed, or might become required depending on the results of a review. If the review fails, these fields can move to `eventually_due`, `currently_due`, `past_due` or `alternatives`. Fields might appear in `eventually_due`, `currently_due`, `past_due` or `alternatives` and in `pending_verification` if one verification fails but another is still pending.
        PendingVerification: string list option
    }
    with
        static member New (currentlyDue: string list option, errors: AccountRequirementsError list option, pastDue: string list option, pendingVerification: string list option) =
            {
                ExternalAccountRequirements.CurrentlyDue = currentlyDue //required
                ExternalAccountRequirements.Errors = errors //required
                ExternalAccountRequirements.PastDue = pastDue //required
                ExternalAccountRequirements.PendingVerification = pendingVerification //required
            }

    and Fee = {
        ///Amount of the fee, in cents.
        Amount: int
        ///ID of the Connect application that earned the fee.
        Application: string option
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///Type of the fee, one of: `application_fee`, `payment_method_passthrough_fee`, `stripe_fee`, `tax`, or `withheld_tax`.
        Type: FeeType
    }
    with
        static member New (amount: int, application: string option, currency: IsoTypes.IsoCurrencyCode, description: string option, ``type``: FeeType) =
            {
                Fee.Amount = amount //required
                Fee.Application = application //required
                Fee.Currency = currency //required
                Fee.Description = description //required
                Fee.Type = ``type`` //required
            }

    ///`Application Fee Refund` objects allow you to refund an application fee that
    ///has previously been created but not yet refunded. Funds will be refunded to
    ///the Stripe account from which the fee was originally collected.
    ///Related guide: [Refunding application fees](https://docs.stripe.com/connect/destination-charges#refunding-app-fee)
    and FeeRefund = {
        ///Amount, in cents (or local equivalent).
        Amount: int
        ///Balance transaction that describes the impact on your account balance.
        BalanceTransaction: FeeRefundBalanceTransaction'AnyOf option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///ID of the application fee that was refunded.
        Fee: FeeRefundFee'AnyOf
        ///Unique identifier for the object.
        Id: string
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "fee_refund"

        static member New (amount: int, balanceTransaction: FeeRefundBalanceTransaction'AnyOf option, created: DateTime, currency: IsoTypes.IsoCurrencyCode, fee: FeeRefundFee'AnyOf, id: string, metadata: Map<string, string> option) =
            {
                FeeRefund.Amount = amount //required
                FeeRefund.BalanceTransaction = balanceTransaction //required
                FeeRefund.Created = created //required
                FeeRefund.Currency = currency //required
                FeeRefund.Fee = fee //required
                FeeRefund.Id = id //required
                FeeRefund.Metadata = metadata //required
            }

    and FeeRefundBalanceTransaction'AnyOf =
        | String of string
        | BalanceTransaction of BalanceTransaction

    and FeeRefundFee'AnyOf =
        | String of string
        | ApplicationFee of ApplicationFee

    ///This object represents files hosted on Stripe's servers. You can upload
    ///files with the [create file](https://api.stripe.com#create_file) request
    ///(for example, when uploading dispute evidence). Stripe also
    ///creates files independently (for example, the results of a [Sigma scheduled
    ///query](#scheduled_queries)).
    ///Related guide: [File upload guide](https://docs.stripe.com/file-upload)
    and File = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The file expires and isn't available at this time in epoch seconds.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime option
        ///The suitable name for saving the file to a filesystem.
        Filename: string option
        ///Unique identifier for the object.
        Id: string
        ///A list of [file links](https://api.stripe.com#file_links) that point at this file.
        Links: FileLinks option
        ///The [purpose](https://docs.stripe.com/file-upload#uploading-a-file) of the uploaded file.
        Purpose: FilePurpose
        ///The size of the file object in bytes.
        Size: int
        ///A suitable title for the document.
        Title: string option
        ///The returned file type (for example, `csv`, `pdf`, `jpg`, or `png`).
        Type: string option
        ///Use your live secret API key to download the file from this URL.
        Url: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "file"

        static member New (created: DateTime, expiresAt: DateTime option, filename: string option, id: string, purpose: FilePurpose, size: int, title: string option, ``type``: string option, url: string option, ?links: FileLinks option) =
            {
                File.Created = created //required
                File.ExpiresAt = expiresAt //required
                File.Filename = filename //required
                File.Id = id //required
                File.Purpose = purpose //required
                File.Size = size //required
                File.Title = title //required
                File.Type = ``type`` //required
                File.Url = url //required
                File.Links = links |> Option.flatten
            }

    ///A list of [file links](https://api.stripe.com#file_links) that point at this file.
    and FileLinks = {
        ///Details about each object.
        Data: FileLink list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: FileLink list, hasMore: bool, url: string) =
            {
                FileLinks.Data = data //required
                FileLinks.HasMore = hasMore //required
                FileLinks.Url = url //required
            }

    ///To share the contents of a `File` object with non-Stripe users, you can
    ///create a `FileLink`. `FileLink`s contain a URL that you can use to
    ///retrieve the contents of the file without authentication.
    and FileLink = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Returns if the link is already expired.
        Expired: bool
        ///Time that the link expires.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime option
        ///The file object this link points to.
        File: FileLinkFile'AnyOf
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///The publicly accessible URL to download the file.
        Url: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "file_link"

        static member New (created: DateTime, expired: bool, expiresAt: DateTime option, file: FileLinkFile'AnyOf, id: string, livemode: bool, metadata: Map<string, string>, url: string option) =
            {
                FileLink.Created = created //required
                FileLink.Expired = expired //required
                FileLink.ExpiresAt = expiresAt //required
                FileLink.File = file //required
                FileLink.Id = id //required
                FileLink.Livemode = livemode //required
                FileLink.Metadata = metadata //required
                FileLink.Url = url //required
            }

    and FileLinkFile'AnyOf =
        | String of string
        | File of File

    ///A Financial Connections Account represents an account that exists outside of Stripe, to which you have been granted some degree of access.
    and FinancialConnectionsAccount = {
        ///The account holder that this account belongs to.
        AccountHolder: BankConnectionsResourceAccountholder option
        ///Details about the account numbers.
        AccountNumbers: BankConnectionsResourceAccountNumberDetails list option
        ///The most recent information about the account's balance.
        Balance: BankConnectionsResourceBalance option
        ///The state of the most recent attempt to refresh the account balance.
        BalanceRefresh: BankConnectionsResourceBalanceRefresh option
        ///The type of the account. Account category is further divided in `subcategory`.
        Category: FinancialConnectionsAccountCategory
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///A human-readable name that has been assigned to this account, either by the account holder or by the institution.
        DisplayName: string option
        ///Unique identifier for the object.
        Id: string
        ///The name of the institution that holds this account.
        InstitutionName: string
        ///The last 4 digits of the account number. If present, this will be 4 numeric characters.
        [<JsonField(Name="last4")>]Last4: string option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The most recent information about the account's owners.
        Ownership: FinancialConnectionsAccountOwnership'AnyOf option
        ///The state of the most recent attempt to refresh the account owners.
        OwnershipRefresh: BankConnectionsResourceOwnershipRefresh option
        ///The list of permissions granted by this account.
        Permissions: FinancialConnectionsAccountPermissions list option
        ///The status of the link to the account.
        Status: FinancialConnectionsAccountStatus
        ///If `category` is `cash`, one of:
        /// - `checking`
        /// - `savings`
        /// - `other`
        ///If `category` is `credit`, one of:
        /// - `mortgage`
        /// - `line_of_credit`
        /// - `credit_card`
        /// - `other`
        ///If `category` is `investment` or `other`, this will be `other`.
        Subcategory: FinancialConnectionsAccountSubcategory
        ///The list of data refresh subscriptions requested on this account.
        Subscriptions: string list option
        ///The [PaymentMethod type](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type)(s) that can be created from this account.
        SupportedPaymentMethodTypes: FinancialConnectionsAccountSupportedPaymentMethodTypes list
        ///The state of the most recent attempt to refresh the account transactions.
        TransactionRefresh: BankConnectionsResourceTransactionRefresh option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "financial_connections.account"

        static member New (accountHolder: BankConnectionsResourceAccountholder option, accountNumbers: BankConnectionsResourceAccountNumberDetails list option, balance: BankConnectionsResourceBalance option, balanceRefresh: BankConnectionsResourceBalanceRefresh option, category: FinancialConnectionsAccountCategory, created: DateTime, displayName: string option, id: string, institutionName: string, last4: string option, livemode: bool, ownership: FinancialConnectionsAccountOwnership'AnyOf option, ownershipRefresh: BankConnectionsResourceOwnershipRefresh option, permissions: FinancialConnectionsAccountPermissions list option, status: FinancialConnectionsAccountStatus, subcategory: FinancialConnectionsAccountSubcategory, subscriptions: string list option, supportedPaymentMethodTypes: FinancialConnectionsAccountSupportedPaymentMethodTypes list, transactionRefresh: BankConnectionsResourceTransactionRefresh option) =
            {
                FinancialConnectionsAccount.AccountHolder = accountHolder //required
                FinancialConnectionsAccount.AccountNumbers = accountNumbers //required
                FinancialConnectionsAccount.Balance = balance //required
                FinancialConnectionsAccount.BalanceRefresh = balanceRefresh //required
                FinancialConnectionsAccount.Category = category //required
                FinancialConnectionsAccount.Created = created //required
                FinancialConnectionsAccount.DisplayName = displayName //required
                FinancialConnectionsAccount.Id = id //required
                FinancialConnectionsAccount.InstitutionName = institutionName //required
                FinancialConnectionsAccount.Last4 = last4 //required
                FinancialConnectionsAccount.Livemode = livemode //required
                FinancialConnectionsAccount.Ownership = ownership //required
                FinancialConnectionsAccount.OwnershipRefresh = ownershipRefresh //required
                FinancialConnectionsAccount.Permissions = permissions //required
                FinancialConnectionsAccount.Status = status //required
                FinancialConnectionsAccount.Subcategory = subcategory //required
                FinancialConnectionsAccount.Subscriptions = subscriptions //required
                FinancialConnectionsAccount.SupportedPaymentMethodTypes = supportedPaymentMethodTypes //required
                FinancialConnectionsAccount.TransactionRefresh = transactionRefresh //required
            }

    and FinancialConnectionsAccountOwnership'AnyOf =
        | String of string
        | FinancialConnectionsAccountOwnership of FinancialConnectionsAccountOwnership

    ///Describes a snapshot of the owners of an account at a particular point in time.
    and FinancialConnectionsAccountOwnership = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Unique identifier for the object.
        Id: string
        ///A paginated list of owners for this account.
        Owners: FinancialConnectionsAccountOwnershipOwners
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "financial_connections.account_ownership"

        static member New (created: DateTime, id: string, owners: FinancialConnectionsAccountOwnershipOwners) =
            {
                FinancialConnectionsAccountOwnership.Created = created //required
                FinancialConnectionsAccountOwnership.Id = id //required
                FinancialConnectionsAccountOwnership.Owners = owners //required
            }

    ///A paginated list of owners for this account.
    and FinancialConnectionsAccountOwnershipOwners = {
        ///Details about each object.
        Data: FinancialConnectionsAccountOwner list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: FinancialConnectionsAccountOwner list, hasMore: bool, url: string) =
            {
                FinancialConnectionsAccountOwnershipOwners.Data = data //required
                FinancialConnectionsAccountOwnershipOwners.HasMore = hasMore //required
                FinancialConnectionsAccountOwnershipOwners.Url = url //required
            }

    ///A Financial Connections Session is the secure way to programmatically launch the client-side Stripe.js modal that lets your users link their accounts.
    and FinancialConnectionsSession = {
        ///The account holder for whom accounts are collected in this session.
        AccountHolder: BankConnectionsResourceAccountholder option
        ///The accounts that were collected as part of this Session.
        Accounts: FinancialConnectionsSessionAccounts
        ///A value that will be passed to the client to launch the authentication flow.
        ClientSecret: string option
        Filters: BankConnectionsResourceLinkAccountSessionFilters option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Permissions requested for accounts collected during this session.
        Permissions: FinancialConnectionsSessionPermissions list
        ///Data features requested to be retrieved upon account creation.
        Prefetch: FinancialConnectionsSessionPrefetch list option
        ///For webview integrations only. Upon completing OAuth login in the native browser, the user will be redirected to this URL to return to your app.
        ReturnUrl: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "financial_connections.session"

        static member New (accountHolder: BankConnectionsResourceAccountholder option, accounts: FinancialConnectionsSessionAccounts, clientSecret: string option, id: string, livemode: bool, permissions: FinancialConnectionsSessionPermissions list, prefetch: FinancialConnectionsSessionPrefetch list option, ?filters: BankConnectionsResourceLinkAccountSessionFilters, ?returnUrl: string) =
            {
                FinancialConnectionsSession.AccountHolder = accountHolder //required
                FinancialConnectionsSession.Accounts = accounts //required
                FinancialConnectionsSession.ClientSecret = clientSecret //required
                FinancialConnectionsSession.Id = id //required
                FinancialConnectionsSession.Livemode = livemode //required
                FinancialConnectionsSession.Permissions = permissions //required
                FinancialConnectionsSession.Prefetch = prefetch //required
                FinancialConnectionsSession.Filters = filters
                FinancialConnectionsSession.ReturnUrl = returnUrl
            }

    ///The accounts that were collected as part of this Session.
    and FinancialConnectionsSessionAccounts = {
        ///Details about each object.
        Data: FinancialConnectionsAccount list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: FinancialConnectionsAccount list, hasMore: bool, url: string) =
            {
                FinancialConnectionsSessionAccounts.Data = data //required
                FinancialConnectionsSessionAccounts.HasMore = hasMore //required
                FinancialConnectionsSessionAccounts.Url = url //required
            }

    ///A Transaction represents a real transaction that affects a Financial Connections Account balance.
    and FinancialConnectionsTransaction = {
        ///The ID of the Financial Connections Account this transaction belongs to.
        Account: string
        ///The amount of this transaction, in cents (or local equivalent).
        Amount: int
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///The description of this transaction.
        Description: string
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The status of the transaction.
        Status: FinancialConnectionsTransactionStatus
        StatusTransitions: BankConnectionsResourceTransactionResourceStatusTransitions
        ///Time at which the transaction was transacted. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]TransactedAt: DateTime
        ///The token of the transaction refresh that last updated or created this transaction.
        TransactionRefresh: string
        ///Time at which the object was last updated. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Updated: DateTime
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "financial_connections.transaction"

        static member New (account: string, amount: int, currency: IsoTypes.IsoCurrencyCode, description: string, id: string, livemode: bool, status: FinancialConnectionsTransactionStatus, statusTransitions: BankConnectionsResourceTransactionResourceStatusTransitions, transactedAt: DateTime, transactionRefresh: string, updated: DateTime) =
            {
                FinancialConnectionsTransaction.Account = account //required
                FinancialConnectionsTransaction.Amount = amount //required
                FinancialConnectionsTransaction.Currency = currency //required
                FinancialConnectionsTransaction.Description = description //required
                FinancialConnectionsTransaction.Id = id //required
                FinancialConnectionsTransaction.Livemode = livemode //required
                FinancialConnectionsTransaction.Status = status //required
                FinancialConnectionsTransaction.StatusTransitions = statusTransitions //required
                FinancialConnectionsTransaction.TransactedAt = transactedAt //required
                FinancialConnectionsTransaction.TransactionRefresh = transactionRefresh //required
                FinancialConnectionsTransaction.Updated = updated //required
            }

    and FinancialReportingFinanceReportRunRunParameters = {
        ///The set of output columns requested for inclusion in the report run.
        Columns: string list option
        ///Connected account ID by which to filter the report run.
        ConnectedAccount: string option
        ///Currency of objects to be included in the report run.
        Currency: IsoTypes.IsoCurrencyCode option
        ///Ending timestamp of data to be included in the report run. Can be any UTC timestamp between 1 second after the user specified `interval_start` and 1 second before this report's last `data_available_end` value.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]IntervalEnd: DateTime option
        ///Starting timestamp of data to be included in the report run. Can be any UTC timestamp between 1 second after this report's `data_available_start` and 1 second before the user specified `interval_end` value.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]IntervalStart: DateTime option
        ///Payout ID by which to filter the report run.
        Payout: string option
        ///Category of balance transactions to be included in the report run.
        ReportingCategory: string option
        ///Defaults to `Etc/UTC`. The output timezone for all timestamps in the report. A list of possible time zone values is maintained at the [IANA Time Zone Database](http://www.iana.org/time-zones). Has no effect on `interval_start` or `interval_end`.
        Timezone: FinancialReportingFinanceReportRunRunParametersTimezone option
    }
    with
        static member New (?columns: string list, ?connectedAccount: string, ?currency: IsoTypes.IsoCurrencyCode, ?intervalEnd: DateTime, ?intervalStart: DateTime, ?payout: string, ?reportingCategory: string, ?timezone: FinancialReportingFinanceReportRunRunParametersTimezone) =
            {
                FinancialReportingFinanceReportRunRunParameters.Columns = columns
                FinancialReportingFinanceReportRunRunParameters.ConnectedAccount = connectedAccount
                FinancialReportingFinanceReportRunRunParameters.Currency = currency
                FinancialReportingFinanceReportRunRunParameters.IntervalEnd = intervalEnd
                FinancialReportingFinanceReportRunRunParameters.IntervalStart = intervalStart
                FinancialReportingFinanceReportRunRunParameters.Payout = payout
                FinancialReportingFinanceReportRunRunParameters.ReportingCategory = reportingCategory
                FinancialReportingFinanceReportRunRunParameters.Timezone = timezone
            }

    ///Details about the request forwarded to the destination endpoint.
    and ForwardedRequestDetails = {
        ///The body payload to send to the destination endpoint.
        Body: string
        ///The headers to include in the forwarded request. Can be omitted if no additional headers (excluding Stripe-generated ones such as the Content-Type header) should be included.
        Headers: ForwardedRequestHeader list
    }
    with
        ///The HTTP method used to call the destination endpoint.
        member _.HttpMethod = "POST"

        static member New (body: string, headers: ForwardedRequestHeader list) =
            {
                ForwardedRequestDetails.Body = body //required
                ForwardedRequestDetails.Headers = headers //required
            }

    ///Details about the response from the destination endpoint.
    and ForwardedResponseDetails = {
        ///The response body from the destination endpoint to Stripe.
        Body: string
        ///HTTP headers that the destination endpoint returned.
        Headers: ForwardedRequestHeader list
        ///The HTTP status code that the destination endpoint returned.
        Status: int
    }
    with
        static member New (body: string, headers: ForwardedRequestHeader list, status: int) =
            {
                ForwardedResponseDetails.Body = body //required
                ForwardedResponseDetails.Headers = headers //required
                ForwardedResponseDetails.Status = status //required
            }

    ///Instructs Stripe to make a request on your behalf using the destination URL. The destination URL
    ///is activated by Stripe at the time of onboarding. Stripe verifies requests with your credentials
    ///provided during onboarding, and injects card details from the payment_method into the request.
    ///Stripe redacts all sensitive fields and headers, including authentication credentials and card numbers,
    ///before storing the request and response data in the forwarding Request object, which are subject to a
    ///30-day retention period.
    ///You can provide a Stripe idempotency key to make sure that requests with the same key result in only one
    ///outbound request. The Stripe idempotency key provided should be unique and different from any idempotency
    ///keys provided on the underlying third-party request.
    ///Forwarding Requests are synchronous requests that return a response or time out according to
    ///Stripe’s limits.
    ///Related guide: [Forward card details to third-party API endpoints](https://docs.stripe.com/payments/forwarding).
    and ForwardingRequest = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///The PaymentMethod to insert into the forwarded request. Forwarding previously consumed PaymentMethods is allowed.
        PaymentMethod: string
        ///The field kinds to be replaced in the forwarded request.
        Replacements: ForwardingRequestReplacements list
        ///Context about the request from Stripe's servers to the destination endpoint.
        RequestContext: ForwardedRequestContext option
        ///The request that was sent to the destination endpoint. We redact any sensitive fields.
        RequestDetails: ForwardedRequestDetails option
        ///The response that the destination endpoint returned to us. We redact any sensitive fields.
        ResponseDetails: ForwardedResponseDetails option
        ///The destination URL for the forwarded request. Must be supported by the config.
        Url: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "forwarding.request"

        static member New (created: DateTime, id: string, livemode: bool, paymentMethod: string, replacements: ForwardingRequestReplacements list, requestContext: ForwardedRequestContext option, requestDetails: ForwardedRequestDetails option, responseDetails: ForwardedResponseDetails option, url: string option, ?metadata: Map<string, string> option) =
            {
                ForwardingRequest.Created = created //required
                ForwardingRequest.Id = id //required
                ForwardingRequest.Livemode = livemode //required
                ForwardingRequest.PaymentMethod = paymentMethod //required
                ForwardingRequest.Replacements = replacements //required
                ForwardingRequest.RequestContext = requestContext //required
                ForwardingRequest.RequestDetails = requestDetails //required
                ForwardingRequest.ResponseDetails = responseDetails //required
                ForwardingRequest.Url = url //required
                ForwardingRequest.Metadata = metadata |> Option.flatten
            }

    ///Each customer has a [`balance`](https://docs.stripe.com/api/customers/object#customer_object-balance) that is
    ///automatically applied to future invoices and payments using the `customer_balance` payment method.
    ///Customers can fund this balance by initiating a bank transfer to any account in the
    ///`financial_addresses` field.
    ///Related guide: [Customer balance funding instructions](https://docs.stripe.com/payments/customer-balance/funding-instructions)
    and FundingInstructions = {
        BankTransfer: FundingInstructionsBankTransfer
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
    }
    with
        ///The `funding_type` of the returned instructions
        member _.FundingType = "bank_transfer"
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "funding_instructions"

        static member New (bankTransfer: FundingInstructionsBankTransfer, currency: IsoTypes.IsoCurrencyCode, livemode: bool) =
            {
                FundingInstructions.BankTransfer = bankTransfer //required
                FundingInstructions.Currency = currency //required
                FundingInstructions.Livemode = livemode //required
            }

    and FundingInstructionsBankTransfer = {
        ///The country of the bank account to fund
        Country: IsoTypes.IsoCountryCode
        ///A list of financial addresses that can be used to fund a particular balance
        FinancialAddresses: FundingInstructionsBankTransferFinancialAddress list
        ///The bank_transfer type
        Type: FundingInstructionsBankTransferType
    }
    with
        static member New (country: IsoTypes.IsoCountryCode, financialAddresses: FundingInstructionsBankTransferFinancialAddress list, ``type``: FundingInstructionsBankTransferType) =
            {
                FundingInstructionsBankTransfer.Country = country //required
                FundingInstructionsBankTransfer.FinancialAddresses = financialAddresses //required
                FundingInstructionsBankTransfer.Type = ``type`` //required
            }

    ///ABA Records contain U.S. bank account details per the ABA format.
    and FundingInstructionsBankTransferAbaRecord = {
        AccountHolderAddress: Address
        ///The account holder name
        AccountHolderName: string
        ///The ABA account number
        AccountNumber: string
        ///The account type
        AccountType: string
        BankAddress: Address
        ///The bank name
        BankName: string
        ///The ABA routing number
        RoutingNumber: string
    }
    with
        static member New (accountHolderAddress: Address, accountHolderName: string, accountNumber: string, accountType: string, bankAddress: Address, bankName: string, routingNumber: string) =
            {
                FundingInstructionsBankTransferAbaRecord.AccountHolderAddress = accountHolderAddress //required
                FundingInstructionsBankTransferAbaRecord.AccountHolderName = accountHolderName //required
                FundingInstructionsBankTransferAbaRecord.AccountNumber = accountNumber //required
                FundingInstructionsBankTransferAbaRecord.AccountType = accountType //required
                FundingInstructionsBankTransferAbaRecord.BankAddress = bankAddress //required
                FundingInstructionsBankTransferAbaRecord.BankName = bankName //required
                FundingInstructionsBankTransferAbaRecord.RoutingNumber = routingNumber //required
            }

    ///FinancialAddresses contain identifying information that resolves to a FinancialAccount.
    and FundingInstructionsBankTransferFinancialAddress = {
        Aba: FundingInstructionsBankTransferAbaRecord option
        Iban: FundingInstructionsBankTransferIbanRecord option
        SortCode: FundingInstructionsBankTransferSortCodeRecord option
        Spei: FundingInstructionsBankTransferSpeiRecord option
        ///The payment networks supported by this FinancialAddress
        SupportedNetworks: FundingInstructionsBankTransferFinancialAddressSupportedNetworks list option
        Swift: FundingInstructionsBankTransferSwiftRecord option
        ///The type of financial address
        Type: FundingInstructionsBankTransferFinancialAddressType
        Zengin: FundingInstructionsBankTransferZenginRecord option
    }
    with
        static member New (``type``: FundingInstructionsBankTransferFinancialAddressType, ?aba: FundingInstructionsBankTransferAbaRecord, ?iban: FundingInstructionsBankTransferIbanRecord, ?sortCode: FundingInstructionsBankTransferSortCodeRecord, ?spei: FundingInstructionsBankTransferSpeiRecord, ?supportedNetworks: FundingInstructionsBankTransferFinancialAddressSupportedNetworks list, ?swift: FundingInstructionsBankTransferSwiftRecord, ?zengin: FundingInstructionsBankTransferZenginRecord) =
            {
                FundingInstructionsBankTransferFinancialAddress.Type = ``type`` //required
                FundingInstructionsBankTransferFinancialAddress.Aba = aba
                FundingInstructionsBankTransferFinancialAddress.Iban = iban
                FundingInstructionsBankTransferFinancialAddress.SortCode = sortCode
                FundingInstructionsBankTransferFinancialAddress.Spei = spei
                FundingInstructionsBankTransferFinancialAddress.SupportedNetworks = supportedNetworks
                FundingInstructionsBankTransferFinancialAddress.Swift = swift
                FundingInstructionsBankTransferFinancialAddress.Zengin = zengin
            }

    ///Iban Records contain E.U. bank account details per the SEPA format.
    and FundingInstructionsBankTransferIbanRecord = {
        AccountHolderAddress: Address
        ///The name of the person or business that owns the bank account
        AccountHolderName: string
        BankAddress: Address
        ///The BIC/SWIFT code of the account.
        Bic: string
        ///Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
        Country: IsoTypes.IsoCountryCode
        ///The IBAN of the account.
        Iban: string
    }
    with
        static member New (accountHolderAddress: Address, accountHolderName: string, bankAddress: Address, bic: string, country: IsoTypes.IsoCountryCode, iban: string) =
            {
                FundingInstructionsBankTransferIbanRecord.AccountHolderAddress = accountHolderAddress //required
                FundingInstructionsBankTransferIbanRecord.AccountHolderName = accountHolderName //required
                FundingInstructionsBankTransferIbanRecord.BankAddress = bankAddress //required
                FundingInstructionsBankTransferIbanRecord.Bic = bic //required
                FundingInstructionsBankTransferIbanRecord.Country = country //required
                FundingInstructionsBankTransferIbanRecord.Iban = iban //required
            }

    ///Sort Code Records contain U.K. bank account details per the sort code format.
    and FundingInstructionsBankTransferSortCodeRecord = {
        AccountHolderAddress: Address
        ///The name of the person or business that owns the bank account
        AccountHolderName: string
        ///The account number
        AccountNumber: string
        BankAddress: Address
        ///The six-digit sort code
        SortCode: string
    }
    with
        static member New (accountHolderAddress: Address, accountHolderName: string, accountNumber: string, bankAddress: Address, sortCode: string) =
            {
                FundingInstructionsBankTransferSortCodeRecord.AccountHolderAddress = accountHolderAddress //required
                FundingInstructionsBankTransferSortCodeRecord.AccountHolderName = accountHolderName //required
                FundingInstructionsBankTransferSortCodeRecord.AccountNumber = accountNumber //required
                FundingInstructionsBankTransferSortCodeRecord.BankAddress = bankAddress //required
                FundingInstructionsBankTransferSortCodeRecord.SortCode = sortCode //required
            }

    ///SPEI Records contain Mexico bank account details per the SPEI format.
    and FundingInstructionsBankTransferSpeiRecord = {
        AccountHolderAddress: Address
        ///The account holder name
        AccountHolderName: string
        BankAddress: Address
        ///The three-digit bank code
        BankCode: string
        ///The short banking institution name
        BankName: string
        ///The CLABE number
        Clabe: string
    }
    with
        static member New (accountHolderAddress: Address, accountHolderName: string, bankAddress: Address, bankCode: string, bankName: string, clabe: string) =
            {
                FundingInstructionsBankTransferSpeiRecord.AccountHolderAddress = accountHolderAddress //required
                FundingInstructionsBankTransferSpeiRecord.AccountHolderName = accountHolderName //required
                FundingInstructionsBankTransferSpeiRecord.BankAddress = bankAddress //required
                FundingInstructionsBankTransferSpeiRecord.BankCode = bankCode //required
                FundingInstructionsBankTransferSpeiRecord.BankName = bankName //required
                FundingInstructionsBankTransferSpeiRecord.Clabe = clabe //required
            }

    ///SWIFT Records contain U.S. bank account details per the SWIFT format.
    and FundingInstructionsBankTransferSwiftRecord = {
        AccountHolderAddress: Address
        ///The account holder name
        AccountHolderName: string
        ///The account number
        AccountNumber: string
        ///The account type
        AccountType: string
        BankAddress: Address
        ///The bank name
        BankName: string
        ///The SWIFT code
        SwiftCode: string
    }
    with
        static member New (accountHolderAddress: Address, accountHolderName: string, accountNumber: string, accountType: string, bankAddress: Address, bankName: string, swiftCode: string) =
            {
                FundingInstructionsBankTransferSwiftRecord.AccountHolderAddress = accountHolderAddress //required
                FundingInstructionsBankTransferSwiftRecord.AccountHolderName = accountHolderName //required
                FundingInstructionsBankTransferSwiftRecord.AccountNumber = accountNumber //required
                FundingInstructionsBankTransferSwiftRecord.AccountType = accountType //required
                FundingInstructionsBankTransferSwiftRecord.BankAddress = bankAddress //required
                FundingInstructionsBankTransferSwiftRecord.BankName = bankName //required
                FundingInstructionsBankTransferSwiftRecord.SwiftCode = swiftCode //required
            }

    ///Zengin Records contain Japan bank account details per the Zengin format.
    and FundingInstructionsBankTransferZenginRecord = {
        AccountHolderAddress: Address
        ///The account holder name
        AccountHolderName: string option
        ///The account number
        AccountNumber: string option
        ///The bank account type. In Japan, this can only be `futsu` or `toza`.
        AccountType: FundingInstructionsBankTransferZenginRecordAccountType option
        BankAddress: Address
        ///The bank code of the account
        BankCode: string option
        ///The bank name of the account
        BankName: string option
        ///The branch code of the account
        BranchCode: string option
        ///The branch name of the account
        BranchName: string option
    }
    with
        static member New (accountHolderAddress: Address, accountHolderName: string option, accountNumber: string option, accountType: FundingInstructionsBankTransferZenginRecordAccountType option, bankAddress: Address, bankCode: string option, bankName: string option, branchCode: string option, branchName: string option) =
            {
                FundingInstructionsBankTransferZenginRecord.AccountHolderAddress = accountHolderAddress //required
                FundingInstructionsBankTransferZenginRecord.AccountHolderName = accountHolderName //required
                FundingInstructionsBankTransferZenginRecord.AccountNumber = accountNumber //required
                FundingInstructionsBankTransferZenginRecord.AccountType = accountType //required
                FundingInstructionsBankTransferZenginRecord.BankAddress = bankAddress //required
                FundingInstructionsBankTransferZenginRecord.BankCode = bankCode //required
                FundingInstructionsBankTransferZenginRecord.BankName = bankName //required
                FundingInstructionsBankTransferZenginRecord.BranchCode = branchCode //required
                FundingInstructionsBankTransferZenginRecord.BranchName = branchName //required
            }

    ///Result from a document check
    and GelatoDocumentReport = {
        ///Address as it appears in the document.
        Address: Address option
        ///Date of birth as it appears in the document.
        Dob: GelatoDataDocumentReportDateOfBirth option
        ///Details on the verification error. Present when status is `unverified`.
        Error: GelatoDocumentReportError option
        ///Expiration date of the document.
        ExpirationDate: GelatoDataDocumentReportExpirationDate option
        ///Array of [File](https://docs.stripe.com/api/files) ids containing images for this document.
        Files: string list option
        ///First name as it appears in the document.
        FirstName: string option
        ///Issued date of the document.
        IssuedDate: GelatoDataDocumentReportIssuedDate option
        ///Issuing country of the document.
        IssuingCountry: IsoTypes.IsoCountryCode option
        ///Last name as it appears in the document.
        LastName: string option
        ///Document ID number.
        Number: string option
        ///Sex of the person in the document.
        Sex: GelatoDocumentReportSex option
        ///Status of this `document` check.
        Status: GelatoDocumentReportStatus
        ///Type of the document.
        Type: GelatoDocumentReportType option
        ///Place of birth as it appears in the document.
        UnparsedPlaceOfBirth: string option
        ///Sex as it appears in the document.
        UnparsedSex: string option
    }
    with
        static member New (address: Address option, error: GelatoDocumentReportError option, files: string list option, firstName: string option, issuedDate: GelatoDataDocumentReportIssuedDate option, issuingCountry: IsoTypes.IsoCountryCode option, lastName: string option, status: GelatoDocumentReportStatus, ``type``: GelatoDocumentReportType option, ?dob: GelatoDataDocumentReportDateOfBirth option, ?expirationDate: GelatoDataDocumentReportExpirationDate option, ?number: string option, ?sex: GelatoDocumentReportSex option, ?unparsedPlaceOfBirth: string option, ?unparsedSex: string option) =
            {
                GelatoDocumentReport.Address = address //required
                GelatoDocumentReport.Error = error //required
                GelatoDocumentReport.Files = files //required
                GelatoDocumentReport.FirstName = firstName //required
                GelatoDocumentReport.IssuedDate = issuedDate //required
                GelatoDocumentReport.IssuingCountry = issuingCountry //required
                GelatoDocumentReport.LastName = lastName //required
                GelatoDocumentReport.Status = status //required
                GelatoDocumentReport.Type = ``type`` //required
                GelatoDocumentReport.Dob = dob |> Option.flatten
                GelatoDocumentReport.ExpirationDate = expirationDate |> Option.flatten
                GelatoDocumentReport.Number = number |> Option.flatten
                GelatoDocumentReport.Sex = sex |> Option.flatten
                GelatoDocumentReport.UnparsedPlaceOfBirth = unparsedPlaceOfBirth |> Option.flatten
                GelatoDocumentReport.UnparsedSex = unparsedSex |> Option.flatten
            }

    and GelatoDocumentReportError = {
        ///A short machine-readable string giving the reason for the verification failure.
        Code: GelatoDocumentReportErrorCode option
        ///A human-readable message giving the reason for the failure. These messages can be shown to your users.
        Reason: string option
    }
    with
        static member New (code: GelatoDocumentReportErrorCode option, reason: string option) =
            {
                GelatoDocumentReportError.Code = code //required
                GelatoDocumentReportError.Reason = reason //required
            }

    ///Result from a email check
    and GelatoEmailReport = {
        ///Email to be verified.
        Email: string option
        ///Details on the verification error. Present when status is `unverified`.
        Error: GelatoEmailReportError option
        ///Status of this `email` check.
        Status: GelatoEmailReportStatus
    }
    with
        static member New (email: string option, error: GelatoEmailReportError option, status: GelatoEmailReportStatus) =
            {
                GelatoEmailReport.Email = email //required
                GelatoEmailReport.Error = error //required
                GelatoEmailReport.Status = status //required
            }

    and GelatoEmailReportError = {
        ///A short machine-readable string giving the reason for the verification failure.
        Code: GelatoEmailReportErrorCode option
        ///A human-readable message giving the reason for the failure. These messages can be shown to your users.
        Reason: string option
    }
    with
        static member New (code: GelatoEmailReportErrorCode option, reason: string option) =
            {
                GelatoEmailReportError.Code = code //required
                GelatoEmailReportError.Reason = reason //required
            }

    ///Result from an id_number check
    and GelatoIdNumberReport = {
        ///Date of birth.
        Dob: GelatoDataIdNumberReportDate option
        ///Details on the verification error. Present when status is `unverified`.
        Error: GelatoIdNumberReportError option
        ///First name.
        FirstName: string option
        ///ID number. When `id_number_type` is `us_ssn`, only the last 4 digits are present.
        IdNumber: string option
        ///Type of ID number.
        IdNumberType: GelatoIdNumberReportIdNumberType option
        ///Last name.
        LastName: string option
        ///Status of this `id_number` check.
        Status: GelatoIdNumberReportStatus
    }
    with
        static member New (error: GelatoIdNumberReportError option, firstName: string option, idNumberType: GelatoIdNumberReportIdNumberType option, lastName: string option, status: GelatoIdNumberReportStatus, ?dob: GelatoDataIdNumberReportDate option, ?idNumber: string option) =
            {
                GelatoIdNumberReport.Error = error //required
                GelatoIdNumberReport.FirstName = firstName //required
                GelatoIdNumberReport.IdNumberType = idNumberType //required
                GelatoIdNumberReport.LastName = lastName //required
                GelatoIdNumberReport.Status = status //required
                GelatoIdNumberReport.Dob = dob |> Option.flatten
                GelatoIdNumberReport.IdNumber = idNumber |> Option.flatten
            }

    and GelatoIdNumberReportError = {
        ///A short machine-readable string giving the reason for the verification failure.
        Code: GelatoIdNumberReportErrorCode option
        ///A human-readable message giving the reason for the failure. These messages can be shown to your users.
        Reason: string option
    }
    with
        static member New (code: GelatoIdNumberReportErrorCode option, reason: string option) =
            {
                GelatoIdNumberReportError.Code = code //required
                GelatoIdNumberReportError.Reason = reason //required
            }

    ///Result from a phone check
    and GelatoPhoneReport = {
        ///Details on the verification error. Present when status is `unverified`.
        Error: GelatoPhoneReportError option
        ///Phone to be verified.
        Phone: string option
        ///Status of this `phone` check.
        Status: GelatoPhoneReportStatus
    }
    with
        static member New (error: GelatoPhoneReportError option, phone: string option, status: GelatoPhoneReportStatus) =
            {
                GelatoPhoneReport.Error = error //required
                GelatoPhoneReport.Phone = phone //required
                GelatoPhoneReport.Status = status //required
            }

    and GelatoPhoneReportError = {
        ///A short machine-readable string giving the reason for the verification failure.
        Code: GelatoPhoneReportErrorCode option
        ///A human-readable message giving the reason for the failure. These messages can be shown to your users.
        Reason: string option
    }
    with
        static member New (code: GelatoPhoneReportErrorCode option, reason: string option) =
            {
                GelatoPhoneReportError.Code = code //required
                GelatoPhoneReportError.Reason = reason //required
            }

    and GelatoReportDocumentOptions = {
        ///Array of strings of allowed identity document types. If the provided identity document isn’t one of the allowed types, the verification check will fail with a document_type_not_allowed error code.
        AllowedTypes: GelatoReportDocumentOptionsAllowedTypes list option
        ///Collect an ID number and perform an [ID number check](https://docs.stripe.com/identity/verification-checks?type=id-number) with the document’s extracted name and date of birth.
        RequireIdNumber: bool option
        ///Disable image uploads, identity document images have to be captured using the device’s camera.
        RequireLiveCapture: bool option
        ///Capture a face image and perform a [selfie check](https://docs.stripe.com/identity/verification-checks?type=selfie) comparing a photo ID and a picture of your user’s face. [Learn more](https://docs.stripe.com/identity/selfie).
        RequireMatchingSelfie: bool option
    }
    with
        static member New (?allowedTypes: GelatoReportDocumentOptionsAllowedTypes list, ?requireIdNumber: bool, ?requireLiveCapture: bool, ?requireMatchingSelfie: bool) =
            {
                GelatoReportDocumentOptions.AllowedTypes = allowedTypes
                GelatoReportDocumentOptions.RequireIdNumber = requireIdNumber
                GelatoReportDocumentOptions.RequireLiveCapture = requireLiveCapture
                GelatoReportDocumentOptions.RequireMatchingSelfie = requireMatchingSelfie
            }

    ///Result from a selfie check
    and GelatoSelfieReport = {
        ///ID of the [File](https://docs.stripe.com/api/files) holding the image of the identity document used in this check.
        Document: string option
        ///Details on the verification error. Present when status is `unverified`.
        Error: GelatoSelfieReportError option
        ///ID of the [File](https://docs.stripe.com/api/files) holding the image of the selfie used in this check.
        Selfie: string option
        ///Status of this `selfie` check.
        Status: GelatoSelfieReportStatus
    }
    with
        static member New (document: string option, error: GelatoSelfieReportError option, selfie: string option, status: GelatoSelfieReportStatus) =
            {
                GelatoSelfieReport.Document = document //required
                GelatoSelfieReport.Error = error //required
                GelatoSelfieReport.Selfie = selfie //required
                GelatoSelfieReport.Status = status //required
            }

    and GelatoSelfieReportError = {
        ///A short machine-readable string giving the reason for the verification failure.
        Code: GelatoSelfieReportErrorCode option
        ///A human-readable message giving the reason for the failure. These messages can be shown to your users.
        Reason: string option
    }
    with
        static member New (code: GelatoSelfieReportErrorCode option, reason: string option) =
            {
                GelatoSelfieReportError.Code = code //required
                GelatoSelfieReportError.Reason = reason //required
            }

    and GelatoSessionDocumentOptions = {
        ///Array of strings of allowed identity document types. If the provided identity document isn’t one of the allowed types, the verification check will fail with a document_type_not_allowed error code.
        AllowedTypes: GelatoSessionDocumentOptionsAllowedTypes list option
        ///Collect an ID number and perform an [ID number check](https://docs.stripe.com/identity/verification-checks?type=id-number) with the document’s extracted name and date of birth.
        RequireIdNumber: bool option
        ///Disable image uploads, identity document images have to be captured using the device’s camera.
        RequireLiveCapture: bool option
        ///Capture a face image and perform a [selfie check](https://docs.stripe.com/identity/verification-checks?type=selfie) comparing a photo ID and a picture of your user’s face. [Learn more](https://docs.stripe.com/identity/selfie).
        RequireMatchingSelfie: bool option
    }
    with
        static member New (?allowedTypes: GelatoSessionDocumentOptionsAllowedTypes list, ?requireIdNumber: bool, ?requireLiveCapture: bool, ?requireMatchingSelfie: bool) =
            {
                GelatoSessionDocumentOptions.AllowedTypes = allowedTypes
                GelatoSessionDocumentOptions.RequireIdNumber = requireIdNumber
                GelatoSessionDocumentOptions.RequireLiveCapture = requireLiveCapture
                GelatoSessionDocumentOptions.RequireMatchingSelfie = requireMatchingSelfie
            }

    ///Shows last VerificationSession error
    and GelatoSessionLastError = {
        ///A short machine-readable string giving the reason for the verification or user-session failure.
        Code: GelatoSessionLastErrorCode option
        ///A message that explains the reason for verification or user-session failure.
        Reason: string option
    }
    with
        static member New (code: GelatoSessionLastErrorCode option, reason: string option) =
            {
                GelatoSessionLastError.Code = code //required
                GelatoSessionLastError.Reason = reason //required
            }

    and GelatoSessionMatchingOptions = {
        ///Strictness of the DOB matching policy to apply.
        Dob: GelatoSessionMatchingOptionsDob option
        ///Strictness of the name matching policy to apply.
        Name: GelatoSessionMatchingOptionsName option
    }
    with
        static member New (?dob: GelatoSessionMatchingOptionsDob, ?name: GelatoSessionMatchingOptionsName) =
            {
                GelatoSessionMatchingOptions.Dob = dob
                GelatoSessionMatchingOptions.Name = name
            }

    and GelatoVerificationReportOptions = {
        Document: GelatoReportDocumentOptions option
        IdNumber: GelatoReportIdNumberOptions option
    }
    with
        static member New (?document: GelatoReportDocumentOptions, ?idNumber: GelatoReportIdNumberOptions) =
            {
                GelatoVerificationReportOptions.Document = document
                GelatoVerificationReportOptions.IdNumber = idNumber
            }

    and GelatoVerificationSessionOptions = {
        Document: GelatoSessionDocumentOptions option
        Email: GelatoSessionEmailOptions option
        IdNumber: GelatoSessionIdNumberOptions option
        Matching: GelatoSessionMatchingOptions option
        Phone: GelatoSessionPhoneOptions option
    }
    with
        static member New (?document: GelatoSessionDocumentOptions, ?email: GelatoSessionEmailOptions, ?idNumber: GelatoSessionIdNumberOptions, ?matching: GelatoSessionMatchingOptions, ?phone: GelatoSessionPhoneOptions) =
            {
                GelatoVerificationSessionOptions.Document = document
                GelatoVerificationSessionOptions.Email = email
                GelatoVerificationSessionOptions.IdNumber = idNumber
                GelatoVerificationSessionOptions.Matching = matching
                GelatoVerificationSessionOptions.Phone = phone
            }

    and GelatoVerifiedOutputs = {
        ///The user's verified address.
        Address: Address option
        ///The user’s verified date of birth.
        Dob: GelatoDataVerifiedOutputsDate option
        ///The user's verified email address
        Email: string option
        ///The user's verified first name.
        FirstName: string option
        ///The user's verified id number.
        IdNumber: string option
        ///The user's verified id number type.
        IdNumberType: GelatoVerifiedOutputsIdNumberType option
        ///The user's verified last name.
        LastName: string option
        ///The user's verified phone number
        Phone: string option
        ///The user's verified sex.
        Sex: GelatoVerifiedOutputsSex option
        ///The user's verified place of birth as it appears in the document.
        UnparsedPlaceOfBirth: string option
        ///The user's verified sex as it appears in the document.
        UnparsedSex: string option
    }
    with
        static member New (address: Address option, email: string option, firstName: string option, idNumberType: GelatoVerifiedOutputsIdNumberType option, lastName: string option, phone: string option, ?dob: GelatoDataVerifiedOutputsDate option, ?idNumber: string option, ?sex: GelatoVerifiedOutputsSex option, ?unparsedPlaceOfBirth: string option, ?unparsedSex: string option) =
            {
                GelatoVerifiedOutputs.Address = address //required
                GelatoVerifiedOutputs.Email = email //required
                GelatoVerifiedOutputs.FirstName = firstName //required
                GelatoVerifiedOutputs.IdNumberType = idNumberType //required
                GelatoVerifiedOutputs.LastName = lastName //required
                GelatoVerifiedOutputs.Phone = phone //required
                GelatoVerifiedOutputs.Dob = dob |> Option.flatten
                GelatoVerifiedOutputs.IdNumber = idNumber |> Option.flatten
                GelatoVerifiedOutputs.Sex = sex |> Option.flatten
                GelatoVerifiedOutputs.UnparsedPlaceOfBirth = unparsedPlaceOfBirth |> Option.flatten
                GelatoVerifiedOutputs.UnparsedSex = unparsedSex |> Option.flatten
            }

    ///A VerificationReport is the result of an attempt to collect and verify data from a user.
    ///The collection of verification checks performed is determined from the `type` and `options`
    ///parameters used. You can find the result of each verification check performed in the
    ///appropriate sub-resource: `document`, `id_number`, `selfie`.
    ///Each VerificationReport contains a copy of any data collected by the user as well as
    ///reference IDs which can be used to access collected images through the [FileUpload](https://docs.stripe.com/api/files)
    ///API. To configure and create VerificationReports, use the
    ///[VerificationSession](https://docs.stripe.com/api/identity/verification_sessions) API.
    ///Related guide: [Accessing verification results](https://docs.stripe.com/identity/verification-sessions#results).
    and IdentityVerificationReport = {
        ///A string to reference this user. This can be a customer ID, a session ID, or similar, and can be used to reconcile this verification with your internal systems.
        ClientReferenceId: string option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        Document: GelatoDocumentReport option
        Email: GelatoEmailReport option
        ///Unique identifier for the object.
        Id: string
        IdNumber: GelatoIdNumberReport option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        Options: GelatoVerificationReportOptions option
        Phone: GelatoPhoneReport option
        Selfie: GelatoSelfieReport option
        ///Type of report.
        Type: IdentityVerificationReportType
        ///The configuration token of a verification flow from the dashboard.
        VerificationFlow: string option
        ///ID of the VerificationSession that created this report.
        VerificationSession: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "identity.verification_report"

        static member New (clientReferenceId: string option, created: DateTime, id: string, livemode: bool, ``type``: IdentityVerificationReportType, verificationSession: string option, ?document: GelatoDocumentReport, ?email: GelatoEmailReport, ?idNumber: GelatoIdNumberReport, ?options: GelatoVerificationReportOptions, ?phone: GelatoPhoneReport, ?selfie: GelatoSelfieReport, ?verificationFlow: string) =
            {
                IdentityVerificationReport.ClientReferenceId = clientReferenceId //required
                IdentityVerificationReport.Created = created //required
                IdentityVerificationReport.Id = id //required
                IdentityVerificationReport.Livemode = livemode //required
                IdentityVerificationReport.Type = ``type`` //required
                IdentityVerificationReport.VerificationSession = verificationSession //required
                IdentityVerificationReport.Document = document
                IdentityVerificationReport.Email = email
                IdentityVerificationReport.IdNumber = idNumber
                IdentityVerificationReport.Options = options
                IdentityVerificationReport.Phone = phone
                IdentityVerificationReport.Selfie = selfie
                IdentityVerificationReport.VerificationFlow = verificationFlow
            }

    ///A VerificationSession guides you through the process of collecting and verifying the identities
    ///of your users. It contains details about the type of verification, such as what [verification
    ///check](/docs/identity/verification-checks) to perform. Only create one VerificationSession for
    ///each verification in your system.
    ///A VerificationSession transitions through [multiple
    ///statuses](/docs/identity/how-sessions-work) throughout its lifetime as it progresses through
    ///the verification flow. The VerificationSession contains the user's verified data after
    ///verification checks are complete.
    ///Related guide: [The Verification Sessions API](https://docs.stripe.com/identity/verification-sessions)
    and IdentityVerificationSession = {
        ///A string to reference this user. This can be a customer ID, a session ID, or similar, and can be used to reconcile this verification with your internal systems.
        ClientReferenceId: string option
        ///The short-lived client secret used by Stripe.js to [show a verification modal](https://docs.stripe.com/js/identity/modal) inside your app. This client secret expires after 24 hours and can only be used once. Don’t store it, log it, embed it in a URL, or expose it to anyone other than the user. Make sure that you have TLS enabled on any page that includes the client secret. Refer to our docs on [passing the client secret to the frontend](https://docs.stripe.com/identity/verification-sessions#client-secret) to learn more.
        ClientSecret: string option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Unique identifier for the object.
        Id: string
        ///If present, this property tells you the last error encountered when processing the verification.
        LastError: GelatoSessionLastError option
        ///ID of the most recent VerificationReport. [Learn more about accessing detailed verification results.](https://docs.stripe.com/identity/verification-sessions#results)
        LastVerificationReport: IdentityVerificationSessionLastVerificationReport'AnyOf option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///A set of options for the session’s verification checks.
        Options: GelatoVerificationSessionOptions option
        ///Details provided about the user being verified. These details may be shown to the user.
        ProvidedDetails: GelatoProvidedDetails option
        ///Redaction status of this VerificationSession. If the VerificationSession is not redacted, this field will be null.
        Redaction: VerificationSessionRedaction option
        ///Customer ID
        RelatedCustomer: string option
        ///The ID of the Account representing a customer.
        RelatedCustomerAccount: string option
        RelatedPerson: GelatoRelatedPerson option
        ///Status of this VerificationSession. [Learn more about the lifecycle of sessions](https://docs.stripe.com/identity/how-sessions-work).
        Status: IdentityVerificationSessionStatus
        ///The type of [verification check](https://docs.stripe.com/identity/verification-checks) to be performed.
        Type: IdentityVerificationSessionType
        ///The short-lived URL that you use to redirect a user to Stripe to submit their identity information. This URL expires after 48 hours and can only be used once. Don’t store it, log it, send it in emails or expose it to anyone other than the user. Refer to our docs on [verifying identity documents](https://docs.stripe.com/identity/verify-identity-documents?platform=web&type=redirect) to learn how to redirect users to Stripe.
        Url: string option
        ///The configuration token of a verification flow from the dashboard.
        VerificationFlow: string option
        ///The user’s verified data.
        VerifiedOutputs: GelatoVerifiedOutputs option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "identity.verification_session"

        static member New (clientReferenceId: string option, clientSecret: string option, created: DateTime, id: string, lastError: GelatoSessionLastError option, lastVerificationReport: IdentityVerificationSessionLastVerificationReport'AnyOf option, livemode: bool, metadata: Map<string, string>, options: GelatoVerificationSessionOptions option, redaction: VerificationSessionRedaction option, relatedCustomer: string option, relatedCustomerAccount: string option, status: IdentityVerificationSessionStatus, ``type``: IdentityVerificationSessionType, url: string option, ?providedDetails: GelatoProvidedDetails option, ?relatedPerson: GelatoRelatedPerson, ?verificationFlow: string, ?verifiedOutputs: GelatoVerifiedOutputs option) =
            {
                IdentityVerificationSession.ClientReferenceId = clientReferenceId //required
                IdentityVerificationSession.ClientSecret = clientSecret //required
                IdentityVerificationSession.Created = created //required
                IdentityVerificationSession.Id = id //required
                IdentityVerificationSession.LastError = lastError //required
                IdentityVerificationSession.LastVerificationReport = lastVerificationReport //required
                IdentityVerificationSession.Livemode = livemode //required
                IdentityVerificationSession.Metadata = metadata //required
                IdentityVerificationSession.Options = options //required
                IdentityVerificationSession.Redaction = redaction //required
                IdentityVerificationSession.RelatedCustomer = relatedCustomer //required
                IdentityVerificationSession.RelatedCustomerAccount = relatedCustomerAccount //required
                IdentityVerificationSession.Status = status //required
                IdentityVerificationSession.Type = ``type`` //required
                IdentityVerificationSession.Url = url //required
                IdentityVerificationSession.ProvidedDetails = providedDetails |> Option.flatten
                IdentityVerificationSession.RelatedPerson = relatedPerson
                IdentityVerificationSession.VerificationFlow = verificationFlow
                IdentityVerificationSession.VerifiedOutputs = verifiedOutputs |> Option.flatten
            }

    and IdentityVerificationSessionLastVerificationReport'AnyOf =
        | String of string
        | IdentityVerificationReport of IdentityVerificationReport

    and InboundTransfers = {
        BillingDetails: TreasurySharedResourceBillingDetails
        UsBankAccount: InboundTransfersPaymentMethodDetailsUsBankAccount option
    }
    with
        ///The type of the payment method used in the InboundTransfer.
        member _.Type = "us_bank_account"

        static member New (billingDetails: TreasurySharedResourceBillingDetails, ?usBankAccount: InboundTransfersPaymentMethodDetailsUsBankAccount) =
            {
                InboundTransfers.BillingDetails = billingDetails //required
                InboundTransfers.UsBankAccount = usBankAccount
            }

    and InboundTransfersPaymentMethodDetailsUsBankAccount = {
        ///Account holder type: individual or company.
        AccountHolderType: InboundTransfersPaymentMethodDetailsUsBankAccountAccountHolderType option
        ///Account type: checkings or savings. Defaults to checking if omitted.
        AccountType: InboundTransfersPaymentMethodDetailsUsBankAccountAccountType option
        ///Name of the bank associated with the bank account.
        BankName: string option
        ///Uniquely identifies this particular bank account. You can use this attribute to check whether two bank accounts are the same.
        Fingerprint: string option
        ///Last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string option
        ///ID of the mandate used to make this payment.
        Mandate: InboundTransfersPaymentMethodDetailsUsBankAccountMandate'AnyOf option
        ///Routing number of the bank account.
        RoutingNumber: string option
    }
    with
        ///The network rails used. See the [docs](https://docs.stripe.com/treasury/money-movement/timelines) to learn more about money movement timelines for each network type.
        member _.Network = "ach"

        static member New (accountHolderType: InboundTransfersPaymentMethodDetailsUsBankAccountAccountHolderType option, accountType: InboundTransfersPaymentMethodDetailsUsBankAccountAccountType option, bankName: string option, fingerprint: string option, last4: string option, routingNumber: string option, ?mandate: InboundTransfersPaymentMethodDetailsUsBankAccountMandate'AnyOf) =
            {
                InboundTransfersPaymentMethodDetailsUsBankAccount.AccountHolderType = accountHolderType //required
                InboundTransfersPaymentMethodDetailsUsBankAccount.AccountType = accountType //required
                InboundTransfersPaymentMethodDetailsUsBankAccount.BankName = bankName //required
                InboundTransfersPaymentMethodDetailsUsBankAccount.Fingerprint = fingerprint //required
                InboundTransfersPaymentMethodDetailsUsBankAccount.Last4 = last4 //required
                InboundTransfersPaymentMethodDetailsUsBankAccount.RoutingNumber = routingNumber //required
                InboundTransfersPaymentMethodDetailsUsBankAccount.Mandate = mandate
            }

    and InboundTransfersPaymentMethodDetailsUsBankAccountMandate'AnyOf =
        | String of string
        | Mandate of Mandate

    ///Address data.
    and InsightsResourcesPaymentEvaluationAddress = {
        ///City, district, suburb, town, or village.
        City: string option
        ///Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
        Country: IsoTypes.IsoCountryCode option
        ///Address line 1, such as the street, PO Box, or company name.
        [<JsonField(Name="line1")>]Line1: string option
        ///Address line 2, such as the apartment, suite, unit, or building.
        [<JsonField(Name="line2")>]Line2: string option
        ///ZIP or postal code.
        PostalCode: string option
        ///State, county, province, or region ([ISO 3166-2](https://en.wikipedia.org/wiki/ISO_3166-2)).
        State: string option
    }
    with
        static member New (city: string option, country: IsoTypes.IsoCountryCode option, line1: string option, line2: string option, postalCode: string option, state: string option) =
            {
                InsightsResourcesPaymentEvaluationAddress.City = city //required
                InsightsResourcesPaymentEvaluationAddress.Country = country //required
                InsightsResourcesPaymentEvaluationAddress.Line1 = line1 //required
                InsightsResourcesPaymentEvaluationAddress.Line2 = line2 //required
                InsightsResourcesPaymentEvaluationAddress.PostalCode = postalCode //required
                InsightsResourcesPaymentEvaluationAddress.State = state //required
            }

    ///Billing details attached to this payment evaluation.
    and InsightsResourcesPaymentEvaluationBillingDetails = {
        Address: InsightsResourcesPaymentEvaluationAddress
        ///Email address.
        Email: string option
        ///Full name.
        Name: string option
        ///Billing phone number (including extension).
        Phone: string option
    }
    with
        static member New (address: InsightsResourcesPaymentEvaluationAddress, email: string option, name: string option, phone: string option) =
            {
                InsightsResourcesPaymentEvaluationBillingDetails.Address = address //required
                InsightsResourcesPaymentEvaluationBillingDetails.Email = email //required
                InsightsResourcesPaymentEvaluationBillingDetails.Name = name //required
                InsightsResourcesPaymentEvaluationBillingDetails.Phone = phone //required
            }

    ///Dispute opened event details attached to this payment evaluation.
    and InsightsResourcesPaymentEvaluationDisputeOpened = {
        ///Amount to dispute for this payment. A positive integer representing how much to charge in [the smallest currency unit](https://docs.stripe.com/currencies#zero-decimal) (for example, 100 cents to charge 1.00 USD or 100 to charge 100 Yen, a zero-decimal currency).
        Amount: int
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///Reason given by cardholder for dispute.
        Reason: InsightsResourcesPaymentEvaluationDisputeOpenedReason
    }
    with
        static member New (amount: int, currency: IsoTypes.IsoCurrencyCode, reason: InsightsResourcesPaymentEvaluationDisputeOpenedReason) =
            {
                InsightsResourcesPaymentEvaluationDisputeOpened.Amount = amount //required
                InsightsResourcesPaymentEvaluationDisputeOpened.Currency = currency //required
                InsightsResourcesPaymentEvaluationDisputeOpened.Reason = reason //required
            }

    ///Early Fraud Warning Received event details attached to this payment evaluation.
    and InsightsResourcesPaymentEvaluationEarlyFraudWarningReceived = {
        ///The type of fraud labeled by the issuer.
        FraudType: InsightsResourcesPaymentEvaluationEarlyFraudWarningReceivedFraudType
    }
    with
        static member New (fraudType: InsightsResourcesPaymentEvaluationEarlyFraudWarningReceivedFraudType) =
            {
                InsightsResourcesPaymentEvaluationEarlyFraudWarningReceived.FraudType = fraudType //required
            }

    ///Event reported for this payment evaluation.
    and InsightsResourcesPaymentEvaluationEvent = {
        DisputeOpened: InsightsResourcesPaymentEvaluationDisputeOpened option
        EarlyFraudWarningReceived: InsightsResourcesPaymentEvaluationEarlyFraudWarningReceived option
        ///Timestamp when the event occurred.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]OccurredAt: DateTime
        Refunded: InsightsResourcesPaymentEvaluationRefunded option
        ///Indicates the type of event attached to the payment evaluation.
        Type: InsightsResourcesPaymentEvaluationEventType
        UserInterventionRaised: InsightsResourcesPaymentEvaluationUserInterventionRaised option
        UserInterventionResolved: InsightsResourcesPaymentEvaluationUserInterventionResolved option
    }
    with
        static member New (occurredAt: DateTime, ``type``: InsightsResourcesPaymentEvaluationEventType, ?disputeOpened: InsightsResourcesPaymentEvaluationDisputeOpened, ?earlyFraudWarningReceived: InsightsResourcesPaymentEvaluationEarlyFraudWarningReceived, ?refunded: InsightsResourcesPaymentEvaluationRefunded, ?userInterventionRaised: InsightsResourcesPaymentEvaluationUserInterventionRaised, ?userInterventionResolved: InsightsResourcesPaymentEvaluationUserInterventionResolved) =
            {
                InsightsResourcesPaymentEvaluationEvent.OccurredAt = occurredAt //required
                InsightsResourcesPaymentEvaluationEvent.Type = ``type`` //required
                InsightsResourcesPaymentEvaluationEvent.DisputeOpened = disputeOpened
                InsightsResourcesPaymentEvaluationEvent.EarlyFraudWarningReceived = earlyFraudWarningReceived
                InsightsResourcesPaymentEvaluationEvent.Refunded = refunded
                InsightsResourcesPaymentEvaluationEvent.UserInterventionRaised = userInterventionRaised
                InsightsResourcesPaymentEvaluationEvent.UserInterventionResolved = userInterventionResolved
            }

    ///Details of a merchant_blocked outcome attached to this payment evaluation.
    and InsightsResourcesPaymentEvaluationMerchantBlocked = {
        ///The reason the payment was blocked by the merchant.
        Reason: InsightsResourcesPaymentEvaluationMerchantBlockedReason
    }
    with
        static member New (reason: InsightsResourcesPaymentEvaluationMerchantBlockedReason) =
            {
                InsightsResourcesPaymentEvaluationMerchantBlocked.Reason = reason //required
            }

    ///Money Movement card details attached to this payment.
    and InsightsResourcesPaymentEvaluationMoneyMovementCard = {
        ///Describes the presence of the customer during the payment.
        CustomerPresence: InsightsResourcesPaymentEvaluationMoneyMovementCardCustomerPresence option
        ///Describes the type of payment.
        PaymentType: InsightsResourcesPaymentEvaluationMoneyMovementCardPaymentType option
    }
    with
        static member New (customerPresence: InsightsResourcesPaymentEvaluationMoneyMovementCardCustomerPresence option, paymentType: InsightsResourcesPaymentEvaluationMoneyMovementCardPaymentType option) =
            {
                InsightsResourcesPaymentEvaluationMoneyMovementCard.CustomerPresence = customerPresence //required
                InsightsResourcesPaymentEvaluationMoneyMovementCard.PaymentType = paymentType //required
            }

    ///Money Movement details attached to this payment.
    and InsightsResourcesPaymentEvaluationMoneyMovementDetails = {
        ///Describes card money movement details for the payment evaluation.
        Card: InsightsResourcesPaymentEvaluationMoneyMovementCard option
    }
    with
        ///Describes the type of money movement. Currently only `card` is supported.
        member _.MoneyMovementType = "card"

        static member New (card: InsightsResourcesPaymentEvaluationMoneyMovementCard option) =
            {
                InsightsResourcesPaymentEvaluationMoneyMovementDetails.Card = card //required
            }

    ///Outcome details for this payment evaluation.
    and InsightsResourcesPaymentEvaluationOutcome = {
        MerchantBlocked: InsightsResourcesPaymentEvaluationMerchantBlocked option
        ///The PaymentIntent ID associated with the payment evaluation.
        PaymentIntentId: string option
        Rejected: InsightsResourcesPaymentEvaluationRejected option
        Succeeded: InsightsResourcesPaymentEvaluationSucceeded option
        ///Indicates the outcome of the payment evaluation.
        Type: InsightsResourcesPaymentEvaluationOutcomeType
    }
    with
        static member New (``type``: InsightsResourcesPaymentEvaluationOutcomeType, ?merchantBlocked: InsightsResourcesPaymentEvaluationMerchantBlocked, ?paymentIntentId: string, ?rejected: InsightsResourcesPaymentEvaluationRejected, ?succeeded: InsightsResourcesPaymentEvaluationSucceeded) =
            {
                InsightsResourcesPaymentEvaluationOutcome.Type = ``type`` //required
                InsightsResourcesPaymentEvaluationOutcome.MerchantBlocked = merchantBlocked
                InsightsResourcesPaymentEvaluationOutcome.PaymentIntentId = paymentIntentId
                InsightsResourcesPaymentEvaluationOutcome.Rejected = rejected
                InsightsResourcesPaymentEvaluationOutcome.Succeeded = succeeded
            }

    ///Payment details attached to this payment evaluation.
    and InsightsResourcesPaymentEvaluationPaymentDetails = {
        ///Amount intended to be collected by this payment. A positive integer representing how much to charge in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal) (e.g., 100 cents to charge $1.00 or 100 to charge ¥100, a zero-decimal currency). The minimum amount is $0.50 US or [equivalent in charge currency](https://docs.stripe.com/currencies#minimum-and-maximum-charge-amounts). The amount value supports up to eight digits (e.g., a value of 99999999 for a USD charge of $999,999.99).
        Amount: int
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///Details about the payment's customer presence and type.
        MoneyMovementDetails: InsightsResourcesPaymentEvaluationMoneyMovementDetails option
        ///Details about the payment method used for the payment.
        PaymentMethodDetails: InsightsResourcesPaymentEvaluationPaymentMethodDetails option
        ///Shipping details for the payment evaluation.
        ShippingDetails: InsightsResourcesPaymentEvaluationShipping option
        ///Payment statement descriptor.
        StatementDescriptor: string option
    }
    with
        static member New (amount: int, currency: IsoTypes.IsoCurrencyCode, description: string option, moneyMovementDetails: InsightsResourcesPaymentEvaluationMoneyMovementDetails option, paymentMethodDetails: InsightsResourcesPaymentEvaluationPaymentMethodDetails option, shippingDetails: InsightsResourcesPaymentEvaluationShipping option, statementDescriptor: string option) =
            {
                InsightsResourcesPaymentEvaluationPaymentDetails.Amount = amount //required
                InsightsResourcesPaymentEvaluationPaymentDetails.Currency = currency //required
                InsightsResourcesPaymentEvaluationPaymentDetails.Description = description //required
                InsightsResourcesPaymentEvaluationPaymentDetails.MoneyMovementDetails = moneyMovementDetails //required
                InsightsResourcesPaymentEvaluationPaymentDetails.PaymentMethodDetails = paymentMethodDetails //required
                InsightsResourcesPaymentEvaluationPaymentDetails.ShippingDetails = shippingDetails //required
                InsightsResourcesPaymentEvaluationPaymentDetails.StatementDescriptor = statementDescriptor //required
            }

    ///Payment method details attached to this payment evaluation.
    and InsightsResourcesPaymentEvaluationPaymentMethodDetails = {
        ///Billing information associated with the payment evaluation.
        BillingDetails: InsightsResourcesPaymentEvaluationBillingDetails option
        ///The payment method used in this payment evaluation.
        PaymentMethod: InsightsResourcesPaymentEvaluationPaymentMethodDetailsPaymentMethod'AnyOf
    }
    with
        static member New (billingDetails: InsightsResourcesPaymentEvaluationBillingDetails option, paymentMethod: InsightsResourcesPaymentEvaluationPaymentMethodDetailsPaymentMethod'AnyOf) =
            {
                InsightsResourcesPaymentEvaluationPaymentMethodDetails.BillingDetails = billingDetails //required
                InsightsResourcesPaymentEvaluationPaymentMethodDetails.PaymentMethod = paymentMethod //required
            }

    and InsightsResourcesPaymentEvaluationPaymentMethodDetailsPaymentMethod'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    ///Refunded Event details attached to this payment evaluation.
    and InsightsResourcesPaymentEvaluationRefunded = {
        ///Amount refunded for this payment. A positive integer representing how much to charge in [the smallest currency unit](https://docs.stripe.com/currencies#zero-decimal) (for example, 100 cents to charge 1.00 USD or 100 to charge 100 Yen, a zero-decimal currency).
        Amount: int
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///Indicates the reason for the refund.
        Reason: InsightsResourcesPaymentEvaluationRefundedReason
    }
    with
        static member New (amount: int, currency: IsoTypes.IsoCurrencyCode, reason: InsightsResourcesPaymentEvaluationRefundedReason) =
            {
                InsightsResourcesPaymentEvaluationRefunded.Amount = amount //required
                InsightsResourcesPaymentEvaluationRefunded.Currency = currency //required
                InsightsResourcesPaymentEvaluationRefunded.Reason = reason //required
            }

    ///Details of an rejected outcome attached to this payment evaluation.
    and InsightsResourcesPaymentEvaluationRejected = {
        Card: InsightsResourcesPaymentEvaluationRejectedCard option
    }
    with
        static member New (?card: InsightsResourcesPaymentEvaluationRejectedCard) =
            {
                InsightsResourcesPaymentEvaluationRejected.Card = card
            }

    ///Details of an rejected card outcome attached to this payment evaluation.
    and InsightsResourcesPaymentEvaluationRejectedCard = {
        ///Result of the address line 1 check.
        [<JsonField(Name="address_line1_check")>]AddressLine1Check: InsightsResourcesPaymentEvaluationRejectedCardAddressLine1Check
        ///Indicates whether the cardholder provided a postal code and if it matched the cardholder’s billing address.
        AddressPostalCodeCheck: InsightsResourcesPaymentEvaluationRejectedCardAddressPostalCodeCheck
        ///Result of the CVC check.
        CvcCheck: InsightsResourcesPaymentEvaluationRejectedCardCvcCheck
        ///Card issuer's reason for the network decline.
        Reason: InsightsResourcesPaymentEvaluationRejectedCardReason
    }
    with
        static member New (addressLine1Check: InsightsResourcesPaymentEvaluationRejectedCardAddressLine1Check, addressPostalCodeCheck: InsightsResourcesPaymentEvaluationRejectedCardAddressPostalCodeCheck, cvcCheck: InsightsResourcesPaymentEvaluationRejectedCardCvcCheck, reason: InsightsResourcesPaymentEvaluationRejectedCardReason) =
            {
                InsightsResourcesPaymentEvaluationRejectedCard.AddressLine1Check = addressLine1Check //required
                InsightsResourcesPaymentEvaluationRejectedCard.AddressPostalCodeCheck = addressPostalCodeCheck //required
                InsightsResourcesPaymentEvaluationRejectedCard.CvcCheck = cvcCheck //required
                InsightsResourcesPaymentEvaluationRejectedCard.Reason = reason //required
            }

    ///Shipping details attached to this payment.
    and InsightsResourcesPaymentEvaluationShipping = {
        Address: InsightsResourcesPaymentEvaluationAddress
        ///Shipping name.
        Name: string option
        ///Shipping phone number.
        Phone: string option
    }
    with
        static member New (address: InsightsResourcesPaymentEvaluationAddress, name: string option, phone: string option) =
            {
                InsightsResourcesPaymentEvaluationShipping.Address = address //required
                InsightsResourcesPaymentEvaluationShipping.Name = name //required
                InsightsResourcesPaymentEvaluationShipping.Phone = phone //required
            }

    ///A payment evaluation signal with evaluated_at, risk_level, and score fields.
    and InsightsResourcesPaymentEvaluationSignalV2 = {
        ///The time when this signal was evaluated.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]EvaluatedAt: DateTime
        ///Risk level of this signal, based on the score.
        RiskLevel: InsightsResourcesPaymentEvaluationSignalV2RiskLevel
        ///Score for this signal. Possible values for evaluated payments are between 0 and 100. The value is returned with two decimal places and higher scores indicate a higher likelihood of the signal being true. A score of -1 is returned when a model evaluation was not performed, such as requests from incomplete integrations.
        Score: decimal
    }
    with
        static member New (evaluatedAt: DateTime, riskLevel: InsightsResourcesPaymentEvaluationSignalV2RiskLevel, score: decimal) =
            {
                InsightsResourcesPaymentEvaluationSignalV2.EvaluatedAt = evaluatedAt //required
                InsightsResourcesPaymentEvaluationSignalV2.RiskLevel = riskLevel //required
                InsightsResourcesPaymentEvaluationSignalV2.Score = score //required
            }

    ///Collection of signals for this payment evaluation.
    and InsightsResourcesPaymentEvaluationSignals = {
        FraudulentPayment: InsightsResourcesPaymentEvaluationSignalV2
    }
    with
        static member New (fraudulentPayment: InsightsResourcesPaymentEvaluationSignalV2) =
            {
                InsightsResourcesPaymentEvaluationSignals.FraudulentPayment = fraudulentPayment //required
            }

    ///Details of a succeeded outcome attached to this payment evaluation.
    and InsightsResourcesPaymentEvaluationSucceeded = {
        Card: InsightsResourcesPaymentEvaluationSucceededCard option
    }
    with
        static member New (?card: InsightsResourcesPaymentEvaluationSucceededCard) =
            {
                InsightsResourcesPaymentEvaluationSucceeded.Card = card
            }

    ///Details of an succeeded card outcome attached to this payment evaluation.
    and InsightsResourcesPaymentEvaluationSucceededCard = {
        ///Result of the address line 1 check.
        [<JsonField(Name="address_line1_check")>]AddressLine1Check: InsightsResourcesPaymentEvaluationSucceededCardAddressLine1Check
        ///Indicates whether the cardholder provided a postal code and if it matched the cardholder’s billing address.
        AddressPostalCodeCheck: InsightsResourcesPaymentEvaluationSucceededCardAddressPostalCodeCheck
        ///Result of the CVC check.
        CvcCheck: InsightsResourcesPaymentEvaluationSucceededCardCvcCheck
    }
    with
        static member New (addressLine1Check: InsightsResourcesPaymentEvaluationSucceededCardAddressLine1Check, addressPostalCodeCheck: InsightsResourcesPaymentEvaluationSucceededCardAddressPostalCodeCheck, cvcCheck: InsightsResourcesPaymentEvaluationSucceededCardCvcCheck) =
            {
                InsightsResourcesPaymentEvaluationSucceededCard.AddressLine1Check = addressLine1Check //required
                InsightsResourcesPaymentEvaluationSucceededCard.AddressPostalCodeCheck = addressPostalCodeCheck //required
                InsightsResourcesPaymentEvaluationSucceededCard.CvcCheck = cvcCheck //required
            }

    ///User intervention raised event details attached to this payment evaluation
    and InsightsResourcesPaymentEvaluationUserInterventionRaised = {
        Custom: InsightsResourcesPaymentEvaluationUserInterventionRaisedCustom option
        ///Unique identifier for the user intervention event.
        Key: string
        ///Type of user intervention raised.
        Type: InsightsResourcesPaymentEvaluationUserInterventionRaisedType
    }
    with
        static member New (key: string, ``type``: InsightsResourcesPaymentEvaluationUserInterventionRaisedType, ?custom: InsightsResourcesPaymentEvaluationUserInterventionRaisedCustom) =
            {
                InsightsResourcesPaymentEvaluationUserInterventionRaised.Key = key //required
                InsightsResourcesPaymentEvaluationUserInterventionRaised.Type = ``type`` //required
                InsightsResourcesPaymentEvaluationUserInterventionRaised.Custom = custom
            }

    ///User Intervention Resolved Event details attached to this payment evaluation
    and InsightsResourcesPaymentEvaluationUserInterventionResolved = {
        ///Unique ID of this intervention. Use this to provide the result.
        Key: string
        ///Result of the intervention if it has been completed.
        Outcome: InsightsResourcesPaymentEvaluationUserInterventionResolvedOutcome option
    }
    with
        static member New (key: string, outcome: InsightsResourcesPaymentEvaluationUserInterventionResolvedOutcome option) =
            {
                InsightsResourcesPaymentEvaluationUserInterventionResolved.Key = key //required
                InsightsResourcesPaymentEvaluationUserInterventionResolved.Outcome = outcome //required
            }

    and InternalCard = {
        ///Brand of the card used in the transaction
        Brand: string option
        ///Two-letter ISO code representing the country of the card
        Country: IsoTypes.IsoCountryCode option
        ///Two digit number representing the card's expiration month
        ExpMonth: int option
        ///Two digit number representing the card's expiration year
        ExpYear: int option
        ///The last 4 digits of the card
        [<JsonField(Name="last4")>]Last4: string option
    }
    with
        static member New (brand: string option, country: IsoTypes.IsoCountryCode option, expMonth: int option, expYear: int option, last4: string option) =
            {
                InternalCard.Brand = brand //required
                InternalCard.Country = country //required
                InternalCard.ExpMonth = expMonth //required
                InternalCard.ExpYear = expYear //required
                InternalCard.Last4 = last4 //required
            }

    ///Invoices are statements of amounts owed by a customer, and are either
    ///generated one-off, or generated periodically from a subscription.
    ///They contain [invoice items](https://api.stripe.com#invoiceitems), and proration adjustments
    ///that may be caused by subscription upgrades/downgrades (if necessary).
    ///If your invoice is configured to be billed through automatic charges,
    ///Stripe automatically finalizes your invoice and attempts payment. Note
    ///that finalizing the invoice,
    ///[when automatic](https://docs.stripe.com/invoicing/integration/automatic-advancement-collection), does
    ///not happen immediately as the invoice is created. Stripe waits
    ///until one hour after the last webhook was successfully sent (or the last
    ///webhook timed out after failing). If you (and the platforms you may have
    ///connected to) have no webhooks configured, Stripe waits one hour after
    ///creation to finalize the invoice.
    ///If your invoice is configured to be billed by sending an email, then based on your
    ///[email settings](https://dashboard.stripe.com/account/billing/automatic),
    ///Stripe will email the invoice to your customer and await payment. These
    ///emails can contain a link to a hosted page to pay the invoice.
    ///Stripe applies any customer credit on the account before determining the
    ///amount due for the invoice (i.e., the amount that will be actually
    ///charged). If the amount due for the invoice is less than Stripe's [minimum allowed charge
    ///per currency](/docs/currencies#minimum-and-maximum-charge-amounts), the
    ///invoice is automatically marked paid, and we add the amount due to the
    ///customer's credit balance which is applied to the next invoice.
    ///More details on the customer's credit balance are
    ///[here](https://docs.stripe.com/billing/customer/balance).
    ///Related guide: [Send invoices to customers](https://docs.stripe.com/billing/invoices/sending)
    and Invoice = {
        ///The country of the business associated with this invoice, most often the business creating the invoice.
        AccountCountry: IsoTypes.IsoCountryCode option
        ///The public name of the business associated with this invoice, most often the business creating the invoice.
        AccountName: string option
        ///The account tax IDs associated with the invoice. Only editable when the invoice is a draft.
        AccountTaxIds: InvoiceAccountTaxIds'AnyOf list option
        ///Final amount due at this time for this invoice. If the invoice's total is smaller than the minimum charge amount, for example, or if there is account credit that can be applied to the invoice, the `amount_due` may be 0. If there is a positive `starting_balance` for the invoice (the customer owes money), the `amount_due` will also take that into account. The charge that gets generated for the invoice will be for the amount specified in `amount_due`.
        AmountDue: int
        ///Amount that was overpaid on the invoice. The amount overpaid is credited to the customer's credit balance.
        AmountOverpaid: int
        ///The amount, in cents (or local equivalent), that was paid.
        AmountPaid: int
        ///The difference between amount_due and amount_paid, in cents (or local equivalent).
        AmountRemaining: int
        ///This is the sum of all the shipping amounts.
        AmountShipping: int
        ///ID of the Connect Application that created the invoice.
        Application: InvoiceApplication'AnyOf option
        ///Number of payment attempts made for this invoice, from the perspective of the payment retry schedule. Any payment attempt counts as the first attempt, and subsequently only automatic retries increment the attempt count. In other words, manual payment attempts after the first attempt do not affect the retry schedule. If a failure is returned with a non-retryable return code, the invoice can no longer be retried unless a new payment method is obtained. Retries will continue to be scheduled, and attempt_count will continue to increment, but retries will only be executed if a new payment method is obtained.
        AttemptCount: int
        ///Whether an attempt has been made to pay the invoice. An invoice is not attempted until 1 hour after the `invoice.created` webhook, for example, so you might not want to display that invoice as unpaid to your users.
        Attempted: bool
        ///Controls whether Stripe performs [automatic collection](https://docs.stripe.com/invoicing/integration/automatic-advancement-collection) of the invoice. If `false`, the invoice's state doesn't automatically advance without an explicit action.
        AutoAdvance: bool option
        AutomaticTax: AutomaticTax
        ///The time when this invoice is currently scheduled to be automatically finalized. The field will be `null` if the invoice is not scheduled to finalize in the future. If the invoice is not in the draft state, this field will always be `null` - see `finalized_at` for the time when an already-finalized invoice was finalized.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]AutomaticallyFinalizesAt: DateTime option
        ///Indicates the reason why the invoice was created.
        ///* `manual`: Unrelated to a subscription, for example, created via the invoice editor.
        ///* `subscription`: No longer in use. Applies to subscriptions from before May 2018 where no distinction was made between updates, cycles, and thresholds.
        ///* `subscription_create`: A new subscription was created.
        ///* `subscription_cycle`: A subscription advanced into a new period.
        ///* `subscription_threshold`: A subscription reached a billing threshold.
        ///* `subscription_update`: A subscription was updated.
        ///* `upcoming`: Reserved for upcoming invoices created through the Create Preview Invoice API or when an `invoice.upcoming` event is generated for an upcoming invoice on a subscription.
        BillingReason: InvoiceBillingReason option
        ///Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this invoice using the default source attached to the customer. When sending an invoice, Stripe will email this invoice to the customer with payment instructions.
        CollectionMethod: InvoiceCollectionMethod
        ///The confirmation secret associated with this invoice. Currently, this contains the client_secret of the PaymentIntent that Stripe creates during invoice finalization.
        ConfirmationSecret: InvoicesResourceConfirmationSecret option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///Custom fields displayed on the invoice.
        CustomFields: InvoiceSettingCustomField list option
        ///The ID of the customer to bill.
        Customer: InvoiceCustomer'AnyOf option
        ///The ID of the account representing the customer to bill.
        CustomerAccount: string option
        ///The customer's address. Until the invoice is finalized, this field will equal `customer.address`. Once the invoice is finalized, this field will no longer be updated.
        CustomerAddress: Address option
        ///The customer's email. Until the invoice is finalized, this field will equal `customer.email`. Once the invoice is finalized, this field will no longer be updated.
        CustomerEmail: string option
        ///The customer's name. Until the invoice is finalized, this field will equal `customer.name`. Once the invoice is finalized, this field will no longer be updated.
        CustomerName: string option
        ///The customer's phone number. Until the invoice is finalized, this field will equal `customer.phone`. Once the invoice is finalized, this field will no longer be updated.
        CustomerPhone: string option
        ///The customer's shipping information. Until the invoice is finalized, this field will equal `customer.shipping`. Once the invoice is finalized, this field will no longer be updated.
        CustomerShipping: Shipping option
        ///The customer's tax exempt status. Until the invoice is finalized, this field will equal `customer.tax_exempt`. Once the invoice is finalized, this field will no longer be updated.
        CustomerTaxExempt: InvoiceCustomerTaxExempt option
        ///The customer's tax IDs. Until the invoice is finalized, this field will contain the same tax IDs as `customer.tax_ids`. Once the invoice is finalized, this field will no longer be updated.
        CustomerTaxIds: InvoicesResourceInvoiceTaxId list option
        ///ID of the default payment method for the invoice. It must belong to the customer associated with the invoice. If not set, defaults to the subscription's default payment method, if any, or to the default payment method in the customer's invoice settings.
        DefaultPaymentMethod: InvoiceDefaultPaymentMethod'AnyOf option
        ///ID of the default payment source for the invoice. It must belong to the customer associated with the invoice and be in a chargeable state. If not set, defaults to the subscription's default source, if any, or to the customer's default source.
        DefaultSource: InvoiceDefaultSource'AnyOf option
        ///The tax rates applied to this invoice, if any.
        DefaultTaxRates: TaxRate list
        ///An arbitrary string attached to the object. Often useful for displaying to users. Referenced as 'memo' in the Dashboard.
        Description: string option
        ///The discounts applied to the invoice. Line item discounts are applied before invoice discounts. Use `expand[]=discounts` to expand each discount.
        Discounts: InvoiceDiscounts'AnyOf list
        ///The date on which payment for this invoice is due. This value will be `null` for invoices where `collection_method=charge_automatically`.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]DueDate: DateTime option
        ///The date when this invoice is in effect. Same as `finalized_at` unless overwritten. When defined, this value replaces the system-generated 'Date of issue' printed on the invoice PDF and receipt.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]EffectiveAt: DateTime option
        ///Ending customer balance after the invoice is finalized. Invoices are finalized approximately an hour after successful webhook delivery or when payment collection is attempted for the invoice. If the invoice has not been finalized yet, this will be null.
        EndingBalance: int option
        ///Footer displayed on the invoice.
        Footer: string option
        ///Details of the invoice that was cloned. See the [revision documentation](https://docs.stripe.com/invoicing/invoice-revisions) for more details.
        FromInvoice: InvoicesResourceFromInvoice option
        ///The URL for the hosted invoice page, which allows customers to view and pay an invoice. If the invoice has not been finalized yet, this will be null.
        HostedInvoiceUrl: string option
        ///Unique identifier for the object. For preview invoices created using the [create preview](https://stripe.com/docs/api/invoices/create_preview) endpoint, this id will be prefixed with `upcoming_in`.
        Id: string option
        ///The link to download the PDF for the invoice. If the invoice has not been finalized yet, this will be null.
        InvoicePdf: string option
        Issuer: ConnectAccountReference
        ///The error encountered during the previous attempt to finalize the invoice. This field is cleared when the invoice is successfully finalized.
        LastFinalizationError: ApiErrors option
        ///The ID of the most recent non-draft revision of this invoice
        LatestRevision: InvoiceLatestRevision'AnyOf option
        ///The individual line items that make up the invoice. `lines` is sorted as follows: (1) pending invoice items (including prorations) in reverse chronological order, (2) subscription items in reverse chronological order, and (3) invoice items added after invoice creation in chronological order.
        Lines: InvoiceLines
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///The time at which payment will next be attempted. This value will be `null` for invoices where `collection_method=send_invoice`.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]NextPaymentAttempt: DateTime option
        ///A unique, identifying string that appears on emails sent to the customer for this invoice. This starts with the customer's unique invoice_prefix if it is specified.
        Number: string option
        ///The account (if any) for which the funds of the invoice payment are intended. If set, the invoice will be presented with the branding and support information of the specified account. See the [Invoices with Connect](https://docs.stripe.com/billing/invoices/connect) documentation for details.
        OnBehalfOf: InvoiceOnBehalfOf'AnyOf option
        ///The parent that generated this invoice
        Parent: BillingBillResourceInvoicingParentsInvoiceParent option
        PaymentSettings: InvoicesPaymentSettings
        ///Payments for this invoice. Use [invoice payment](/api/invoice-payment) to get more details.
        Payments: InvoicePayments option
        ///The latest timestamp at which invoice items can be associated with this invoice. Use the [line item period](/api/invoices/line_item#invoice_line_item_object-period) to get the service period for each price.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]PeriodEnd: DateTime
        ///The earliest timestamp at which invoice items can be associated with this invoice. Use the [line item period](/api/invoices/line_item#invoice_line_item_object-period) to get the service period for each price.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]PeriodStart: DateTime
        ///Total amount of all post-payment credit notes issued for this invoice.
        PostPaymentCreditNotesAmount: int
        ///Total amount of all pre-payment credit notes issued for this invoice.
        PrePaymentCreditNotesAmount: int
        ///This is the transaction number that appears on email receipts sent for this invoice.
        ReceiptNumber: string option
        ///The rendering-related settings that control how the invoice is displayed on customer-facing surfaces such as PDF and Hosted Invoice Page.
        Rendering: InvoicesResourceInvoiceRendering option
        ///The details of the cost of shipping, including the ShippingRate applied on the invoice.
        ShippingCost: InvoicesResourceShippingCost option
        ///Shipping details for the invoice. The Invoice PDF will use the `shipping_details` value if it is set, otherwise the PDF will render the shipping address from the customer.
        ShippingDetails: Shipping option
        ///Starting customer balance before the invoice is finalized. If the invoice has not been finalized yet, this will be the current customer balance. For revision invoices, this also includes any customer balance that was applied to the original invoice.
        StartingBalance: int
        ///Extra information about an invoice for the customer's credit card statement.
        StatementDescriptor: string option
        ///The status of the invoice, one of `draft`, `open`, `paid`, `uncollectible`, or `void`. [Learn more](https://docs.stripe.com/billing/invoices/workflow#workflow-overview)
        Status: InvoiceStatus option
        StatusTransitions: InvoicesResourceStatusTransitions
        Subscription: InvoiceSubscription'AnyOf option
        ///Total of all subscriptions, invoice items, and prorations on the invoice before any invoice level discount or exclusive tax is applied. Item discounts are already incorporated
        Subtotal: int
        ///The integer amount in cents (or local equivalent) representing the subtotal of the invoice before any invoice level discount or tax is applied. Item discounts are already incorporated
        SubtotalExcludingTax: int option
        ///ID of the test clock this invoice belongs to.
        TestClock: InvoiceTestClock'AnyOf option
        ThresholdReason: InvoiceThresholdReason option
        ///Total after discounts and taxes.
        Total: int
        ///The aggregate amounts calculated per discount across all line items.
        TotalDiscountAmounts: DiscountsResourceDiscountAmount list option
        ///The integer amount in cents (or local equivalent) representing the total amount of the invoice including all discounts but excluding all tax.
        TotalExcludingTax: int option
        ///Contains pretax credit amounts (ex: discount, credit grants, etc) that apply to this invoice. This is a combined list of total_pretax_credit_amounts across all invoice line items.
        TotalPretaxCreditAmounts: InvoicesResourcePretaxCreditAmount list option
        ///The aggregate tax information of all line items.
        TotalTaxes: BillingBillResourceInvoicingTaxesTax list option
        ///Invoices are automatically paid or sent 1 hour after webhooks are delivered, or until all webhook delivery attempts have [been exhausted](https://docs.stripe.com/billing/webhooks#understand). This field tracks the time when webhooks for this invoice were successfully delivered. If the invoice had no webhooks to deliver, this will be set while the invoice is being created.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]WebhooksDeliveredAt: DateTime option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "invoice"

        static member New (accountCountry: IsoTypes.IsoCountryCode option, accountName: string option, accountTaxIds: InvoiceAccountTaxIds'AnyOf list option, amountDue: int, amountOverpaid: int, amountPaid: int, amountRemaining: int, amountShipping: int, application: InvoiceApplication'AnyOf option, attemptCount: int, attempted: bool, automaticTax: AutomaticTax, automaticallyFinalizesAt: DateTime option, billingReason: InvoiceBillingReason option, collectionMethod: InvoiceCollectionMethod, created: DateTime, currency: IsoTypes.IsoCurrencyCode, customFields: InvoiceSettingCustomField list option, customer: InvoiceCustomer'AnyOf option, customerAccount: string option, customerAddress: Address option, customerEmail: string option, customerName: string option, customerPhone: string option, customerShipping: Shipping option, customerTaxExempt: InvoiceCustomerTaxExempt option, defaultPaymentMethod: InvoiceDefaultPaymentMethod'AnyOf option, defaultSource: InvoiceDefaultSource'AnyOf option, defaultTaxRates: TaxRate list, description: string option, discounts: InvoiceDiscounts'AnyOf list, dueDate: DateTime option, effectiveAt: DateTime option, endingBalance: int option, footer: string option, fromInvoice: InvoicesResourceFromInvoice option, issuer: ConnectAccountReference, lastFinalizationError: ApiErrors option, latestRevision: InvoiceLatestRevision'AnyOf option, lines: InvoiceLines, livemode: bool, metadata: Map<string, string> option, nextPaymentAttempt: DateTime option, number: string option, onBehalfOf: InvoiceOnBehalfOf'AnyOf option, parent: BillingBillResourceInvoicingParentsInvoiceParent option, paymentSettings: InvoicesPaymentSettings, periodEnd: DateTime, periodStart: DateTime, postPaymentCreditNotesAmount: int, prePaymentCreditNotesAmount: int, receiptNumber: string option, rendering: InvoicesResourceInvoiceRendering option, shippingCost: InvoicesResourceShippingCost option, shippingDetails: Shipping option, startingBalance: int, statementDescriptor: string option, status: InvoiceStatus option, statusTransitions: InvoicesResourceStatusTransitions, subtotal: int, subtotalExcludingTax: int option, testClock: InvoiceTestClock'AnyOf option, total: int, totalDiscountAmounts: DiscountsResourceDiscountAmount list option, totalExcludingTax: int option, totalPretaxCreditAmounts: InvoicesResourcePretaxCreditAmount list option, totalTaxes: BillingBillResourceInvoicingTaxesTax list option, webhooksDeliveredAt: DateTime option, ?autoAdvance: bool, ?confirmationSecret: InvoicesResourceConfirmationSecret option, ?customerTaxIds: InvoicesResourceInvoiceTaxId list option, ?hostedInvoiceUrl: string option, ?id: string, ?invoicePdf: string option, ?payments: InvoicePayments, ?subscription: InvoiceSubscription'AnyOf option, ?thresholdReason: InvoiceThresholdReason) =
            {
                Invoice.AccountCountry = accountCountry //required
                Invoice.AccountName = accountName //required
                Invoice.AccountTaxIds = accountTaxIds //required
                Invoice.AmountDue = amountDue //required
                Invoice.AmountOverpaid = amountOverpaid //required
                Invoice.AmountPaid = amountPaid //required
                Invoice.AmountRemaining = amountRemaining //required
                Invoice.AmountShipping = amountShipping //required
                Invoice.Application = application //required
                Invoice.AttemptCount = attemptCount //required
                Invoice.Attempted = attempted //required
                Invoice.AutomaticTax = automaticTax //required
                Invoice.AutomaticallyFinalizesAt = automaticallyFinalizesAt //required
                Invoice.BillingReason = billingReason //required
                Invoice.CollectionMethod = collectionMethod //required
                Invoice.Created = created //required
                Invoice.Currency = currency //required
                Invoice.CustomFields = customFields //required
                Invoice.Customer = customer //required
                Invoice.CustomerAccount = customerAccount //required
                Invoice.CustomerAddress = customerAddress //required
                Invoice.CustomerEmail = customerEmail //required
                Invoice.CustomerName = customerName //required
                Invoice.CustomerPhone = customerPhone //required
                Invoice.CustomerShipping = customerShipping //required
                Invoice.CustomerTaxExempt = customerTaxExempt //required
                Invoice.DefaultPaymentMethod = defaultPaymentMethod //required
                Invoice.DefaultSource = defaultSource //required
                Invoice.DefaultTaxRates = defaultTaxRates //required
                Invoice.Description = description //required
                Invoice.Discounts = discounts //required
                Invoice.DueDate = dueDate //required
                Invoice.EffectiveAt = effectiveAt //required
                Invoice.EndingBalance = endingBalance //required
                Invoice.Footer = footer //required
                Invoice.FromInvoice = fromInvoice //required
                Invoice.Issuer = issuer //required
                Invoice.LastFinalizationError = lastFinalizationError //required
                Invoice.LatestRevision = latestRevision //required
                Invoice.Lines = lines //required
                Invoice.Livemode = livemode //required
                Invoice.Metadata = metadata //required
                Invoice.NextPaymentAttempt = nextPaymentAttempt //required
                Invoice.Number = number //required
                Invoice.OnBehalfOf = onBehalfOf //required
                Invoice.Parent = parent //required
                Invoice.PaymentSettings = paymentSettings //required
                Invoice.PeriodEnd = periodEnd //required
                Invoice.PeriodStart = periodStart //required
                Invoice.PostPaymentCreditNotesAmount = postPaymentCreditNotesAmount //required
                Invoice.PrePaymentCreditNotesAmount = prePaymentCreditNotesAmount //required
                Invoice.ReceiptNumber = receiptNumber //required
                Invoice.Rendering = rendering //required
                Invoice.ShippingCost = shippingCost //required
                Invoice.ShippingDetails = shippingDetails //required
                Invoice.StartingBalance = startingBalance //required
                Invoice.StatementDescriptor = statementDescriptor //required
                Invoice.Status = status //required
                Invoice.StatusTransitions = statusTransitions //required
                Invoice.Subtotal = subtotal //required
                Invoice.SubtotalExcludingTax = subtotalExcludingTax //required
                Invoice.TestClock = testClock //required
                Invoice.Total = total //required
                Invoice.TotalDiscountAmounts = totalDiscountAmounts //required
                Invoice.TotalExcludingTax = totalExcludingTax //required
                Invoice.TotalPretaxCreditAmounts = totalPretaxCreditAmounts //required
                Invoice.TotalTaxes = totalTaxes //required
                Invoice.WebhooksDeliveredAt = webhooksDeliveredAt //required
                Invoice.AutoAdvance = autoAdvance
                Invoice.ConfirmationSecret = confirmationSecret |> Option.flatten
                Invoice.CustomerTaxIds = customerTaxIds |> Option.flatten
                Invoice.HostedInvoiceUrl = hostedInvoiceUrl |> Option.flatten
                Invoice.Id = id
                Invoice.InvoicePdf = invoicePdf |> Option.flatten
                Invoice.Payments = payments
                Invoice.Subscription = subscription |> Option.flatten
                Invoice.ThresholdReason = thresholdReason
            }

    and InvoiceApplication'AnyOf =
        | String of string
        | Application of Application
        | DeletedApplication of DeletedApplication

    and InvoiceCustomer'AnyOf =
        | String of string
        | Customer of Customer
        | DeletedCustomer of DeletedCustomer

    and InvoiceDefaultPaymentMethod'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and InvoiceDefaultSource'AnyOf =
        | String of string
        | PaymentSource of PaymentSource

    and InvoiceLatestRevision'AnyOf =
        | String of string
        | Invoice of Invoice

    and InvoiceOnBehalfOf'AnyOf =
        | String of string
        | Account of Account

    and InvoiceSubscription'AnyOf =
        | String of string
        | Subscription of Subscription

    and InvoiceTestClock'AnyOf =
        | String of string
        | TestHelpersTestClock of TestHelpersTestClock

    and InvoiceAccountTaxIds'AnyOf =
        | String of string
        | TaxId of TaxId
        | DeletedTaxId of DeletedTaxId

    and InvoiceDiscounts'AnyOf =
        | String of string
        | Discount of Discount
        | DeletedDiscount of DeletedDiscount

    ///The individual line items that make up the invoice. `lines` is sorted as follows: (1) pending invoice items (including prorations) in reverse chronological order, (2) subscription items in reverse chronological order, and (3) invoice items added after invoice creation in chronological order.
    and InvoiceLines = {
        ///Details about each object.
        Data: LineItem list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: LineItem list, hasMore: bool, url: string) =
            {
                InvoiceLines.Data = data //required
                InvoiceLines.HasMore = hasMore //required
                InvoiceLines.Url = url //required
            }

    ///Payments for this invoice. Use [invoice payment](/api/invoice-payment) to get more details.
    and InvoicePayments = {
        ///Details about each object.
        Data: InvoicePayment list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: InvoicePayment list, hasMore: bool, url: string) =
            {
                InvoicePayments.Data = data //required
                InvoicePayments.HasMore = hasMore //required
                InvoicePayments.Url = url //required
            }

    and InvoiceItemThresholdReason = {
        ///The IDs of the line items that triggered the threshold invoice.
        LineItemIds: string list
        ///The quantity threshold boundary that applied to the given line item.
        UsageGte: int
    }
    with
        static member New (lineItemIds: string list, usageGte: int) =
            {
                InvoiceItemThresholdReason.LineItemIds = lineItemIds //required
                InvoiceItemThresholdReason.UsageGte = usageGte //required
            }

    and InvoiceMandateOptionsCard = {
        ///Amount to be charged for future payments, specified in the presentment currency.
        Amount: int option
        ///One of `fixed` or `maximum`. If `fixed`, the `amount` param refers to the exact amount to be charged in future payments. If `maximum`, the amount charged can be up to the value passed for the `amount` param.
        AmountType: InvoiceMandateOptionsCardAmountType option
        ///A description of the mandate or subscription that is meant to be displayed to the customer.
        Description: string option
    }
    with
        static member New (amount: int option, amountType: InvoiceMandateOptionsCardAmountType option, description: string option) =
            {
                InvoiceMandateOptionsCard.Amount = amount //required
                InvoiceMandateOptionsCard.AmountType = amountType //required
                InvoiceMandateOptionsCard.Description = description //required
            }

    and InvoiceMandateOptionsPayto = {
        ///The maximum amount that can be collected in a single invoice. If you don't specify a maximum, then there is no limit.
        Amount: int option
        ///Only `maximum` is supported.
        AmountType: InvoiceMandateOptionsPaytoAmountType option
        ///The purpose for which payments are made. Has a default value based on your merchant category code.
        Purpose: InvoiceMandateOptionsPaytoPurpose option
    }
    with
        static member New (amount: int option, amountType: InvoiceMandateOptionsPaytoAmountType option, purpose: InvoiceMandateOptionsPaytoPurpose option) =
            {
                InvoiceMandateOptionsPayto.Amount = amount //required
                InvoiceMandateOptionsPayto.AmountType = amountType //required
                InvoiceMandateOptionsPayto.Purpose = purpose //required
            }

    ///Invoice Payments represent payments made against invoices. Invoice Payments can
    ///be accessed in two ways:
    ///1. By expanding the `payments` field on the [Invoice](https://api.stripe.com#invoice) resource.
    ///2. By using the Invoice Payment retrieve and list endpoints.
    ///Invoice Payments include the mapping between payment objects, such as Payment Intent, and Invoices.
    ///This resource and its endpoints allows you to easily track if a payment is associated with a specific invoice and
    ///monitor the allocation details of the payments.
    and InvoicePayment = {
        ///Amount that was actually paid for this invoice, in cents (or local equivalent). This field is null until the payment is `paid`. This amount can be less than the `amount_requested` if the PaymentIntent’s `amount_received` is not sufficient to pay all of the invoices that it is attached to.
        AmountPaid: int option
        ///Amount intended to be paid toward this invoice, in cents (or local equivalent)
        AmountRequested: int
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///Unique identifier for the object.
        Id: string
        ///The invoice that was paid.
        Invoice: InvoicePaymentInvoice'AnyOf
        ///Stripe automatically creates a default InvoicePayment when the invoice is finalized, and keeps it synchronized with the invoice’s `amount_remaining`. The PaymentIntent associated with the default payment can’t be edited or canceled directly.
        IsDefault: bool
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        Payment: InvoicesPaymentsInvoicePaymentAssociatedPayment
        ///The status of the payment, one of `open`, `paid`, or `canceled`.
        Status: InvoicePaymentStatus
        StatusTransitions: InvoicesPaymentsInvoicePaymentStatusTransitions
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "invoice_payment"

        static member New (amountPaid: int option, amountRequested: int, created: DateTime, currency: IsoTypes.IsoCurrencyCode, id: string, invoice: InvoicePaymentInvoice'AnyOf, isDefault: bool, livemode: bool, payment: InvoicesPaymentsInvoicePaymentAssociatedPayment, status: InvoicePaymentStatus, statusTransitions: InvoicesPaymentsInvoicePaymentStatusTransitions) =
            {
                InvoicePayment.AmountPaid = amountPaid //required
                InvoicePayment.AmountRequested = amountRequested //required
                InvoicePayment.Created = created //required
                InvoicePayment.Currency = currency //required
                InvoicePayment.Id = id //required
                InvoicePayment.Invoice = invoice //required
                InvoicePayment.IsDefault = isDefault //required
                InvoicePayment.Livemode = livemode //required
                InvoicePayment.Payment = payment //required
                InvoicePayment.Status = status //required
                InvoicePayment.StatusTransitions = statusTransitions //required
            }

    and InvoicePaymentInvoice'AnyOf =
        | String of string
        | Invoice of Invoice
        | DeletedInvoice of DeletedInvoice

    and InvoicePaymentMethodOptionsAcssDebit = {
        MandateOptions: InvoicePaymentMethodOptionsAcssDebitMandateOptions option
        ///Bank account verification method. The default value is `automatic`.
        VerificationMethod: InvoicePaymentMethodOptionsAcssDebitVerificationMethod option
    }
    with
        static member New (?mandateOptions: InvoicePaymentMethodOptionsAcssDebitMandateOptions, ?verificationMethod: InvoicePaymentMethodOptionsAcssDebitVerificationMethod) =
            {
                InvoicePaymentMethodOptionsAcssDebit.MandateOptions = mandateOptions
                InvoicePaymentMethodOptionsAcssDebit.VerificationMethod = verificationMethod
            }

    and InvoicePaymentMethodOptionsAcssDebitMandateOptions = {
        ///Transaction type of the mandate.
        TransactionType: InvoicePaymentMethodOptionsAcssDebitMandateOptionsTransactionType option
    }
    with
        static member New (transactionType: InvoicePaymentMethodOptionsAcssDebitMandateOptionsTransactionType option) =
            {
                InvoicePaymentMethodOptionsAcssDebitMandateOptions.TransactionType = transactionType //required
            }

    and InvoicePaymentMethodOptionsBancontact = {
        ///Preferred language of the Bancontact authorization page that the customer is redirected to.
        PreferredLanguage: InvoicePaymentMethodOptionsBancontactPreferredLanguage
    }
    with
        static member New (preferredLanguage: InvoicePaymentMethodOptionsBancontactPreferredLanguage) =
            {
                InvoicePaymentMethodOptionsBancontact.PreferredLanguage = preferredLanguage //required
            }

    and InvoicePaymentMethodOptionsCard = {
        Installments: InvoiceInstallmentsCard option
        ///We strongly recommend that you rely on our SCA Engine to automatically prompt your customers for authentication based on risk level and [other requirements](https://docs.stripe.com/strong-customer-authentication). However, if you wish to request 3D Secure based on logic from your own fraud engine, provide this option. Read our guide on [manually requesting 3D Secure](https://docs.stripe.com/payments/3d-secure/authentication-flow#manual-three-ds) for more information on how this configuration interacts with Radar and our SCA Engine.
        RequestThreeDSecure: InvoicePaymentMethodOptionsCardRequestThreeDSecure option
    }
    with
        static member New (requestThreeDSecure: InvoicePaymentMethodOptionsCardRequestThreeDSecure option, ?installments: InvoiceInstallmentsCard) =
            {
                InvoicePaymentMethodOptionsCard.RequestThreeDSecure = requestThreeDSecure //required
                InvoicePaymentMethodOptionsCard.Installments = installments
            }

    and InvoicePaymentMethodOptionsCustomerBalance = {
        BankTransfer: InvoicePaymentMethodOptionsCustomerBalanceBankTransfer option
    }
    with
        ///The funding method type to be used when there are not enough funds in the customer balance. Permitted values include: `bank_transfer`.
        member _.FundingType = "bank_transfer"

        static member New (?bankTransfer: InvoicePaymentMethodOptionsCustomerBalanceBankTransfer) =
            {
                InvoicePaymentMethodOptionsCustomerBalance.BankTransfer = bankTransfer
            }

    and InvoicePaymentMethodOptionsCustomerBalanceBankTransfer = {
        EuBankTransfer: InvoicePaymentMethodOptionsCustomerBalanceBankTransferEuBankTransfer option
        ///The bank transfer type that can be used for funding. Permitted values include: `eu_bank_transfer`, `gb_bank_transfer`, `jp_bank_transfer`, `mx_bank_transfer`, or `us_bank_transfer`.
        Type: InvoicePaymentMethodOptionsCustomerBalanceBankTransferType option
    }
    with
        static member New (``type``: InvoicePaymentMethodOptionsCustomerBalanceBankTransferType option, ?euBankTransfer: InvoicePaymentMethodOptionsCustomerBalanceBankTransferEuBankTransfer) =
            {
                InvoicePaymentMethodOptionsCustomerBalanceBankTransfer.Type = ``type`` //required
                InvoicePaymentMethodOptionsCustomerBalanceBankTransfer.EuBankTransfer = euBankTransfer
            }

    and InvoicePaymentMethodOptionsCustomerBalanceBankTransferEuBankTransfer = {
        ///The desired country code of the bank account information. Permitted values include: `DE`, `FR`, `IE`, or `NL`.
        Country: InvoicePaymentMethodOptionsCustomerBalanceBankTransferEuBankTransferCountry
    }
    with
        static member New (country: InvoicePaymentMethodOptionsCustomerBalanceBankTransferEuBankTransferCountry) =
            {
                InvoicePaymentMethodOptionsCustomerBalanceBankTransferEuBankTransfer.Country = country //required
            }

    and InvoicePaymentMethodOptionsMandateOptionsUpi = {
        ///Amount to be charged for future payments.
        Amount: int option
        ///One of `fixed` or `maximum`. If `fixed`, the `amount` param refers to the exact amount to be charged in future payments. If `maximum`, the amount charged can be up to the value passed for the `amount` param.
        AmountType: InvoicePaymentMethodOptionsMandateOptionsUpiAmountType option
        ///A description of the mandate or subscription that is meant to be displayed to the customer.
        Description: string option
        ///End date of the mandate or subscription.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]EndDate: DateTime option
    }
    with
        static member New (amount: int option, amountType: InvoicePaymentMethodOptionsMandateOptionsUpiAmountType option, description: string option, endDate: DateTime option) =
            {
                InvoicePaymentMethodOptionsMandateOptionsUpi.Amount = amount //required
                InvoicePaymentMethodOptionsMandateOptionsUpi.AmountType = amountType //required
                InvoicePaymentMethodOptionsMandateOptionsUpi.Description = description //required
                InvoicePaymentMethodOptionsMandateOptionsUpi.EndDate = endDate //required
            }

    and InvoicePaymentMethodOptionsPayto = {
        MandateOptions: InvoiceMandateOptionsPayto option
    }
    with
        static member New (?mandateOptions: InvoiceMandateOptionsPayto) =
            {
                InvoicePaymentMethodOptionsPayto.MandateOptions = mandateOptions
            }

    and InvoicePaymentMethodOptionsPix = {
        ///Determines if the amount includes the IOF tax.
        AmountIncludesIof: InvoicePaymentMethodOptionsPixAmountIncludesIof option
        ///The number of seconds (between 10 and 1209600) after which Pix payment will expire. Defaults to 86400 seconds.
        ExpiresAfterSeconds: int option
    }
    with
        static member New (amountIncludesIof: InvoicePaymentMethodOptionsPixAmountIncludesIof option, ?expiresAfterSeconds: int) =
            {
                InvoicePaymentMethodOptionsPix.AmountIncludesIof = amountIncludesIof //required
                InvoicePaymentMethodOptionsPix.ExpiresAfterSeconds = expiresAfterSeconds
            }

    and InvoicePaymentMethodOptionsUpi = {
        MandateOptions: InvoicePaymentMethodOptionsMandateOptionsUpi option
    }
    with
        static member New (?mandateOptions: InvoicePaymentMethodOptionsMandateOptionsUpi) =
            {
                InvoicePaymentMethodOptionsUpi.MandateOptions = mandateOptions
            }

    and InvoicePaymentMethodOptionsUsBankAccount = {
        FinancialConnections: InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptions option
        ///Bank account verification method. The default value is `automatic`.
        VerificationMethod: InvoicePaymentMethodOptionsUsBankAccountVerificationMethod option
    }
    with
        static member New (?financialConnections: InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptions, ?verificationMethod: InvoicePaymentMethodOptionsUsBankAccountVerificationMethod) =
            {
                InvoicePaymentMethodOptionsUsBankAccount.FinancialConnections = financialConnections
                InvoicePaymentMethodOptionsUsBankAccount.VerificationMethod = verificationMethod
            }

    and InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptions = {
        Filters: InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptionsFilters option
        ///The list of permissions to request. The `payment_method` permission must be included.
        Permissions: InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptionsPermissions list option
        ///Data features requested to be retrieved upon account creation.
        Prefetch: InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptionsPrefetch list option
    }
    with
        static member New (prefetch: InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptionsPrefetch list option, ?filters: InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptionsFilters, ?permissions: InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptionsPermissions list) =
            {
                InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptions.Prefetch = prefetch //required
                InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptions.Filters = filters
                InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptions.Permissions = permissions
            }

    and InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptionsFilters = {
        ///The account subcategories to use to filter for possible accounts to link. Valid subcategories are `checking` and `savings`.
        AccountSubcategories: InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptionsFiltersAccountSubcategories list option
    }
    with
        static member New (?accountSubcategories: InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptionsFiltersAccountSubcategories list) =
            {
                InvoicePaymentMethodOptionsUsBankAccountLinkedAccountOptionsFilters.AccountSubcategories = accountSubcategories
            }

    and InvoiceRenderingPdf = {
        ///Page size of invoice pdf. Options include a4, letter, and auto. If set to auto, page size will be switched to a4 or letter based on customer locale.
        PageSize: InvoiceRenderingPdfPageSize option
    }
    with
        static member New (pageSize: InvoiceRenderingPdfPageSize option) =
            {
                InvoiceRenderingPdf.PageSize = pageSize //required
            }

    ///Invoice Rendering Templates are used to configure how invoices are rendered on surfaces like the PDF. Invoice Rendering Templates
    ///can be created from within the Dashboard, and they can be used over the API when creating invoices.
    and InvoiceRenderingTemplate = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///A brief description of the template, hidden from customers
        Nickname: string option
        ///The status of the template, one of `active` or `archived`.
        Status: InvoiceRenderingTemplateStatus
        ///Version of this template; version increases by one when an update on the template changes any field that controls invoice rendering
        Version: int
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "invoice_rendering_template"

        static member New (created: DateTime, id: string, livemode: bool, metadata: Map<string, string> option, nickname: string option, status: InvoiceRenderingTemplateStatus, version: int) =
            {
                InvoiceRenderingTemplate.Created = created //required
                InvoiceRenderingTemplate.Id = id //required
                InvoiceRenderingTemplate.Livemode = livemode //required
                InvoiceRenderingTemplate.Metadata = metadata //required
                InvoiceRenderingTemplate.Nickname = nickname //required
                InvoiceRenderingTemplate.Status = status //required
                InvoiceRenderingTemplate.Version = version //required
            }

    and InvoiceSettingCustomerSetting = {
        ///Default custom fields to be displayed on invoices for this customer.
        CustomFields: InvoiceSettingCustomField list option
        ///ID of a payment method that's attached to the customer, to be used as the customer's default payment method for subscriptions and invoices.
        DefaultPaymentMethod: InvoiceSettingCustomerSettingDefaultPaymentMethod'AnyOf option
        ///Default footer to be displayed on invoices for this customer.
        Footer: string option
        ///Default options for invoice PDF rendering for this customer.
        RenderingOptions: InvoiceSettingCustomerRenderingOptions option
    }
    with
        static member New (customFields: InvoiceSettingCustomField list option, defaultPaymentMethod: InvoiceSettingCustomerSettingDefaultPaymentMethod'AnyOf option, footer: string option, renderingOptions: InvoiceSettingCustomerRenderingOptions option) =
            {
                InvoiceSettingCustomerSetting.CustomFields = customFields //required
                InvoiceSettingCustomerSetting.DefaultPaymentMethod = defaultPaymentMethod //required
                InvoiceSettingCustomerSetting.Footer = footer //required
                InvoiceSettingCustomerSetting.RenderingOptions = renderingOptions //required
            }

    and InvoiceSettingCustomerSettingDefaultPaymentMethod'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and InvoiceSettingQuoteSetting = {
        ///Number of days within which a customer must pay invoices generated by this quote. This value will be `null` for quotes where `collection_method=charge_automatically`.
        DaysUntilDue: int option
        Issuer: ConnectAccountReference
    }
    with
        static member New (daysUntilDue: int option, issuer: ConnectAccountReference) =
            {
                InvoiceSettingQuoteSetting.DaysUntilDue = daysUntilDue //required
                InvoiceSettingQuoteSetting.Issuer = issuer //required
            }

    and InvoiceSettingSubscriptionSchedulePhaseSetting = {
        ///The account tax IDs associated with this phase of the subscription schedule. Will be set on invoices generated by this phase of the subscription schedule.
        AccountTaxIds: InvoiceSettingSubscriptionSchedulePhaseSettingAccountTaxIds'AnyOf list option
        ///Number of days within which a customer must pay invoices generated by this subscription schedule. This value will be `null` for subscription schedules where `billing=charge_automatically`.
        DaysUntilDue: int option
        ///The connected account that issues the invoice. The invoice is presented with the branding and support information of the specified account.
        Issuer: ConnectAccountReference option
    }
    with
        static member New (accountTaxIds: InvoiceSettingSubscriptionSchedulePhaseSettingAccountTaxIds'AnyOf list option, daysUntilDue: int option, issuer: ConnectAccountReference option) =
            {
                InvoiceSettingSubscriptionSchedulePhaseSetting.AccountTaxIds = accountTaxIds //required
                InvoiceSettingSubscriptionSchedulePhaseSetting.DaysUntilDue = daysUntilDue //required
                InvoiceSettingSubscriptionSchedulePhaseSetting.Issuer = issuer //required
            }

    and InvoiceSettingSubscriptionSchedulePhaseSettingAccountTaxIds'AnyOf =
        | String of string
        | TaxId of TaxId
        | DeletedTaxId of DeletedTaxId

    and InvoiceSettingSubscriptionScheduleSetting = {
        ///The account tax IDs associated with the subscription schedule. Will be set on invoices generated by the subscription schedule.
        AccountTaxIds: InvoiceSettingSubscriptionScheduleSettingAccountTaxIds'AnyOf list option
        ///Number of days within which a customer must pay invoices generated by this subscription schedule. This value will be `null` for subscription schedules where `billing=charge_automatically`.
        DaysUntilDue: int option
        Issuer: ConnectAccountReference
    }
    with
        static member New (accountTaxIds: InvoiceSettingSubscriptionScheduleSettingAccountTaxIds'AnyOf list option, daysUntilDue: int option, issuer: ConnectAccountReference) =
            {
                InvoiceSettingSubscriptionScheduleSetting.AccountTaxIds = accountTaxIds //required
                InvoiceSettingSubscriptionScheduleSetting.DaysUntilDue = daysUntilDue //required
                InvoiceSettingSubscriptionScheduleSetting.Issuer = issuer //required
            }

    and InvoiceSettingSubscriptionScheduleSettingAccountTaxIds'AnyOf =
        | String of string
        | TaxId of TaxId
        | DeletedTaxId of DeletedTaxId

    and InvoiceThresholdReason = {
        ///The total invoice amount threshold boundary if it triggered the threshold invoice.
        AmountGte: int option
        ///Indicates which line items triggered a threshold invoice.
        ItemReasons: InvoiceItemThresholdReason list
    }
    with
        static member New (amountGte: int option, itemReasons: InvoiceItemThresholdReason list) =
            {
                InvoiceThresholdReason.AmountGte = amountGte //required
                InvoiceThresholdReason.ItemReasons = itemReasons //required
            }

    ///Invoice Items represent the component lines of an [invoice](https://docs.stripe.com/api/invoices). When you create an invoice item with an `invoice` field, it is attached to the specified invoice and included as [an invoice line item](https://docs.stripe.com/api/invoices/line_item) within [invoice.lines](https://docs.stripe.com/api/invoices/object#invoice_object-lines).
    ///Invoice Items can be created before you are ready to actually send the invoice. This can be particularly useful when combined
    ///with a [subscription](https://docs.stripe.com/api/subscriptions). Sometimes you want to add a charge or credit to a customer, but actually charge
    ///or credit the customer's card only at the end of a regular billing cycle. This is useful for combining several charges
    ///(to minimize per-transaction fees), or for having Stripe tabulate your usage-based billing totals.
    ///Related guides: [Integrate with the Invoicing API](https://docs.stripe.com/invoicing/integration), [Subscription Invoices](https://docs.stripe.com/billing/invoices/subscription#adding-upcoming-invoice-items).
    and Invoiceitem = {
        ///Amount (in the `currency` specified) of the invoice item. This should always be equal to `unit_amount * quantity`.
        Amount: int
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///The ID of the customer to bill for this invoice item.
        Customer: InvoiceitemCustomer'AnyOf
        ///The ID of the account to bill for this invoice item.
        CustomerAccount: string option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Date: DateTime
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///If true, discounts will apply to this invoice item. Always false for prorations.
        Discountable: bool
        ///The discounts which apply to the invoice item. Item discounts are applied before invoice discounts. Use `expand[]=discounts` to expand each discount.
        Discounts: InvoiceitemDiscounts'AnyOf list option
        ///Unique identifier for the object.
        Id: string
        ///The ID of the invoice this invoice item belongs to.
        Invoice: InvoiceitemInvoice'AnyOf option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///The amount after discounts, but before credits and taxes. This field is `null` for `discountable=true` items.
        NetAmount: int option
        ///The parent that generated this invoice item.
        Parent: BillingBillResourceInvoiceItemParentsInvoiceItemParent option
        Period: InvoiceLineItemPeriod
        ///The pricing information of the invoice item.
        Pricing: BillingBillResourceInvoicingPricingPricing option
        ///Whether the invoice item was created automatically as a proration adjustment when the customer switched plans.
        Proration: bool
        ProrationDetails: ProrationDetails option
        ///Quantity of units for the invoice item in integer format, with any decimal precision truncated. For the item's full-precision decimal quantity, use `quantity_decimal`. This field will be deprecated in favor of `quantity_decimal` in a future version. If the invoice item is a proration, the quantity of the subscription that the proration was computed for.
        Quantity: int
        ///Non-negative decimal with at most 12 decimal places. The quantity of units for the invoice item.
        QuantityDecimal: string
        ///The tax rates which apply to the invoice item. When set, the `default_tax_rates` on the invoice do not apply to this invoice item.
        TaxRates: TaxRate list option
        ///ID of the test clock this invoice item belongs to.
        TestClock: InvoiceitemTestClock'AnyOf option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "invoiceitem"

        static member New (amount: int, currency: IsoTypes.IsoCurrencyCode, customer: InvoiceitemCustomer'AnyOf, customerAccount: string option, date: DateTime, description: string option, discountable: bool, discounts: InvoiceitemDiscounts'AnyOf list option, id: string, invoice: InvoiceitemInvoice'AnyOf option, livemode: bool, metadata: Map<string, string> option, parent: BillingBillResourceInvoiceItemParentsInvoiceItemParent option, period: InvoiceLineItemPeriod, pricing: BillingBillResourceInvoicingPricingPricing option, proration: bool, quantity: int, quantityDecimal: string, taxRates: TaxRate list option, testClock: InvoiceitemTestClock'AnyOf option, ?netAmount: int, ?prorationDetails: ProrationDetails) =
            {
                Invoiceitem.Amount = amount //required
                Invoiceitem.Currency = currency //required
                Invoiceitem.Customer = customer //required
                Invoiceitem.CustomerAccount = customerAccount //required
                Invoiceitem.Date = date //required
                Invoiceitem.Description = description //required
                Invoiceitem.Discountable = discountable //required
                Invoiceitem.Discounts = discounts //required
                Invoiceitem.Id = id //required
                Invoiceitem.Invoice = invoice //required
                Invoiceitem.Livemode = livemode //required
                Invoiceitem.Metadata = metadata //required
                Invoiceitem.Parent = parent //required
                Invoiceitem.Period = period //required
                Invoiceitem.Pricing = pricing //required
                Invoiceitem.Proration = proration //required
                Invoiceitem.Quantity = quantity //required
                Invoiceitem.QuantityDecimal = quantityDecimal //required
                Invoiceitem.TaxRates = taxRates //required
                Invoiceitem.TestClock = testClock //required
                Invoiceitem.NetAmount = netAmount
                Invoiceitem.ProrationDetails = prorationDetails
            }

    and InvoiceitemCustomer'AnyOf =
        | String of string
        | Customer of Customer
        | DeletedCustomer of DeletedCustomer

    and InvoiceitemInvoice'AnyOf =
        | String of string
        | Invoice of Invoice

    and InvoiceitemTestClock'AnyOf =
        | String of string
        | TestHelpersTestClock of TestHelpersTestClock

    and InvoiceitemDiscounts'AnyOf =
        | String of string
        | Discount of Discount

    and InvoicesPaymentMethodOptions = {
        ///If paying by `acss_debit`, this sub-hash contains details about the Canadian pre-authorized debit payment method options to pass to the invoice’s PaymentIntent.
        AcssDebit: InvoicePaymentMethodOptionsAcssDebit option
        ///If paying by `bancontact`, this sub-hash contains details about the Bancontact payment method options to pass to the invoice’s PaymentIntent.
        Bancontact: InvoicePaymentMethodOptionsBancontact option
        ///If paying by `card`, this sub-hash contains details about the Card payment method options to pass to the invoice’s PaymentIntent.
        Card: InvoicePaymentMethodOptionsCard option
        ///If paying by `customer_balance`, this sub-hash contains details about the Bank transfer payment method options to pass to the invoice’s PaymentIntent.
        CustomerBalance: InvoicePaymentMethodOptionsCustomerBalance option
        ///If paying by `konbini`, this sub-hash contains details about the Konbini payment method options to pass to the invoice’s PaymentIntent.
        Konbini: InvoicePaymentMethodOptionsKonbini option
        ///If paying by `payto`, this sub-hash contains details about the PayTo payment method options to pass to the invoice’s PaymentIntent.
        Payto: InvoicePaymentMethodOptionsPayto option
        ///If paying by `pix`, this sub-hash contains details about the Pix payment method options to pass to the invoice’s PaymentIntent.
        Pix: InvoicePaymentMethodOptionsPix option
        ///If paying by `sepa_debit`, this sub-hash contains details about the SEPA Direct Debit payment method options to pass to the invoice’s PaymentIntent.
        SepaDebit: InvoicePaymentMethodOptionsSepaDebit option
        ///If paying by `upi`, this sub-hash contains details about the UPI payment method options to pass to the invoice’s PaymentIntent.
        Upi: InvoicePaymentMethodOptionsUpi option
        ///If paying by `us_bank_account`, this sub-hash contains details about the ACH direct debit payment method options to pass to the invoice’s PaymentIntent.
        UsBankAccount: InvoicePaymentMethodOptionsUsBankAccount option
    }
    with
        static member New (acssDebit: InvoicePaymentMethodOptionsAcssDebit option, bancontact: InvoicePaymentMethodOptionsBancontact option, card: InvoicePaymentMethodOptionsCard option, customerBalance: InvoicePaymentMethodOptionsCustomerBalance option, konbini: InvoicePaymentMethodOptionsKonbini option, payto: InvoicePaymentMethodOptionsPayto option, pix: InvoicePaymentMethodOptionsPix option, sepaDebit: InvoicePaymentMethodOptionsSepaDebit option, upi: InvoicePaymentMethodOptionsUpi option, usBankAccount: InvoicePaymentMethodOptionsUsBankAccount option) =
            {
                InvoicesPaymentMethodOptions.AcssDebit = acssDebit //required
                InvoicesPaymentMethodOptions.Bancontact = bancontact //required
                InvoicesPaymentMethodOptions.Card = card //required
                InvoicesPaymentMethodOptions.CustomerBalance = customerBalance //required
                InvoicesPaymentMethodOptions.Konbini = konbini //required
                InvoicesPaymentMethodOptions.Payto = payto //required
                InvoicesPaymentMethodOptions.Pix = pix //required
                InvoicesPaymentMethodOptions.SepaDebit = sepaDebit //required
                InvoicesPaymentMethodOptions.Upi = upi //required
                InvoicesPaymentMethodOptions.UsBankAccount = usBankAccount //required
            }

    and InvoicesPaymentSettings = {
        ///ID of the mandate to be used for this invoice. It must correspond to the payment method used to pay the invoice, including the invoice's default_payment_method or default_source, if set.
        DefaultMandate: string option
        ///Payment-method-specific configuration to provide to the invoice’s PaymentIntent.
        PaymentMethodOptions: InvoicesPaymentMethodOptions option
        ///The list of payment method types (e.g. card) to provide to the invoice’s PaymentIntent. If not set, Stripe attempts to automatically determine the types to use by looking at the invoice’s default payment method, the subscription’s default payment method, the customer’s default payment method, and your [invoice template settings](https://dashboard.stripe.com/settings/billing/invoice).
        PaymentMethodTypes: InvoicesPaymentSettingsPaymentMethodTypes list option
    }
    with
        static member New (defaultMandate: string option, paymentMethodOptions: InvoicesPaymentMethodOptions option, paymentMethodTypes: InvoicesPaymentSettingsPaymentMethodTypes list option) =
            {
                InvoicesPaymentSettings.DefaultMandate = defaultMandate //required
                InvoicesPaymentSettings.PaymentMethodOptions = paymentMethodOptions //required
                InvoicesPaymentSettings.PaymentMethodTypes = paymentMethodTypes //required
            }

    and InvoicesPaymentsInvoicePaymentAssociatedPayment = {
        ///ID of the successful charge for this payment when `type` is `charge`.Note: charge is only surfaced if the charge object is not associated with a payment intent. If the charge object does have a payment intent, the Invoice Payment surfaces the payment intent instead.
        Charge: InvoicesPaymentsInvoicePaymentAssociatedPaymentCharge'AnyOf option
        ///ID of the PaymentIntent associated with this payment when `type` is `payment_intent`. Note: This property is only populated for invoices finalized on or after March 15th, 2019.
        PaymentIntent: InvoicesPaymentsInvoicePaymentAssociatedPaymentPaymentIntent'AnyOf option
        ///ID of the PaymentRecord associated with this payment when `type` is `payment_record`.
        PaymentRecord: InvoicesPaymentsInvoicePaymentAssociatedPaymentPaymentRecord'AnyOf option
        ///Type of payment object associated with this invoice payment.
        Type: InvoicesPaymentsInvoicePaymentAssociatedPaymentType
    }
    with
        static member New (``type``: InvoicesPaymentsInvoicePaymentAssociatedPaymentType, ?charge: InvoicesPaymentsInvoicePaymentAssociatedPaymentCharge'AnyOf, ?paymentIntent: InvoicesPaymentsInvoicePaymentAssociatedPaymentPaymentIntent'AnyOf, ?paymentRecord: InvoicesPaymentsInvoicePaymentAssociatedPaymentPaymentRecord'AnyOf) =
            {
                InvoicesPaymentsInvoicePaymentAssociatedPayment.Type = ``type`` //required
                InvoicesPaymentsInvoicePaymentAssociatedPayment.Charge = charge
                InvoicesPaymentsInvoicePaymentAssociatedPayment.PaymentIntent = paymentIntent
                InvoicesPaymentsInvoicePaymentAssociatedPayment.PaymentRecord = paymentRecord
            }

    and InvoicesPaymentsInvoicePaymentAssociatedPaymentCharge'AnyOf =
        | String of string
        | Charge of Charge

    and InvoicesPaymentsInvoicePaymentAssociatedPaymentPaymentIntent'AnyOf =
        | String of string
        | PaymentIntent of PaymentIntent

    and InvoicesPaymentsInvoicePaymentAssociatedPaymentPaymentRecord'AnyOf =
        | String of string
        | PaymentRecord of PaymentRecord

    and InvoicesResourceFromInvoice = {
        ///The relation between this invoice and the cloned invoice
        Action: string
        ///The invoice that was cloned.
        Invoice: InvoicesResourceFromInvoiceInvoice'AnyOf
    }
    with
        static member New (action: string, invoice: InvoicesResourceFromInvoiceInvoice'AnyOf) =
            {
                InvoicesResourceFromInvoice.Action = action //required
                InvoicesResourceFromInvoice.Invoice = invoice //required
            }

    and InvoicesResourceFromInvoiceInvoice'AnyOf =
        | String of string
        | Invoice of Invoice

    and InvoicesResourceInvoiceRendering = {
        ///How line-item prices and amounts will be displayed with respect to tax on invoice PDFs.
        AmountTaxDisplay: string option
        ///Invoice pdf rendering options
        Pdf: InvoiceRenderingPdf option
        ///ID of the rendering template that the invoice is formatted by.
        Template: string option
        ///Version of the rendering template that the invoice is using.
        TemplateVersion: int option
    }
    with
        static member New (amountTaxDisplay: string option, pdf: InvoiceRenderingPdf option, template: string option, templateVersion: int option) =
            {
                InvoicesResourceInvoiceRendering.AmountTaxDisplay = amountTaxDisplay //required
                InvoicesResourceInvoiceRendering.Pdf = pdf //required
                InvoicesResourceInvoiceRendering.Template = template //required
                InvoicesResourceInvoiceRendering.TemplateVersion = templateVersion //required
            }

    and InvoicesResourceInvoiceTaxId = {
        ///The type of the tax ID, one of `ad_nrt`, `ar_cuit`, `eu_vat`, `bo_tin`, `br_cnpj`, `br_cpf`, `cn_tin`, `co_nit`, `cr_tin`, `do_rcn`, `ec_ruc`, `eu_oss_vat`, `hr_oib`, `pe_ruc`, `ro_tin`, `rs_pib`, `sv_nit`, `uy_ruc`, `ve_rif`, `vn_tin`, `gb_vat`, `nz_gst`, `au_abn`, `au_arn`, `in_gst`, `no_vat`, `no_voec`, `za_vat`, `ch_vat`, `mx_rfc`, `sg_uen`, `ru_inn`, `ru_kpp`, `ca_bn`, `hk_br`, `es_cif`, `pl_nip`, `it_cf`, `fo_vat`, `gi_tin`, `py_ruc`, `tw_vat`, `th_vat`, `jp_cn`, `jp_rn`, `jp_trn`, `li_uid`, `li_vat`, `lk_vat`, `my_itn`, `us_ein`, `kr_brn`, `ca_qst`, `ca_gst_hst`, `ca_pst_bc`, `ca_pst_mb`, `ca_pst_sk`, `my_sst`, `sg_gst`, `ae_trn`, `cl_tin`, `sa_vat`, `id_npwp`, `my_frp`, `il_vat`, `ge_vat`, `ua_vat`, `is_vat`, `bg_uic`, `hu_tin`, `si_tin`, `ke_pin`, `tr_tin`, `eg_tin`, `ph_tin`, `al_tin`, `bh_vat`, `kz_bin`, `ng_tin`, `om_vat`, `de_stn`, `ch_uid`, `tz_vat`, `uz_vat`, `uz_tin`, `md_vat`, `ma_vat`, `by_tin`, `ao_tin`, `bs_tin`, `bb_tin`, `cd_nif`, `mr_nif`, `me_pib`, `zw_tin`, `ba_tin`, `gn_nif`, `mk_vat`, `sr_fin`, `sn_ninea`, `am_tin`, `np_pan`, `tj_tin`, `ug_tin`, `zm_tin`, `kh_tin`, `aw_tin`, `az_tin`, `bd_bin`, `bj_ifu`, `et_tin`, `kg_tin`, `la_tin`, `cm_niu`, `cv_nif`, `bf_ifu`, or `unknown`
        Type: InvoicesResourceInvoiceTaxIdType
        ///The value of the tax ID.
        Value: string option
    }
    with
        static member New (``type``: InvoicesResourceInvoiceTaxIdType, value: string option) =
            {
                InvoicesResourceInvoiceTaxId.Type = ``type`` //required
                InvoicesResourceInvoiceTaxId.Value = value //required
            }

    and InvoicesResourcePretaxCreditAmount = {
        ///The amount, in cents (or local equivalent), of the pretax credit amount.
        Amount: int
        ///The credit balance transaction that was applied to get this pretax credit amount.
        CreditBalanceTransaction: InvoicesResourcePretaxCreditAmountCreditBalanceTransaction'AnyOf option
        ///The discount that was applied to get this pretax credit amount.
        Discount: InvoicesResourcePretaxCreditAmountDiscount'AnyOf option
        ///Type of the pretax credit amount referenced.
        Type: InvoicesResourcePretaxCreditAmountType
    }
    with
        static member New (amount: int, ``type``: InvoicesResourcePretaxCreditAmountType, ?creditBalanceTransaction: InvoicesResourcePretaxCreditAmountCreditBalanceTransaction'AnyOf option, ?discount: InvoicesResourcePretaxCreditAmountDiscount'AnyOf) =
            {
                InvoicesResourcePretaxCreditAmount.Amount = amount //required
                InvoicesResourcePretaxCreditAmount.Type = ``type`` //required
                InvoicesResourcePretaxCreditAmount.CreditBalanceTransaction = creditBalanceTransaction |> Option.flatten
                InvoicesResourcePretaxCreditAmount.Discount = discount
            }

    and InvoicesResourcePretaxCreditAmountCreditBalanceTransaction'AnyOf =
        | String of string
        | BillingCreditBalanceTransaction of BillingCreditBalanceTransaction

    and InvoicesResourcePretaxCreditAmountDiscount'AnyOf =
        | String of string
        | Discount of Discount
        | DeletedDiscount of DeletedDiscount

    and InvoicesResourceShippingCost = {
        ///Total shipping cost before any taxes are applied.
        AmountSubtotal: int
        ///Total tax amount applied due to shipping costs. If no tax was applied, defaults to 0.
        AmountTax: int
        ///Total shipping cost after taxes are applied.
        AmountTotal: int
        ///The ID of the ShippingRate for this invoice.
        ShippingRate: InvoicesResourceShippingCostShippingRate'AnyOf option
        ///The taxes applied to the shipping rate.
        Taxes: LineItemsTaxAmount list option
    }
    with
        static member New (amountSubtotal: int, amountTax: int, amountTotal: int, shippingRate: InvoicesResourceShippingCostShippingRate'AnyOf option, ?taxes: LineItemsTaxAmount list) =
            {
                InvoicesResourceShippingCost.AmountSubtotal = amountSubtotal //required
                InvoicesResourceShippingCost.AmountTax = amountTax //required
                InvoicesResourceShippingCost.AmountTotal = amountTotal //required
                InvoicesResourceShippingCost.ShippingRate = shippingRate //required
                InvoicesResourceShippingCost.Taxes = taxes
            }

    and InvoicesResourceShippingCostShippingRate'AnyOf =
        | String of string
        | ShippingRate of ShippingRate

    ///When an [issued card](https://docs.stripe.com/issuing) is used to make a purchase, an Issuing `Authorization`
    ///object is created. [Authorizations](https://docs.stripe.com/issuing/purchases/authorizations) must be approved for the
    ///purchase to be completed successfully.
    ///Related guide: [Issued card authorizations](https://docs.stripe.com/issuing/purchases/authorizations)
    and IssuingAuthorization = {
        ///The total amount that was authorized or rejected. This amount is in `currency` and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal). `amount` should be the same as `merchant_amount`, unless `currency` and `merchant_currency` are different.
        Amount: int
        ///Detailed breakdown of amount components. These amounts are denominated in `currency` and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
        AmountDetails: IssuingAuthorizationAmountDetails option
        ///Whether the authorization has been approved.
        Approved: bool
        ///How the card details were provided.
        AuthorizationMethod: IssuingAuthorizationAuthorizationMethod
        ///List of balance transactions associated with this authorization.
        BalanceTransactions: BalanceTransaction list
        Card: IssuingCard
        ///Whether the card was present at the point of sale for the authorization.
        CardPresence: IssuingAuthorizationCardPresence option
        ///The cardholder to whom this authorization belongs.
        Cardholder: IssuingAuthorizationCardholder'AnyOf option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The currency of the cardholder. This currency can be different from the currency presented at authorization and the `merchant_currency` field on this authorization. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///Fleet-specific information for authorizations using Fleet cards.
        Fleet: IssuingAuthorizationFleetData option
        ///Fraud challenges sent to the cardholder, if this authorization was declined for fraud risk reasons.
        FraudChallenges: IssuingAuthorizationFraudChallenge list option
        ///Information about fuel that was purchased with this transaction. Typically this information is received from the merchant after the authorization has been approved and the fuel dispensed.
        Fuel: IssuingAuthorizationFuelData option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The total amount that was authorized or rejected. This amount is in the `merchant_currency` and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal). `merchant_amount` should be the same as `amount`, unless `merchant_currency` and `currency` are different.
        MerchantAmount: int
        ///The local currency that was presented to the cardholder for the authorization. This currency can be different from the cardholder currency and the `currency` field on this authorization. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        MerchantCurrency: IsoTypes.IsoCurrencyCode
        MerchantData: IssuingAuthorizationMerchantData
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///Details about the authorization, such as identifiers, set by the card network.
        NetworkData: IssuingAuthorizationNetworkData option
        ///The pending authorization request. This field will only be non-null during an `issuing_authorization.request` webhook.
        PendingRequest: IssuingAuthorizationPendingRequest option
        ///History of every time a `pending_request` authorization was approved/declined, either by you directly or by Stripe (e.g. based on your spending_controls). If the merchant changes the authorization by performing an incremental authorization, you can look at this field to see the previous requests for the authorization. This field can be helpful in determining why a given authorization was approved/declined.
        RequestHistory: IssuingAuthorizationRequest list
        ///The current status of the authorization in its lifecycle.
        Status: IssuingAuthorizationStatus
        ///[Token](https://docs.stripe.com/api/issuing/tokens/object) object used for this authorization. If a network token was not used for this authorization, this field will be null.
        Token: IssuingAuthorizationToken'AnyOf option
        ///List of [transactions](https://docs.stripe.com/api/issuing/transactions) associated with this authorization.
        Transactions: IssuingTransaction list
        ///[Treasury](https://docs.stripe.com/api/treasury) details related to this authorization if it was created on a [FinancialAccount](https://docs.stripe.com/api/treasury/financial_accounts).
        Treasury: IssuingAuthorizationTreasury option
        VerificationData: IssuingAuthorizationVerificationData
        ///Whether the authorization bypassed fraud risk checks because the cardholder has previously completed a fraud challenge on a similar high-risk authorization from the same merchant.
        VerifiedByFraudChallenge: bool option
        ///The digital wallet used for this transaction. One of `apple_pay`, `google_pay`, or `samsung_pay`. Will populate as `null` when no digital wallet was utilized.
        Wallet: IssuingAuthorizationWallet option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "issuing.authorization"

        static member New (amount: int, amountDetails: IssuingAuthorizationAmountDetails option, approved: bool, authorizationMethod: IssuingAuthorizationAuthorizationMethod, balanceTransactions: BalanceTransaction list, card: IssuingCard, cardPresence: IssuingAuthorizationCardPresence option, cardholder: IssuingAuthorizationCardholder'AnyOf option, created: DateTime, currency: IsoTypes.IsoCurrencyCode, fleet: IssuingAuthorizationFleetData option, fuel: IssuingAuthorizationFuelData option, id: string, livemode: bool, merchantAmount: int, merchantCurrency: IsoTypes.IsoCurrencyCode, merchantData: IssuingAuthorizationMerchantData, metadata: Map<string, string>, networkData: IssuingAuthorizationNetworkData option, pendingRequest: IssuingAuthorizationPendingRequest option, requestHistory: IssuingAuthorizationRequest list, status: IssuingAuthorizationStatus, transactions: IssuingTransaction list, verificationData: IssuingAuthorizationVerificationData, verifiedByFraudChallenge: bool option, wallet: IssuingAuthorizationWallet option, ?fraudChallenges: IssuingAuthorizationFraudChallenge list option, ?token: IssuingAuthorizationToken'AnyOf option, ?treasury: IssuingAuthorizationTreasury option) =
            {
                IssuingAuthorization.Amount = amount //required
                IssuingAuthorization.AmountDetails = amountDetails //required
                IssuingAuthorization.Approved = approved //required
                IssuingAuthorization.AuthorizationMethod = authorizationMethod //required
                IssuingAuthorization.BalanceTransactions = balanceTransactions //required
                IssuingAuthorization.Card = card //required
                IssuingAuthorization.CardPresence = cardPresence //required
                IssuingAuthorization.Cardholder = cardholder //required
                IssuingAuthorization.Created = created //required
                IssuingAuthorization.Currency = currency //required
                IssuingAuthorization.Fleet = fleet //required
                IssuingAuthorization.Fuel = fuel //required
                IssuingAuthorization.Id = id //required
                IssuingAuthorization.Livemode = livemode //required
                IssuingAuthorization.MerchantAmount = merchantAmount //required
                IssuingAuthorization.MerchantCurrency = merchantCurrency //required
                IssuingAuthorization.MerchantData = merchantData //required
                IssuingAuthorization.Metadata = metadata //required
                IssuingAuthorization.NetworkData = networkData //required
                IssuingAuthorization.PendingRequest = pendingRequest //required
                IssuingAuthorization.RequestHistory = requestHistory //required
                IssuingAuthorization.Status = status //required
                IssuingAuthorization.Transactions = transactions //required
                IssuingAuthorization.VerificationData = verificationData //required
                IssuingAuthorization.VerifiedByFraudChallenge = verifiedByFraudChallenge //required
                IssuingAuthorization.Wallet = wallet //required
                IssuingAuthorization.FraudChallenges = fraudChallenges |> Option.flatten
                IssuingAuthorization.Token = token |> Option.flatten
                IssuingAuthorization.Treasury = treasury |> Option.flatten
            }

    and IssuingAuthorizationCardholder'AnyOf =
        | String of string
        | IssuingCardholder of IssuingCardholder

    and IssuingAuthorizationToken'AnyOf =
        | String of string
        | IssuingToken of IssuingToken

    ///You can [create physical or virtual cards](https://docs.stripe.com/issuing) that are issued to cardholders.
    and IssuingCard = {
        ///The brand of the card.
        Brand: string
        ///The reason why the card was canceled.
        CancellationReason: IssuingCardCancellationReason option
        Cardholder: IssuingCardholder
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Supported currencies are `usd` in the US, `eur` in the EU, and `gbp` in the UK.
        Currency: IsoTypes.IsoCurrencyCode
        ///The card's CVC. For security reasons, this is only available for virtual cards, and will be omitted unless you explicitly request it with [the `expand` parameter](https://docs.stripe.com/api/expanding_objects). Additionally, it's only available via the ["Retrieve a card" endpoint](https://docs.stripe.com/api/issuing/cards/retrieve), not via "List all cards" or any other endpoint.
        Cvc: string option
        ///The expiration month of the card.
        ExpMonth: int
        ///The expiration year of the card.
        ExpYear: int
        ///The financial account this card is attached to.
        FinancialAccount: string option
        ///Unique identifier for the object.
        Id: string
        ///The last 4 digits of the card number.
        [<JsonField(Name="last4")>]Last4: string
        ///Stripe’s assessment of whether this card’s details have been compromised. If this property isn't null, cancel and reissue the card to prevent fraudulent activity risk.
        LatestFraudWarning: IssuingCardFraudWarning option
        ///Rules that control the lifecycle of this card, such as automatic cancellation. Refer to our [documentation](/issuing/controls/lifecycle-controls) for more details.
        LifecycleControls: IssuingCardLifecycleControls option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///The full unredacted card number. For security reasons, this is only available for virtual cards, and will be omitted unless you explicitly request it with [the `expand` parameter](https://docs.stripe.com/api/expanding_objects). Additionally, it's only available via the ["Retrieve a card" endpoint](https://docs.stripe.com/api/issuing/cards/retrieve), not via "List all cards" or any other endpoint.
        Number: string option
        ///The personalization design object belonging to this card.
        PersonalizationDesign: IssuingCardPersonalizationDesign'AnyOf option
        ///The latest card that replaces this card, if any.
        ReplacedBy: IssuingCardReplacedBy'AnyOf option
        ///The card this card replaces, if any.
        ReplacementFor: IssuingCardReplacementFor'AnyOf option
        ///The reason why the previous card needed to be replaced.
        ReplacementReason: IssuingCardReplacementReason option
        ///Text separate from cardholder name, printed on the card.
        SecondLine: string option
        ///Where and how the card will be shipped.
        Shipping: IssuingCardShipping option
        SpendingControls: IssuingCardAuthorizationControls
        ///Whether authorizations can be approved on this card. May be blocked from activating cards depending on past-due Cardholder requirements. Defaults to `inactive`.
        Status: IssuingCardStatus
        ///The type of the card.
        Type: IssuingCardType
        ///Information relating to digital wallets (like Apple Pay and Google Pay).
        Wallets: IssuingCardWallets option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "issuing.card"

        static member New (brand: string, cancellationReason: IssuingCardCancellationReason option, cardholder: IssuingCardholder, created: DateTime, currency: IsoTypes.IsoCurrencyCode, expMonth: int, expYear: int, id: string, last4: string, latestFraudWarning: IssuingCardFraudWarning option, lifecycleControls: IssuingCardLifecycleControls option, livemode: bool, metadata: Map<string, string>, personalizationDesign: IssuingCardPersonalizationDesign'AnyOf option, replacedBy: IssuingCardReplacedBy'AnyOf option, replacementFor: IssuingCardReplacementFor'AnyOf option, replacementReason: IssuingCardReplacementReason option, secondLine: string option, shipping: IssuingCardShipping option, spendingControls: IssuingCardAuthorizationControls, status: IssuingCardStatus, ``type``: IssuingCardType, wallets: IssuingCardWallets option, ?cvc: string, ?financialAccount: string option, ?number: string) =
            {
                IssuingCard.Brand = brand //required
                IssuingCard.CancellationReason = cancellationReason //required
                IssuingCard.Cardholder = cardholder //required
                IssuingCard.Created = created //required
                IssuingCard.Currency = currency //required
                IssuingCard.ExpMonth = expMonth //required
                IssuingCard.ExpYear = expYear //required
                IssuingCard.Id = id //required
                IssuingCard.Last4 = last4 //required
                IssuingCard.LatestFraudWarning = latestFraudWarning //required
                IssuingCard.LifecycleControls = lifecycleControls //required
                IssuingCard.Livemode = livemode //required
                IssuingCard.Metadata = metadata //required
                IssuingCard.PersonalizationDesign = personalizationDesign //required
                IssuingCard.ReplacedBy = replacedBy //required
                IssuingCard.ReplacementFor = replacementFor //required
                IssuingCard.ReplacementReason = replacementReason //required
                IssuingCard.SecondLine = secondLine //required
                IssuingCard.Shipping = shipping //required
                IssuingCard.SpendingControls = spendingControls //required
                IssuingCard.Status = status //required
                IssuingCard.Type = ``type`` //required
                IssuingCard.Wallets = wallets //required
                IssuingCard.Cvc = cvc
                IssuingCard.FinancialAccount = financialAccount |> Option.flatten
                IssuingCard.Number = number
            }

    and IssuingCardPersonalizationDesign'AnyOf =
        | String of string
        | IssuingPersonalizationDesign of IssuingPersonalizationDesign

    and IssuingCardReplacedBy'AnyOf =
        | String of string
        | IssuingCard of IssuingCard

    and IssuingCardReplacementFor'AnyOf =
        | String of string
        | IssuingCard of IssuingCard

    ///An Issuing `Cardholder` object represents an individual or business entity who is [issued](https://docs.stripe.com/issuing) cards.
    ///Related guide: [How to create a cardholder](https://docs.stripe.com/issuing/cards/virtual/issue-cards#create-cardholder)
    and IssuingCardholder = {
        Billing: IssuingCardholderAddress
        ///Additional information about a `company` cardholder.
        Company: IssuingCardholderCompany option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The cardholder's email address.
        Email: string option
        ///Unique identifier for the object.
        Id: string
        ///Additional information about an `individual` cardholder.
        Individual: IssuingCardholderIndividual option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///The cardholder's name. This will be printed on cards issued to them.
        Name: string
        ///The cardholder's phone number. This is required for all cardholders who will be creating EU cards. See the [3D Secure documentation](https://docs.stripe.com/issuing/3d-secure#when-is-3d-secure-applied) for more details.
        PhoneNumber: string option
        ///The cardholder’s preferred locales (languages), ordered by preference. Locales can be `da`, `de`, `en`, `es`, `fr`, `it`, `pl`, or `sv`.
        /// This changes the language of the [3D Secure flow](https://docs.stripe.com/issuing/3d-secure) and one-time password messages sent to the cardholder.
        PreferredLocales: IssuingCardholderPreferredLocales list option
        Requirements: IssuingCardholderRequirements
        ///Rules that control spending across this cardholder's cards. Refer to our [documentation](https://docs.stripe.com/issuing/controls/spending-controls) for more details.
        SpendingControls: IssuingCardholderAuthorizationControls option
        ///Specifies whether to permit authorizations on this cardholder's cards.
        Status: IssuingCardholderStatus
        ///One of `individual` or `company`. See [Choose a cardholder type](https://docs.stripe.com/issuing/other/choose-cardholder) for more details.
        Type: IssuingCardholderType
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "issuing.cardholder"

        static member New (billing: IssuingCardholderAddress, company: IssuingCardholderCompany option, created: DateTime, email: string option, id: string, individual: IssuingCardholderIndividual option, livemode: bool, metadata: Map<string, string>, name: string, phoneNumber: string option, preferredLocales: IssuingCardholderPreferredLocales list option, requirements: IssuingCardholderRequirements, spendingControls: IssuingCardholderAuthorizationControls option, status: IssuingCardholderStatus, ``type``: IssuingCardholderType) =
            {
                IssuingCardholder.Billing = billing //required
                IssuingCardholder.Company = company //required
                IssuingCardholder.Created = created //required
                IssuingCardholder.Email = email //required
                IssuingCardholder.Id = id //required
                IssuingCardholder.Individual = individual //required
                IssuingCardholder.Livemode = livemode //required
                IssuingCardholder.Metadata = metadata //required
                IssuingCardholder.Name = name //required
                IssuingCardholder.PhoneNumber = phoneNumber //required
                IssuingCardholder.PreferredLocales = preferredLocales //required
                IssuingCardholder.Requirements = requirements //required
                IssuingCardholder.SpendingControls = spendingControls //required
                IssuingCardholder.Status = status //required
                IssuingCardholder.Type = ``type`` //required
            }

    ///As a [card issuer](https://docs.stripe.com/issuing), you can dispute transactions that the cardholder does not recognize, suspects to be fraudulent, or has other issues with.
    ///Related guide: [Issuing disputes](https://docs.stripe.com/issuing/purchases/disputes)
    and IssuingDispute = {
        ///Disputed amount in the card's currency and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). Usually the amount of the `transaction`, but can differ (usually because of currency fluctuation).
        Amount: int
        ///List of balance transactions associated with the dispute.
        BalanceTransactions: BalanceTransaction list option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The currency the `transaction` was made in.
        Currency: IsoTypes.IsoCurrencyCode
        Evidence: IssuingDisputeEvidence
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The enum that describes the dispute loss outcome. If the dispute is not lost, this field will be absent. New enum values may be added in the future, so be sure to handle unknown values.
        LossReason: IssuingDisputeLossReason option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///Current status of the dispute.
        Status: IssuingDisputeStatus
        ///The transaction being disputed.
        Transaction: IssuingDisputeTransaction'AnyOf
        ///[Treasury](https://docs.stripe.com/api/treasury) details related to this dispute if it was created on a [FinancialAccount](/docs/api/treasury/financial_accounts
        Treasury: IssuingDisputeTreasury option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "issuing.dispute"

        static member New (amount: int, created: DateTime, currency: IsoTypes.IsoCurrencyCode, evidence: IssuingDisputeEvidence, id: string, livemode: bool, metadata: Map<string, string>, status: IssuingDisputeStatus, transaction: IssuingDisputeTransaction'AnyOf, ?balanceTransactions: BalanceTransaction list option, ?lossReason: IssuingDisputeLossReason, ?treasury: IssuingDisputeTreasury option) =
            {
                IssuingDispute.Amount = amount //required
                IssuingDispute.Created = created //required
                IssuingDispute.Currency = currency //required
                IssuingDispute.Evidence = evidence //required
                IssuingDispute.Id = id //required
                IssuingDispute.Livemode = livemode //required
                IssuingDispute.Metadata = metadata //required
                IssuingDispute.Status = status //required
                IssuingDispute.Transaction = transaction //required
                IssuingDispute.BalanceTransactions = balanceTransactions |> Option.flatten
                IssuingDispute.LossReason = lossReason
                IssuingDispute.Treasury = treasury |> Option.flatten
            }

    and IssuingDisputeTransaction'AnyOf =
        | String of string
        | IssuingTransaction of IssuingTransaction

    ///A Personalization Design is a logical grouping of a Physical Bundle, card logo, and carrier text that represents a product line.
    and IssuingPersonalizationDesign = {
        ///The file for the card logo to use with physical bundles that support card logos. Must have a `purpose` value of `issuing_logo`.
        CardLogo: IssuingPersonalizationDesignCardLogo'AnyOf option
        ///Hash containing carrier text, for use with physical bundles that support carrier text.
        CarrierText: IssuingPersonalizationDesignCarrierText option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///A lookup key used to retrieve personalization designs dynamically from a static string. This may be up to 200 characters.
        LookupKey: string option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///Friendly display name.
        Name: string option
        ///The physical bundle object belonging to this personalization design.
        PhysicalBundle: IssuingPersonalizationDesignPhysicalBundle'AnyOf
        Preferences: IssuingPersonalizationDesignPreferences
        RejectionReasons: IssuingPersonalizationDesignRejectionReasons
        ///Whether this personalization design can be used to create cards.
        Status: IssuingPersonalizationDesignStatus
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "issuing.personalization_design"

        static member New (cardLogo: IssuingPersonalizationDesignCardLogo'AnyOf option, carrierText: IssuingPersonalizationDesignCarrierText option, created: DateTime, id: string, livemode: bool, lookupKey: string option, metadata: Map<string, string>, name: string option, physicalBundle: IssuingPersonalizationDesignPhysicalBundle'AnyOf, preferences: IssuingPersonalizationDesignPreferences, rejectionReasons: IssuingPersonalizationDesignRejectionReasons, status: IssuingPersonalizationDesignStatus) =
            {
                IssuingPersonalizationDesign.CardLogo = cardLogo //required
                IssuingPersonalizationDesign.CarrierText = carrierText //required
                IssuingPersonalizationDesign.Created = created //required
                IssuingPersonalizationDesign.Id = id //required
                IssuingPersonalizationDesign.Livemode = livemode //required
                IssuingPersonalizationDesign.LookupKey = lookupKey //required
                IssuingPersonalizationDesign.Metadata = metadata //required
                IssuingPersonalizationDesign.Name = name //required
                IssuingPersonalizationDesign.PhysicalBundle = physicalBundle //required
                IssuingPersonalizationDesign.Preferences = preferences //required
                IssuingPersonalizationDesign.RejectionReasons = rejectionReasons //required
                IssuingPersonalizationDesign.Status = status //required
            }

    and IssuingPersonalizationDesignCardLogo'AnyOf =
        | String of string
        | File of File

    and IssuingPersonalizationDesignPhysicalBundle'AnyOf =
        | String of string
        | IssuingPhysicalBundle of IssuingPhysicalBundle

    ///A Physical Bundle represents the bundle of physical items - card stock, carrier letter, and envelope - that is shipped to a cardholder when you create a physical card.
    and IssuingPhysicalBundle = {
        Features: IssuingPhysicalBundleFeatures
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Friendly display name.
        Name: string
        ///Whether this physical bundle can be used to create cards.
        Status: IssuingPhysicalBundleStatus
        ///Whether this physical bundle is a standard Stripe offering or custom-made for you.
        Type: IssuingPhysicalBundleType
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "issuing.physical_bundle"

        static member New (features: IssuingPhysicalBundleFeatures, id: string, livemode: bool, name: string, status: IssuingPhysicalBundleStatus, ``type``: IssuingPhysicalBundleType) =
            {
                IssuingPhysicalBundle.Features = features //required
                IssuingPhysicalBundle.Id = id //required
                IssuingPhysicalBundle.Livemode = livemode //required
                IssuingPhysicalBundle.Name = name //required
                IssuingPhysicalBundle.Status = status //required
                IssuingPhysicalBundle.Type = ``type`` //required
            }

    ///An issuing token object is created when an issued card is added to a digital wallet. As a [card issuer](https://docs.stripe.com/issuing), you can [view and manage these tokens](https://docs.stripe.com/issuing/controls/token-management) through Stripe.
    and IssuingToken = {
        ///Card associated with this token.
        Card: IssuingTokenCard'AnyOf
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The hashed ID derived from the device ID from the card network associated with the token.
        DeviceFingerprint: string option
        ///Unique identifier for the object.
        Id: string
        ///The last four digits of the token.
        [<JsonField(Name="last4")>]Last4: string option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The token service provider / card network associated with the token.
        Network: IssuingTokenNetwork
        NetworkData: IssuingNetworkTokenNetworkData option
        ///Time at which the token was last updated by the card network. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]NetworkUpdatedAt: DateTime
        ///The usage state of the token.
        Status: IssuingTokenStatus
        ///The digital wallet for this token, if one was used.
        WalletProvider: IssuingTokenWalletProvider option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "issuing.token"

        static member New (card: IssuingTokenCard'AnyOf, created: DateTime, deviceFingerprint: string option, id: string, livemode: bool, network: IssuingTokenNetwork, networkUpdatedAt: DateTime, status: IssuingTokenStatus, ?last4: string, ?networkData: IssuingNetworkTokenNetworkData, ?walletProvider: IssuingTokenWalletProvider) =
            {
                IssuingToken.Card = card //required
                IssuingToken.Created = created //required
                IssuingToken.DeviceFingerprint = deviceFingerprint //required
                IssuingToken.Id = id //required
                IssuingToken.Livemode = livemode //required
                IssuingToken.Network = network //required
                IssuingToken.NetworkUpdatedAt = networkUpdatedAt //required
                IssuingToken.Status = status //required
                IssuingToken.Last4 = last4
                IssuingToken.NetworkData = networkData
                IssuingToken.WalletProvider = walletProvider
            }

    and IssuingTokenCard'AnyOf =
        | String of string
        | IssuingCard of IssuingCard

    ///Any use of an [issued card](https://docs.stripe.com/issuing) that results in funds entering or leaving
    ///your Stripe account, such as a completed purchase or refund, is represented by an Issuing
    ///`Transaction` object.
    ///Related guide: [Issued card transactions](https://docs.stripe.com/issuing/purchases/transactions)
    and IssuingTransaction = {
        ///The transaction amount, which will be reflected in your balance. This amount is in your currency and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
        Amount: int
        ///Detailed breakdown of amount components. These amounts are denominated in `currency` and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
        AmountDetails: IssuingTransactionAmountDetails option
        ///The `Authorization` object that led to this transaction.
        Authorization: IssuingTransactionAuthorization'AnyOf option
        ///ID of the [balance transaction](https://docs.stripe.com/api/balance_transactions) associated with this transaction.
        BalanceTransaction: IssuingTransactionBalanceTransaction'AnyOf option
        ///The card used to make this transaction.
        Card: IssuingTransactionCard'AnyOf
        ///The cardholder to whom this transaction belongs.
        Cardholder: IssuingTransactionCardholder'AnyOf option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///If you've disputed the transaction, the ID of the dispute.
        Dispute: IssuingTransactionDispute'AnyOf option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The amount that the merchant will receive, denominated in `merchant_currency` and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). It will be different from `amount` if the merchant is taking payment in a different currency.
        MerchantAmount: int
        ///The currency with which the merchant is taking payment.
        MerchantCurrency: IsoTypes.IsoCurrencyCode
        MerchantData: IssuingAuthorizationMerchantData
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///Details about the transaction, such as processing dates, set by the card network.
        NetworkData: IssuingTransactionNetworkData option
        ///Additional purchase information that is optionally provided by the merchant.
        PurchaseDetails: IssuingTransactionPurchaseDetails option
        ///[Token](https://docs.stripe.com/api/issuing/tokens/object) object used for this transaction. If a network token was not used for this transaction, this field will be null.
        Token: IssuingTransactionToken'AnyOf option
        ///[Treasury](https://docs.stripe.com/api/treasury) details related to this transaction if it was created on a [FinancialAccount](/docs/api/treasury/financial_accounts
        Treasury: IssuingTransactionTreasury option
        ///The nature of the transaction.
        Type: IssuingTransactionType
        ///The digital wallet used for this transaction. One of `apple_pay`, `google_pay`, or `samsung_pay`.
        Wallet: IssuingTransactionWallet option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "issuing.transaction"

        static member New (amount: int, amountDetails: IssuingTransactionAmountDetails option, authorization: IssuingTransactionAuthorization'AnyOf option, balanceTransaction: IssuingTransactionBalanceTransaction'AnyOf option, card: IssuingTransactionCard'AnyOf, cardholder: IssuingTransactionCardholder'AnyOf option, created: DateTime, currency: IsoTypes.IsoCurrencyCode, dispute: IssuingTransactionDispute'AnyOf option, id: string, livemode: bool, merchantAmount: int, merchantCurrency: IsoTypes.IsoCurrencyCode, merchantData: IssuingAuthorizationMerchantData, metadata: Map<string, string>, networkData: IssuingTransactionNetworkData option, ``type``: IssuingTransactionType, wallet: IssuingTransactionWallet option, ?purchaseDetails: IssuingTransactionPurchaseDetails option, ?token: IssuingTransactionToken'AnyOf option, ?treasury: IssuingTransactionTreasury option) =
            {
                IssuingTransaction.Amount = amount //required
                IssuingTransaction.AmountDetails = amountDetails //required
                IssuingTransaction.Authorization = authorization //required
                IssuingTransaction.BalanceTransaction = balanceTransaction //required
                IssuingTransaction.Card = card //required
                IssuingTransaction.Cardholder = cardholder //required
                IssuingTransaction.Created = created //required
                IssuingTransaction.Currency = currency //required
                IssuingTransaction.Dispute = dispute //required
                IssuingTransaction.Id = id //required
                IssuingTransaction.Livemode = livemode //required
                IssuingTransaction.MerchantAmount = merchantAmount //required
                IssuingTransaction.MerchantCurrency = merchantCurrency //required
                IssuingTransaction.MerchantData = merchantData //required
                IssuingTransaction.Metadata = metadata //required
                IssuingTransaction.NetworkData = networkData //required
                IssuingTransaction.Type = ``type`` //required
                IssuingTransaction.Wallet = wallet //required
                IssuingTransaction.PurchaseDetails = purchaseDetails |> Option.flatten
                IssuingTransaction.Token = token |> Option.flatten
                IssuingTransaction.Treasury = treasury |> Option.flatten
            }

    and IssuingTransactionAuthorization'AnyOf =
        | String of string
        | IssuingAuthorization of IssuingAuthorization

    and IssuingTransactionBalanceTransaction'AnyOf =
        | String of string
        | BalanceTransaction of BalanceTransaction

    and IssuingTransactionCard'AnyOf =
        | String of string
        | IssuingCard of IssuingCard

    and IssuingTransactionCardholder'AnyOf =
        | String of string
        | IssuingCardholder of IssuingCardholder

    and IssuingTransactionDispute'AnyOf =
        | String of string
        | IssuingDispute of IssuingDispute

    and IssuingTransactionToken'AnyOf =
        | String of string
        | IssuingToken of IssuingToken

    and IssuingAuthorizationAuthenticationExemption = {
        ///The entity that requested the exemption, either the acquiring merchant or the Issuing user.
        ClaimedBy: IssuingAuthorizationAuthenticationExemptionClaimedBy
        ///The specific exemption claimed for this authorization.
        Type: IssuingAuthorizationAuthenticationExemptionType
    }
    with
        static member New (claimedBy: IssuingAuthorizationAuthenticationExemptionClaimedBy, ``type``: IssuingAuthorizationAuthenticationExemptionType) =
            {
                IssuingAuthorizationAuthenticationExemption.ClaimedBy = claimedBy //required
                IssuingAuthorizationAuthenticationExemption.Type = ``type`` //required
            }

    and IssuingAuthorizationFleetData = {
        ///Answers to prompts presented to the cardholder at the point of sale. Prompted fields vary depending on the configuration of your physical fleet cards. Typical points of sale support only numeric entry.
        CardholderPromptData: IssuingAuthorizationFleetCardholderPromptData option
        ///The type of purchase.
        PurchaseType: IssuingAuthorizationFleetDataPurchaseType option
        ///More information about the total amount. Typically this information is received from the merchant after the authorization has been approved and the fuel dispensed. This information is not guaranteed to be accurate as some merchants may provide unreliable data.
        ReportedBreakdown: IssuingAuthorizationFleetReportedBreakdown option
        ///The type of fuel service.
        ServiceType: IssuingAuthorizationFleetDataServiceType option
    }
    with
        static member New (cardholderPromptData: IssuingAuthorizationFleetCardholderPromptData option, purchaseType: IssuingAuthorizationFleetDataPurchaseType option, reportedBreakdown: IssuingAuthorizationFleetReportedBreakdown option, serviceType: IssuingAuthorizationFleetDataServiceType option) =
            {
                IssuingAuthorizationFleetData.CardholderPromptData = cardholderPromptData //required
                IssuingAuthorizationFleetData.PurchaseType = purchaseType //required
                IssuingAuthorizationFleetData.ReportedBreakdown = reportedBreakdown //required
                IssuingAuthorizationFleetData.ServiceType = serviceType //required
            }

    and IssuingAuthorizationFleetReportedBreakdown = {
        ///Breakdown of fuel portion of the purchase.
        Fuel: IssuingAuthorizationFleetFuelPriceData option
        ///Breakdown of non-fuel portion of the purchase.
        NonFuel: IssuingAuthorizationFleetNonFuelPriceData option
        ///Information about tax included in this transaction.
        Tax: IssuingAuthorizationFleetTaxData option
    }
    with
        static member New (fuel: IssuingAuthorizationFleetFuelPriceData option, nonFuel: IssuingAuthorizationFleetNonFuelPriceData option, tax: IssuingAuthorizationFleetTaxData option) =
            {
                IssuingAuthorizationFleetReportedBreakdown.Fuel = fuel //required
                IssuingAuthorizationFleetReportedBreakdown.NonFuel = nonFuel //required
                IssuingAuthorizationFleetReportedBreakdown.Tax = tax //required
            }

    and IssuingAuthorizationFraudChallenge = {
        ///The status of the fraud challenge.
        Status: IssuingAuthorizationFraudChallengeStatus
        ///If the challenge is not deliverable, the reason why.
        UndeliverableReason: IssuingAuthorizationFraudChallengeUndeliverableReason option
    }
    with
        ///The method by which the fraud challenge was delivered to the cardholder.
        member _.Channel = "sms"

        static member New (status: IssuingAuthorizationFraudChallengeStatus, undeliverableReason: IssuingAuthorizationFraudChallengeUndeliverableReason option) =
            {
                IssuingAuthorizationFraudChallenge.Status = status //required
                IssuingAuthorizationFraudChallenge.UndeliverableReason = undeliverableReason //required
            }

    and IssuingAuthorizationFuelData = {
        ///[Conexxus Payment System Product Code](https://www.conexxus.org/conexxus-payment-system-product-codes) identifying the primary fuel product purchased.
        IndustryProductCode: string option
        ///The quantity of `unit`s of fuel that was dispensed, represented as a decimal string with at most 12 decimal places.
        QuantityDecimal: string option
        ///The type of fuel that was purchased.
        Type: IssuingAuthorizationFuelDataType option
        ///The units for `quantity_decimal`.
        Unit: IssuingAuthorizationFuelDataUnit option
        ///The cost in cents per each unit of fuel, represented as a decimal string with at most 12 decimal places.
        UnitCostDecimal: string option
    }
    with
        static member New (industryProductCode: string option, quantityDecimal: string option, ``type``: IssuingAuthorizationFuelDataType option, unit: IssuingAuthorizationFuelDataUnit option, unitCostDecimal: string option) =
            {
                IssuingAuthorizationFuelData.IndustryProductCode = industryProductCode //required
                IssuingAuthorizationFuelData.QuantityDecimal = quantityDecimal //required
                IssuingAuthorizationFuelData.Type = ``type`` //required
                IssuingAuthorizationFuelData.Unit = unit //required
                IssuingAuthorizationFuelData.UnitCostDecimal = unitCostDecimal //required
            }

    and IssuingAuthorizationMerchantData = {
        ///A categorization of the seller's type of business. See our [merchant categories guide](https://docs.stripe.com/issuing/merchant-categories) for a list of possible values.
        Category: string
        ///The merchant category code for the seller’s business
        CategoryCode: string
        ///City where the seller is located
        City: string option
        ///Country where the seller is located
        Country: IsoTypes.IsoCountryCode option
        ///Name of the seller
        Name: string option
        ///Identifier assigned to the seller by the card network. Different card networks may assign different network_id fields to the same merchant.
        NetworkId: string
        ///Postal code where the seller is located
        PostalCode: string option
        ///State where the seller is located
        State: string option
        ///The seller's tax identification number. Currently populated for French merchants only.
        TaxId: string option
        ///An ID assigned by the seller to the location of the sale.
        TerminalId: string option
        ///URL provided by the merchant on a 3DS request
        Url: string option
    }
    with
        static member New (category: string, categoryCode: string, city: string option, country: IsoTypes.IsoCountryCode option, name: string option, networkId: string, postalCode: string option, state: string option, taxId: string option, terminalId: string option, url: string option) =
            {
                IssuingAuthorizationMerchantData.Category = category //required
                IssuingAuthorizationMerchantData.CategoryCode = categoryCode //required
                IssuingAuthorizationMerchantData.City = city //required
                IssuingAuthorizationMerchantData.Country = country //required
                IssuingAuthorizationMerchantData.Name = name //required
                IssuingAuthorizationMerchantData.NetworkId = networkId //required
                IssuingAuthorizationMerchantData.PostalCode = postalCode //required
                IssuingAuthorizationMerchantData.State = state //required
                IssuingAuthorizationMerchantData.TaxId = taxId //required
                IssuingAuthorizationMerchantData.TerminalId = terminalId //required
                IssuingAuthorizationMerchantData.Url = url //required
            }

    and IssuingAuthorizationPendingRequest = {
        ///The additional amount Stripe will hold if the authorization is approved, in the card's [currency](https://docs.stripe.com/api#issuing_authorization_object-pending-request-currency) and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
        Amount: int
        ///Detailed breakdown of amount components. These amounts are denominated in `currency` and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
        AmountDetails: IssuingAuthorizationAmountDetails option
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///If set `true`, you may provide [amount](https://docs.stripe.com/api/issuing/authorizations/approve#approve_issuing_authorization-amount) to control how much to hold for the authorization.
        IsAmountControllable: bool
        ///The amount the merchant is requesting to be authorized in the `merchant_currency`. The amount is in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
        MerchantAmount: int
        ///The local currency the merchant is requesting to authorize.
        MerchantCurrency: IsoTypes.IsoCurrencyCode
        ///The card network's estimate of the likelihood that an authorization is fraudulent. Takes on values between 1 and 99.
        NetworkRiskScore: int option
    }
    with
        static member New (amount: int, amountDetails: IssuingAuthorizationAmountDetails option, currency: IsoTypes.IsoCurrencyCode, isAmountControllable: bool, merchantAmount: int, merchantCurrency: IsoTypes.IsoCurrencyCode, networkRiskScore: int option) =
            {
                IssuingAuthorizationPendingRequest.Amount = amount //required
                IssuingAuthorizationPendingRequest.AmountDetails = amountDetails //required
                IssuingAuthorizationPendingRequest.Currency = currency //required
                IssuingAuthorizationPendingRequest.IsAmountControllable = isAmountControllable //required
                IssuingAuthorizationPendingRequest.MerchantAmount = merchantAmount //required
                IssuingAuthorizationPendingRequest.MerchantCurrency = merchantCurrency //required
                IssuingAuthorizationPendingRequest.NetworkRiskScore = networkRiskScore //required
            }

    and IssuingAuthorizationRequest = {
        ///The `pending_request.amount` at the time of the request, presented in your card's currency and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). Stripe held this amount from your account to fund the authorization if the request was approved.
        Amount: int
        ///Detailed breakdown of amount components. These amounts are denominated in `currency` and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
        AmountDetails: IssuingAuthorizationAmountDetails option
        ///Whether this request was approved.
        Approved: bool
        ///A code created by Stripe which is shared with the merchant to validate the authorization. This field will be populated if the authorization message was approved. The code typically starts with the letter "S", followed by a six-digit number. For example, "S498162". Please note that the code is not guaranteed to be unique across authorizations.
        AuthorizationCode: string option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///The `pending_request.merchant_amount` at the time of the request, presented in the `merchant_currency` and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
        MerchantAmount: int
        ///The currency that was collected by the merchant and presented to the cardholder for the authorization. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        MerchantCurrency: IsoTypes.IsoCurrencyCode
        ///The card network's estimate of the likelihood that an authorization is fraudulent. Takes on values between 1 and 99.
        NetworkRiskScore: int option
        ///When an authorization is approved or declined by you or by Stripe, this field provides additional detail on the reason for the outcome.
        Reason: IssuingAuthorizationRequestReason
        ///If the `request_history.reason` is `webhook_error` because the direct webhook response is invalid (for example, parsing errors or missing parameters), we surface a more detailed error message via this field.
        ReasonMessage: string option
        ///Time when the card network received an authorization request from the acquirer in UTC. Referred to by networks as transmission time.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]RequestedAt: DateTime option
    }
    with
        static member New (amount: int, amountDetails: IssuingAuthorizationAmountDetails option, approved: bool, authorizationCode: string option, created: DateTime, currency: IsoTypes.IsoCurrencyCode, merchantAmount: int, merchantCurrency: IsoTypes.IsoCurrencyCode, networkRiskScore: int option, reason: IssuingAuthorizationRequestReason, reasonMessage: string option, requestedAt: DateTime option) =
            {
                IssuingAuthorizationRequest.Amount = amount //required
                IssuingAuthorizationRequest.AmountDetails = amountDetails //required
                IssuingAuthorizationRequest.Approved = approved //required
                IssuingAuthorizationRequest.AuthorizationCode = authorizationCode //required
                IssuingAuthorizationRequest.Created = created //required
                IssuingAuthorizationRequest.Currency = currency //required
                IssuingAuthorizationRequest.MerchantAmount = merchantAmount //required
                IssuingAuthorizationRequest.MerchantCurrency = merchantCurrency //required
                IssuingAuthorizationRequest.NetworkRiskScore = networkRiskScore //required
                IssuingAuthorizationRequest.Reason = reason //required
                IssuingAuthorizationRequest.ReasonMessage = reasonMessage //required
                IssuingAuthorizationRequest.RequestedAt = requestedAt //required
            }

    and IssuingAuthorizationThreeDSecure = {
        ///The outcome of the 3D Secure authentication request.
        Result: IssuingAuthorizationThreeDSecureResult
    }
    with
        static member New (result: IssuingAuthorizationThreeDSecureResult) =
            {
                IssuingAuthorizationThreeDSecure.Result = result //required
            }

    and IssuingAuthorizationTreasury = {
        ///The array of [ReceivedCredits](https://docs.stripe.com/api/treasury/received_credits) associated with this authorization
        ReceivedCredits: string list
        ///The array of [ReceivedDebits](https://docs.stripe.com/api/treasury/received_debits) associated with this authorization
        ReceivedDebits: string list
        ///The Treasury [Transaction](https://docs.stripe.com/api/treasury/transactions) associated with this authorization
        Transaction: string option
    }
    with
        static member New (receivedCredits: string list, receivedDebits: string list, transaction: string option) =
            {
                IssuingAuthorizationTreasury.ReceivedCredits = receivedCredits //required
                IssuingAuthorizationTreasury.ReceivedDebits = receivedDebits //required
                IssuingAuthorizationTreasury.Transaction = transaction //required
            }

    and IssuingAuthorizationVerificationData = {
        ///Whether the cardholder provided an address first line and if it matched the cardholder’s `billing.address.line1`.
        [<JsonField(Name="address_line1_check")>]AddressLine1Check: IssuingAuthorizationVerificationDataAddressLine1Check
        ///Whether the cardholder provided a postal code and if it matched the cardholder’s `billing.address.postal_code`.
        AddressPostalCodeCheck: IssuingAuthorizationVerificationDataAddressPostalCodeCheck
        ///The exemption applied to this authorization.
        AuthenticationExemption: IssuingAuthorizationAuthenticationExemption option
        ///Whether the cardholder provided a CVC and if it matched Stripe’s record.
        CvcCheck: IssuingAuthorizationVerificationDataCvcCheck
        ///Whether the cardholder provided an expiry date and if it matched Stripe’s record.
        ExpiryCheck: IssuingAuthorizationVerificationDataExpiryCheck
        ///The postal code submitted as part of the authorization used for postal code verification.
        PostalCode: string option
        ///3D Secure details.
        ThreeDSecure: IssuingAuthorizationThreeDSecure option
    }
    with
        static member New (addressLine1Check: IssuingAuthorizationVerificationDataAddressLine1Check, addressPostalCodeCheck: IssuingAuthorizationVerificationDataAddressPostalCodeCheck, authenticationExemption: IssuingAuthorizationAuthenticationExemption option, cvcCheck: IssuingAuthorizationVerificationDataCvcCheck, expiryCheck: IssuingAuthorizationVerificationDataExpiryCheck, postalCode: string option, threeDSecure: IssuingAuthorizationThreeDSecure option) =
            {
                IssuingAuthorizationVerificationData.AddressLine1Check = addressLine1Check //required
                IssuingAuthorizationVerificationData.AddressPostalCodeCheck = addressPostalCodeCheck //required
                IssuingAuthorizationVerificationData.AuthenticationExemption = authenticationExemption //required
                IssuingAuthorizationVerificationData.CvcCheck = cvcCheck //required
                IssuingAuthorizationVerificationData.ExpiryCheck = expiryCheck //required
                IssuingAuthorizationVerificationData.PostalCode = postalCode //required
                IssuingAuthorizationVerificationData.ThreeDSecure = threeDSecure //required
            }

    and IssuingCardApplePay = {
        ///Apple Pay Eligibility
        Eligible: bool
        ///Reason the card is ineligible for Apple Pay
        IneligibleReason: IssuingCardApplePayIneligibleReason option
    }
    with
        static member New (eligible: bool, ineligibleReason: IssuingCardApplePayIneligibleReason option) =
            {
                IssuingCardApplePay.Eligible = eligible //required
                IssuingCardApplePay.IneligibleReason = ineligibleReason //required
            }

    and IssuingCardAuthorizationControls = {
        ///Array of card presence statuses from which authorizations will be allowed. Possible options are `present`, `not_present`. All other statuses will be blocked. Cannot be set with `blocked_card_presences`. Provide an empty value to unset this control.
        AllowedCardPresences: IssuingCardAuthorizationControlsAllowedCardPresences list option
        ///Array of strings containing [categories](https://docs.stripe.com/api#issuing_authorization_object-merchant_data-category) of authorizations to allow. All other categories will be blocked. Cannot be set with `blocked_categories`.
        AllowedCategories: IssuingCardAuthorizationControlsAllowedCategories list option
        ///Array of strings containing representing countries from which authorizations will be allowed. Authorizations from merchants in all other countries will be declined. Country codes should be ISO 3166 alpha-2 country codes (e.g. `US`). Cannot be set with `blocked_merchant_countries`. Provide an empty value to unset this control.
        AllowedMerchantCountries: string list option
        ///Array of card presence statuses from which authorizations will be declined. Possible options are `present`, `not_present`. Cannot be set with `allowed_card_presences`. Provide an empty value to unset this control.
        BlockedCardPresences: IssuingCardAuthorizationControlsBlockedCardPresences list option
        ///Array of strings containing [categories](https://docs.stripe.com/api#issuing_authorization_object-merchant_data-category) of authorizations to decline. All other categories will be allowed. Cannot be set with `allowed_categories`.
        BlockedCategories: IssuingCardAuthorizationControlsBlockedCategories list option
        ///Array of strings containing representing countries from which authorizations will be declined. Country codes should be ISO 3166 alpha-2 country codes (e.g. `US`). Cannot be set with `allowed_merchant_countries`. Provide an empty value to unset this control.
        BlockedMerchantCountries: string list option
        ///Limit spending with amount-based rules that apply across any cards this card replaced (i.e., its `replacement_for` card and _that_ card's `replacement_for` card, up the chain).
        SpendingLimits: IssuingCardSpendingLimit list option
        ///Currency of the amounts within `spending_limits`. Always the same as the currency of the card.
        SpendingLimitsCurrency: IsoTypes.IsoCurrencyCode option
    }
    with
        static member New (allowedCardPresences: IssuingCardAuthorizationControlsAllowedCardPresences list option, allowedCategories: IssuingCardAuthorizationControlsAllowedCategories list option, allowedMerchantCountries: string list option, blockedCardPresences: IssuingCardAuthorizationControlsBlockedCardPresences list option, blockedCategories: IssuingCardAuthorizationControlsBlockedCategories list option, blockedMerchantCountries: string list option, spendingLimits: IssuingCardSpendingLimit list option, spendingLimitsCurrency: IsoTypes.IsoCurrencyCode option) =
            {
                IssuingCardAuthorizationControls.AllowedCardPresences = allowedCardPresences //required
                IssuingCardAuthorizationControls.AllowedCategories = allowedCategories //required
                IssuingCardAuthorizationControls.AllowedMerchantCountries = allowedMerchantCountries //required
                IssuingCardAuthorizationControls.BlockedCardPresences = blockedCardPresences //required
                IssuingCardAuthorizationControls.BlockedCategories = blockedCategories //required
                IssuingCardAuthorizationControls.BlockedMerchantCountries = blockedMerchantCountries //required
                IssuingCardAuthorizationControls.SpendingLimits = spendingLimits //required
                IssuingCardAuthorizationControls.SpendingLimitsCurrency = spendingLimitsCurrency //required
            }

    and IssuingCardFraudWarning = {
        ///Timestamp of the most recent fraud warning.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]StartedAt: DateTime option
        ///The type of fraud warning that most recently took place on this card. This field updates with every new fraud warning, so the value changes over time. If populated, cancel and reissue the card.
        Type: IssuingCardFraudWarningType option
    }
    with
        static member New (startedAt: DateTime option, ``type``: IssuingCardFraudWarningType option) =
            {
                IssuingCardFraudWarning.StartedAt = startedAt //required
                IssuingCardFraudWarning.Type = ``type`` //required
            }

    and IssuingCardGooglePay = {
        ///Google Pay Eligibility
        Eligible: bool
        ///Reason the card is ineligible for Google Pay
        IneligibleReason: IssuingCardGooglePayIneligibleReason option
    }
    with
        static member New (eligible: bool, ineligibleReason: IssuingCardGooglePayIneligibleReason option) =
            {
                IssuingCardGooglePay.Eligible = eligible //required
                IssuingCardGooglePay.IneligibleReason = ineligibleReason //required
            }

    and IssuingCardLifecycleControls = {
        CancelAfter: IssuingCardLifecycleConditions
    }
    with
        static member New (cancelAfter: IssuingCardLifecycleConditions) =
            {
                IssuingCardLifecycleControls.CancelAfter = cancelAfter //required
            }

    and IssuingCardShipping = {
        Address: Address
        ///Address validation details for the shipment.
        AddressValidation: IssuingCardShippingAddressValidation option
        ///The delivery company that shipped a card.
        Carrier: IssuingCardShippingCarrier option
        ///Additional information that may be required for clearing customs.
        Customs: IssuingCardShippingCustoms option
        ///A unix timestamp representing a best estimate of when the card will be delivered.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Eta: DateTime option
        ///Recipient name.
        Name: string
        ///The phone number of the receiver of the shipment. Our courier partners will use this number to contact you in the event of card delivery issues. For individual shipments to the EU/UK, if this field is empty, we will provide them with the phone number provided when the cardholder was initially created.
        PhoneNumber: string option
        ///Whether a signature is required for card delivery. This feature is only supported for US users. Standard shipping service does not support signature on delivery. The default value for standard shipping service is false and for express and priority services is true.
        RequireSignature: bool option
        ///Shipment service, such as `standard` or `express`.
        Service: IssuingCardShippingService
        ///The delivery status of the card.
        Status: IssuingCardShippingStatus option
        ///A tracking number for a card shipment.
        TrackingNumber: string option
        ///A link to the shipping carrier's site where you can view detailed information about a card shipment.
        TrackingUrl: string option
        ///Packaging options.
        Type: IssuingCardShippingType
    }
    with
        static member New (address: Address, addressValidation: IssuingCardShippingAddressValidation option, carrier: IssuingCardShippingCarrier option, customs: IssuingCardShippingCustoms option, eta: DateTime option, name: string, phoneNumber: string option, requireSignature: bool option, service: IssuingCardShippingService, status: IssuingCardShippingStatus option, trackingNumber: string option, trackingUrl: string option, ``type``: IssuingCardShippingType) =
            {
                IssuingCardShipping.Address = address //required
                IssuingCardShipping.AddressValidation = addressValidation //required
                IssuingCardShipping.Carrier = carrier //required
                IssuingCardShipping.Customs = customs //required
                IssuingCardShipping.Eta = eta //required
                IssuingCardShipping.Name = name //required
                IssuingCardShipping.PhoneNumber = phoneNumber //required
                IssuingCardShipping.RequireSignature = requireSignature //required
                IssuingCardShipping.Service = service //required
                IssuingCardShipping.Status = status //required
                IssuingCardShipping.TrackingNumber = trackingNumber //required
                IssuingCardShipping.TrackingUrl = trackingUrl //required
                IssuingCardShipping.Type = ``type`` //required
            }

    and IssuingCardShippingAddressValidation = {
        ///The address validation capabilities to use.
        Mode: IssuingCardShippingAddressValidationMode
        ///The normalized shipping address.
        NormalizedAddress: Address option
        ///The validation result for the shipping address.
        Result: IssuingCardShippingAddressValidationResult option
    }
    with
        static member New (mode: IssuingCardShippingAddressValidationMode, normalizedAddress: Address option, result: IssuingCardShippingAddressValidationResult option) =
            {
                IssuingCardShippingAddressValidation.Mode = mode //required
                IssuingCardShippingAddressValidation.NormalizedAddress = normalizedAddress //required
                IssuingCardShippingAddressValidation.Result = result //required
            }

    and IssuingCardSpendingLimit = {
        ///Maximum amount allowed to spend per interval. This amount is in the card's currency and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
        Amount: int
        ///Array of strings containing [categories](https://docs.stripe.com/api#issuing_authorization_object-merchant_data-category) this limit applies to. Omitting this field will apply the limit to all categories.
        Categories: IssuingCardSpendingLimitCategories list option
        ///Interval (or event) to which the amount applies.
        Interval: IssuingCardSpendingLimitInterval
    }
    with
        static member New (amount: int, categories: IssuingCardSpendingLimitCategories list option, interval: IssuingCardSpendingLimitInterval) =
            {
                IssuingCardSpendingLimit.Amount = amount //required
                IssuingCardSpendingLimit.Categories = categories //required
                IssuingCardSpendingLimit.Interval = interval //required
            }

    and IssuingCardWallets = {
        ApplePay: IssuingCardApplePay
        GooglePay: IssuingCardGooglePay
        ///Unique identifier for a card used with digital wallets
        PrimaryAccountIdentifier: string option
    }
    with
        static member New (applePay: IssuingCardApplePay, googlePay: IssuingCardGooglePay, primaryAccountIdentifier: string option) =
            {
                IssuingCardWallets.ApplePay = applePay //required
                IssuingCardWallets.GooglePay = googlePay //required
                IssuingCardWallets.PrimaryAccountIdentifier = primaryAccountIdentifier //required
            }

    and IssuingCardholderAddress = {
        Address: Address
    }
    with
        static member New (address: Address) =
            {
                IssuingCardholderAddress.Address = address //required
            }

    and IssuingCardholderAuthorizationControls = {
        ///Array of card presence statuses from which authorizations will be allowed. Possible options are `present`, `not_present`. All other statuses will be blocked. Cannot be set with `blocked_card_presences`. Provide an empty value to unset this control.
        AllowedCardPresences: IssuingCardholderAuthorizationControlsAllowedCardPresences list option
        ///Array of strings containing [categories](https://docs.stripe.com/api#issuing_authorization_object-merchant_data-category) of authorizations to allow. All other categories will be blocked. Cannot be set with `blocked_categories`.
        AllowedCategories: IssuingCardholderAuthorizationControlsAllowedCategories list option
        ///Array of strings containing representing countries from which authorizations will be allowed. Authorizations from merchants in all other countries will be declined. Country codes should be ISO 3166 alpha-2 country codes (e.g. `US`). Cannot be set with `blocked_merchant_countries`. Provide an empty value to unset this control.
        AllowedMerchantCountries: string list option
        ///Array of card presence statuses from which authorizations will be declined. Possible options are `present`, `not_present`. Cannot be set with `allowed_card_presences`. Provide an empty value to unset this control.
        BlockedCardPresences: IssuingCardholderAuthorizationControlsBlockedCardPresences list option
        ///Array of strings containing [categories](https://docs.stripe.com/api#issuing_authorization_object-merchant_data-category) of authorizations to decline. All other categories will be allowed. Cannot be set with `allowed_categories`.
        BlockedCategories: IssuingCardholderAuthorizationControlsBlockedCategories list option
        ///Array of strings containing representing countries from which authorizations will be declined. Country codes should be ISO 3166 alpha-2 country codes (e.g. `US`). Cannot be set with `allowed_merchant_countries`. Provide an empty value to unset this control.
        BlockedMerchantCountries: string list option
        ///Limit spending with amount-based rules that apply across this cardholder's cards.
        SpendingLimits: IssuingCardholderSpendingLimit list option
        ///Currency of the amounts within `spending_limits`.
        SpendingLimitsCurrency: IsoTypes.IsoCurrencyCode option
    }
    with
        static member New (allowedCardPresences: IssuingCardholderAuthorizationControlsAllowedCardPresences list option, allowedCategories: IssuingCardholderAuthorizationControlsAllowedCategories list option, allowedMerchantCountries: string list option, blockedCardPresences: IssuingCardholderAuthorizationControlsBlockedCardPresences list option, blockedCategories: IssuingCardholderAuthorizationControlsBlockedCategories list option, blockedMerchantCountries: string list option, spendingLimits: IssuingCardholderSpendingLimit list option, spendingLimitsCurrency: IsoTypes.IsoCurrencyCode option) =
            {
                IssuingCardholderAuthorizationControls.AllowedCardPresences = allowedCardPresences //required
                IssuingCardholderAuthorizationControls.AllowedCategories = allowedCategories //required
                IssuingCardholderAuthorizationControls.AllowedMerchantCountries = allowedMerchantCountries //required
                IssuingCardholderAuthorizationControls.BlockedCardPresences = blockedCardPresences //required
                IssuingCardholderAuthorizationControls.BlockedCategories = blockedCategories //required
                IssuingCardholderAuthorizationControls.BlockedMerchantCountries = blockedMerchantCountries //required
                IssuingCardholderAuthorizationControls.SpendingLimits = spendingLimits //required
                IssuingCardholderAuthorizationControls.SpendingLimitsCurrency = spendingLimitsCurrency //required
            }

    and IssuingCardholderCardIssuing = {
        ///Information about cardholder acceptance of Celtic [Authorized User Terms](https://stripe.com/docs/issuing/cards#accept-authorized-user-terms). Required for cards backed by a Celtic program.
        UserTermsAcceptance: IssuingCardholderUserTermsAcceptance option
    }
    with
        static member New (userTermsAcceptance: IssuingCardholderUserTermsAcceptance option) =
            {
                IssuingCardholderCardIssuing.UserTermsAcceptance = userTermsAcceptance //required
            }

    and IssuingCardholderIdDocument = {
        ///The back of a document returned by a [file upload](https://api.stripe.com#create_file) with a `purpose` value of `identity_document`.
        Back: IssuingCardholderIdDocumentBack'AnyOf option
        ///The front of a document returned by a [file upload](https://api.stripe.com#create_file) with a `purpose` value of `identity_document`.
        Front: IssuingCardholderIdDocumentFront'AnyOf option
    }
    with
        static member New (back: IssuingCardholderIdDocumentBack'AnyOf option, front: IssuingCardholderIdDocumentFront'AnyOf option) =
            {
                IssuingCardholderIdDocument.Back = back //required
                IssuingCardholderIdDocument.Front = front //required
            }

    and IssuingCardholderIdDocumentBack'AnyOf =
        | String of string
        | File of File

    and IssuingCardholderIdDocumentFront'AnyOf =
        | String of string
        | File of File

    and IssuingCardholderIndividual = {
        ///Information related to the card_issuing program for this cardholder.
        CardIssuing: IssuingCardholderCardIssuing option
        ///The date of birth of this cardholder.
        Dob: IssuingCardholderIndividualDob option
        ///The first name of this cardholder. Required before activating Cards. This field cannot contain any numbers, special characters (except periods, commas, hyphens, spaces and apostrophes) or non-latin letters.
        FirstName: string option
        ///The last name of this cardholder. Required before activating Cards. This field cannot contain any numbers, special characters (except periods, commas, hyphens, spaces and apostrophes) or non-latin letters.
        LastName: string option
        ///Government-issued ID document for this cardholder.
        Verification: IssuingCardholderVerification option
    }
    with
        static member New (dob: IssuingCardholderIndividualDob option, firstName: string option, lastName: string option, verification: IssuingCardholderVerification option, ?cardIssuing: IssuingCardholderCardIssuing option) =
            {
                IssuingCardholderIndividual.Dob = dob //required
                IssuingCardholderIndividual.FirstName = firstName //required
                IssuingCardholderIndividual.LastName = lastName //required
                IssuingCardholderIndividual.Verification = verification //required
                IssuingCardholderIndividual.CardIssuing = cardIssuing |> Option.flatten
            }

    and IssuingCardholderRequirements = {
        ///If `disabled_reason` is present, all cards will decline authorizations with `cardholder_verification_required` reason.
        DisabledReason: IssuingCardholderRequirementsDisabledReason option
        ///Array of fields that need to be collected in order to verify and re-enable the cardholder.
        PastDue: IssuingCardholderRequirementsPastDue list option
    }
    with
        static member New (disabledReason: IssuingCardholderRequirementsDisabledReason option, pastDue: IssuingCardholderRequirementsPastDue list option) =
            {
                IssuingCardholderRequirements.DisabledReason = disabledReason //required
                IssuingCardholderRequirements.PastDue = pastDue //required
            }

    and IssuingCardholderSpendingLimit = {
        ///Maximum amount allowed to spend per interval. This amount is in the card's currency and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
        Amount: int
        ///Array of strings containing [categories](https://docs.stripe.com/api#issuing_authorization_object-merchant_data-category) this limit applies to. Omitting this field will apply the limit to all categories.
        Categories: IssuingCardholderSpendingLimitCategories list option
        ///Interval (or event) to which the amount applies.
        Interval: IssuingCardholderSpendingLimitInterval
    }
    with
        static member New (amount: int, categories: IssuingCardholderSpendingLimitCategories list option, interval: IssuingCardholderSpendingLimitInterval) =
            {
                IssuingCardholderSpendingLimit.Amount = amount //required
                IssuingCardholderSpendingLimit.Categories = categories //required
                IssuingCardholderSpendingLimit.Interval = interval //required
            }

    and IssuingCardholderVerification = {
        ///An identifying document, either a passport or local ID card.
        Document: IssuingCardholderIdDocument option
    }
    with
        static member New (document: IssuingCardholderIdDocument option) =
            {
                IssuingCardholderVerification.Document = document //required
            }

    and IssuingDisputeCanceledEvidence = {
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Additional documentation supporting the dispute.
        AdditionalDocumentation: IssuingDisputeCanceledEvidenceAdditionalDocumentation'AnyOf option
        ///Date when order was canceled.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CanceledAt: DateTime option
        ///Whether the cardholder was provided with a cancellation policy.
        CancellationPolicyProvided: bool option
        ///Reason for canceling the order.
        CancellationReason: string option
        ///Date when the cardholder expected to receive the product.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpectedAt: DateTime option
        ///Explanation of why the cardholder is disputing this transaction.
        Explanation: string option
        ///Description of the merchandise or service that was purchased.
        ProductDescription: string option
        ///Whether the product was a merchandise or service.
        ProductType: IssuingDisputeCanceledEvidenceProductType option
        ///Result of cardholder's attempt to return the product.
        ReturnStatus: IssuingDisputeCanceledEvidenceReturnStatus option
        ///Date when the product was returned or attempted to be returned.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ReturnedAt: DateTime option
    }
    with
        static member New (additionalDocumentation: IssuingDisputeCanceledEvidenceAdditionalDocumentation'AnyOf option, canceledAt: DateTime option, cancellationPolicyProvided: bool option, cancellationReason: string option, expectedAt: DateTime option, explanation: string option, productDescription: string option, productType: IssuingDisputeCanceledEvidenceProductType option, returnStatus: IssuingDisputeCanceledEvidenceReturnStatus option, returnedAt: DateTime option) =
            {
                IssuingDisputeCanceledEvidence.AdditionalDocumentation = additionalDocumentation //required
                IssuingDisputeCanceledEvidence.CanceledAt = canceledAt //required
                IssuingDisputeCanceledEvidence.CancellationPolicyProvided = cancellationPolicyProvided //required
                IssuingDisputeCanceledEvidence.CancellationReason = cancellationReason //required
                IssuingDisputeCanceledEvidence.ExpectedAt = expectedAt //required
                IssuingDisputeCanceledEvidence.Explanation = explanation //required
                IssuingDisputeCanceledEvidence.ProductDescription = productDescription //required
                IssuingDisputeCanceledEvidence.ProductType = productType //required
                IssuingDisputeCanceledEvidence.ReturnStatus = returnStatus //required
                IssuingDisputeCanceledEvidence.ReturnedAt = returnedAt //required
            }

    and IssuingDisputeCanceledEvidenceAdditionalDocumentation'AnyOf =
        | String of string
        | File of File

    and IssuingDisputeDuplicateEvidence = {
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Additional documentation supporting the dispute.
        AdditionalDocumentation: IssuingDisputeDuplicateEvidenceAdditionalDocumentation'AnyOf option
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Copy of the card statement showing that the product had already been paid for.
        CardStatement: IssuingDisputeDuplicateEvidenceCardStatement'AnyOf option
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Copy of the receipt showing that the product had been paid for in cash.
        CashReceipt: IssuingDisputeDuplicateEvidenceCashReceipt'AnyOf option
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Image of the front and back of the check that was used to pay for the product.
        CheckImage: IssuingDisputeDuplicateEvidenceCheckImage'AnyOf option
        ///Explanation of why the cardholder is disputing this transaction.
        Explanation: string option
        ///Transaction (e.g., ipi_...) that the disputed transaction is a duplicate of. Of the two or more transactions that are copies of each other, this is original undisputed one.
        OriginalTransaction: string option
    }
    with
        static member New (additionalDocumentation: IssuingDisputeDuplicateEvidenceAdditionalDocumentation'AnyOf option, cardStatement: IssuingDisputeDuplicateEvidenceCardStatement'AnyOf option, cashReceipt: IssuingDisputeDuplicateEvidenceCashReceipt'AnyOf option, checkImage: IssuingDisputeDuplicateEvidenceCheckImage'AnyOf option, explanation: string option, originalTransaction: string option) =
            {
                IssuingDisputeDuplicateEvidence.AdditionalDocumentation = additionalDocumentation //required
                IssuingDisputeDuplicateEvidence.CardStatement = cardStatement //required
                IssuingDisputeDuplicateEvidence.CashReceipt = cashReceipt //required
                IssuingDisputeDuplicateEvidence.CheckImage = checkImage //required
                IssuingDisputeDuplicateEvidence.Explanation = explanation //required
                IssuingDisputeDuplicateEvidence.OriginalTransaction = originalTransaction //required
            }

    and IssuingDisputeDuplicateEvidenceAdditionalDocumentation'AnyOf =
        | String of string
        | File of File

    and IssuingDisputeDuplicateEvidenceCardStatement'AnyOf =
        | String of string
        | File of File

    and IssuingDisputeDuplicateEvidenceCashReceipt'AnyOf =
        | String of string
        | File of File

    and IssuingDisputeDuplicateEvidenceCheckImage'AnyOf =
        | String of string
        | File of File

    and IssuingDisputeEvidence = {
        Canceled: IssuingDisputeCanceledEvidence option
        Duplicate: IssuingDisputeDuplicateEvidence option
        Fraudulent: IssuingDisputeFraudulentEvidence option
        MerchandiseNotAsDescribed: IssuingDisputeMerchandiseNotAsDescribedEvidence option
        NoValidAuthorization: IssuingDisputeNoValidAuthorizationEvidence option
        NotReceived: IssuingDisputeNotReceivedEvidence option
        Other: IssuingDisputeOtherEvidence option
        ///The reason for filing the dispute. Its value will match the field containing the evidence.
        Reason: IssuingDisputeEvidenceReason
        ServiceNotAsDescribed: IssuingDisputeServiceNotAsDescribedEvidence option
    }
    with
        static member New (reason: IssuingDisputeEvidenceReason, ?canceled: IssuingDisputeCanceledEvidence, ?duplicate: IssuingDisputeDuplicateEvidence, ?fraudulent: IssuingDisputeFraudulentEvidence, ?merchandiseNotAsDescribed: IssuingDisputeMerchandiseNotAsDescribedEvidence, ?noValidAuthorization: IssuingDisputeNoValidAuthorizationEvidence, ?notReceived: IssuingDisputeNotReceivedEvidence, ?other: IssuingDisputeOtherEvidence, ?serviceNotAsDescribed: IssuingDisputeServiceNotAsDescribedEvidence) =
            {
                IssuingDisputeEvidence.Reason = reason //required
                IssuingDisputeEvidence.Canceled = canceled
                IssuingDisputeEvidence.Duplicate = duplicate
                IssuingDisputeEvidence.Fraudulent = fraudulent
                IssuingDisputeEvidence.MerchandiseNotAsDescribed = merchandiseNotAsDescribed
                IssuingDisputeEvidence.NoValidAuthorization = noValidAuthorization
                IssuingDisputeEvidence.NotReceived = notReceived
                IssuingDisputeEvidence.Other = other
                IssuingDisputeEvidence.ServiceNotAsDescribed = serviceNotAsDescribed
            }

    and IssuingDisputeFraudulentEvidence = {
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Additional documentation supporting the dispute.
        AdditionalDocumentation: IssuingDisputeFraudulentEvidenceAdditionalDocumentation'AnyOf option
        ///Explanation of why the cardholder is disputing this transaction.
        Explanation: string option
    }
    with
        static member New (additionalDocumentation: IssuingDisputeFraudulentEvidenceAdditionalDocumentation'AnyOf option, explanation: string option) =
            {
                IssuingDisputeFraudulentEvidence.AdditionalDocumentation = additionalDocumentation //required
                IssuingDisputeFraudulentEvidence.Explanation = explanation //required
            }

    and IssuingDisputeFraudulentEvidenceAdditionalDocumentation'AnyOf =
        | String of string
        | File of File

    and IssuingDisputeMerchandiseNotAsDescribedEvidence = {
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Additional documentation supporting the dispute.
        AdditionalDocumentation: IssuingDisputeMerchandiseNotAsDescribedEvidenceAdditionalDocumentation'AnyOf option
        ///Explanation of why the cardholder is disputing this transaction.
        Explanation: string option
        ///Date when the product was received.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ReceivedAt: DateTime option
        ///Description of the cardholder's attempt to return the product.
        ReturnDescription: string option
        ///Result of cardholder's attempt to return the product.
        ReturnStatus: IssuingDisputeMerchandiseNotAsDescribedEvidenceReturnStatus option
        ///Date when the product was returned or attempted to be returned.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ReturnedAt: DateTime option
    }
    with
        static member New (additionalDocumentation: IssuingDisputeMerchandiseNotAsDescribedEvidenceAdditionalDocumentation'AnyOf option, explanation: string option, receivedAt: DateTime option, returnDescription: string option, returnStatus: IssuingDisputeMerchandiseNotAsDescribedEvidenceReturnStatus option, returnedAt: DateTime option) =
            {
                IssuingDisputeMerchandiseNotAsDescribedEvidence.AdditionalDocumentation = additionalDocumentation //required
                IssuingDisputeMerchandiseNotAsDescribedEvidence.Explanation = explanation //required
                IssuingDisputeMerchandiseNotAsDescribedEvidence.ReceivedAt = receivedAt //required
                IssuingDisputeMerchandiseNotAsDescribedEvidence.ReturnDescription = returnDescription //required
                IssuingDisputeMerchandiseNotAsDescribedEvidence.ReturnStatus = returnStatus //required
                IssuingDisputeMerchandiseNotAsDescribedEvidence.ReturnedAt = returnedAt //required
            }

    and IssuingDisputeMerchandiseNotAsDescribedEvidenceAdditionalDocumentation'AnyOf =
        | String of string
        | File of File

    and IssuingDisputeNoValidAuthorizationEvidence = {
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Additional documentation supporting the dispute.
        AdditionalDocumentation: IssuingDisputeNoValidAuthorizationEvidenceAdditionalDocumentation'AnyOf option
        ///Explanation of why the cardholder is disputing this transaction.
        Explanation: string option
    }
    with
        static member New (additionalDocumentation: IssuingDisputeNoValidAuthorizationEvidenceAdditionalDocumentation'AnyOf option, explanation: string option) =
            {
                IssuingDisputeNoValidAuthorizationEvidence.AdditionalDocumentation = additionalDocumentation //required
                IssuingDisputeNoValidAuthorizationEvidence.Explanation = explanation //required
            }

    and IssuingDisputeNoValidAuthorizationEvidenceAdditionalDocumentation'AnyOf =
        | String of string
        | File of File

    and IssuingDisputeNotReceivedEvidence = {
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Additional documentation supporting the dispute.
        AdditionalDocumentation: IssuingDisputeNotReceivedEvidenceAdditionalDocumentation'AnyOf option
        ///Date when the cardholder expected to receive the product.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpectedAt: DateTime option
        ///Explanation of why the cardholder is disputing this transaction.
        Explanation: string option
        ///Description of the merchandise or service that was purchased.
        ProductDescription: string option
        ///Whether the product was a merchandise or service.
        ProductType: IssuingDisputeNotReceivedEvidenceProductType option
    }
    with
        static member New (additionalDocumentation: IssuingDisputeNotReceivedEvidenceAdditionalDocumentation'AnyOf option, expectedAt: DateTime option, explanation: string option, productDescription: string option, productType: IssuingDisputeNotReceivedEvidenceProductType option) =
            {
                IssuingDisputeNotReceivedEvidence.AdditionalDocumentation = additionalDocumentation //required
                IssuingDisputeNotReceivedEvidence.ExpectedAt = expectedAt //required
                IssuingDisputeNotReceivedEvidence.Explanation = explanation //required
                IssuingDisputeNotReceivedEvidence.ProductDescription = productDescription //required
                IssuingDisputeNotReceivedEvidence.ProductType = productType //required
            }

    and IssuingDisputeNotReceivedEvidenceAdditionalDocumentation'AnyOf =
        | String of string
        | File of File

    and IssuingDisputeOtherEvidence = {
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Additional documentation supporting the dispute.
        AdditionalDocumentation: IssuingDisputeOtherEvidenceAdditionalDocumentation'AnyOf option
        ///Explanation of why the cardholder is disputing this transaction.
        Explanation: string option
        ///Description of the merchandise or service that was purchased.
        ProductDescription: string option
        ///Whether the product was a merchandise or service.
        ProductType: IssuingDisputeOtherEvidenceProductType option
    }
    with
        static member New (additionalDocumentation: IssuingDisputeOtherEvidenceAdditionalDocumentation'AnyOf option, explanation: string option, productDescription: string option, productType: IssuingDisputeOtherEvidenceProductType option) =
            {
                IssuingDisputeOtherEvidence.AdditionalDocumentation = additionalDocumentation //required
                IssuingDisputeOtherEvidence.Explanation = explanation //required
                IssuingDisputeOtherEvidence.ProductDescription = productDescription //required
                IssuingDisputeOtherEvidence.ProductType = productType //required
            }

    and IssuingDisputeOtherEvidenceAdditionalDocumentation'AnyOf =
        | String of string
        | File of File

    and IssuingDisputeServiceNotAsDescribedEvidence = {
        ///(ID of a [file upload](https://stripe.com/docs/guides/file-upload)) Additional documentation supporting the dispute.
        AdditionalDocumentation: IssuingDisputeServiceNotAsDescribedEvidenceAdditionalDocumentation'AnyOf option
        ///Date when order was canceled.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CanceledAt: DateTime option
        ///Reason for canceling the order.
        CancellationReason: string option
        ///Explanation of why the cardholder is disputing this transaction.
        Explanation: string option
        ///Date when the product was received.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ReceivedAt: DateTime option
    }
    with
        static member New (additionalDocumentation: IssuingDisputeServiceNotAsDescribedEvidenceAdditionalDocumentation'AnyOf option, canceledAt: DateTime option, cancellationReason: string option, explanation: string option, receivedAt: DateTime option) =
            {
                IssuingDisputeServiceNotAsDescribedEvidence.AdditionalDocumentation = additionalDocumentation //required
                IssuingDisputeServiceNotAsDescribedEvidence.CanceledAt = canceledAt //required
                IssuingDisputeServiceNotAsDescribedEvidence.CancellationReason = cancellationReason //required
                IssuingDisputeServiceNotAsDescribedEvidence.Explanation = explanation //required
                IssuingDisputeServiceNotAsDescribedEvidence.ReceivedAt = receivedAt //required
            }

    and IssuingDisputeServiceNotAsDescribedEvidenceAdditionalDocumentation'AnyOf =
        | String of string
        | File of File

    and IssuingNetworkTokenDevice = {
        ///An obfuscated ID derived from the device ID.
        DeviceFingerprint: string option
        ///The IP address of the device at provisioning time.
        IpAddress: string option
        ///The geographic latitude/longitude coordinates of the device at provisioning time. The format is [+-]decimal/[+-]decimal.
        Location: string option
        ///The name of the device used for tokenization.
        Name: string option
        ///The phone number of the device used for tokenization.
        PhoneNumber: string option
        ///The type of device used for tokenization.
        Type: IssuingNetworkTokenDeviceType option
    }
    with
        static member New (?deviceFingerprint: string, ?ipAddress: string, ?location: string, ?name: string, ?phoneNumber: string, ?``type``: IssuingNetworkTokenDeviceType) =
            {
                IssuingNetworkTokenDevice.DeviceFingerprint = deviceFingerprint
                IssuingNetworkTokenDevice.IpAddress = ipAddress
                IssuingNetworkTokenDevice.Location = location
                IssuingNetworkTokenDevice.Name = name
                IssuingNetworkTokenDevice.PhoneNumber = phoneNumber
                IssuingNetworkTokenDevice.Type = ``type``
            }

    and IssuingNetworkTokenNetworkData = {
        Device: IssuingNetworkTokenDevice option
        Mastercard: IssuingNetworkTokenMastercard option
        ///The network that the token is associated with. An additional hash is included with a name matching this value, containing tokenization data specific to the card network.
        Type: IssuingNetworkTokenNetworkDataType
        Visa: IssuingNetworkTokenVisa option
        WalletProvider: IssuingNetworkTokenWalletProvider option
    }
    with
        static member New (``type``: IssuingNetworkTokenNetworkDataType, ?device: IssuingNetworkTokenDevice, ?mastercard: IssuingNetworkTokenMastercard, ?visa: IssuingNetworkTokenVisa, ?walletProvider: IssuingNetworkTokenWalletProvider) =
            {
                IssuingNetworkTokenNetworkData.Type = ``type`` //required
                IssuingNetworkTokenNetworkData.Device = device
                IssuingNetworkTokenNetworkData.Mastercard = mastercard
                IssuingNetworkTokenNetworkData.Visa = visa
                IssuingNetworkTokenNetworkData.WalletProvider = walletProvider
            }

    and IssuingNetworkTokenWalletProvider = {
        ///The wallet provider-given account ID of the digital wallet the token belongs to.
        AccountId: string option
        ///An evaluation on the trustworthiness of the wallet account between 1 and 5. A higher score indicates more trustworthy.
        AccountTrustScore: int option
        ///The method used for tokenizing a card.
        CardNumberSource: IssuingNetworkTokenWalletProviderCardNumberSource option
        CardholderAddress: IssuingNetworkTokenAddress option
        ///The name of the cardholder tokenizing the card.
        CardholderName: string option
        ///An evaluation on the trustworthiness of the device. A higher score indicates more trustworthy.
        DeviceTrustScore: int option
        ///The hashed email address of the cardholder's account with the wallet provider.
        HashedAccountEmailAddress: string option
        ///The reasons for suggested tokenization given by the card network.
        ReasonCodes: IssuingNetworkTokenWalletProviderReasonCodes list option
        ///The recommendation on responding to the tokenization request.
        SuggestedDecision: IssuingNetworkTokenWalletProviderSuggestedDecision option
        ///The version of the standard for mapping reason codes followed by the wallet provider.
        SuggestedDecisionVersion: string option
    }
    with
        static member New (?accountId: string, ?accountTrustScore: int, ?cardNumberSource: IssuingNetworkTokenWalletProviderCardNumberSource, ?cardholderAddress: IssuingNetworkTokenAddress, ?cardholderName: string, ?deviceTrustScore: int, ?hashedAccountEmailAddress: string, ?reasonCodes: IssuingNetworkTokenWalletProviderReasonCodes list, ?suggestedDecision: IssuingNetworkTokenWalletProviderSuggestedDecision, ?suggestedDecisionVersion: string) =
            {
                IssuingNetworkTokenWalletProvider.AccountId = accountId
                IssuingNetworkTokenWalletProvider.AccountTrustScore = accountTrustScore
                IssuingNetworkTokenWalletProvider.CardNumberSource = cardNumberSource
                IssuingNetworkTokenWalletProvider.CardholderAddress = cardholderAddress
                IssuingNetworkTokenWalletProvider.CardholderName = cardholderName
                IssuingNetworkTokenWalletProvider.DeviceTrustScore = deviceTrustScore
                IssuingNetworkTokenWalletProvider.HashedAccountEmailAddress = hashedAccountEmailAddress
                IssuingNetworkTokenWalletProvider.ReasonCodes = reasonCodes
                IssuingNetworkTokenWalletProvider.SuggestedDecision = suggestedDecision
                IssuingNetworkTokenWalletProvider.SuggestedDecisionVersion = suggestedDecisionVersion
            }

    and IssuingPersonalizationDesignRejectionReasons = {
        ///The reason(s) the card logo was rejected.
        CardLogo: IssuingPersonalizationDesignRejectionReasonsCardLogo list option
        ///The reason(s) the carrier text was rejected.
        CarrierText: IssuingPersonalizationDesignRejectionReasonsCarrierText list option
    }
    with
        static member New (cardLogo: IssuingPersonalizationDesignRejectionReasonsCardLogo list option, carrierText: IssuingPersonalizationDesignRejectionReasonsCarrierText list option) =
            {
                IssuingPersonalizationDesignRejectionReasons.CardLogo = cardLogo //required
                IssuingPersonalizationDesignRejectionReasons.CarrierText = carrierText //required
            }

    and IssuingPhysicalBundleFeatures = {
        ///The policy for how to use card logo images in a card design with this physical bundle.
        CardLogo: IssuingPhysicalBundleFeaturesCardLogo
        ///The policy for how to use carrier letter text in a card design with this physical bundle.
        CarrierText: IssuingPhysicalBundleFeaturesCarrierText
        ///The policy for how to use a second line on a card with this physical bundle.
        SecondLine: IssuingPhysicalBundleFeaturesSecondLine
    }
    with
        static member New (cardLogo: IssuingPhysicalBundleFeaturesCardLogo, carrierText: IssuingPhysicalBundleFeaturesCarrierText, secondLine: IssuingPhysicalBundleFeaturesSecondLine) =
            {
                IssuingPhysicalBundleFeatures.CardLogo = cardLogo //required
                IssuingPhysicalBundleFeatures.CarrierText = carrierText //required
                IssuingPhysicalBundleFeatures.SecondLine = secondLine //required
            }

    and IssuingTransactionFleetData = {
        ///Answers to prompts presented to cardholder at point of sale.
        CardholderPromptData: IssuingTransactionFleetCardholderPromptData option
        ///The type of purchase. One of `fuel_purchase`, `non_fuel_purchase`, or `fuel_and_non_fuel_purchase`.
        PurchaseType: IssuingTransactionFleetDataPurchaseType option
        ///More information about the total amount. This information is not guaranteed to be accurate as some merchants may provide unreliable data.
        ReportedBreakdown: IssuingTransactionFleetReportedBreakdown option
        ///The type of fuel service. One of `non_fuel_transaction`, `full_service`, or `self_service`.
        ServiceType: IssuingTransactionFleetDataServiceType option
    }
    with
        static member New (cardholderPromptData: IssuingTransactionFleetCardholderPromptData option, purchaseType: IssuingTransactionFleetDataPurchaseType option, reportedBreakdown: IssuingTransactionFleetReportedBreakdown option, serviceType: IssuingTransactionFleetDataServiceType option) =
            {
                IssuingTransactionFleetData.CardholderPromptData = cardholderPromptData //required
                IssuingTransactionFleetData.PurchaseType = purchaseType //required
                IssuingTransactionFleetData.ReportedBreakdown = reportedBreakdown //required
                IssuingTransactionFleetData.ServiceType = serviceType //required
            }

    and IssuingTransactionFleetReportedBreakdown = {
        ///Breakdown of fuel portion of the purchase.
        Fuel: IssuingTransactionFleetFuelPriceData option
        ///Breakdown of non-fuel portion of the purchase.
        NonFuel: IssuingTransactionFleetNonFuelPriceData option
        ///Information about tax included in this transaction.
        Tax: IssuingTransactionFleetTaxData option
    }
    with
        static member New (fuel: IssuingTransactionFleetFuelPriceData option, nonFuel: IssuingTransactionFleetNonFuelPriceData option, tax: IssuingTransactionFleetTaxData option) =
            {
                IssuingTransactionFleetReportedBreakdown.Fuel = fuel //required
                IssuingTransactionFleetReportedBreakdown.NonFuel = nonFuel //required
                IssuingTransactionFleetReportedBreakdown.Tax = tax //required
            }

    and IssuingTransactionFlightData = {
        ///The time that the flight departed.
        DepartureAt: int option
        ///The name of the passenger.
        PassengerName: string option
        ///Whether the ticket is refundable.
        Refundable: bool option
        ///The legs of the trip.
        Segments: IssuingTransactionFlightDataLeg list option
        ///The travel agency that issued the ticket.
        TravelAgency: string option
    }
    with
        static member New (departureAt: int option, passengerName: string option, refundable: bool option, segments: IssuingTransactionFlightDataLeg list option, travelAgency: string option) =
            {
                IssuingTransactionFlightData.DepartureAt = departureAt //required
                IssuingTransactionFlightData.PassengerName = passengerName //required
                IssuingTransactionFlightData.Refundable = refundable //required
                IssuingTransactionFlightData.Segments = segments //required
                IssuingTransactionFlightData.TravelAgency = travelAgency //required
            }

    and IssuingTransactionFuelData = {
        ///[Conexxus Payment System Product Code](https://www.conexxus.org/conexxus-payment-system-product-codes) identifying the primary fuel product purchased.
        IndustryProductCode: string option
        ///The quantity of `unit`s of fuel that was dispensed, represented as a decimal string with at most 12 decimal places.
        QuantityDecimal: string option
        ///The type of fuel that was purchased. One of `diesel`, `unleaded_plus`, `unleaded_regular`, `unleaded_super`, or `other`.
        Type: IssuingTransactionFuelDataType
        ///The units for `quantity_decimal`. One of `charging_minute`, `imperial_gallon`, `kilogram`, `kilowatt_hour`, `liter`, `pound`, `us_gallon`, or `other`.
        Unit: IssuingTransactionFuelDataUnit
        ///The cost in cents per each unit of fuel, represented as a decimal string with at most 12 decimal places.
        UnitCostDecimal: string
    }
    with
        static member New (industryProductCode: string option, quantityDecimal: string option, ``type``: IssuingTransactionFuelDataType, unit: IssuingTransactionFuelDataUnit, unitCostDecimal: string) =
            {
                IssuingTransactionFuelData.IndustryProductCode = industryProductCode //required
                IssuingTransactionFuelData.QuantityDecimal = quantityDecimal //required
                IssuingTransactionFuelData.Type = ``type`` //required
                IssuingTransactionFuelData.Unit = unit //required
                IssuingTransactionFuelData.UnitCostDecimal = unitCostDecimal //required
            }

    and IssuingTransactionPurchaseDetails = {
        ///Fleet-specific information for transactions using Fleet cards.
        Fleet: IssuingTransactionFleetData option
        ///Information about the flight that was purchased with this transaction.
        Flight: IssuingTransactionFlightData option
        ///Information about fuel that was purchased with this transaction.
        Fuel: IssuingTransactionFuelData option
        ///Information about lodging that was purchased with this transaction.
        Lodging: IssuingTransactionLodgingData option
        ///The line items in the purchase.
        Receipt: IssuingTransactionReceiptData list option
        ///A merchant-specific order number.
        Reference: string option
    }
    with
        static member New (fleet: IssuingTransactionFleetData option, flight: IssuingTransactionFlightData option, fuel: IssuingTransactionFuelData option, lodging: IssuingTransactionLodgingData option, receipt: IssuingTransactionReceiptData list option, reference: string option) =
            {
                IssuingTransactionPurchaseDetails.Fleet = fleet //required
                IssuingTransactionPurchaseDetails.Flight = flight //required
                IssuingTransactionPurchaseDetails.Fuel = fuel //required
                IssuingTransactionPurchaseDetails.Lodging = lodging //required
                IssuingTransactionPurchaseDetails.Receipt = receipt //required
                IssuingTransactionPurchaseDetails.Reference = reference //required
            }

    ///A line item.
    and Item = {
        AdjustableQuantity: LineItemsAdjustableQuantity option
        ///Total discount amount applied. If no discounts were applied, defaults to 0.
        AmountDiscount: int
        ///Total before any discounts or taxes are applied.
        AmountSubtotal: int
        ///Total tax amount applied. If no tax was applied, defaults to 0.
        AmountTax: int
        ///Total after discounts and taxes.
        AmountTotal: int
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///An arbitrary string attached to the object. Often useful for displaying to users. Defaults to product name.
        Description: string option
        ///The discounts applied to the line item.
        Discounts: LineItemsDiscountAmount list option
        ///Unique identifier for the object.
        Id: string
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///The price used to generate the line item.
        Price: Price option
        ///The quantity of products being purchased.
        Quantity: int option
        ///The taxes applied to the line item.
        Taxes: LineItemsTaxAmount list option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "item"

        static member New (adjustableQuantity: LineItemsAdjustableQuantity option, amountDiscount: int, amountSubtotal: int, amountTax: int, amountTotal: int, currency: IsoTypes.IsoCurrencyCode, description: string option, id: string, metadata: Map<string, string> option, price: Price option, quantity: int option, ?discounts: LineItemsDiscountAmount list, ?taxes: LineItemsTaxAmount list) =
            {
                Item.AdjustableQuantity = adjustableQuantity //required
                Item.AmountDiscount = amountDiscount //required
                Item.AmountSubtotal = amountSubtotal //required
                Item.AmountTax = amountTax //required
                Item.AmountTotal = amountTotal //required
                Item.Currency = currency //required
                Item.Description = description //required
                Item.Id = id //required
                Item.Metadata = metadata //required
                Item.Price = price //required
                Item.Quantity = quantity //required
                Item.Discounts = discounts
                Item.Taxes = taxes
            }

    and KlarnaAddress = {
        ///The payer address country
        Country: IsoTypes.IsoCountryCode option
    }
    with
        static member New (country: IsoTypes.IsoCountryCode option) =
            {
                KlarnaAddress.Country = country //required
            }

    and KlarnaPayerDetails = {
        ///The payer's address
        Address: KlarnaAddress option
    }
    with
        static member New (address: KlarnaAddress option) =
            {
                KlarnaPayerDetails.Address = address //required
            }

    and LegalEntityCompany = {
        Address: Address option
        ///The Kana variation of the company's primary address (Japan only).
        AddressKana: LegalEntityJapanAddress option
        ///The Kanji variation of the company's primary address (Japan only).
        AddressKanji: LegalEntityJapanAddress option
        ///Whether the company's directors have been provided. This Boolean will be `true` if you've manually indicated that all directors are provided via [the `directors_provided` parameter](https://docs.stripe.com/api/accounts/update#update_account-company-directors_provided).
        DirectorsProvided: bool option
        ///This hash is used to attest that the director information provided to Stripe is both current and correct.
        DirectorshipDeclaration: LegalEntityDirectorshipDeclaration option
        ///Whether the company's executives have been provided. This Boolean will be `true` if you've manually indicated that all executives are provided via [the `executives_provided` parameter](https://docs.stripe.com/api/accounts/update#update_account-company-executives_provided), or if Stripe determined that sufficient executives were provided.
        ExecutivesProvided: bool option
        ///The export license ID number of the company, also referred as Import Export Code (India only).
        ExportLicenseId: string option
        ///The purpose code to use for export transactions (India only).
        ExportPurposeCode: string option
        ///The company's legal name. Also available for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `stripe`.
        Name: string option
        ///The Kana variation of the company's legal name (Japan only). Also available for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `stripe`.
        NameKana: string option
        ///The Kanji variation of the company's legal name (Japan only). Also available for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `stripe`.
        NameKanji: string option
        ///Whether the company's owners have been provided. This Boolean will be `true` if you've manually indicated that all owners are provided via [the `owners_provided` parameter](https://docs.stripe.com/api/accounts/update#update_account-company-owners_provided), or if Stripe determined that sufficient owners were provided. Stripe determines ownership requirements using both the number of owners provided and their total percent ownership (calculated by adding the `percent_ownership` of each owner together).
        OwnersProvided: bool option
        ///This hash is used to attest that the beneficial owner information provided to Stripe is both current and correct.
        OwnershipDeclaration: LegalEntityUboDeclaration option
        ///This value is used to determine if a business is exempt from providing ultimate beneficial owners. See [this support article](https://support.stripe.com/questions/exemption-from-providing-ownership-details) and [changelog](https://docs.stripe.com/changelog/acacia/2025-01-27/ownership-exemption-reason-accounts-api) for more details.
        OwnershipExemptionReason: LegalEntityCompanyOwnershipExemptionReason option
        ///The company's phone number (used for verification).
        Phone: string option
        RegistrationDate: LegalEntityRegistrationDate option
        ///This hash is used to attest that the representative is authorized to act as the representative of their legal entity.
        RepresentativeDeclaration: LegalEntityRepresentativeDeclaration option
        ///The category identifying the legal structure of the company or legal entity. Also available for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `stripe`. See [Business structure](https://docs.stripe.com/connect/identity-verification#business-structure) for more details.
        Structure: LegalEntityCompanyStructure option
        ///Whether the company's business ID number was provided.
        TaxIdProvided: bool option
        ///The jurisdiction in which the `tax_id` is registered (Germany-based companies only).
        TaxIdRegistrar: string option
        ///Whether the company's business VAT number was provided.
        VatIdProvided: bool option
        ///Information on the verification state of the company.
        Verification: LegalEntityCompanyVerification option
    }
    with
        static member New (?address: Address, ?addressKana: LegalEntityJapanAddress option, ?addressKanji: LegalEntityJapanAddress option, ?directorsProvided: bool, ?directorshipDeclaration: LegalEntityDirectorshipDeclaration option, ?executivesProvided: bool, ?exportLicenseId: string, ?exportPurposeCode: string, ?name: string option, ?nameKana: string option, ?nameKanji: string option, ?ownersProvided: bool, ?ownershipDeclaration: LegalEntityUboDeclaration option, ?ownershipExemptionReason: LegalEntityCompanyOwnershipExemptionReason, ?phone: string option, ?registrationDate: LegalEntityRegistrationDate, ?representativeDeclaration: LegalEntityRepresentativeDeclaration option, ?structure: LegalEntityCompanyStructure, ?taxIdProvided: bool, ?taxIdRegistrar: string, ?vatIdProvided: bool, ?verification: LegalEntityCompanyVerification option) =
            {
                LegalEntityCompany.Address = address
                LegalEntityCompany.AddressKana = addressKana |> Option.flatten
                LegalEntityCompany.AddressKanji = addressKanji |> Option.flatten
                LegalEntityCompany.DirectorsProvided = directorsProvided
                LegalEntityCompany.DirectorshipDeclaration = directorshipDeclaration |> Option.flatten
                LegalEntityCompany.ExecutivesProvided = executivesProvided
                LegalEntityCompany.ExportLicenseId = exportLicenseId
                LegalEntityCompany.ExportPurposeCode = exportPurposeCode
                LegalEntityCompany.Name = name |> Option.flatten
                LegalEntityCompany.NameKana = nameKana |> Option.flatten
                LegalEntityCompany.NameKanji = nameKanji |> Option.flatten
                LegalEntityCompany.OwnersProvided = ownersProvided
                LegalEntityCompany.OwnershipDeclaration = ownershipDeclaration |> Option.flatten
                LegalEntityCompany.OwnershipExemptionReason = ownershipExemptionReason
                LegalEntityCompany.Phone = phone |> Option.flatten
                LegalEntityCompany.RegistrationDate = registrationDate
                LegalEntityCompany.RepresentativeDeclaration = representativeDeclaration |> Option.flatten
                LegalEntityCompany.Structure = structure
                LegalEntityCompany.TaxIdProvided = taxIdProvided
                LegalEntityCompany.TaxIdRegistrar = taxIdRegistrar
                LegalEntityCompany.VatIdProvided = vatIdProvided
                LegalEntityCompany.Verification = verification |> Option.flatten
            }

    and LegalEntityCompanyVerification = {
        Document: LegalEntityCompanyVerificationDocument
    }
    with
        static member New (document: LegalEntityCompanyVerificationDocument) =
            {
                LegalEntityCompanyVerification.Document = document //required
            }

    and LegalEntityCompanyVerificationDocument = {
        ///The back of a document returned by a [file upload](https://api.stripe.com#create_file) with a `purpose` value of `additional_verification`. Note that `additional_verification` files are [not downloadable](/file-upload#uploading-a-file).
        Back: LegalEntityCompanyVerificationDocumentBack'AnyOf option
        ///A user-displayable string describing the verification state of this document.
        Details: string option
        ///One of `document_corrupt`, `document_expired`, `document_failed_copy`, `document_failed_greyscale`, `document_failed_other`, `document_failed_test_mode`, `document_fraudulent`, `document_incomplete`, `document_invalid`, `document_manipulated`, `document_not_readable`, `document_not_uploaded`, `document_type_not_supported`, or `document_too_large`. A machine-readable code specifying the verification state for this document.
        DetailsCode: LegalEntityCompanyVerificationDocumentDetailsCode option
        ///The front of a document returned by a [file upload](https://api.stripe.com#create_file) with a `purpose` value of `additional_verification`. Note that `additional_verification` files are [not downloadable](/file-upload#uploading-a-file).
        Front: LegalEntityCompanyVerificationDocumentFront'AnyOf option
    }
    with
        static member New (back: LegalEntityCompanyVerificationDocumentBack'AnyOf option, details: string option, detailsCode: LegalEntityCompanyVerificationDocumentDetailsCode option, front: LegalEntityCompanyVerificationDocumentFront'AnyOf option) =
            {
                LegalEntityCompanyVerificationDocument.Back = back //required
                LegalEntityCompanyVerificationDocument.Details = details //required
                LegalEntityCompanyVerificationDocument.DetailsCode = detailsCode //required
                LegalEntityCompanyVerificationDocument.Front = front //required
            }

    and LegalEntityCompanyVerificationDocumentBack'AnyOf =
        | String of string
        | File of File

    and LegalEntityCompanyVerificationDocumentFront'AnyOf =
        | String of string
        | File of File

    and LegalEntityJapanAddress = {
        ///City/Ward.
        City: string option
        ///Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
        Country: IsoTypes.IsoCountryCode option
        ///Block/Building number.
        [<JsonField(Name="line1")>]Line1: string option
        ///Building details.
        [<JsonField(Name="line2")>]Line2: string option
        ///ZIP or postal code.
        PostalCode: string option
        ///Prefecture.
        State: string option
        ///Town/cho-me.
        Town: string option
    }
    with
        static member New (city: string option, country: IsoTypes.IsoCountryCode option, line1: string option, line2: string option, postalCode: string option, state: string option, town: string option) =
            {
                LegalEntityJapanAddress.City = city //required
                LegalEntityJapanAddress.Country = country //required
                LegalEntityJapanAddress.Line1 = line1 //required
                LegalEntityJapanAddress.Line2 = line2 //required
                LegalEntityJapanAddress.PostalCode = postalCode //required
                LegalEntityJapanAddress.State = state //required
                LegalEntityJapanAddress.Town = town //required
            }

    and LegalEntityPersonVerification = {
        ///A document showing address, either a passport, local ID card, or utility bill from a well-known utility company.
        AdditionalDocument: LegalEntityPersonVerificationDocument option
        ///A user-displayable string describing the verification state for the person. For example, this may say "Provided identity information could not be verified".
        Details: string option
        ///One of `document_address_mismatch`, `document_dob_mismatch`, `document_duplicate_type`, `document_id_number_mismatch`, `document_name_mismatch`, `document_nationality_mismatch`, `failed_keyed_identity`, or `failed_other`. A machine-readable code specifying the verification state for the person.
        DetailsCode: LegalEntityPersonVerificationDetailsCode option
        Document: LegalEntityPersonVerificationDocument option
        ///The state of verification for the person. Possible values are `unverified`, `pending`, or `verified`. Please refer [guide](https://docs.stripe.com/connect/handling-api-verification) to handle verification updates.
        Status: LegalEntityPersonVerificationStatus
    }
    with
        static member New (status: LegalEntityPersonVerificationStatus, ?additionalDocument: LegalEntityPersonVerificationDocument option, ?details: string option, ?detailsCode: LegalEntityPersonVerificationDetailsCode option, ?document: LegalEntityPersonVerificationDocument) =
            {
                LegalEntityPersonVerification.Status = status //required
                LegalEntityPersonVerification.AdditionalDocument = additionalDocument |> Option.flatten
                LegalEntityPersonVerification.Details = details |> Option.flatten
                LegalEntityPersonVerification.DetailsCode = detailsCode |> Option.flatten
                LegalEntityPersonVerification.Document = document
            }

    and LegalEntityPersonVerificationDocument = {
        ///The back of an ID returned by a [file upload](https://api.stripe.com#create_file) with a `purpose` value of `identity_document`.
        Back: LegalEntityPersonVerificationDocumentBack'AnyOf option
        ///A user-displayable string describing the verification state of this document. For example, if a document is uploaded and the picture is too fuzzy, this may say "Identity document is too unclear to read".
        Details: string option
        ///One of `document_corrupt`, `document_country_not_supported`, `document_expired`, `document_failed_copy`, `document_failed_other`, `document_failed_test_mode`, `document_fraudulent`, `document_failed_greyscale`, `document_incomplete`, `document_invalid`, `document_manipulated`, `document_missing_back`, `document_missing_front`, `document_not_readable`, `document_not_uploaded`, `document_photo_mismatch`, `document_too_large`, or `document_type_not_supported`. A machine-readable code specifying the verification state for this document.
        DetailsCode: LegalEntityPersonVerificationDocumentDetailsCode option
        ///The front of an ID returned by a [file upload](https://api.stripe.com#create_file) with a `purpose` value of `identity_document`.
        Front: LegalEntityPersonVerificationDocumentFront'AnyOf option
    }
    with
        static member New (back: LegalEntityPersonVerificationDocumentBack'AnyOf option, details: string option, detailsCode: LegalEntityPersonVerificationDocumentDetailsCode option, front: LegalEntityPersonVerificationDocumentFront'AnyOf option) =
            {
                LegalEntityPersonVerificationDocument.Back = back //required
                LegalEntityPersonVerificationDocument.Details = details //required
                LegalEntityPersonVerificationDocument.DetailsCode = detailsCode //required
                LegalEntityPersonVerificationDocument.Front = front //required
            }

    and LegalEntityPersonVerificationDocumentBack'AnyOf =
        | String of string
        | File of File

    and LegalEntityPersonVerificationDocumentFront'AnyOf =
        | String of string
        | File of File

    and Level3 = {
        CustomerReference: string option
        LineItems: Level3LineItems list
        MerchantReference: string
        ShippingAddressZip: string option
        ShippingAmount: int option
        ShippingFromZip: string option
    }
    with
        static member New (lineItems: Level3LineItems list, merchantReference: string, ?customerReference: string, ?shippingAddressZip: string, ?shippingAmount: int, ?shippingFromZip: string) =
            {
                Level3.LineItems = lineItems //required
                Level3.MerchantReference = merchantReference //required
                Level3.CustomerReference = customerReference
                Level3.ShippingAddressZip = shippingAddressZip
                Level3.ShippingAmount = shippingAmount
                Level3.ShippingFromZip = shippingFromZip
            }

    ///Invoice Line Items represent the individual lines within an [invoice](https://docs.stripe.com/api/invoices) and only exist within the context of an invoice.
    ///Each line item is backed by either an [invoice item](https://docs.stripe.com/api/invoiceitems) or a [subscription item](https://docs.stripe.com/api/subscription_items).
    and LineItem = {
        ///The amount, in cents (or local equivalent).
        Amount: int
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///The amount of discount calculated per discount for this line item.
        DiscountAmounts: DiscountsResourceDiscountAmount list option
        ///If true, discounts will apply to this line item. Always false for prorations.
        Discountable: bool
        ///The discounts applied to the invoice line item. Line item discounts are applied before invoice discounts. Use `expand[]=discounts` to expand each discount.
        Discounts: LineItemDiscounts'AnyOf list
        ///Unique identifier for the object.
        Id: string
        ///The ID of the invoice that contains this line item.
        Invoice: string option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Note that for line items with `type=subscription`, `metadata` reflects the current metadata from the subscription associated with the line item, unless the invoice line was directly updated with different metadata after creation.
        Metadata: Map<string, string>
        ///The parent that generated this line item.
        Parent: BillingBillResourceInvoicingLinesParentsInvoiceLineItemParent option
        Period: InvoiceLineItemPeriod
        ///Contains pretax credit amounts (ex: discount, credit grants, etc) that apply to this line item.
        PretaxCreditAmounts: InvoicesResourcePretaxCreditAmount list option
        ///The pricing information of the line item.
        Pricing: BillingBillResourceInvoicingPricingPricing option
        ///Quantity of units for the invoice line item in integer format, with any decimal precision truncated. For the line item's full-precision decimal quantity, use `quantity_decimal`. This field will be deprecated in favor of `quantity_decimal` in a future version. If the line item is a proration or subscription, the quantity of the subscription that the proration was computed for.
        Quantity: int option
        ///Non-negative decimal with at most 12 decimal places. The quantity of units for the line item.
        QuantityDecimal: string option
        Subscription: LineItemSubscription'AnyOf option
        ///The subtotal of the line item, in cents (or local equivalent), before any discounts or taxes.
        Subtotal: int
        ///The tax information of the line item.
        Taxes: BillingBillResourceInvoicingTaxesTax list option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "line_item"

        static member New (amount: int, currency: IsoTypes.IsoCurrencyCode, description: string option, discountAmounts: DiscountsResourceDiscountAmount list option, discountable: bool, discounts: LineItemDiscounts'AnyOf list, id: string, invoice: string option, livemode: bool, metadata: Map<string, string>, parent: BillingBillResourceInvoicingLinesParentsInvoiceLineItemParent option, period: InvoiceLineItemPeriod, pretaxCreditAmounts: InvoicesResourcePretaxCreditAmount list option, pricing: BillingBillResourceInvoicingPricingPricing option, quantity: int option, quantityDecimal: string option, subscription: LineItemSubscription'AnyOf option, subtotal: int, taxes: BillingBillResourceInvoicingTaxesTax list option) =
            {
                LineItem.Amount = amount //required
                LineItem.Currency = currency //required
                LineItem.Description = description //required
                LineItem.DiscountAmounts = discountAmounts //required
                LineItem.Discountable = discountable //required
                LineItem.Discounts = discounts //required
                LineItem.Id = id //required
                LineItem.Invoice = invoice //required
                LineItem.Livemode = livemode //required
                LineItem.Metadata = metadata //required
                LineItem.Parent = parent //required
                LineItem.Period = period //required
                LineItem.PretaxCreditAmounts = pretaxCreditAmounts //required
                LineItem.Pricing = pricing //required
                LineItem.Quantity = quantity //required
                LineItem.QuantityDecimal = quantityDecimal //required
                LineItem.Subscription = subscription //required
                LineItem.Subtotal = subtotal //required
                LineItem.Taxes = taxes //required
            }

    and LineItemSubscription'AnyOf =
        | String of string
        | Subscription of Subscription

    and LineItemDiscounts'AnyOf =
        | String of string
        | Discount of Discount

    and LineItemsDiscountAmount = {
        ///The amount discounted.
        Amount: int
        Discount: Discount
    }
    with
        static member New (amount: int, discount: Discount) =
            {
                LineItemsDiscountAmount.Amount = amount //required
                LineItemsDiscountAmount.Discount = discount //required
            }

    and LineItemsTaxAmount = {
        ///Amount of tax applied for this rate.
        Amount: int
        Rate: TaxRate
        ///The reasoning behind this tax, for example, if the product is tax exempt. The possible values for this field may be extended as new tax rules are supported.
        TaxabilityReason: LineItemsTaxAmountTaxabilityReason option
        ///The amount on which tax is calculated, in cents (or local equivalent).
        TaxableAmount: int option
    }
    with
        static member New (amount: int, rate: TaxRate, taxabilityReason: LineItemsTaxAmountTaxabilityReason option, taxableAmount: int option) =
            {
                LineItemsTaxAmount.Amount = amount //required
                LineItemsTaxAmount.Rate = rate //required
                LineItemsTaxAmount.TaxabilityReason = taxabilityReason //required
                LineItemsTaxAmount.TaxableAmount = taxableAmount //required
            }

    and LinkedAccountOptionsCommon = {
        Filters: PaymentFlowsPrivatePaymentMethodsFinancialConnectionsCommonLinkedAccountOptionsFilters option
        ///The list of permissions to request. The `payment_method` permission must be included.
        Permissions: LinkedAccountOptionsCommonPermissions list option
        ///Data features requested to be retrieved upon account creation.
        Prefetch: LinkedAccountOptionsCommonPrefetch list option
        ///For webview integrations only. Upon completing OAuth login in the native browser, the user will be redirected to this URL to return to your app.
        ReturnUrl: string option
    }
    with
        static member New (prefetch: LinkedAccountOptionsCommonPrefetch list option, ?filters: PaymentFlowsPrivatePaymentMethodsFinancialConnectionsCommonLinkedAccountOptionsFilters, ?permissions: LinkedAccountOptionsCommonPermissions list, ?returnUrl: string) =
            {
                LinkedAccountOptionsCommon.Prefetch = prefetch //required
                LinkedAccountOptionsCommon.Filters = filters
                LinkedAccountOptionsCommon.Permissions = permissions
                LinkedAccountOptionsCommon.ReturnUrl = returnUrl
            }

    ///A Mandate is a record of the permission that your customer gives you to debit their payment method.
    and Mandate = {
        CustomerAcceptance: CustomerAcceptance
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        MultiUse: MandateMultiUse option
        ///The account (if any) that the mandate is intended for.
        OnBehalfOf: string option
        ///ID of the payment method associated with this mandate.
        PaymentMethod: MandatePaymentMethod'AnyOf
        PaymentMethodDetails: MandatePaymentMethodDetails
        SingleUse: MandateSingleUse option
        ///The mandate status indicates whether or not you can use it to initiate a payment.
        Status: MandateStatus
        ///The type of the mandate.
        Type: MandateType
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "mandate"

        static member New (customerAcceptance: CustomerAcceptance, id: string, livemode: bool, paymentMethod: MandatePaymentMethod'AnyOf, paymentMethodDetails: MandatePaymentMethodDetails, status: MandateStatus, ``type``: MandateType, ?multiUse: MandateMultiUse, ?onBehalfOf: string, ?singleUse: MandateSingleUse) =
            {
                Mandate.CustomerAcceptance = customerAcceptance //required
                Mandate.Id = id //required
                Mandate.Livemode = livemode //required
                Mandate.PaymentMethod = paymentMethod //required
                Mandate.PaymentMethodDetails = paymentMethodDetails //required
                Mandate.Status = status //required
                Mandate.Type = ``type`` //required
                Mandate.MultiUse = multiUse
                Mandate.OnBehalfOf = onBehalfOf
                Mandate.SingleUse = singleUse
            }

    and MandatePaymentMethod'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and MandateAcssDebit = {
        ///List of Stripe products where this mandate can be selected automatically.
        DefaultFor: MandateAcssDebitDefaultFor list option
        ///Description of the interval. Only required if the 'payment_schedule' parameter is 'interval' or 'combined'.
        IntervalDescription: string option
        ///Payment schedule for the mandate.
        PaymentSchedule: MandateAcssDebitPaymentSchedule
        ///Transaction type of the mandate.
        TransactionType: MandateAcssDebitTransactionType
    }
    with
        static member New (intervalDescription: string option, paymentSchedule: MandateAcssDebitPaymentSchedule, transactionType: MandateAcssDebitTransactionType, ?defaultFor: MandateAcssDebitDefaultFor list) =
            {
                MandateAcssDebit.IntervalDescription = intervalDescription //required
                MandateAcssDebit.PaymentSchedule = paymentSchedule //required
                MandateAcssDebit.TransactionType = transactionType //required
                MandateAcssDebit.DefaultFor = defaultFor
            }

    and MandateBacsDebit = {
        ///The display name for the account on this mandate.
        DisplayName: string option
        ///The status of the mandate on the Bacs network. Can be one of `pending`, `revoked`, `refused`, or `accepted`.
        NetworkStatus: MandateBacsDebitNetworkStatus
        ///The unique reference identifying the mandate on the Bacs network.
        Reference: string
        ///When the mandate is revoked on the Bacs network this field displays the reason for the revocation.
        RevocationReason: MandateBacsDebitRevocationReason option
        ///The service user number for the account on this mandate.
        ServiceUserNumber: string option
        ///The URL that will contain the mandate that the customer has signed.
        Url: string
    }
    with
        static member New (displayName: string option, networkStatus: MandateBacsDebitNetworkStatus, reference: string, revocationReason: MandateBacsDebitRevocationReason option, serviceUserNumber: string option, url: string) =
            {
                MandateBacsDebit.DisplayName = displayName //required
                MandateBacsDebit.NetworkStatus = networkStatus //required
                MandateBacsDebit.Reference = reference //required
                MandateBacsDebit.RevocationReason = revocationReason //required
                MandateBacsDebit.ServiceUserNumber = serviceUserNumber //required
                MandateBacsDebit.Url = url //required
            }

    and MandateMultiUse = {
        ///The amount of the payment on a multi use mandate.
        Amount: int option
        ///The currency of the payment on a multi use mandate.
        Currency: IsoTypes.IsoCurrencyCode option
    }
    with
        static member New (?amount: int, ?currency: IsoTypes.IsoCurrencyCode) =
            {
                MandateMultiUse.Amount = amount
                MandateMultiUse.Currency = currency
            }

    and MandateOptionsPayto = {
        ///Amount that will be collected. It is required when `amount_type` is `fixed`.
        Amount: int option
        ///The type of amount that will be collected. The amount charged must be exact or up to the value of `amount` param for `fixed` or `maximum` type respectively. Defaults to `maximum`.
        AmountType: MandateOptionsPaytoAmountType option
        ///Date, in YYYY-MM-DD format, after which payments will not be collected. Defaults to no end date.
        EndDate: string option
        ///The periodicity at which payments will be collected. Defaults to `adhoc`.
        PaymentSchedule: MandateOptionsPaytoPaymentSchedule option
        ///The number of payments that will be made during a payment period. Defaults to 1 except for when `payment_schedule` is `adhoc`. In that case, it defaults to no limit.
        PaymentsPerPeriod: int option
        ///The purpose for which payments are made. Has a default value based on your merchant category code.
        Purpose: MandateOptionsPaytoPurpose option
        ///Date, in YYYY-MM-DD format, from which payments will be collected. Defaults to confirmation time.
        StartDate: string option
    }
    with
        static member New (amount: int option, amountType: MandateOptionsPaytoAmountType option, endDate: string option, paymentSchedule: MandateOptionsPaytoPaymentSchedule option, paymentsPerPeriod: int option, purpose: MandateOptionsPaytoPurpose option, startDate: string option) =
            {
                MandateOptionsPayto.Amount = amount //required
                MandateOptionsPayto.AmountType = amountType //required
                MandateOptionsPayto.EndDate = endDate //required
                MandateOptionsPayto.PaymentSchedule = paymentSchedule //required
                MandateOptionsPayto.PaymentsPerPeriod = paymentsPerPeriod //required
                MandateOptionsPayto.Purpose = purpose //required
                MandateOptionsPayto.StartDate = startDate //required
            }

    and MandateOptionsUpi = {
        ///Amount to be charged for future payments.
        Amount: int option
        ///One of `fixed` or `maximum`. If `fixed`, the `amount` param refers to the exact amount to be charged in future payments. If `maximum`, the amount charged can be up to the value passed for the `amount` param.
        AmountType: MandateOptionsUpiAmountType option
        ///A description of the mandate or subscription that is meant to be displayed to the customer.
        Description: string option
        ///End date of the mandate or subscription.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]EndDate: DateTime option
    }
    with
        static member New (amount: int option, amountType: MandateOptionsUpiAmountType option, description: string option, endDate: DateTime option) =
            {
                MandateOptionsUpi.Amount = amount //required
                MandateOptionsUpi.AmountType = amountType //required
                MandateOptionsUpi.Description = description //required
                MandateOptionsUpi.EndDate = endDate //required
            }

    and MandatePaymentMethodDetails = {
        AcssDebit: MandateAcssDebit option
        AmazonPay: MandateAmazonPay option
        AuBecsDebit: MandateAuBecsDebit option
        BacsDebit: MandateBacsDebit option
        Card: CardMandatePaymentMethodDetails option
        Cashapp: MandateCashapp option
        KakaoPay: MandateKakaoPay option
        Klarna: MandateKlarna option
        KrCard: MandateKrCard option
        Link: MandateLink option
        NaverPay: MandateNaverPay option
        NzBankAccount: MandateNzBankAccount option
        Paypal: MandatePaypal option
        Payto: MandatePayto option
        Pix: MandatePix option
        RevolutPay: MandateRevolutPay option
        SepaDebit: MandateSepaDebit option
        ///This mandate corresponds with a specific payment method type. The `payment_method_details` includes an additional hash with the same name and contains mandate information that's specific to that payment method.
        Type: string
        Upi: MandateUpi option
        UsBankAccount: MandateUsBankAccount option
    }
    with
        static member New (``type``: string, ?acssDebit: MandateAcssDebit, ?amazonPay: MandateAmazonPay, ?auBecsDebit: MandateAuBecsDebit, ?bacsDebit: MandateBacsDebit, ?card: CardMandatePaymentMethodDetails, ?cashapp: MandateCashapp, ?kakaoPay: MandateKakaoPay, ?klarna: MandateKlarna, ?krCard: MandateKrCard, ?link: MandateLink, ?naverPay: MandateNaverPay, ?nzBankAccount: MandateNzBankAccount, ?paypal: MandatePaypal, ?payto: MandatePayto, ?pix: MandatePix, ?revolutPay: MandateRevolutPay, ?sepaDebit: MandateSepaDebit, ?upi: MandateUpi, ?usBankAccount: MandateUsBankAccount) =
            {
                MandatePaymentMethodDetails.Type = ``type`` //required
                MandatePaymentMethodDetails.AcssDebit = acssDebit
                MandatePaymentMethodDetails.AmazonPay = amazonPay
                MandatePaymentMethodDetails.AuBecsDebit = auBecsDebit
                MandatePaymentMethodDetails.BacsDebit = bacsDebit
                MandatePaymentMethodDetails.Card = card
                MandatePaymentMethodDetails.Cashapp = cashapp
                MandatePaymentMethodDetails.KakaoPay = kakaoPay
                MandatePaymentMethodDetails.Klarna = klarna
                MandatePaymentMethodDetails.KrCard = krCard
                MandatePaymentMethodDetails.Link = link
                MandatePaymentMethodDetails.NaverPay = naverPay
                MandatePaymentMethodDetails.NzBankAccount = nzBankAccount
                MandatePaymentMethodDetails.Paypal = paypal
                MandatePaymentMethodDetails.Payto = payto
                MandatePaymentMethodDetails.Pix = pix
                MandatePaymentMethodDetails.RevolutPay = revolutPay
                MandatePaymentMethodDetails.SepaDebit = sepaDebit
                MandatePaymentMethodDetails.Upi = upi
                MandatePaymentMethodDetails.UsBankAccount = usBankAccount
            }

    and MandatePayto = {
        ///Amount that will be collected. It is required when `amount_type` is `fixed`.
        Amount: int option
        ///The type of amount that will be collected. The amount charged must be exact or up to the value of `amount` param for `fixed` or `maximum` type respectively. Defaults to `maximum`.
        AmountType: MandatePaytoAmountType
        ///Date, in YYYY-MM-DD format, after which payments will not be collected. Defaults to no end date.
        EndDate: string option
        ///The periodicity at which payments will be collected. Defaults to `adhoc`.
        PaymentSchedule: MandatePaytoPaymentSchedule
        ///The number of payments that will be made during a payment period. Defaults to 1 except for when `payment_schedule` is `adhoc`. In that case, it defaults to no limit.
        PaymentsPerPeriod: int option
        ///The purpose for which payments are made. Has a default value based on your merchant category code.
        Purpose: MandatePaytoPurpose option
        ///Date, in YYYY-MM-DD format, from which payments will be collected. Defaults to confirmation time.
        StartDate: string option
    }
    with
        static member New (amount: int option, amountType: MandatePaytoAmountType, endDate: string option, paymentSchedule: MandatePaytoPaymentSchedule, paymentsPerPeriod: int option, purpose: MandatePaytoPurpose option, startDate: string option) =
            {
                MandatePayto.Amount = amount //required
                MandatePayto.AmountType = amountType //required
                MandatePayto.EndDate = endDate //required
                MandatePayto.PaymentSchedule = paymentSchedule //required
                MandatePayto.PaymentsPerPeriod = paymentsPerPeriod //required
                MandatePayto.Purpose = purpose //required
                MandatePayto.StartDate = startDate //required
            }

    and MandatePix = {
        ///Determines if the amount includes the IOF tax.
        AmountIncludesIof: MandatePixAmountIncludesIof option
        ///Type of amount.
        AmountType: MandatePixAmountType option
        ///Date when the mandate expires and no further payments will be charged, in `YYYY-MM-DD`.
        EndDate: string option
        ///Schedule at which the future payments will be charged.
        PaymentSchedule: MandatePixPaymentSchedule option
        ///Subscription name displayed to buyers in their bank app.
        Reference: string option
        ///Start date of the mandate, in `YYYY-MM-DD`.
        StartDate: string option
    }
    with
        static member New (?amountIncludesIof: MandatePixAmountIncludesIof, ?amountType: MandatePixAmountType, ?endDate: string, ?paymentSchedule: MandatePixPaymentSchedule, ?reference: string, ?startDate: string) =
            {
                MandatePix.AmountIncludesIof = amountIncludesIof
                MandatePix.AmountType = amountType
                MandatePix.EndDate = endDate
                MandatePix.PaymentSchedule = paymentSchedule
                MandatePix.Reference = reference
                MandatePix.StartDate = startDate
            }

    and MandateSingleUse = {
        ///The amount of the payment on a single use mandate.
        Amount: int
        ///The currency of the payment on a single use mandate.
        Currency: IsoTypes.IsoCurrencyCode
    }
    with
        static member New (amount: int, currency: IsoTypes.IsoCurrencyCode) =
            {
                MandateSingleUse.Amount = amount //required
                MandateSingleUse.Currency = currency //required
            }

    and MandateUpi = {
        ///Amount to be charged for future payments.
        Amount: int option
        ///One of `fixed` or `maximum`. If `fixed`, the `amount` param refers to the exact amount to be charged in future payments. If `maximum`, the amount charged can be up to the value passed for the `amount` param.
        AmountType: MandateUpiAmountType option
        ///A description of the mandate or subscription that is meant to be displayed to the customer.
        Description: string option
        ///End date of the mandate or subscription.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]EndDate: DateTime option
    }
    with
        static member New (amount: int option, amountType: MandateUpiAmountType option, description: string option, endDate: DateTime option) =
            {
                MandateUpi.Amount = amount //required
                MandateUpi.AmountType = amountType //required
                MandateUpi.Description = description //required
                MandateUpi.EndDate = endDate //required
            }

    and Networks = {
        ///All networks available for selection via [payment_method_options.card.network](/api/payment_intents/confirm#confirm_payment_intent-payment_method_options-card-network).
        Available: string list
        ///The preferred network for co-branded cards. Can be `cartes_bancaires`, `mastercard`, `visa` or `invalid_preference` if requested network is not valid for the card.
        Preferred: string option
    }
    with
        static member New (available: string list, preferred: string option) =
            {
                Networks.Available = available //required
                Networks.Preferred = preferred //required
            }

    and OutboundPaymentsPaymentMethodDetails = {
        BillingDetails: TreasurySharedResourceBillingDetails
        FinancialAccount: OutboundPaymentsPaymentMethodDetailsFinancialAccount option
        ///The type of the payment method used in the OutboundPayment.
        Type: OutboundPaymentsPaymentMethodDetailsType
        UsBankAccount: OutboundPaymentsPaymentMethodDetailsUsBankAccount option
    }
    with
        static member New (billingDetails: TreasurySharedResourceBillingDetails, ``type``: OutboundPaymentsPaymentMethodDetailsType, ?financialAccount: OutboundPaymentsPaymentMethodDetailsFinancialAccount, ?usBankAccount: OutboundPaymentsPaymentMethodDetailsUsBankAccount) =
            {
                OutboundPaymentsPaymentMethodDetails.BillingDetails = billingDetails //required
                OutboundPaymentsPaymentMethodDetails.Type = ``type`` //required
                OutboundPaymentsPaymentMethodDetails.FinancialAccount = financialAccount
                OutboundPaymentsPaymentMethodDetails.UsBankAccount = usBankAccount
            }

    and OutboundPaymentsPaymentMethodDetailsUsBankAccount = {
        ///Account holder type: individual or company.
        AccountHolderType: OutboundPaymentsPaymentMethodDetailsUsBankAccountAccountHolderType option
        ///Account type: checkings or savings. Defaults to checking if omitted.
        AccountType: OutboundPaymentsPaymentMethodDetailsUsBankAccountAccountType option
        ///Name of the bank associated with the bank account.
        BankName: string option
        ///Uniquely identifies this particular bank account. You can use this attribute to check whether two bank accounts are the same.
        Fingerprint: string option
        ///Last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string option
        ///ID of the mandate used to make this payment.
        Mandate: OutboundPaymentsPaymentMethodDetailsUsBankAccountMandate'AnyOf option
        ///The network rails used. See the [docs](https://docs.stripe.com/treasury/money-movement/timelines) to learn more about money movement timelines for each network type.
        Network: OutboundPaymentsPaymentMethodDetailsUsBankAccountNetwork
        ///Routing number of the bank account.
        RoutingNumber: string option
    }
    with
        static member New (accountHolderType: OutboundPaymentsPaymentMethodDetailsUsBankAccountAccountHolderType option, accountType: OutboundPaymentsPaymentMethodDetailsUsBankAccountAccountType option, bankName: string option, fingerprint: string option, last4: string option, network: OutboundPaymentsPaymentMethodDetailsUsBankAccountNetwork, routingNumber: string option, ?mandate: OutboundPaymentsPaymentMethodDetailsUsBankAccountMandate'AnyOf) =
            {
                OutboundPaymentsPaymentMethodDetailsUsBankAccount.AccountHolderType = accountHolderType //required
                OutboundPaymentsPaymentMethodDetailsUsBankAccount.AccountType = accountType //required
                OutboundPaymentsPaymentMethodDetailsUsBankAccount.BankName = bankName //required
                OutboundPaymentsPaymentMethodDetailsUsBankAccount.Fingerprint = fingerprint //required
                OutboundPaymentsPaymentMethodDetailsUsBankAccount.Last4 = last4 //required
                OutboundPaymentsPaymentMethodDetailsUsBankAccount.Network = network //required
                OutboundPaymentsPaymentMethodDetailsUsBankAccount.RoutingNumber = routingNumber //required
                OutboundPaymentsPaymentMethodDetailsUsBankAccount.Mandate = mandate
            }

    and OutboundPaymentsPaymentMethodDetailsUsBankAccountMandate'AnyOf =
        | String of string
        | Mandate of Mandate

    and OutboundTransfersPaymentMethodDetails = {
        BillingDetails: TreasurySharedResourceBillingDetails
        FinancialAccount: OutboundTransfersPaymentMethodDetailsFinancialAccount option
        ///The type of the payment method used in the OutboundTransfer.
        Type: OutboundTransfersPaymentMethodDetailsType
        UsBankAccount: OutboundTransfersPaymentMethodDetailsUsBankAccount option
    }
    with
        static member New (billingDetails: TreasurySharedResourceBillingDetails, ``type``: OutboundTransfersPaymentMethodDetailsType, ?financialAccount: OutboundTransfersPaymentMethodDetailsFinancialAccount, ?usBankAccount: OutboundTransfersPaymentMethodDetailsUsBankAccount) =
            {
                OutboundTransfersPaymentMethodDetails.BillingDetails = billingDetails //required
                OutboundTransfersPaymentMethodDetails.Type = ``type`` //required
                OutboundTransfersPaymentMethodDetails.FinancialAccount = financialAccount
                OutboundTransfersPaymentMethodDetails.UsBankAccount = usBankAccount
            }

    and OutboundTransfersPaymentMethodDetailsUsBankAccount = {
        ///Account holder type: individual or company.
        AccountHolderType: OutboundTransfersPaymentMethodDetailsUsBankAccountAccountHolderType option
        ///Account type: checkings or savings. Defaults to checking if omitted.
        AccountType: OutboundTransfersPaymentMethodDetailsUsBankAccountAccountType option
        ///Name of the bank associated with the bank account.
        BankName: string option
        ///Uniquely identifies this particular bank account. You can use this attribute to check whether two bank accounts are the same.
        Fingerprint: string option
        ///Last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string option
        ///ID of the mandate used to make this payment.
        Mandate: OutboundTransfersPaymentMethodDetailsUsBankAccountMandate'AnyOf option
        ///The network rails used. See the [docs](https://docs.stripe.com/treasury/money-movement/timelines) to learn more about money movement timelines for each network type.
        Network: OutboundTransfersPaymentMethodDetailsUsBankAccountNetwork
        ///Routing number of the bank account.
        RoutingNumber: string option
    }
    with
        static member New (accountHolderType: OutboundTransfersPaymentMethodDetailsUsBankAccountAccountHolderType option, accountType: OutboundTransfersPaymentMethodDetailsUsBankAccountAccountType option, bankName: string option, fingerprint: string option, last4: string option, network: OutboundTransfersPaymentMethodDetailsUsBankAccountNetwork, routingNumber: string option, ?mandate: OutboundTransfersPaymentMethodDetailsUsBankAccountMandate'AnyOf) =
            {
                OutboundTransfersPaymentMethodDetailsUsBankAccount.AccountHolderType = accountHolderType //required
                OutboundTransfersPaymentMethodDetailsUsBankAccount.AccountType = accountType //required
                OutboundTransfersPaymentMethodDetailsUsBankAccount.BankName = bankName //required
                OutboundTransfersPaymentMethodDetailsUsBankAccount.Fingerprint = fingerprint //required
                OutboundTransfersPaymentMethodDetailsUsBankAccount.Last4 = last4 //required
                OutboundTransfersPaymentMethodDetailsUsBankAccount.Network = network //required
                OutboundTransfersPaymentMethodDetailsUsBankAccount.RoutingNumber = routingNumber //required
                OutboundTransfersPaymentMethodDetailsUsBankAccount.Mandate = mandate
            }

    and OutboundTransfersPaymentMethodDetailsUsBankAccountMandate'AnyOf =
        | String of string
        | Mandate of Mandate

    ///A Payment Attempt Record represents an individual attempt at making a payment, on or off Stripe.
    ///Each payment attempt tries to collect a fixed amount of money from a fixed customer and payment
    ///method. Payment Attempt Records are attached to Payment Records. Only one attempt per Payment Record
    ///can have guaranteed funds.
    and PaymentAttemptRecord = {
        Amount: PaymentsPrimitivesPaymentRecordsResourceAmount
        AmountAuthorized: PaymentsPrimitivesPaymentRecordsResourceAmount
        AmountCanceled: PaymentsPrimitivesPaymentRecordsResourceAmount
        AmountFailed: PaymentsPrimitivesPaymentRecordsResourceAmount
        AmountGuaranteed: PaymentsPrimitivesPaymentRecordsResourceAmount
        AmountRefunded: PaymentsPrimitivesPaymentRecordsResourceAmount
        AmountRequested: PaymentsPrimitivesPaymentRecordsResourceAmount
        ///ID of the Connect application that created the PaymentAttemptRecord.
        Application: string option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Customer information for this payment.
        CustomerDetails: PaymentsPrimitivesPaymentRecordsResourceCustomerDetails option
        ///Indicates whether the customer was present in your checkout flow during this payment.
        CustomerPresence: PaymentAttemptRecordCustomerPresence option
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///Information about the Payment Method debited for this payment.
        PaymentMethodDetails: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails option
        ///ID of the Payment Record this Payment Attempt Record belongs to.
        PaymentRecord: string option
        ProcessorDetails: PaymentsPrimitivesPaymentRecordsResourceProcessorDetails
        ///Indicates who reported the payment.
        ReportedBy: PaymentAttemptRecordReportedBy
        ///Shipping information for this payment.
        ShippingDetails: PaymentsPrimitivesPaymentRecordsResourceShippingDetails option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "payment_attempt_record"

        static member New (amount: PaymentsPrimitivesPaymentRecordsResourceAmount, amountAuthorized: PaymentsPrimitivesPaymentRecordsResourceAmount, amountCanceled: PaymentsPrimitivesPaymentRecordsResourceAmount, amountFailed: PaymentsPrimitivesPaymentRecordsResourceAmount, amountGuaranteed: PaymentsPrimitivesPaymentRecordsResourceAmount, amountRefunded: PaymentsPrimitivesPaymentRecordsResourceAmount, amountRequested: PaymentsPrimitivesPaymentRecordsResourceAmount, application: string option, created: DateTime, customerDetails: PaymentsPrimitivesPaymentRecordsResourceCustomerDetails option, customerPresence: PaymentAttemptRecordCustomerPresence option, description: string option, id: string, livemode: bool, metadata: Map<string, string>, paymentMethodDetails: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails option, paymentRecord: string option, processorDetails: PaymentsPrimitivesPaymentRecordsResourceProcessorDetails, reportedBy: PaymentAttemptRecordReportedBy, shippingDetails: PaymentsPrimitivesPaymentRecordsResourceShippingDetails option) =
            {
                PaymentAttemptRecord.Amount = amount //required
                PaymentAttemptRecord.AmountAuthorized = amountAuthorized //required
                PaymentAttemptRecord.AmountCanceled = amountCanceled //required
                PaymentAttemptRecord.AmountFailed = amountFailed //required
                PaymentAttemptRecord.AmountGuaranteed = amountGuaranteed //required
                PaymentAttemptRecord.AmountRefunded = amountRefunded //required
                PaymentAttemptRecord.AmountRequested = amountRequested //required
                PaymentAttemptRecord.Application = application //required
                PaymentAttemptRecord.Created = created //required
                PaymentAttemptRecord.CustomerDetails = customerDetails //required
                PaymentAttemptRecord.CustomerPresence = customerPresence //required
                PaymentAttemptRecord.Description = description //required
                PaymentAttemptRecord.Id = id //required
                PaymentAttemptRecord.Livemode = livemode //required
                PaymentAttemptRecord.Metadata = metadata //required
                PaymentAttemptRecord.PaymentMethodDetails = paymentMethodDetails //required
                PaymentAttemptRecord.PaymentRecord = paymentRecord //required
                PaymentAttemptRecord.ProcessorDetails = processorDetails //required
                PaymentAttemptRecord.ReportedBy = reportedBy //required
                PaymentAttemptRecord.ShippingDetails = shippingDetails //required
            }

    and PaymentFlowsAmountDetails = {
        ///The total discount applied on the transaction represented in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). An integer greater than 0.
        ///This field is mutually exclusive with the `amount_details[line_items][#][discount_amount]` field.
        DiscountAmount: int option
        Error: PaymentFlowsAmountDetailsResourceError option
        ///A list of line items, each containing information about a product in the PaymentIntent. There is a maximum of 200 line items.
        LineItems: PaymentFlowsAmountDetailsLineItems option
        Shipping: PaymentFlowsAmountDetailsResourceShipping option
        Tax: PaymentFlowsAmountDetailsResourceTax option
        Tip: PaymentFlowsAmountDetailsClientResourceTip option
    }
    with
        static member New (?discountAmount: int, ?error: PaymentFlowsAmountDetailsResourceError, ?lineItems: PaymentFlowsAmountDetailsLineItems, ?shipping: PaymentFlowsAmountDetailsResourceShipping, ?tax: PaymentFlowsAmountDetailsResourceTax, ?tip: PaymentFlowsAmountDetailsClientResourceTip) =
            {
                PaymentFlowsAmountDetails.DiscountAmount = discountAmount
                PaymentFlowsAmountDetails.Error = error
                PaymentFlowsAmountDetails.LineItems = lineItems
                PaymentFlowsAmountDetails.Shipping = shipping
                PaymentFlowsAmountDetails.Tax = tax
                PaymentFlowsAmountDetails.Tip = tip
            }

    ///A list of line items, each containing information about a product in the PaymentIntent. There is a maximum of 200 line items.
    and PaymentFlowsAmountDetailsLineItems = {
        ///Details about each object.
        Data: PaymentIntentAmountDetailsLineItem list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: PaymentIntentAmountDetailsLineItem list, hasMore: bool, url: string) =
            {
                PaymentFlowsAmountDetailsLineItems.Data = data //required
                PaymentFlowsAmountDetailsLineItems.HasMore = hasMore //required
                PaymentFlowsAmountDetailsLineItems.Url = url //required
            }

    and PaymentFlowsAmountDetailsClient = {
        Tip: PaymentFlowsAmountDetailsClientResourceTip option
    }
    with
        static member New (?tip: PaymentFlowsAmountDetailsClientResourceTip) =
            {
                PaymentFlowsAmountDetailsClient.Tip = tip
            }

    and PaymentFlowsAmountDetailsResourceError = {
        ///The code of the error that occurred when validating the current amount details.
        Code: PaymentFlowsAmountDetailsResourceErrorCode option
        ///A message providing more details about the error.
        Message: string option
    }
    with
        static member New (code: PaymentFlowsAmountDetailsResourceErrorCode option, message: string option) =
            {
                PaymentFlowsAmountDetailsResourceError.Code = code //required
                PaymentFlowsAmountDetailsResourceError.Message = message //required
            }

    and PaymentFlowsAmountDetailsResourceLineItemsListResourceLineItemResourcePaymentMethodOptions = {
        Card: PaymentFlowsPrivatePaymentMethodsCardPaymentIntentAmountDetailsLineItemPaymentMethodOptions option
        CardPresent: PaymentFlowsPrivatePaymentMethodsCardPresentAmountDetailsLineItemPaymentMethodOptions option
        Klarna: PaymentFlowsPrivatePaymentMethodsKlarnaPaymentIntentAmountDetailsLineItemPaymentMethodOptions option
        Paypal: PaymentFlowsPrivatePaymentMethodsPaypalAmountDetailsLineItemPaymentMethodOptions option
    }
    with
        static member New (?card: PaymentFlowsPrivatePaymentMethodsCardPaymentIntentAmountDetailsLineItemPaymentMethodOptions, ?cardPresent: PaymentFlowsPrivatePaymentMethodsCardPresentAmountDetailsLineItemPaymentMethodOptions, ?klarna: PaymentFlowsPrivatePaymentMethodsKlarnaPaymentIntentAmountDetailsLineItemPaymentMethodOptions, ?paypal: PaymentFlowsPrivatePaymentMethodsPaypalAmountDetailsLineItemPaymentMethodOptions) =
            {
                PaymentFlowsAmountDetailsResourceLineItemsListResourceLineItemResourcePaymentMethodOptions.Card = card
                PaymentFlowsAmountDetailsResourceLineItemsListResourceLineItemResourcePaymentMethodOptions.CardPresent = cardPresent
                PaymentFlowsAmountDetailsResourceLineItemsListResourceLineItemResourcePaymentMethodOptions.Klarna = klarna
                PaymentFlowsAmountDetailsResourceLineItemsListResourceLineItemResourcePaymentMethodOptions.Paypal = paypal
            }

    and PaymentFlowsAutomaticPaymentMethodsPaymentIntent = {
        ///Controls whether this PaymentIntent will accept redirect-based payment methods.
        ///Redirect-based payment methods may require your customer to be redirected to a payment method's app or site for authentication or additional steps. To [confirm](https://docs.stripe.com/api/payment_intents/confirm) this PaymentIntent, you may be required to provide a `return_url` to redirect customers back to your site after they authenticate or complete the payment.
        AllowRedirects: PaymentFlowsAutomaticPaymentMethodsPaymentIntentAllowRedirects option
        ///Automatically calculates compatible payment methods
        Enabled: bool
    }
    with
        static member New (enabled: bool, ?allowRedirects: PaymentFlowsAutomaticPaymentMethodsPaymentIntentAllowRedirects) =
            {
                PaymentFlowsAutomaticPaymentMethodsPaymentIntent.Enabled = enabled //required
                PaymentFlowsAutomaticPaymentMethodsPaymentIntent.AllowRedirects = allowRedirects
            }

    and PaymentFlowsAutomaticPaymentMethodsSetupIntent = {
        ///Controls whether this SetupIntent will accept redirect-based payment methods.
        ///Redirect-based payment methods may require your customer to be redirected to a payment method's app or site for authentication or additional steps. To [confirm](https://docs.stripe.com/api/setup_intents/confirm) this SetupIntent, you may be required to provide a `return_url` to redirect customers back to your site after they authenticate or complete the setup.
        AllowRedirects: PaymentFlowsAutomaticPaymentMethodsSetupIntentAllowRedirects option
        ///Automatically calculates compatible payment methods
        Enabled: bool option
    }
    with
        static member New (enabled: bool option, ?allowRedirects: PaymentFlowsAutomaticPaymentMethodsSetupIntentAllowRedirects) =
            {
                PaymentFlowsAutomaticPaymentMethodsSetupIntent.Enabled = enabled //required
                PaymentFlowsAutomaticPaymentMethodsSetupIntent.AllowRedirects = allowRedirects
            }

    and PaymentFlowsInstallmentOptions = {
        Enabled: bool
        Plan: PaymentMethodDetailsCardInstallmentsPlan option
    }
    with
        static member New (enabled: bool, ?plan: PaymentMethodDetailsCardInstallmentsPlan) =
            {
                PaymentFlowsInstallmentOptions.Enabled = enabled //required
                PaymentFlowsInstallmentOptions.Plan = plan
            }

    and PaymentFlowsPaymentIntentAsyncWorkflows = {
        Inputs: PaymentFlowsPaymentIntentAsyncWorkflowsResourceInputs option
    }
    with
        static member New (?inputs: PaymentFlowsPaymentIntentAsyncWorkflowsResourceInputs) =
            {
                PaymentFlowsPaymentIntentAsyncWorkflows.Inputs = inputs
            }

    and PaymentFlowsPaymentIntentAsyncWorkflowsResourceInputs = {
        Tax: PaymentFlowsPaymentIntentAsyncWorkflowsResourceInputsResourceTax option
    }
    with
        static member New (?tax: PaymentFlowsPaymentIntentAsyncWorkflowsResourceInputsResourceTax) =
            {
                PaymentFlowsPaymentIntentAsyncWorkflowsResourceInputs.Tax = tax
            }

    and PaymentFlowsPaymentIntentPresentmentDetails = {
        ///Amount intended to be collected by this payment, denominated in `presentment_currency`.
        PresentmentAmount: int
        ///Currency presented to the customer during payment.
        PresentmentCurrency: IsoTypes.IsoCurrencyCode
    }
    with
        static member New (presentmentAmount: int, presentmentCurrency: IsoTypes.IsoCurrencyCode) =
            {
                PaymentFlowsPaymentIntentPresentmentDetails.PresentmentAmount = presentmentAmount //required
                PaymentFlowsPaymentIntentPresentmentDetails.PresentmentCurrency = presentmentCurrency //required
            }

    and PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesExtendedAuthorizationExtendedAuthorization = {
        ///Indicates whether or not the capture window is extended beyond the standard authorization.
        Status: PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesExtendedAuthorizationExtendedAuthorizationStatus
    }
    with
        static member New (status: PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesExtendedAuthorizationExtendedAuthorizationStatus) =
            {
                PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesExtendedAuthorizationExtendedAuthorization.Status = status //required
            }

    and PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesIncrementalAuthorizationIncrementalAuthorization = {
        ///Indicates whether or not the incremental authorization feature is supported.
        Status: PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesIncrementalAuthorizationIncrementalAuthorizationStatus
    }
    with
        static member New (status: PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesIncrementalAuthorizationIncrementalAuthorizationStatus) =
            {
                PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesIncrementalAuthorizationIncrementalAuthorization.Status = status //required
            }

    and PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesOvercaptureOvercapture = {
        ///The maximum amount that can be captured.
        MaximumAmountCapturable: int
        ///Indicates whether or not the authorized amount can be over-captured.
        Status: PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesOvercaptureOvercaptureStatus
    }
    with
        static member New (maximumAmountCapturable: int, status: PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesOvercaptureOvercaptureStatus) =
            {
                PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesOvercaptureOvercapture.MaximumAmountCapturable = maximumAmountCapturable //required
                PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesOvercaptureOvercapture.Status = status //required
            }

    and PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceMulticapture = {
        ///Indicates whether or not multiple captures are supported.
        Status: PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceMulticaptureStatus
    }
    with
        static member New (status: PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceMulticaptureStatus) =
            {
                PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceMulticapture.Status = status //required
            }

    and PaymentFlowsPrivatePaymentMethodsCardPresentCommonWallet = {
        ///The type of mobile wallet, one of `apple_pay`, `google_pay`, `samsung_pay`, or `unknown`.
        Type: PaymentFlowsPrivatePaymentMethodsCardPresentCommonWalletType
    }
    with
        static member New (``type``: PaymentFlowsPrivatePaymentMethodsCardPresentCommonWalletType) =
            {
                PaymentFlowsPrivatePaymentMethodsCardPresentCommonWallet.Type = ``type`` //required
            }

    and PaymentFlowsPrivatePaymentMethodsFinancialConnectionsCommonLinkedAccountOptionsFilters = {
        ///The account subcategories to use to filter for possible accounts to link. Valid subcategories are `checking` and `savings`.
        AccountSubcategories: PaymentFlowsPrivatePaymentMethodsFinancialConnectionsCommonLinkedAccountOptionsFiltersAccountSubcategories list option
    }
    with
        static member New (?accountSubcategories: PaymentFlowsPrivatePaymentMethodsFinancialConnectionsCommonLinkedAccountOptionsFiltersAccountSubcategories list) =
            {
                PaymentFlowsPrivatePaymentMethodsFinancialConnectionsCommonLinkedAccountOptionsFilters.AccountSubcategories = accountSubcategories
            }

    and PaymentFlowsPrivatePaymentMethodsKakaoPayPaymentMethodOptions = {
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentFlowsPrivatePaymentMethodsKakaoPayPaymentMethodOptionsSetupFutureUsage option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (?setupFutureUsage: PaymentFlowsPrivatePaymentMethodsKakaoPayPaymentMethodOptionsSetupFutureUsage) =
            {
                PaymentFlowsPrivatePaymentMethodsKakaoPayPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
            }

    and PaymentFlowsPrivatePaymentMethodsNaverPayPaymentMethodOptions = {
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentFlowsPrivatePaymentMethodsNaverPayPaymentMethodOptionsSetupFutureUsage option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (?setupFutureUsage: PaymentFlowsPrivatePaymentMethodsNaverPayPaymentMethodOptionsSetupFutureUsage) =
            {
                PaymentFlowsPrivatePaymentMethodsNaverPayPaymentMethodOptions.SetupFutureUsage = setupFutureUsage
            }

    and PaymentFlowsPrivatePaymentMethodsPaypalAmountDetailsLineItemPaymentMethodOptions = {
        ///Type of the line item.
        Category: PaymentFlowsPrivatePaymentMethodsPaypalAmountDetailsLineItemPaymentMethodOptionsCategory option
        ///Description of the line item.
        Description: string option
        ///The Stripe account ID of the connected account that sells the item. This is only needed when using [Separate Charges and Transfers](https://docs.stripe.com/connect/separate-charges-and-transfers).
        SoldBy: string option
    }
    with
        static member New (?category: PaymentFlowsPrivatePaymentMethodsPaypalAmountDetailsLineItemPaymentMethodOptionsCategory, ?description: string, ?soldBy: string) =
            {
                PaymentFlowsPrivatePaymentMethodsPaypalAmountDetailsLineItemPaymentMethodOptions.Category = category
                PaymentFlowsPrivatePaymentMethodsPaypalAmountDetailsLineItemPaymentMethodOptions.Description = description
                PaymentFlowsPrivatePaymentMethodsPaypalAmountDetailsLineItemPaymentMethodOptions.SoldBy = soldBy
            }

    ///A PaymentIntent guides you through the process of collecting a payment from your customer.
    ///We recommend that you create exactly one PaymentIntent for each order or
    ///customer session in your system. You can reference the PaymentIntent later to
    ///see the history of payment attempts for a particular session.
    ///A PaymentIntent transitions through
    ///[multiple statuses](/payments/paymentintents/lifecycle)
    ///throughout its lifetime as it interfaces with Stripe.js to perform
    ///authentication flows and ultimately creates at most one successful charge.
    ///Related guide: [Payment Intents API](https://docs.stripe.com/payments/payment-intents)
    and PaymentIntent = {
        ///Amount intended to be collected by this PaymentIntent. A positive integer representing how much to charge in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal) (e.g., 100 cents to charge $1.00 or 100 to charge ¥100, a zero-decimal currency). The minimum amount is $0.50 US or [equivalent in charge currency](https://docs.stripe.com/currencies#minimum-and-maximum-charge-amounts). The amount value supports up to eight digits (e.g., a value of 99999999 for a USD charge of $999,999.99).
        Amount: int
        ///Amount that can be captured from this PaymentIntent.
        AmountCapturable: int
        AmountDetails: PaymentFlowsAmountDetails option
        ///Amount that this PaymentIntent collects.
        AmountReceived: int
        ///ID of the Connect application that created the PaymentIntent.
        Application: PaymentIntentApplication'AnyOf option
        ///The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. The amount of the application fee collected will be capped at the total amount captured. For more information, see the PaymentIntents [use case for connected accounts](https://docs.stripe.com/payments/connected-accounts).
        ApplicationFeeAmount: int option
        ///Settings to configure compatible payment methods from the [Stripe Dashboard](https://dashboard.stripe.com/settings/payment_methods)
        AutomaticPaymentMethods: PaymentFlowsAutomaticPaymentMethodsPaymentIntent option
        ///Populated when `status` is `canceled`, this is the time at which the PaymentIntent was canceled. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CanceledAt: DateTime option
        ///Reason for cancellation of this PaymentIntent, either user-provided (`duplicate`, `fraudulent`, `requested_by_customer`, or `abandoned`) or generated by Stripe internally (`failed_invoice`, `void_invoice`, `automatic`, or `expired`).
        CancellationReason: PaymentIntentCancellationReason option
        ///Controls when the funds will be captured from the customer's account.
        CaptureMethod: PaymentIntentCaptureMethod
        ///The client secret of this PaymentIntent. Used for client-side retrieval using a publishable key. 
        ///The client secret can be used to complete a payment from your frontend. It should not be stored, logged, or exposed to anyone other than the customer. Make sure that you have TLS enabled on any page that includes the client secret.
        ///Refer to our docs to [accept a payment](https://docs.stripe.com/payments/accept-a-payment?ui=elements) and learn about how `client_secret` should be handled.
        ClientSecret: string option
        ///Describes whether we can confirm this PaymentIntent automatically, or if it requires customer action to confirm the payment.
        ConfirmationMethod: PaymentIntentConfirmationMethod
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///ID of the Customer this PaymentIntent belongs to, if one exists.
        ///Payment methods attached to other Customers cannot be used with this PaymentIntent.
        ///If [setup_future_usage](https://api.stripe.com#payment_intent_object-setup_future_usage) is set and this PaymentIntent's payment method is not `card_present`, then the payment method attaches to the Customer after the PaymentIntent has been confirmed and any required actions from the user are complete. If the payment method is `card_present` and isn't a digital wallet, then a [generated_card](https://docs.stripe.com/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card is created and attached to the Customer instead.
        Customer: PaymentIntentCustomer'AnyOf option
        ///ID of the Account representing the customer that this PaymentIntent belongs to, if one exists.
        ///Payment methods attached to other Accounts cannot be used with this PaymentIntent.
        ///If [setup_future_usage](https://api.stripe.com#payment_intent_object-setup_future_usage) is set and this PaymentIntent's payment method is not `card_present`, then the payment method attaches to the Account after the PaymentIntent has been confirmed and any required actions from the user are complete. If the payment method is `card_present` and isn't a digital wallet, then a [generated_card](https://docs.stripe.com/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card is created and attached to the Account instead.
        CustomerAccount: string option
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///The list of payment method types to exclude from use with this payment.
        ExcludedPaymentMethodTypes: PaymentIntentExcludedPaymentMethodTypes list option
        Hooks: PaymentFlowsPaymentIntentAsyncWorkflows option
        ///Unique identifier for the object.
        Id: string
        ///The payment error encountered in the previous PaymentIntent confirmation. It will be cleared if the PaymentIntent is later updated for any reason.
        LastPaymentError: ApiErrors option
        ///ID of the latest [Charge object](https://docs.stripe.com/api/charges) created by this PaymentIntent. This property is `null` until PaymentIntent confirmation is attempted.
        LatestCharge: PaymentIntentLatestCharge'AnyOf option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Settings for Managed Payments.
        ManagedPayments: SmorResourceManagedPayments option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Learn more about [storing information in metadata](https://docs.stripe.com/payments/payment-intents/creating-payment-intents#storing-information-in-metadata).
        Metadata: Map<string, string>
        ///If present, this property tells you what actions you need to take in order for your customer to fulfill a payment using the provided source.
        NextAction: PaymentIntentNextAction option
        ///You can specify the settlement merchant as the
        ///connected account using the `on_behalf_of` attribute on the charge. See the PaymentIntents [use case for connected accounts](/payments/connected-accounts) for details.
        OnBehalfOf: PaymentIntentOnBehalfOf'AnyOf option
        PaymentDetails: PaymentFlowsPaymentDetails option
        ///ID of the payment method used in this PaymentIntent.
        PaymentMethod: PaymentIntentPaymentMethod'AnyOf option
        ///Information about the [payment method configuration](https://docs.stripe.com/api/payment_method_configurations) used for this PaymentIntent.
        PaymentMethodConfigurationDetails: PaymentMethodConfigBizPaymentMethodConfigurationDetails option
        ///Payment-method-specific configuration for this PaymentIntent.
        PaymentMethodOptions: PaymentIntentPaymentMethodOptions option
        ///The list of payment method types (e.g. card) that this PaymentIntent is allowed to use. A comprehensive list of valid payment method types can be found [here](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type).
        PaymentMethodTypes: string list
        PresentmentDetails: PaymentFlowsPaymentIntentPresentmentDetails option
        ///If present, this property tells you about the processing state of the payment.
        Processing: PaymentIntentProcessing option
        ///Email address that the receipt for the resulting payment will be sent to. If `receipt_email` is specified for a payment in live mode, a receipt will be sent regardless of your [email settings](https://dashboard.stripe.com/account/emails).
        ReceiptEmail: string option
        ///ID of the review associated with this PaymentIntent, if any.
        Review: PaymentIntentReview'AnyOf option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentIntentSetupFutureUsage option
        ///Shipping information for this PaymentIntent.
        Shipping: Shipping option
        ///This is a legacy field that will be removed in the future. It is the ID of the Source object that is associated with this PaymentIntent, if one was supplied.
        Source: PaymentIntentSource'AnyOf option
        ///Text that appears on the customer's statement as the statement descriptor for a non-card charge. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).
        ///Setting this value for a card charge returns an error. For card charges, set the [statement_descriptor_suffix](https://docs.stripe.com/get-started/account/statement-descriptors#dynamic) instead.
        StatementDescriptor: string option
        ///Provides information about a card charge. Concatenated to the account's [statement descriptor prefix](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the customer's statement.
        StatementDescriptorSuffix: string option
        ///Status of this PaymentIntent, one of `requires_payment_method`, `requires_confirmation`, `requires_action`, `processing`, `requires_capture`, `canceled`, or `succeeded`. Read more about each PaymentIntent [status](https://docs.stripe.com/payments/intents#intent-statuses).
        Status: PaymentIntentStatus
        ///The data that automatically creates a Transfer after the payment finalizes. Learn more about the [use case for connected accounts](https://docs.stripe.com/payments/connected-accounts).
        TransferData: TransferData option
        ///A string that identifies the resulting payment as part of a group. Learn more about the [use case for connected accounts](https://docs.stripe.com/connect/separate-charges-and-transfers).
        TransferGroup: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "payment_intent"

        static member New (amount: int, amountCapturable: int, amountReceived: int, application: PaymentIntentApplication'AnyOf option, applicationFeeAmount: int option, automaticPaymentMethods: PaymentFlowsAutomaticPaymentMethodsPaymentIntent option, canceledAt: DateTime option, cancellationReason: PaymentIntentCancellationReason option, captureMethod: PaymentIntentCaptureMethod, clientSecret: string option, confirmationMethod: PaymentIntentConfirmationMethod, created: DateTime, currency: IsoTypes.IsoCurrencyCode, customer: PaymentIntentCustomer'AnyOf option, customerAccount: string option, description: string option, excludedPaymentMethodTypes: PaymentIntentExcludedPaymentMethodTypes list option, id: string, lastPaymentError: ApiErrors option, latestCharge: PaymentIntentLatestCharge'AnyOf option, livemode: bool, managedPayments: SmorResourceManagedPayments option, metadata: Map<string, string>, nextAction: PaymentIntentNextAction option, onBehalfOf: PaymentIntentOnBehalfOf'AnyOf option, paymentMethod: PaymentIntentPaymentMethod'AnyOf option, paymentMethodConfigurationDetails: PaymentMethodConfigBizPaymentMethodConfigurationDetails option, paymentMethodOptions: PaymentIntentPaymentMethodOptions option, paymentMethodTypes: string list, processing: PaymentIntentProcessing option, receiptEmail: string option, review: PaymentIntentReview'AnyOf option, setupFutureUsage: PaymentIntentSetupFutureUsage option, shipping: Shipping option, source: PaymentIntentSource'AnyOf option, statementDescriptor: string option, statementDescriptorSuffix: string option, status: PaymentIntentStatus, transferGroup: string option, ?amountDetails: PaymentFlowsAmountDetails, ?hooks: PaymentFlowsPaymentIntentAsyncWorkflows, ?paymentDetails: PaymentFlowsPaymentDetails, ?presentmentDetails: PaymentFlowsPaymentIntentPresentmentDetails, ?transferData: TransferData option) =
            {
                PaymentIntent.Amount = amount //required
                PaymentIntent.AmountCapturable = amountCapturable //required
                PaymentIntent.AmountReceived = amountReceived //required
                PaymentIntent.Application = application //required
                PaymentIntent.ApplicationFeeAmount = applicationFeeAmount //required
                PaymentIntent.AutomaticPaymentMethods = automaticPaymentMethods //required
                PaymentIntent.CanceledAt = canceledAt //required
                PaymentIntent.CancellationReason = cancellationReason //required
                PaymentIntent.CaptureMethod = captureMethod //required
                PaymentIntent.ClientSecret = clientSecret //required
                PaymentIntent.ConfirmationMethod = confirmationMethod //required
                PaymentIntent.Created = created //required
                PaymentIntent.Currency = currency //required
                PaymentIntent.Customer = customer //required
                PaymentIntent.CustomerAccount = customerAccount //required
                PaymentIntent.Description = description //required
                PaymentIntent.ExcludedPaymentMethodTypes = excludedPaymentMethodTypes //required
                PaymentIntent.Id = id //required
                PaymentIntent.LastPaymentError = lastPaymentError //required
                PaymentIntent.LatestCharge = latestCharge //required
                PaymentIntent.Livemode = livemode //required
                PaymentIntent.ManagedPayments = managedPayments //required
                PaymentIntent.Metadata = metadata //required
                PaymentIntent.NextAction = nextAction //required
                PaymentIntent.OnBehalfOf = onBehalfOf //required
                PaymentIntent.PaymentMethod = paymentMethod //required
                PaymentIntent.PaymentMethodConfigurationDetails = paymentMethodConfigurationDetails //required
                PaymentIntent.PaymentMethodOptions = paymentMethodOptions //required
                PaymentIntent.PaymentMethodTypes = paymentMethodTypes //required
                PaymentIntent.Processing = processing //required
                PaymentIntent.ReceiptEmail = receiptEmail //required
                PaymentIntent.Review = review //required
                PaymentIntent.SetupFutureUsage = setupFutureUsage //required
                PaymentIntent.Shipping = shipping //required
                PaymentIntent.Source = source //required
                PaymentIntent.StatementDescriptor = statementDescriptor //required
                PaymentIntent.StatementDescriptorSuffix = statementDescriptorSuffix //required
                PaymentIntent.Status = status //required
                PaymentIntent.TransferGroup = transferGroup //required
                PaymentIntent.AmountDetails = amountDetails
                PaymentIntent.Hooks = hooks
                PaymentIntent.PaymentDetails = paymentDetails
                PaymentIntent.PresentmentDetails = presentmentDetails
                PaymentIntent.TransferData = transferData |> Option.flatten
            }

    and PaymentIntentApplication'AnyOf =
        | String of string
        | Application of Application

    and PaymentIntentCustomer'AnyOf =
        | String of string
        | Customer of Customer
        | DeletedCustomer of DeletedCustomer

    and PaymentIntentLatestCharge'AnyOf =
        | String of string
        | Charge of Charge

    and PaymentIntentOnBehalfOf'AnyOf =
        | String of string
        | Account of Account

    and PaymentIntentPaymentMethod'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and PaymentIntentReview'AnyOf =
        | String of string
        | Review of Review

    and PaymentIntentSource'AnyOf =
        | String of string
        | PaymentSource of PaymentSource
        | DeletedPaymentSource of DeletedPaymentSource

    and PaymentIntentAmountDetailsLineItem = {
        ///The discount applied on this line item represented in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). An integer greater than 0.
        ///This field is mutually exclusive with the `amount_details[discount_amount]` field.
        DiscountAmount: int option
        ///Unique identifier for the object.
        Id: string
        ///Payment method-specific information for line items.
        PaymentMethodOptions: PaymentFlowsAmountDetailsResourceLineItemsListResourceLineItemResourcePaymentMethodOptions option
        ///The product code of the line item, such as an SKU. Required for L3 rates. At most 12 characters long.
        ProductCode: string option
        ///The product name of the line item. Required for L3 rates. At most 1024 characters long.
        ///For Cards, this field is truncated to 26 alphanumeric characters before being sent to the card networks. For PayPal, this field is truncated to 127 characters.
        ProductName: string
        ///The quantity of items. Required for L3 rates. An integer greater than 0.
        Quantity: int
        ///Contains information about the tax on the item.
        Tax: PaymentFlowsAmountDetailsResourceLineItemsListResourceLineItemResourceTax option
        ///The unit cost of the line item represented in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). Required for L3 rates. An integer greater than or equal to 0.
        UnitCost: int
        ///A unit of measure for the line item, such as gallons, feet, meters, etc. Required for L3 rates. At most 12 alphanumeric characters long.
        UnitOfMeasure: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "payment_intent_amount_details_line_item"

        static member New (discountAmount: int option, id: string, paymentMethodOptions: PaymentFlowsAmountDetailsResourceLineItemsListResourceLineItemResourcePaymentMethodOptions option, productCode: string option, productName: string, quantity: int, tax: PaymentFlowsAmountDetailsResourceLineItemsListResourceLineItemResourceTax option, unitCost: int, unitOfMeasure: string option) =
            {
                PaymentIntentAmountDetailsLineItem.DiscountAmount = discountAmount //required
                PaymentIntentAmountDetailsLineItem.Id = id //required
                PaymentIntentAmountDetailsLineItem.PaymentMethodOptions = paymentMethodOptions //required
                PaymentIntentAmountDetailsLineItem.ProductCode = productCode //required
                PaymentIntentAmountDetailsLineItem.ProductName = productName //required
                PaymentIntentAmountDetailsLineItem.Quantity = quantity //required
                PaymentIntentAmountDetailsLineItem.Tax = tax //required
                PaymentIntentAmountDetailsLineItem.UnitCost = unitCost //required
                PaymentIntentAmountDetailsLineItem.UnitOfMeasure = unitOfMeasure //required
            }

    and PaymentIntentCardProcessing = {
        CustomerNotification: PaymentIntentProcessingCustomerNotification option
    }
    with
        static member New (?customerNotification: PaymentIntentProcessingCustomerNotification) =
            {
                PaymentIntentCardProcessing.CustomerNotification = customerNotification
            }

    and PaymentIntentNextAction = {
        AlipayHandleRedirect: PaymentIntentNextActionAlipayHandleRedirect option
        BoletoDisplayDetails: PaymentIntentNextActionBoleto option
        CardAwaitNotification: PaymentIntentNextActionCardAwaitNotification option
        CashappHandleRedirectOrDisplayQrCode: PaymentIntentNextActionCashappHandleRedirectOrDisplayQrCode option
        DisplayBankTransferInstructions: PaymentIntentNextActionDisplayBankTransferInstructions option
        KlarnaDisplayQrCode: PaymentIntentNextActionKlarnaDisplayQrCode option
        KonbiniDisplayDetails: PaymentIntentNextActionKonbini option
        MultibancoDisplayDetails: PaymentIntentNextActionDisplayMultibancoDetails option
        OxxoDisplayDetails: PaymentIntentNextActionDisplayOxxoDetails option
        PaynowDisplayQrCode: PaymentIntentNextActionPaynowDisplayQrCode option
        PixDisplayQrCode: PaymentIntentNextActionPixDisplayQrCode option
        PromptpayDisplayQrCode: PaymentIntentNextActionPromptpayDisplayQrCode option
        RedirectToUrl: PaymentIntentNextActionRedirectToUrl option
        SwishHandleRedirectOrDisplayQrCode: PaymentIntentNextActionSwishHandleRedirectOrDisplayQrCode option
        ///Type of the next action to perform. Refer to the other child attributes under `next_action` for available values. Examples include: `redirect_to_url`, `use_stripe_sdk`, `alipay_handle_redirect`, `oxxo_display_details`, or `verify_with_microdeposits`.
        Type: PaymentIntentNextActionType
        UpiHandleRedirectOrDisplayQrCode: PaymentIntentNextActionUpiHandleRedirectOrDisplayQrCode option
        ///When confirming a PaymentIntent with Stripe.js, Stripe.js depends on the contents of this dictionary to invoke authentication flows. The shape of the contents is subject to change and is only intended to be used by Stripe.js.
        UseStripeSdk: string option
        VerifyWithMicrodeposits: PaymentIntentNextActionVerifyWithMicrodeposits option
        WechatPayDisplayQrCode: PaymentIntentNextActionWechatPayDisplayQrCode option
        WechatPayRedirectToAndroidApp: PaymentIntentNextActionWechatPayRedirectToAndroidApp option
        WechatPayRedirectToIosApp: PaymentIntentNextActionWechatPayRedirectToIosApp option
    }
    with
        static member New (``type``: PaymentIntentNextActionType, ?alipayHandleRedirect: PaymentIntentNextActionAlipayHandleRedirect, ?boletoDisplayDetails: PaymentIntentNextActionBoleto, ?cardAwaitNotification: PaymentIntentNextActionCardAwaitNotification, ?cashappHandleRedirectOrDisplayQrCode: PaymentIntentNextActionCashappHandleRedirectOrDisplayQrCode, ?displayBankTransferInstructions: PaymentIntentNextActionDisplayBankTransferInstructions, ?klarnaDisplayQrCode: PaymentIntentNextActionKlarnaDisplayQrCode, ?konbiniDisplayDetails: PaymentIntentNextActionKonbini, ?multibancoDisplayDetails: PaymentIntentNextActionDisplayMultibancoDetails, ?oxxoDisplayDetails: PaymentIntentNextActionDisplayOxxoDetails, ?paynowDisplayQrCode: PaymentIntentNextActionPaynowDisplayQrCode, ?pixDisplayQrCode: PaymentIntentNextActionPixDisplayQrCode, ?promptpayDisplayQrCode: PaymentIntentNextActionPromptpayDisplayQrCode, ?redirectToUrl: PaymentIntentNextActionRedirectToUrl, ?swishHandleRedirectOrDisplayQrCode: PaymentIntentNextActionSwishHandleRedirectOrDisplayQrCode, ?upiHandleRedirectOrDisplayQrCode: PaymentIntentNextActionUpiHandleRedirectOrDisplayQrCode, ?useStripeSdk: string, ?verifyWithMicrodeposits: PaymentIntentNextActionVerifyWithMicrodeposits, ?wechatPayDisplayQrCode: PaymentIntentNextActionWechatPayDisplayQrCode, ?wechatPayRedirectToAndroidApp: PaymentIntentNextActionWechatPayRedirectToAndroidApp, ?wechatPayRedirectToIosApp: PaymentIntentNextActionWechatPayRedirectToIosApp) =
            {
                PaymentIntentNextAction.Type = ``type`` //required
                PaymentIntentNextAction.AlipayHandleRedirect = alipayHandleRedirect
                PaymentIntentNextAction.BoletoDisplayDetails = boletoDisplayDetails
                PaymentIntentNextAction.CardAwaitNotification = cardAwaitNotification
                PaymentIntentNextAction.CashappHandleRedirectOrDisplayQrCode = cashappHandleRedirectOrDisplayQrCode
                PaymentIntentNextAction.DisplayBankTransferInstructions = displayBankTransferInstructions
                PaymentIntentNextAction.KlarnaDisplayQrCode = klarnaDisplayQrCode
                PaymentIntentNextAction.KonbiniDisplayDetails = konbiniDisplayDetails
                PaymentIntentNextAction.MultibancoDisplayDetails = multibancoDisplayDetails
                PaymentIntentNextAction.OxxoDisplayDetails = oxxoDisplayDetails
                PaymentIntentNextAction.PaynowDisplayQrCode = paynowDisplayQrCode
                PaymentIntentNextAction.PixDisplayQrCode = pixDisplayQrCode
                PaymentIntentNextAction.PromptpayDisplayQrCode = promptpayDisplayQrCode
                PaymentIntentNextAction.RedirectToUrl = redirectToUrl
                PaymentIntentNextAction.SwishHandleRedirectOrDisplayQrCode = swishHandleRedirectOrDisplayQrCode
                PaymentIntentNextAction.UpiHandleRedirectOrDisplayQrCode = upiHandleRedirectOrDisplayQrCode
                PaymentIntentNextAction.UseStripeSdk = useStripeSdk
                PaymentIntentNextAction.VerifyWithMicrodeposits = verifyWithMicrodeposits
                PaymentIntentNextAction.WechatPayDisplayQrCode = wechatPayDisplayQrCode
                PaymentIntentNextAction.WechatPayRedirectToAndroidApp = wechatPayRedirectToAndroidApp
                PaymentIntentNextAction.WechatPayRedirectToIosApp = wechatPayRedirectToIosApp
            }

    and PaymentIntentNextActionCashappHandleRedirectOrDisplayQrCode = {
        ///The URL to the hosted Cash App Pay instructions page, which allows customers to view the QR code, and supports QR code refreshing on expiration.
        HostedInstructionsUrl: string
        ///The url for mobile redirect based auth
        MobileAuthUrl: string
        QrCode: PaymentIntentNextActionCashappQrCode
    }
    with
        static member New (hostedInstructionsUrl: string, mobileAuthUrl: string, qrCode: PaymentIntentNextActionCashappQrCode) =
            {
                PaymentIntentNextActionCashappHandleRedirectOrDisplayQrCode.HostedInstructionsUrl = hostedInstructionsUrl //required
                PaymentIntentNextActionCashappHandleRedirectOrDisplayQrCode.MobileAuthUrl = mobileAuthUrl //required
                PaymentIntentNextActionCashappHandleRedirectOrDisplayQrCode.QrCode = qrCode //required
            }

    and PaymentIntentNextActionDisplayBankTransferInstructions = {
        ///The remaining amount that needs to be transferred to complete the payment.
        AmountRemaining: int option
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode option
        ///A list of financial addresses that can be used to fund the customer balance
        FinancialAddresses: FundingInstructionsBankTransferFinancialAddress list option
        ///A link to a hosted page that guides your customer through completing the transfer.
        HostedInstructionsUrl: string option
        ///A string identifying this payment. Instruct your customer to include this code in the reference or memo field of their bank transfer.
        Reference: string option
        ///Type of bank transfer
        Type: PaymentIntentNextActionDisplayBankTransferInstructionsType
    }
    with
        static member New (amountRemaining: int option, currency: IsoTypes.IsoCurrencyCode option, hostedInstructionsUrl: string option, reference: string option, ``type``: PaymentIntentNextActionDisplayBankTransferInstructionsType, ?financialAddresses: FundingInstructionsBankTransferFinancialAddress list) =
            {
                PaymentIntentNextActionDisplayBankTransferInstructions.AmountRemaining = amountRemaining //required
                PaymentIntentNextActionDisplayBankTransferInstructions.Currency = currency //required
                PaymentIntentNextActionDisplayBankTransferInstructions.HostedInstructionsUrl = hostedInstructionsUrl //required
                PaymentIntentNextActionDisplayBankTransferInstructions.Reference = reference //required
                PaymentIntentNextActionDisplayBankTransferInstructions.Type = ``type`` //required
                PaymentIntentNextActionDisplayBankTransferInstructions.FinancialAddresses = financialAddresses
            }

    and PaymentIntentNextActionKonbini = {
        ///The timestamp at which the pending Konbini payment expires.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime
        ///The URL for the Konbini payment instructions page, which allows customers to view and print a Konbini voucher.
        HostedVoucherUrl: string option
        Stores: PaymentIntentNextActionKonbiniStores
    }
    with
        static member New (expiresAt: DateTime, hostedVoucherUrl: string option, stores: PaymentIntentNextActionKonbiniStores) =
            {
                PaymentIntentNextActionKonbini.ExpiresAt = expiresAt //required
                PaymentIntentNextActionKonbini.HostedVoucherUrl = hostedVoucherUrl //required
                PaymentIntentNextActionKonbini.Stores = stores //required
            }

    and PaymentIntentNextActionKonbiniStores = {
        ///FamilyMart instruction details.
        Familymart: PaymentIntentNextActionKonbiniFamilymart option
        ///Lawson instruction details.
        Lawson: PaymentIntentNextActionKonbiniLawson option
        ///Ministop instruction details.
        Ministop: PaymentIntentNextActionKonbiniMinistop option
        ///Seicomart instruction details.
        Seicomart: PaymentIntentNextActionKonbiniSeicomart option
    }
    with
        static member New (familymart: PaymentIntentNextActionKonbiniFamilymart option, lawson: PaymentIntentNextActionKonbiniLawson option, ministop: PaymentIntentNextActionKonbiniMinistop option, seicomart: PaymentIntentNextActionKonbiniSeicomart option) =
            {
                PaymentIntentNextActionKonbiniStores.Familymart = familymart //required
                PaymentIntentNextActionKonbiniStores.Lawson = lawson //required
                PaymentIntentNextActionKonbiniStores.Ministop = ministop //required
                PaymentIntentNextActionKonbiniStores.Seicomart = seicomart //required
            }

    and PaymentIntentNextActionSwishHandleRedirectOrDisplayQrCode = {
        ///The URL to the hosted Swish instructions page, which allows customers to view the QR code.
        HostedInstructionsUrl: string
        ///The url for mobile redirect based auth (for internal use only and not typically available in standard API requests).
        MobileAuthUrl: string
        QrCode: PaymentIntentNextActionSwishQrCode
    }
    with
        static member New (hostedInstructionsUrl: string, mobileAuthUrl: string, qrCode: PaymentIntentNextActionSwishQrCode) =
            {
                PaymentIntentNextActionSwishHandleRedirectOrDisplayQrCode.HostedInstructionsUrl = hostedInstructionsUrl //required
                PaymentIntentNextActionSwishHandleRedirectOrDisplayQrCode.MobileAuthUrl = mobileAuthUrl //required
                PaymentIntentNextActionSwishHandleRedirectOrDisplayQrCode.QrCode = qrCode //required
            }

    and PaymentIntentNextActionUpiHandleRedirectOrDisplayQrCode = {
        ///The URL to the hosted UPI instructions page, which allows customers to view the QR code.
        HostedInstructionsUrl: string
        QrCode: PaymentIntentNextActionUpiqrCode
    }
    with
        static member New (hostedInstructionsUrl: string, qrCode: PaymentIntentNextActionUpiqrCode) =
            {
                PaymentIntentNextActionUpiHandleRedirectOrDisplayQrCode.HostedInstructionsUrl = hostedInstructionsUrl //required
                PaymentIntentNextActionUpiHandleRedirectOrDisplayQrCode.QrCode = qrCode //required
            }

    and PaymentIntentNextActionVerifyWithMicrodeposits = {
        ///The timestamp when the microdeposits are expected to land.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ArrivalDate: DateTime
        ///The URL for the hosted verification page, which allows customers to verify their bank account.
        HostedVerificationUrl: string
        ///The type of the microdeposit sent to the customer. Used to distinguish between different verification methods.
        MicrodepositType: PaymentIntentNextActionVerifyWithMicrodepositsMicrodepositType option
    }
    with
        static member New (arrivalDate: DateTime, hostedVerificationUrl: string, microdepositType: PaymentIntentNextActionVerifyWithMicrodepositsMicrodepositType option) =
            {
                PaymentIntentNextActionVerifyWithMicrodeposits.ArrivalDate = arrivalDate //required
                PaymentIntentNextActionVerifyWithMicrodeposits.HostedVerificationUrl = hostedVerificationUrl //required
                PaymentIntentNextActionVerifyWithMicrodeposits.MicrodepositType = microdepositType //required
            }

    and PaymentIntentPaymentMethodOptions = {
        AcssDebit: PaymentIntentPaymentMethodOptionsAcssDebit option
        Affirm: PaymentMethodOptionsAffirm option
        AfterpayClearpay: PaymentMethodOptionsAfterpayClearpay option
        Alipay: PaymentMethodOptionsAlipay option
        Alma: PaymentMethodOptionsAlma option
        AmazonPay: PaymentMethodOptionsAmazonPay option
        AuBecsDebit: PaymentIntentPaymentMethodOptionsAuBecsDebit option
        BacsDebit: PaymentIntentPaymentMethodOptionsBacsDebit option
        Bancontact: PaymentMethodOptionsBancontact option
        Billie: PaymentMethodOptionsBillie option
        Blik: PaymentIntentPaymentMethodOptionsBlik option
        Boleto: PaymentMethodOptionsBoleto option
        Card: PaymentIntentPaymentMethodOptionsCard option
        CardPresent: PaymentMethodOptionsCardPresent option
        Cashapp: PaymentMethodOptionsCashapp option
        Crypto: PaymentMethodOptionsCrypto option
        CustomerBalance: PaymentMethodOptionsCustomerBalance option
        Eps: PaymentIntentPaymentMethodOptionsEps option
        Fpx: PaymentMethodOptionsFpx option
        Giropay: PaymentMethodOptionsGiropay option
        Grabpay: PaymentMethodOptionsGrabpay option
        Ideal: PaymentMethodOptionsIdeal option
        InteracPresent: PaymentMethodOptionsInteracPresent option
        KakaoPay: PaymentFlowsPrivatePaymentMethodsKakaoPayPaymentMethodOptions option
        Klarna: PaymentMethodOptionsKlarna option
        Konbini: PaymentMethodOptionsKonbini option
        KrCard: PaymentMethodOptionsKrCard option
        Link: PaymentIntentPaymentMethodOptionsLink option
        MbWay: PaymentMethodOptionsMbWay option
        Mobilepay: PaymentIntentPaymentMethodOptionsMobilepay option
        Multibanco: PaymentMethodOptionsMultibanco option
        NaverPay: PaymentFlowsPrivatePaymentMethodsNaverPayPaymentMethodOptions option
        NzBankAccount: PaymentIntentPaymentMethodOptionsNzBankAccount option
        Oxxo: PaymentMethodOptionsOxxo option
        [<JsonField(Name="p24")>]P24: PaymentMethodOptionsP24 option
        PayByBank: PaymentMethodOptionsPayByBank option
        Payco: PaymentFlowsPrivatePaymentMethodsPaycoPaymentMethodOptions option
        Paynow: PaymentMethodOptionsPaynow option
        Paypal: PaymentMethodOptionsPaypal option
        Payto: PaymentIntentPaymentMethodOptionsPayto option
        Pix: PaymentMethodOptionsPix option
        Promptpay: PaymentMethodOptionsPromptpay option
        RevolutPay: PaymentMethodOptionsRevolutPay option
        SamsungPay: PaymentFlowsPrivatePaymentMethodsSamsungPayPaymentMethodOptions option
        Satispay: PaymentMethodOptionsSatispay option
        SepaDebit: PaymentIntentPaymentMethodOptionsSepaDebit option
        Sofort: PaymentMethodOptionsSofort option
        Swish: PaymentIntentPaymentMethodOptionsSwish option
        Twint: PaymentMethodOptionsTwint option
        Upi: PaymentMethodOptionsUpi option
        UsBankAccount: PaymentIntentPaymentMethodOptionsUsBankAccount option
        WechatPay: PaymentMethodOptionsWechatPay option
        Zip: PaymentMethodOptionsZip option
    }
    with
        static member New (?acssDebit: PaymentIntentPaymentMethodOptionsAcssDebit, ?affirm: PaymentMethodOptionsAffirm, ?afterpayClearpay: PaymentMethodOptionsAfterpayClearpay, ?alipay: PaymentMethodOptionsAlipay, ?alma: PaymentMethodOptionsAlma, ?amazonPay: PaymentMethodOptionsAmazonPay, ?auBecsDebit: PaymentIntentPaymentMethodOptionsAuBecsDebit, ?bacsDebit: PaymentIntentPaymentMethodOptionsBacsDebit, ?bancontact: PaymentMethodOptionsBancontact, ?billie: PaymentMethodOptionsBillie, ?blik: PaymentIntentPaymentMethodOptionsBlik, ?boleto: PaymentMethodOptionsBoleto, ?card: PaymentIntentPaymentMethodOptionsCard, ?cardPresent: PaymentMethodOptionsCardPresent, ?cashapp: PaymentMethodOptionsCashapp, ?crypto: PaymentMethodOptionsCrypto, ?customerBalance: PaymentMethodOptionsCustomerBalance, ?eps: PaymentIntentPaymentMethodOptionsEps, ?fpx: PaymentMethodOptionsFpx, ?giropay: PaymentMethodOptionsGiropay, ?grabpay: PaymentMethodOptionsGrabpay, ?ideal: PaymentMethodOptionsIdeal, ?interacPresent: PaymentMethodOptionsInteracPresent, ?kakaoPay: PaymentFlowsPrivatePaymentMethodsKakaoPayPaymentMethodOptions, ?klarna: PaymentMethodOptionsKlarna, ?konbini: PaymentMethodOptionsKonbini, ?krCard: PaymentMethodOptionsKrCard, ?link: PaymentIntentPaymentMethodOptionsLink, ?mbWay: PaymentMethodOptionsMbWay, ?mobilepay: PaymentIntentPaymentMethodOptionsMobilepay, ?multibanco: PaymentMethodOptionsMultibanco, ?naverPay: PaymentFlowsPrivatePaymentMethodsNaverPayPaymentMethodOptions, ?nzBankAccount: PaymentIntentPaymentMethodOptionsNzBankAccount, ?oxxo: PaymentMethodOptionsOxxo, ?p24: PaymentMethodOptionsP24, ?payByBank: PaymentMethodOptionsPayByBank, ?payco: PaymentFlowsPrivatePaymentMethodsPaycoPaymentMethodOptions, ?paynow: PaymentMethodOptionsPaynow, ?paypal: PaymentMethodOptionsPaypal, ?payto: PaymentIntentPaymentMethodOptionsPayto, ?pix: PaymentMethodOptionsPix, ?promptpay: PaymentMethodOptionsPromptpay, ?revolutPay: PaymentMethodOptionsRevolutPay, ?samsungPay: PaymentFlowsPrivatePaymentMethodsSamsungPayPaymentMethodOptions, ?satispay: PaymentMethodOptionsSatispay, ?sepaDebit: PaymentIntentPaymentMethodOptionsSepaDebit, ?sofort: PaymentMethodOptionsSofort, ?swish: PaymentIntentPaymentMethodOptionsSwish, ?twint: PaymentMethodOptionsTwint, ?upi: PaymentMethodOptionsUpi, ?usBankAccount: PaymentIntentPaymentMethodOptionsUsBankAccount, ?wechatPay: PaymentMethodOptionsWechatPay, ?zip: PaymentMethodOptionsZip) =
            {
                PaymentIntentPaymentMethodOptions.AcssDebit = acssDebit
                PaymentIntentPaymentMethodOptions.Affirm = affirm
                PaymentIntentPaymentMethodOptions.AfterpayClearpay = afterpayClearpay
                PaymentIntentPaymentMethodOptions.Alipay = alipay
                PaymentIntentPaymentMethodOptions.Alma = alma
                PaymentIntentPaymentMethodOptions.AmazonPay = amazonPay
                PaymentIntentPaymentMethodOptions.AuBecsDebit = auBecsDebit
                PaymentIntentPaymentMethodOptions.BacsDebit = bacsDebit
                PaymentIntentPaymentMethodOptions.Bancontact = bancontact
                PaymentIntentPaymentMethodOptions.Billie = billie
                PaymentIntentPaymentMethodOptions.Blik = blik
                PaymentIntentPaymentMethodOptions.Boleto = boleto
                PaymentIntentPaymentMethodOptions.Card = card
                PaymentIntentPaymentMethodOptions.CardPresent = cardPresent
                PaymentIntentPaymentMethodOptions.Cashapp = cashapp
                PaymentIntentPaymentMethodOptions.Crypto = crypto
                PaymentIntentPaymentMethodOptions.CustomerBalance = customerBalance
                PaymentIntentPaymentMethodOptions.Eps = eps
                PaymentIntentPaymentMethodOptions.Fpx = fpx
                PaymentIntentPaymentMethodOptions.Giropay = giropay
                PaymentIntentPaymentMethodOptions.Grabpay = grabpay
                PaymentIntentPaymentMethodOptions.Ideal = ideal
                PaymentIntentPaymentMethodOptions.InteracPresent = interacPresent
                PaymentIntentPaymentMethodOptions.KakaoPay = kakaoPay
                PaymentIntentPaymentMethodOptions.Klarna = klarna
                PaymentIntentPaymentMethodOptions.Konbini = konbini
                PaymentIntentPaymentMethodOptions.KrCard = krCard
                PaymentIntentPaymentMethodOptions.Link = link
                PaymentIntentPaymentMethodOptions.MbWay = mbWay
                PaymentIntentPaymentMethodOptions.Mobilepay = mobilepay
                PaymentIntentPaymentMethodOptions.Multibanco = multibanco
                PaymentIntentPaymentMethodOptions.NaverPay = naverPay
                PaymentIntentPaymentMethodOptions.NzBankAccount = nzBankAccount
                PaymentIntentPaymentMethodOptions.Oxxo = oxxo
                PaymentIntentPaymentMethodOptions.P24 = p24
                PaymentIntentPaymentMethodOptions.PayByBank = payByBank
                PaymentIntentPaymentMethodOptions.Payco = payco
                PaymentIntentPaymentMethodOptions.Paynow = paynow
                PaymentIntentPaymentMethodOptions.Paypal = paypal
                PaymentIntentPaymentMethodOptions.Payto = payto
                PaymentIntentPaymentMethodOptions.Pix = pix
                PaymentIntentPaymentMethodOptions.Promptpay = promptpay
                PaymentIntentPaymentMethodOptions.RevolutPay = revolutPay
                PaymentIntentPaymentMethodOptions.SamsungPay = samsungPay
                PaymentIntentPaymentMethodOptions.Satispay = satispay
                PaymentIntentPaymentMethodOptions.SepaDebit = sepaDebit
                PaymentIntentPaymentMethodOptions.Sofort = sofort
                PaymentIntentPaymentMethodOptions.Swish = swish
                PaymentIntentPaymentMethodOptions.Twint = twint
                PaymentIntentPaymentMethodOptions.Upi = upi
                PaymentIntentPaymentMethodOptions.UsBankAccount = usBankAccount
                PaymentIntentPaymentMethodOptions.WechatPay = wechatPay
                PaymentIntentPaymentMethodOptions.Zip = zip
            }

    and PaymentIntentPaymentMethodOptionsAcssDebit = {
        MandateOptions: PaymentIntentPaymentMethodOptionsMandateOptionsAcssDebit option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentIntentPaymentMethodOptionsAcssDebitSetupFutureUsage option
        ///Controls when Stripe will attempt to debit the funds from the customer's account. The date must be a string in YYYY-MM-DD format. The date must be in the future and between 3 and 15 calendar days from now.
        TargetDate: string option
        ///Bank account verification method. The default value is `automatic`.
        VerificationMethod: PaymentIntentPaymentMethodOptionsAcssDebitVerificationMethod option
    }
    with
        static member New (?mandateOptions: PaymentIntentPaymentMethodOptionsMandateOptionsAcssDebit, ?setupFutureUsage: PaymentIntentPaymentMethodOptionsAcssDebitSetupFutureUsage, ?targetDate: string, ?verificationMethod: PaymentIntentPaymentMethodOptionsAcssDebitVerificationMethod) =
            {
                PaymentIntentPaymentMethodOptionsAcssDebit.MandateOptions = mandateOptions
                PaymentIntentPaymentMethodOptionsAcssDebit.SetupFutureUsage = setupFutureUsage
                PaymentIntentPaymentMethodOptionsAcssDebit.TargetDate = targetDate
                PaymentIntentPaymentMethodOptionsAcssDebit.VerificationMethod = verificationMethod
            }

    and PaymentIntentPaymentMethodOptionsAuBecsDebit = {
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentIntentPaymentMethodOptionsAuBecsDebitSetupFutureUsage option
        ///Controls when Stripe will attempt to debit the funds from the customer's account. The date must be a string in YYYY-MM-DD format. The date must be in the future and between 3 and 15 calendar days from now.
        TargetDate: string option
    }
    with
        static member New (?setupFutureUsage: PaymentIntentPaymentMethodOptionsAuBecsDebitSetupFutureUsage, ?targetDate: string) =
            {
                PaymentIntentPaymentMethodOptionsAuBecsDebit.SetupFutureUsage = setupFutureUsage
                PaymentIntentPaymentMethodOptionsAuBecsDebit.TargetDate = targetDate
            }

    and PaymentIntentPaymentMethodOptionsBacsDebit = {
        MandateOptions: PaymentIntentPaymentMethodOptionsMandateOptionsBacsDebit option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentIntentPaymentMethodOptionsBacsDebitSetupFutureUsage option
        ///Controls when Stripe will attempt to debit the funds from the customer's account. The date must be a string in YYYY-MM-DD format. The date must be in the future and between 3 and 15 calendar days from now.
        TargetDate: string option
    }
    with
        static member New (?mandateOptions: PaymentIntentPaymentMethodOptionsMandateOptionsBacsDebit, ?setupFutureUsage: PaymentIntentPaymentMethodOptionsBacsDebitSetupFutureUsage, ?targetDate: string) =
            {
                PaymentIntentPaymentMethodOptionsBacsDebit.MandateOptions = mandateOptions
                PaymentIntentPaymentMethodOptionsBacsDebit.SetupFutureUsage = setupFutureUsage
                PaymentIntentPaymentMethodOptionsBacsDebit.TargetDate = targetDate
            }

    and PaymentIntentPaymentMethodOptionsCard = {
        ///Installment details for this payment.
        ///For more information, see the [installments integration guide](https://docs.stripe.com/payments/installments).
        Installments: PaymentMethodOptionsCardInstallments option
        ///Configuration options for setting up an eMandate for cards issued in India.
        MandateOptions: PaymentMethodOptionsCardMandateOptions option
        ///Selected network to process this payment intent on. Depends on the available networks of the card attached to the payment intent. Can be only set confirm-time.
        Network: PaymentIntentPaymentMethodOptionsCardNetwork option
        ///Request ability to [capture beyond the standard authorization validity window](https://docs.stripe.com/payments/extended-authorization) for this PaymentIntent.
        RequestExtendedAuthorization: PaymentIntentPaymentMethodOptionsCardRequestExtendedAuthorization option
        ///Request ability to [increment the authorization](https://docs.stripe.com/payments/incremental-authorization) for this PaymentIntent.
        RequestIncrementalAuthorization: PaymentIntentPaymentMethodOptionsCardRequestIncrementalAuthorization option
        ///Request ability to make [multiple captures](https://docs.stripe.com/payments/multicapture) for this PaymentIntent.
        RequestMulticapture: PaymentIntentPaymentMethodOptionsCardRequestMulticapture option
        ///Request ability to [overcapture](https://docs.stripe.com/payments/overcapture) for this PaymentIntent.
        RequestOvercapture: PaymentIntentPaymentMethodOptionsCardRequestOvercapture option
        ///We strongly recommend that you rely on our SCA Engine to automatically prompt your customers for authentication based on risk level and [other requirements](https://docs.stripe.com/strong-customer-authentication). However, if you wish to request 3D Secure based on logic from your own fraud engine, provide this option. If not provided, this value defaults to `automatic`. Read our guide on [manually requesting 3D Secure](https://docs.stripe.com/payments/3d-secure/authentication-flow#manual-three-ds) for more information on how this configuration interacts with Radar and our SCA Engine.
        RequestThreeDSecure: PaymentIntentPaymentMethodOptionsCardRequestThreeDSecure option
        ///When enabled, using a card that is attached to a customer will require the CVC to be provided again (i.e. using the cvc_token parameter).
        RequireCvcRecollection: bool option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentIntentPaymentMethodOptionsCardSetupFutureUsage option
        ///Provides information about a card payment that customers see on their statements. Concatenated with the Kana prefix (shortened Kana descriptor) or Kana statement descriptor that’s set on the account to form the complete statement descriptor. Maximum 22 characters. On card statements, the *concatenation* of both prefix and suffix (including separators) will appear truncated to 22 characters.
        StatementDescriptorSuffixKana: string option
        ///Provides information about a card payment that customers see on their statements. Concatenated with the Kanji prefix (shortened Kanji descriptor) or Kanji statement descriptor that’s set on the account to form the complete statement descriptor. Maximum 17 characters. On card statements, the *concatenation* of both prefix and suffix (including separators) will appear truncated to 17 characters.
        StatementDescriptorSuffixKanji: string option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (installments: PaymentMethodOptionsCardInstallments option, mandateOptions: PaymentMethodOptionsCardMandateOptions option, network: PaymentIntentPaymentMethodOptionsCardNetwork option, requestThreeDSecure: PaymentIntentPaymentMethodOptionsCardRequestThreeDSecure option, ?requestExtendedAuthorization: PaymentIntentPaymentMethodOptionsCardRequestExtendedAuthorization, ?requestIncrementalAuthorization: PaymentIntentPaymentMethodOptionsCardRequestIncrementalAuthorization, ?requestMulticapture: PaymentIntentPaymentMethodOptionsCardRequestMulticapture, ?requestOvercapture: PaymentIntentPaymentMethodOptionsCardRequestOvercapture, ?requireCvcRecollection: bool, ?setupFutureUsage: PaymentIntentPaymentMethodOptionsCardSetupFutureUsage, ?statementDescriptorSuffixKana: string, ?statementDescriptorSuffixKanji: string) =
            {
                PaymentIntentPaymentMethodOptionsCard.Installments = installments //required
                PaymentIntentPaymentMethodOptionsCard.MandateOptions = mandateOptions //required
                PaymentIntentPaymentMethodOptionsCard.Network = network //required
                PaymentIntentPaymentMethodOptionsCard.RequestThreeDSecure = requestThreeDSecure //required
                PaymentIntentPaymentMethodOptionsCard.RequestExtendedAuthorization = requestExtendedAuthorization
                PaymentIntentPaymentMethodOptionsCard.RequestIncrementalAuthorization = requestIncrementalAuthorization
                PaymentIntentPaymentMethodOptionsCard.RequestMulticapture = requestMulticapture
                PaymentIntentPaymentMethodOptionsCard.RequestOvercapture = requestOvercapture
                PaymentIntentPaymentMethodOptionsCard.RequireCvcRecollection = requireCvcRecollection
                PaymentIntentPaymentMethodOptionsCard.SetupFutureUsage = setupFutureUsage
                PaymentIntentPaymentMethodOptionsCard.StatementDescriptorSuffixKana = statementDescriptorSuffixKana
                PaymentIntentPaymentMethodOptionsCard.StatementDescriptorSuffixKanji = statementDescriptorSuffixKanji
            }

    and PaymentIntentPaymentMethodOptionsLink = {
        ///[Deprecated] This is a legacy parameter that no longer has any function.
        PersistentToken: string option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentIntentPaymentMethodOptionsLinkSetupFutureUsage option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (persistentToken: string option, ?setupFutureUsage: PaymentIntentPaymentMethodOptionsLinkSetupFutureUsage) =
            {
                PaymentIntentPaymentMethodOptionsLink.PersistentToken = persistentToken //required
                PaymentIntentPaymentMethodOptionsLink.SetupFutureUsage = setupFutureUsage
            }

    and PaymentIntentPaymentMethodOptionsMandateOptionsAcssDebit = {
        ///A URL for custom mandate text
        CustomMandateUrl: string option
        ///Description of the interval. Only required if the 'payment_schedule' parameter is 'interval' or 'combined'.
        IntervalDescription: string option
        ///Payment schedule for the mandate.
        PaymentSchedule: PaymentIntentPaymentMethodOptionsMandateOptionsAcssDebitPaymentSchedule option
        ///Transaction type of the mandate.
        TransactionType: PaymentIntentPaymentMethodOptionsMandateOptionsAcssDebitTransactionType option
    }
    with
        static member New (intervalDescription: string option, paymentSchedule: PaymentIntentPaymentMethodOptionsMandateOptionsAcssDebitPaymentSchedule option, transactionType: PaymentIntentPaymentMethodOptionsMandateOptionsAcssDebitTransactionType option, ?customMandateUrl: string) =
            {
                PaymentIntentPaymentMethodOptionsMandateOptionsAcssDebit.IntervalDescription = intervalDescription //required
                PaymentIntentPaymentMethodOptionsMandateOptionsAcssDebit.PaymentSchedule = paymentSchedule //required
                PaymentIntentPaymentMethodOptionsMandateOptionsAcssDebit.TransactionType = transactionType //required
                PaymentIntentPaymentMethodOptionsMandateOptionsAcssDebit.CustomMandateUrl = customMandateUrl
            }

    and PaymentIntentPaymentMethodOptionsMandateOptionsPayto = {
        ///Amount that will be collected. It is required when `amount_type` is `fixed`.
        Amount: int option
        ///The type of amount that will be collected. The amount charged must be exact or up to the value of `amount` param for `fixed` or `maximum` type respectively. Defaults to `maximum`.
        AmountType: PaymentIntentPaymentMethodOptionsMandateOptionsPaytoAmountType option
        ///Date, in YYYY-MM-DD format, after which payments will not be collected. Defaults to no end date.
        EndDate: string option
        ///The periodicity at which payments will be collected. Defaults to `adhoc`.
        PaymentSchedule: PaymentIntentPaymentMethodOptionsMandateOptionsPaytoPaymentSchedule option
        ///The number of payments that will be made during a payment period. Defaults to 1 except for when `payment_schedule` is `adhoc`. In that case, it defaults to no limit.
        PaymentsPerPeriod: int option
        ///The purpose for which payments are made. Has a default value based on your merchant category code.
        Purpose: PaymentIntentPaymentMethodOptionsMandateOptionsPaytoPurpose option
    }
    with
        static member New (amount: int option, amountType: PaymentIntentPaymentMethodOptionsMandateOptionsPaytoAmountType option, endDate: string option, paymentSchedule: PaymentIntentPaymentMethodOptionsMandateOptionsPaytoPaymentSchedule option, paymentsPerPeriod: int option, purpose: PaymentIntentPaymentMethodOptionsMandateOptionsPaytoPurpose option) =
            {
                PaymentIntentPaymentMethodOptionsMandateOptionsPayto.Amount = amount //required
                PaymentIntentPaymentMethodOptionsMandateOptionsPayto.AmountType = amountType //required
                PaymentIntentPaymentMethodOptionsMandateOptionsPayto.EndDate = endDate //required
                PaymentIntentPaymentMethodOptionsMandateOptionsPayto.PaymentSchedule = paymentSchedule //required
                PaymentIntentPaymentMethodOptionsMandateOptionsPayto.PaymentsPerPeriod = paymentsPerPeriod //required
                PaymentIntentPaymentMethodOptionsMandateOptionsPayto.Purpose = purpose //required
            }

    and PaymentIntentPaymentMethodOptionsNzBankAccount = {
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentIntentPaymentMethodOptionsNzBankAccountSetupFutureUsage option
        ///Controls when Stripe will attempt to debit the funds from the customer's account. The date must be a string in YYYY-MM-DD format. The date must be in the future and between 3 and 15 calendar days from now.
        TargetDate: string option
    }
    with
        static member New (?setupFutureUsage: PaymentIntentPaymentMethodOptionsNzBankAccountSetupFutureUsage, ?targetDate: string) =
            {
                PaymentIntentPaymentMethodOptionsNzBankAccount.SetupFutureUsage = setupFutureUsage
                PaymentIntentPaymentMethodOptionsNzBankAccount.TargetDate = targetDate
            }

    and PaymentIntentPaymentMethodOptionsPayto = {
        MandateOptions: PaymentIntentPaymentMethodOptionsMandateOptionsPayto option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentIntentPaymentMethodOptionsPaytoSetupFutureUsage option
    }
    with
        static member New (?mandateOptions: PaymentIntentPaymentMethodOptionsMandateOptionsPayto, ?setupFutureUsage: PaymentIntentPaymentMethodOptionsPaytoSetupFutureUsage) =
            {
                PaymentIntentPaymentMethodOptionsPayto.MandateOptions = mandateOptions
                PaymentIntentPaymentMethodOptionsPayto.SetupFutureUsage = setupFutureUsage
            }

    and PaymentIntentPaymentMethodOptionsSepaDebit = {
        MandateOptions: PaymentIntentPaymentMethodOptionsMandateOptionsSepaDebit option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentIntentPaymentMethodOptionsSepaDebitSetupFutureUsage option
        ///Controls when Stripe will attempt to debit the funds from the customer's account. The date must be a string in YYYY-MM-DD format. The date must be in the future and between 3 and 15 calendar days from now.
        TargetDate: string option
    }
    with
        static member New (?mandateOptions: PaymentIntentPaymentMethodOptionsMandateOptionsSepaDebit, ?setupFutureUsage: PaymentIntentPaymentMethodOptionsSepaDebitSetupFutureUsage, ?targetDate: string) =
            {
                PaymentIntentPaymentMethodOptionsSepaDebit.MandateOptions = mandateOptions
                PaymentIntentPaymentMethodOptionsSepaDebit.SetupFutureUsage = setupFutureUsage
                PaymentIntentPaymentMethodOptionsSepaDebit.TargetDate = targetDate
            }

    and PaymentIntentPaymentMethodOptionsUsBankAccount = {
        FinancialConnections: LinkedAccountOptionsCommon option
        MandateOptions: PaymentMethodOptionsUsBankAccountMandateOptions option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentIntentPaymentMethodOptionsUsBankAccountSetupFutureUsage option
        ///Controls when Stripe will attempt to debit the funds from the customer's account. The date must be a string in YYYY-MM-DD format. The date must be in the future and between 3 and 15 calendar days from now.
        TargetDate: string option
        ///The purpose of the transaction.
        TransactionPurpose: PaymentIntentPaymentMethodOptionsUsBankAccountTransactionPurpose option
        ///Bank account verification method. The default value is `automatic`.
        VerificationMethod: PaymentIntentPaymentMethodOptionsUsBankAccountVerificationMethod option
    }
    with
        static member New (?financialConnections: LinkedAccountOptionsCommon, ?mandateOptions: PaymentMethodOptionsUsBankAccountMandateOptions, ?setupFutureUsage: PaymentIntentPaymentMethodOptionsUsBankAccountSetupFutureUsage, ?targetDate: string, ?transactionPurpose: PaymentIntentPaymentMethodOptionsUsBankAccountTransactionPurpose, ?verificationMethod: PaymentIntentPaymentMethodOptionsUsBankAccountVerificationMethod) =
            {
                PaymentIntentPaymentMethodOptionsUsBankAccount.FinancialConnections = financialConnections
                PaymentIntentPaymentMethodOptionsUsBankAccount.MandateOptions = mandateOptions
                PaymentIntentPaymentMethodOptionsUsBankAccount.SetupFutureUsage = setupFutureUsage
                PaymentIntentPaymentMethodOptionsUsBankAccount.TargetDate = targetDate
                PaymentIntentPaymentMethodOptionsUsBankAccount.TransactionPurpose = transactionPurpose
                PaymentIntentPaymentMethodOptionsUsBankAccount.VerificationMethod = verificationMethod
            }

    and PaymentIntentProcessing = {
        Card: PaymentIntentCardProcessing option
    }
    with
        ///Type of the payment method for which payment is in `processing` state, one of `card`.
        member _.Type = "card"

        static member New (?card: PaymentIntentCardProcessing) =
            {
                PaymentIntentProcessing.Card = card
            }

    and PaymentIntentTypeSpecificPaymentMethodOptionsClient = {
        ///Controls when the funds will be captured from the customer's account.
        CaptureMethod: PaymentIntentTypeSpecificPaymentMethodOptionsClientCaptureMethod option
        Installments: PaymentFlowsInstallmentOptions option
        MandateOptions: PaymentIntentPaymentMethodOptionsMandateOptionsPayto option
        ///Request ability to [increment](https://docs.stripe.com/terminal/features/incremental-authorizations) this PaymentIntent if the combination of MCC and card brand is eligible. Check [incremental_authorization_supported](https://docs.stripe.com/api/charges/object#charge_object-payment_method_details-card_present-incremental_authorization_supported) in the [Confirm](https://docs.stripe.com/api/payment_intents/confirm) response to verify support.
        RequestIncrementalAuthorizationSupport: bool option
        ///When enabled, using a card that is attached to a customer will require the CVC to be provided again (i.e. using the cvc_token parameter).
        RequireCvcRecollection: bool option
        Routing: PaymentMethodOptionsCardPresentRouting option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentIntentTypeSpecificPaymentMethodOptionsClientSetupFutureUsage option
        ///Bank account verification method. The default value is `automatic`.
        VerificationMethod: PaymentIntentTypeSpecificPaymentMethodOptionsClientVerificationMethod option
    }
    with
        static member New (?captureMethod: PaymentIntentTypeSpecificPaymentMethodOptionsClientCaptureMethod, ?installments: PaymentFlowsInstallmentOptions, ?mandateOptions: PaymentIntentPaymentMethodOptionsMandateOptionsPayto, ?requestIncrementalAuthorizationSupport: bool, ?requireCvcRecollection: bool, ?routing: PaymentMethodOptionsCardPresentRouting, ?setupFutureUsage: PaymentIntentTypeSpecificPaymentMethodOptionsClientSetupFutureUsage, ?verificationMethod: PaymentIntentTypeSpecificPaymentMethodOptionsClientVerificationMethod) =
            {
                PaymentIntentTypeSpecificPaymentMethodOptionsClient.CaptureMethod = captureMethod
                PaymentIntentTypeSpecificPaymentMethodOptionsClient.Installments = installments
                PaymentIntentTypeSpecificPaymentMethodOptionsClient.MandateOptions = mandateOptions
                PaymentIntentTypeSpecificPaymentMethodOptionsClient.RequestIncrementalAuthorizationSupport = requestIncrementalAuthorizationSupport
                PaymentIntentTypeSpecificPaymentMethodOptionsClient.RequireCvcRecollection = requireCvcRecollection
                PaymentIntentTypeSpecificPaymentMethodOptionsClient.Routing = routing
                PaymentIntentTypeSpecificPaymentMethodOptionsClient.SetupFutureUsage = setupFutureUsage
                PaymentIntentTypeSpecificPaymentMethodOptionsClient.VerificationMethod = verificationMethod
            }

    ///A payment link is a shareable URL that will take your customers to a hosted payment page. A payment link can be shared and used multiple times.
    ///When a customer opens a payment link it will open a new [checkout session](https://docs.stripe.com/api/checkout/sessions) to render the payment page. You can use [checkout session events](https://docs.stripe.com/api/events/types#event_types-checkout.session.completed) to track payments through payment links.
    ///Related guide: [Payment Links API](https://docs.stripe.com/payment-links)
    and PaymentLink = {
        ///Whether the payment link's `url` is active. If `false`, customers visiting the URL will be shown a page saying that the link has been deactivated.
        Active: bool
        AfterCompletion: PaymentLinksResourceAfterCompletion
        ///Whether user redeemable promotion codes are enabled.
        AllowPromotionCodes: bool
        ///The ID of the Connect application that created the Payment Link.
        Application: PaymentLinkApplication'AnyOf option
        ///The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account.
        ApplicationFeeAmount: int option
        ///This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account.
        ApplicationFeePercent: decimal option
        AutomaticTax: PaymentLinksResourceAutomaticTax
        ///Configuration for collecting the customer's billing address. Defaults to `auto`.
        BillingAddressCollection: PaymentLinkBillingAddressCollection
        ///When set, provides configuration to gather active consent from customers.
        ConsentCollection: PaymentLinksResourceConsentCollection option
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///Collect additional information from your customer using custom fields. Up to 3 fields are supported. You can't set this parameter if `ui_mode` is `custom`.
        CustomFields: PaymentLinksResourceCustomFields list
        CustomText: PaymentLinksResourceCustomText
        ///Configuration for Customer creation during checkout.
        CustomerCreation: PaymentLinkCustomerCreation
        ///Unique identifier for the object.
        Id: string
        ///The custom message to be displayed to a customer when a payment link is no longer active.
        InactiveMessage: string option
        ///Configuration for creating invoice for payment mode payment links.
        InvoiceCreation: PaymentLinksResourceInvoiceCreation option
        ///The line items representing what is being sold.
        LineItems: PaymentLinkLineItems option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Settings for Managed Payments for this Payment Link and resulting [CheckoutSessions](/api/checkout/sessions/object), [PaymentIntents](/api/payment_intents/object), [Invoices](/api/invoices/object), and [Subscriptions](/api/subscriptions/object).
        ManagedPayments: PaymentPagesCheckoutSessionManagedPayments option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        NameCollection: PaymentLinksResourceNameCollection option
        ///The account on behalf of which to charge. See the [Connect documentation](https://support.stripe.com/questions/sending-invoices-on-behalf-of-connected-accounts) for details.
        OnBehalfOf: PaymentLinkOnBehalfOf'AnyOf option
        ///The optional items presented to the customer at checkout.
        OptionalItems: PaymentLinksResourceOptionalItem list option
        ///Indicates the parameters to be passed to PaymentIntent creation during checkout.
        PaymentIntentData: PaymentLinksResourcePaymentIntentData option
        ///Configuration for collecting a payment method during checkout. Defaults to `always`.
        PaymentMethodCollection: PaymentLinkPaymentMethodCollection
        ///The list of payment method types that customers can use. When `null`, Stripe will dynamically show relevant payment methods you've enabled in your [payment method settings](https://dashboard.stripe.com/settings/payment_methods).
        PaymentMethodTypes: PaymentLinkPaymentMethodTypes list option
        PhoneNumberCollection: PaymentLinksResourcePhoneNumberCollection
        ///Settings that restrict the usage of a payment link.
        Restrictions: PaymentLinksResourceRestrictions option
        ///Configuration for collecting the customer's shipping address.
        ShippingAddressCollection: PaymentLinksResourceShippingAddressCollection option
        ///The shipping rate options applied to the session.
        ShippingOptions: PaymentLinksResourceShippingOption list
        ///Indicates the type of transaction being performed which customizes relevant text on the page, such as the submit button.
        SubmitType: PaymentLinkSubmitType
        ///When creating a subscription, the specified configuration data will be used. There must be at least one line item with a recurring price to use `subscription_data`.
        SubscriptionData: PaymentLinksResourceSubscriptionData option
        TaxIdCollection: PaymentLinksResourceTaxIdCollection
        ///The account (if any) the payments will be attributed to for tax reporting, and where funds from each payment will be transferred to.
        TransferData: PaymentLinksResourceTransferData option
        ///The public URL that can be shared with customers.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "payment_link"

        static member New (active: bool, afterCompletion: PaymentLinksResourceAfterCompletion, allowPromotionCodes: bool, application: PaymentLinkApplication'AnyOf option, applicationFeeAmount: int option, applicationFeePercent: decimal option, automaticTax: PaymentLinksResourceAutomaticTax, billingAddressCollection: PaymentLinkBillingAddressCollection, consentCollection: PaymentLinksResourceConsentCollection option, currency: IsoTypes.IsoCurrencyCode, customFields: PaymentLinksResourceCustomFields list, customText: PaymentLinksResourceCustomText, customerCreation: PaymentLinkCustomerCreation, id: string, inactiveMessage: string option, invoiceCreation: PaymentLinksResourceInvoiceCreation option, livemode: bool, managedPayments: PaymentPagesCheckoutSessionManagedPayments option, metadata: Map<string, string>, onBehalfOf: PaymentLinkOnBehalfOf'AnyOf option, paymentIntentData: PaymentLinksResourcePaymentIntentData option, paymentMethodCollection: PaymentLinkPaymentMethodCollection, paymentMethodTypes: PaymentLinkPaymentMethodTypes list option, phoneNumberCollection: PaymentLinksResourcePhoneNumberCollection, restrictions: PaymentLinksResourceRestrictions option, shippingAddressCollection: PaymentLinksResourceShippingAddressCollection option, shippingOptions: PaymentLinksResourceShippingOption list, submitType: PaymentLinkSubmitType, subscriptionData: PaymentLinksResourceSubscriptionData option, taxIdCollection: PaymentLinksResourceTaxIdCollection, transferData: PaymentLinksResourceTransferData option, url: string, ?lineItems: PaymentLinkLineItems, ?nameCollection: PaymentLinksResourceNameCollection, ?optionalItems: PaymentLinksResourceOptionalItem list option) =
            {
                PaymentLink.Active = active //required
                PaymentLink.AfterCompletion = afterCompletion //required
                PaymentLink.AllowPromotionCodes = allowPromotionCodes //required
                PaymentLink.Application = application //required
                PaymentLink.ApplicationFeeAmount = applicationFeeAmount //required
                PaymentLink.ApplicationFeePercent = applicationFeePercent //required
                PaymentLink.AutomaticTax = automaticTax //required
                PaymentLink.BillingAddressCollection = billingAddressCollection //required
                PaymentLink.ConsentCollection = consentCollection //required
                PaymentLink.Currency = currency //required
                PaymentLink.CustomFields = customFields //required
                PaymentLink.CustomText = customText //required
                PaymentLink.CustomerCreation = customerCreation //required
                PaymentLink.Id = id //required
                PaymentLink.InactiveMessage = inactiveMessage //required
                PaymentLink.InvoiceCreation = invoiceCreation //required
                PaymentLink.Livemode = livemode //required
                PaymentLink.ManagedPayments = managedPayments //required
                PaymentLink.Metadata = metadata //required
                PaymentLink.OnBehalfOf = onBehalfOf //required
                PaymentLink.PaymentIntentData = paymentIntentData //required
                PaymentLink.PaymentMethodCollection = paymentMethodCollection //required
                PaymentLink.PaymentMethodTypes = paymentMethodTypes //required
                PaymentLink.PhoneNumberCollection = phoneNumberCollection //required
                PaymentLink.Restrictions = restrictions //required
                PaymentLink.ShippingAddressCollection = shippingAddressCollection //required
                PaymentLink.ShippingOptions = shippingOptions //required
                PaymentLink.SubmitType = submitType //required
                PaymentLink.SubscriptionData = subscriptionData //required
                PaymentLink.TaxIdCollection = taxIdCollection //required
                PaymentLink.TransferData = transferData //required
                PaymentLink.Url = url //required
                PaymentLink.LineItems = lineItems
                PaymentLink.NameCollection = nameCollection
                PaymentLink.OptionalItems = optionalItems |> Option.flatten
            }

    and PaymentLinkApplication'AnyOf =
        | String of string
        | Application of Application
        | DeletedApplication of DeletedApplication

    and PaymentLinkOnBehalfOf'AnyOf =
        | String of string
        | Account of Account

    ///The line items representing what is being sold.
    and PaymentLinkLineItems = {
        ///Details about each object.
        Data: Item list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: Item list, hasMore: bool, url: string) =
            {
                PaymentLinkLineItems.Data = data //required
                PaymentLinkLineItems.HasMore = hasMore //required
                PaymentLinkLineItems.Url = url //required
            }

    and PaymentLinksResourceAfterCompletion = {
        HostedConfirmation: PaymentLinksResourceCompletionBehaviorConfirmationPage option
        Redirect: PaymentLinksResourceCompletionBehaviorRedirect option
        ///The specified behavior after the purchase is complete.
        Type: PaymentLinksResourceAfterCompletionType
    }
    with
        static member New (``type``: PaymentLinksResourceAfterCompletionType, ?hostedConfirmation: PaymentLinksResourceCompletionBehaviorConfirmationPage, ?redirect: PaymentLinksResourceCompletionBehaviorRedirect) =
            {
                PaymentLinksResourceAfterCompletion.Type = ``type`` //required
                PaymentLinksResourceAfterCompletion.HostedConfirmation = hostedConfirmation
                PaymentLinksResourceAfterCompletion.Redirect = redirect
            }

    and PaymentLinksResourceAutomaticTax = {
        ///If `true`, tax will be calculated automatically using the customer's location.
        Enabled: bool
        ///The account that's liable for tax. If set, the business address and tax registrations required to perform the tax calculation are loaded from this account. The tax transaction is returned in the report of the connected account.
        Liability: ConnectAccountReference option
    }
    with
        static member New (enabled: bool, liability: ConnectAccountReference option) =
            {
                PaymentLinksResourceAutomaticTax.Enabled = enabled //required
                PaymentLinksResourceAutomaticTax.Liability = liability //required
            }

    and PaymentLinksResourceConsentCollection = {
        ///Settings related to the payment method reuse text shown in the Checkout UI.
        PaymentMethodReuseAgreement: PaymentLinksResourcePaymentMethodReuseAgreement option
        ///If set to `auto`, enables the collection of customer consent for promotional communications.
        Promotions: PaymentLinksResourceConsentCollectionPromotions option
        ///If set to `required`, it requires cutomers to accept the terms of service before being able to pay. If set to `none`, customers won't be shown a checkbox to accept the terms of service.
        TermsOfService: PaymentLinksResourceConsentCollectionTermsOfService option
    }
    with
        static member New (paymentMethodReuseAgreement: PaymentLinksResourcePaymentMethodReuseAgreement option, promotions: PaymentLinksResourceConsentCollectionPromotions option, termsOfService: PaymentLinksResourceConsentCollectionTermsOfService option) =
            {
                PaymentLinksResourceConsentCollection.PaymentMethodReuseAgreement = paymentMethodReuseAgreement //required
                PaymentLinksResourceConsentCollection.Promotions = promotions //required
                PaymentLinksResourceConsentCollection.TermsOfService = termsOfService //required
            }

    and PaymentLinksResourceCustomFields = {
        Dropdown: PaymentLinksResourceCustomFieldsDropdown option
        ///String of your choice that your integration can use to reconcile this field. Must be unique to this field, alphanumeric, and up to 200 characters.
        Key: string
        Label: PaymentLinksResourceCustomFieldsLabel
        Numeric: PaymentLinksResourceCustomFieldsNumeric option
        ///Whether the customer is required to complete the field before completing the Checkout Session. Defaults to `false`.
        Optional: bool
        Text: PaymentLinksResourceCustomFieldsText option
        ///The type of the field.
        Type: PaymentLinksResourceCustomFieldsType
    }
    with
        static member New (key: string, label: PaymentLinksResourceCustomFieldsLabel, optional: bool, ``type``: PaymentLinksResourceCustomFieldsType, ?dropdown: PaymentLinksResourceCustomFieldsDropdown, ?numeric: PaymentLinksResourceCustomFieldsNumeric, ?text: PaymentLinksResourceCustomFieldsText) =
            {
                PaymentLinksResourceCustomFields.Key = key //required
                PaymentLinksResourceCustomFields.Label = label //required
                PaymentLinksResourceCustomFields.Optional = optional //required
                PaymentLinksResourceCustomFields.Type = ``type`` //required
                PaymentLinksResourceCustomFields.Dropdown = dropdown
                PaymentLinksResourceCustomFields.Numeric = numeric
                PaymentLinksResourceCustomFields.Text = text
            }

    and PaymentLinksResourceCustomFieldsDropdown = {
        ///The value that pre-fills on the payment page.
        DefaultValue: string option
        ///The options available for the customer to select. Up to 200 options allowed.
        Options: PaymentLinksResourceCustomFieldsDropdownOption list
    }
    with
        static member New (defaultValue: string option, options: PaymentLinksResourceCustomFieldsDropdownOption list) =
            {
                PaymentLinksResourceCustomFieldsDropdown.DefaultValue = defaultValue //required
                PaymentLinksResourceCustomFieldsDropdown.Options = options //required
            }

    and PaymentLinksResourceCustomText = {
        ///Custom text that should be displayed after the payment confirmation button.
        AfterSubmit: PaymentLinksResourceCustomTextPosition option
        ///Custom text that should be displayed alongside shipping address collection.
        ShippingAddress: PaymentLinksResourceCustomTextPosition option
        ///Custom text that should be displayed alongside the payment confirmation button.
        Submit: PaymentLinksResourceCustomTextPosition option
        ///Custom text that should be displayed in place of the default terms of service agreement text.
        TermsOfServiceAcceptance: PaymentLinksResourceCustomTextPosition option
    }
    with
        static member New (afterSubmit: PaymentLinksResourceCustomTextPosition option, shippingAddress: PaymentLinksResourceCustomTextPosition option, submit: PaymentLinksResourceCustomTextPosition option, termsOfServiceAcceptance: PaymentLinksResourceCustomTextPosition option) =
            {
                PaymentLinksResourceCustomText.AfterSubmit = afterSubmit //required
                PaymentLinksResourceCustomText.ShippingAddress = shippingAddress //required
                PaymentLinksResourceCustomText.Submit = submit //required
                PaymentLinksResourceCustomText.TermsOfServiceAcceptance = termsOfServiceAcceptance //required
            }

    and PaymentLinksResourceInvoiceCreation = {
        ///Enable creating an invoice on successful payment.
        Enabled: bool
        ///Configuration for the invoice. Default invoice values will be used if unspecified.
        InvoiceData: PaymentLinksResourceInvoiceSettings option
    }
    with
        static member New (enabled: bool, invoiceData: PaymentLinksResourceInvoiceSettings option) =
            {
                PaymentLinksResourceInvoiceCreation.Enabled = enabled //required
                PaymentLinksResourceInvoiceCreation.InvoiceData = invoiceData //required
            }

    and PaymentLinksResourceInvoiceSettings = {
        ///The account tax IDs associated with the invoice.
        AccountTaxIds: PaymentLinksResourceInvoiceSettingsAccountTaxIds'AnyOf list option
        ///A list of up to 4 custom fields to be displayed on the invoice.
        CustomFields: InvoiceSettingCustomField list option
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///Footer to be displayed on the invoice.
        Footer: string option
        ///The connected account that issues the invoice. The invoice is presented with the branding and support information of the specified account.
        Issuer: ConnectAccountReference option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///Options for invoice PDF rendering.
        RenderingOptions: InvoiceSettingCheckoutRenderingOptions option
    }
    with
        static member New (accountTaxIds: PaymentLinksResourceInvoiceSettingsAccountTaxIds'AnyOf list option, customFields: InvoiceSettingCustomField list option, description: string option, footer: string option, issuer: ConnectAccountReference option, metadata: Map<string, string> option, renderingOptions: InvoiceSettingCheckoutRenderingOptions option) =
            {
                PaymentLinksResourceInvoiceSettings.AccountTaxIds = accountTaxIds //required
                PaymentLinksResourceInvoiceSettings.CustomFields = customFields //required
                PaymentLinksResourceInvoiceSettings.Description = description //required
                PaymentLinksResourceInvoiceSettings.Footer = footer //required
                PaymentLinksResourceInvoiceSettings.Issuer = issuer //required
                PaymentLinksResourceInvoiceSettings.Metadata = metadata //required
                PaymentLinksResourceInvoiceSettings.RenderingOptions = renderingOptions //required
            }

    and PaymentLinksResourceInvoiceSettingsAccountTaxIds'AnyOf =
        | String of string
        | TaxId of TaxId
        | DeletedTaxId of DeletedTaxId

    and PaymentLinksResourceNameCollection = {
        Business: PaymentLinksResourceBusinessName option
        Individual: PaymentLinksResourceIndividualName option
    }
    with
        static member New (?business: PaymentLinksResourceBusinessName, ?individual: PaymentLinksResourceIndividualName) =
            {
                PaymentLinksResourceNameCollection.Business = business
                PaymentLinksResourceNameCollection.Individual = individual
            }

    and PaymentLinksResourceOptionalItem = {
        AdjustableQuantity: PaymentLinksResourceOptionalItemAdjustableQuantity option
        Price: string
        Quantity: int
    }
    with
        static member New (adjustableQuantity: PaymentLinksResourceOptionalItemAdjustableQuantity option, price: string, quantity: int) =
            {
                PaymentLinksResourceOptionalItem.AdjustableQuantity = adjustableQuantity //required
                PaymentLinksResourceOptionalItem.Price = price //required
                PaymentLinksResourceOptionalItem.Quantity = quantity //required
            }

    and PaymentLinksResourcePaymentIntentData = {
        ///Indicates when the funds will be captured from the customer's account.
        CaptureMethod: PaymentLinksResourcePaymentIntentDataCaptureMethod option
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that will set metadata on [Payment Intents](https://docs.stripe.com/api/payment_intents) generated from this payment link.
        Metadata: Map<string, string>
        ///Indicates that you intend to make future payments with the payment method collected during checkout.
        SetupFutureUsage: PaymentLinksResourcePaymentIntentDataSetupFutureUsage option
        ///For a non-card payment, information about the charge that appears on the customer's statement when this payment succeeds in creating a charge.
        StatementDescriptor: string option
        ///For a card payment, information about the charge that appears on the customer's statement when this payment succeeds in creating a charge. Concatenated with the account's statement descriptor prefix to form the complete statement descriptor.
        StatementDescriptorSuffix: string option
        ///A string that identifies the resulting payment as part of a group. See the PaymentIntents [use case for connected accounts](https://docs.stripe.com/connect/separate-charges-and-transfers) for details.
        TransferGroup: string option
    }
    with
        static member New (captureMethod: PaymentLinksResourcePaymentIntentDataCaptureMethod option, description: string option, metadata: Map<string, string>, setupFutureUsage: PaymentLinksResourcePaymentIntentDataSetupFutureUsage option, statementDescriptor: string option, statementDescriptorSuffix: string option, transferGroup: string option) =
            {
                PaymentLinksResourcePaymentIntentData.CaptureMethod = captureMethod //required
                PaymentLinksResourcePaymentIntentData.Description = description //required
                PaymentLinksResourcePaymentIntentData.Metadata = metadata //required
                PaymentLinksResourcePaymentIntentData.SetupFutureUsage = setupFutureUsage //required
                PaymentLinksResourcePaymentIntentData.StatementDescriptor = statementDescriptor //required
                PaymentLinksResourcePaymentIntentData.StatementDescriptorSuffix = statementDescriptorSuffix //required
                PaymentLinksResourcePaymentIntentData.TransferGroup = transferGroup //required
            }

    and PaymentLinksResourcePaymentMethodReuseAgreement = {
        ///Determines the position and visibility of the payment method reuse agreement in the UI. When set to `auto`, Stripe's defaults will be used.
        ///When set to `hidden`, the payment method reuse agreement text will always be hidden in the UI.
        Position: PaymentLinksResourcePaymentMethodReuseAgreementPosition
    }
    with
        static member New (position: PaymentLinksResourcePaymentMethodReuseAgreementPosition) =
            {
                PaymentLinksResourcePaymentMethodReuseAgreement.Position = position //required
            }

    and PaymentLinksResourceRestrictions = {
        CompletedSessions: PaymentLinksResourceCompletedSessions
    }
    with
        static member New (completedSessions: PaymentLinksResourceCompletedSessions) =
            {
                PaymentLinksResourceRestrictions.CompletedSessions = completedSessions //required
            }

    and PaymentLinksResourceShippingAddressCollection = {
        ///An array of two-letter ISO country codes representing which countries Checkout should provide as options for shipping locations. Unsupported country codes: `AS, CX, CC, CU, HM, IR, KP, MH, FM, NF, MP, PW, SD, SY, UM, VI`.
        AllowedCountries: PaymentLinksResourceShippingAddressCollectionAllowedCountries list
    }
    with
        static member New (allowedCountries: PaymentLinksResourceShippingAddressCollectionAllowedCountries list) =
            {
                PaymentLinksResourceShippingAddressCollection.AllowedCountries = allowedCountries //required
            }

    and PaymentLinksResourceShippingOption = {
        ///A non-negative integer in cents representing how much to charge.
        ShippingAmount: int
        ///The ID of the Shipping Rate to use for this shipping option.
        ShippingRate: PaymentLinksResourceShippingOptionShippingRate'AnyOf
    }
    with
        static member New (shippingAmount: int, shippingRate: PaymentLinksResourceShippingOptionShippingRate'AnyOf) =
            {
                PaymentLinksResourceShippingOption.ShippingAmount = shippingAmount //required
                PaymentLinksResourceShippingOption.ShippingRate = shippingRate //required
            }

    and PaymentLinksResourceShippingOptionShippingRate'AnyOf =
        | String of string
        | ShippingRate of ShippingRate

    and PaymentLinksResourceSubscriptionData = {
        ///The subscription's description, meant to be displayable to the customer. Use this field to optionally store an explanation of the subscription for rendering in Stripe surfaces and certain local payment methods UIs.
        Description: string option
        InvoiceSettings: PaymentLinksResourceSubscriptionDataInvoiceSettings
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that will set metadata on [Subscriptions](https://docs.stripe.com/api/subscriptions) generated from this payment link.
        Metadata: Map<string, string>
        ///Integer representing the number of trial period days before the customer is charged for the first time.
        TrialPeriodDays: int option
        ///Settings related to subscription trials.
        TrialSettings: SubscriptionsTrialsResourceTrialSettings option
    }
    with
        static member New (description: string option, invoiceSettings: PaymentLinksResourceSubscriptionDataInvoiceSettings, metadata: Map<string, string>, trialPeriodDays: int option, trialSettings: SubscriptionsTrialsResourceTrialSettings option) =
            {
                PaymentLinksResourceSubscriptionData.Description = description //required
                PaymentLinksResourceSubscriptionData.InvoiceSettings = invoiceSettings //required
                PaymentLinksResourceSubscriptionData.Metadata = metadata //required
                PaymentLinksResourceSubscriptionData.TrialPeriodDays = trialPeriodDays //required
                PaymentLinksResourceSubscriptionData.TrialSettings = trialSettings //required
            }

    and PaymentLinksResourceSubscriptionDataInvoiceSettings = {
        Issuer: ConnectAccountReference
    }
    with
        static member New (issuer: ConnectAccountReference) =
            {
                PaymentLinksResourceSubscriptionDataInvoiceSettings.Issuer = issuer //required
            }

    and PaymentLinksResourceTaxIdCollection = {
        ///Indicates whether tax ID collection is enabled for the session.
        Enabled: bool
        Required: PaymentLinksResourceTaxIdCollectionRequired
    }
    with
        static member New (enabled: bool, required: PaymentLinksResourceTaxIdCollectionRequired) =
            {
                PaymentLinksResourceTaxIdCollection.Enabled = enabled //required
                PaymentLinksResourceTaxIdCollection.Required = required //required
            }

    and PaymentLinksResourceTransferData = {
        ///The amount in cents (or local equivalent) that will be transferred to the destination account. By default, the entire amount is transferred to the destination.
        Amount: int option
        ///The connected account receiving the transfer.
        Destination: PaymentLinksResourceTransferDataDestination'AnyOf
    }
    with
        static member New (amount: int option, destination: PaymentLinksResourceTransferDataDestination'AnyOf) =
            {
                PaymentLinksResourceTransferData.Amount = amount //required
                PaymentLinksResourceTransferData.Destination = destination //required
            }

    and PaymentLinksResourceTransferDataDestination'AnyOf =
        | String of string
        | Account of Account

    ///PaymentMethod objects represent your customer's payment instruments.
    ///You can use them with [PaymentIntents](https://docs.stripe.com/payments/payment-intents) to collect payments or save them to
    ///Customer objects to store instrument details for future payments.
    ///Related guides: [Payment Methods](https://docs.stripe.com/payments/payment-methods) and [More Payment Scenarios](https://docs.stripe.com/payments/more-payment-scenarios).
    and PaymentMethod = {
        AcssDebit: PaymentMethodAcssDebit option
        Affirm: PaymentMethodAffirm option
        AfterpayClearpay: PaymentMethodAfterpayClearpay option
        Alipay: PaymentFlowsPrivatePaymentMethodsAlipay option
        ///This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment method in a checkout flow. The field defaults to “unspecified”.
        AllowRedisplay: PaymentMethodAllowRedisplay option
        Alma: PaymentMethodAlma option
        AmazonPay: PaymentMethodAmazonPay option
        AuBecsDebit: PaymentMethodAuBecsDebit option
        BacsDebit: PaymentMethodBacsDebit option
        Bancontact: PaymentMethodBancontact option
        Billie: PaymentMethodBillie option
        BillingDetails: BillingDetails
        Blik: PaymentMethodBlik option
        Boleto: PaymentMethodBoleto option
        Card: PaymentMethodCard option
        CardPresent: PaymentMethodCardPresent option
        Cashapp: PaymentMethodCashapp option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        Crypto: PaymentMethodCrypto option
        Custom: PaymentMethodCustom option
        ///The ID of the Customer to which this PaymentMethod is saved. This will not be set when the PaymentMethod has not been saved to a Customer.
        Customer: PaymentMethodCustomer'AnyOf option
        CustomerAccount: string option
        CustomerBalance: PaymentMethodCustomerBalance option
        Eps: PaymentMethodEps option
        Fpx: PaymentMethodFpx option
        Giropay: PaymentMethodGiropay option
        Grabpay: PaymentMethodGrabpay option
        ///Unique identifier for the object.
        Id: string
        Ideal: PaymentMethodIdeal option
        InteracPresent: PaymentMethodInteracPresent option
        KakaoPay: PaymentMethodKakaoPay option
        Klarna: PaymentMethodKlarna option
        Konbini: PaymentMethodKonbini option
        KrCard: PaymentMethodKrCard option
        Link: PaymentMethodLink option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        MbWay: PaymentMethodMbWay option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        Mobilepay: PaymentMethodMobilepay option
        Multibanco: PaymentMethodMultibanco option
        NaverPay: PaymentMethodNaverPay option
        NzBankAccount: PaymentMethodNzBankAccount option
        Oxxo: PaymentMethodOxxo option
        [<JsonField(Name="p24")>]P24: PaymentMethodP24 option
        PayByBank: PaymentMethodPayByBank option
        Payco: PaymentMethodPayco option
        Paynow: PaymentMethodPaynow option
        Paypal: PaymentMethodPaypal option
        Payto: PaymentMethodPayto option
        Pix: PaymentMethodPix option
        Promptpay: PaymentMethodPromptpay option
        RadarOptions: RadarRadarOptions option
        RevolutPay: PaymentMethodRevolutPay option
        SamsungPay: PaymentMethodSamsungPay option
        Satispay: PaymentMethodSatispay option
        SepaDebit: PaymentMethodSepaDebit option
        Sofort: PaymentMethodSofort option
        Sunbit: PaymentMethodSunbit option
        Swish: PaymentMethodSwish option
        Twint: PaymentMethodTwint option
        ///The type of the PaymentMethod. An additional hash is included on the PaymentMethod with a name matching this value. It contains additional information specific to the PaymentMethod type.
        Type: PaymentMethodType
        Upi: PaymentMethodUpi option
        UsBankAccount: PaymentMethodUsBankAccount option
        WechatPay: PaymentMethodWechatPay option
        Zip: PaymentMethodZip option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "payment_method"

        static member New (billingDetails: BillingDetails, created: DateTime, customer: PaymentMethodCustomer'AnyOf option, customerAccount: string option, id: string, livemode: bool, metadata: Map<string, string> option, ``type``: PaymentMethodType, ?acssDebit: PaymentMethodAcssDebit, ?affirm: PaymentMethodAffirm, ?afterpayClearpay: PaymentMethodAfterpayClearpay, ?alipay: PaymentFlowsPrivatePaymentMethodsAlipay, ?allowRedisplay: PaymentMethodAllowRedisplay, ?alma: PaymentMethodAlma, ?amazonPay: PaymentMethodAmazonPay, ?auBecsDebit: PaymentMethodAuBecsDebit, ?bacsDebit: PaymentMethodBacsDebit, ?bancontact: PaymentMethodBancontact, ?billie: PaymentMethodBillie, ?blik: PaymentMethodBlik, ?boleto: PaymentMethodBoleto, ?card: PaymentMethodCard, ?cardPresent: PaymentMethodCardPresent, ?cashapp: PaymentMethodCashapp, ?crypto: PaymentMethodCrypto, ?custom: PaymentMethodCustom, ?customerBalance: PaymentMethodCustomerBalance, ?eps: PaymentMethodEps, ?fpx: PaymentMethodFpx, ?giropay: PaymentMethodGiropay, ?grabpay: PaymentMethodGrabpay, ?ideal: PaymentMethodIdeal, ?interacPresent: PaymentMethodInteracPresent, ?kakaoPay: PaymentMethodKakaoPay, ?klarna: PaymentMethodKlarna, ?konbini: PaymentMethodKonbini, ?krCard: PaymentMethodKrCard, ?link: PaymentMethodLink, ?mbWay: PaymentMethodMbWay, ?mobilepay: PaymentMethodMobilepay, ?multibanco: PaymentMethodMultibanco, ?naverPay: PaymentMethodNaverPay, ?nzBankAccount: PaymentMethodNzBankAccount, ?oxxo: PaymentMethodOxxo, ?p24: PaymentMethodP24, ?payByBank: PaymentMethodPayByBank, ?payco: PaymentMethodPayco, ?paynow: PaymentMethodPaynow, ?paypal: PaymentMethodPaypal, ?payto: PaymentMethodPayto, ?pix: PaymentMethodPix, ?promptpay: PaymentMethodPromptpay, ?radarOptions: RadarRadarOptions, ?revolutPay: PaymentMethodRevolutPay, ?samsungPay: PaymentMethodSamsungPay, ?satispay: PaymentMethodSatispay, ?sepaDebit: PaymentMethodSepaDebit, ?sofort: PaymentMethodSofort, ?sunbit: PaymentMethodSunbit, ?swish: PaymentMethodSwish, ?twint: PaymentMethodTwint, ?upi: PaymentMethodUpi, ?usBankAccount: PaymentMethodUsBankAccount, ?wechatPay: PaymentMethodWechatPay, ?zip: PaymentMethodZip) =
            {
                PaymentMethod.BillingDetails = billingDetails //required
                PaymentMethod.Created = created //required
                PaymentMethod.Customer = customer //required
                PaymentMethod.CustomerAccount = customerAccount //required
                PaymentMethod.Id = id //required
                PaymentMethod.Livemode = livemode //required
                PaymentMethod.Metadata = metadata //required
                PaymentMethod.Type = ``type`` //required
                PaymentMethod.AcssDebit = acssDebit
                PaymentMethod.Affirm = affirm
                PaymentMethod.AfterpayClearpay = afterpayClearpay
                PaymentMethod.Alipay = alipay
                PaymentMethod.AllowRedisplay = allowRedisplay
                PaymentMethod.Alma = alma
                PaymentMethod.AmazonPay = amazonPay
                PaymentMethod.AuBecsDebit = auBecsDebit
                PaymentMethod.BacsDebit = bacsDebit
                PaymentMethod.Bancontact = bancontact
                PaymentMethod.Billie = billie
                PaymentMethod.Blik = blik
                PaymentMethod.Boleto = boleto
                PaymentMethod.Card = card
                PaymentMethod.CardPresent = cardPresent
                PaymentMethod.Cashapp = cashapp
                PaymentMethod.Crypto = crypto
                PaymentMethod.Custom = custom
                PaymentMethod.CustomerBalance = customerBalance
                PaymentMethod.Eps = eps
                PaymentMethod.Fpx = fpx
                PaymentMethod.Giropay = giropay
                PaymentMethod.Grabpay = grabpay
                PaymentMethod.Ideal = ideal
                PaymentMethod.InteracPresent = interacPresent
                PaymentMethod.KakaoPay = kakaoPay
                PaymentMethod.Klarna = klarna
                PaymentMethod.Konbini = konbini
                PaymentMethod.KrCard = krCard
                PaymentMethod.Link = link
                PaymentMethod.MbWay = mbWay
                PaymentMethod.Mobilepay = mobilepay
                PaymentMethod.Multibanco = multibanco
                PaymentMethod.NaverPay = naverPay
                PaymentMethod.NzBankAccount = nzBankAccount
                PaymentMethod.Oxxo = oxxo
                PaymentMethod.P24 = p24
                PaymentMethod.PayByBank = payByBank
                PaymentMethod.Payco = payco
                PaymentMethod.Paynow = paynow
                PaymentMethod.Paypal = paypal
                PaymentMethod.Payto = payto
                PaymentMethod.Pix = pix
                PaymentMethod.Promptpay = promptpay
                PaymentMethod.RadarOptions = radarOptions
                PaymentMethod.RevolutPay = revolutPay
                PaymentMethod.SamsungPay = samsungPay
                PaymentMethod.Satispay = satispay
                PaymentMethod.SepaDebit = sepaDebit
                PaymentMethod.Sofort = sofort
                PaymentMethod.Sunbit = sunbit
                PaymentMethod.Swish = swish
                PaymentMethod.Twint = twint
                PaymentMethod.Upi = upi
                PaymentMethod.UsBankAccount = usBankAccount
                PaymentMethod.WechatPay = wechatPay
                PaymentMethod.Zip = zip
            }

    and PaymentMethodCustomer'AnyOf =
        | String of string
        | Customer of Customer

    and PaymentMethodCard = {
        ///Card brand. Can be `amex`, `cartes_bancaires`, `diners`, `discover`, `eftpos_au`, `jcb`, `link`, `mastercard`, `unionpay`, `visa` or `unknown`.
        Brand: PaymentMethodCardBrand
        ///Checks on Card address and CVC if provided.
        Checks: PaymentMethodCardChecks option
        ///Two-letter ISO code representing the country of the card. You could use this attribute to get a sense of the international breakdown of cards you've collected.
        Country: IsoTypes.IsoCountryCode option
        ///A high-level description of the type of cards issued in this range. (For internal use only and not typically available in standard API requests.)
        Description: string option
        ///The brand to use when displaying the card, this accounts for customer's brand choice on dual-branded cards. Can be `american_express`, `cartes_bancaires`, `diners_club`, `discover`, `eftpos_australia`, `interac`, `jcb`, `mastercard`, `union_pay`, `visa`, or `other` and may contain more values in the future.
        DisplayBrand: string option
        ///Two-digit number representing the card's expiration month.
        ExpMonth: int
        ///Four-digit number representing the card's expiration year.
        ExpYear: int
        ///Uniquely identifies this particular card number. You can use this attribute to check whether two customers who’ve signed up with you are using the same card number, for example. For payment methods that tokenize card information (Apple Pay, Google Pay), the tokenized number might be provided instead of the underlying card number.
        ///*As of May 1, 2021, card fingerprint in India for Connect changed to allow two fingerprints for the same card---one for India and one for the rest of the world.*
        Fingerprint: string option
        ///Card funding type. Can be `credit`, `debit`, `prepaid`, or `unknown`.
        Funding: PaymentMethodCardFunding
        ///Details of the original PaymentMethod that created this object.
        GeneratedFrom: PaymentMethodCardGeneratedCard option
        ///Issuer identification number of the card. (For internal use only and not typically available in standard API requests.)
        Iin: string option
        ///The name of the card's issuing bank. (For internal use only and not typically available in standard API requests.)
        Issuer: string option
        ///The last four digits of the card.
        [<JsonField(Name="last4")>]Last4: string
        ///Contains information about card networks that can be used to process the payment.
        Networks: Networks option
        ///Status of a card based on the card issuer.
        RegulatedStatus: PaymentMethodCardRegulatedStatus option
        ///Contains details on how this Card may be used for 3D Secure authentication.
        ThreeDSecureUsage: ThreeDSecureUsage option
        ///If this Card is part of a card wallet, this contains the details of the card wallet.
        Wallet: PaymentMethodCardWallet option
    }
    with
        static member New (brand: PaymentMethodCardBrand, checks: PaymentMethodCardChecks option, country: IsoTypes.IsoCountryCode option, displayBrand: string option, expMonth: int, expYear: int, funding: PaymentMethodCardFunding, generatedFrom: PaymentMethodCardGeneratedCard option, last4: string, networks: Networks option, regulatedStatus: PaymentMethodCardRegulatedStatus option, threeDSecureUsage: ThreeDSecureUsage option, wallet: PaymentMethodCardWallet option, ?description: string option, ?fingerprint: string option, ?iin: string option, ?issuer: string option) =
            {
                PaymentMethodCard.Brand = brand //required
                PaymentMethodCard.Checks = checks //required
                PaymentMethodCard.Country = country //required
                PaymentMethodCard.DisplayBrand = displayBrand //required
                PaymentMethodCard.ExpMonth = expMonth //required
                PaymentMethodCard.ExpYear = expYear //required
                PaymentMethodCard.Funding = funding //required
                PaymentMethodCard.GeneratedFrom = generatedFrom //required
                PaymentMethodCard.Last4 = last4 //required
                PaymentMethodCard.Networks = networks //required
                PaymentMethodCard.RegulatedStatus = regulatedStatus //required
                PaymentMethodCard.ThreeDSecureUsage = threeDSecureUsage //required
                PaymentMethodCard.Wallet = wallet //required
                PaymentMethodCard.Description = description |> Option.flatten
                PaymentMethodCard.Fingerprint = fingerprint |> Option.flatten
                PaymentMethodCard.Iin = iin |> Option.flatten
                PaymentMethodCard.Issuer = issuer |> Option.flatten
            }

    and PaymentMethodCardChecks = {
        ///If a address line1 was provided, results of the check, one of `pass`, `fail`, `unavailable`, or `unchecked`.
        [<JsonField(Name="address_line1_check")>]AddressLine1Check: PaymentMethodCardChecksAddressLine1Check option
        ///If a address postal code was provided, results of the check, one of `pass`, `fail`, `unavailable`, or `unchecked`.
        AddressPostalCodeCheck: PaymentMethodCardChecksAddressPostalCodeCheck option
        ///If a CVC was provided, results of the check, one of `pass`, `fail`, `unavailable`, or `unchecked`.
        CvcCheck: PaymentMethodCardChecksCvcCheck option
    }
    with
        static member New (addressLine1Check: PaymentMethodCardChecksAddressLine1Check option, addressPostalCodeCheck: PaymentMethodCardChecksAddressPostalCodeCheck option, cvcCheck: PaymentMethodCardChecksCvcCheck option) =
            {
                PaymentMethodCardChecks.AddressLine1Check = addressLine1Check //required
                PaymentMethodCardChecks.AddressPostalCodeCheck = addressPostalCodeCheck //required
                PaymentMethodCardChecks.CvcCheck = cvcCheck //required
            }

    and PaymentMethodCardGeneratedCard = {
        ///The charge that created this object.
        Charge: string option
        ///Transaction-specific details of the payment method used in the payment.
        PaymentMethodDetails: CardGeneratedFromPaymentMethodDetails option
        ///The ID of the SetupAttempt that generated this PaymentMethod, if any.
        SetupAttempt: PaymentMethodCardGeneratedCardSetupAttempt'AnyOf option
    }
    with
        static member New (charge: string option, paymentMethodDetails: CardGeneratedFromPaymentMethodDetails option, setupAttempt: PaymentMethodCardGeneratedCardSetupAttempt'AnyOf option) =
            {
                PaymentMethodCardGeneratedCard.Charge = charge //required
                PaymentMethodCardGeneratedCard.PaymentMethodDetails = paymentMethodDetails //required
                PaymentMethodCardGeneratedCard.SetupAttempt = setupAttempt //required
            }

    and PaymentMethodCardGeneratedCardSetupAttempt'AnyOf =
        | String of string
        | SetupAttempt of SetupAttempt

    and PaymentMethodCardPresent = {
        ///Card brand. Can be `amex`, `cartes_bancaires`, `diners`, `discover`, `eftpos_au`, `jcb`, `link`, `mastercard`, `unionpay`, `visa` or `unknown`.
        Brand: PaymentMethodCardPresentBrand option
        ///The [product code](https://stripe.com/docs/card-product-codes) that identifies the specific program or product associated with a card.
        BrandProduct: string option
        ///The cardholder name as read from the card, in [ISO 7813](https://en.wikipedia.org/wiki/ISO/IEC_7813) format. May include alphanumeric characters, special characters and first/last name separator (`/`). In some cases, the cardholder name may not be available depending on how the issuer has configured the card. Cardholder name is typically not available on swipe or contactless payments, such as those made with Apple Pay and Google Pay.
        CardholderName: string option
        ///Two-letter ISO code representing the country of the card. You could use this attribute to get a sense of the international breakdown of cards you've collected.
        Country: IsoTypes.IsoCountryCode option
        ///A high-level description of the type of cards issued in this range. (For internal use only and not typically available in standard API requests.)
        Description: string option
        ///Two-digit number representing the card's expiration month.
        ExpMonth: int
        ///Four-digit number representing the card's expiration year.
        ExpYear: int
        ///Uniquely identifies this particular card number. You can use this attribute to check whether two customers who’ve signed up with you are using the same card number, for example. For payment methods that tokenize card information (Apple Pay, Google Pay), the tokenized number might be provided instead of the underlying card number.
        ///*As of May 1, 2021, card fingerprint in India for Connect changed to allow two fingerprints for the same card---one for India and one for the rest of the world.*
        Fingerprint: string option
        ///Card funding type. Can be `credit`, `debit`, `prepaid`, or `unknown`.
        Funding: PaymentMethodCardPresentFunding option
        ///Issuer identification number of the card. (For internal use only and not typically available in standard API requests.)
        Iin: string option
        ///The name of the card's issuing bank. (For internal use only and not typically available in standard API requests.)
        Issuer: string option
        ///The last four digits of the card.
        [<JsonField(Name="last4")>]Last4: string option
        ///Contains information about card networks that can be used to process the payment.
        Networks: PaymentMethodCardPresentNetworks option
        ///Details about payment methods collected offline.
        Offline: PaymentMethodDetailsCardPresentOffline option
        ///The languages that the issuing bank recommends using for localizing any customer-facing text, as read from the card. Referenced from EMV tag 5F2D, data encoded on the card's chip.
        PreferredLocales: string list option
        ///How card details were read in this transaction.
        ReadMethod: PaymentMethodCardPresentReadMethod option
        Wallet: PaymentFlowsPrivatePaymentMethodsCardPresentCommonWallet option
    }
    with
        static member New (brand: PaymentMethodCardPresentBrand option, brandProduct: string option, cardholderName: string option, country: IsoTypes.IsoCountryCode option, expMonth: int, expYear: int, fingerprint: string option, funding: PaymentMethodCardPresentFunding option, last4: string option, networks: PaymentMethodCardPresentNetworks option, offline: PaymentMethodDetailsCardPresentOffline option, preferredLocales: string list option, readMethod: PaymentMethodCardPresentReadMethod option, ?description: string option, ?iin: string option, ?issuer: string option, ?wallet: PaymentFlowsPrivatePaymentMethodsCardPresentCommonWallet) =
            {
                PaymentMethodCardPresent.Brand = brand //required
                PaymentMethodCardPresent.BrandProduct = brandProduct //required
                PaymentMethodCardPresent.CardholderName = cardholderName //required
                PaymentMethodCardPresent.Country = country //required
                PaymentMethodCardPresent.ExpMonth = expMonth //required
                PaymentMethodCardPresent.ExpYear = expYear //required
                PaymentMethodCardPresent.Fingerprint = fingerprint //required
                PaymentMethodCardPresent.Funding = funding //required
                PaymentMethodCardPresent.Last4 = last4 //required
                PaymentMethodCardPresent.Networks = networks //required
                PaymentMethodCardPresent.Offline = offline //required
                PaymentMethodCardPresent.PreferredLocales = preferredLocales //required
                PaymentMethodCardPresent.ReadMethod = readMethod //required
                PaymentMethodCardPresent.Description = description |> Option.flatten
                PaymentMethodCardPresent.Iin = iin |> Option.flatten
                PaymentMethodCardPresent.Issuer = issuer |> Option.flatten
                PaymentMethodCardPresent.Wallet = wallet
            }

    and PaymentMethodCardPresentNetworks = {
        ///All networks available for selection via [payment_method_options.card.network](/api/payment_intents/confirm#confirm_payment_intent-payment_method_options-card-network).
        Available: string list
        ///The preferred network for the card.
        Preferred: string option
    }
    with
        static member New (available: string list, preferred: string option) =
            {
                PaymentMethodCardPresentNetworks.Available = available //required
                PaymentMethodCardPresentNetworks.Preferred = preferred //required
            }

    and PaymentMethodCardWallet = {
        AmexExpressCheckout: PaymentMethodCardWalletAmexExpressCheckout option
        ApplePay: PaymentMethodCardWalletApplePay option
        ///(For tokenized numbers only.) The last four digits of the device account number.
        [<JsonField(Name="dynamic_last4")>]DynamicLast4: string option
        GooglePay: PaymentMethodCardWalletGooglePay option
        Link: PaymentMethodCardWalletLink option
        Masterpass: PaymentMethodCardWalletMasterpass option
        SamsungPay: PaymentMethodCardWalletSamsungPay option
        ///The type of the card wallet, one of `amex_express_checkout`, `apple_pay`, `google_pay`, `masterpass`, `samsung_pay`, `visa_checkout`, or `link`. An additional hash is included on the Wallet subhash with a name matching this value. It contains additional information specific to the card wallet type.
        Type: PaymentMethodCardWalletType
        VisaCheckout: PaymentMethodCardWalletVisaCheckout option
    }
    with
        static member New (dynamicLast4: string option, ``type``: PaymentMethodCardWalletType, ?amexExpressCheckout: PaymentMethodCardWalletAmexExpressCheckout, ?applePay: PaymentMethodCardWalletApplePay, ?googlePay: PaymentMethodCardWalletGooglePay, ?link: PaymentMethodCardWalletLink, ?masterpass: PaymentMethodCardWalletMasterpass, ?samsungPay: PaymentMethodCardWalletSamsungPay, ?visaCheckout: PaymentMethodCardWalletVisaCheckout) =
            {
                PaymentMethodCardWallet.DynamicLast4 = dynamicLast4 //required
                PaymentMethodCardWallet.Type = ``type`` //required
                PaymentMethodCardWallet.AmexExpressCheckout = amexExpressCheckout
                PaymentMethodCardWallet.ApplePay = applePay
                PaymentMethodCardWallet.GooglePay = googlePay
                PaymentMethodCardWallet.Link = link
                PaymentMethodCardWallet.Masterpass = masterpass
                PaymentMethodCardWallet.SamsungPay = samsungPay
                PaymentMethodCardWallet.VisaCheckout = visaCheckout
            }

    and PaymentMethodCardWalletMasterpass = {
        ///Owner's verified billing address. Values are verified or provided by the wallet directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        BillingAddress: Address option
        ///Owner's verified email. Values are verified or provided by the wallet directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        Email: string option
        ///Owner's verified full name. Values are verified or provided by the wallet directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        Name: string option
        ///Owner's verified shipping address. Values are verified or provided by the wallet directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        ShippingAddress: Address option
    }
    with
        static member New (billingAddress: Address option, email: string option, name: string option, shippingAddress: Address option) =
            {
                PaymentMethodCardWalletMasterpass.BillingAddress = billingAddress //required
                PaymentMethodCardWalletMasterpass.Email = email //required
                PaymentMethodCardWalletMasterpass.Name = name //required
                PaymentMethodCardWalletMasterpass.ShippingAddress = shippingAddress //required
            }

    and PaymentMethodCardWalletVisaCheckout = {
        ///Owner's verified billing address. Values are verified or provided by the wallet directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        BillingAddress: Address option
        ///Owner's verified email. Values are verified or provided by the wallet directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        Email: string option
        ///Owner's verified full name. Values are verified or provided by the wallet directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        Name: string option
        ///Owner's verified shipping address. Values are verified or provided by the wallet directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        ShippingAddress: Address option
    }
    with
        static member New (billingAddress: Address option, email: string option, name: string option, shippingAddress: Address option) =
            {
                PaymentMethodCardWalletVisaCheckout.BillingAddress = billingAddress //required
                PaymentMethodCardWalletVisaCheckout.Email = email //required
                PaymentMethodCardWalletVisaCheckout.Name = name //required
                PaymentMethodCardWalletVisaCheckout.ShippingAddress = shippingAddress //required
            }

    and PaymentMethodConfigResourceDisplayPreference = {
        ///For child configs, whether or not the account's preference will be observed. If `false`, the parent configuration's default is used.
        Overridable: bool option
        ///The account's display preference.
        Preference: PaymentMethodConfigResourceDisplayPreferencePreference
        ///The effective display preference value.
        Value: PaymentMethodConfigResourceDisplayPreferenceValue
    }
    with
        static member New (overridable: bool option, preference: PaymentMethodConfigResourceDisplayPreferencePreference, value: PaymentMethodConfigResourceDisplayPreferenceValue) =
            {
                PaymentMethodConfigResourceDisplayPreference.Overridable = overridable //required
                PaymentMethodConfigResourceDisplayPreference.Preference = preference //required
                PaymentMethodConfigResourceDisplayPreference.Value = value //required
            }

    and PaymentMethodConfigResourcePaymentMethodProperties = {
        ///Whether this payment method may be offered at checkout. True if `display_preference` is `on` and the payment method's capability is active.
        Available: bool
        DisplayPreference: PaymentMethodConfigResourceDisplayPreference
    }
    with
        static member New (available: bool, displayPreference: PaymentMethodConfigResourceDisplayPreference) =
            {
                PaymentMethodConfigResourcePaymentMethodProperties.Available = available //required
                PaymentMethodConfigResourcePaymentMethodProperties.DisplayPreference = displayPreference //required
            }

    ///PaymentMethodConfigurations control which payment methods are displayed to your customers when you don't explicitly specify payment method types. You can have multiple configurations with different sets of payment methods for different scenarios.
    ///There are two types of PaymentMethodConfigurations. Which is used depends on the [charge type](https://docs.stripe.com/connect/charges):
    ///**Direct** configurations apply to payments created on your account, including Connect destination charges, Connect separate charges and transfers, and payments not involving Connect.
    ///**Child** configurations apply to payments created on your connected accounts using direct charges, and charges with the on_behalf_of parameter.
    ///Child configurations have a `parent` that sets default values and controls which settings connected accounts may override. You can specify a parent ID at payment time, and Stripe will automatically resolve the connected account’s associated child configuration. Parent configurations are [managed in the dashboard](https://dashboard.stripe.com/settings/payment_methods/connected_accounts) and are not available in this API.
    ///Related guides:
    ///- [Payment Method Configurations API](https://docs.stripe.com/connect/payment-method-configurations)
    ///- [Multiple configurations on dynamic payment methods](https://docs.stripe.com/payments/multiple-payment-method-configs)
    ///- [Multiple configurations for your Connect accounts](https://docs.stripe.com/connect/multiple-payment-method-configurations)
    and PaymentMethodConfiguration = {
        AcssDebit: PaymentMethodConfigResourcePaymentMethodProperties option
        ///Whether the configuration can be used for new payments.
        Active: bool
        Affirm: PaymentMethodConfigResourcePaymentMethodProperties option
        AfterpayClearpay: PaymentMethodConfigResourcePaymentMethodProperties option
        Alipay: PaymentMethodConfigResourcePaymentMethodProperties option
        Alma: PaymentMethodConfigResourcePaymentMethodProperties option
        AmazonPay: PaymentMethodConfigResourcePaymentMethodProperties option
        ApplePay: PaymentMethodConfigResourcePaymentMethodProperties option
        ///For child configs, the Connect application associated with the configuration.
        Application: string option
        AuBecsDebit: PaymentMethodConfigResourcePaymentMethodProperties option
        BacsDebit: PaymentMethodConfigResourcePaymentMethodProperties option
        Bancontact: PaymentMethodConfigResourcePaymentMethodProperties option
        Billie: PaymentMethodConfigResourcePaymentMethodProperties option
        Blik: PaymentMethodConfigResourcePaymentMethodProperties option
        Boleto: PaymentMethodConfigResourcePaymentMethodProperties option
        Card: PaymentMethodConfigResourcePaymentMethodProperties option
        CartesBancaires: PaymentMethodConfigResourcePaymentMethodProperties option
        Cashapp: PaymentMethodConfigResourcePaymentMethodProperties option
        Crypto: PaymentMethodConfigResourcePaymentMethodProperties option
        CustomerBalance: PaymentMethodConfigResourcePaymentMethodProperties option
        Eps: PaymentMethodConfigResourcePaymentMethodProperties option
        Fpx: PaymentMethodConfigResourcePaymentMethodProperties option
        Giropay: PaymentMethodConfigResourcePaymentMethodProperties option
        GooglePay: PaymentMethodConfigResourcePaymentMethodProperties option
        Grabpay: PaymentMethodConfigResourcePaymentMethodProperties option
        ///Unique identifier for the object.
        Id: string
        Ideal: PaymentMethodConfigResourcePaymentMethodProperties option
        ///The default configuration is used whenever a payment method configuration is not specified.
        IsDefault: bool
        Jcb: PaymentMethodConfigResourcePaymentMethodProperties option
        KakaoPay: PaymentMethodConfigResourcePaymentMethodProperties option
        Klarna: PaymentMethodConfigResourcePaymentMethodProperties option
        Konbini: PaymentMethodConfigResourcePaymentMethodProperties option
        KrCard: PaymentMethodConfigResourcePaymentMethodProperties option
        Link: PaymentMethodConfigResourcePaymentMethodProperties option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        MbWay: PaymentMethodConfigResourcePaymentMethodProperties option
        Mobilepay: PaymentMethodConfigResourcePaymentMethodProperties option
        Multibanco: PaymentMethodConfigResourcePaymentMethodProperties option
        ///The configuration's name.
        Name: string
        NaverPay: PaymentMethodConfigResourcePaymentMethodProperties option
        NzBankAccount: PaymentMethodConfigResourcePaymentMethodProperties option
        Oxxo: PaymentMethodConfigResourcePaymentMethodProperties option
        [<JsonField(Name="p24")>]P24: PaymentMethodConfigResourcePaymentMethodProperties option
        ///For child configs, the configuration's parent configuration.
        Parent: string option
        PayByBank: PaymentMethodConfigResourcePaymentMethodProperties option
        Payco: PaymentMethodConfigResourcePaymentMethodProperties option
        Paynow: PaymentMethodConfigResourcePaymentMethodProperties option
        Paypal: PaymentMethodConfigResourcePaymentMethodProperties option
        Payto: PaymentMethodConfigResourcePaymentMethodProperties option
        Pix: PaymentMethodConfigResourcePaymentMethodProperties option
        Promptpay: PaymentMethodConfigResourcePaymentMethodProperties option
        RevolutPay: PaymentMethodConfigResourcePaymentMethodProperties option
        SamsungPay: PaymentMethodConfigResourcePaymentMethodProperties option
        Satispay: PaymentMethodConfigResourcePaymentMethodProperties option
        SepaDebit: PaymentMethodConfigResourcePaymentMethodProperties option
        Sofort: PaymentMethodConfigResourcePaymentMethodProperties option
        Sunbit: PaymentMethodConfigResourcePaymentMethodProperties option
        Swish: PaymentMethodConfigResourcePaymentMethodProperties option
        Twint: PaymentMethodConfigResourcePaymentMethodProperties option
        Upi: PaymentMethodConfigResourcePaymentMethodProperties option
        UsBankAccount: PaymentMethodConfigResourcePaymentMethodProperties option
        WechatPay: PaymentMethodConfigResourcePaymentMethodProperties option
        Zip: PaymentMethodConfigResourcePaymentMethodProperties option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "payment_method_configuration"

        static member New (active: bool, application: string option, id: string, isDefault: bool, livemode: bool, name: string, parent: string option, ?acssDebit: PaymentMethodConfigResourcePaymentMethodProperties, ?affirm: PaymentMethodConfigResourcePaymentMethodProperties, ?afterpayClearpay: PaymentMethodConfigResourcePaymentMethodProperties, ?alipay: PaymentMethodConfigResourcePaymentMethodProperties, ?alma: PaymentMethodConfigResourcePaymentMethodProperties, ?amazonPay: PaymentMethodConfigResourcePaymentMethodProperties, ?applePay: PaymentMethodConfigResourcePaymentMethodProperties, ?auBecsDebit: PaymentMethodConfigResourcePaymentMethodProperties, ?bacsDebit: PaymentMethodConfigResourcePaymentMethodProperties, ?bancontact: PaymentMethodConfigResourcePaymentMethodProperties, ?billie: PaymentMethodConfigResourcePaymentMethodProperties, ?blik: PaymentMethodConfigResourcePaymentMethodProperties, ?boleto: PaymentMethodConfigResourcePaymentMethodProperties, ?card: PaymentMethodConfigResourcePaymentMethodProperties, ?cartesBancaires: PaymentMethodConfigResourcePaymentMethodProperties, ?cashapp: PaymentMethodConfigResourcePaymentMethodProperties, ?crypto: PaymentMethodConfigResourcePaymentMethodProperties, ?customerBalance: PaymentMethodConfigResourcePaymentMethodProperties, ?eps: PaymentMethodConfigResourcePaymentMethodProperties, ?fpx: PaymentMethodConfigResourcePaymentMethodProperties, ?giropay: PaymentMethodConfigResourcePaymentMethodProperties, ?googlePay: PaymentMethodConfigResourcePaymentMethodProperties, ?grabpay: PaymentMethodConfigResourcePaymentMethodProperties, ?ideal: PaymentMethodConfigResourcePaymentMethodProperties, ?jcb: PaymentMethodConfigResourcePaymentMethodProperties, ?kakaoPay: PaymentMethodConfigResourcePaymentMethodProperties, ?klarna: PaymentMethodConfigResourcePaymentMethodProperties, ?konbini: PaymentMethodConfigResourcePaymentMethodProperties, ?krCard: PaymentMethodConfigResourcePaymentMethodProperties, ?link: PaymentMethodConfigResourcePaymentMethodProperties, ?mbWay: PaymentMethodConfigResourcePaymentMethodProperties, ?mobilepay: PaymentMethodConfigResourcePaymentMethodProperties, ?multibanco: PaymentMethodConfigResourcePaymentMethodProperties, ?naverPay: PaymentMethodConfigResourcePaymentMethodProperties, ?nzBankAccount: PaymentMethodConfigResourcePaymentMethodProperties, ?oxxo: PaymentMethodConfigResourcePaymentMethodProperties, ?p24: PaymentMethodConfigResourcePaymentMethodProperties, ?payByBank: PaymentMethodConfigResourcePaymentMethodProperties, ?payco: PaymentMethodConfigResourcePaymentMethodProperties, ?paynow: PaymentMethodConfigResourcePaymentMethodProperties, ?paypal: PaymentMethodConfigResourcePaymentMethodProperties, ?payto: PaymentMethodConfigResourcePaymentMethodProperties, ?pix: PaymentMethodConfigResourcePaymentMethodProperties, ?promptpay: PaymentMethodConfigResourcePaymentMethodProperties, ?revolutPay: PaymentMethodConfigResourcePaymentMethodProperties, ?samsungPay: PaymentMethodConfigResourcePaymentMethodProperties, ?satispay: PaymentMethodConfigResourcePaymentMethodProperties, ?sepaDebit: PaymentMethodConfigResourcePaymentMethodProperties, ?sofort: PaymentMethodConfigResourcePaymentMethodProperties, ?sunbit: PaymentMethodConfigResourcePaymentMethodProperties, ?swish: PaymentMethodConfigResourcePaymentMethodProperties, ?twint: PaymentMethodConfigResourcePaymentMethodProperties, ?upi: PaymentMethodConfigResourcePaymentMethodProperties, ?usBankAccount: PaymentMethodConfigResourcePaymentMethodProperties, ?wechatPay: PaymentMethodConfigResourcePaymentMethodProperties, ?zip: PaymentMethodConfigResourcePaymentMethodProperties) =
            {
                PaymentMethodConfiguration.Active = active //required
                PaymentMethodConfiguration.Application = application //required
                PaymentMethodConfiguration.Id = id //required
                PaymentMethodConfiguration.IsDefault = isDefault //required
                PaymentMethodConfiguration.Livemode = livemode //required
                PaymentMethodConfiguration.Name = name //required
                PaymentMethodConfiguration.Parent = parent //required
                PaymentMethodConfiguration.AcssDebit = acssDebit
                PaymentMethodConfiguration.Affirm = affirm
                PaymentMethodConfiguration.AfterpayClearpay = afterpayClearpay
                PaymentMethodConfiguration.Alipay = alipay
                PaymentMethodConfiguration.Alma = alma
                PaymentMethodConfiguration.AmazonPay = amazonPay
                PaymentMethodConfiguration.ApplePay = applePay
                PaymentMethodConfiguration.AuBecsDebit = auBecsDebit
                PaymentMethodConfiguration.BacsDebit = bacsDebit
                PaymentMethodConfiguration.Bancontact = bancontact
                PaymentMethodConfiguration.Billie = billie
                PaymentMethodConfiguration.Blik = blik
                PaymentMethodConfiguration.Boleto = boleto
                PaymentMethodConfiguration.Card = card
                PaymentMethodConfiguration.CartesBancaires = cartesBancaires
                PaymentMethodConfiguration.Cashapp = cashapp
                PaymentMethodConfiguration.Crypto = crypto
                PaymentMethodConfiguration.CustomerBalance = customerBalance
                PaymentMethodConfiguration.Eps = eps
                PaymentMethodConfiguration.Fpx = fpx
                PaymentMethodConfiguration.Giropay = giropay
                PaymentMethodConfiguration.GooglePay = googlePay
                PaymentMethodConfiguration.Grabpay = grabpay
                PaymentMethodConfiguration.Ideal = ideal
                PaymentMethodConfiguration.Jcb = jcb
                PaymentMethodConfiguration.KakaoPay = kakaoPay
                PaymentMethodConfiguration.Klarna = klarna
                PaymentMethodConfiguration.Konbini = konbini
                PaymentMethodConfiguration.KrCard = krCard
                PaymentMethodConfiguration.Link = link
                PaymentMethodConfiguration.MbWay = mbWay
                PaymentMethodConfiguration.Mobilepay = mobilepay
                PaymentMethodConfiguration.Multibanco = multibanco
                PaymentMethodConfiguration.NaverPay = naverPay
                PaymentMethodConfiguration.NzBankAccount = nzBankAccount
                PaymentMethodConfiguration.Oxxo = oxxo
                PaymentMethodConfiguration.P24 = p24
                PaymentMethodConfiguration.PayByBank = payByBank
                PaymentMethodConfiguration.Payco = payco
                PaymentMethodConfiguration.Paynow = paynow
                PaymentMethodConfiguration.Paypal = paypal
                PaymentMethodConfiguration.Payto = payto
                PaymentMethodConfiguration.Pix = pix
                PaymentMethodConfiguration.Promptpay = promptpay
                PaymentMethodConfiguration.RevolutPay = revolutPay
                PaymentMethodConfiguration.SamsungPay = samsungPay
                PaymentMethodConfiguration.Satispay = satispay
                PaymentMethodConfiguration.SepaDebit = sepaDebit
                PaymentMethodConfiguration.Sofort = sofort
                PaymentMethodConfiguration.Sunbit = sunbit
                PaymentMethodConfiguration.Swish = swish
                PaymentMethodConfiguration.Twint = twint
                PaymentMethodConfiguration.Upi = upi
                PaymentMethodConfiguration.UsBankAccount = usBankAccount
                PaymentMethodConfiguration.WechatPay = wechatPay
                PaymentMethodConfiguration.Zip = zip
            }

    and PaymentMethodCustom = {
        ///Display name of the Dashboard-only CustomPaymentMethodType.
        DisplayName: string option
        ///Contains information about the Dashboard-only CustomPaymentMethodType logo.
        Logo: CustomLogo option
        ///ID of the Dashboard-only CustomPaymentMethodType. Not expandable.
        Type: string
    }
    with
        static member New (displayName: string option, logo: CustomLogo option, ``type``: string) =
            {
                PaymentMethodCustom.DisplayName = displayName //required
                PaymentMethodCustom.Logo = logo //required
                PaymentMethodCustom.Type = ``type`` //required
            }

    and PaymentMethodDetails = {
        AchCreditTransfer: PaymentMethodDetailsAchCreditTransfer option
        AchDebit: PaymentMethodDetailsAchDebit option
        AcssDebit: PaymentMethodDetailsAcssDebit option
        Affirm: PaymentMethodDetailsAffirm option
        AfterpayClearpay: PaymentMethodDetailsAfterpayClearpay option
        Alipay: PaymentFlowsPrivatePaymentMethodsAlipayDetails option
        Alma: PaymentMethodDetailsAlma option
        AmazonPay: PaymentMethodDetailsAmazonPay option
        AuBecsDebit: PaymentMethodDetailsAuBecsDebit option
        BacsDebit: PaymentMethodDetailsBacsDebit option
        Bancontact: PaymentMethodDetailsBancontact option
        Billie: PaymentMethodDetailsBillie option
        Blik: PaymentMethodDetailsBlik option
        Boleto: PaymentMethodDetailsBoleto option
        Card: PaymentMethodDetailsCard option
        CardPresent: PaymentMethodDetailsCardPresent option
        Cashapp: PaymentMethodDetailsCashapp option
        Crypto: PaymentMethodDetailsCrypto option
        CustomerBalance: PaymentMethodDetailsCustomerBalance option
        Eps: PaymentMethodDetailsEps option
        Fpx: PaymentMethodDetailsFpx option
        Giropay: PaymentMethodDetailsGiropay option
        Grabpay: PaymentMethodDetailsGrabpay option
        Ideal: PaymentMethodDetailsIdeal option
        InteracPresent: PaymentMethodDetailsInteracPresent option
        KakaoPay: PaymentMethodDetailsKakaoPay option
        Klarna: PaymentMethodDetailsKlarna option
        Konbini: PaymentMethodDetailsKonbini option
        KrCard: PaymentMethodDetailsKrCard option
        Link: PaymentMethodDetailsLink option
        MbWay: PaymentMethodDetailsMbWay option
        Mobilepay: PaymentMethodDetailsMobilepay option
        Multibanco: PaymentMethodDetailsMultibanco option
        NaverPay: PaymentMethodDetailsNaverPay option
        NzBankAccount: PaymentMethodDetailsNzBankAccount option
        Oxxo: PaymentMethodDetailsOxxo option
        [<JsonField(Name="p24")>]P24: PaymentMethodDetailsP24 option
        PayByBank: PaymentMethodDetailsPayByBank option
        Payco: PaymentMethodDetailsPayco option
        Paynow: PaymentMethodDetailsPaynow option
        Paypal: PaymentMethodDetailsPaypal option
        Payto: PaymentMethodDetailsPayto option
        Pix: PaymentMethodDetailsPix option
        Promptpay: PaymentMethodDetailsPromptpay option
        RevolutPay: PaymentMethodDetailsRevolutPay option
        SamsungPay: PaymentMethodDetailsSamsungPay option
        Satispay: PaymentMethodDetailsSatispay option
        SepaCreditTransfer: PaymentMethodDetailsSepaCreditTransfer option
        SepaDebit: PaymentMethodDetailsSepaDebit option
        Sofort: PaymentMethodDetailsSofort option
        StripeAccount: PaymentMethodDetailsStripeAccount option
        Sunbit: PaymentMethodDetailsSunbit option
        Swish: PaymentMethodDetailsSwish option
        Twint: PaymentMethodDetailsTwint option
        ///The type of transaction-specific details of the payment method used in the payment. See [PaymentMethod.type](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type) for the full list of possible types.
        ///An additional hash is included on `payment_method_details` with a name matching this value.
        ///It contains information specific to the payment method.
        Type: string
        Upi: PaymentMethodDetailsUpi option
        UsBankAccount: PaymentMethodDetailsUsBankAccount option
        Wechat: PaymentMethodDetailsWechat option
        WechatPay: PaymentMethodDetailsWechatPay option
        Zip: PaymentMethodDetailsZip option
    }
    with
        static member New (``type``: string, ?achCreditTransfer: PaymentMethodDetailsAchCreditTransfer, ?achDebit: PaymentMethodDetailsAchDebit, ?acssDebit: PaymentMethodDetailsAcssDebit, ?affirm: PaymentMethodDetailsAffirm, ?afterpayClearpay: PaymentMethodDetailsAfterpayClearpay, ?alipay: PaymentFlowsPrivatePaymentMethodsAlipayDetails, ?alma: PaymentMethodDetailsAlma, ?amazonPay: PaymentMethodDetailsAmazonPay, ?auBecsDebit: PaymentMethodDetailsAuBecsDebit, ?bacsDebit: PaymentMethodDetailsBacsDebit, ?bancontact: PaymentMethodDetailsBancontact, ?billie: PaymentMethodDetailsBillie, ?blik: PaymentMethodDetailsBlik, ?boleto: PaymentMethodDetailsBoleto, ?card: PaymentMethodDetailsCard, ?cardPresent: PaymentMethodDetailsCardPresent, ?cashapp: PaymentMethodDetailsCashapp, ?crypto: PaymentMethodDetailsCrypto, ?customerBalance: PaymentMethodDetailsCustomerBalance, ?eps: PaymentMethodDetailsEps, ?fpx: PaymentMethodDetailsFpx, ?giropay: PaymentMethodDetailsGiropay, ?grabpay: PaymentMethodDetailsGrabpay, ?ideal: PaymentMethodDetailsIdeal, ?interacPresent: PaymentMethodDetailsInteracPresent, ?kakaoPay: PaymentMethodDetailsKakaoPay, ?klarna: PaymentMethodDetailsKlarna, ?konbini: PaymentMethodDetailsKonbini, ?krCard: PaymentMethodDetailsKrCard, ?link: PaymentMethodDetailsLink, ?mbWay: PaymentMethodDetailsMbWay, ?mobilepay: PaymentMethodDetailsMobilepay, ?multibanco: PaymentMethodDetailsMultibanco, ?naverPay: PaymentMethodDetailsNaverPay, ?nzBankAccount: PaymentMethodDetailsNzBankAccount, ?oxxo: PaymentMethodDetailsOxxo, ?p24: PaymentMethodDetailsP24, ?payByBank: PaymentMethodDetailsPayByBank, ?payco: PaymentMethodDetailsPayco, ?paynow: PaymentMethodDetailsPaynow, ?paypal: PaymentMethodDetailsPaypal, ?payto: PaymentMethodDetailsPayto, ?pix: PaymentMethodDetailsPix, ?promptpay: PaymentMethodDetailsPromptpay, ?revolutPay: PaymentMethodDetailsRevolutPay, ?samsungPay: PaymentMethodDetailsSamsungPay, ?satispay: PaymentMethodDetailsSatispay, ?sepaCreditTransfer: PaymentMethodDetailsSepaCreditTransfer, ?sepaDebit: PaymentMethodDetailsSepaDebit, ?sofort: PaymentMethodDetailsSofort, ?stripeAccount: PaymentMethodDetailsStripeAccount, ?sunbit: PaymentMethodDetailsSunbit, ?swish: PaymentMethodDetailsSwish, ?twint: PaymentMethodDetailsTwint, ?upi: PaymentMethodDetailsUpi, ?usBankAccount: PaymentMethodDetailsUsBankAccount, ?wechat: PaymentMethodDetailsWechat, ?wechatPay: PaymentMethodDetailsWechatPay, ?zip: PaymentMethodDetailsZip) =
            {
                PaymentMethodDetails.Type = ``type`` //required
                PaymentMethodDetails.AchCreditTransfer = achCreditTransfer
                PaymentMethodDetails.AchDebit = achDebit
                PaymentMethodDetails.AcssDebit = acssDebit
                PaymentMethodDetails.Affirm = affirm
                PaymentMethodDetails.AfterpayClearpay = afterpayClearpay
                PaymentMethodDetails.Alipay = alipay
                PaymentMethodDetails.Alma = alma
                PaymentMethodDetails.AmazonPay = amazonPay
                PaymentMethodDetails.AuBecsDebit = auBecsDebit
                PaymentMethodDetails.BacsDebit = bacsDebit
                PaymentMethodDetails.Bancontact = bancontact
                PaymentMethodDetails.Billie = billie
                PaymentMethodDetails.Blik = blik
                PaymentMethodDetails.Boleto = boleto
                PaymentMethodDetails.Card = card
                PaymentMethodDetails.CardPresent = cardPresent
                PaymentMethodDetails.Cashapp = cashapp
                PaymentMethodDetails.Crypto = crypto
                PaymentMethodDetails.CustomerBalance = customerBalance
                PaymentMethodDetails.Eps = eps
                PaymentMethodDetails.Fpx = fpx
                PaymentMethodDetails.Giropay = giropay
                PaymentMethodDetails.Grabpay = grabpay
                PaymentMethodDetails.Ideal = ideal
                PaymentMethodDetails.InteracPresent = interacPresent
                PaymentMethodDetails.KakaoPay = kakaoPay
                PaymentMethodDetails.Klarna = klarna
                PaymentMethodDetails.Konbini = konbini
                PaymentMethodDetails.KrCard = krCard
                PaymentMethodDetails.Link = link
                PaymentMethodDetails.MbWay = mbWay
                PaymentMethodDetails.Mobilepay = mobilepay
                PaymentMethodDetails.Multibanco = multibanco
                PaymentMethodDetails.NaverPay = naverPay
                PaymentMethodDetails.NzBankAccount = nzBankAccount
                PaymentMethodDetails.Oxxo = oxxo
                PaymentMethodDetails.P24 = p24
                PaymentMethodDetails.PayByBank = payByBank
                PaymentMethodDetails.Payco = payco
                PaymentMethodDetails.Paynow = paynow
                PaymentMethodDetails.Paypal = paypal
                PaymentMethodDetails.Payto = payto
                PaymentMethodDetails.Pix = pix
                PaymentMethodDetails.Promptpay = promptpay
                PaymentMethodDetails.RevolutPay = revolutPay
                PaymentMethodDetails.SamsungPay = samsungPay
                PaymentMethodDetails.Satispay = satispay
                PaymentMethodDetails.SepaCreditTransfer = sepaCreditTransfer
                PaymentMethodDetails.SepaDebit = sepaDebit
                PaymentMethodDetails.Sofort = sofort
                PaymentMethodDetails.StripeAccount = stripeAccount
                PaymentMethodDetails.Sunbit = sunbit
                PaymentMethodDetails.Swish = swish
                PaymentMethodDetails.Twint = twint
                PaymentMethodDetails.Upi = upi
                PaymentMethodDetails.UsBankAccount = usBankAccount
                PaymentMethodDetails.Wechat = wechat
                PaymentMethodDetails.WechatPay = wechatPay
                PaymentMethodDetails.Zip = zip
            }

    and PaymentMethodDetailsAchDebit = {
        ///Type of entity that holds the account. This can be either `individual` or `company`.
        AccountHolderType: PaymentMethodDetailsAchDebitAccountHolderType option
        ///Name of the bank associated with the bank account.
        BankName: string option
        ///Two-letter ISO code representing the country the bank account is located in.
        Country: IsoTypes.IsoCountryCode option
        ///Uniquely identifies this particular bank account. You can use this attribute to check whether two bank accounts are the same.
        Fingerprint: string option
        ///Last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string option
        ///Routing transit number of the bank account.
        RoutingNumber: string option
    }
    with
        static member New (accountHolderType: PaymentMethodDetailsAchDebitAccountHolderType option, bankName: string option, country: IsoTypes.IsoCountryCode option, fingerprint: string option, last4: string option, routingNumber: string option) =
            {
                PaymentMethodDetailsAchDebit.AccountHolderType = accountHolderType //required
                PaymentMethodDetailsAchDebit.BankName = bankName //required
                PaymentMethodDetailsAchDebit.Country = country //required
                PaymentMethodDetailsAchDebit.Fingerprint = fingerprint //required
                PaymentMethodDetailsAchDebit.Last4 = last4 //required
                PaymentMethodDetailsAchDebit.RoutingNumber = routingNumber //required
            }

    and PaymentMethodDetailsAlma = {
        Installments: AlmaInstallments option
        ///The Alma transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (transactionId: string option, ?installments: AlmaInstallments) =
            {
                PaymentMethodDetailsAlma.TransactionId = transactionId //required
                PaymentMethodDetailsAlma.Installments = installments
            }

    and PaymentMethodDetailsAmazonPay = {
        Funding: AmazonPayUnderlyingPaymentMethodFundingDetails option
        ///The Amazon Pay transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (transactionId: string option, ?funding: AmazonPayUnderlyingPaymentMethodFundingDetails) =
            {
                PaymentMethodDetailsAmazonPay.TransactionId = transactionId //required
                PaymentMethodDetailsAmazonPay.Funding = funding
            }

    and PaymentMethodDetailsBancontact = {
        ///Bank code of bank associated with the bank account.
        BankCode: string option
        ///Name of the bank associated with the bank account.
        BankName: string option
        ///Bank Identifier Code of the bank associated with the bank account.
        Bic: string option
        ///The ID of the SEPA Direct Debit PaymentMethod which was generated by this Charge.
        GeneratedSepaDebit: PaymentMethodDetailsBancontactGeneratedSepaDebit'AnyOf option
        ///The mandate for the SEPA Direct Debit PaymentMethod which was generated by this Charge.
        GeneratedSepaDebitMandate: PaymentMethodDetailsBancontactGeneratedSepaDebitMandate'AnyOf option
        ///Last four characters of the IBAN.
        [<JsonField(Name="iban_last4")>]IbanLast4: string option
        ///Preferred language of the Bancontact authorization page that the customer is redirected to.
        ///Can be one of `en`, `de`, `fr`, or `nl`
        PreferredLanguage: PaymentMethodDetailsBancontactPreferredLanguage option
        ///Owner's verified full name. Values are verified or provided by Bancontact directly
        ///(if supported) at the time of authorization or settlement. They cannot be set or mutated.
        VerifiedName: string option
    }
    with
        static member New (bankCode: string option, bankName: string option, bic: string option, generatedSepaDebit: PaymentMethodDetailsBancontactGeneratedSepaDebit'AnyOf option, generatedSepaDebitMandate: PaymentMethodDetailsBancontactGeneratedSepaDebitMandate'AnyOf option, ibanLast4: string option, preferredLanguage: PaymentMethodDetailsBancontactPreferredLanguage option, verifiedName: string option) =
            {
                PaymentMethodDetailsBancontact.BankCode = bankCode //required
                PaymentMethodDetailsBancontact.BankName = bankName //required
                PaymentMethodDetailsBancontact.Bic = bic //required
                PaymentMethodDetailsBancontact.GeneratedSepaDebit = generatedSepaDebit //required
                PaymentMethodDetailsBancontact.GeneratedSepaDebitMandate = generatedSepaDebitMandate //required
                PaymentMethodDetailsBancontact.IbanLast4 = ibanLast4 //required
                PaymentMethodDetailsBancontact.PreferredLanguage = preferredLanguage //required
                PaymentMethodDetailsBancontact.VerifiedName = verifiedName //required
            }

    and PaymentMethodDetailsBancontactGeneratedSepaDebit'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and PaymentMethodDetailsBancontactGeneratedSepaDebitMandate'AnyOf =
        | String of string
        | Mandate of Mandate

    and PaymentMethodDetailsCard = {
        ///The authorized amount.
        AmountAuthorized: int option
        ///Authorization code on the charge.
        AuthorizationCode: string option
        ///Card brand. Can be `amex`, `cartes_bancaires`, `diners`, `discover`, `eftpos_au`, `jcb`, `link`, `mastercard`, `unionpay`, `visa` or `unknown`.
        Brand: PaymentMethodDetailsCardBrand option
        ///When using manual capture, a future timestamp at which the charge will be automatically refunded if uncaptured.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CaptureBefore: DateTime option
        ///Check results by Card networks on Card address and CVC at time of payment.
        Checks: PaymentMethodDetailsCardChecks option
        ///Two-letter ISO code representing the country of the card. You could use this attribute to get a sense of the international breakdown of cards you've collected.
        Country: IsoTypes.IsoCountryCode option
        ///A high-level description of the type of cards issued in this range. (For internal use only and not typically available in standard API requests.)
        Description: string option
        ///Two-digit number representing the card's expiration month.
        ExpMonth: int
        ///Four-digit number representing the card's expiration year.
        ExpYear: int
        ExtendedAuthorization: PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesExtendedAuthorizationExtendedAuthorization option
        ///Uniquely identifies this particular card number. You can use this attribute to check whether two customers who’ve signed up with you are using the same card number, for example. For payment methods that tokenize card information (Apple Pay, Google Pay), the tokenized number might be provided instead of the underlying card number.
        ///*As of May 1, 2021, card fingerprint in India for Connect changed to allow two fingerprints for the same card---one for India and one for the rest of the world.*
        Fingerprint: string option
        ///Card funding type. Can be `credit`, `debit`, `prepaid`, or `unknown`.
        Funding: PaymentMethodDetailsCardFunding option
        ///Issuer identification number of the card. (For internal use only and not typically available in standard API requests.)
        Iin: string option
        IncrementalAuthorization: PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesIncrementalAuthorizationIncrementalAuthorization option
        ///Installment details for this payment.
        ///For more information, see the [installments integration guide](https://docs.stripe.com/payments/installments).
        Installments: PaymentMethodDetailsCardInstallments option
        ///The name of the card's issuing bank. (For internal use only and not typically available in standard API requests.)
        Issuer: string option
        ///The last four digits of the card.
        [<JsonField(Name="last4")>]Last4: string option
        ///ID of the mandate used to make this payment or created by it.
        Mandate: string option
        ///True if this payment was marked as MOTO and out of scope for SCA.
        Moto: bool option
        Multicapture: PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceMulticapture option
        ///Identifies which network this charge was processed on. Can be `amex`, `cartes_bancaires`, `diners`, `discover`, `eftpos_au`, `interac`, `jcb`, `link`, `mastercard`, `unionpay`, `visa`, or `unknown`.
        Network: PaymentMethodDetailsCardNetwork option
        ///If this card has network token credentials, this contains the details of the network token credentials.
        NetworkToken: PaymentMethodDetailsCardNetworkToken option
        ///This is used by the financial networks to identify a transaction. Visa calls this the Transaction ID, Mastercard calls this the Trace ID, and American Express calls this the Acquirer Reference Data. This value will be present if it is returned by the financial network in the authorization response, and null otherwise.
        NetworkTransactionId: string option
        Overcapture: PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesOvercaptureOvercapture option
        ///Status of a card based on the card issuer.
        RegulatedStatus: PaymentMethodDetailsCardRegulatedStatus option
        ///Populated if this transaction used 3D Secure authentication.
        ThreeDSecure: ThreeDSecureDetailsCharge option
        ///If this Card is part of a card wallet, this contains the details of the card wallet.
        Wallet: PaymentMethodDetailsCardWallet option
    }
    with
        static member New (amountAuthorized: int option, authorizationCode: string option, brand: PaymentMethodDetailsCardBrand option, checks: PaymentMethodDetailsCardChecks option, country: IsoTypes.IsoCountryCode option, expMonth: int, expYear: int, funding: PaymentMethodDetailsCardFunding option, installments: PaymentMethodDetailsCardInstallments option, last4: string option, mandate: string option, network: PaymentMethodDetailsCardNetwork option, networkTransactionId: string option, regulatedStatus: PaymentMethodDetailsCardRegulatedStatus option, threeDSecure: ThreeDSecureDetailsCharge option, wallet: PaymentMethodDetailsCardWallet option, ?captureBefore: DateTime, ?description: string option, ?extendedAuthorization: PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesExtendedAuthorizationExtendedAuthorization, ?fingerprint: string option, ?iin: string option, ?incrementalAuthorization: PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesIncrementalAuthorizationIncrementalAuthorization, ?issuer: string option, ?moto: bool option, ?multicapture: PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceMulticapture, ?networkToken: PaymentMethodDetailsCardNetworkToken option, ?overcapture: PaymentFlowsPrivatePaymentMethodsCardDetailsApiResourceEnterpriseFeaturesOvercaptureOvercapture) =
            {
                PaymentMethodDetailsCard.AmountAuthorized = amountAuthorized //required
                PaymentMethodDetailsCard.AuthorizationCode = authorizationCode //required
                PaymentMethodDetailsCard.Brand = brand //required
                PaymentMethodDetailsCard.Checks = checks //required
                PaymentMethodDetailsCard.Country = country //required
                PaymentMethodDetailsCard.ExpMonth = expMonth //required
                PaymentMethodDetailsCard.ExpYear = expYear //required
                PaymentMethodDetailsCard.Funding = funding //required
                PaymentMethodDetailsCard.Installments = installments //required
                PaymentMethodDetailsCard.Last4 = last4 //required
                PaymentMethodDetailsCard.Mandate = mandate //required
                PaymentMethodDetailsCard.Network = network //required
                PaymentMethodDetailsCard.NetworkTransactionId = networkTransactionId //required
                PaymentMethodDetailsCard.RegulatedStatus = regulatedStatus //required
                PaymentMethodDetailsCard.ThreeDSecure = threeDSecure //required
                PaymentMethodDetailsCard.Wallet = wallet //required
                PaymentMethodDetailsCard.CaptureBefore = captureBefore
                PaymentMethodDetailsCard.Description = description |> Option.flatten
                PaymentMethodDetailsCard.ExtendedAuthorization = extendedAuthorization
                PaymentMethodDetailsCard.Fingerprint = fingerprint |> Option.flatten
                PaymentMethodDetailsCard.Iin = iin |> Option.flatten
                PaymentMethodDetailsCard.IncrementalAuthorization = incrementalAuthorization
                PaymentMethodDetailsCard.Issuer = issuer |> Option.flatten
                PaymentMethodDetailsCard.Moto = moto |> Option.flatten
                PaymentMethodDetailsCard.Multicapture = multicapture
                PaymentMethodDetailsCard.NetworkToken = networkToken |> Option.flatten
                PaymentMethodDetailsCard.Overcapture = overcapture
            }

    and PaymentMethodDetailsCardChecks = {
        ///If a address line1 was provided, results of the check, one of `pass`, `fail`, `unavailable`, or `unchecked`.
        [<JsonField(Name="address_line1_check")>]AddressLine1Check: PaymentMethodDetailsCardChecksAddressLine1Check option
        ///If a address postal code was provided, results of the check, one of `pass`, `fail`, `unavailable`, or `unchecked`.
        AddressPostalCodeCheck: PaymentMethodDetailsCardChecksAddressPostalCodeCheck option
        ///If a CVC was provided, results of the check, one of `pass`, `fail`, `unavailable`, or `unchecked`.
        CvcCheck: PaymentMethodDetailsCardChecksCvcCheck option
    }
    with
        static member New (addressLine1Check: PaymentMethodDetailsCardChecksAddressLine1Check option, addressPostalCodeCheck: PaymentMethodDetailsCardChecksAddressPostalCodeCheck option, cvcCheck: PaymentMethodDetailsCardChecksCvcCheck option) =
            {
                PaymentMethodDetailsCardChecks.AddressLine1Check = addressLine1Check //required
                PaymentMethodDetailsCardChecks.AddressPostalCodeCheck = addressPostalCodeCheck //required
                PaymentMethodDetailsCardChecks.CvcCheck = cvcCheck //required
            }

    and PaymentMethodDetailsCardInstallments = {
        ///Installment plan selected for the payment.
        Plan: PaymentMethodDetailsCardInstallmentsPlan option
    }
    with
        static member New (plan: PaymentMethodDetailsCardInstallmentsPlan option) =
            {
                PaymentMethodDetailsCardInstallments.Plan = plan //required
            }

    and PaymentMethodDetailsCardInstallmentsPlan = {
        ///For `fixed_count` installment plans, this is the number of installment payments your customer will make to their credit card.
        Count: int option
        ///Type of installment plan, one of `fixed_count`, `bonus`, or `revolving`.
        Type: PaymentMethodDetailsCardInstallmentsPlanType
    }
    with
        ///For `fixed_count` installment plans, this is the interval between installment payments your customer will make to their credit card.
        ///One of `month`.
        member _.Interval = "month"

        static member New (count: int option, ``type``: PaymentMethodDetailsCardInstallmentsPlanType) =
            {
                PaymentMethodDetailsCardInstallmentsPlan.Count = count //required
                PaymentMethodDetailsCardInstallmentsPlan.Type = ``type`` //required
            }

    and PaymentMethodDetailsCardPresent = {
        ///The authorized amount
        AmountAuthorized: int option
        ///Card brand. Can be `amex`, `cartes_bancaires`, `diners`, `discover`, `eftpos_au`, `jcb`, `link`, `mastercard`, `unionpay`, `visa` or `unknown`.
        Brand: PaymentMethodDetailsCardPresentBrand option
        ///The [product code](https://stripe.com/docs/card-product-codes) that identifies the specific program or product associated with a card.
        BrandProduct: string option
        ///When using manual capture, a future timestamp after which the charge will be automatically refunded if uncaptured.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CaptureBefore: DateTime option
        ///The cardholder name as read from the card, in [ISO 7813](https://en.wikipedia.org/wiki/ISO/IEC_7813) format. May include alphanumeric characters, special characters and first/last name separator (`/`). In some cases, the cardholder name may not be available depending on how the issuer has configured the card. Cardholder name is typically not available on swipe or contactless payments, such as those made with Apple Pay and Google Pay.
        CardholderName: string option
        ///Two-letter ISO code representing the country of the card. You could use this attribute to get a sense of the international breakdown of cards you've collected.
        Country: IsoTypes.IsoCountryCode option
        ///A high-level description of the type of cards issued in this range. (For internal use only and not typically available in standard API requests.)
        Description: string option
        ///Authorization response cryptogram.
        EmvAuthData: string option
        ///Two-digit number representing the card's expiration month.
        ExpMonth: int
        ///Four-digit number representing the card's expiration year.
        ExpYear: int
        ///Uniquely identifies this particular card number. You can use this attribute to check whether two customers who’ve signed up with you are using the same card number, for example. For payment methods that tokenize card information (Apple Pay, Google Pay), the tokenized number might be provided instead of the underlying card number.
        ///*As of May 1, 2021, card fingerprint in India for Connect changed to allow two fingerprints for the same card---one for India and one for the rest of the world.*
        Fingerprint: string option
        ///Card funding type. Can be `credit`, `debit`, `prepaid`, or `unknown`.
        Funding: PaymentMethodDetailsCardPresentFunding option
        ///ID of a card PaymentMethod generated from the card_present PaymentMethod that may be attached to a Customer for future transactions. Only present if it was possible to generate a card PaymentMethod.
        GeneratedCard: string option
        ///Issuer identification number of the card. (For internal use only and not typically available in standard API requests.)
        Iin: string option
        ///Whether this [PaymentIntent](https://docs.stripe.com/api/payment_intents) is eligible for incremental authorizations. Request support using [request_incremental_authorization_support](https://docs.stripe.com/api/payment_intents/create#create_payment_intent-payment_method_options-card_present-request_incremental_authorization_support).
        IncrementalAuthorizationSupported: bool
        ///The name of the card's issuing bank. (For internal use only and not typically available in standard API requests.)
        Issuer: string option
        ///The last four digits of the card.
        [<JsonField(Name="last4")>]Last4: string option
        ///ID of the [location](https://docs.stripe.com/api/terminal/locations) that this transaction's reader is assigned to.
        Location: string option
        ///Identifies which network this charge was processed on. Can be `amex`, `cartes_bancaires`, `diners`, `discover`, `eftpos_au`, `interac`, `jcb`, `link`, `mastercard`, `unionpay`, `visa`, or `unknown`.
        Network: PaymentMethodDetailsCardPresentNetwork option
        ///This is used by the financial networks to identify a transaction. Visa calls this the Transaction ID, Mastercard calls this the Trace ID, and American Express calls this the Acquirer Reference Data. This value will be present if it is returned by the financial network in the authorization response, and null otherwise.
        NetworkTransactionId: string option
        ///Details about payments collected offline.
        Offline: PaymentMethodDetailsCardPresentOffline option
        ///Defines whether the authorized amount can be over-captured or not
        OvercaptureSupported: bool
        ///The languages that the issuing bank recommends using for localizing any customer-facing text, as read from the card. Referenced from EMV tag 5F2D, data encoded on the card's chip.
        PreferredLocales: string list option
        ///How card details were read in this transaction.
        ReadMethod: PaymentMethodDetailsCardPresentReadMethod option
        ///ID of the [reader](https://docs.stripe.com/api/terminal/readers) this transaction was made on.
        Reader: string option
        ///A collection of fields required to be displayed on receipts. Only required for EMV transactions.
        Receipt: PaymentMethodDetailsCardPresentReceipt option
        Wallet: PaymentFlowsPrivatePaymentMethodsCardPresentCommonWallet option
    }
    with
        static member New (amountAuthorized: int option, brand: PaymentMethodDetailsCardPresentBrand option, brandProduct: string option, cardholderName: string option, country: IsoTypes.IsoCountryCode option, emvAuthData: string option, expMonth: int, expYear: int, fingerprint: string option, funding: PaymentMethodDetailsCardPresentFunding option, generatedCard: string option, incrementalAuthorizationSupported: bool, last4: string option, network: PaymentMethodDetailsCardPresentNetwork option, networkTransactionId: string option, offline: PaymentMethodDetailsCardPresentOffline option, overcaptureSupported: bool, preferredLocales: string list option, readMethod: PaymentMethodDetailsCardPresentReadMethod option, receipt: PaymentMethodDetailsCardPresentReceipt option, ?captureBefore: DateTime, ?description: string option, ?iin: string option, ?issuer: string option, ?location: string, ?reader: string, ?wallet: PaymentFlowsPrivatePaymentMethodsCardPresentCommonWallet) =
            {
                PaymentMethodDetailsCardPresent.AmountAuthorized = amountAuthorized //required
                PaymentMethodDetailsCardPresent.Brand = brand //required
                PaymentMethodDetailsCardPresent.BrandProduct = brandProduct //required
                PaymentMethodDetailsCardPresent.CardholderName = cardholderName //required
                PaymentMethodDetailsCardPresent.Country = country //required
                PaymentMethodDetailsCardPresent.EmvAuthData = emvAuthData //required
                PaymentMethodDetailsCardPresent.ExpMonth = expMonth //required
                PaymentMethodDetailsCardPresent.ExpYear = expYear //required
                PaymentMethodDetailsCardPresent.Fingerprint = fingerprint //required
                PaymentMethodDetailsCardPresent.Funding = funding //required
                PaymentMethodDetailsCardPresent.GeneratedCard = generatedCard //required
                PaymentMethodDetailsCardPresent.IncrementalAuthorizationSupported = incrementalAuthorizationSupported //required
                PaymentMethodDetailsCardPresent.Last4 = last4 //required
                PaymentMethodDetailsCardPresent.Network = network //required
                PaymentMethodDetailsCardPresent.NetworkTransactionId = networkTransactionId //required
                PaymentMethodDetailsCardPresent.Offline = offline //required
                PaymentMethodDetailsCardPresent.OvercaptureSupported = overcaptureSupported //required
                PaymentMethodDetailsCardPresent.PreferredLocales = preferredLocales //required
                PaymentMethodDetailsCardPresent.ReadMethod = readMethod //required
                PaymentMethodDetailsCardPresent.Receipt = receipt //required
                PaymentMethodDetailsCardPresent.CaptureBefore = captureBefore
                PaymentMethodDetailsCardPresent.Description = description |> Option.flatten
                PaymentMethodDetailsCardPresent.Iin = iin |> Option.flatten
                PaymentMethodDetailsCardPresent.Issuer = issuer |> Option.flatten
                PaymentMethodDetailsCardPresent.Location = location
                PaymentMethodDetailsCardPresent.Reader = reader
                PaymentMethodDetailsCardPresent.Wallet = wallet
            }

    and PaymentMethodDetailsCardPresentReceipt = {
        ///The type of account being debited or credited
        AccountType: PaymentMethodDetailsCardPresentReceiptAccountType option
        ///The Application Cryptogram, a unique value generated by the card to authenticate the transaction with issuers.
        ApplicationCryptogram: string option
        ///The Application Identifier (AID) on the card used to determine which networks are eligible to process the transaction. Referenced from EMV tag 9F12, data encoded on the card's chip.
        ApplicationPreferredName: string option
        ///Identifier for this transaction.
        AuthorizationCode: string option
        ///EMV tag 8A. A code returned by the card issuer.
        AuthorizationResponseCode: string option
        ///Describes the method used by the cardholder to verify ownership of the card. One of the following: `approval`, `failure`, `none`, `offline_pin`, `offline_pin_and_signature`, `online_pin`, or `signature`.
        CardholderVerificationMethod: PaymentMethodDetailsCardPresentReceiptCardholderVerificationMethod option
        ///Similar to the application_preferred_name, identifying the applications (AIDs) available on the card. Referenced from EMV tag 84.
        DedicatedFileName: string option
        ///A 5-byte string that records the checks and validations that occur between the card and the terminal. These checks determine how the terminal processes the transaction and what risk tolerance is acceptable. Referenced from EMV Tag 95.
        TerminalVerificationResults: string option
        ///An indication of which steps were completed during the card read process. Referenced from EMV Tag 9B.
        TransactionStatusInformation: string option
    }
    with
        static member New (applicationCryptogram: string option, applicationPreferredName: string option, authorizationCode: string option, authorizationResponseCode: string option, cardholderVerificationMethod: PaymentMethodDetailsCardPresentReceiptCardholderVerificationMethod option, dedicatedFileName: string option, terminalVerificationResults: string option, transactionStatusInformation: string option, ?accountType: PaymentMethodDetailsCardPresentReceiptAccountType) =
            {
                PaymentMethodDetailsCardPresentReceipt.ApplicationCryptogram = applicationCryptogram //required
                PaymentMethodDetailsCardPresentReceipt.ApplicationPreferredName = applicationPreferredName //required
                PaymentMethodDetailsCardPresentReceipt.AuthorizationCode = authorizationCode //required
                PaymentMethodDetailsCardPresentReceipt.AuthorizationResponseCode = authorizationResponseCode //required
                PaymentMethodDetailsCardPresentReceipt.CardholderVerificationMethod = cardholderVerificationMethod //required
                PaymentMethodDetailsCardPresentReceipt.DedicatedFileName = dedicatedFileName //required
                PaymentMethodDetailsCardPresentReceipt.TerminalVerificationResults = terminalVerificationResults //required
                PaymentMethodDetailsCardPresentReceipt.TransactionStatusInformation = transactionStatusInformation //required
                PaymentMethodDetailsCardPresentReceipt.AccountType = accountType
            }

    and PaymentMethodDetailsCardWallet = {
        AmexExpressCheckout: PaymentMethodDetailsCardWalletAmexExpressCheckout option
        ApplePay: PaymentMethodDetailsCardWalletApplePay option
        ///(For tokenized numbers only.) The last four digits of the device account number.
        [<JsonField(Name="dynamic_last4")>]DynamicLast4: string option
        GooglePay: PaymentMethodDetailsCardWalletGooglePay option
        Link: PaymentMethodDetailsCardWalletLink option
        Masterpass: PaymentMethodDetailsCardWalletMasterpass option
        SamsungPay: PaymentMethodDetailsCardWalletSamsungPay option
        ///The type of the card wallet, one of `amex_express_checkout`, `apple_pay`, `google_pay`, `masterpass`, `samsung_pay`, `visa_checkout`, or `link`. An additional hash is included on the Wallet subhash with a name matching this value. It contains additional information specific to the card wallet type.
        Type: PaymentMethodDetailsCardWalletType
        VisaCheckout: PaymentMethodDetailsCardWalletVisaCheckout option
    }
    with
        static member New (dynamicLast4: string option, ``type``: PaymentMethodDetailsCardWalletType, ?amexExpressCheckout: PaymentMethodDetailsCardWalletAmexExpressCheckout, ?applePay: PaymentMethodDetailsCardWalletApplePay, ?googlePay: PaymentMethodDetailsCardWalletGooglePay, ?link: PaymentMethodDetailsCardWalletLink, ?masterpass: PaymentMethodDetailsCardWalletMasterpass, ?samsungPay: PaymentMethodDetailsCardWalletSamsungPay, ?visaCheckout: PaymentMethodDetailsCardWalletVisaCheckout) =
            {
                PaymentMethodDetailsCardWallet.DynamicLast4 = dynamicLast4 //required
                PaymentMethodDetailsCardWallet.Type = ``type`` //required
                PaymentMethodDetailsCardWallet.AmexExpressCheckout = amexExpressCheckout
                PaymentMethodDetailsCardWallet.ApplePay = applePay
                PaymentMethodDetailsCardWallet.GooglePay = googlePay
                PaymentMethodDetailsCardWallet.Link = link
                PaymentMethodDetailsCardWallet.Masterpass = masterpass
                PaymentMethodDetailsCardWallet.SamsungPay = samsungPay
                PaymentMethodDetailsCardWallet.VisaCheckout = visaCheckout
            }

    and PaymentMethodDetailsCardWalletMasterpass = {
        ///Owner's verified billing address. Values are verified or provided by the wallet directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        BillingAddress: Address option
        ///Owner's verified email. Values are verified or provided by the wallet directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        Email: string option
        ///Owner's verified full name. Values are verified or provided by the wallet directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        Name: string option
        ///Owner's verified shipping address. Values are verified or provided by the wallet directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        ShippingAddress: Address option
    }
    with
        static member New (billingAddress: Address option, email: string option, name: string option, shippingAddress: Address option) =
            {
                PaymentMethodDetailsCardWalletMasterpass.BillingAddress = billingAddress //required
                PaymentMethodDetailsCardWalletMasterpass.Email = email //required
                PaymentMethodDetailsCardWalletMasterpass.Name = name //required
                PaymentMethodDetailsCardWalletMasterpass.ShippingAddress = shippingAddress //required
            }

    and PaymentMethodDetailsCardWalletVisaCheckout = {
        ///Owner's verified billing address. Values are verified or provided by the wallet directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        BillingAddress: Address option
        ///Owner's verified email. Values are verified or provided by the wallet directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        Email: string option
        ///Owner's verified full name. Values are verified or provided by the wallet directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        Name: string option
        ///Owner's verified shipping address. Values are verified or provided by the wallet directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        ShippingAddress: Address option
    }
    with
        static member New (billingAddress: Address option, email: string option, name: string option, shippingAddress: Address option) =
            {
                PaymentMethodDetailsCardWalletVisaCheckout.BillingAddress = billingAddress //required
                PaymentMethodDetailsCardWalletVisaCheckout.Email = email //required
                PaymentMethodDetailsCardWalletVisaCheckout.Name = name //required
                PaymentMethodDetailsCardWalletVisaCheckout.ShippingAddress = shippingAddress //required
            }

    and PaymentMethodDetailsCrypto = {
        ///The wallet address of the customer.
        BuyerAddress: string option
        ///The blockchain network that the transaction was sent on.
        Network: PaymentMethodDetailsCryptoNetwork option
        ///The token currency that the transaction was sent with.
        TokenCurrency: PaymentMethodDetailsCryptoTokenCurrency option
        ///The blockchain transaction hash of the crypto payment.
        TransactionHash: string option
    }
    with
        static member New (?buyerAddress: string, ?network: PaymentMethodDetailsCryptoNetwork, ?tokenCurrency: PaymentMethodDetailsCryptoTokenCurrency, ?transactionHash: string) =
            {
                PaymentMethodDetailsCrypto.BuyerAddress = buyerAddress
                PaymentMethodDetailsCrypto.Network = network
                PaymentMethodDetailsCrypto.TokenCurrency = tokenCurrency
                PaymentMethodDetailsCrypto.TransactionHash = transactionHash
            }

    and PaymentMethodDetailsEps = {
        ///The customer's bank. Should be one of `arzte_und_apotheker_bank`, `austrian_anadi_bank_ag`, `bank_austria`, `bankhaus_carl_spangler`, `bankhaus_schelhammer_und_schattera_ag`, `bawag_psk_ag`, `bks_bank_ag`, `brull_kallmus_bank_ag`, `btv_vier_lander_bank`, `capital_bank_grawe_gruppe_ag`, `deutsche_bank_ag`, `dolomitenbank`, `easybank_ag`, `erste_bank_und_sparkassen`, `hypo_alpeadriabank_international_ag`, `hypo_noe_lb_fur_niederosterreich_u_wien`, `hypo_oberosterreich_salzburg_steiermark`, `hypo_tirol_bank_ag`, `hypo_vorarlberg_bank_ag`, `hypo_bank_burgenland_aktiengesellschaft`, `marchfelder_bank`, `oberbank_ag`, `raiffeisen_bankengruppe_osterreich`, `schoellerbank_ag`, `sparda_bank_wien`, `volksbank_gruppe`, `volkskreditbank_ag`, or `vr_bank_braunau`.
        Bank: PaymentMethodDetailsEpsBank option
        ///Owner's verified full name. Values are verified or provided by EPS directly
        ///(if supported) at the time of authorization or settlement. They cannot be set or mutated.
        ///EPS rarely provides this information so the attribute is usually empty.
        VerifiedName: string option
    }
    with
        static member New (bank: PaymentMethodDetailsEpsBank option, verifiedName: string option) =
            {
                PaymentMethodDetailsEps.Bank = bank //required
                PaymentMethodDetailsEps.VerifiedName = verifiedName //required
            }

    and PaymentMethodDetailsFpx = {
        ///Account holder type, if provided. Can be one of `individual` or `company`.
        AccountHolderType: PaymentMethodDetailsFpxAccountHolderType option
        ///The customer's bank. Can be one of `affin_bank`, `agrobank`, `alliance_bank`, `ambank`, `bank_islam`, `bank_muamalat`, `bank_rakyat`, `bsn`, `cimb`, `hong_leong_bank`, `hsbc`, `kfh`, `maybank2u`, `ocbc`, `public_bank`, `rhb`, `standard_chartered`, `uob`, `deutsche_bank`, `maybank2e`, `pb_enterprise`, or `bank_of_china`.
        Bank: PaymentMethodDetailsFpxBank
        ///Unique transaction id generated by FPX for every request from the merchant
        TransactionId: string option
    }
    with
        static member New (accountHolderType: PaymentMethodDetailsFpxAccountHolderType option, bank: PaymentMethodDetailsFpxBank, transactionId: string option) =
            {
                PaymentMethodDetailsFpx.AccountHolderType = accountHolderType //required
                PaymentMethodDetailsFpx.Bank = bank //required
                PaymentMethodDetailsFpx.TransactionId = transactionId //required
            }

    and PaymentMethodDetailsIdeal = {
        ///The customer's bank. Can be one of `abn_amro`, `adyen`, `asn_bank`, `bunq`, `buut`, `finom`, `handelsbanken`, `ing`, `knab`, `mollie`, `moneyou`, `n26`, `nn`, `rabobank`, `regiobank`, `revolut`, `sns_bank`, `triodos_bank`, `van_lanschot`, or `yoursafe`.
        Bank: PaymentMethodDetailsIdealBank option
        ///The Bank Identifier Code of the customer's bank.
        Bic: PaymentMethodDetailsIdealBic option
        ///The ID of the SEPA Direct Debit PaymentMethod which was generated by this Charge.
        GeneratedSepaDebit: PaymentMethodDetailsIdealGeneratedSepaDebit'AnyOf option
        ///The mandate for the SEPA Direct Debit PaymentMethod which was generated by this Charge.
        GeneratedSepaDebitMandate: PaymentMethodDetailsIdealGeneratedSepaDebitMandate'AnyOf option
        ///Last four characters of the IBAN.
        [<JsonField(Name="iban_last4")>]IbanLast4: string option
        ///Unique transaction ID generated by iDEAL.
        TransactionId: string option
        ///Owner's verified full name. Values are verified or provided by iDEAL directly
        ///(if supported) at the time of authorization or settlement. They cannot be set or mutated.
        VerifiedName: string option
    }
    with
        static member New (bank: PaymentMethodDetailsIdealBank option, bic: PaymentMethodDetailsIdealBic option, generatedSepaDebit: PaymentMethodDetailsIdealGeneratedSepaDebit'AnyOf option, generatedSepaDebitMandate: PaymentMethodDetailsIdealGeneratedSepaDebitMandate'AnyOf option, ibanLast4: string option, transactionId: string option, verifiedName: string option) =
            {
                PaymentMethodDetailsIdeal.Bank = bank //required
                PaymentMethodDetailsIdeal.Bic = bic //required
                PaymentMethodDetailsIdeal.GeneratedSepaDebit = generatedSepaDebit //required
                PaymentMethodDetailsIdeal.GeneratedSepaDebitMandate = generatedSepaDebitMandate //required
                PaymentMethodDetailsIdeal.IbanLast4 = ibanLast4 //required
                PaymentMethodDetailsIdeal.TransactionId = transactionId //required
                PaymentMethodDetailsIdeal.VerifiedName = verifiedName //required
            }

    and PaymentMethodDetailsIdealGeneratedSepaDebit'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and PaymentMethodDetailsIdealGeneratedSepaDebitMandate'AnyOf =
        | String of string
        | Mandate of Mandate

    and PaymentMethodDetailsInteracPresent = {
        ///Card brand. Can be `interac`, `mastercard` or `visa`.
        Brand: PaymentMethodDetailsInteracPresentBrand option
        ///The cardholder name as read from the card, in [ISO 7813](https://en.wikipedia.org/wiki/ISO/IEC_7813) format. May include alphanumeric characters, special characters and first/last name separator (`/`). In some cases, the cardholder name may not be available depending on how the issuer has configured the card. Cardholder name is typically not available on swipe or contactless payments, such as those made with Apple Pay and Google Pay.
        CardholderName: string option
        ///Two-letter ISO code representing the country of the card. You could use this attribute to get a sense of the international breakdown of cards you've collected.
        Country: IsoTypes.IsoCountryCode option
        ///A high-level description of the type of cards issued in this range. (For internal use only and not typically available in standard API requests.)
        Description: string option
        ///Authorization response cryptogram.
        EmvAuthData: string option
        ///Two-digit number representing the card's expiration month.
        ExpMonth: int
        ///Four-digit number representing the card's expiration year.
        ExpYear: int
        ///Uniquely identifies this particular card number. You can use this attribute to check whether two customers who’ve signed up with you are using the same card number, for example. For payment methods that tokenize card information (Apple Pay, Google Pay), the tokenized number might be provided instead of the underlying card number.
        ///*As of May 1, 2021, card fingerprint in India for Connect changed to allow two fingerprints for the same card---one for India and one for the rest of the world.*
        Fingerprint: string option
        ///Card funding type. Can be `credit`, `debit`, `prepaid`, or `unknown`.
        Funding: PaymentMethodDetailsInteracPresentFunding option
        ///ID of a card PaymentMethod generated from the card_present PaymentMethod that may be attached to a Customer for future transactions. Only present if it was possible to generate a card PaymentMethod.
        GeneratedCard: string option
        ///Issuer identification number of the card. (For internal use only and not typically available in standard API requests.)
        Iin: string option
        ///The name of the card's issuing bank. (For internal use only and not typically available in standard API requests.)
        Issuer: string option
        ///The last four digits of the card.
        [<JsonField(Name="last4")>]Last4: string option
        ///ID of the [location](https://docs.stripe.com/api/terminal/locations) that this transaction's reader is assigned to.
        Location: string option
        ///Identifies which network this charge was processed on. Can be `amex`, `cartes_bancaires`, `diners`, `discover`, `eftpos_au`, `interac`, `jcb`, `link`, `mastercard`, `unionpay`, `visa`, or `unknown`.
        Network: PaymentMethodDetailsInteracPresentNetwork option
        ///This is used by the financial networks to identify a transaction. Visa calls this the Transaction ID, Mastercard calls this the Trace ID, and American Express calls this the Acquirer Reference Data. This value will be present if it is returned by the financial network in the authorization response, and null otherwise.
        NetworkTransactionId: string option
        ///The languages that the issuing bank recommends using for localizing any customer-facing text, as read from the card. Referenced from EMV tag 5F2D, data encoded on the card's chip.
        PreferredLocales: string list option
        ///How card details were read in this transaction.
        ReadMethod: PaymentMethodDetailsInteracPresentReadMethod option
        ///ID of the [reader](https://docs.stripe.com/api/terminal/readers) this transaction was made on.
        Reader: string option
        ///A collection of fields required to be displayed on receipts. Only required for EMV transactions.
        Receipt: PaymentMethodDetailsInteracPresentReceipt option
    }
    with
        static member New (brand: PaymentMethodDetailsInteracPresentBrand option, cardholderName: string option, country: IsoTypes.IsoCountryCode option, emvAuthData: string option, expMonth: int, expYear: int, fingerprint: string option, funding: PaymentMethodDetailsInteracPresentFunding option, generatedCard: string option, last4: string option, network: PaymentMethodDetailsInteracPresentNetwork option, networkTransactionId: string option, preferredLocales: string list option, readMethod: PaymentMethodDetailsInteracPresentReadMethod option, receipt: PaymentMethodDetailsInteracPresentReceipt option, ?description: string option, ?iin: string option, ?issuer: string option, ?location: string, ?reader: string) =
            {
                PaymentMethodDetailsInteracPresent.Brand = brand //required
                PaymentMethodDetailsInteracPresent.CardholderName = cardholderName //required
                PaymentMethodDetailsInteracPresent.Country = country //required
                PaymentMethodDetailsInteracPresent.EmvAuthData = emvAuthData //required
                PaymentMethodDetailsInteracPresent.ExpMonth = expMonth //required
                PaymentMethodDetailsInteracPresent.ExpYear = expYear //required
                PaymentMethodDetailsInteracPresent.Fingerprint = fingerprint //required
                PaymentMethodDetailsInteracPresent.Funding = funding //required
                PaymentMethodDetailsInteracPresent.GeneratedCard = generatedCard //required
                PaymentMethodDetailsInteracPresent.Last4 = last4 //required
                PaymentMethodDetailsInteracPresent.Network = network //required
                PaymentMethodDetailsInteracPresent.NetworkTransactionId = networkTransactionId //required
                PaymentMethodDetailsInteracPresent.PreferredLocales = preferredLocales //required
                PaymentMethodDetailsInteracPresent.ReadMethod = readMethod //required
                PaymentMethodDetailsInteracPresent.Receipt = receipt //required
                PaymentMethodDetailsInteracPresent.Description = description |> Option.flatten
                PaymentMethodDetailsInteracPresent.Iin = iin |> Option.flatten
                PaymentMethodDetailsInteracPresent.Issuer = issuer |> Option.flatten
                PaymentMethodDetailsInteracPresent.Location = location
                PaymentMethodDetailsInteracPresent.Reader = reader
            }

    and PaymentMethodDetailsInteracPresentReceipt = {
        ///The type of account being debited or credited
        AccountType: PaymentMethodDetailsInteracPresentReceiptAccountType option
        ///The Application Cryptogram, a unique value generated by the card to authenticate the transaction with issuers.
        ApplicationCryptogram: string option
        ///The Application Identifier (AID) on the card used to determine which networks are eligible to process the transaction. Referenced from EMV tag 9F12, data encoded on the card's chip.
        ApplicationPreferredName: string option
        ///Identifier for this transaction.
        AuthorizationCode: string option
        ///EMV tag 8A. A code returned by the card issuer.
        AuthorizationResponseCode: string option
        ///Describes the method used by the cardholder to verify ownership of the card. One of the following: `approval`, `failure`, `none`, `offline_pin`, `offline_pin_and_signature`, `online_pin`, or `signature`.
        CardholderVerificationMethod: PaymentMethodDetailsInteracPresentReceiptCardholderVerificationMethod option
        ///Similar to the application_preferred_name, identifying the applications (AIDs) available on the card. Referenced from EMV tag 84.
        DedicatedFileName: string option
        ///A 5-byte string that records the checks and validations that occur between the card and the terminal. These checks determine how the terminal processes the transaction and what risk tolerance is acceptable. Referenced from EMV Tag 95.
        TerminalVerificationResults: string option
        ///An indication of which steps were completed during the card read process. Referenced from EMV Tag 9B.
        TransactionStatusInformation: string option
    }
    with
        static member New (applicationCryptogram: string option, applicationPreferredName: string option, authorizationCode: string option, authorizationResponseCode: string option, cardholderVerificationMethod: PaymentMethodDetailsInteracPresentReceiptCardholderVerificationMethod option, dedicatedFileName: string option, terminalVerificationResults: string option, transactionStatusInformation: string option, ?accountType: PaymentMethodDetailsInteracPresentReceiptAccountType) =
            {
                PaymentMethodDetailsInteracPresentReceipt.ApplicationCryptogram = applicationCryptogram //required
                PaymentMethodDetailsInteracPresentReceipt.ApplicationPreferredName = applicationPreferredName //required
                PaymentMethodDetailsInteracPresentReceipt.AuthorizationCode = authorizationCode //required
                PaymentMethodDetailsInteracPresentReceipt.AuthorizationResponseCode = authorizationResponseCode //required
                PaymentMethodDetailsInteracPresentReceipt.CardholderVerificationMethod = cardholderVerificationMethod //required
                PaymentMethodDetailsInteracPresentReceipt.DedicatedFileName = dedicatedFileName //required
                PaymentMethodDetailsInteracPresentReceipt.TerminalVerificationResults = terminalVerificationResults //required
                PaymentMethodDetailsInteracPresentReceipt.TransactionStatusInformation = transactionStatusInformation //required
                PaymentMethodDetailsInteracPresentReceipt.AccountType = accountType
            }

    and PaymentMethodDetailsKlarna = {
        ///ID of the [location](https://docs.stripe.com/api/terminal/locations) that this transaction's reader is assigned to.
        Location: string option
        ///The payer details for this transaction.
        PayerDetails: KlarnaPayerDetails option
        ///The Klarna payment method used for this transaction.
        ///Can be one of `pay_later`, `pay_now`, `pay_with_financing`, or `pay_in_installments`
        PaymentMethodCategory: string option
        ///Preferred language of the Klarna authorization page that the customer is redirected to.
        ///Can be one of `de-AT`, `en-AT`, `nl-BE`, `fr-BE`, `en-BE`, `de-DE`, `en-DE`, `da-DK`, `en-DK`, `es-ES`, `en-ES`, `fi-FI`, `sv-FI`, `en-FI`, `en-GB`, `en-IE`, `it-IT`, `en-IT`, `nl-NL`, `en-NL`, `nb-NO`, `en-NO`, `sv-SE`, `en-SE`, `en-US`, `es-US`, `fr-FR`, `en-FR`, `cs-CZ`, `en-CZ`, `ro-RO`, `en-RO`, `el-GR`, `en-GR`, `en-AU`, `en-NZ`, `en-CA`, `fr-CA`, `pl-PL`, `en-PL`, `pt-PT`, `en-PT`, `de-CH`, `fr-CH`, `it-CH`, or `en-CH`
        PreferredLocale: string option
        ///ID of the [reader](https://docs.stripe.com/api/terminal/readers) this transaction was made on.
        Reader: string option
    }
    with
        static member New (payerDetails: KlarnaPayerDetails option, paymentMethodCategory: string option, preferredLocale: string option, ?location: string, ?reader: string) =
            {
                PaymentMethodDetailsKlarna.PayerDetails = payerDetails //required
                PaymentMethodDetailsKlarna.PaymentMethodCategory = paymentMethodCategory //required
                PaymentMethodDetailsKlarna.PreferredLocale = preferredLocale //required
                PaymentMethodDetailsKlarna.Location = location
                PaymentMethodDetailsKlarna.Reader = reader
            }

    and PaymentMethodDetailsKonbini = {
        ///If the payment succeeded, this contains the details of the convenience store where the payment was completed.
        Store: PaymentMethodDetailsKonbiniStore option
    }
    with
        static member New (store: PaymentMethodDetailsKonbiniStore option) =
            {
                PaymentMethodDetailsKonbini.Store = store //required
            }

    and PaymentMethodDetailsKonbiniStore = {
        ///The name of the convenience store chain where the payment was completed.
        Chain: PaymentMethodDetailsKonbiniStoreChain option
    }
    with
        static member New (chain: PaymentMethodDetailsKonbiniStoreChain option) =
            {
                PaymentMethodDetailsKonbiniStore.Chain = chain //required
            }

    and PaymentMethodDetailsKrCard = {
        ///The local credit or debit card brand.
        Brand: PaymentMethodDetailsKrCardBrand option
        ///A unique identifier for the buyer as determined by the local payment processor.
        BuyerId: string option
        ///The last four digits of the card. This may not be present for American Express cards.
        [<JsonField(Name="last4")>]Last4: string option
        ///The Korean Card transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (brand: PaymentMethodDetailsKrCardBrand option, buyerId: string option, last4: string option, transactionId: string option) =
            {
                PaymentMethodDetailsKrCard.Brand = brand //required
                PaymentMethodDetailsKrCard.BuyerId = buyerId //required
                PaymentMethodDetailsKrCard.Last4 = last4 //required
                PaymentMethodDetailsKrCard.TransactionId = transactionId //required
            }

    and PaymentMethodDetailsLink = {
        ///Two-letter ISO code representing the funding source country beneath the Link payment.
        ///You could use this attribute to get a sense of international fees.
        Country: IsoTypes.IsoCountryCode option
    }
    with
        static member New (country: IsoTypes.IsoCountryCode option) =
            {
                PaymentMethodDetailsLink.Country = country //required
            }

    and PaymentMethodDetailsMobilepay = {
        ///Internal card details
        Card: InternalCard option
    }
    with
        static member New (card: InternalCard option) =
            {
                PaymentMethodDetailsMobilepay.Card = card //required
            }

    and PaymentMethodDetailsP24 = {
        ///The customer's bank. Can be one of `ing`, `citi_handlowy`, `tmobile_usbugi_bankowe`, `plus_bank`, `etransfer_pocztowy24`, `banki_spbdzielcze`, `bank_nowy_bfg_sa`, `getin_bank`, `velobank`, `blik`, `noble_pay`, `ideabank`, `envelobank`, `santander_przelew24`, `nest_przelew`, `mbank_mtransfer`, `inteligo`, `pbac_z_ipko`, `bnp_paribas`, `credit_agricole`, `toyota_bank`, `bank_pekao_sa`, `volkswagen_bank`, `bank_millennium`, `alior_bank`, or `boz`.
        Bank: PaymentMethodDetailsP24Bank option
        ///Unique reference for this Przelewy24 payment.
        Reference: string option
        ///Owner's verified full name. Values are verified or provided by Przelewy24 directly
        ///(if supported) at the time of authorization or settlement. They cannot be set or mutated.
        ///Przelewy24 rarely provides this information so the attribute is usually empty.
        VerifiedName: string option
    }
    with
        static member New (bank: PaymentMethodDetailsP24Bank option, reference: string option, verifiedName: string option) =
            {
                PaymentMethodDetailsP24.Bank = bank //required
                PaymentMethodDetailsP24.Reference = reference //required
                PaymentMethodDetailsP24.VerifiedName = verifiedName //required
            }

    and PaymentMethodDetailsPassthroughCard = {
        ///Card brand. Can be `amex`, `cartes_bancaires`, `diners`, `discover`, `eftpos_au`, `jcb`, `link`, `mastercard`, `unionpay`, `visa` or `unknown`.
        Brand: PaymentMethodDetailsPassthroughCardBrand option
        ///Two-letter ISO code representing the country of the card. You could use this attribute to get a sense of the international breakdown of cards you've collected.
        Country: IsoTypes.IsoCountryCode option
        ///Two-digit number representing the card's expiration month.
        ExpMonth: int option
        ///Four-digit number representing the card's expiration year.
        ExpYear: int option
        ///Card funding type. Can be `credit`, `debit`, `prepaid`, or `unknown`.
        Funding: PaymentMethodDetailsPassthroughCardFunding option
        ///The last four digits of the card.
        [<JsonField(Name="last4")>]Last4: string option
    }
    with
        static member New (brand: PaymentMethodDetailsPassthroughCardBrand option, country: IsoTypes.IsoCountryCode option, expMonth: int option, expYear: int option, funding: PaymentMethodDetailsPassthroughCardFunding option, last4: string option) =
            {
                PaymentMethodDetailsPassthroughCard.Brand = brand //required
                PaymentMethodDetailsPassthroughCard.Country = country //required
                PaymentMethodDetailsPassthroughCard.ExpMonth = expMonth //required
                PaymentMethodDetailsPassthroughCard.ExpYear = expYear //required
                PaymentMethodDetailsPassthroughCard.Funding = funding //required
                PaymentMethodDetailsPassthroughCard.Last4 = last4 //required
            }

    and PaymentMethodDetailsPaymentRecordAlma = {
        Installments: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAlmaDetailsResourceInstallments option
        ///The Alma transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (transactionId: string option, ?installments: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAlmaDetailsResourceInstallments) =
            {
                PaymentMethodDetailsPaymentRecordAlma.TransactionId = transactionId //required
                PaymentMethodDetailsPaymentRecordAlma.Installments = installments
            }

    and PaymentMethodDetailsPaymentRecordAmazonPay = {
        Funding: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFunding option
        ///The Amazon Pay transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (transactionId: string option, ?funding: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFunding) =
            {
                PaymentMethodDetailsPaymentRecordAmazonPay.TransactionId = transactionId //required
                PaymentMethodDetailsPaymentRecordAmazonPay.Funding = funding
            }

    and PaymentMethodDetailsPaymentRecordBancontact = {
        ///Bank code of bank associated with the bank account.
        BankCode: string option
        ///Name of the bank associated with the bank account.
        BankName: string option
        ///Bank Identifier Code of the bank associated with the bank account.
        Bic: string option
        ///The ID of the SEPA Direct Debit PaymentMethod which was generated by this Charge.
        GeneratedSepaDebit: PaymentMethodDetailsPaymentRecordBancontactGeneratedSepaDebit'AnyOf option
        ///The mandate for the SEPA Direct Debit PaymentMethod which was generated by this Charge.
        GeneratedSepaDebitMandate: PaymentMethodDetailsPaymentRecordBancontactGeneratedSepaDebitMandate'AnyOf option
        ///Last four characters of the IBAN.
        [<JsonField(Name="iban_last4")>]IbanLast4: string option
        ///Preferred language of the Bancontact authorization page that the customer is redirected to. Can be one of `en`, `de`, `fr`, or `nl`
        PreferredLanguage: PaymentMethodDetailsPaymentRecordBancontactPreferredLanguage option
        ///Owner's verified full name. Values are verified or provided by Bancontact directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        VerifiedName: string option
    }
    with
        static member New (bankCode: string option, bankName: string option, bic: string option, generatedSepaDebit: PaymentMethodDetailsPaymentRecordBancontactGeneratedSepaDebit'AnyOf option, generatedSepaDebitMandate: PaymentMethodDetailsPaymentRecordBancontactGeneratedSepaDebitMandate'AnyOf option, ibanLast4: string option, preferredLanguage: PaymentMethodDetailsPaymentRecordBancontactPreferredLanguage option, verifiedName: string option) =
            {
                PaymentMethodDetailsPaymentRecordBancontact.BankCode = bankCode //required
                PaymentMethodDetailsPaymentRecordBancontact.BankName = bankName //required
                PaymentMethodDetailsPaymentRecordBancontact.Bic = bic //required
                PaymentMethodDetailsPaymentRecordBancontact.GeneratedSepaDebit = generatedSepaDebit //required
                PaymentMethodDetailsPaymentRecordBancontact.GeneratedSepaDebitMandate = generatedSepaDebitMandate //required
                PaymentMethodDetailsPaymentRecordBancontact.IbanLast4 = ibanLast4 //required
                PaymentMethodDetailsPaymentRecordBancontact.PreferredLanguage = preferredLanguage //required
                PaymentMethodDetailsPaymentRecordBancontact.VerifiedName = verifiedName //required
            }

    and PaymentMethodDetailsPaymentRecordBancontactGeneratedSepaDebit'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and PaymentMethodDetailsPaymentRecordBancontactGeneratedSepaDebitMandate'AnyOf =
        | String of string
        | Mandate of Mandate

    and PaymentMethodDetailsPaymentRecordEps = {
        ///The customer's bank. Should be one of `arzte_und_apotheker_bank`, `austrian_anadi_bank_ag`, `bank_austria`, `bankhaus_carl_spangler`, `bankhaus_schelhammer_und_schattera_ag`, `bawag_psk_ag`, `bks_bank_ag`, `brull_kallmus_bank_ag`, `btv_vier_lander_bank`, `capital_bank_grawe_gruppe_ag`, `deutsche_bank_ag`, `dolomitenbank`, `easybank_ag`, `erste_bank_und_sparkassen`, `hypo_alpeadriabank_international_ag`, `hypo_noe_lb_fur_niederosterreich_u_wien`, `hypo_oberosterreich_salzburg_steiermark`, `hypo_tirol_bank_ag`, `hypo_vorarlberg_bank_ag`, `hypo_bank_burgenland_aktiengesellschaft`, `marchfelder_bank`, `oberbank_ag`, `raiffeisen_bankengruppe_osterreich`, `schoellerbank_ag`, `sparda_bank_wien`, `volksbank_gruppe`, `volkskreditbank_ag`, or `vr_bank_braunau`
        Bank: PaymentMethodDetailsPaymentRecordEpsBank option
        ///Owner's verified full name. Values are verified or provided by EPS directly (if supported) at the time of authorization or settlement. They cannot be set or mutated. EPS rarely provides this information so the attribute is usually empty.
        VerifiedName: string option
    }
    with
        static member New (bank: PaymentMethodDetailsPaymentRecordEpsBank option, verifiedName: string option) =
            {
                PaymentMethodDetailsPaymentRecordEps.Bank = bank //required
                PaymentMethodDetailsPaymentRecordEps.VerifiedName = verifiedName //required
            }

    and PaymentMethodDetailsPaymentRecordIdeal = {
        ///The customer's bank. Can be one of `abn_amro`, `adyen`, `asn_bank`, `bunq`, `buut`, `finom`, `handelsbanken`, `ing`, `knab`, `mollie`, `moneyou`, `n26`, `nn`, `rabobank`, `regiobank`, `revolut`, `sns_bank`, `triodos_bank`, `van_lanschot`, or `yoursafe`.
        Bank: PaymentMethodDetailsPaymentRecordIdealBank option
        ///The Bank Identifier Code of the customer's bank.
        Bic: PaymentMethodDetailsPaymentRecordIdealBic option
        ///The ID of the SEPA Direct Debit PaymentMethod which was generated by this Charge.
        GeneratedSepaDebit: PaymentMethodDetailsPaymentRecordIdealGeneratedSepaDebit'AnyOf option
        ///The mandate for the SEPA Direct Debit PaymentMethod which was generated by this Charge.
        GeneratedSepaDebitMandate: PaymentMethodDetailsPaymentRecordIdealGeneratedSepaDebitMandate'AnyOf option
        ///Last four characters of the IBAN.
        [<JsonField(Name="iban_last4")>]IbanLast4: string option
        ///Unique transaction ID generated by iDEAL.
        TransactionId: string option
        ///Owner's verified full name. Values are verified or provided by iDEAL directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        VerifiedName: string option
    }
    with
        static member New (bank: PaymentMethodDetailsPaymentRecordIdealBank option, bic: PaymentMethodDetailsPaymentRecordIdealBic option, generatedSepaDebit: PaymentMethodDetailsPaymentRecordIdealGeneratedSepaDebit'AnyOf option, generatedSepaDebitMandate: PaymentMethodDetailsPaymentRecordIdealGeneratedSepaDebitMandate'AnyOf option, ibanLast4: string option, transactionId: string option, verifiedName: string option) =
            {
                PaymentMethodDetailsPaymentRecordIdeal.Bank = bank //required
                PaymentMethodDetailsPaymentRecordIdeal.Bic = bic //required
                PaymentMethodDetailsPaymentRecordIdeal.GeneratedSepaDebit = generatedSepaDebit //required
                PaymentMethodDetailsPaymentRecordIdeal.GeneratedSepaDebitMandate = generatedSepaDebitMandate //required
                PaymentMethodDetailsPaymentRecordIdeal.IbanLast4 = ibanLast4 //required
                PaymentMethodDetailsPaymentRecordIdeal.TransactionId = transactionId //required
                PaymentMethodDetailsPaymentRecordIdeal.VerifiedName = verifiedName //required
            }

    and PaymentMethodDetailsPaymentRecordIdealGeneratedSepaDebit'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and PaymentMethodDetailsPaymentRecordIdealGeneratedSepaDebitMandate'AnyOf =
        | String of string
        | Mandate of Mandate

    and PaymentMethodDetailsPaymentRecordKonbini = {
        ///If the payment succeeded, this contains the details of the convenience store where the payment was completed.
        Store: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodKonbiniDetailsResourceStore option
    }
    with
        static member New (store: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodKonbiniDetailsResourceStore option) =
            {
                PaymentMethodDetailsPaymentRecordKonbini.Store = store //required
            }

    and PaymentMethodDetailsPaymentRecordMobilepay = {
        ///Internal card details
        Card: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodMobilepayDetailsResourceCard option
    }
    with
        static member New (card: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodMobilepayDetailsResourceCard option) =
            {
                PaymentMethodDetailsPaymentRecordMobilepay.Card = card //required
            }

    and PaymentMethodDetailsPaymentRecordSepaDebit = {
        ///Bank code of bank associated with the bank account.
        BankCode: string option
        ///Branch code of bank associated with the bank account.
        BranchCode: string option
        ///Two-letter ISO code representing the country the bank account is located in.
        Country: IsoTypes.IsoCountryCode option
        ///Estimated date to debit the customer's bank account. A date string in YYYY-MM-DD format.
        ExpectedDebitDate: string option
        ///Uniquely identifies this particular bank account. You can use this attribute to check whether two bank accounts are the same.
        Fingerprint: string option
        ///Last four characters of the IBAN.
        [<JsonField(Name="last4")>]Last4: string option
        ///Find the ID of the mandate used for this payment under the [payment_method_details.sepa_debit.mandate](https://docs.stripe.com/api/charges/object#charge_object-payment_method_details-sepa_debit-mandate) property on the Charge. Use this mandate ID to [retrieve the Mandate](https://docs.stripe.com/api/mandates/retrieve).
        Mandate: string option
    }
    with
        static member New (bankCode: string option, branchCode: string option, country: IsoTypes.IsoCountryCode option, fingerprint: string option, last4: string option, mandate: string option, ?expectedDebitDate: string) =
            {
                PaymentMethodDetailsPaymentRecordSepaDebit.BankCode = bankCode //required
                PaymentMethodDetailsPaymentRecordSepaDebit.BranchCode = branchCode //required
                PaymentMethodDetailsPaymentRecordSepaDebit.Country = country //required
                PaymentMethodDetailsPaymentRecordSepaDebit.Fingerprint = fingerprint //required
                PaymentMethodDetailsPaymentRecordSepaDebit.Last4 = last4 //required
                PaymentMethodDetailsPaymentRecordSepaDebit.Mandate = mandate //required
                PaymentMethodDetailsPaymentRecordSepaDebit.ExpectedDebitDate = expectedDebitDate
            }

    and PaymentMethodDetailsPaymentRecordSofort = {
        ///Bank code of bank associated with the bank account.
        BankCode: string option
        ///Name of the bank associated with the bank account.
        BankName: string option
        ///Bank Identifier Code of the bank associated with the bank account.
        Bic: string option
        ///Two-letter ISO code representing the country the bank account is located in.
        Country: IsoTypes.IsoCountryCode option
        ///The ID of the SEPA Direct Debit PaymentMethod which was generated by this Charge.
        GeneratedSepaDebit: PaymentMethodDetailsPaymentRecordSofortGeneratedSepaDebit'AnyOf option
        ///The mandate for the SEPA Direct Debit PaymentMethod which was generated by this Charge.
        GeneratedSepaDebitMandate: PaymentMethodDetailsPaymentRecordSofortGeneratedSepaDebitMandate'AnyOf option
        ///Last four characters of the IBAN.
        [<JsonField(Name="iban_last4")>]IbanLast4: string option
        ///Preferred language of the SOFORT authorization page that the customer is redirected to. Can be one of `de`, `en`, `es`, `fr`, `it`, `nl`, or `pl`
        PreferredLanguage: PaymentMethodDetailsPaymentRecordSofortPreferredLanguage option
        ///Owner's verified full name. Values are verified or provided by SOFORT directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        VerifiedName: string option
    }
    with
        static member New (bankCode: string option, bankName: string option, bic: string option, country: IsoTypes.IsoCountryCode option, generatedSepaDebit: PaymentMethodDetailsPaymentRecordSofortGeneratedSepaDebit'AnyOf option, generatedSepaDebitMandate: PaymentMethodDetailsPaymentRecordSofortGeneratedSepaDebitMandate'AnyOf option, ibanLast4: string option, preferredLanguage: PaymentMethodDetailsPaymentRecordSofortPreferredLanguage option, verifiedName: string option) =
            {
                PaymentMethodDetailsPaymentRecordSofort.BankCode = bankCode //required
                PaymentMethodDetailsPaymentRecordSofort.BankName = bankName //required
                PaymentMethodDetailsPaymentRecordSofort.Bic = bic //required
                PaymentMethodDetailsPaymentRecordSofort.Country = country //required
                PaymentMethodDetailsPaymentRecordSofort.GeneratedSepaDebit = generatedSepaDebit //required
                PaymentMethodDetailsPaymentRecordSofort.GeneratedSepaDebitMandate = generatedSepaDebitMandate //required
                PaymentMethodDetailsPaymentRecordSofort.IbanLast4 = ibanLast4 //required
                PaymentMethodDetailsPaymentRecordSofort.PreferredLanguage = preferredLanguage //required
                PaymentMethodDetailsPaymentRecordSofort.VerifiedName = verifiedName //required
            }

    and PaymentMethodDetailsPaymentRecordSofortGeneratedSepaDebit'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and PaymentMethodDetailsPaymentRecordSofortGeneratedSepaDebitMandate'AnyOf =
        | String of string
        | Mandate of Mandate

    and PaymentMethodDetailsPaymentRecordUsBankAccount = {
        ///The type of entity that holds the account. This can be either 'individual' or 'company'.
        AccountHolderType: PaymentMethodDetailsPaymentRecordUsBankAccountAccountHolderType option
        ///The type of the bank account. This can be either 'checking' or 'savings'.
        AccountType: PaymentMethodDetailsPaymentRecordUsBankAccountAccountType option
        ///Name of the bank associated with the bank account.
        BankName: string option
        ///Estimated date to debit the customer's bank account. A date string in YYYY-MM-DD format.
        ExpectedDebitDate: string option
        ///Uniquely identifies this particular bank account. You can use this attribute to check whether two bank accounts are the same.
        Fingerprint: string option
        ///Last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string option
        ///ID of the mandate used to make this payment.
        Mandate: PaymentMethodDetailsPaymentRecordUsBankAccountMandate'AnyOf option
        ///The ACH payment reference for this transaction.
        PaymentReference: string option
        ///The routing number for the bank account.
        RoutingNumber: string option
    }
    with
        static member New (accountHolderType: PaymentMethodDetailsPaymentRecordUsBankAccountAccountHolderType option, accountType: PaymentMethodDetailsPaymentRecordUsBankAccountAccountType option, bankName: string option, fingerprint: string option, last4: string option, paymentReference: string option, routingNumber: string option, ?expectedDebitDate: string, ?mandate: PaymentMethodDetailsPaymentRecordUsBankAccountMandate'AnyOf) =
            {
                PaymentMethodDetailsPaymentRecordUsBankAccount.AccountHolderType = accountHolderType //required
                PaymentMethodDetailsPaymentRecordUsBankAccount.AccountType = accountType //required
                PaymentMethodDetailsPaymentRecordUsBankAccount.BankName = bankName //required
                PaymentMethodDetailsPaymentRecordUsBankAccount.Fingerprint = fingerprint //required
                PaymentMethodDetailsPaymentRecordUsBankAccount.Last4 = last4 //required
                PaymentMethodDetailsPaymentRecordUsBankAccount.PaymentReference = paymentReference //required
                PaymentMethodDetailsPaymentRecordUsBankAccount.RoutingNumber = routingNumber //required
                PaymentMethodDetailsPaymentRecordUsBankAccount.ExpectedDebitDate = expectedDebitDate
                PaymentMethodDetailsPaymentRecordUsBankAccount.Mandate = mandate
            }

    and PaymentMethodDetailsPaymentRecordUsBankAccountMandate'AnyOf =
        | String of string
        | Mandate of Mandate

    and PaymentMethodDetailsPaypal = {
        ///Two-letter ISO code representing the buyer's country. Values are provided by PayPal directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        Country: IsoTypes.IsoCountryCode option
        ///Owner's email. Values are provided by PayPal directly
        ///(if supported) at the time of authorization or settlement. They cannot be set or mutated.
        PayerEmail: string option
        ///PayPal account PayerID. This identifier uniquely identifies the PayPal customer.
        PayerId: string option
        ///Owner's full name. Values provided by PayPal directly
        ///(if supported) at the time of authorization or settlement. They cannot be set or mutated.
        PayerName: string option
        ///The level of protection offered as defined by PayPal Seller Protection for Merchants, for this transaction.
        SellerProtection: PaypalSellerProtection option
        ///A unique ID generated by PayPal for this transaction.
        TransactionId: string option
    }
    with
        static member New (country: IsoTypes.IsoCountryCode option, payerEmail: string option, payerId: string option, payerName: string option, sellerProtection: PaypalSellerProtection option, transactionId: string option) =
            {
                PaymentMethodDetailsPaypal.Country = country //required
                PaymentMethodDetailsPaypal.PayerEmail = payerEmail //required
                PaymentMethodDetailsPaypal.PayerId = payerId //required
                PaymentMethodDetailsPaypal.PayerName = payerName //required
                PaymentMethodDetailsPaypal.SellerProtection = sellerProtection //required
                PaymentMethodDetailsPaypal.TransactionId = transactionId //required
            }

    and PaymentMethodDetailsRevolutPay = {
        Funding: RevolutPayUnderlyingPaymentMethodFundingDetails option
        ///The Revolut Pay transaction ID associated with this payment.
        TransactionId: string option
    }
    with
        static member New (transactionId: string option, ?funding: RevolutPayUnderlyingPaymentMethodFundingDetails) =
            {
                PaymentMethodDetailsRevolutPay.TransactionId = transactionId //required
                PaymentMethodDetailsRevolutPay.Funding = funding
            }

    and PaymentMethodDetailsSepaDebit = {
        ///Bank code of bank associated with the bank account.
        BankCode: string option
        ///Branch code of bank associated with the bank account.
        BranchCode: string option
        ///Two-letter ISO code representing the country the bank account is located in.
        Country: IsoTypes.IsoCountryCode option
        ///Estimated date to debit the customer's bank account. A date string in YYYY-MM-DD format.
        ExpectedDebitDate: string option
        ///Uniquely identifies this particular bank account. You can use this attribute to check whether two bank accounts are the same.
        Fingerprint: string option
        ///Last four characters of the IBAN.
        [<JsonField(Name="last4")>]Last4: string option
        ///Find the ID of the mandate used for this payment under the [payment_method_details.sepa_debit.mandate](https://docs.stripe.com/api/charges/object#charge_object-payment_method_details-sepa_debit-mandate) property on the Charge. Use this mandate ID to [retrieve the Mandate](https://docs.stripe.com/api/mandates/retrieve).
        Mandate: string option
    }
    with
        static member New (bankCode: string option, branchCode: string option, country: IsoTypes.IsoCountryCode option, fingerprint: string option, last4: string option, mandate: string option, ?expectedDebitDate: string) =
            {
                PaymentMethodDetailsSepaDebit.BankCode = bankCode //required
                PaymentMethodDetailsSepaDebit.BranchCode = branchCode //required
                PaymentMethodDetailsSepaDebit.Country = country //required
                PaymentMethodDetailsSepaDebit.Fingerprint = fingerprint //required
                PaymentMethodDetailsSepaDebit.Last4 = last4 //required
                PaymentMethodDetailsSepaDebit.Mandate = mandate //required
                PaymentMethodDetailsSepaDebit.ExpectedDebitDate = expectedDebitDate
            }

    and PaymentMethodDetailsSofort = {
        ///Bank code of bank associated with the bank account.
        BankCode: string option
        ///Name of the bank associated with the bank account.
        BankName: string option
        ///Bank Identifier Code of the bank associated with the bank account.
        Bic: string option
        ///Two-letter ISO code representing the country the bank account is located in.
        Country: IsoTypes.IsoCountryCode option
        ///The ID of the SEPA Direct Debit PaymentMethod which was generated by this Charge.
        GeneratedSepaDebit: PaymentMethodDetailsSofortGeneratedSepaDebit'AnyOf option
        ///The mandate for the SEPA Direct Debit PaymentMethod which was generated by this Charge.
        GeneratedSepaDebitMandate: PaymentMethodDetailsSofortGeneratedSepaDebitMandate'AnyOf option
        ///Last four characters of the IBAN.
        [<JsonField(Name="iban_last4")>]IbanLast4: string option
        ///Preferred language of the SOFORT authorization page that the customer is redirected to.
        ///Can be one of `de`, `en`, `es`, `fr`, `it`, `nl`, or `pl`
        PreferredLanguage: PaymentMethodDetailsSofortPreferredLanguage option
        ///Owner's verified full name. Values are verified or provided by SOFORT directly
        ///(if supported) at the time of authorization or settlement. They cannot be set or mutated.
        VerifiedName: string option
    }
    with
        static member New (bankCode: string option, bankName: string option, bic: string option, country: IsoTypes.IsoCountryCode option, generatedSepaDebit: PaymentMethodDetailsSofortGeneratedSepaDebit'AnyOf option, generatedSepaDebitMandate: PaymentMethodDetailsSofortGeneratedSepaDebitMandate'AnyOf option, ibanLast4: string option, preferredLanguage: PaymentMethodDetailsSofortPreferredLanguage option, verifiedName: string option) =
            {
                PaymentMethodDetailsSofort.BankCode = bankCode //required
                PaymentMethodDetailsSofort.BankName = bankName //required
                PaymentMethodDetailsSofort.Bic = bic //required
                PaymentMethodDetailsSofort.Country = country //required
                PaymentMethodDetailsSofort.GeneratedSepaDebit = generatedSepaDebit //required
                PaymentMethodDetailsSofort.GeneratedSepaDebitMandate = generatedSepaDebitMandate //required
                PaymentMethodDetailsSofort.IbanLast4 = ibanLast4 //required
                PaymentMethodDetailsSofort.PreferredLanguage = preferredLanguage //required
                PaymentMethodDetailsSofort.VerifiedName = verifiedName //required
            }

    and PaymentMethodDetailsSofortGeneratedSepaDebit'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and PaymentMethodDetailsSofortGeneratedSepaDebitMandate'AnyOf =
        | String of string
        | Mandate of Mandate

    and PaymentMethodDetailsUsBankAccount = {
        ///Account holder type: individual or company.
        AccountHolderType: PaymentMethodDetailsUsBankAccountAccountHolderType option
        ///Account type: checkings or savings. Defaults to checking if omitted.
        AccountType: PaymentMethodDetailsUsBankAccountAccountType option
        ///Name of the bank associated with the bank account.
        BankName: string option
        ///Estimated date to debit the customer's bank account. A date string in YYYY-MM-DD format.
        ExpectedDebitDate: string option
        ///Uniquely identifies this particular bank account. You can use this attribute to check whether two bank accounts are the same.
        Fingerprint: string option
        ///Last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string option
        ///ID of the mandate used to make this payment.
        Mandate: PaymentMethodDetailsUsBankAccountMandate'AnyOf option
        ///Reference number to locate ACH payments with customer's bank.
        PaymentReference: string option
        ///Routing number of the bank account.
        RoutingNumber: string option
    }
    with
        static member New (accountHolderType: PaymentMethodDetailsUsBankAccountAccountHolderType option, accountType: PaymentMethodDetailsUsBankAccountAccountType option, bankName: string option, fingerprint: string option, last4: string option, paymentReference: string option, routingNumber: string option, ?expectedDebitDate: string, ?mandate: PaymentMethodDetailsUsBankAccountMandate'AnyOf) =
            {
                PaymentMethodDetailsUsBankAccount.AccountHolderType = accountHolderType //required
                PaymentMethodDetailsUsBankAccount.AccountType = accountType //required
                PaymentMethodDetailsUsBankAccount.BankName = bankName //required
                PaymentMethodDetailsUsBankAccount.Fingerprint = fingerprint //required
                PaymentMethodDetailsUsBankAccount.Last4 = last4 //required
                PaymentMethodDetailsUsBankAccount.PaymentReference = paymentReference //required
                PaymentMethodDetailsUsBankAccount.RoutingNumber = routingNumber //required
                PaymentMethodDetailsUsBankAccount.ExpectedDebitDate = expectedDebitDate
                PaymentMethodDetailsUsBankAccount.Mandate = mandate
            }

    and PaymentMethodDetailsUsBankAccountMandate'AnyOf =
        | String of string
        | Mandate of Mandate

    ///A payment method domain represents a web domain that you have registered with Stripe.
    ///Stripe Elements use registered payment method domains to control where certain payment methods are shown.
    ///Related guide: [Payment method domains](https://docs.stripe.com/payments/payment-methods/pmd-registration).
    and PaymentMethodDomain = {
        AmazonPay: PaymentMethodDomainResourcePaymentMethodStatus
        ApplePay: PaymentMethodDomainResourcePaymentMethodStatus
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The domain name that this payment method domain object represents.
        DomainName: string
        ///Whether this payment method domain is enabled. If the domain is not enabled, payment methods that require a payment method domain will not appear in Elements.
        Enabled: bool
        GooglePay: PaymentMethodDomainResourcePaymentMethodStatus
        ///Unique identifier for the object.
        Id: string
        Klarna: PaymentMethodDomainResourcePaymentMethodStatus
        Link: PaymentMethodDomainResourcePaymentMethodStatus
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        Paypal: PaymentMethodDomainResourcePaymentMethodStatus
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "payment_method_domain"

        static member New (amazonPay: PaymentMethodDomainResourcePaymentMethodStatus, applePay: PaymentMethodDomainResourcePaymentMethodStatus, created: DateTime, domainName: string, enabled: bool, googlePay: PaymentMethodDomainResourcePaymentMethodStatus, id: string, klarna: PaymentMethodDomainResourcePaymentMethodStatus, link: PaymentMethodDomainResourcePaymentMethodStatus, livemode: bool, paypal: PaymentMethodDomainResourcePaymentMethodStatus) =
            {
                PaymentMethodDomain.AmazonPay = amazonPay //required
                PaymentMethodDomain.ApplePay = applePay //required
                PaymentMethodDomain.Created = created //required
                PaymentMethodDomain.DomainName = domainName //required
                PaymentMethodDomain.Enabled = enabled //required
                PaymentMethodDomain.GooglePay = googlePay //required
                PaymentMethodDomain.Id = id //required
                PaymentMethodDomain.Klarna = klarna //required
                PaymentMethodDomain.Link = link //required
                PaymentMethodDomain.Livemode = livemode //required
                PaymentMethodDomain.Paypal = paypal //required
            }

    ///Indicates the status of a specific payment method on a payment method domain.
    and PaymentMethodDomainResourcePaymentMethodStatus = {
        ///The status of the payment method on the domain.
        Status: PaymentMethodDomainResourcePaymentMethodStatusStatus
        StatusDetails: PaymentMethodDomainResourcePaymentMethodStatusDetails option
    }
    with
        static member New (status: PaymentMethodDomainResourcePaymentMethodStatusStatus, ?statusDetails: PaymentMethodDomainResourcePaymentMethodStatusDetails) =
            {
                PaymentMethodDomainResourcePaymentMethodStatus.Status = status //required
                PaymentMethodDomainResourcePaymentMethodStatus.StatusDetails = statusDetails
            }

    and PaymentMethodEps = {
        ///The customer's bank. Should be one of `arzte_und_apotheker_bank`, `austrian_anadi_bank_ag`, `bank_austria`, `bankhaus_carl_spangler`, `bankhaus_schelhammer_und_schattera_ag`, `bawag_psk_ag`, `bks_bank_ag`, `brull_kallmus_bank_ag`, `btv_vier_lander_bank`, `capital_bank_grawe_gruppe_ag`, `deutsche_bank_ag`, `dolomitenbank`, `easybank_ag`, `erste_bank_und_sparkassen`, `hypo_alpeadriabank_international_ag`, `hypo_noe_lb_fur_niederosterreich_u_wien`, `hypo_oberosterreich_salzburg_steiermark`, `hypo_tirol_bank_ag`, `hypo_vorarlberg_bank_ag`, `hypo_bank_burgenland_aktiengesellschaft`, `marchfelder_bank`, `oberbank_ag`, `raiffeisen_bankengruppe_osterreich`, `schoellerbank_ag`, `sparda_bank_wien`, `volksbank_gruppe`, `volkskreditbank_ag`, or `vr_bank_braunau`.
        Bank: PaymentMethodEpsBank option
    }
    with
        static member New (bank: PaymentMethodEpsBank option) =
            {
                PaymentMethodEps.Bank = bank //required
            }

    and PaymentMethodFpx = {
        ///Account holder type, if provided. Can be one of `individual` or `company`.
        AccountHolderType: PaymentMethodFpxAccountHolderType option
        ///The customer's bank, if provided. Can be one of `affin_bank`, `agrobank`, `alliance_bank`, `ambank`, `bank_islam`, `bank_muamalat`, `bank_rakyat`, `bsn`, `cimb`, `hong_leong_bank`, `hsbc`, `kfh`, `maybank2u`, `ocbc`, `public_bank`, `rhb`, `standard_chartered`, `uob`, `deutsche_bank`, `maybank2e`, `pb_enterprise`, or `bank_of_china`.
        Bank: PaymentMethodFpxBank
    }
    with
        static member New (accountHolderType: PaymentMethodFpxAccountHolderType option, bank: PaymentMethodFpxBank) =
            {
                PaymentMethodFpx.AccountHolderType = accountHolderType //required
                PaymentMethodFpx.Bank = bank //required
            }

    and PaymentMethodIdeal = {
        ///The customer's bank, if provided. Can be one of `abn_amro`, `adyen`, `asn_bank`, `bunq`, `buut`, `finom`, `handelsbanken`, `ing`, `knab`, `mollie`, `moneyou`, `n26`, `nn`, `rabobank`, `regiobank`, `revolut`, `sns_bank`, `triodos_bank`, `van_lanschot`, or `yoursafe`.
        Bank: PaymentMethodIdealBank option
        ///The Bank Identifier Code of the customer's bank, if the bank was provided.
        Bic: PaymentMethodIdealBic option
    }
    with
        static member New (bank: PaymentMethodIdealBank option, bic: PaymentMethodIdealBic option) =
            {
                PaymentMethodIdeal.Bank = bank //required
                PaymentMethodIdeal.Bic = bic //required
            }

    and PaymentMethodInteracPresent = {
        ///Card brand. Can be `interac`, `mastercard` or `visa`.
        Brand: PaymentMethodInteracPresentBrand option
        ///The cardholder name as read from the card, in [ISO 7813](https://en.wikipedia.org/wiki/ISO/IEC_7813) format. May include alphanumeric characters, special characters and first/last name separator (`/`). In some cases, the cardholder name may not be available depending on how the issuer has configured the card. Cardholder name is typically not available on swipe or contactless payments, such as those made with Apple Pay and Google Pay.
        CardholderName: string option
        ///Two-letter ISO code representing the country of the card. You could use this attribute to get a sense of the international breakdown of cards you've collected.
        Country: IsoTypes.IsoCountryCode option
        ///A high-level description of the type of cards issued in this range. (For internal use only and not typically available in standard API requests.)
        Description: string option
        ///Two-digit number representing the card's expiration month.
        ExpMonth: int
        ///Four-digit number representing the card's expiration year.
        ExpYear: int
        ///Uniquely identifies this particular card number. You can use this attribute to check whether two customers who’ve signed up with you are using the same card number, for example. For payment methods that tokenize card information (Apple Pay, Google Pay), the tokenized number might be provided instead of the underlying card number.
        ///*As of May 1, 2021, card fingerprint in India for Connect changed to allow two fingerprints for the same card---one for India and one for the rest of the world.*
        Fingerprint: string option
        ///Card funding type. Can be `credit`, `debit`, `prepaid`, or `unknown`.
        Funding: PaymentMethodInteracPresentFunding option
        ///Issuer identification number of the card. (For internal use only and not typically available in standard API requests.)
        Iin: string option
        ///The name of the card's issuing bank. (For internal use only and not typically available in standard API requests.)
        Issuer: string option
        ///The last four digits of the card.
        [<JsonField(Name="last4")>]Last4: string option
        ///Contains information about card networks that can be used to process the payment.
        Networks: PaymentMethodCardPresentNetworks option
        ///The languages that the issuing bank recommends using for localizing any customer-facing text, as read from the card. Referenced from EMV tag 5F2D, data encoded on the card's chip.
        PreferredLocales: string list option
        ///How card details were read in this transaction.
        ReadMethod: PaymentMethodInteracPresentReadMethod option
    }
    with
        static member New (brand: PaymentMethodInteracPresentBrand option, cardholderName: string option, country: IsoTypes.IsoCountryCode option, expMonth: int, expYear: int, fingerprint: string option, funding: PaymentMethodInteracPresentFunding option, last4: string option, networks: PaymentMethodCardPresentNetworks option, preferredLocales: string list option, readMethod: PaymentMethodInteracPresentReadMethod option, ?description: string option, ?iin: string option, ?issuer: string option) =
            {
                PaymentMethodInteracPresent.Brand = brand //required
                PaymentMethodInteracPresent.CardholderName = cardholderName //required
                PaymentMethodInteracPresent.Country = country //required
                PaymentMethodInteracPresent.ExpMonth = expMonth //required
                PaymentMethodInteracPresent.ExpYear = expYear //required
                PaymentMethodInteracPresent.Fingerprint = fingerprint //required
                PaymentMethodInteracPresent.Funding = funding //required
                PaymentMethodInteracPresent.Last4 = last4 //required
                PaymentMethodInteracPresent.Networks = networks //required
                PaymentMethodInteracPresent.PreferredLocales = preferredLocales //required
                PaymentMethodInteracPresent.ReadMethod = readMethod //required
                PaymentMethodInteracPresent.Description = description |> Option.flatten
                PaymentMethodInteracPresent.Iin = iin |> Option.flatten
                PaymentMethodInteracPresent.Issuer = issuer |> Option.flatten
            }

    and PaymentMethodKlarna = {
        ///The customer's date of birth, if provided.
        Dob: PaymentFlowsPrivatePaymentMethodsKlarnaDob option
    }
    with
        static member New (?dob: PaymentFlowsPrivatePaymentMethodsKlarnaDob option) =
            {
                PaymentMethodKlarna.Dob = dob |> Option.flatten
            }

    and PaymentMethodKrCard = {
        ///The local credit or debit card brand.
        Brand: PaymentMethodKrCardBrand option
        ///The last four digits of the card. This may not be present for American Express cards.
        [<JsonField(Name="last4")>]Last4: string option
    }
    with
        static member New (brand: PaymentMethodKrCardBrand option, last4: string option) =
            {
                PaymentMethodKrCard.Brand = brand //required
                PaymentMethodKrCard.Last4 = last4 //required
            }

    and PaymentMethodNaverPay = {
        ///Uniquely identifies this particular Naver Pay account. You can use this attribute to check whether two Naver Pay accounts are the same.
        BuyerId: string option
        ///Whether to fund this transaction with Naver Pay points or a card.
        Funding: PaymentMethodNaverPayFunding
    }
    with
        static member New (buyerId: string option, funding: PaymentMethodNaverPayFunding) =
            {
                PaymentMethodNaverPay.BuyerId = buyerId //required
                PaymentMethodNaverPay.Funding = funding //required
            }

    and PaymentMethodOptionsAlipay = {
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentMethodOptionsAlipaySetupFutureUsage option
    }
    with
        static member New (?setupFutureUsage: PaymentMethodOptionsAlipaySetupFutureUsage) =
            {
                PaymentMethodOptionsAlipay.SetupFutureUsage = setupFutureUsage
            }

    and PaymentMethodOptionsAmazonPay = {
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentMethodOptionsAmazonPaySetupFutureUsage option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (?setupFutureUsage: PaymentMethodOptionsAmazonPaySetupFutureUsage) =
            {
                PaymentMethodOptionsAmazonPay.SetupFutureUsage = setupFutureUsage
            }

    and PaymentMethodOptionsBancontact = {
        ///Preferred language of the Bancontact authorization page that the customer is redirected to.
        PreferredLanguage: PaymentMethodOptionsBancontactPreferredLanguage
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentMethodOptionsBancontactSetupFutureUsage option
    }
    with
        static member New (preferredLanguage: PaymentMethodOptionsBancontactPreferredLanguage, ?setupFutureUsage: PaymentMethodOptionsBancontactSetupFutureUsage) =
            {
                PaymentMethodOptionsBancontact.PreferredLanguage = preferredLanguage //required
                PaymentMethodOptionsBancontact.SetupFutureUsage = setupFutureUsage
            }

    and PaymentMethodOptionsBoleto = {
        ///The number of calendar days before a Boleto voucher expires. For example, if you create a Boleto voucher on Monday and you set expires_after_days to 2, the Boleto voucher will expire on Wednesday at 23:59 America/Sao_Paulo time.
        ExpiresAfterDays: int
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentMethodOptionsBoletoSetupFutureUsage option
    }
    with
        static member New (expiresAfterDays: int, ?setupFutureUsage: PaymentMethodOptionsBoletoSetupFutureUsage) =
            {
                PaymentMethodOptionsBoleto.ExpiresAfterDays = expiresAfterDays //required
                PaymentMethodOptionsBoleto.SetupFutureUsage = setupFutureUsage
            }

    and PaymentMethodOptionsCardInstallments = {
        ///Installment plans that may be selected for this PaymentIntent.
        AvailablePlans: PaymentMethodDetailsCardInstallmentsPlan list option
        ///Whether Installments are enabled for this PaymentIntent.
        Enabled: bool
        ///Installment plan selected for this PaymentIntent.
        Plan: PaymentMethodDetailsCardInstallmentsPlan option
    }
    with
        static member New (availablePlans: PaymentMethodDetailsCardInstallmentsPlan list option, enabled: bool, plan: PaymentMethodDetailsCardInstallmentsPlan option) =
            {
                PaymentMethodOptionsCardInstallments.AvailablePlans = availablePlans //required
                PaymentMethodOptionsCardInstallments.Enabled = enabled //required
                PaymentMethodOptionsCardInstallments.Plan = plan //required
            }

    and PaymentMethodOptionsCardMandateOptions = {
        ///Amount to be charged for future payments, specified in the presentment currency.
        Amount: int
        ///One of `fixed` or `maximum`. If `fixed`, the `amount` param refers to the exact amount to be charged in future payments. If `maximum`, the amount charged can be up to the value passed for the `amount` param.
        AmountType: PaymentMethodOptionsCardMandateOptionsAmountType
        ///A description of the mandate or subscription that is meant to be displayed to the customer.
        Description: string option
        ///End date of the mandate or subscription. If not provided, the mandate will be active until canceled. If provided, end date should be after start date.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]EndDate: DateTime option
        ///Specifies payment frequency. One of `day`, `week`, `month`, `year`, or `sporadic`.
        Interval: PaymentMethodOptionsCardMandateOptionsInterval
        ///The number of intervals between payments. For example, `interval=month` and `interval_count=3` indicates one payment every three months. Maximum of one year interval allowed (1 year, 12 months, or 52 weeks). This parameter is optional when `interval=sporadic`.
        IntervalCount: int option
        ///Unique identifier for the mandate or subscription.
        Reference: string
        ///Start date of the mandate or subscription. Start date should not be lesser than yesterday.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]StartDate: DateTime
        ///Specifies the type of mandates supported. Possible values are `india`.
        SupportedTypes: string list option
    }
    with
        static member New (amount: int, amountType: PaymentMethodOptionsCardMandateOptionsAmountType, description: string option, endDate: DateTime option, interval: PaymentMethodOptionsCardMandateOptionsInterval, intervalCount: int option, reference: string, startDate: DateTime, supportedTypes: string list option) =
            {
                PaymentMethodOptionsCardMandateOptions.Amount = amount //required
                PaymentMethodOptionsCardMandateOptions.AmountType = amountType //required
                PaymentMethodOptionsCardMandateOptions.Description = description //required
                PaymentMethodOptionsCardMandateOptions.EndDate = endDate //required
                PaymentMethodOptionsCardMandateOptions.Interval = interval //required
                PaymentMethodOptionsCardMandateOptions.IntervalCount = intervalCount //required
                PaymentMethodOptionsCardMandateOptions.Reference = reference //required
                PaymentMethodOptionsCardMandateOptions.StartDate = startDate //required
                PaymentMethodOptionsCardMandateOptions.SupportedTypes = supportedTypes //required
            }

    and PaymentMethodOptionsCardPresent = {
        ///Controls when the funds will be captured from the customer's account.
        CaptureMethod: PaymentMethodOptionsCardPresentCaptureMethod option
        ///Request ability to capture this payment beyond the standard [authorization validity window](https://docs.stripe.com/terminal/features/extended-authorizations#authorization-validity)
        RequestExtendedAuthorization: bool option
        ///Request ability to [increment](https://docs.stripe.com/terminal/features/incremental-authorizations) this PaymentIntent if the combination of MCC and card brand is eligible. Check [incremental_authorization_supported](https://docs.stripe.com/api/charges/object#charge_object-payment_method_details-card_present-incremental_authorization_supported) in the [Confirm](https://docs.stripe.com/api/payment_intents/confirm) response to verify support.
        RequestIncrementalAuthorizationSupport: bool option
        Routing: PaymentMethodOptionsCardPresentRouting option
    }
    with
        static member New (requestExtendedAuthorization: bool option, requestIncrementalAuthorizationSupport: bool option, ?captureMethod: PaymentMethodOptionsCardPresentCaptureMethod, ?routing: PaymentMethodOptionsCardPresentRouting) =
            {
                PaymentMethodOptionsCardPresent.RequestExtendedAuthorization = requestExtendedAuthorization //required
                PaymentMethodOptionsCardPresent.RequestIncrementalAuthorizationSupport = requestIncrementalAuthorizationSupport //required
                PaymentMethodOptionsCardPresent.CaptureMethod = captureMethod
                PaymentMethodOptionsCardPresent.Routing = routing
            }

    and PaymentMethodOptionsCardPresentRouting = {
        ///Requested routing priority
        RequestedPriority: PaymentMethodOptionsCardPresentRoutingRequestedPriority option
    }
    with
        static member New (requestedPriority: PaymentMethodOptionsCardPresentRoutingRequestedPriority option) =
            {
                PaymentMethodOptionsCardPresentRouting.RequestedPriority = requestedPriority //required
            }

    and PaymentMethodOptionsCashapp = {
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentMethodOptionsCashappSetupFutureUsage option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (?setupFutureUsage: PaymentMethodOptionsCashappSetupFutureUsage) =
            {
                PaymentMethodOptionsCashapp.SetupFutureUsage = setupFutureUsage
            }

    and PaymentMethodOptionsCustomerBalance = {
        BankTransfer: PaymentMethodOptionsCustomerBalanceBankTransfer option
    }
    with
        ///The funding method type to be used when there are not enough funds in the customer balance. Permitted values include: `bank_transfer`.
        member _.FundingType = "bank_transfer"
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"

        static member New (?bankTransfer: PaymentMethodOptionsCustomerBalanceBankTransfer) =
            {
                PaymentMethodOptionsCustomerBalance.BankTransfer = bankTransfer
            }

    and PaymentMethodOptionsCustomerBalanceBankTransfer = {
        EuBankTransfer: PaymentMethodOptionsCustomerBalanceEuBankAccount option
        ///List of address types that should be returned in the financial_addresses response. If not specified, all valid types will be returned.
        ///Permitted values include: `sort_code`, `zengin`, `iban`, or `spei`.
        RequestedAddressTypes: PaymentMethodOptionsCustomerBalanceBankTransferRequestedAddressTypes list option
        ///The bank transfer type that this PaymentIntent is allowed to use for funding Permitted values include: `eu_bank_transfer`, `gb_bank_transfer`, `jp_bank_transfer`, `mx_bank_transfer`, or `us_bank_transfer`.
        Type: PaymentMethodOptionsCustomerBalanceBankTransferType option
    }
    with
        static member New (``type``: PaymentMethodOptionsCustomerBalanceBankTransferType option, ?euBankTransfer: PaymentMethodOptionsCustomerBalanceEuBankAccount, ?requestedAddressTypes: PaymentMethodOptionsCustomerBalanceBankTransferRequestedAddressTypes list) =
            {
                PaymentMethodOptionsCustomerBalanceBankTransfer.Type = ``type`` //required
                PaymentMethodOptionsCustomerBalanceBankTransfer.EuBankTransfer = euBankTransfer
                PaymentMethodOptionsCustomerBalanceBankTransfer.RequestedAddressTypes = requestedAddressTypes
            }

    and PaymentMethodOptionsCustomerBalanceEuBankAccount = {
        ///The desired country code of the bank account information. Permitted values include: `DE`, `FR`, `IE`, or `NL`.
        Country: PaymentMethodOptionsCustomerBalanceEuBankAccountCountry
    }
    with
        static member New (country: PaymentMethodOptionsCustomerBalanceEuBankAccountCountry) =
            {
                PaymentMethodOptionsCustomerBalanceEuBankAccount.Country = country //required
            }

    and PaymentMethodOptionsIdeal = {
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentMethodOptionsIdealSetupFutureUsage option
    }
    with
        static member New (?setupFutureUsage: PaymentMethodOptionsIdealSetupFutureUsage) =
            {
                PaymentMethodOptionsIdeal.SetupFutureUsage = setupFutureUsage
            }

    and PaymentMethodOptionsKlarna = {
        ///Preferred locale of the Klarna checkout page that the customer is redirected to.
        PreferredLocale: string option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentMethodOptionsKlarnaSetupFutureUsage option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (preferredLocale: string option, ?setupFutureUsage: PaymentMethodOptionsKlarnaSetupFutureUsage) =
            {
                PaymentMethodOptionsKlarna.PreferredLocale = preferredLocale //required
                PaymentMethodOptionsKlarna.SetupFutureUsage = setupFutureUsage
            }

    and PaymentMethodOptionsKrCard = {
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentMethodOptionsKrCardSetupFutureUsage option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (?setupFutureUsage: PaymentMethodOptionsKrCardSetupFutureUsage) =
            {
                PaymentMethodOptionsKrCard.SetupFutureUsage = setupFutureUsage
            }

    and PaymentMethodOptionsMandateOptionsPix = {
        ///Amount to be charged for future payments.
        Amount: int option
        ///Determines if the amount includes the IOF tax.
        AmountIncludesIof: PaymentMethodOptionsMandateOptionsPixAmountIncludesIof option
        ///Type of amount.
        AmountType: PaymentMethodOptionsMandateOptionsPixAmountType option
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
        Currency: IsoTypes.IsoCurrencyCode option
        ///Date when the mandate expires and no further payments will be charged, in `YYYY-MM-DD`.
        EndDate: string option
        ///Schedule at which the future payments will be charged.
        PaymentSchedule: PaymentMethodOptionsMandateOptionsPixPaymentSchedule option
        ///Subscription name displayed to buyers in their bank app.
        Reference: string option
        ///Start date of the mandate, in `YYYY-MM-DD`.
        StartDate: string option
    }
    with
        static member New (?amount: int, ?amountIncludesIof: PaymentMethodOptionsMandateOptionsPixAmountIncludesIof, ?amountType: PaymentMethodOptionsMandateOptionsPixAmountType, ?currency: IsoTypes.IsoCurrencyCode, ?endDate: string, ?paymentSchedule: PaymentMethodOptionsMandateOptionsPixPaymentSchedule, ?reference: string, ?startDate: string) =
            {
                PaymentMethodOptionsMandateOptionsPix.Amount = amount
                PaymentMethodOptionsMandateOptionsPix.AmountIncludesIof = amountIncludesIof
                PaymentMethodOptionsMandateOptionsPix.AmountType = amountType
                PaymentMethodOptionsMandateOptionsPix.Currency = currency
                PaymentMethodOptionsMandateOptionsPix.EndDate = endDate
                PaymentMethodOptionsMandateOptionsPix.PaymentSchedule = paymentSchedule
                PaymentMethodOptionsMandateOptionsPix.Reference = reference
                PaymentMethodOptionsMandateOptionsPix.StartDate = startDate
            }

    and PaymentMethodOptionsMandateOptionsUpi = {
        ///Amount to be charged for future payments.
        Amount: int option
        ///One of `fixed` or `maximum`. If `fixed`, the `amount` param refers to the exact amount to be charged in future payments. If `maximum`, the amount charged can be up to the value passed for the `amount` param.
        AmountType: PaymentMethodOptionsMandateOptionsUpiAmountType option
        ///A description of the mandate or subscription that is meant to be displayed to the customer.
        Description: string option
        ///End date of the mandate or subscription.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]EndDate: DateTime option
    }
    with
        static member New (amount: int option, amountType: PaymentMethodOptionsMandateOptionsUpiAmountType option, description: string option, endDate: DateTime option) =
            {
                PaymentMethodOptionsMandateOptionsUpi.Amount = amount //required
                PaymentMethodOptionsMandateOptionsUpi.AmountType = amountType //required
                PaymentMethodOptionsMandateOptionsUpi.Description = description //required
                PaymentMethodOptionsMandateOptionsUpi.EndDate = endDate //required
            }

    and PaymentMethodOptionsPaypal = {
        ///Preferred locale of the PayPal checkout page that the customer is redirected to.
        PreferredLocale: string option
        ///A reference of the PayPal transaction visible to customer which is mapped to PayPal's invoice ID. This must be a globally unique ID if you have configured in your PayPal settings to block multiple payments per invoice ID.
        Reference: string option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentMethodOptionsPaypalSetupFutureUsage option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (preferredLocale: string option, reference: string option, ?setupFutureUsage: PaymentMethodOptionsPaypalSetupFutureUsage) =
            {
                PaymentMethodOptionsPaypal.PreferredLocale = preferredLocale //required
                PaymentMethodOptionsPaypal.Reference = reference //required
                PaymentMethodOptionsPaypal.SetupFutureUsage = setupFutureUsage
            }

    and PaymentMethodOptionsPix = {
        ///Determines if the amount includes the IOF tax.
        AmountIncludesIof: PaymentMethodOptionsPixAmountIncludesIof option
        ///The number of seconds (between 10 and 1209600) after which Pix payment will expire.
        ExpiresAfterSeconds: int option
        ///The timestamp at which the Pix expires.
        ExpiresAt: int option
        MandateOptions: PaymentMethodOptionsMandateOptionsPix option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentMethodOptionsPixSetupFutureUsage option
    }
    with
        static member New (expiresAfterSeconds: int option, expiresAt: int option, ?amountIncludesIof: PaymentMethodOptionsPixAmountIncludesIof, ?mandateOptions: PaymentMethodOptionsMandateOptionsPix, ?setupFutureUsage: PaymentMethodOptionsPixSetupFutureUsage) =
            {
                PaymentMethodOptionsPix.ExpiresAfterSeconds = expiresAfterSeconds //required
                PaymentMethodOptionsPix.ExpiresAt = expiresAt //required
                PaymentMethodOptionsPix.AmountIncludesIof = amountIncludesIof
                PaymentMethodOptionsPix.MandateOptions = mandateOptions
                PaymentMethodOptionsPix.SetupFutureUsage = setupFutureUsage
            }

    and PaymentMethodOptionsRevolutPay = {
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentMethodOptionsRevolutPaySetupFutureUsage option
    }
    with
        ///Controls when the funds will be captured from the customer's account.
        member _.CaptureMethod = "manual"

        static member New (?setupFutureUsage: PaymentMethodOptionsRevolutPaySetupFutureUsage) =
            {
                PaymentMethodOptionsRevolutPay.SetupFutureUsage = setupFutureUsage
            }

    and PaymentMethodOptionsSofort = {
        ///Preferred language of the SOFORT authorization page that the customer is redirected to.
        PreferredLanguage: PaymentMethodOptionsSofortPreferredLanguage option
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentMethodOptionsSofortSetupFutureUsage option
    }
    with
        static member New (preferredLanguage: PaymentMethodOptionsSofortPreferredLanguage option, ?setupFutureUsage: PaymentMethodOptionsSofortSetupFutureUsage) =
            {
                PaymentMethodOptionsSofort.PreferredLanguage = preferredLanguage //required
                PaymentMethodOptionsSofort.SetupFutureUsage = setupFutureUsage
            }

    and PaymentMethodOptionsUpi = {
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        SetupFutureUsage: PaymentMethodOptionsUpiSetupFutureUsage option
    }
    with
        static member New (?setupFutureUsage: PaymentMethodOptionsUpiSetupFutureUsage) =
            {
                PaymentMethodOptionsUpi.SetupFutureUsage = setupFutureUsage
            }

    and PaymentMethodOptionsWechatPay = {
        ///The app ID registered with WeChat Pay. Only required when client is ios or android.
        AppId: string option
        ///The client type that the end customer will pay from
        Client: PaymentMethodOptionsWechatPayClient option
    }
    with
        ///Indicates that you intend to make future payments with this PaymentIntent's payment method.
        ///If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.
        ///If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.
        ///When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
        member _.SetupFutureUsage = "none"

        static member New (appId: string option, client: PaymentMethodOptionsWechatPayClient option) =
            {
                PaymentMethodOptionsWechatPay.AppId = appId //required
                PaymentMethodOptionsWechatPay.Client = client //required
            }

    and PaymentMethodP24 = {
        ///The customer's bank, if provided.
        Bank: PaymentMethodP24Bank option
    }
    with
        static member New (bank: PaymentMethodP24Bank option) =
            {
                PaymentMethodP24.Bank = bank //required
            }

    and PaymentMethodPaypal = {
        ///Two-letter ISO code representing the buyer's country. Values are provided by PayPal directly (if supported) at the time of authorization or settlement. They cannot be set or mutated.
        Country: IsoTypes.IsoCountryCode option
        ///Owner's email. Values are provided by PayPal directly
        ///(if supported) at the time of authorization or settlement. They cannot be set or mutated.
        PayerEmail: string option
        ///PayPal account PayerID. This identifier uniquely identifies the PayPal customer.
        PayerId: string option
    }
    with
        static member New (country: IsoTypes.IsoCountryCode option, payerEmail: string option, payerId: string option) =
            {
                PaymentMethodPaypal.Country = country //required
                PaymentMethodPaypal.PayerEmail = payerEmail //required
                PaymentMethodPaypal.PayerId = payerId //required
            }

    and PaymentMethodSepaDebit = {
        ///Bank code of bank associated with the bank account.
        BankCode: string option
        ///Branch code of bank associated with the bank account.
        BranchCode: string option
        ///Two-letter ISO code representing the country the bank account is located in.
        Country: IsoTypes.IsoCountryCode option
        ///Uniquely identifies this particular bank account. You can use this attribute to check whether two bank accounts are the same.
        Fingerprint: string option
        ///Information about the object that generated this PaymentMethod.
        GeneratedFrom: SepaDebitGeneratedFrom option
        ///Last four characters of the IBAN.
        [<JsonField(Name="last4")>]Last4: string option
    }
    with
        static member New (bankCode: string option, branchCode: string option, country: IsoTypes.IsoCountryCode option, fingerprint: string option, generatedFrom: SepaDebitGeneratedFrom option, last4: string option) =
            {
                PaymentMethodSepaDebit.BankCode = bankCode //required
                PaymentMethodSepaDebit.BranchCode = branchCode //required
                PaymentMethodSepaDebit.Country = country //required
                PaymentMethodSepaDebit.Fingerprint = fingerprint //required
                PaymentMethodSepaDebit.GeneratedFrom = generatedFrom //required
                PaymentMethodSepaDebit.Last4 = last4 //required
            }

    and PaymentMethodSofort = {
        ///Two-letter ISO code representing the country the bank account is located in.
        Country: IsoTypes.IsoCountryCode option
    }
    with
        static member New (country: IsoTypes.IsoCountryCode option) =
            {
                PaymentMethodSofort.Country = country //required
            }

    and PaymentMethodUsBankAccount = {
        ///Account holder type: individual or company.
        AccountHolderType: PaymentMethodUsBankAccountAccountHolderType option
        ///Account type: checkings or savings. Defaults to checking if omitted.
        AccountType: PaymentMethodUsBankAccountAccountType option
        ///The name of the bank.
        BankName: string option
        ///The ID of the Financial Connections Account used to create the payment method.
        FinancialConnectionsAccount: string option
        ///Uniquely identifies this particular bank account. You can use this attribute to check whether two bank accounts are the same.
        Fingerprint: string option
        ///Last four digits of the bank account number.
        [<JsonField(Name="last4")>]Last4: string option
        ///Contains information about US bank account networks that can be used.
        Networks: UsBankAccountNetworks option
        ///Routing number of the bank account.
        RoutingNumber: string option
        ///Contains information about the future reusability of this PaymentMethod.
        StatusDetails: PaymentMethodUsBankAccountStatusDetails option
    }
    with
        static member New (accountHolderType: PaymentMethodUsBankAccountAccountHolderType option, accountType: PaymentMethodUsBankAccountAccountType option, bankName: string option, financialConnectionsAccount: string option, fingerprint: string option, last4: string option, networks: UsBankAccountNetworks option, routingNumber: string option, statusDetails: PaymentMethodUsBankAccountStatusDetails option) =
            {
                PaymentMethodUsBankAccount.AccountHolderType = accountHolderType //required
                PaymentMethodUsBankAccount.AccountType = accountType //required
                PaymentMethodUsBankAccount.BankName = bankName //required
                PaymentMethodUsBankAccount.FinancialConnectionsAccount = financialConnectionsAccount //required
                PaymentMethodUsBankAccount.Fingerprint = fingerprint //required
                PaymentMethodUsBankAccount.Last4 = last4 //required
                PaymentMethodUsBankAccount.Networks = networks //required
                PaymentMethodUsBankAccount.RoutingNumber = routingNumber //required
                PaymentMethodUsBankAccount.StatusDetails = statusDetails //required
            }

    and PaymentMethodUsBankAccountBlocked = {
        ///The ACH network code that resulted in this block.
        NetworkCode: PaymentMethodUsBankAccountBlockedNetworkCode option
        ///The reason why this PaymentMethod's fingerprint has been blocked
        Reason: PaymentMethodUsBankAccountBlockedReason option
    }
    with
        static member New (networkCode: PaymentMethodUsBankAccountBlockedNetworkCode option, reason: PaymentMethodUsBankAccountBlockedReason option) =
            {
                PaymentMethodUsBankAccountBlocked.NetworkCode = networkCode //required
                PaymentMethodUsBankAccountBlocked.Reason = reason //required
            }

    and PaymentMethodUsBankAccountStatusDetails = {
        Blocked: PaymentMethodUsBankAccountBlocked option
    }
    with
        static member New (?blocked: PaymentMethodUsBankAccountBlocked) =
            {
                PaymentMethodUsBankAccountStatusDetails.Blocked = blocked
            }

    and PaymentPagesCheckoutSessionAfterExpiration = {
        ///When set, configuration used to recover the Checkout Session on expiry.
        Recovery: PaymentPagesCheckoutSessionAfterExpirationRecovery option
    }
    with
        static member New (recovery: PaymentPagesCheckoutSessionAfterExpirationRecovery option) =
            {
                PaymentPagesCheckoutSessionAfterExpiration.Recovery = recovery //required
            }

    and PaymentPagesCheckoutSessionAutomaticTax = {
        ///Indicates whether automatic tax is enabled for the session
        Enabled: bool
        ///The account that's liable for tax. If set, the business address and tax registrations required to perform the tax calculation are loaded from this account. The tax transaction is returned in the report of the connected account.
        Liability: ConnectAccountReference option
        ///The tax provider powering automatic tax.
        Provider: string option
        ///The status of the most recent automated tax calculation for this session.
        Status: PaymentPagesCheckoutSessionAutomaticTaxStatus option
    }
    with
        static member New (enabled: bool, liability: ConnectAccountReference option, provider: string option, status: PaymentPagesCheckoutSessionAutomaticTaxStatus option) =
            {
                PaymentPagesCheckoutSessionAutomaticTax.Enabled = enabled //required
                PaymentPagesCheckoutSessionAutomaticTax.Liability = liability //required
                PaymentPagesCheckoutSessionAutomaticTax.Provider = provider //required
                PaymentPagesCheckoutSessionAutomaticTax.Status = status //required
            }

    and PaymentPagesCheckoutSessionBrandingSettings = {
        ///A hex color value starting with `#` representing the background color for the Checkout Session.
        BackgroundColor: string
        ///The border style for the Checkout Session. Must be one of `rounded`, `rectangular`, or `pill`.
        BorderStyle: PaymentPagesCheckoutSessionBrandingSettingsBorderStyle
        ///A hex color value starting with `#` representing the button color for the Checkout Session.
        ButtonColor: string
        ///The display name shown on the Checkout Session.
        DisplayName: string
        ///The font family for the Checkout Session. Must be one of the [supported font families](https://docs.stripe.com/payments/checkout/customization/appearance?payment-ui=stripe-hosted#font-compatibility).
        FontFamily: string
        ///The icon for the Checkout Session. You cannot set both `logo` and `icon`.
        Icon: PaymentPagesCheckoutSessionBrandingSettingsIcon option
        ///The logo for the Checkout Session. You cannot set both `logo` and `icon`.
        Logo: PaymentPagesCheckoutSessionBrandingSettingsLogo option
    }
    with
        static member New (backgroundColor: string, borderStyle: PaymentPagesCheckoutSessionBrandingSettingsBorderStyle, buttonColor: string, displayName: string, fontFamily: string, icon: PaymentPagesCheckoutSessionBrandingSettingsIcon option, logo: PaymentPagesCheckoutSessionBrandingSettingsLogo option) =
            {
                PaymentPagesCheckoutSessionBrandingSettings.BackgroundColor = backgroundColor //required
                PaymentPagesCheckoutSessionBrandingSettings.BorderStyle = borderStyle //required
                PaymentPagesCheckoutSessionBrandingSettings.ButtonColor = buttonColor //required
                PaymentPagesCheckoutSessionBrandingSettings.DisplayName = displayName //required
                PaymentPagesCheckoutSessionBrandingSettings.FontFamily = fontFamily //required
                PaymentPagesCheckoutSessionBrandingSettings.Icon = icon //required
                PaymentPagesCheckoutSessionBrandingSettings.Logo = logo //required
            }

    and PaymentPagesCheckoutSessionBrandingSettingsIcon = {
        ///The ID of a [File upload](https://stripe.com/docs/api/files) representing the icon. Purpose must be `business_icon`. Required if `type` is `file` and disallowed otherwise.
        File: string option
        ///The type of image for the icon. Must be one of `file` or `url`.
        Type: PaymentPagesCheckoutSessionBrandingSettingsIconType
        ///The URL of the image. Present when `type` is `url`.
        Url: string option
    }
    with
        static member New (``type``: PaymentPagesCheckoutSessionBrandingSettingsIconType, ?file: string, ?url: string) =
            {
                PaymentPagesCheckoutSessionBrandingSettingsIcon.Type = ``type`` //required
                PaymentPagesCheckoutSessionBrandingSettingsIcon.File = file
                PaymentPagesCheckoutSessionBrandingSettingsIcon.Url = url
            }

    and PaymentPagesCheckoutSessionBrandingSettingsLogo = {
        ///The ID of a [File upload](https://stripe.com/docs/api/files) representing the logo. Purpose must be `business_logo`. Required if `type` is `file` and disallowed otherwise.
        File: string option
        ///The type of image for the logo. Must be one of `file` or `url`.
        Type: PaymentPagesCheckoutSessionBrandingSettingsLogoType
        ///The URL of the image. Present when `type` is `url`.
        Url: string option
    }
    with
        static member New (``type``: PaymentPagesCheckoutSessionBrandingSettingsLogoType, ?file: string, ?url: string) =
            {
                PaymentPagesCheckoutSessionBrandingSettingsLogo.Type = ``type`` //required
                PaymentPagesCheckoutSessionBrandingSettingsLogo.File = file
                PaymentPagesCheckoutSessionBrandingSettingsLogo.Url = url
            }

    and PaymentPagesCheckoutSessionCheckoutAddressDetails = {
        Address: Address
        ///Customer name.
        Name: string
    }
    with
        static member New (address: Address, name: string) =
            {
                PaymentPagesCheckoutSessionCheckoutAddressDetails.Address = address //required
                PaymentPagesCheckoutSessionCheckoutAddressDetails.Name = name //required
            }

    and PaymentPagesCheckoutSessionCollectedInformation = {
        ///Customer’s business name for this Checkout Session
        BusinessName: string option
        ///Customer’s individual name for this Checkout Session
        IndividualName: string option
        ///Shipping information for this Checkout Session.
        ShippingDetails: PaymentPagesCheckoutSessionCheckoutAddressDetails option
    }
    with
        static member New (businessName: string option, individualName: string option, shippingDetails: PaymentPagesCheckoutSessionCheckoutAddressDetails option) =
            {
                PaymentPagesCheckoutSessionCollectedInformation.BusinessName = businessName //required
                PaymentPagesCheckoutSessionCollectedInformation.IndividualName = individualName //required
                PaymentPagesCheckoutSessionCollectedInformation.ShippingDetails = shippingDetails //required
            }

    and PaymentPagesCheckoutSessionConsent = {
        ///If `opt_in`, the customer consents to receiving promotional communications
        ///from the merchant about this Checkout Session.
        Promotions: PaymentPagesCheckoutSessionConsentPromotions option
    }
    with
        ///If `accepted`, the customer in this Checkout Session has agreed to the merchant's terms of service.
        member _.TermsOfService = "accepted"

        static member New (promotions: PaymentPagesCheckoutSessionConsentPromotions option) =
            {
                PaymentPagesCheckoutSessionConsent.Promotions = promotions //required
            }

    and PaymentPagesCheckoutSessionConsentCollection = {
        ///If set to `hidden`, it will hide legal text related to the reuse of a payment method.
        PaymentMethodReuseAgreement: PaymentPagesCheckoutSessionPaymentMethodReuseAgreement option
        ///If set to `auto`, enables the collection of customer consent for promotional communications. The Checkout
        ///Session will determine whether to display an option to opt into promotional communication
        ///from the merchant depending on the customer's locale. Only available to US merchants.
        Promotions: PaymentPagesCheckoutSessionConsentCollectionPromotions option
        ///If set to `required`, it requires customers to accept the terms of service before being able to pay.
        TermsOfService: PaymentPagesCheckoutSessionConsentCollectionTermsOfService option
    }
    with
        static member New (paymentMethodReuseAgreement: PaymentPagesCheckoutSessionPaymentMethodReuseAgreement option, promotions: PaymentPagesCheckoutSessionConsentCollectionPromotions option, termsOfService: PaymentPagesCheckoutSessionConsentCollectionTermsOfService option) =
            {
                PaymentPagesCheckoutSessionConsentCollection.PaymentMethodReuseAgreement = paymentMethodReuseAgreement //required
                PaymentPagesCheckoutSessionConsentCollection.Promotions = promotions //required
                PaymentPagesCheckoutSessionConsentCollection.TermsOfService = termsOfService //required
            }

    and PaymentPagesCheckoutSessionCurrencyConversion = {
        ///Total of all items in source currency before discounts or taxes are applied.
        AmountSubtotal: int
        ///Total of all items in source currency after discounts and taxes are applied.
        AmountTotal: int
        ///Exchange rate used to convert source currency amounts to customer currency amounts
        FxRate: string
        ///Creation currency of the CheckoutSession before localization
        SourceCurrency: IsoTypes.IsoCurrencyCode
    }
    with
        static member New (amountSubtotal: int, amountTotal: int, fxRate: string, sourceCurrency: IsoTypes.IsoCurrencyCode) =
            {
                PaymentPagesCheckoutSessionCurrencyConversion.AmountSubtotal = amountSubtotal //required
                PaymentPagesCheckoutSessionCurrencyConversion.AmountTotal = amountTotal //required
                PaymentPagesCheckoutSessionCurrencyConversion.FxRate = fxRate //required
                PaymentPagesCheckoutSessionCurrencyConversion.SourceCurrency = sourceCurrency //required
            }

    and PaymentPagesCheckoutSessionCustomFields = {
        Dropdown: PaymentPagesCheckoutSessionCustomFieldsDropdown option
        ///String of your choice that your integration can use to reconcile this field. Must be unique to this field, alphanumeric, and up to 200 characters.
        Key: string
        Label: PaymentPagesCheckoutSessionCustomFieldsLabel
        Numeric: PaymentPagesCheckoutSessionCustomFieldsNumeric option
        ///Whether the customer is required to complete the field before completing the Checkout Session. Defaults to `false`.
        Optional: bool
        Text: PaymentPagesCheckoutSessionCustomFieldsText option
        ///The type of the field.
        Type: PaymentPagesCheckoutSessionCustomFieldsType
    }
    with
        static member New (key: string, label: PaymentPagesCheckoutSessionCustomFieldsLabel, optional: bool, ``type``: PaymentPagesCheckoutSessionCustomFieldsType, ?dropdown: PaymentPagesCheckoutSessionCustomFieldsDropdown, ?numeric: PaymentPagesCheckoutSessionCustomFieldsNumeric, ?text: PaymentPagesCheckoutSessionCustomFieldsText) =
            {
                PaymentPagesCheckoutSessionCustomFields.Key = key //required
                PaymentPagesCheckoutSessionCustomFields.Label = label //required
                PaymentPagesCheckoutSessionCustomFields.Optional = optional //required
                PaymentPagesCheckoutSessionCustomFields.Type = ``type`` //required
                PaymentPagesCheckoutSessionCustomFields.Dropdown = dropdown
                PaymentPagesCheckoutSessionCustomFields.Numeric = numeric
                PaymentPagesCheckoutSessionCustomFields.Text = text
            }

    and PaymentPagesCheckoutSessionCustomFieldsDropdown = {
        ///The value that pre-fills on the payment page.
        DefaultValue: string option
        ///The options available for the customer to select. Up to 200 options allowed.
        Options: PaymentPagesCheckoutSessionCustomFieldsOption list
        ///The option selected by the customer. This will be the `value` for the option.
        Value: string option
    }
    with
        static member New (defaultValue: string option, options: PaymentPagesCheckoutSessionCustomFieldsOption list, value: string option) =
            {
                PaymentPagesCheckoutSessionCustomFieldsDropdown.DefaultValue = defaultValue //required
                PaymentPagesCheckoutSessionCustomFieldsDropdown.Options = options //required
                PaymentPagesCheckoutSessionCustomFieldsDropdown.Value = value //required
            }

    and PaymentPagesCheckoutSessionCustomText = {
        ///Custom text that should be displayed after the payment confirmation button.
        AfterSubmit: PaymentPagesCheckoutSessionCustomTextPosition option
        ///Custom text that should be displayed alongside shipping address collection.
        ShippingAddress: PaymentPagesCheckoutSessionCustomTextPosition option
        ///Custom text that should be displayed alongside the payment confirmation button.
        Submit: PaymentPagesCheckoutSessionCustomTextPosition option
        ///Custom text that should be displayed in place of the default terms of service agreement text.
        TermsOfServiceAcceptance: PaymentPagesCheckoutSessionCustomTextPosition option
    }
    with
        static member New (afterSubmit: PaymentPagesCheckoutSessionCustomTextPosition option, shippingAddress: PaymentPagesCheckoutSessionCustomTextPosition option, submit: PaymentPagesCheckoutSessionCustomTextPosition option, termsOfServiceAcceptance: PaymentPagesCheckoutSessionCustomTextPosition option) =
            {
                PaymentPagesCheckoutSessionCustomText.AfterSubmit = afterSubmit //required
                PaymentPagesCheckoutSessionCustomText.ShippingAddress = shippingAddress //required
                PaymentPagesCheckoutSessionCustomText.Submit = submit //required
                PaymentPagesCheckoutSessionCustomText.TermsOfServiceAcceptance = termsOfServiceAcceptance //required
            }

    and PaymentPagesCheckoutSessionCustomerDetails = {
        ///The customer's address after a completed Checkout Session. Note: This property is populated only for sessions on or after March 30, 2022.
        Address: Address option
        ///The customer's business name after a completed Checkout Session.
        BusinessName: string option
        ///The email associated with the Customer, if one exists, on the Checkout Session after a completed Checkout Session or at time of session expiry.
        ///Otherwise, if the customer has consented to promotional content, this value is the most recent valid email provided by the customer on the Checkout form.
        Email: string option
        ///The customer's individual name after a completed Checkout Session.
        IndividualName: string option
        ///The customer's name after a completed Checkout Session. Note: This property is populated only for sessions on or after March 30, 2022.
        Name: string option
        ///The customer's phone number after a completed Checkout Session.
        Phone: string option
        ///The customer’s tax exempt status after a completed Checkout Session.
        TaxExempt: PaymentPagesCheckoutSessionCustomerDetailsTaxExempt option
        ///The customer’s tax IDs after a completed Checkout Session.
        TaxIds: PaymentPagesCheckoutSessionTaxId list option
    }
    with
        static member New (address: Address option, businessName: string option, email: string option, individualName: string option, name: string option, phone: string option, taxExempt: PaymentPagesCheckoutSessionCustomerDetailsTaxExempt option, taxIds: PaymentPagesCheckoutSessionTaxId list option) =
            {
                PaymentPagesCheckoutSessionCustomerDetails.Address = address //required
                PaymentPagesCheckoutSessionCustomerDetails.BusinessName = businessName //required
                PaymentPagesCheckoutSessionCustomerDetails.Email = email //required
                PaymentPagesCheckoutSessionCustomerDetails.IndividualName = individualName //required
                PaymentPagesCheckoutSessionCustomerDetails.Name = name //required
                PaymentPagesCheckoutSessionCustomerDetails.Phone = phone //required
                PaymentPagesCheckoutSessionCustomerDetails.TaxExempt = taxExempt //required
                PaymentPagesCheckoutSessionCustomerDetails.TaxIds = taxIds //required
            }

    and PaymentPagesCheckoutSessionDiscount = {
        ///Coupon attached to the Checkout Session.
        Coupon: PaymentPagesCheckoutSessionDiscountCoupon'AnyOf option
        ///Promotion code attached to the Checkout Session.
        PromotionCode: PaymentPagesCheckoutSessionDiscountPromotionCode'AnyOf option
    }
    with
        static member New (coupon: PaymentPagesCheckoutSessionDiscountCoupon'AnyOf option, promotionCode: PaymentPagesCheckoutSessionDiscountPromotionCode'AnyOf option) =
            {
                PaymentPagesCheckoutSessionDiscount.Coupon = coupon //required
                PaymentPagesCheckoutSessionDiscount.PromotionCode = promotionCode //required
            }

    and PaymentPagesCheckoutSessionDiscountCoupon'AnyOf =
        | String of string
        | Coupon of Coupon

    and PaymentPagesCheckoutSessionDiscountPromotionCode'AnyOf =
        | String of string
        | PromotionCode of PromotionCode

    and PaymentPagesCheckoutSessionInvoiceCreation = {
        ///Indicates whether invoice creation is enabled for the Checkout Session.
        Enabled: bool
        InvoiceData: PaymentPagesCheckoutSessionInvoiceSettings
    }
    with
        static member New (enabled: bool, invoiceData: PaymentPagesCheckoutSessionInvoiceSettings) =
            {
                PaymentPagesCheckoutSessionInvoiceCreation.Enabled = enabled //required
                PaymentPagesCheckoutSessionInvoiceCreation.InvoiceData = invoiceData //required
            }

    and PaymentPagesCheckoutSessionInvoiceSettings = {
        ///The account tax IDs associated with the invoice.
        AccountTaxIds: PaymentPagesCheckoutSessionInvoiceSettingsAccountTaxIds'AnyOf list option
        ///Custom fields displayed on the invoice.
        CustomFields: InvoiceSettingCustomField list option
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///Footer displayed on the invoice.
        Footer: string option
        ///The connected account that issues the invoice. The invoice is presented with the branding and support information of the specified account.
        Issuer: ConnectAccountReference option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///Options for invoice PDF rendering.
        RenderingOptions: InvoiceSettingCheckoutRenderingOptions option
    }
    with
        static member New (accountTaxIds: PaymentPagesCheckoutSessionInvoiceSettingsAccountTaxIds'AnyOf list option, customFields: InvoiceSettingCustomField list option, description: string option, footer: string option, issuer: ConnectAccountReference option, metadata: Map<string, string> option, renderingOptions: InvoiceSettingCheckoutRenderingOptions option) =
            {
                PaymentPagesCheckoutSessionInvoiceSettings.AccountTaxIds = accountTaxIds //required
                PaymentPagesCheckoutSessionInvoiceSettings.CustomFields = customFields //required
                PaymentPagesCheckoutSessionInvoiceSettings.Description = description //required
                PaymentPagesCheckoutSessionInvoiceSettings.Footer = footer //required
                PaymentPagesCheckoutSessionInvoiceSettings.Issuer = issuer //required
                PaymentPagesCheckoutSessionInvoiceSettings.Metadata = metadata //required
                PaymentPagesCheckoutSessionInvoiceSettings.RenderingOptions = renderingOptions //required
            }

    and PaymentPagesCheckoutSessionInvoiceSettingsAccountTaxIds'AnyOf =
        | String of string
        | TaxId of TaxId
        | DeletedTaxId of DeletedTaxId

    and PaymentPagesCheckoutSessionNameCollection = {
        Business: PaymentPagesCheckoutSessionBusinessName option
        Individual: PaymentPagesCheckoutSessionIndividualName option
    }
    with
        static member New (?business: PaymentPagesCheckoutSessionBusinessName, ?individual: PaymentPagesCheckoutSessionIndividualName) =
            {
                PaymentPagesCheckoutSessionNameCollection.Business = business
                PaymentPagesCheckoutSessionNameCollection.Individual = individual
            }

    and PaymentPagesCheckoutSessionOptionalItem = {
        AdjustableQuantity: PaymentPagesCheckoutSessionOptionalItemAdjustableQuantity option
        Price: string
        Quantity: int
    }
    with
        static member New (adjustableQuantity: PaymentPagesCheckoutSessionOptionalItemAdjustableQuantity option, price: string, quantity: int) =
            {
                PaymentPagesCheckoutSessionOptionalItem.AdjustableQuantity = adjustableQuantity //required
                PaymentPagesCheckoutSessionOptionalItem.Price = price //required
                PaymentPagesCheckoutSessionOptionalItem.Quantity = quantity //required
            }

    and PaymentPagesCheckoutSessionPaymentMethodReuseAgreement = {
        ///Determines the position and visibility of the payment method reuse agreement in the UI. When set to `auto`, Stripe's defaults will be used.
        ///When set to `hidden`, the payment method reuse agreement text will always be hidden in the UI.
        Position: PaymentPagesCheckoutSessionPaymentMethodReuseAgreementPosition
    }
    with
        static member New (position: PaymentPagesCheckoutSessionPaymentMethodReuseAgreementPosition) =
            {
                PaymentPagesCheckoutSessionPaymentMethodReuseAgreement.Position = position //required
            }

    and PaymentPagesCheckoutSessionPermissions = {
        ///Determines which entity is allowed to update the shipping details.
        ///Default is `client_only`. Stripe Checkout client will automatically update the shipping details. If set to `server_only`, only your server is allowed to update the shipping details.
        ///When set to `server_only`, you must add the onShippingDetailsChange event handler when initializing the Stripe Checkout client and manually update the shipping details from your server using the Stripe API.
        UpdateShippingDetails: PaymentPagesCheckoutSessionPermissionsUpdateShippingDetails option
    }
    with
        static member New (updateShippingDetails: PaymentPagesCheckoutSessionPermissionsUpdateShippingDetails option) =
            {
                PaymentPagesCheckoutSessionPermissions.UpdateShippingDetails = updateShippingDetails //required
            }

    and PaymentPagesCheckoutSessionSavedPaymentMethodOptions = {
        ///Uses the `allow_redisplay` value of each saved payment method to filter the set presented to a returning customer. By default, only saved payment methods with ’allow_redisplay: ‘always’ are shown in Checkout.
        AllowRedisplayFilters: PaymentPagesCheckoutSessionSavedPaymentMethodOptionsAllowRedisplayFilters list option
        ///Enable customers to choose if they wish to remove their saved payment methods. Disabled by default.
        PaymentMethodRemove: PaymentPagesCheckoutSessionSavedPaymentMethodOptionsPaymentMethodRemove option
        ///Enable customers to choose if they wish to save their payment method for future use. Disabled by default.
        PaymentMethodSave: PaymentPagesCheckoutSessionSavedPaymentMethodOptionsPaymentMethodSave option
    }
    with
        static member New (allowRedisplayFilters: PaymentPagesCheckoutSessionSavedPaymentMethodOptionsAllowRedisplayFilters list option, paymentMethodRemove: PaymentPagesCheckoutSessionSavedPaymentMethodOptionsPaymentMethodRemove option, paymentMethodSave: PaymentPagesCheckoutSessionSavedPaymentMethodOptionsPaymentMethodSave option) =
            {
                PaymentPagesCheckoutSessionSavedPaymentMethodOptions.AllowRedisplayFilters = allowRedisplayFilters //required
                PaymentPagesCheckoutSessionSavedPaymentMethodOptions.PaymentMethodRemove = paymentMethodRemove //required
                PaymentPagesCheckoutSessionSavedPaymentMethodOptions.PaymentMethodSave = paymentMethodSave //required
            }

    and PaymentPagesCheckoutSessionShippingAddressCollection = {
        ///An array of two-letter ISO country codes representing which countries Checkout should provide as options for
        ///shipping locations. Unsupported country codes: `AS, CX, CC, CU, HM, IR, KP, MH, FM, NF, MP, PW, SY, UM, VI`.
        AllowedCountries: PaymentPagesCheckoutSessionShippingAddressCollectionAllowedCountries list
    }
    with
        static member New (allowedCountries: PaymentPagesCheckoutSessionShippingAddressCollectionAllowedCountries list) =
            {
                PaymentPagesCheckoutSessionShippingAddressCollection.AllowedCountries = allowedCountries //required
            }

    and PaymentPagesCheckoutSessionShippingCost = {
        ///Total shipping cost before any discounts or taxes are applied.
        AmountSubtotal: int
        ///Total tax amount applied due to shipping costs. If no tax was applied, defaults to 0.
        AmountTax: int
        ///Total shipping cost after discounts and taxes are applied.
        AmountTotal: int
        ///The ID of the ShippingRate for this order.
        ShippingRate: PaymentPagesCheckoutSessionShippingCostShippingRate'AnyOf option
        ///The taxes applied to the shipping rate.
        Taxes: LineItemsTaxAmount list option
    }
    with
        static member New (amountSubtotal: int, amountTax: int, amountTotal: int, shippingRate: PaymentPagesCheckoutSessionShippingCostShippingRate'AnyOf option, ?taxes: LineItemsTaxAmount list) =
            {
                PaymentPagesCheckoutSessionShippingCost.AmountSubtotal = amountSubtotal //required
                PaymentPagesCheckoutSessionShippingCost.AmountTax = amountTax //required
                PaymentPagesCheckoutSessionShippingCost.AmountTotal = amountTotal //required
                PaymentPagesCheckoutSessionShippingCost.ShippingRate = shippingRate //required
                PaymentPagesCheckoutSessionShippingCost.Taxes = taxes
            }

    and PaymentPagesCheckoutSessionShippingCostShippingRate'AnyOf =
        | String of string
        | ShippingRate of ShippingRate

    and PaymentPagesCheckoutSessionShippingOption = {
        ///A non-negative integer in cents representing how much to charge.
        ShippingAmount: int
        ///The shipping rate.
        ShippingRate: PaymentPagesCheckoutSessionShippingOptionShippingRate'AnyOf
    }
    with
        static member New (shippingAmount: int, shippingRate: PaymentPagesCheckoutSessionShippingOptionShippingRate'AnyOf) =
            {
                PaymentPagesCheckoutSessionShippingOption.ShippingAmount = shippingAmount //required
                PaymentPagesCheckoutSessionShippingOption.ShippingRate = shippingRate //required
            }

    and PaymentPagesCheckoutSessionShippingOptionShippingRate'AnyOf =
        | String of string
        | ShippingRate of ShippingRate

    and PaymentPagesCheckoutSessionTaxId = {
        ///The type of the tax ID, one of `ad_nrt`, `ar_cuit`, `eu_vat`, `bo_tin`, `br_cnpj`, `br_cpf`, `cn_tin`, `co_nit`, `cr_tin`, `do_rcn`, `ec_ruc`, `eu_oss_vat`, `hr_oib`, `pe_ruc`, `ro_tin`, `rs_pib`, `sv_nit`, `uy_ruc`, `ve_rif`, `vn_tin`, `gb_vat`, `nz_gst`, `au_abn`, `au_arn`, `in_gst`, `no_vat`, `no_voec`, `za_vat`, `ch_vat`, `mx_rfc`, `sg_uen`, `ru_inn`, `ru_kpp`, `ca_bn`, `hk_br`, `es_cif`, `pl_nip`, `it_cf`, `fo_vat`, `gi_tin`, `py_ruc`, `tw_vat`, `th_vat`, `jp_cn`, `jp_rn`, `jp_trn`, `li_uid`, `li_vat`, `lk_vat`, `my_itn`, `us_ein`, `kr_brn`, `ca_qst`, `ca_gst_hst`, `ca_pst_bc`, `ca_pst_mb`, `ca_pst_sk`, `my_sst`, `sg_gst`, `ae_trn`, `cl_tin`, `sa_vat`, `id_npwp`, `my_frp`, `il_vat`, `ge_vat`, `ua_vat`, `is_vat`, `bg_uic`, `hu_tin`, `si_tin`, `ke_pin`, `tr_tin`, `eg_tin`, `ph_tin`, `al_tin`, `bh_vat`, `kz_bin`, `ng_tin`, `om_vat`, `de_stn`, `ch_uid`, `tz_vat`, `uz_vat`, `uz_tin`, `md_vat`, `ma_vat`, `by_tin`, `ao_tin`, `bs_tin`, `bb_tin`, `cd_nif`, `mr_nif`, `me_pib`, `zw_tin`, `ba_tin`, `gn_nif`, `mk_vat`, `sr_fin`, `sn_ninea`, `am_tin`, `np_pan`, `tj_tin`, `ug_tin`, `zm_tin`, `kh_tin`, `aw_tin`, `az_tin`, `bd_bin`, `bj_ifu`, `et_tin`, `kg_tin`, `la_tin`, `cm_niu`, `cv_nif`, `bf_ifu`, or `unknown`
        Type: PaymentPagesCheckoutSessionTaxIdType
        ///The value of the tax ID.
        Value: string option
    }
    with
        static member New (``type``: PaymentPagesCheckoutSessionTaxIdType, value: string option) =
            {
                PaymentPagesCheckoutSessionTaxId.Type = ``type`` //required
                PaymentPagesCheckoutSessionTaxId.Value = value //required
            }

    and PaymentPagesCheckoutSessionTaxIdCollection = {
        ///Indicates whether tax ID collection is enabled for the session
        Enabled: bool
        ///Indicates whether a tax ID is required on the payment page
        Required: PaymentPagesCheckoutSessionTaxIdCollectionRequired
    }
    with
        static member New (enabled: bool, required: PaymentPagesCheckoutSessionTaxIdCollectionRequired) =
            {
                PaymentPagesCheckoutSessionTaxIdCollection.Enabled = enabled //required
                PaymentPagesCheckoutSessionTaxIdCollection.Required = required //required
            }

    and PaymentPagesCheckoutSessionTotalDetails = {
        ///This is the sum of all the discounts.
        AmountDiscount: int
        ///This is the sum of all the shipping amounts.
        AmountShipping: int option
        ///This is the sum of all the tax amounts.
        AmountTax: int
        Breakdown: PaymentPagesCheckoutSessionTotalDetailsResourceBreakdown option
    }
    with
        static member New (amountDiscount: int, amountShipping: int option, amountTax: int, ?breakdown: PaymentPagesCheckoutSessionTotalDetailsResourceBreakdown) =
            {
                PaymentPagesCheckoutSessionTotalDetails.AmountDiscount = amountDiscount //required
                PaymentPagesCheckoutSessionTotalDetails.AmountShipping = amountShipping //required
                PaymentPagesCheckoutSessionTotalDetails.AmountTax = amountTax //required
                PaymentPagesCheckoutSessionTotalDetails.Breakdown = breakdown
            }

    and PaymentPagesCheckoutSessionTotalDetailsResourceBreakdown = {
        ///The aggregated discounts.
        Discounts: LineItemsDiscountAmount list
        ///The aggregated tax amounts by rate.
        Taxes: LineItemsTaxAmount list
    }
    with
        static member New (discounts: LineItemsDiscountAmount list, taxes: LineItemsTaxAmount list) =
            {
                PaymentPagesCheckoutSessionTotalDetailsResourceBreakdown.Discounts = discounts //required
                PaymentPagesCheckoutSessionTotalDetailsResourceBreakdown.Taxes = taxes //required
            }

    and PaymentPagesPrivateCardPaymentMethodOptionsResourceRestrictions = {
        ///Specify the card brands to block in the Checkout Session. If a customer enters or selects a card belonging to a blocked brand, they can't complete the Session.
        BrandsBlocked: PaymentPagesPrivateCardPaymentMethodOptionsResourceRestrictionsBrandsBlocked list option
    }
    with
        static member New (?brandsBlocked: PaymentPagesPrivateCardPaymentMethodOptionsResourceRestrictionsBrandsBlocked list) =
            {
                PaymentPagesPrivateCardPaymentMethodOptionsResourceRestrictions.BrandsBlocked = brandsBlocked
            }

    ///A Payment Record is a resource that allows you to represent payments that occur on- or off-Stripe.
    ///For example, you can create a Payment Record to model a payment made on a different payment processor,
    ///in order to mark an Invoice as paid and a Subscription as active. Payment Records consist of one or
    ///more Payment Attempt Records, which represent individual attempts made on a payment network.
    and PaymentRecord = {
        Amount: PaymentsPrimitivesPaymentRecordsResourceAmount
        AmountAuthorized: PaymentsPrimitivesPaymentRecordsResourceAmount
        AmountCanceled: PaymentsPrimitivesPaymentRecordsResourceAmount
        AmountFailed: PaymentsPrimitivesPaymentRecordsResourceAmount
        AmountGuaranteed: PaymentsPrimitivesPaymentRecordsResourceAmount
        AmountRefunded: PaymentsPrimitivesPaymentRecordsResourceAmount
        AmountRequested: PaymentsPrimitivesPaymentRecordsResourceAmount
        ///ID of the Connect application that created the PaymentRecord.
        Application: string option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Customer information for this payment.
        CustomerDetails: PaymentsPrimitivesPaymentRecordsResourceCustomerDetails option
        ///Indicates whether the customer was present in your checkout flow during this payment.
        CustomerPresence: PaymentRecordCustomerPresence option
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///Unique identifier for the object.
        Id: string
        ///ID of the latest Payment Attempt Record attached to this Payment Record.
        LatestPaymentAttemptRecord: string option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///Information about the Payment Method debited for this payment.
        PaymentMethodDetails: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails option
        ProcessorDetails: PaymentsPrimitivesPaymentRecordsResourceProcessorDetails
        ///Indicates who reported the payment.
        ReportedBy: PaymentRecordReportedBy
        ///Shipping information for this payment.
        ShippingDetails: PaymentsPrimitivesPaymentRecordsResourceShippingDetails option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "payment_record"

        static member New (amount: PaymentsPrimitivesPaymentRecordsResourceAmount, amountAuthorized: PaymentsPrimitivesPaymentRecordsResourceAmount, amountCanceled: PaymentsPrimitivesPaymentRecordsResourceAmount, amountFailed: PaymentsPrimitivesPaymentRecordsResourceAmount, amountGuaranteed: PaymentsPrimitivesPaymentRecordsResourceAmount, amountRefunded: PaymentsPrimitivesPaymentRecordsResourceAmount, amountRequested: PaymentsPrimitivesPaymentRecordsResourceAmount, application: string option, created: DateTime, customerDetails: PaymentsPrimitivesPaymentRecordsResourceCustomerDetails option, customerPresence: PaymentRecordCustomerPresence option, description: string option, id: string, latestPaymentAttemptRecord: string option, livemode: bool, metadata: Map<string, string>, paymentMethodDetails: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails option, processorDetails: PaymentsPrimitivesPaymentRecordsResourceProcessorDetails, reportedBy: PaymentRecordReportedBy, shippingDetails: PaymentsPrimitivesPaymentRecordsResourceShippingDetails option) =
            {
                PaymentRecord.Amount = amount //required
                PaymentRecord.AmountAuthorized = amountAuthorized //required
                PaymentRecord.AmountCanceled = amountCanceled //required
                PaymentRecord.AmountFailed = amountFailed //required
                PaymentRecord.AmountGuaranteed = amountGuaranteed //required
                PaymentRecord.AmountRefunded = amountRefunded //required
                PaymentRecord.AmountRequested = amountRequested //required
                PaymentRecord.Application = application //required
                PaymentRecord.Created = created //required
                PaymentRecord.CustomerDetails = customerDetails //required
                PaymentRecord.CustomerPresence = customerPresence //required
                PaymentRecord.Description = description //required
                PaymentRecord.Id = id //required
                PaymentRecord.LatestPaymentAttemptRecord = latestPaymentAttemptRecord //required
                PaymentRecord.Livemode = livemode //required
                PaymentRecord.Metadata = metadata //required
                PaymentRecord.PaymentMethodDetails = paymentMethodDetails //required
                PaymentRecord.ProcessorDetails = processorDetails //required
                PaymentRecord.ReportedBy = reportedBy //required
                PaymentRecord.ShippingDetails = shippingDetails //required
            }

    and PaymentSource =
        | Account of Account
        | BankAccount of BankAccount
        | Card of Card
        | Source of Source

    ///A representation of a physical address.
    and PaymentsPrimitivesPaymentRecordsResourceAddress = {
        ///City, district, suburb, town, or village.
        City: string option
        ///Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
        Country: IsoTypes.IsoCountryCode option
        ///Address line 1, such as the street, PO Box, or company name.
        [<JsonField(Name="line1")>]Line1: string option
        ///Address line 2, such as the apartment, suite, unit, or building.
        [<JsonField(Name="line2")>]Line2: string option
        ///ZIP or postal code.
        PostalCode: string option
        ///State, county, province, or region ([ISO 3166-2](https://en.wikipedia.org/wiki/ISO_3166-2)).
        State: string option
    }
    with
        static member New (city: string option, country: IsoTypes.IsoCountryCode option, line1: string option, line2: string option, postalCode: string option, state: string option) =
            {
                PaymentsPrimitivesPaymentRecordsResourceAddress.City = city //required
                PaymentsPrimitivesPaymentRecordsResourceAddress.Country = country //required
                PaymentsPrimitivesPaymentRecordsResourceAddress.Line1 = line1 //required
                PaymentsPrimitivesPaymentRecordsResourceAddress.Line2 = line2 //required
                PaymentsPrimitivesPaymentRecordsResourceAddress.PostalCode = postalCode //required
                PaymentsPrimitivesPaymentRecordsResourceAddress.State = state //required
            }

    ///A representation of an amount of money, consisting of an amount and a currency.
    and PaymentsPrimitivesPaymentRecordsResourceAmount = {
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///A positive integer representing the amount in the currency's [minor unit](https://docs.stripe.com/currencies#zero-decimal). For example, `100` can represent 1 USD or 100 JPY.
        Value: int
    }
    with
        static member New (currency: IsoTypes.IsoCurrencyCode, value: int) =
            {
                PaymentsPrimitivesPaymentRecordsResourceAmount.Currency = currency //required
                PaymentsPrimitivesPaymentRecordsResourceAmount.Value = value //required
            }

    ///Billing details used by the customer for this payment.
    and PaymentsPrimitivesPaymentRecordsResourceBillingDetails = {
        Address: PaymentsPrimitivesPaymentRecordsResourceAddress
        ///The billing email associated with the method of payment.
        Email: string option
        ///The billing name associated with the method of payment.
        Name: string option
        ///The billing phone number associated with the method of payment.
        Phone: string option
    }
    with
        static member New (address: PaymentsPrimitivesPaymentRecordsResourceAddress, email: string option, name: string option, phone: string option) =
            {
                PaymentsPrimitivesPaymentRecordsResourceBillingDetails.Address = address //required
                PaymentsPrimitivesPaymentRecordsResourceBillingDetails.Email = email //required
                PaymentsPrimitivesPaymentRecordsResourceBillingDetails.Name = name //required
                PaymentsPrimitivesPaymentRecordsResourceBillingDetails.Phone = phone //required
            }

    and PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFunding = {
        Card: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFundingResourceFundingCard option
    }
    with
        ///funding type of the underlying payment method.
        member _.Type = "card"

        static member New (?card: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFundingResourceFundingCard) =
            {
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFunding.Card = card
            }

    and PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFundingResourceFundingCard = {
        ///Card brand. Can be `amex`, `cartes_bancaires`, `diners`, `discover`, `eftpos_au`, `jcb`, `link`, `mastercard`, `unionpay`, `visa` or `unknown`.
        Brand: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFundingResourceFundingCardBrand option
        ///Two-letter ISO code representing the country of the card. You could use this attribute to get a sense of the international breakdown of cards you've collected.
        Country: IsoTypes.IsoCountryCode option
        ///Two-digit number representing the card's expiration month.
        ExpMonth: int option
        ///Four-digit number representing the card's expiration year.
        ExpYear: int option
        ///Card funding type. Can be `credit`, `debit`, `prepaid`, or `unknown`.
        Funding: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFundingResourceFundingCardFunding option
        ///The last four digits of the card.
        [<JsonField(Name="last4")>]Last4: string option
    }
    with
        static member New (brand: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFundingResourceFundingCardBrand option, country: IsoTypes.IsoCountryCode option, expMonth: int option, expYear: int option, funding: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFundingResourceFundingCardFunding option, last4: string option) =
            {
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFundingResourceFundingCard.Brand = brand //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFundingResourceFundingCard.Country = country //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFundingResourceFundingCard.ExpMonth = expMonth //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFundingResourceFundingCard.ExpYear = expYear //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFundingResourceFundingCard.Funding = funding //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodAmazonPayDetailsResourceFundingResourceFundingCard.Last4 = last4 //required
            }

    ///Details of the card used for this payment attempt.
    and PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails = {
        ///The authorization code of the payment.
        AuthorizationCode: string option
        ///Card brand. Can be `amex`, `cartes_bancaires`, `diners`, `discover`, `eftpos_au`, `jcb`, `link`, `mastercard`, `unionpay`, `visa` or `unknown`.
        Brand: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsBrand option
        ///When using manual capture, a future timestamp at which the charge will be automatically refunded if uncaptured.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CaptureBefore: DateTime option
        ///Check results by Card networks on Card address and CVC at time of payment.
        Checks: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceChecks option
        ///Two-letter ISO code representing the country of the card. You could use this attribute to get a sense of the international breakdown of cards you've collected.
        Country: IsoTypes.IsoCountryCode option
        ///A high-level description of the type of cards issued in this range.
        Description: string option
        ///Two-digit number representing the card's expiration month.
        ExpMonth: int option
        ///Four-digit number representing the card's expiration year.
        ExpYear: int option
        ///Uniquely identifies this particular card number. You can use this attribute to check whether two customers who’ve signed up with you are using the same card number, for example. For payment methods that tokenize card information (Apple Pay, Google Pay), the tokenized number might be provided instead of the underlying card number.
        ///*As of May 1, 2021, card fingerprint in India for Connect changed to allow two fingerprints for the same card---one for India and one for the rest of the world.*
        Fingerprint: string option
        ///Card funding type. Can be `credit`, `debit`, `prepaid`, or `unknown`.
        Funding: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsFunding option
        ///Issuer identification number of the card.
        Iin: string option
        ///Installment details for this payment.
        Installments: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceInstallments option
        ///The name of the card's issuing bank.
        Issuer: string option
        ///The last four digits of the card.
        [<JsonField(Name="last4")>]Last4: string option
        ///True if this payment was marked as MOTO and out of scope for SCA.
        Moto: bool option
        ///Identifies which network this charge was processed on. Can be `amex`, `cartes_bancaires`, `diners`, `discover`, `eftpos_au`, `interac`, `jcb`, `link`, `mastercard`, `unionpay`, `visa`, or `unknown`.
        Network: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsNetwork option
        ///Advice code from the card network for the failed payment.
        NetworkAdviceCode: string option
        ///Decline code from the card network for the failed payment.
        NetworkDeclineCode: string option
        ///If this card has network token credentials, this contains the details of the network token credentials.
        NetworkToken: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceNetworkToken option
        ///This is used by the financial networks to identify a transaction. Visa calls this the Transaction ID, Mastercard calls this the Trace ID, and American Express calls this the Acquirer Reference Data. This value will be present if it is returned by the financial network in the authorization response, and null otherwise.
        NetworkTransactionId: string option
        ///The transaction type that was passed for an off-session, Merchant-Initiated transaction, one of `recurring` or `unscheduled`.
        StoredCredentialUsage: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsStoredCredentialUsage option
        ///Populated if this transaction used 3D Secure authentication.
        ThreeDSecure: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecure option
        ///If this Card is part of a card wallet, this contains the details of the card wallet.
        Wallet: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWallet option
    }
    with
        static member New (authorizationCode: string option, brand: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsBrand option, checks: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceChecks option, country: IsoTypes.IsoCountryCode option, description: string option, expMonth: int option, expYear: int option, funding: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsFunding option, iin: string option, installments: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceInstallments option, issuer: string option, last4: string option, network: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsNetwork option, networkAdviceCode: string option, networkDeclineCode: string option, networkTransactionId: string option, storedCredentialUsage: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsStoredCredentialUsage option, threeDSecure: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecure option, wallet: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWallet option, ?captureBefore: DateTime, ?fingerprint: string option, ?moto: bool option, ?networkToken: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceNetworkToken option) =
            {
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.AuthorizationCode = authorizationCode //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.Brand = brand //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.Checks = checks //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.Country = country //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.Description = description //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.ExpMonth = expMonth //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.ExpYear = expYear //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.Funding = funding //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.Iin = iin //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.Installments = installments //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.Issuer = issuer //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.Last4 = last4 //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.Network = network //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.NetworkAdviceCode = networkAdviceCode //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.NetworkDeclineCode = networkDeclineCode //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.NetworkTransactionId = networkTransactionId //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.StoredCredentialUsage = storedCredentialUsage //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.ThreeDSecure = threeDSecure //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.Wallet = wallet //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.CaptureBefore = captureBefore
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.Fingerprint = fingerprint |> Option.flatten
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.Moto = moto |> Option.flatten
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails.NetworkToken = networkToken |> Option.flatten
            }

    and PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceChecks = {
        ///If you provide a value for `address.line1`, the check result is one of `pass`, `fail`, `unavailable`, or `unchecked`.
        [<JsonField(Name="address_line1_check")>]AddressLine1Check: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceChecksAddressLine1Check option
        ///If you provide a address postal code, the check result is one of `pass`, `fail`, `unavailable`, or `unchecked`.
        AddressPostalCodeCheck: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceChecksAddressPostalCodeCheck option
        ///If you provide a CVC, the check results is one of `pass`, `fail`, `unavailable`, or `unchecked`.
        CvcCheck: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceChecksCvcCheck option
    }
    with
        static member New (addressLine1Check: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceChecksAddressLine1Check option, addressPostalCodeCheck: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceChecksAddressPostalCodeCheck option, cvcCheck: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceChecksCvcCheck option) =
            {
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceChecks.AddressLine1Check = addressLine1Check //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceChecks.AddressPostalCodeCheck = addressPostalCodeCheck //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceChecks.CvcCheck = cvcCheck //required
            }

    and PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceInstallmentPlan = {
        ///For `fixed_count` installment plans, this is the number of installment payments your customer will make to their credit card.
        Count: int option
        ///Type of installment plan, one of `fixed_count`, `revolving`, or `bonus`.
        Type: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceInstallmentPlanType
    }
    with
        ///For `fixed_count` installment plans, this is the interval between installment payments your customer will make to their credit card. One of `month`.
        member _.Interval = "month"

        static member New (count: int option, ``type``: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceInstallmentPlanType) =
            {
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceInstallmentPlan.Count = count //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceInstallmentPlan.Type = ``type`` //required
            }

    and PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceInstallments = {
        ///Installment plan selected for the payment.
        Plan: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceInstallmentPlan option
    }
    with
        static member New (plan: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceInstallmentPlan option) =
            {
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceInstallments.Plan = plan //required
            }

    and PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecure = {
        ///For authenticated transactions: Indicates how the issuing bank authenticated the customer.
        AuthenticationFlow: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecureAuthenticationFlow option
        ///The 3D Secure cryptogram, also known as the "authentication value" (AAV, CAVV or AEVV).
        Cryptogram: string option
        ///The Electronic Commerce Indicator (ECI). A protocol-level field indicating what degree of authentication was performed.
        ElectronicCommerceIndicator: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecureElectronicCommerceIndicator option
        ///The exemption requested via 3DS and accepted by the issuer at authentication time.
        ExemptionIndicator: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecureExemptionIndicator option
        ///Whether Stripe requested the value of `exemption_indicator` in the transaction. This will depend on the outcome of Stripe's internal risk assessment.
        ExemptionIndicatorApplied: bool option
        ///Indicates the outcome of 3D Secure authentication.
        Result: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecureResult option
        ///Additional information about why 3D Secure succeeded or failed, based on the `result`.
        ResultReason: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecureResultReason option
        ///The version of 3D Secure that was used.
        Version: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecureVersion option
    }
    with
        static member New (authenticationFlow: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecureAuthenticationFlow option, cryptogram: string option, electronicCommerceIndicator: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecureElectronicCommerceIndicator option, exemptionIndicator: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecureExemptionIndicator option, exemptionIndicatorApplied: bool option, result: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecureResult option, resultReason: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecureResultReason option, version: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecureVersion option) =
            {
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecure.AuthenticationFlow = authenticationFlow //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecure.Cryptogram = cryptogram //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecure.ElectronicCommerceIndicator = electronicCommerceIndicator //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecure.ExemptionIndicator = exemptionIndicator //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecure.ExemptionIndicatorApplied = exemptionIndicatorApplied //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecure.Result = result //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecure.ResultReason = resultReason //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceThreeDSecure.Version = version //required
            }

    and PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWallet = {
        ApplePay: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWalletResourceApplePay option
        ///(For tokenized numbers only.) The last four digits of the device account number.
        [<JsonField(Name="dynamic_last4")>]DynamicLast4: string option
        GooglePay: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWalletResourceGooglePay option
        ///The type of the card wallet, one of `apple_pay` or `google_pay`. An additional hash is included on the Wallet subhash with a name matching this value. It contains additional information specific to the card wallet type.
        Type: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWalletType
    }
    with
        static member New (``type``: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWalletType, ?applePay: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWalletResourceApplePay, ?dynamicLast4: string, ?googlePay: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWalletResourceGooglePay) =
            {
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWallet.Type = ``type`` //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWallet.ApplePay = applePay
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWallet.DynamicLast4 = dynamicLast4
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWallet.GooglePay = googlePay
            }

    and PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWalletResourceApplePay = {
        ///Type of the apple_pay transaction, one of `apple_pay` or `apple_pay_later`.
        Type: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWalletResourceApplePayType
    }
    with
        static member New (``type``: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWalletResourceApplePayType) =
            {
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetailsResourceWalletResourceApplePay.Type = ``type`` //required
            }

    ///Details about the Payment Method used in this payment attempt.
    and PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails = {
        AchCreditTransfer: PaymentMethodDetailsAchCreditTransfer option
        AchDebit: PaymentMethodDetailsAchDebit option
        AcssDebit: PaymentMethodDetailsPaymentRecordAcssDebit option
        Affirm: PaymentMethodDetailsPaymentRecordAffirm option
        AfterpayClearpay: PaymentMethodDetailsPaymentRecordAfterpayClearpay option
        Alipay: PaymentFlowsPrivatePaymentMethodsAlipayDetails option
        Alma: PaymentMethodDetailsPaymentRecordAlma option
        AmazonPay: PaymentMethodDetailsPaymentRecordAmazonPay option
        AuBecsDebit: PaymentMethodDetailsAuBecsDebit option
        BacsDebit: PaymentMethodDetailsBacsDebit option
        Bancontact: PaymentMethodDetailsPaymentRecordBancontact option
        Billie: PaymentMethodDetailsPaymentRecordBillie option
        ///The billing details associated with the method of payment.
        BillingDetails: PaymentsPrimitivesPaymentRecordsResourceBillingDetails option
        Blik: PaymentMethodDetailsPaymentRecordBlik option
        Boleto: PaymentMethodDetailsPaymentRecordBoleto option
        Card: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails option
        CardPresent: PaymentMethodDetailsCardPresent option
        Cashapp: PaymentMethodDetailsPaymentRecordCashapp option
        Crypto: PaymentMethodDetailsCrypto option
        Custom: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCustomDetails option
        CustomerBalance: PaymentMethodDetailsCustomerBalance option
        Eps: PaymentMethodDetailsPaymentRecordEps option
        Fpx: PaymentMethodDetailsFpx option
        Giropay: PaymentMethodDetailsPaymentRecordGiropay option
        Grabpay: PaymentMethodDetailsGrabpay option
        Ideal: PaymentMethodDetailsPaymentRecordIdeal option
        InteracPresent: PaymentMethodDetailsInteracPresent option
        KakaoPay: PaymentMethodDetailsPaymentRecordKakaoPay option
        Klarna: PaymentMethodDetailsKlarna option
        Konbini: PaymentMethodDetailsPaymentRecordKonbini option
        KrCard: PaymentMethodDetailsKrCard option
        Link: PaymentMethodDetailsLink option
        MbWay: PaymentMethodDetailsPaymentRecordMbWay option
        Mobilepay: PaymentMethodDetailsPaymentRecordMobilepay option
        Multibanco: PaymentMethodDetailsPaymentRecordMultibanco option
        NaverPay: PaymentMethodDetailsPaymentRecordNaverPay option
        NzBankAccount: PaymentMethodDetailsNzBankAccount option
        Oxxo: PaymentMethodDetailsPaymentRecordOxxo option
        [<JsonField(Name="p24")>]P24: PaymentMethodDetailsP24 option
        PayByBank: PaymentMethodDetailsPaymentRecordPayByBank option
        Payco: PaymentMethodDetailsPaymentRecordPayco option
        ///ID of the Stripe PaymentMethod used to make this payment.
        PaymentMethod: string option
        Paynow: PaymentMethodDetailsPaymentRecordPaynow option
        Paypal: PaymentMethodDetailsPaypal option
        Payto: PaymentMethodDetailsPayto option
        Pix: PaymentMethodDetailsPaymentRecordPix option
        Promptpay: PaymentMethodDetailsPaymentRecordPromptpay option
        RevolutPay: PaymentMethodDetailsRevolutPay option
        SamsungPay: PaymentMethodDetailsPaymentRecordSamsungPay option
        Satispay: PaymentMethodDetailsSatispay option
        SepaCreditTransfer: PaymentMethodDetailsSepaCreditTransfer option
        SepaDebit: PaymentMethodDetailsPaymentRecordSepaDebit option
        Sofort: PaymentMethodDetailsPaymentRecordSofort option
        StripeAccount: PaymentMethodDetailsStripeAccount option
        Sunbit: PaymentMethodDetailsSunbit option
        Swish: PaymentMethodDetailsPaymentRecordSwish option
        Twint: PaymentMethodDetailsPaymentRecordTwint option
        ///The type of transaction-specific details of the payment method used in the payment. See [PaymentMethod.type](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type) for the full list of possible types.
        ///An additional hash is included on `payment_method_details` with a name matching this value.
        ///It contains information specific to the payment method.
        Type: string
        Upi: PaymentMethodDetailsPaymentRecordUpi option
        UsBankAccount: PaymentMethodDetailsPaymentRecordUsBankAccount option
        Wechat: PaymentMethodDetailsWechat option
        WechatPay: PaymentMethodDetailsPaymentRecordWechatPay option
        Zip: PaymentMethodDetailsPaymentRecordZip option
    }
    with
        static member New (billingDetails: PaymentsPrimitivesPaymentRecordsResourceBillingDetails option, paymentMethod: string option, ``type``: string, ?achCreditTransfer: PaymentMethodDetailsAchCreditTransfer, ?achDebit: PaymentMethodDetailsAchDebit, ?acssDebit: PaymentMethodDetailsPaymentRecordAcssDebit, ?affirm: PaymentMethodDetailsPaymentRecordAffirm, ?afterpayClearpay: PaymentMethodDetailsPaymentRecordAfterpayClearpay, ?alipay: PaymentFlowsPrivatePaymentMethodsAlipayDetails, ?alma: PaymentMethodDetailsPaymentRecordAlma, ?amazonPay: PaymentMethodDetailsPaymentRecordAmazonPay, ?auBecsDebit: PaymentMethodDetailsAuBecsDebit, ?bacsDebit: PaymentMethodDetailsBacsDebit, ?bancontact: PaymentMethodDetailsPaymentRecordBancontact, ?billie: PaymentMethodDetailsPaymentRecordBillie, ?blik: PaymentMethodDetailsPaymentRecordBlik, ?boleto: PaymentMethodDetailsPaymentRecordBoleto, ?card: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCardDetails, ?cardPresent: PaymentMethodDetailsCardPresent, ?cashapp: PaymentMethodDetailsPaymentRecordCashapp, ?crypto: PaymentMethodDetailsCrypto, ?custom: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodCustomDetails, ?customerBalance: PaymentMethodDetailsCustomerBalance, ?eps: PaymentMethodDetailsPaymentRecordEps, ?fpx: PaymentMethodDetailsFpx, ?giropay: PaymentMethodDetailsPaymentRecordGiropay, ?grabpay: PaymentMethodDetailsGrabpay, ?ideal: PaymentMethodDetailsPaymentRecordIdeal, ?interacPresent: PaymentMethodDetailsInteracPresent, ?kakaoPay: PaymentMethodDetailsPaymentRecordKakaoPay, ?klarna: PaymentMethodDetailsKlarna, ?konbini: PaymentMethodDetailsPaymentRecordKonbini, ?krCard: PaymentMethodDetailsKrCard, ?link: PaymentMethodDetailsLink, ?mbWay: PaymentMethodDetailsPaymentRecordMbWay, ?mobilepay: PaymentMethodDetailsPaymentRecordMobilepay, ?multibanco: PaymentMethodDetailsPaymentRecordMultibanco, ?naverPay: PaymentMethodDetailsPaymentRecordNaverPay, ?nzBankAccount: PaymentMethodDetailsNzBankAccount, ?oxxo: PaymentMethodDetailsPaymentRecordOxxo, ?p24: PaymentMethodDetailsP24, ?payByBank: PaymentMethodDetailsPaymentRecordPayByBank, ?payco: PaymentMethodDetailsPaymentRecordPayco, ?paynow: PaymentMethodDetailsPaymentRecordPaynow, ?paypal: PaymentMethodDetailsPaypal, ?payto: PaymentMethodDetailsPayto, ?pix: PaymentMethodDetailsPaymentRecordPix, ?promptpay: PaymentMethodDetailsPaymentRecordPromptpay, ?revolutPay: PaymentMethodDetailsRevolutPay, ?samsungPay: PaymentMethodDetailsPaymentRecordSamsungPay, ?satispay: PaymentMethodDetailsSatispay, ?sepaCreditTransfer: PaymentMethodDetailsSepaCreditTransfer, ?sepaDebit: PaymentMethodDetailsPaymentRecordSepaDebit, ?sofort: PaymentMethodDetailsPaymentRecordSofort, ?stripeAccount: PaymentMethodDetailsStripeAccount, ?sunbit: PaymentMethodDetailsSunbit, ?swish: PaymentMethodDetailsPaymentRecordSwish, ?twint: PaymentMethodDetailsPaymentRecordTwint, ?upi: PaymentMethodDetailsPaymentRecordUpi, ?usBankAccount: PaymentMethodDetailsPaymentRecordUsBankAccount, ?wechat: PaymentMethodDetailsWechat, ?wechatPay: PaymentMethodDetailsPaymentRecordWechatPay, ?zip: PaymentMethodDetailsPaymentRecordZip) =
            {
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.BillingDetails = billingDetails //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.PaymentMethod = paymentMethod //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Type = ``type`` //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.AchCreditTransfer = achCreditTransfer
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.AchDebit = achDebit
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.AcssDebit = acssDebit
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Affirm = affirm
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.AfterpayClearpay = afterpayClearpay
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Alipay = alipay
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Alma = alma
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.AmazonPay = amazonPay
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.AuBecsDebit = auBecsDebit
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.BacsDebit = bacsDebit
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Bancontact = bancontact
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Billie = billie
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Blik = blik
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Boleto = boleto
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Card = card
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.CardPresent = cardPresent
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Cashapp = cashapp
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Crypto = crypto
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Custom = custom
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.CustomerBalance = customerBalance
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Eps = eps
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Fpx = fpx
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Giropay = giropay
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Grabpay = grabpay
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Ideal = ideal
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.InteracPresent = interacPresent
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.KakaoPay = kakaoPay
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Klarna = klarna
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Konbini = konbini
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.KrCard = krCard
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Link = link
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.MbWay = mbWay
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Mobilepay = mobilepay
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Multibanco = multibanco
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.NaverPay = naverPay
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.NzBankAccount = nzBankAccount
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Oxxo = oxxo
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.P24 = p24
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.PayByBank = payByBank
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Payco = payco
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Paynow = paynow
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Paypal = paypal
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Payto = payto
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Pix = pix
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Promptpay = promptpay
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.RevolutPay = revolutPay
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.SamsungPay = samsungPay
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Satispay = satispay
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.SepaCreditTransfer = sepaCreditTransfer
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.SepaDebit = sepaDebit
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Sofort = sofort
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.StripeAccount = stripeAccount
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Sunbit = sunbit
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Swish = swish
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Twint = twint
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Upi = upi
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.UsBankAccount = usBankAccount
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Wechat = wechat
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.WechatPay = wechatPay
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodDetails.Zip = zip
            }

    and PaymentsPrimitivesPaymentRecordsResourcePaymentMethodKonbiniDetailsResourceStore = {
        ///The name of the convenience store chain where the payment was completed.
        Chain: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodKonbiniDetailsResourceStoreChain option
    }
    with
        static member New (chain: PaymentsPrimitivesPaymentRecordsResourcePaymentMethodKonbiniDetailsResourceStoreChain option) =
            {
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodKonbiniDetailsResourceStore.Chain = chain //required
            }

    and PaymentsPrimitivesPaymentRecordsResourcePaymentMethodMobilepayDetailsResourceCard = {
        ///Brand of the card used in the transaction
        Brand: string option
        ///Two-letter ISO code representing the country of the card
        Country: IsoTypes.IsoCountryCode option
        ///Two digit number representing the card's expiration month
        ExpMonth: int option
        ///Two digit number representing the card's expiration year
        ExpYear: int option
        ///The last 4 digits of the card
        [<JsonField(Name="last4")>]Last4: string option
    }
    with
        static member New (brand: string option, country: IsoTypes.IsoCountryCode option, expMonth: int option, expYear: int option, last4: string option) =
            {
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodMobilepayDetailsResourceCard.Brand = brand //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodMobilepayDetailsResourceCard.Country = country //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodMobilepayDetailsResourceCard.ExpMonth = expMonth //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodMobilepayDetailsResourceCard.ExpYear = expYear //required
                PaymentsPrimitivesPaymentRecordsResourcePaymentMethodMobilepayDetailsResourceCard.Last4 = last4 //required
            }

    ///Processor information associated with this payment.
    and PaymentsPrimitivesPaymentRecordsResourceProcessorDetails = {
        Custom: PaymentsPrimitivesPaymentRecordsResourceProcessorDetailsResourceCustomDetails option
    }
    with
        ///The processor used for this payment attempt.
        member _.Type = "custom"

        static member New (?custom: PaymentsPrimitivesPaymentRecordsResourceProcessorDetailsResourceCustomDetails) =
            {
                PaymentsPrimitivesPaymentRecordsResourceProcessorDetails.Custom = custom
            }

    ///The customer's shipping information associated with this payment.
    and PaymentsPrimitivesPaymentRecordsResourceShippingDetails = {
        Address: PaymentsPrimitivesPaymentRecordsResourceAddress
        ///The shipping recipient's name.
        Name: string option
        ///The shipping recipient's phone number.
        Phone: string option
    }
    with
        static member New (address: PaymentsPrimitivesPaymentRecordsResourceAddress, name: string option, phone: string option) =
            {
                PaymentsPrimitivesPaymentRecordsResourceShippingDetails.Address = address //required
                PaymentsPrimitivesPaymentRecordsResourceShippingDetails.Name = name //required
                PaymentsPrimitivesPaymentRecordsResourceShippingDetails.Phone = phone //required
            }

    ///A `Payout` object is created when you receive funds from Stripe, or when you
    ///initiate a payout to either a bank account or debit card of a [connected
    ///Stripe account](/docs/connect/bank-debit-card-payouts). You can retrieve individual payouts,
    ///and list all payouts. Payouts are made on [varying
    ///schedules](/docs/connect/manage-payout-schedule), depending on your country and
    ///industry.
    ///Related guide: [Receiving payouts](https://docs.stripe.com/payouts)
    and Payout = {
        ///The amount (in cents (or local equivalent)) that transfers to your bank account or debit card.
        Amount: int
        ///The application fee (if any) for the payout. [See the Connect documentation](https://docs.stripe.com/connect/instant-payouts#monetization-and-fees) for details.
        ApplicationFee: PayoutApplicationFee'AnyOf option
        ///The amount of the application fee (if any) requested for the payout. [See the Connect documentation](https://docs.stripe.com/connect/instant-payouts#monetization-and-fees) for details.
        ApplicationFeeAmount: int option
        ///Date that you can expect the payout to arrive in the bank. This factors in delays to account for weekends or bank holidays.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ArrivalDate: DateTime
        ///Returns `true` if the payout is created by an [automated payout schedule](https://docs.stripe.com/payouts#payout-schedule) and `false` if it's [requested manually](https://stripe.com/docs/payouts#manual-payouts).
        Automatic: bool
        ///ID of the balance transaction that describes the impact of this payout on your account balance.
        BalanceTransaction: PayoutBalanceTransaction'AnyOf option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///ID of the bank account or card the payout is sent to.
        Destination: PayoutDestination'AnyOf option
        ///If the payout fails or cancels, this is the ID of the balance transaction that reverses the initial balance transaction and returns the funds from the failed payout back in your balance.
        FailureBalanceTransaction: PayoutFailureBalanceTransaction'AnyOf option
        ///Error code that provides a reason for a payout failure, if available. View our [list of failure codes](https://docs.stripe.com/api#payout_failures).
        FailureCode: string option
        ///Message that provides the reason for a payout failure, if available.
        FailureMessage: string option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///The method used to send this payout, which can be `standard` or `instant`. `instant` is supported for payouts to debit cards and bank accounts in certain countries. Learn more about [bank support for Instant Payouts](https://stripe.com/docs/payouts/instant-payouts-banks).
        Method: PayoutMethod
        ///If the payout reverses another, this is the ID of the original payout.
        OriginalPayout: PayoutOriginalPayout'AnyOf option
        ///ID of the v2 FinancialAccount the funds are sent to.
        PayoutMethod: string option
        ///If `completed`, you can use the [Balance Transactions API](https://docs.stripe.com/api/balance_transactions/list#balance_transaction_list-payout) to list all balance transactions that are paid out in this payout.
        ReconciliationStatus: PayoutReconciliationStatus
        ///If the payout reverses, this is the ID of the payout that reverses this payout.
        ReversedBy: PayoutReversedBy'AnyOf option
        ///The source balance this payout came from, which can be one of the following: `card`, `fpx`, or `bank_account`.
        SourceType: PayoutSourceType
        ///Extra information about a payout that displays on the user's bank statement.
        StatementDescriptor: string option
        ///Current status of the payout: `paid`, `pending`, `in_transit`, `canceled` or `failed`. A payout is `pending` until it's submitted to the bank, when it becomes `in_transit`. The status changes to `paid` if the transaction succeeds, or to `failed` or `canceled` (within 5 business days). Some payouts that fail might initially show as `paid`, then change to `failed`.
        Status: PayoutStatus
        ///A value that generates from the beneficiary's bank that allows users to track payouts with their bank. Banks might call this a "reference number" or something similar.
        TraceId: PayoutsTraceId option
        ///Can be `bank_account` or `card`.
        Type: PayoutType
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "payout"

        static member New (amount: int, applicationFee: PayoutApplicationFee'AnyOf option, applicationFeeAmount: int option, arrivalDate: DateTime, automatic: bool, balanceTransaction: PayoutBalanceTransaction'AnyOf option, created: DateTime, currency: IsoTypes.IsoCurrencyCode, description: string option, destination: PayoutDestination'AnyOf option, failureBalanceTransaction: PayoutFailureBalanceTransaction'AnyOf option, failureCode: string option, failureMessage: string option, id: string, livemode: bool, metadata: Map<string, string> option, method: PayoutMethod, originalPayout: PayoutOriginalPayout'AnyOf option, payoutMethod: string option, reconciliationStatus: PayoutReconciliationStatus, reversedBy: PayoutReversedBy'AnyOf option, sourceType: PayoutSourceType, statementDescriptor: string option, status: PayoutStatus, traceId: PayoutsTraceId option, ``type``: PayoutType) =
            {
                Payout.Amount = amount //required
                Payout.ApplicationFee = applicationFee //required
                Payout.ApplicationFeeAmount = applicationFeeAmount //required
                Payout.ArrivalDate = arrivalDate //required
                Payout.Automatic = automatic //required
                Payout.BalanceTransaction = balanceTransaction //required
                Payout.Created = created //required
                Payout.Currency = currency //required
                Payout.Description = description //required
                Payout.Destination = destination //required
                Payout.FailureBalanceTransaction = failureBalanceTransaction //required
                Payout.FailureCode = failureCode //required
                Payout.FailureMessage = failureMessage //required
                Payout.Id = id //required
                Payout.Livemode = livemode //required
                Payout.Metadata = metadata //required
                Payout.Method = method //required
                Payout.OriginalPayout = originalPayout //required
                Payout.PayoutMethod = payoutMethod //required
                Payout.ReconciliationStatus = reconciliationStatus //required
                Payout.ReversedBy = reversedBy //required
                Payout.SourceType = sourceType //required
                Payout.StatementDescriptor = statementDescriptor //required
                Payout.Status = status //required
                Payout.TraceId = traceId //required
                Payout.Type = ``type`` //required
            }

    and PayoutApplicationFee'AnyOf =
        | String of string
        | ApplicationFee of ApplicationFee

    and PayoutBalanceTransaction'AnyOf =
        | String of string
        | BalanceTransaction of BalanceTransaction

    and PayoutDestination'AnyOf =
        | String of string
        | ExternalAccount of ExternalAccount
        | DeletedExternalAccount of DeletedExternalAccount

    and PayoutFailureBalanceTransaction'AnyOf =
        | String of string
        | BalanceTransaction of BalanceTransaction

    and PayoutOriginalPayout'AnyOf =
        | String of string
        | Payout of Payout

    and PayoutReversedBy'AnyOf =
        | String of string
        | Payout of Payout

    and PayoutsTraceId = {
        ///Possible values are `pending`, `supported`, and `unsupported`. When `payout.status` is `pending` or `in_transit`, this will be `pending`. When the payout transitions to `paid`, `failed`, or `canceled`, this status will become `supported` or `unsupported` shortly after in most cases. In some cases, this may appear as `pending` for up to 10 days after `arrival_date` until transitioning to `supported` or `unsupported`.
        Status: PayoutsTraceIdStatus
        ///The trace ID value if `trace_id.status` is `supported`, otherwise `nil`.
        Value: string option
    }
    with
        static member New (status: PayoutsTraceIdStatus, value: string option) =
            {
                PayoutsTraceId.Status = status //required
                PayoutsTraceId.Value = value //required
            }

    and PaypalSellerProtection = {
        ///An array of conditions that are covered for the transaction, if applicable.
        DisputeCategories: PaypalSellerProtectionDisputeCategories list option
        ///Indicates whether the transaction is eligible for PayPal's seller protection.
        Status: PaypalSellerProtectionStatus
    }
    with
        static member New (disputeCategories: PaypalSellerProtectionDisputeCategories list option, status: PaypalSellerProtectionStatus) =
            {
                PaypalSellerProtection.DisputeCategories = disputeCategories //required
                PaypalSellerProtection.Status = status //required
            }

    ///This is an object representing a person associated with a Stripe account.
    ///A platform can only access a subset of data in a person for an account where [account.controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `stripe`, which includes Standard and Express accounts, after creating an Account Link or Account Session to start Connect onboarding.
    ///See the [Standard onboarding](/connect/standard-accounts) or [Express onboarding](/connect/express-accounts) documentation for information about prefilling information and account onboarding steps. Learn more about [handling identity verification with the API](/connect/handling-api-verification#person-information).
    and Person = {
        ///The account the person is associated with.
        Account: string option
        AdditionalTosAcceptances: PersonAdditionalTosAcceptances option
        Address: Address option
        ///The Kana variation of the person's address (Japan only).
        AddressKana: LegalEntityJapanAddress option
        ///The Kanji variation of the person's address (Japan only).
        AddressKanji: LegalEntityJapanAddress option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        Dob: LegalEntityDob option
        ///The person's email address. Also available for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `stripe`.
        Email: string option
        ///The person's first name. Also available for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `stripe`.
        FirstName: string option
        ///The Kana variation of the person's first name (Japan only). Also available for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `stripe`.
        FirstNameKana: string option
        ///The Kanji variation of the person's first name (Japan only). Also available for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `stripe`.
        FirstNameKanji: string option
        ///A list of alternate names or aliases that the person is known by. Also available for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `stripe`.
        FullNameAliases: string list option
        ///Information about the [upcoming new requirements for this person](https://docs.stripe.com/connect/custom-accounts/future-requirements), including what information needs to be collected, and by when.
        FutureRequirements: PersonFutureRequirements option
        ///The person's gender.
        Gender: string option
        ///Unique identifier for the object.
        Id: string
        ///Whether the person's `id_number` was provided. True if either the full ID number was provided or if only the required part of the ID number was provided (ex. last four of an individual's SSN for the US indicated by `ssn_last_4_provided`).
        IdNumberProvided: bool option
        ///Whether the person's `id_number_secondary` was provided.
        IdNumberSecondaryProvided: bool option
        ///The person's last name. Also available for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `stripe`.
        LastName: string option
        ///The Kana variation of the person's last name (Japan only). Also available for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `stripe`.
        LastNameKana: string option
        ///The Kanji variation of the person's last name (Japan only). Also available for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `stripe`.
        LastNameKanji: string option
        ///The person's maiden name.
        MaidenName: string option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///The country where the person is a national.
        Nationality: string option
        ///The person's phone number.
        Phone: string option
        ///Indicates if the person or any of their representatives, family members, or other closely related persons, declares that they hold or have held an important public job or function, in any jurisdiction.
        PoliticalExposure: PersonPoliticalExposure option
        RegisteredAddress: Address option
        Relationship: PersonRelationship option
        ///Information about the requirements for this person, including what information needs to be collected, and by when.
        Requirements: PersonRequirements option
        ///Whether the last four digits of the person's Social Security number have been provided (U.S. only).
        SsnLast4Provided: bool option
        ///Demographic data related to the person.
        UsCfpbData: PersonUsCfpbData option
        Verification: LegalEntityPersonVerification option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "person"

        static member New (created: DateTime, id: string, ?account: string, ?additionalTosAcceptances: PersonAdditionalTosAcceptances, ?address: Address, ?addressKana: LegalEntityJapanAddress option, ?addressKanji: LegalEntityJapanAddress option, ?dob: LegalEntityDob, ?email: string option, ?firstName: string option, ?firstNameKana: string option, ?firstNameKanji: string option, ?fullNameAliases: string list, ?futureRequirements: PersonFutureRequirements option, ?gender: string option, ?idNumberProvided: bool, ?idNumberSecondaryProvided: bool, ?lastName: string option, ?lastNameKana: string option, ?lastNameKanji: string option, ?maidenName: string option, ?metadata: Map<string, string>, ?nationality: string option, ?phone: string option, ?politicalExposure: PersonPoliticalExposure, ?registeredAddress: Address, ?relationship: PersonRelationship, ?requirements: PersonRequirements option, ?ssnLast4Provided: bool, ?usCfpbData: PersonUsCfpbData option, ?verification: LegalEntityPersonVerification) =
            {
                Person.Created = created //required
                Person.Id = id //required
                Person.Account = account
                Person.AdditionalTosAcceptances = additionalTosAcceptances
                Person.Address = address
                Person.AddressKana = addressKana |> Option.flatten
                Person.AddressKanji = addressKanji |> Option.flatten
                Person.Dob = dob
                Person.Email = email |> Option.flatten
                Person.FirstName = firstName |> Option.flatten
                Person.FirstNameKana = firstNameKana |> Option.flatten
                Person.FirstNameKanji = firstNameKanji |> Option.flatten
                Person.FullNameAliases = fullNameAliases
                Person.FutureRequirements = futureRequirements |> Option.flatten
                Person.Gender = gender |> Option.flatten
                Person.IdNumberProvided = idNumberProvided
                Person.IdNumberSecondaryProvided = idNumberSecondaryProvided
                Person.LastName = lastName |> Option.flatten
                Person.LastNameKana = lastNameKana |> Option.flatten
                Person.LastNameKanji = lastNameKanji |> Option.flatten
                Person.MaidenName = maidenName |> Option.flatten
                Person.Metadata = metadata
                Person.Nationality = nationality |> Option.flatten
                Person.Phone = phone |> Option.flatten
                Person.PoliticalExposure = politicalExposure
                Person.RegisteredAddress = registeredAddress
                Person.Relationship = relationship
                Person.Requirements = requirements |> Option.flatten
                Person.SsnLast4Provided = ssnLast4Provided
                Person.UsCfpbData = usCfpbData |> Option.flatten
                Person.Verification = verification
            }

    and PersonAdditionalTosAcceptances = {
        ///Details on the legal guardian's acceptance of the main Stripe service agreement.
        Account: PersonAdditionalTosAcceptance option
    }
    with
        static member New (account: PersonAdditionalTosAcceptance option) =
            {
                PersonAdditionalTosAcceptances.Account = account //required
            }

    and PersonEthnicityDetails = {
        ///The persons ethnicity
        Ethnicity: PersonEthnicityDetailsEthnicity list option
        ///Please specify your origin, when other is selected.
        EthnicityOther: string option
    }
    with
        static member New (ethnicity: PersonEthnicityDetailsEthnicity list option, ethnicityOther: string option) =
            {
                PersonEthnicityDetails.Ethnicity = ethnicity //required
                PersonEthnicityDetails.EthnicityOther = ethnicityOther //required
            }

    and PersonFutureRequirements = {
        ///Fields that are due and can be resolved by providing the corresponding alternative fields instead. Many alternatives can list the same `original_fields_due`, and any of these alternatives can serve as a pathway for attempting to resolve the fields again. Re-providing `original_fields_due` also serves as a pathway for attempting to resolve the fields again.
        Alternatives: AccountRequirementsAlternative list option
        ///Fields that need to be resolved to keep the person's account enabled. If not resolved by the account's `future_requirements[current_deadline]`, these fields will transition to the main `requirements` hash, and may immediately become `past_due`, but the account may also be given a grace period depending on the account's enablement state prior to transition.
        CurrentlyDue: string list
        ///Details about validation and verification failures for `due` requirements that must be resolved.
        Errors: AccountRequirementsError list
        ///Fields you must collect when all thresholds are reached. As they become required, they appear in `currently_due` as well, and the account's `future_requirements[current_deadline]` becomes set.
        EventuallyDue: string list
        ///Fields that haven't been resolved by the account's `requirements.current_deadline`. These fields need to be resolved to enable the person's account. `future_requirements.past_due` is a subset of `requirements.past_due`.
        PastDue: string list
        ///Fields that are being reviewed, or might become required depending on the results of a review. If the review fails, these fields can move to `eventually_due`, `currently_due`, `past_due` or `alternatives`. Fields might appear in `eventually_due`, `currently_due`, `past_due` or `alternatives` and in `pending_verification` if one verification fails but another is still pending.
        PendingVerification: string list
    }
    with
        static member New (alternatives: AccountRequirementsAlternative list option, currentlyDue: string list, errors: AccountRequirementsError list, eventuallyDue: string list, pastDue: string list, pendingVerification: string list) =
            {
                PersonFutureRequirements.Alternatives = alternatives //required
                PersonFutureRequirements.CurrentlyDue = currentlyDue //required
                PersonFutureRequirements.Errors = errors //required
                PersonFutureRequirements.EventuallyDue = eventuallyDue //required
                PersonFutureRequirements.PastDue = pastDue //required
                PersonFutureRequirements.PendingVerification = pendingVerification //required
            }

    and PersonRaceDetails = {
        ///The persons race.
        Race: PersonRaceDetailsRace list option
        ///Please specify your race, when other is selected.
        RaceOther: string option
    }
    with
        static member New (race: PersonRaceDetailsRace list option, raceOther: string option) =
            {
                PersonRaceDetails.Race = race //required
                PersonRaceDetails.RaceOther = raceOther //required
            }

    and PersonRequirements = {
        ///Fields that are due and can be resolved by providing the corresponding alternative fields instead. Many alternatives can list the same `original_fields_due`, and any of these alternatives can serve as a pathway for attempting to resolve the fields again. Re-providing `original_fields_due` also serves as a pathway for attempting to resolve the fields again.
        Alternatives: AccountRequirementsAlternative list option
        ///Fields that need to be resolved to keep the person's account enabled. If not resolved by the account's `current_deadline`, these fields will appear in `past_due` as well, and the account is disabled.
        CurrentlyDue: string list
        ///Details about validation and verification failures for `due` requirements that must be resolved.
        Errors: AccountRequirementsError list
        ///Fields you must collect when all thresholds are reached. As they become required, they appear in `currently_due` as well, and the account's `current_deadline` becomes set.
        EventuallyDue: string list
        ///Fields that haven't been resolved by `current_deadline`. These fields need to be resolved to enable the person's account.
        PastDue: string list
        ///Fields that are being reviewed, or might become required depending on the results of a review. If the review fails, these fields can move to `eventually_due`, `currently_due`, `past_due` or `alternatives`. Fields might appear in `eventually_due`, `currently_due`, `past_due` or `alternatives` and in `pending_verification` if one verification fails but another is still pending.
        PendingVerification: string list
    }
    with
        static member New (alternatives: AccountRequirementsAlternative list option, currentlyDue: string list, errors: AccountRequirementsError list, eventuallyDue: string list, pastDue: string list, pendingVerification: string list) =
            {
                PersonRequirements.Alternatives = alternatives //required
                PersonRequirements.CurrentlyDue = currentlyDue //required
                PersonRequirements.Errors = errors //required
                PersonRequirements.EventuallyDue = eventuallyDue //required
                PersonRequirements.PastDue = pastDue //required
                PersonRequirements.PendingVerification = pendingVerification //required
            }

    and PersonUsCfpbData = {
        ///The persons ethnicity details
        EthnicityDetails: PersonEthnicityDetails option
        ///The persons race details
        RaceDetails: PersonRaceDetails option
        ///The persons self-identified gender
        SelfIdentifiedGender: string option
    }
    with
        static member New (ethnicityDetails: PersonEthnicityDetails option, raceDetails: PersonRaceDetails option, selfIdentifiedGender: string option) =
            {
                PersonUsCfpbData.EthnicityDetails = ethnicityDetails //required
                PersonUsCfpbData.RaceDetails = raceDetails //required
                PersonUsCfpbData.SelfIdentifiedGender = selfIdentifiedGender //required
            }

    ///You can now model subscriptions more flexibly using the [Prices API](https://api.stripe.com#prices). It replaces the Plans API and is backwards compatible to simplify your migration.
    ///Plans define the base price, currency, and billing cycle for recurring purchases of products.
    ///[Products](https://api.stripe.com#products) help you track inventory or provisioning, and plans help you track pricing. Different physical goods or levels of service should be represented by products, and pricing options should be represented by plans. This approach lets you change prices without having to change your provisioning scheme.
    ///For example, you might have a single "gold" product that has plans for $10/month, $100/year, €9/month, and €90/year.
    ///Related guides: [Set up a subscription](https://docs.stripe.com/billing/subscriptions/set-up-subscription) and more about [products and prices](https://docs.stripe.com/products-prices/overview).
    and Plan = {
        ///Whether the plan can be used for new purchases.
        Active: bool
        ///The unit amount in cents (or local equivalent) to be charged, represented as a whole integer if possible. Only set if `billing_scheme=per_unit`.
        Amount: int option
        ///The unit amount in cents (or local equivalent) to be charged, represented as a decimal string with at most 12 decimal places. Only set if `billing_scheme=per_unit`.
        AmountDecimal: string option
        ///Describes how to compute the price per period. Either `per_unit` or `tiered`. `per_unit` indicates that the fixed amount (specified in `amount`) will be charged per unit in `quantity` (for plans with `usage_type=licensed`), or per unit of total usage (for plans with `usage_type=metered`). `tiered` indicates that the unit pricing will be computed using a tiering strategy as defined using the `tiers` and `tiers_mode` attributes.
        BillingScheme: PlanBillingScheme
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///Unique identifier for the object.
        Id: string
        ///The frequency at which a subscription is billed. One of `day`, `week`, `month` or `year`.
        Interval: PlanInterval
        ///The number of intervals (specified in the `interval` attribute) between subscription billings. For example, `interval=month` and `interval_count=3` bills every 3 months.
        IntervalCount: int
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///The meter tracking the usage of a metered price
        Meter: string option
        ///A brief description of the plan, hidden from customers.
        Nickname: string option
        ///The product whose pricing this plan determines.
        Product: PlanProduct'AnyOf option
        ///Each element represents a pricing tier. This parameter requires `billing_scheme` to be set to `tiered`. See also the documentation for `billing_scheme`.
        Tiers: PlanTier list option
        ///Defines if the tiering price should be `graduated` or `volume` based. In `volume`-based tiering, the maximum quantity within a period determines the per unit price. In `graduated` tiering, pricing can change as the quantity grows.
        TiersMode: PlanTiersMode option
        ///Apply a transformation to the reported usage or set quantity before computing the amount billed. Cannot be combined with `tiers`.
        TransformUsage: TransformUsage option
        ///Default number of trial days when subscribing a customer to this plan using [`trial_from_plan=true`](https://docs.stripe.com/api#create_subscription-trial_from_plan).
        TrialPeriodDays: int option
        ///Configures how the quantity per period should be determined. Can be either `metered` or `licensed`. `licensed` automatically bills the `quantity` set when adding it to a subscription. `metered` aggregates the total usage based on usage records. Defaults to `licensed`.
        UsageType: PlanUsageType
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "plan"

        static member New (active: bool, amount: int option, amountDecimal: string option, billingScheme: PlanBillingScheme, created: DateTime, currency: IsoTypes.IsoCurrencyCode, id: string, interval: PlanInterval, intervalCount: int, livemode: bool, metadata: Map<string, string> option, meter: string option, nickname: string option, product: PlanProduct'AnyOf option, tiersMode: PlanTiersMode option, transformUsage: TransformUsage option, trialPeriodDays: int option, usageType: PlanUsageType, ?tiers: PlanTier list) =
            {
                Plan.Active = active //required
                Plan.Amount = amount //required
                Plan.AmountDecimal = amountDecimal //required
                Plan.BillingScheme = billingScheme //required
                Plan.Created = created //required
                Plan.Currency = currency //required
                Plan.Id = id //required
                Plan.Interval = interval //required
                Plan.IntervalCount = intervalCount //required
                Plan.Livemode = livemode //required
                Plan.Metadata = metadata //required
                Plan.Meter = meter //required
                Plan.Nickname = nickname //required
                Plan.Product = product //required
                Plan.TiersMode = tiersMode //required
                Plan.TransformUsage = transformUsage //required
                Plan.TrialPeriodDays = trialPeriodDays //required
                Plan.UsageType = usageType //required
                Plan.Tiers = tiers
            }

    and PlanProduct'AnyOf =
        | String of string
        | Product of Product
        | DeletedProduct of DeletedProduct

    and PlatformEarningFeeSource = {
        ///Charge ID that created this application fee.
        Charge: string option
        ///Payout ID that created this application fee.
        Payout: string option
        ///Type of object that created the application fee.
        Type: PlatformEarningFeeSourceType
    }
    with
        static member New (``type``: PlatformEarningFeeSourceType, ?charge: string, ?payout: string) =
            {
                PlatformEarningFeeSource.Type = ``type`` //required
                PlatformEarningFeeSource.Charge = charge
                PlatformEarningFeeSource.Payout = payout
            }

    and PortalCustomerUpdate = {
        ///The types of customer updates that are supported. When empty, customers are not updateable.
        AllowedUpdates: PortalCustomerUpdateAllowedUpdates list
        ///Whether the feature is enabled.
        Enabled: bool
    }
    with
        static member New (allowedUpdates: PortalCustomerUpdateAllowedUpdates list, enabled: bool) =
            {
                PortalCustomerUpdate.AllowedUpdates = allowedUpdates //required
                PortalCustomerUpdate.Enabled = enabled //required
            }

    and PortalFeatures = {
        CustomerUpdate: PortalCustomerUpdate
        InvoiceHistory: PortalInvoiceList
        PaymentMethodUpdate: PortalPaymentMethodUpdate
        SubscriptionCancel: PortalSubscriptionCancel
        SubscriptionUpdate: PortalSubscriptionUpdate
    }
    with
        static member New (customerUpdate: PortalCustomerUpdate, invoiceHistory: PortalInvoiceList, paymentMethodUpdate: PortalPaymentMethodUpdate, subscriptionCancel: PortalSubscriptionCancel, subscriptionUpdate: PortalSubscriptionUpdate) =
            {
                PortalFeatures.CustomerUpdate = customerUpdate //required
                PortalFeatures.InvoiceHistory = invoiceHistory //required
                PortalFeatures.PaymentMethodUpdate = paymentMethodUpdate //required
                PortalFeatures.SubscriptionCancel = subscriptionCancel //required
                PortalFeatures.SubscriptionUpdate = subscriptionUpdate //required
            }

    and PortalFlowsFlow = {
        AfterCompletion: PortalFlowsFlowAfterCompletion
        ///Configuration when `flow.type=subscription_cancel`.
        SubscriptionCancel: PortalFlowsFlowSubscriptionCancel option
        ///Configuration when `flow.type=subscription_update`.
        SubscriptionUpdate: PortalFlowsFlowSubscriptionUpdate option
        ///Configuration when `flow.type=subscription_update_confirm`.
        SubscriptionUpdateConfirm: PortalFlowsFlowSubscriptionUpdateConfirm option
        ///Type of flow that the customer will go through.
        Type: PortalFlowsFlowType
    }
    with
        static member New (afterCompletion: PortalFlowsFlowAfterCompletion, subscriptionCancel: PortalFlowsFlowSubscriptionCancel option, subscriptionUpdate: PortalFlowsFlowSubscriptionUpdate option, subscriptionUpdateConfirm: PortalFlowsFlowSubscriptionUpdateConfirm option, ``type``: PortalFlowsFlowType) =
            {
                PortalFlowsFlow.AfterCompletion = afterCompletion //required
                PortalFlowsFlow.SubscriptionCancel = subscriptionCancel //required
                PortalFlowsFlow.SubscriptionUpdate = subscriptionUpdate //required
                PortalFlowsFlow.SubscriptionUpdateConfirm = subscriptionUpdateConfirm //required
                PortalFlowsFlow.Type = ``type`` //required
            }

    and PortalFlowsFlowAfterCompletion = {
        ///Configuration when `after_completion.type=hosted_confirmation`.
        HostedConfirmation: PortalFlowsAfterCompletionHostedConfirmation option
        ///Configuration when `after_completion.type=redirect`.
        Redirect: PortalFlowsAfterCompletionRedirect option
        ///The specified type of behavior after the flow is completed.
        Type: PortalFlowsFlowAfterCompletionType
    }
    with
        static member New (hostedConfirmation: PortalFlowsAfterCompletionHostedConfirmation option, redirect: PortalFlowsAfterCompletionRedirect option, ``type``: PortalFlowsFlowAfterCompletionType) =
            {
                PortalFlowsFlowAfterCompletion.HostedConfirmation = hostedConfirmation //required
                PortalFlowsFlowAfterCompletion.Redirect = redirect //required
                PortalFlowsFlowAfterCompletion.Type = ``type`` //required
            }

    and PortalFlowsFlowSubscriptionCancel = {
        ///Specify a retention strategy to be used in the cancellation flow.
        Retention: PortalFlowsRetention option
        ///The ID of the subscription to be canceled.
        Subscription: string
    }
    with
        static member New (retention: PortalFlowsRetention option, subscription: string) =
            {
                PortalFlowsFlowSubscriptionCancel.Retention = retention //required
                PortalFlowsFlowSubscriptionCancel.Subscription = subscription //required
            }

    and PortalFlowsFlowSubscriptionUpdateConfirm = {
        ///The coupon or promotion code to apply to this subscription update.
        Discounts: PortalFlowsSubscriptionUpdateConfirmDiscount list option
        ///The [subscription item](https://docs.stripe.com/api/subscription_items) to be updated through this flow. Currently, only up to one may be specified and subscriptions with multiple items are not updatable.
        Items: PortalFlowsSubscriptionUpdateConfirmItem list
        ///The ID of the subscription to be updated.
        Subscription: string
    }
    with
        static member New (discounts: PortalFlowsSubscriptionUpdateConfirmDiscount list option, items: PortalFlowsSubscriptionUpdateConfirmItem list, subscription: string) =
            {
                PortalFlowsFlowSubscriptionUpdateConfirm.Discounts = discounts //required
                PortalFlowsFlowSubscriptionUpdateConfirm.Items = items //required
                PortalFlowsFlowSubscriptionUpdateConfirm.Subscription = subscription //required
            }

    and PortalFlowsRetention = {
        ///Configuration when `retention.type=coupon_offer`.
        CouponOffer: PortalFlowsCouponOffer option
    }
    with
        ///Type of retention strategy that will be used.
        member _.Type = "coupon_offer"

        static member New (couponOffer: PortalFlowsCouponOffer option) =
            {
                PortalFlowsRetention.CouponOffer = couponOffer //required
            }

    and PortalResourceScheduleUpdateAtPeriodEnd = {
        ///List of conditions. When any condition is true, an update will be scheduled at the end of the current period.
        Conditions: PortalResourceScheduleUpdateAtPeriodEndCondition list
    }
    with
        static member New (conditions: PortalResourceScheduleUpdateAtPeriodEndCondition list) =
            {
                PortalResourceScheduleUpdateAtPeriodEnd.Conditions = conditions //required
            }

    and PortalResourceScheduleUpdateAtPeriodEndCondition = {
        ///The type of condition.
        Type: PortalResourceScheduleUpdateAtPeriodEndConditionType
    }
    with
        static member New (``type``: PortalResourceScheduleUpdateAtPeriodEndConditionType) =
            {
                PortalResourceScheduleUpdateAtPeriodEndCondition.Type = ``type`` //required
            }

    and PortalSubscriptionCancel = {
        CancellationReason: PortalSubscriptionCancellationReason
        ///Whether the feature is enabled.
        Enabled: bool
        ///Whether to cancel subscriptions immediately or at the end of the billing period.
        Mode: PortalSubscriptionCancelMode
        ///Whether to create prorations when canceling subscriptions. Possible values are `none` and `create_prorations`.
        ProrationBehavior: PortalSubscriptionCancelProrationBehavior
    }
    with
        static member New (cancellationReason: PortalSubscriptionCancellationReason, enabled: bool, mode: PortalSubscriptionCancelMode, prorationBehavior: PortalSubscriptionCancelProrationBehavior) =
            {
                PortalSubscriptionCancel.CancellationReason = cancellationReason //required
                PortalSubscriptionCancel.Enabled = enabled //required
                PortalSubscriptionCancel.Mode = mode //required
                PortalSubscriptionCancel.ProrationBehavior = prorationBehavior //required
            }

    and PortalSubscriptionCancellationReason = {
        ///Whether the feature is enabled.
        Enabled: bool
        ///Which cancellation reasons will be given as options to the customer.
        Options: PortalSubscriptionCancellationReasonOptions list
    }
    with
        static member New (enabled: bool, options: PortalSubscriptionCancellationReasonOptions list) =
            {
                PortalSubscriptionCancellationReason.Enabled = enabled //required
                PortalSubscriptionCancellationReason.Options = options //required
            }

    and PortalSubscriptionUpdate = {
        ///Determines the value to use for the billing cycle anchor on subscription updates. Valid values are `now` or `unchanged`, and the default value is `unchanged`. Setting the value to `now` resets the subscription's billing cycle anchor to the current time (in UTC). For more information, see the billing cycle [documentation](https://docs.stripe.com/billing/subscriptions/billing-cycle).
        BillingCycleAnchor: PortalSubscriptionUpdateBillingCycleAnchor option
        ///The types of subscription updates that are supported for items listed in the `products` attribute. When empty, subscriptions are not updateable.
        DefaultAllowedUpdates: PortalSubscriptionUpdateDefaultAllowedUpdates list
        ///Whether the feature is enabled.
        Enabled: bool
        ///The list of up to 10 products that support subscription updates.
        Products: PortalSubscriptionUpdateProduct list option
        ///Determines how to handle prorations resulting from subscription updates. Valid values are `none`, `create_prorations`, and `always_invoice`. Defaults to a value of `none` if you don't set it during creation.
        ProrationBehavior: PortalSubscriptionUpdateProrationBehavior
        ScheduleAtPeriodEnd: PortalResourceScheduleUpdateAtPeriodEnd
        ///Determines how handle updates to trialing subscriptions. Valid values are `end_trial` and `continue_trial`. Defaults to a value of `end_trial` if you don't set it during creation.
        TrialUpdateBehavior: PortalSubscriptionUpdateTrialUpdateBehavior
    }
    with
        static member New (billingCycleAnchor: PortalSubscriptionUpdateBillingCycleAnchor option, defaultAllowedUpdates: PortalSubscriptionUpdateDefaultAllowedUpdates list, enabled: bool, prorationBehavior: PortalSubscriptionUpdateProrationBehavior, scheduleAtPeriodEnd: PortalResourceScheduleUpdateAtPeriodEnd, trialUpdateBehavior: PortalSubscriptionUpdateTrialUpdateBehavior, ?products: PortalSubscriptionUpdateProduct list option) =
            {
                PortalSubscriptionUpdate.BillingCycleAnchor = billingCycleAnchor //required
                PortalSubscriptionUpdate.DefaultAllowedUpdates = defaultAllowedUpdates //required
                PortalSubscriptionUpdate.Enabled = enabled //required
                PortalSubscriptionUpdate.ProrationBehavior = prorationBehavior //required
                PortalSubscriptionUpdate.ScheduleAtPeriodEnd = scheduleAtPeriodEnd //required
                PortalSubscriptionUpdate.TrialUpdateBehavior = trialUpdateBehavior //required
                PortalSubscriptionUpdate.Products = products |> Option.flatten
            }

    and PortalSubscriptionUpdateProduct = {
        AdjustableQuantity: PortalSubscriptionUpdateProductAdjustableQuantity
        ///The list of price IDs which, when subscribed to, a subscription can be updated.
        Prices: string list
        ///The product ID.
        Product: string
    }
    with
        static member New (adjustableQuantity: PortalSubscriptionUpdateProductAdjustableQuantity, prices: string list, product: string) =
            {
                PortalSubscriptionUpdateProduct.AdjustableQuantity = adjustableQuantity //required
                PortalSubscriptionUpdateProduct.Prices = prices //required
                PortalSubscriptionUpdateProduct.Product = product //required
            }

    ///Prices define the unit cost, currency, and (optional) billing cycle for both recurring and one-time purchases of products.
    ///[Products](https://api.stripe.com#products) help you track inventory or provisioning, and prices help you track payment terms. Different physical goods or levels of service should be represented by products, and pricing options should be represented by prices. This approach lets you change prices without having to change your provisioning scheme.
    ///For example, you might have a single "gold" product that has prices for $10/month, $100/year, and €9 once.
    ///Related guides: [Set up a subscription](https://docs.stripe.com/billing/subscriptions/set-up-subscription), [create an invoice](https://docs.stripe.com/billing/invoices/create), and more about [products and prices](https://docs.stripe.com/products-prices/overview).
    and Price = {
        ///Whether the price can be used for new purchases.
        Active: bool
        ///Describes how to compute the price per period. Either `per_unit` or `tiered`. `per_unit` indicates that the fixed amount (specified in `unit_amount` or `unit_amount_decimal`) will be charged per unit in `quantity` (for prices with `usage_type=licensed`), or per unit of total usage (for prices with `usage_type=metered`). `tiered` indicates that the unit pricing will be computed using a tiering strategy as defined using the `tiers` and `tiers_mode` attributes.
        BillingScheme: PriceBillingScheme
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///Prices defined in each available currency option. Each key must be a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html) and a [supported currency](https://stripe.com/docs/currencies).
        CurrencyOptions: Map<string, string list> option
        ///When set, provides configuration for the amount to be adjusted by the customer during Checkout Sessions and Payment Links.
        CustomUnitAmount: CustomUnitAmount option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///A lookup key used to retrieve prices dynamically from a static string. This may be up to 200 characters.
        LookupKey: string option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///A brief description of the price, hidden from customers.
        Nickname: string option
        ///The ID of the product this price is associated with.
        Product: PriceProduct'AnyOf
        ///The recurring components of a price such as `interval` and `usage_type`.
        Recurring: Recurring option
        ///Only required if a [default tax behavior](https://docs.stripe.com/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed.
        TaxBehavior: PriceTaxBehavior option
        ///Each element represents a pricing tier. This parameter requires `billing_scheme` to be set to `tiered`. See also the documentation for `billing_scheme`.
        Tiers: PriceTier list option
        ///Defines if the tiering price should be `graduated` or `volume` based. In `volume`-based tiering, the maximum quantity within a period determines the per unit price. In `graduated` tiering, pricing can change as the quantity grows.
        TiersMode: PriceTiersMode option
        ///Apply a transformation to the reported usage or set quantity before computing the amount billed. Cannot be combined with `tiers`.
        TransformQuantity: TransformQuantity option
        ///One of `one_time` or `recurring` depending on whether the price is for a one-time purchase or a recurring (subscription) purchase.
        Type: PriceType
        ///The unit amount in cents (or local equivalent) to be charged, represented as a whole integer if possible. Only set if `billing_scheme=per_unit`.
        UnitAmount: int option
        ///The unit amount in cents (or local equivalent) to be charged, represented as a decimal string with at most 12 decimal places. Only set if `billing_scheme=per_unit`.
        UnitAmountDecimal: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "price"

        static member New (active: bool, billingScheme: PriceBillingScheme, created: DateTime, currency: IsoTypes.IsoCurrencyCode, customUnitAmount: CustomUnitAmount option, id: string, livemode: bool, lookupKey: string option, metadata: Map<string, string>, nickname: string option, product: PriceProduct'AnyOf, recurring: Recurring option, taxBehavior: PriceTaxBehavior option, tiersMode: PriceTiersMode option, transformQuantity: TransformQuantity option, ``type``: PriceType, unitAmount: int option, unitAmountDecimal: string option, ?currencyOptions: Map<string, string list>, ?tiers: PriceTier list) =
            {
                Price.Active = active //required
                Price.BillingScheme = billingScheme //required
                Price.Created = created //required
                Price.Currency = currency //required
                Price.CustomUnitAmount = customUnitAmount //required
                Price.Id = id //required
                Price.Livemode = livemode //required
                Price.LookupKey = lookupKey //required
                Price.Metadata = metadata //required
                Price.Nickname = nickname //required
                Price.Product = product //required
                Price.Recurring = recurring //required
                Price.TaxBehavior = taxBehavior //required
                Price.TiersMode = tiersMode //required
                Price.TransformQuantity = transformQuantity //required
                Price.Type = ``type`` //required
                Price.UnitAmount = unitAmount //required
                Price.UnitAmountDecimal = unitAmountDecimal //required
                Price.CurrencyOptions = currencyOptions
                Price.Tiers = tiers
            }

    and PriceProduct'AnyOf =
        | String of string
        | Product of Product
        | DeletedProduct of DeletedProduct

    ///Products describe the specific goods or services you offer to your customers.
    ///For example, you might offer a Standard and Premium version of your goods or service; each version would be a separate Product.
    ///They can be used in conjunction with [Prices](https://api.stripe.com#prices) to configure pricing in Payment Links, Checkout, and Subscriptions.
    ///Related guides: [Set up a subscription](https://docs.stripe.com/billing/subscriptions/set-up-subscription),
    ///[share a Payment Link](https://docs.stripe.com/payment-links),
    ///[accept payments with Checkout](https://docs.stripe.com/payments/accept-a-payment#create-product-prices-upfront),
    ///and more about [Products and Prices](https://docs.stripe.com/products-prices/overview)
    and Product = {
        ///Whether the product is currently available for purchase.
        Active: bool
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The ID of the [Price](https://docs.stripe.com/api/prices) object that is the default price for this product.
        DefaultPrice: ProductDefaultPrice'AnyOf option
        ///The product's description, meant to be displayable to the customer. Use this field to optionally store a long form explanation of the product being sold for your own rendering purposes.
        Description: string option
        ///Unique identifier for the object.
        Id: string
        ///A list of up to 8 URLs of images for this product, meant to be displayable to the customer.
        Images: string list
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///A list of up to 15 marketing features for this product. These are displayed in [pricing tables](https://docs.stripe.com/payments/checkout/pricing-table).
        MarketingFeatures: ProductMarketingFeature list
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///The product's name, meant to be displayable to the customer.
        Name: string
        ///The dimensions of this product for shipping purposes.
        PackageDimensions: PackageDimensions option
        ///Whether this product is shipped (i.e., physical goods).
        Shippable: bool option
        ///Extra information about a product which will appear on your customer's credit card statement. In the case that multiple products are billed at once, the first statement descriptor will be used. Only used for subscription payments.
        StatementDescriptor: string option
        ///A [tax code](https://docs.stripe.com/tax/tax-categories) ID.
        TaxCode: ProductTaxCode'AnyOf option
        ///The type of the product. The product is either of type `good`, which is eligible for use with Orders and SKUs, or `service`, which is eligible for use with Subscriptions and Plans.
        Type: ProductType
        ///A label that represents units of this product. When set, this will be included in customers' receipts, invoices, Checkout, and the customer portal.
        UnitLabel: string option
        ///Time at which the object was last updated. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Updated: DateTime
        ///A URL of a publicly-accessible webpage for this product.
        Url: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "product"

        static member New (active: bool, created: DateTime, description: string option, id: string, images: string list, livemode: bool, marketingFeatures: ProductMarketingFeature list, metadata: Map<string, string>, name: string, packageDimensions: PackageDimensions option, shippable: bool option, ``type``: ProductType, updated: DateTime, url: string option, ?defaultPrice: ProductDefaultPrice'AnyOf option, ?statementDescriptor: string option, ?taxCode: ProductTaxCode'AnyOf option, ?unitLabel: string option) =
            {
                Product.Active = active //required
                Product.Created = created //required
                Product.Description = description //required
                Product.Id = id //required
                Product.Images = images //required
                Product.Livemode = livemode //required
                Product.MarketingFeatures = marketingFeatures //required
                Product.Metadata = metadata //required
                Product.Name = name //required
                Product.PackageDimensions = packageDimensions //required
                Product.Shippable = shippable //required
                Product.Type = ``type`` //required
                Product.Updated = updated //required
                Product.Url = url //required
                Product.DefaultPrice = defaultPrice |> Option.flatten
                Product.StatementDescriptor = statementDescriptor |> Option.flatten
                Product.TaxCode = taxCode |> Option.flatten
                Product.UnitLabel = unitLabel |> Option.flatten
            }

    and ProductDefaultPrice'AnyOf =
        | String of string
        | Price of Price

    and ProductTaxCode'AnyOf =
        | String of string
        | TaxCode of TaxCode

    ///A product_feature represents an attachment between a feature and a product.
    ///When a product is purchased that has a feature attached, Stripe will create an entitlement to the feature for the purchasing customer.
    and ProductFeature = {
        EntitlementFeature: EntitlementsFeature
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "product_feature"

        static member New (entitlementFeature: EntitlementsFeature, id: string, livemode: bool) =
            {
                ProductFeature.EntitlementFeature = entitlementFeature //required
                ProductFeature.Id = id //required
                ProductFeature.Livemode = livemode //required
            }

    ///A Promotion Code represents a customer-redeemable code for an underlying promotion.
    ///You can create multiple codes for a single promotion.
    ///If you enable promotion codes in your [customer portal configuration](https://docs.stripe.com/customer-management/configure-portal), then customers can redeem a code themselves when updating a subscription in the portal.
    ///Customers can also view the currently active promotion codes and coupons on each of their subscriptions in the portal.
    and PromotionCode = {
        ///Whether the promotion code is currently active. A promotion code is only active if the coupon is also valid.
        Active: bool
        ///The customer-facing code. Regardless of case, this code must be unique across all active promotion codes for each customer. Valid characters are lower case letters (a-z), upper case letters (A-Z), digits (0-9), and dashes (-).
        Code: string
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The customer who can use this promotion code.
        Customer: PromotionCodeCustomer'AnyOf option
        ///The account representing the customer who can use this promotion code.
        CustomerAccount: string option
        ///Date at which the promotion code can no longer be redeemed.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Maximum number of times this promotion code can be redeemed.
        MaxRedemptions: int option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        Promotion: PromotionCodesResourcePromotion
        Restrictions: PromotionCodesResourceRestrictions
        ///Number of times this promotion code has been used.
        TimesRedeemed: int
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "promotion_code"

        static member New (active: bool, code: string, created: DateTime, customer: PromotionCodeCustomer'AnyOf option, customerAccount: string option, expiresAt: DateTime option, id: string, livemode: bool, maxRedemptions: int option, metadata: Map<string, string> option, promotion: PromotionCodesResourcePromotion, restrictions: PromotionCodesResourceRestrictions, timesRedeemed: int) =
            {
                PromotionCode.Active = active //required
                PromotionCode.Code = code //required
                PromotionCode.Created = created //required
                PromotionCode.Customer = customer //required
                PromotionCode.CustomerAccount = customerAccount //required
                PromotionCode.ExpiresAt = expiresAt //required
                PromotionCode.Id = id //required
                PromotionCode.Livemode = livemode //required
                PromotionCode.MaxRedemptions = maxRedemptions //required
                PromotionCode.Metadata = metadata //required
                PromotionCode.Promotion = promotion //required
                PromotionCode.Restrictions = restrictions //required
                PromotionCode.TimesRedeemed = timesRedeemed //required
            }

    and PromotionCodeCustomer'AnyOf =
        | String of string
        | Customer of Customer
        | DeletedCustomer of DeletedCustomer

    and PromotionCodesResourcePromotion = {
        ///If promotion `type` is `coupon`, the coupon for this promotion.
        Coupon: PromotionCodesResourcePromotionCoupon'AnyOf option
    }
    with
        ///The type of promotion.
        member _.Type = "coupon"

        static member New (coupon: PromotionCodesResourcePromotionCoupon'AnyOf option) =
            {
                PromotionCodesResourcePromotion.Coupon = coupon //required
            }

    and PromotionCodesResourcePromotionCoupon'AnyOf =
        | String of string
        | Coupon of Coupon

    and PromotionCodesResourceRestrictions = {
        ///Promotion code restrictions defined in each available currency option. Each key must be a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html) and a [supported currency](https://stripe.com/docs/currencies).
        CurrencyOptions: Map<string, string list> option
        ///A Boolean indicating if the Promotion Code should only be redeemed for Customers without any successful payments or invoices
        FirstTimeTransaction: bool
        ///Minimum amount required to redeem this Promotion Code into a Coupon (e.g., a purchase must be $100 or more to work).
        MinimumAmount: int option
        ///Three-letter [ISO code](https://stripe.com/docs/currencies) for minimum_amount
        MinimumAmountCurrency: IsoTypes.IsoCurrencyCode option
    }
    with
        static member New (firstTimeTransaction: bool, minimumAmount: int option, minimumAmountCurrency: IsoTypes.IsoCurrencyCode option, ?currencyOptions: Map<string, string list>) =
            {
                PromotionCodesResourceRestrictions.FirstTimeTransaction = firstTimeTransaction //required
                PromotionCodesResourceRestrictions.MinimumAmount = minimumAmount //required
                PromotionCodesResourceRestrictions.MinimumAmountCurrency = minimumAmountCurrency //required
                PromotionCodesResourceRestrictions.CurrencyOptions = currencyOptions
            }

    and ProrationDetails = {
        ///Discount amounts applied when the proration was created.
        DiscountAmounts: DiscountsResourceDiscountAmount list
    }
    with
        static member New (discountAmounts: DiscountsResourceDiscountAmount list) =
            {
                ProrationDetails.DiscountAmounts = discountAmounts //required
            }

    ///A Quote is a way to model prices that you'd like to provide to a customer.
    ///Once accepted, it will automatically create an invoice, subscription or subscription schedule.
    and Quote = {
        ///Total before any discounts or taxes are applied.
        AmountSubtotal: int
        ///Total after discounts and taxes are applied.
        AmountTotal: int
        ///ID of the Connect Application that created the quote.
        Application: QuoteApplication'AnyOf option
        ///The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. Only applicable if there are no line items with recurring prices on the quote.
        ApplicationFeeAmount: int option
        ///A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account. Only applicable if there are line items with recurring prices on the quote.
        ApplicationFeePercent: decimal option
        AutomaticTax: QuotesResourceAutomaticTax
        ///Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay invoices at the end of the subscription cycle or on finalization using the default payment method attached to the subscription or customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`.
        CollectionMethod: QuoteCollectionMethod
        Computed: QuotesResourceComputed
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode option
        ///The customer who received this quote. A customer is required to finalize the quote. Once specified, you can't change it.
        Customer: QuoteCustomer'AnyOf option
        ///The account representing the customer who received this quote. A customer or account is required to finalize the quote. Once specified, you can't change it.
        CustomerAccount: string option
        ///The tax rates applied to this quote.
        DefaultTaxRates: QuoteDefaultTaxRates'AnyOf list option
        ///A description that will be displayed on the quote PDF.
        Description: string option
        ///The discounts applied to this quote.
        Discounts: QuoteDiscounts'AnyOf list
        ///The date on which the quote will be canceled if in `open` or `draft` status. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime
        ///A footer that will be displayed on the quote PDF.
        Footer: string option
        ///Details of the quote that was cloned. See the [cloning documentation](https://docs.stripe.com/quotes/clone) for more details.
        FromQuote: QuotesResourceFromQuote option
        ///A header that will be displayed on the quote PDF.
        Header: string option
        ///Unique identifier for the object.
        Id: string
        ///The invoice that was created from this quote.
        Invoice: QuoteInvoice'AnyOf option
        InvoiceSettings: InvoiceSettingQuoteSetting
        ///A list of items the customer is being quoted for.
        LineItems: QuoteLineItems option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///A unique number that identifies this particular quote. This number is assigned once the quote is [finalized](https://docs.stripe.com/quotes/overview#finalize).
        Number: string option
        ///The account on behalf of which to charge. See the [Connect documentation](https://support.stripe.com/questions/sending-invoices-on-behalf-of-connected-accounts) for details.
        OnBehalfOf: QuoteOnBehalfOf'AnyOf option
        ///The status of the quote.
        Status: QuoteStatus
        StatusTransitions: QuotesResourceStatusTransitions
        ///The subscription that was created or updated from this quote.
        Subscription: QuoteSubscription'AnyOf option
        SubscriptionData: QuotesResourceSubscriptionDataSubscriptionData
        ///The subscription schedule that was created or updated from this quote.
        SubscriptionSchedule: QuoteSubscriptionSchedule'AnyOf option
        ///ID of the test clock this quote belongs to.
        TestClock: QuoteTestClock'AnyOf option
        TotalDetails: QuotesResourceTotalDetails
        ///The account (if any) the payments will be attributed to for tax reporting, and where funds from each payment will be transferred to for each of the invoices.
        TransferData: QuotesResourceTransferData option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "quote"

        static member New (amountSubtotal: int, amountTotal: int, application: QuoteApplication'AnyOf option, applicationFeeAmount: int option, applicationFeePercent: decimal option, automaticTax: QuotesResourceAutomaticTax, collectionMethod: QuoteCollectionMethod, computed: QuotesResourceComputed, created: DateTime, currency: IsoTypes.IsoCurrencyCode option, customer: QuoteCustomer'AnyOf option, customerAccount: string option, description: string option, discounts: QuoteDiscounts'AnyOf list, expiresAt: DateTime, footer: string option, fromQuote: QuotesResourceFromQuote option, header: string option, id: string, invoice: QuoteInvoice'AnyOf option, invoiceSettings: InvoiceSettingQuoteSetting, livemode: bool, metadata: Map<string, string>, number: string option, onBehalfOf: QuoteOnBehalfOf'AnyOf option, status: QuoteStatus, statusTransitions: QuotesResourceStatusTransitions, subscription: QuoteSubscription'AnyOf option, subscriptionData: QuotesResourceSubscriptionDataSubscriptionData, subscriptionSchedule: QuoteSubscriptionSchedule'AnyOf option, testClock: QuoteTestClock'AnyOf option, totalDetails: QuotesResourceTotalDetails, transferData: QuotesResourceTransferData option, ?defaultTaxRates: QuoteDefaultTaxRates'AnyOf list, ?lineItems: QuoteLineItems) =
            {
                Quote.AmountSubtotal = amountSubtotal //required
                Quote.AmountTotal = amountTotal //required
                Quote.Application = application //required
                Quote.ApplicationFeeAmount = applicationFeeAmount //required
                Quote.ApplicationFeePercent = applicationFeePercent //required
                Quote.AutomaticTax = automaticTax //required
                Quote.CollectionMethod = collectionMethod //required
                Quote.Computed = computed //required
                Quote.Created = created //required
                Quote.Currency = currency //required
                Quote.Customer = customer //required
                Quote.CustomerAccount = customerAccount //required
                Quote.Description = description //required
                Quote.Discounts = discounts //required
                Quote.ExpiresAt = expiresAt //required
                Quote.Footer = footer //required
                Quote.FromQuote = fromQuote //required
                Quote.Header = header //required
                Quote.Id = id //required
                Quote.Invoice = invoice //required
                Quote.InvoiceSettings = invoiceSettings //required
                Quote.Livemode = livemode //required
                Quote.Metadata = metadata //required
                Quote.Number = number //required
                Quote.OnBehalfOf = onBehalfOf //required
                Quote.Status = status //required
                Quote.StatusTransitions = statusTransitions //required
                Quote.Subscription = subscription //required
                Quote.SubscriptionData = subscriptionData //required
                Quote.SubscriptionSchedule = subscriptionSchedule //required
                Quote.TestClock = testClock //required
                Quote.TotalDetails = totalDetails //required
                Quote.TransferData = transferData //required
                Quote.DefaultTaxRates = defaultTaxRates
                Quote.LineItems = lineItems
            }

    and QuoteApplication'AnyOf =
        | String of string
        | Application of Application
        | DeletedApplication of DeletedApplication

    and QuoteCustomer'AnyOf =
        | String of string
        | Customer of Customer
        | DeletedCustomer of DeletedCustomer

    and QuoteInvoice'AnyOf =
        | String of string
        | Invoice of Invoice
        | DeletedInvoice of DeletedInvoice

    and QuoteOnBehalfOf'AnyOf =
        | String of string
        | Account of Account

    and QuoteSubscription'AnyOf =
        | String of string
        | Subscription of Subscription

    and QuoteSubscriptionSchedule'AnyOf =
        | String of string
        | SubscriptionSchedule of SubscriptionSchedule

    and QuoteTestClock'AnyOf =
        | String of string
        | TestHelpersTestClock of TestHelpersTestClock

    and QuoteDefaultTaxRates'AnyOf =
        | String of string
        | TaxRate of TaxRate

    and QuoteDiscounts'AnyOf =
        | String of string
        | Discount of Discount

    ///A list of items the customer is being quoted for.
    and QuoteLineItems = {
        ///Details about each object.
        Data: Item list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: Item list, hasMore: bool, url: string) =
            {
                QuoteLineItems.Data = data //required
                QuoteLineItems.HasMore = hasMore //required
                QuoteLineItems.Url = url //required
            }

    and QuotesResourceAutomaticTax = {
        ///Automatically calculate taxes
        Enabled: bool
        ///The account that's liable for tax. If set, the business address and tax registrations required to perform the tax calculation are loaded from this account. The tax transaction is returned in the report of the connected account.
        Liability: ConnectAccountReference option
        ///The tax provider powering automatic tax.
        Provider: string option
        ///The status of the most recent automated tax calculation for this quote.
        Status: QuotesResourceAutomaticTaxStatus option
    }
    with
        static member New (enabled: bool, liability: ConnectAccountReference option, provider: string option, status: QuotesResourceAutomaticTaxStatus option) =
            {
                QuotesResourceAutomaticTax.Enabled = enabled //required
                QuotesResourceAutomaticTax.Liability = liability //required
                QuotesResourceAutomaticTax.Provider = provider //required
                QuotesResourceAutomaticTax.Status = status //required
            }

    and QuotesResourceComputed = {
        ///The definitive totals and line items the customer will be charged on a recurring basis. Takes into account the line items with recurring prices and discounts with `duration=forever` coupons only. Defaults to `null` if no inputted line items with recurring prices.
        Recurring: QuotesResourceRecurring option
        Upfront: QuotesResourceUpfront
    }
    with
        static member New (recurring: QuotesResourceRecurring option, upfront: QuotesResourceUpfront) =
            {
                QuotesResourceComputed.Recurring = recurring //required
                QuotesResourceComputed.Upfront = upfront //required
            }

    and QuotesResourceFromQuote = {
        ///Whether this quote is a revision of a different quote.
        IsRevision: bool
        ///The quote that was cloned.
        Quote: QuotesResourceFromQuoteQuote'AnyOf
    }
    with
        static member New (isRevision: bool, quote: QuotesResourceFromQuoteQuote'AnyOf) =
            {
                QuotesResourceFromQuote.IsRevision = isRevision //required
                QuotesResourceFromQuote.Quote = quote //required
            }

    and QuotesResourceFromQuoteQuote'AnyOf =
        | String of string
        | Quote of Quote

    and QuotesResourceRecurring = {
        ///Total before any discounts or taxes are applied.
        AmountSubtotal: int
        ///Total after discounts and taxes are applied.
        AmountTotal: int
        ///The frequency at which a subscription is billed. One of `day`, `week`, `month` or `year`.
        Interval: QuotesResourceRecurringInterval
        ///The number of intervals (specified in the `interval` attribute) between subscription billings. For example, `interval=month` and `interval_count=3` bills every 3 months.
        IntervalCount: int
        TotalDetails: QuotesResourceTotalDetails
    }
    with
        static member New (amountSubtotal: int, amountTotal: int, interval: QuotesResourceRecurringInterval, intervalCount: int, totalDetails: QuotesResourceTotalDetails) =
            {
                QuotesResourceRecurring.AmountSubtotal = amountSubtotal //required
                QuotesResourceRecurring.AmountTotal = amountTotal //required
                QuotesResourceRecurring.Interval = interval //required
                QuotesResourceRecurring.IntervalCount = intervalCount //required
                QuotesResourceRecurring.TotalDetails = totalDetails //required
            }

    ///The billing mode of the quote.
    and QuotesResourceSubscriptionDataBillingMode = {
        Flexible: SubscriptionsResourceBillingModeFlexible option
        ///Controls how prorations and invoices for subscriptions are calculated and orchestrated.
        Type: QuotesResourceSubscriptionDataBillingModeType
    }
    with
        static member New (``type``: QuotesResourceSubscriptionDataBillingModeType, ?flexible: SubscriptionsResourceBillingModeFlexible) =
            {
                QuotesResourceSubscriptionDataBillingMode.Type = ``type`` //required
                QuotesResourceSubscriptionDataBillingMode.Flexible = flexible
            }

    and QuotesResourceSubscriptionDataSubscriptionData = {
        BillingMode: QuotesResourceSubscriptionDataBillingMode
        ///The subscription's description, meant to be displayable to the customer. Use this field to optionally store an explanation of the subscription for rendering in Stripe surfaces and certain local payment methods UIs.
        Description: string option
        ///When creating a new subscription, the date of which the subscription schedule will start after the quote is accepted. This date is ignored if it is in the past when the quote is accepted. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]EffectiveDate: DateTime option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that will set metadata on the subscription or subscription schedule when the quote is accepted. If a recurring price is included in `line_items`, this field will be passed to the resulting subscription's `metadata` field. If `subscription_data.effective_date` is used, this field will be passed to the resulting subscription schedule's `phases.metadata` field. Unlike object-level metadata, this field is declarative. Updates will clear prior values.
        Metadata: Map<string, string> option
        ///Integer representing the number of trial period days before the customer is charged for the first time.
        TrialPeriodDays: int option
    }
    with
        static member New (billingMode: QuotesResourceSubscriptionDataBillingMode, description: string option, effectiveDate: DateTime option, metadata: Map<string, string> option, trialPeriodDays: int option) =
            {
                QuotesResourceSubscriptionDataSubscriptionData.BillingMode = billingMode //required
                QuotesResourceSubscriptionDataSubscriptionData.Description = description //required
                QuotesResourceSubscriptionDataSubscriptionData.EffectiveDate = effectiveDate //required
                QuotesResourceSubscriptionDataSubscriptionData.Metadata = metadata //required
                QuotesResourceSubscriptionDataSubscriptionData.TrialPeriodDays = trialPeriodDays //required
            }

    and QuotesResourceTotalDetails = {
        ///This is the sum of all the discounts.
        AmountDiscount: int
        ///This is the sum of all the shipping amounts.
        AmountShipping: int option
        ///This is the sum of all the tax amounts.
        AmountTax: int
        Breakdown: QuotesResourceTotalDetailsResourceBreakdown option
    }
    with
        static member New (amountDiscount: int, amountShipping: int option, amountTax: int, ?breakdown: QuotesResourceTotalDetailsResourceBreakdown) =
            {
                QuotesResourceTotalDetails.AmountDiscount = amountDiscount //required
                QuotesResourceTotalDetails.AmountShipping = amountShipping //required
                QuotesResourceTotalDetails.AmountTax = amountTax //required
                QuotesResourceTotalDetails.Breakdown = breakdown
            }

    and QuotesResourceTotalDetailsResourceBreakdown = {
        ///The aggregated discounts.
        Discounts: LineItemsDiscountAmount list
        ///The aggregated tax amounts by rate.
        Taxes: LineItemsTaxAmount list
    }
    with
        static member New (discounts: LineItemsDiscountAmount list, taxes: LineItemsTaxAmount list) =
            {
                QuotesResourceTotalDetailsResourceBreakdown.Discounts = discounts //required
                QuotesResourceTotalDetailsResourceBreakdown.Taxes = taxes //required
            }

    and QuotesResourceTransferData = {
        ///The amount in cents (or local equivalent) that will be transferred to the destination account when the invoice is paid. By default, the entire amount is transferred to the destination.
        Amount: int option
        ///A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the destination account. By default, the entire amount will be transferred to the destination.
        AmountPercent: decimal option
        ///The account where funds from the payment will be transferred to upon payment success.
        Destination: QuotesResourceTransferDataDestination'AnyOf
    }
    with
        static member New (amount: int option, amountPercent: decimal option, destination: QuotesResourceTransferDataDestination'AnyOf) =
            {
                QuotesResourceTransferData.Amount = amount //required
                QuotesResourceTransferData.AmountPercent = amountPercent //required
                QuotesResourceTransferData.Destination = destination //required
            }

    and QuotesResourceTransferDataDestination'AnyOf =
        | String of string
        | Account of Account

    and QuotesResourceUpfront = {
        ///Total before any discounts or taxes are applied.
        AmountSubtotal: int
        ///Total after discounts and taxes are applied.
        AmountTotal: int
        ///The line items that will appear on the next invoice after this quote is accepted. This does not include pending invoice items that exist on the customer but may still be included in the next invoice.
        LineItems: QuotesResourceUpfrontLineItems option
        TotalDetails: QuotesResourceTotalDetails
    }
    with
        static member New (amountSubtotal: int, amountTotal: int, totalDetails: QuotesResourceTotalDetails, ?lineItems: QuotesResourceUpfrontLineItems) =
            {
                QuotesResourceUpfront.AmountSubtotal = amountSubtotal //required
                QuotesResourceUpfront.AmountTotal = amountTotal //required
                QuotesResourceUpfront.TotalDetails = totalDetails //required
                QuotesResourceUpfront.LineItems = lineItems
            }

    ///The line items that will appear on the next invoice after this quote is accepted. This does not include pending invoice items that exist on the customer but may still be included in the next invoice.
    and QuotesResourceUpfrontLineItems = {
        ///Details about each object.
        Data: Item list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: Item list, hasMore: bool, url: string) =
            {
                QuotesResourceUpfrontLineItems.Data = data //required
                QuotesResourceUpfrontLineItems.HasMore = hasMore //required
                QuotesResourceUpfrontLineItems.Url = url //required
            }

    ///An early fraud warning indicates that the card issuer has notified us that a
    ///charge may be fraudulent.
    ///Related guide: [Early fraud warnings](https://docs.stripe.com/disputes/measuring#early-fraud-warnings)
    and RadarEarlyFraudWarning = {
        ///An EFW is actionable if it has not received a dispute and has not been fully refunded. You may wish to proactively refund a charge that receives an EFW, in order to avoid receiving a dispute later.
        Actionable: bool
        ///ID of the charge this early fraud warning is for, optionally expanded.
        Charge: RadarEarlyFraudWarningCharge'AnyOf
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The type of fraud labelled by the issuer. One of `card_never_received`, `fraudulent_card_application`, `made_with_counterfeit_card`, `made_with_lost_card`, `made_with_stolen_card`, `misc`, `unauthorized_use_of_card`.
        FraudType: string
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///ID of the Payment Intent this early fraud warning is for, optionally expanded.
        PaymentIntent: RadarEarlyFraudWarningPaymentIntent'AnyOf option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "radar.early_fraud_warning"

        static member New (actionable: bool, charge: RadarEarlyFraudWarningCharge'AnyOf, created: DateTime, fraudType: string, id: string, livemode: bool, ?paymentIntent: RadarEarlyFraudWarningPaymentIntent'AnyOf) =
            {
                RadarEarlyFraudWarning.Actionable = actionable //required
                RadarEarlyFraudWarning.Charge = charge //required
                RadarEarlyFraudWarning.Created = created //required
                RadarEarlyFraudWarning.FraudType = fraudType //required
                RadarEarlyFraudWarning.Id = id //required
                RadarEarlyFraudWarning.Livemode = livemode //required
                RadarEarlyFraudWarning.PaymentIntent = paymentIntent
            }

    and RadarEarlyFraudWarningCharge'AnyOf =
        | String of string
        | Charge of Charge

    and RadarEarlyFraudWarningPaymentIntent'AnyOf =
        | String of string
        | PaymentIntent of PaymentIntent

    ///Payment Evaluations represent the risk lifecycle of an externally processed payment. It includes the Radar risk score from Stripe, payment outcome taken by the merchant or processor, and any post transaction events, such as refunds or disputes. See the [Radar API guide](/radar/multiprocessor) for integration steps.
    and RadarPaymentEvaluation = {
        ClientDeviceMetadataDetails: InsightsResourcesPaymentEvaluationClientDeviceMetadata option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CreatedAt: DateTime
        CustomerDetails: InsightsResourcesPaymentEvaluationCustomerDetails option
        ///Event information associated with the payment evaluation, such as refunds, dispute, early fraud warnings, or user interventions.
        Events: InsightsResourcesPaymentEvaluationEvent list option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///Indicates the final outcome for the payment evaluation.
        Outcome: InsightsResourcesPaymentEvaluationOutcome option
        PaymentDetails: InsightsResourcesPaymentEvaluationPaymentDetails option
        ///Recommended action based on the score of the `fraudulent_payment` signal. Possible values are `block` and `continue`.
        RecommendedAction: RadarPaymentEvaluationRecommendedAction
        Signals: InsightsResourcesPaymentEvaluationSignals
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "radar.payment_evaluation"

        static member New (createdAt: DateTime, id: string, livemode: bool, metadata: Map<string, string> option, recommendedAction: RadarPaymentEvaluationRecommendedAction, signals: InsightsResourcesPaymentEvaluationSignals, ?clientDeviceMetadataDetails: InsightsResourcesPaymentEvaluationClientDeviceMetadata, ?customerDetails: InsightsResourcesPaymentEvaluationCustomerDetails, ?events: InsightsResourcesPaymentEvaluationEvent list, ?outcome: InsightsResourcesPaymentEvaluationOutcome option, ?paymentDetails: InsightsResourcesPaymentEvaluationPaymentDetails) =
            {
                RadarPaymentEvaluation.CreatedAt = createdAt //required
                RadarPaymentEvaluation.Id = id //required
                RadarPaymentEvaluation.Livemode = livemode //required
                RadarPaymentEvaluation.Metadata = metadata //required
                RadarPaymentEvaluation.RecommendedAction = recommendedAction //required
                RadarPaymentEvaluation.Signals = signals //required
                RadarPaymentEvaluation.ClientDeviceMetadataDetails = clientDeviceMetadataDetails
                RadarPaymentEvaluation.CustomerDetails = customerDetails
                RadarPaymentEvaluation.Events = events
                RadarPaymentEvaluation.Outcome = outcome |> Option.flatten
                RadarPaymentEvaluation.PaymentDetails = paymentDetails
            }

    ///Value lists allow you to group values together which can then be referenced in rules.
    ///Related guide: [Default Stripe lists](https://docs.stripe.com/radar/lists#managing-list-items)
    and RadarValueList = {
        ///The name of the value list for use in rules.
        Alias: string
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The name or email address of the user who created this value list.
        CreatedBy: string
        ///Unique identifier for the object.
        Id: string
        ///The type of items in the value list. One of `card_fingerprint`, `card_bin`, `crypto_fingerprint`, `email`, `ip_address`, `country`, `string`, `case_sensitive_string`, `customer_id`, `account`, `sepa_debit_fingerprint`, or `us_bank_account_fingerprint`.
        ItemType: RadarValueListItemType
        ///List of items contained within this value list.
        ListItems: RadarValueListListItems
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///The name of the value list.
        Name: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "radar.value_list"

        static member New (alias: string, created: DateTime, createdBy: string, id: string, itemType: RadarValueListItemType, listItems: RadarValueListListItems, livemode: bool, metadata: Map<string, string>, name: string) =
            {
                RadarValueList.Alias = alias //required
                RadarValueList.Created = created //required
                RadarValueList.CreatedBy = createdBy //required
                RadarValueList.Id = id //required
                RadarValueList.ItemType = itemType //required
                RadarValueList.ListItems = listItems //required
                RadarValueList.Livemode = livemode //required
                RadarValueList.Metadata = metadata //required
                RadarValueList.Name = name //required
            }

    ///List of items contained within this value list.
    and RadarValueListListItems = {
        ///Details about each object.
        Data: RadarValueListItem list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: RadarValueListItem list, hasMore: bool, url: string) =
            {
                RadarValueListListItems.Data = data //required
                RadarValueListListItems.HasMore = hasMore //required
                RadarValueListListItems.Url = url //required
            }

    and RadarReviewResourceLocation = {
        ///The city where the payment originated.
        City: string option
        ///Two-letter ISO code representing the country where the payment originated.
        Country: IsoTypes.IsoCountryCode option
        ///The geographic latitude where the payment originated.
        Latitude: decimal option
        ///The geographic longitude where the payment originated.
        Longitude: decimal option
        ///The state/county/province/region where the payment originated.
        Region: string option
    }
    with
        static member New (city: string option, country: IsoTypes.IsoCountryCode option, latitude: decimal option, longitude: decimal option, region: string option) =
            {
                RadarReviewResourceLocation.City = city //required
                RadarReviewResourceLocation.Country = country //required
                RadarReviewResourceLocation.Latitude = latitude //required
                RadarReviewResourceLocation.Longitude = longitude //required
                RadarReviewResourceLocation.Region = region //required
            }

    and Recurring = {
        ///The frequency at which a subscription is billed. One of `day`, `week`, `month` or `year`.
        Interval: RecurringInterval
        ///The number of intervals (specified in the `interval` attribute) between subscription billings. For example, `interval=month` and `interval_count=3` bills every 3 months.
        IntervalCount: int
        ///The meter tracking the usage of a metered price
        Meter: string option
        ///Default number of trial days when subscribing a customer to this price using [`trial_from_plan=true`](https://docs.stripe.com/api#create_subscription-trial_from_plan).
        TrialPeriodDays: int option
        ///Configures how the quantity per period should be determined. Can be either `metered` or `licensed`. `licensed` automatically bills the `quantity` set when adding it to a subscription. `metered` aggregates the total usage based on usage records. Defaults to `licensed`.
        UsageType: RecurringUsageType
    }
    with
        static member New (interval: RecurringInterval, intervalCount: int, meter: string option, trialPeriodDays: int option, usageType: RecurringUsageType) =
            {
                Recurring.Interval = interval //required
                Recurring.IntervalCount = intervalCount //required
                Recurring.Meter = meter //required
                Recurring.TrialPeriodDays = trialPeriodDays //required
                Recurring.UsageType = usageType //required
            }

    ///Refund objects allow you to refund a previously created charge that isn't
    ///refunded yet. Funds are refunded to the credit or debit card that's
    ///initially charged.
    ///Related guide: [Refunds](https://docs.stripe.com/refunds)
    and Refund = {
        ///Amount, in cents (or local equivalent).
        Amount: int
        ///Balance transaction that describes the impact on your account balance.
        BalanceTransaction: RefundBalanceTransaction'AnyOf option
        ///ID of the charge that's refunded.
        Charge: RefundCharge'AnyOf option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///An arbitrary string attached to the object. You can use this for displaying to users (available on non-card refunds only).
        Description: string option
        DestinationDetails: RefundDestinationDetails option
        ///After the refund fails, this balance transaction describes the adjustment made on your account balance that reverses the initial balance transaction.
        FailureBalanceTransaction: RefundFailureBalanceTransaction'AnyOf option
        ///Provides the reason for the refund failure. Possible values are: `lost_or_stolen_card`, `expired_or_canceled_card`, `charge_for_pending_refund_disputed`, `insufficient_funds`, `declined`, `merchant_request`, or `unknown`.
        FailureReason: RefundFailureReason option
        ///Unique identifier for the object.
        Id: string
        ///For payment methods without native refund support (for example, Konbini, PromptPay), provide an email address for the customer to receive refund instructions.
        InstructionsEmail: string option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        NextAction: RefundNextAction option
        ///ID of the PaymentIntent that's refunded.
        PaymentIntent: RefundPaymentIntent'AnyOf option
        ///Provides the reason for why the refund is pending. Possible values are: `processing`, `insufficient_funds`, or `charge_pending`.
        PendingReason: RefundPendingReason option
        PresentmentDetails: PaymentFlowsPaymentIntentPresentmentDetails option
        ///Reason for the refund, which is either user-provided (`duplicate`, `fraudulent`, or `requested_by_customer`) or generated by Stripe internally (`expired_uncaptured_charge`).
        Reason: RefundReason option
        ///This is the transaction number that appears on email receipts sent for this refund.
        ReceiptNumber: string option
        ///The transfer reversal that's associated with the refund. Only present if the charge came from another Stripe account.
        SourceTransferReversal: RefundSourceTransferReversal'AnyOf option
        ///Status of the refund. This can be `pending`, `requires_action`, `succeeded`, `failed`, or `canceled`. Learn more about [failed refunds](https://docs.stripe.com/refunds#failed-refunds).
        Status: RefundStatus option
        ///This refers to the transfer reversal object if the accompanying transfer reverses. This is only applicable if the charge was created using the destination parameter.
        TransferReversal: RefundTransferReversal'AnyOf option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "refund"

        static member New (amount: int, balanceTransaction: RefundBalanceTransaction'AnyOf option, charge: RefundCharge'AnyOf option, created: DateTime, currency: IsoTypes.IsoCurrencyCode, id: string, metadata: Map<string, string> option, paymentIntent: RefundPaymentIntent'AnyOf option, reason: RefundReason option, receiptNumber: string option, sourceTransferReversal: RefundSourceTransferReversal'AnyOf option, status: RefundStatus option, transferReversal: RefundTransferReversal'AnyOf option, ?description: string, ?destinationDetails: RefundDestinationDetails, ?failureBalanceTransaction: RefundFailureBalanceTransaction'AnyOf, ?failureReason: RefundFailureReason, ?instructionsEmail: string, ?nextAction: RefundNextAction, ?pendingReason: RefundPendingReason, ?presentmentDetails: PaymentFlowsPaymentIntentPresentmentDetails) =
            {
                Refund.Amount = amount //required
                Refund.BalanceTransaction = balanceTransaction //required
                Refund.Charge = charge //required
                Refund.Created = created //required
                Refund.Currency = currency //required
                Refund.Id = id //required
                Refund.Metadata = metadata //required
                Refund.PaymentIntent = paymentIntent //required
                Refund.Reason = reason //required
                Refund.ReceiptNumber = receiptNumber //required
                Refund.SourceTransferReversal = sourceTransferReversal //required
                Refund.Status = status //required
                Refund.TransferReversal = transferReversal //required
                Refund.Description = description
                Refund.DestinationDetails = destinationDetails
                Refund.FailureBalanceTransaction = failureBalanceTransaction
                Refund.FailureReason = failureReason
                Refund.InstructionsEmail = instructionsEmail
                Refund.NextAction = nextAction
                Refund.PendingReason = pendingReason
                Refund.PresentmentDetails = presentmentDetails
            }

    and RefundBalanceTransaction'AnyOf =
        | String of string
        | BalanceTransaction of BalanceTransaction

    and RefundCharge'AnyOf =
        | String of string
        | Charge of Charge

    and RefundFailureBalanceTransaction'AnyOf =
        | String of string
        | BalanceTransaction of BalanceTransaction

    and RefundPaymentIntent'AnyOf =
        | String of string
        | PaymentIntent of PaymentIntent

    and RefundSourceTransferReversal'AnyOf =
        | String of string
        | TransferReversal of TransferReversal

    and RefundTransferReversal'AnyOf =
        | String of string
        | TransferReversal of TransferReversal

    and RefundDestinationDetails = {
        Affirm: DestinationDetailsUnimplemented option
        AfterpayClearpay: DestinationDetailsUnimplemented option
        Alipay: DestinationDetailsUnimplemented option
        Alma: DestinationDetailsUnimplemented option
        AmazonPay: DestinationDetailsUnimplemented option
        AuBankTransfer: DestinationDetailsUnimplemented option
        Blik: RefundDestinationDetailsBlik option
        BrBankTransfer: RefundDestinationDetailsBrBankTransfer option
        Card: RefundDestinationDetailsCard option
        Cashapp: DestinationDetailsUnimplemented option
        Crypto: RefundDestinationDetailsCrypto option
        CustomerCashBalance: DestinationDetailsUnimplemented option
        Eps: DestinationDetailsUnimplemented option
        EuBankTransfer: RefundDestinationDetailsEuBankTransfer option
        GbBankTransfer: RefundDestinationDetailsGbBankTransfer option
        Giropay: DestinationDetailsUnimplemented option
        Grabpay: DestinationDetailsUnimplemented option
        JpBankTransfer: RefundDestinationDetailsJpBankTransfer option
        Klarna: DestinationDetailsUnimplemented option
        MbWay: RefundDestinationDetailsMbWay option
        Multibanco: RefundDestinationDetailsMultibanco option
        MxBankTransfer: RefundDestinationDetailsMxBankTransfer option
        NzBankTransfer: DestinationDetailsUnimplemented option
        [<JsonField(Name="p24")>]P24: RefundDestinationDetailsP24 option
        Paynow: DestinationDetailsUnimplemented option
        Paypal: RefundDestinationDetailsPaypal option
        Pix: DestinationDetailsUnimplemented option
        Revolut: DestinationDetailsUnimplemented option
        Sofort: DestinationDetailsUnimplemented option
        Swish: RefundDestinationDetailsSwish option
        ThBankTransfer: RefundDestinationDetailsThBankTransfer option
        Twint: DestinationDetailsUnimplemented option
        ///The type of transaction-specific details of the payment method used in the refund (e.g., `card`). An additional hash is included on `destination_details` with a name matching this value. It contains information specific to the refund transaction.
        Type: string
        UsBankTransfer: RefundDestinationDetailsUsBankTransfer option
        WechatPay: DestinationDetailsUnimplemented option
        Zip: DestinationDetailsUnimplemented option
    }
    with
        static member New (``type``: string, ?affirm: DestinationDetailsUnimplemented, ?afterpayClearpay: DestinationDetailsUnimplemented, ?alipay: DestinationDetailsUnimplemented, ?alma: DestinationDetailsUnimplemented, ?amazonPay: DestinationDetailsUnimplemented, ?auBankTransfer: DestinationDetailsUnimplemented, ?blik: RefundDestinationDetailsBlik, ?brBankTransfer: RefundDestinationDetailsBrBankTransfer, ?card: RefundDestinationDetailsCard, ?cashapp: DestinationDetailsUnimplemented, ?crypto: RefundDestinationDetailsCrypto, ?customerCashBalance: DestinationDetailsUnimplemented, ?eps: DestinationDetailsUnimplemented, ?euBankTransfer: RefundDestinationDetailsEuBankTransfer, ?gbBankTransfer: RefundDestinationDetailsGbBankTransfer, ?giropay: DestinationDetailsUnimplemented, ?grabpay: DestinationDetailsUnimplemented, ?jpBankTransfer: RefundDestinationDetailsJpBankTransfer, ?klarna: DestinationDetailsUnimplemented, ?mbWay: RefundDestinationDetailsMbWay, ?multibanco: RefundDestinationDetailsMultibanco, ?mxBankTransfer: RefundDestinationDetailsMxBankTransfer, ?nzBankTransfer: DestinationDetailsUnimplemented, ?p24: RefundDestinationDetailsP24, ?paynow: DestinationDetailsUnimplemented, ?paypal: RefundDestinationDetailsPaypal, ?pix: DestinationDetailsUnimplemented, ?revolut: DestinationDetailsUnimplemented, ?sofort: DestinationDetailsUnimplemented, ?swish: RefundDestinationDetailsSwish, ?thBankTransfer: RefundDestinationDetailsThBankTransfer, ?twint: DestinationDetailsUnimplemented, ?usBankTransfer: RefundDestinationDetailsUsBankTransfer, ?wechatPay: DestinationDetailsUnimplemented, ?zip: DestinationDetailsUnimplemented) =
            {
                RefundDestinationDetails.Type = ``type`` //required
                RefundDestinationDetails.Affirm = affirm
                RefundDestinationDetails.AfterpayClearpay = afterpayClearpay
                RefundDestinationDetails.Alipay = alipay
                RefundDestinationDetails.Alma = alma
                RefundDestinationDetails.AmazonPay = amazonPay
                RefundDestinationDetails.AuBankTransfer = auBankTransfer
                RefundDestinationDetails.Blik = blik
                RefundDestinationDetails.BrBankTransfer = brBankTransfer
                RefundDestinationDetails.Card = card
                RefundDestinationDetails.Cashapp = cashapp
                RefundDestinationDetails.Crypto = crypto
                RefundDestinationDetails.CustomerCashBalance = customerCashBalance
                RefundDestinationDetails.Eps = eps
                RefundDestinationDetails.EuBankTransfer = euBankTransfer
                RefundDestinationDetails.GbBankTransfer = gbBankTransfer
                RefundDestinationDetails.Giropay = giropay
                RefundDestinationDetails.Grabpay = grabpay
                RefundDestinationDetails.JpBankTransfer = jpBankTransfer
                RefundDestinationDetails.Klarna = klarna
                RefundDestinationDetails.MbWay = mbWay
                RefundDestinationDetails.Multibanco = multibanco
                RefundDestinationDetails.MxBankTransfer = mxBankTransfer
                RefundDestinationDetails.NzBankTransfer = nzBankTransfer
                RefundDestinationDetails.P24 = p24
                RefundDestinationDetails.Paynow = paynow
                RefundDestinationDetails.Paypal = paypal
                RefundDestinationDetails.Pix = pix
                RefundDestinationDetails.Revolut = revolut
                RefundDestinationDetails.Sofort = sofort
                RefundDestinationDetails.Swish = swish
                RefundDestinationDetails.ThBankTransfer = thBankTransfer
                RefundDestinationDetails.Twint = twint
                RefundDestinationDetails.UsBankTransfer = usBankTransfer
                RefundDestinationDetails.WechatPay = wechatPay
                RefundDestinationDetails.Zip = zip
            }

    and RefundDestinationDetailsBlik = {
        ///For refunds declined by the network, a decline code provided by the network which indicates the reason the refund failed.
        NetworkDeclineCode: string option
        ///The reference assigned to the refund.
        Reference: string option
        ///Status of the reference on the refund. This can be `pending`, `available` or `unavailable`.
        ReferenceStatus: RefundDestinationDetailsBlikReferenceStatus option
    }
    with
        static member New (networkDeclineCode: string option, reference: string option, referenceStatus: RefundDestinationDetailsBlikReferenceStatus option) =
            {
                RefundDestinationDetailsBlik.NetworkDeclineCode = networkDeclineCode //required
                RefundDestinationDetailsBlik.Reference = reference //required
                RefundDestinationDetailsBlik.ReferenceStatus = referenceStatus //required
            }

    and RefundDestinationDetailsBrBankTransfer = {
        ///The reference assigned to the refund.
        Reference: string option
        ///Status of the reference on the refund. This can be `pending`, `available` or `unavailable`.
        ReferenceStatus: RefundDestinationDetailsBrBankTransferReferenceStatus option
    }
    with
        static member New (reference: string option, referenceStatus: RefundDestinationDetailsBrBankTransferReferenceStatus option) =
            {
                RefundDestinationDetailsBrBankTransfer.Reference = reference //required
                RefundDestinationDetailsBrBankTransfer.ReferenceStatus = referenceStatus //required
            }

    and RefundDestinationDetailsCard = {
        ///Value of the reference number assigned to the refund.
        Reference: string option
        ///Status of the reference number on the refund. This can be `pending`, `available` or `unavailable`.
        ReferenceStatus: RefundDestinationDetailsCardReferenceStatus option
        ///Type of the reference number assigned to the refund.
        ReferenceType: string option
        ///The type of refund. This can be `refund`, `reversal`, or `pending`.
        Type: RefundDestinationDetailsCardType
    }
    with
        static member New (``type``: RefundDestinationDetailsCardType, ?reference: string, ?referenceStatus: RefundDestinationDetailsCardReferenceStatus, ?referenceType: string) =
            {
                RefundDestinationDetailsCard.Type = ``type`` //required
                RefundDestinationDetailsCard.Reference = reference
                RefundDestinationDetailsCard.ReferenceStatus = referenceStatus
                RefundDestinationDetailsCard.ReferenceType = referenceType
            }

    and RefundDestinationDetailsEuBankTransfer = {
        ///The reference assigned to the refund.
        Reference: string option
        ///Status of the reference on the refund. This can be `pending`, `available` or `unavailable`.
        ReferenceStatus: RefundDestinationDetailsEuBankTransferReferenceStatus option
    }
    with
        static member New (reference: string option, referenceStatus: RefundDestinationDetailsEuBankTransferReferenceStatus option) =
            {
                RefundDestinationDetailsEuBankTransfer.Reference = reference //required
                RefundDestinationDetailsEuBankTransfer.ReferenceStatus = referenceStatus //required
            }

    and RefundDestinationDetailsGbBankTransfer = {
        ///The reference assigned to the refund.
        Reference: string option
        ///Status of the reference on the refund. This can be `pending`, `available` or `unavailable`.
        ReferenceStatus: RefundDestinationDetailsGbBankTransferReferenceStatus option
    }
    with
        static member New (reference: string option, referenceStatus: RefundDestinationDetailsGbBankTransferReferenceStatus option) =
            {
                RefundDestinationDetailsGbBankTransfer.Reference = reference //required
                RefundDestinationDetailsGbBankTransfer.ReferenceStatus = referenceStatus //required
            }

    and RefundDestinationDetailsJpBankTransfer = {
        ///The reference assigned to the refund.
        Reference: string option
        ///Status of the reference on the refund. This can be `pending`, `available` or `unavailable`.
        ReferenceStatus: RefundDestinationDetailsJpBankTransferReferenceStatus option
    }
    with
        static member New (reference: string option, referenceStatus: RefundDestinationDetailsJpBankTransferReferenceStatus option) =
            {
                RefundDestinationDetailsJpBankTransfer.Reference = reference //required
                RefundDestinationDetailsJpBankTransfer.ReferenceStatus = referenceStatus //required
            }

    and RefundDestinationDetailsMbWay = {
        ///The reference assigned to the refund.
        Reference: string option
        ///Status of the reference on the refund. This can be `pending`, `available` or `unavailable`.
        ReferenceStatus: RefundDestinationDetailsMbWayReferenceStatus option
    }
    with
        static member New (reference: string option, referenceStatus: RefundDestinationDetailsMbWayReferenceStatus option) =
            {
                RefundDestinationDetailsMbWay.Reference = reference //required
                RefundDestinationDetailsMbWay.ReferenceStatus = referenceStatus //required
            }

    and RefundDestinationDetailsMultibanco = {
        ///The reference assigned to the refund.
        Reference: string option
        ///Status of the reference on the refund. This can be `pending`, `available` or `unavailable`.
        ReferenceStatus: RefundDestinationDetailsMultibancoReferenceStatus option
    }
    with
        static member New (reference: string option, referenceStatus: RefundDestinationDetailsMultibancoReferenceStatus option) =
            {
                RefundDestinationDetailsMultibanco.Reference = reference //required
                RefundDestinationDetailsMultibanco.ReferenceStatus = referenceStatus //required
            }

    and RefundDestinationDetailsMxBankTransfer = {
        ///The reference assigned to the refund.
        Reference: string option
        ///Status of the reference on the refund. This can be `pending`, `available` or `unavailable`.
        ReferenceStatus: RefundDestinationDetailsMxBankTransferReferenceStatus option
    }
    with
        static member New (reference: string option, referenceStatus: RefundDestinationDetailsMxBankTransferReferenceStatus option) =
            {
                RefundDestinationDetailsMxBankTransfer.Reference = reference //required
                RefundDestinationDetailsMxBankTransfer.ReferenceStatus = referenceStatus //required
            }

    and RefundDestinationDetailsP24 = {
        ///The reference assigned to the refund.
        Reference: string option
        ///Status of the reference on the refund. This can be `pending`, `available` or `unavailable`.
        ReferenceStatus: RefundDestinationDetailsP24ReferenceStatus option
    }
    with
        static member New (reference: string option, referenceStatus: RefundDestinationDetailsP24ReferenceStatus option) =
            {
                RefundDestinationDetailsP24.Reference = reference //required
                RefundDestinationDetailsP24.ReferenceStatus = referenceStatus //required
            }

    and RefundDestinationDetailsSwish = {
        ///For refunds declined by the network, a decline code provided by the network which indicates the reason the refund failed.
        NetworkDeclineCode: string option
        ///The reference assigned to the refund.
        Reference: string option
        ///Status of the reference on the refund. This can be `pending`, `available` or `unavailable`.
        ReferenceStatus: RefundDestinationDetailsSwishReferenceStatus option
    }
    with
        static member New (networkDeclineCode: string option, reference: string option, referenceStatus: RefundDestinationDetailsSwishReferenceStatus option) =
            {
                RefundDestinationDetailsSwish.NetworkDeclineCode = networkDeclineCode //required
                RefundDestinationDetailsSwish.Reference = reference //required
                RefundDestinationDetailsSwish.ReferenceStatus = referenceStatus //required
            }

    and RefundDestinationDetailsThBankTransfer = {
        ///The reference assigned to the refund.
        Reference: string option
        ///Status of the reference on the refund. This can be `pending`, `available` or `unavailable`.
        ReferenceStatus: RefundDestinationDetailsThBankTransferReferenceStatus option
    }
    with
        static member New (reference: string option, referenceStatus: RefundDestinationDetailsThBankTransferReferenceStatus option) =
            {
                RefundDestinationDetailsThBankTransfer.Reference = reference //required
                RefundDestinationDetailsThBankTransfer.ReferenceStatus = referenceStatus //required
            }

    and RefundDestinationDetailsUsBankTransfer = {
        ///The reference assigned to the refund.
        Reference: string option
        ///Status of the reference on the refund. This can be `pending`, `available` or `unavailable`.
        ReferenceStatus: RefundDestinationDetailsUsBankTransferReferenceStatus option
    }
    with
        static member New (reference: string option, referenceStatus: RefundDestinationDetailsUsBankTransferReferenceStatus option) =
            {
                RefundDestinationDetailsUsBankTransfer.Reference = reference //required
                RefundDestinationDetailsUsBankTransfer.ReferenceStatus = referenceStatus //required
            }

    and RefundNextAction = {
        DisplayDetails: RefundNextActionDisplayDetails option
        ///Type of the next action to perform.
        Type: string
    }
    with
        static member New (``type``: string, ?displayDetails: RefundNextActionDisplayDetails) =
            {
                RefundNextAction.Type = ``type`` //required
                RefundNextAction.DisplayDetails = displayDetails
            }

    and RefundNextActionDisplayDetails = {
        EmailSent: EmailSent
        ///The expiry timestamp.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime
    }
    with
        static member New (emailSent: EmailSent, expiresAt: DateTime) =
            {
                RefundNextActionDisplayDetails.EmailSent = emailSent //required
                RefundNextActionDisplayDetails.ExpiresAt = expiresAt //required
            }

    ///The Report Run object represents an instance of a report type generated with
    ///specific run parameters. Once the object is created, Stripe begins processing the report.
    ///When the report has finished running, it will give you a reference to a file
    ///where you can retrieve your results. For an overview, see
    ///[API Access to Reports](https://docs.stripe.com/reporting/statements/api).
    ///Note that certain report types can only be run based on your live-mode data (not test-mode
    ///data), and will error when queried without a [live-mode API key](https://docs.stripe.com/keys#test-live-modes).
    and ReportingReportRun = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///If something should go wrong during the run, a message about the failure (populated when
        /// `status=failed`).
        Error: string option
        ///Unique identifier for the object.
        Id: string
        ///`true` if the report is run on live mode data and `false` if it is run on test mode data.
        Livemode: bool
        Parameters: FinancialReportingFinanceReportRunRunParameters
        ///The ID of the [report type](https://docs.stripe.com/reports/report-types) to run, such as `"balance.summary.1"`.
        ReportType: string
        ///The file object representing the result of the report run (populated when
        /// `status=succeeded`).
        Result: File option
        ///Status of this report run. This will be `pending` when the run is initially created.
        /// When the run finishes, this will be set to `succeeded` and the `result` field will be populated.
        /// Rarely, we may encounter an error, at which point this will be set to `failed` and the `error` field will be populated.
        Status: string
        ///Timestamp at which this run successfully finished (populated when
        /// `status=succeeded`). Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]SucceededAt: DateTime option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "reporting.report_run"

        static member New (created: DateTime, error: string option, id: string, livemode: bool, parameters: FinancialReportingFinanceReportRunRunParameters, reportType: string, result: File option, status: string, succeededAt: DateTime option) =
            {
                ReportingReportRun.Created = created //required
                ReportingReportRun.Error = error //required
                ReportingReportRun.Id = id //required
                ReportingReportRun.Livemode = livemode //required
                ReportingReportRun.Parameters = parameters //required
                ReportingReportRun.ReportType = reportType //required
                ReportingReportRun.Result = result //required
                ReportingReportRun.Status = status //required
                ReportingReportRun.SucceededAt = succeededAt //required
            }

    ///The Report Type resource corresponds to a particular type of report, such as
    ///the "Activity summary" or "Itemized payouts" reports. These objects are
    ///identified by an ID belonging to a set of enumerated values. See
    ///[API Access to Reports documentation](https://docs.stripe.com/reporting/statements/api)
    ///for those Report Type IDs, along with required and optional parameters.
    ///Note that certain report types can only be run based on your live-mode data (not test-mode
    ///data), and will error when queried without a [live-mode API key](https://docs.stripe.com/keys#test-live-modes).
    and ReportingReportType = {
        ///Most recent time for which this Report Type is available. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]DataAvailableEnd: DateTime
        ///Earliest time for which this Report Type is available. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]DataAvailableStart: DateTime
        ///List of column names that are included by default when this Report Type gets run. (If the Report Type doesn't support the `columns` parameter, this will be null.)
        DefaultColumns: string list option
        ///The [ID of the Report Type](https://docs.stripe.com/reporting/statements/api#available-report-types), such as `balance.summary.1`.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Human-readable name of the Report Type
        Name: string
        ///When this Report Type was latest updated. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Updated: DateTime
        ///Version of the Report Type. Different versions report with the same ID will have the same purpose, but may take different run parameters or have different result schemas.
        Version: int
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "reporting.report_type"

        static member New (dataAvailableEnd: DateTime, dataAvailableStart: DateTime, defaultColumns: string list option, id: string, livemode: bool, name: string, updated: DateTime, version: int) =
            {
                ReportingReportType.DataAvailableEnd = dataAvailableEnd //required
                ReportingReportType.DataAvailableStart = dataAvailableStart //required
                ReportingReportType.DefaultColumns = defaultColumns //required
                ReportingReportType.Id = id //required
                ReportingReportType.Livemode = livemode //required
                ReportingReportType.Name = name //required
                ReportingReportType.Updated = updated //required
                ReportingReportType.Version = version //required
            }

    ///ReserveHolds are used to place a temporary ReserveHold on a merchant's funds.
    and ReserveHold = {
        ///Amount reserved. A positive integer representing how much is reserved in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
        Amount: int
        ///Amount in cents that can be released from this ReserveHold
        AmountReleasable: int option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Indicates which party created this ReserveHold.
        CreatedBy: ReserveHoldCreatedBy
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///Unique identifier for the object.
        Id: string
        ///Whether there are any funds available to release on this ReserveHold. Note that if the ReserveHold is in the process of being released, this could be false, even though the funds haven't been fully released yet.
        IsReleasable: bool option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///The reason for the ReserveHold.
        Reason: ReserveHoldReason
        ReleaseSchedule: ReservesReserveHoldsResourcesReleaseSchedule
        ///The ReservePlan which produced this ReserveHold (i.e., resplan_123)
        ReservePlan: ReserveHoldReservePlan'AnyOf option
        ///The Charge which funded this ReserveHold (e.g., ch_123)
        SourceCharge: ReserveHoldSourceCharge'AnyOf option
        ///Which source balance type this ReserveHold reserves funds from. One of `bank_account`, `card`, or `fpx`.
        SourceType: ReserveHoldSourceType
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "reserve.hold"

        static member New (amount: int, created: DateTime, createdBy: ReserveHoldCreatedBy, currency: IsoTypes.IsoCurrencyCode, id: string, livemode: bool, reason: ReserveHoldReason, releaseSchedule: ReservesReserveHoldsResourcesReleaseSchedule, reservePlan: ReserveHoldReservePlan'AnyOf option, sourceCharge: ReserveHoldSourceCharge'AnyOf option, sourceType: ReserveHoldSourceType, ?amountReleasable: int, ?isReleasable: bool, ?metadata: Map<string, string>) =
            {
                ReserveHold.Amount = amount //required
                ReserveHold.Created = created //required
                ReserveHold.CreatedBy = createdBy //required
                ReserveHold.Currency = currency //required
                ReserveHold.Id = id //required
                ReserveHold.Livemode = livemode //required
                ReserveHold.Reason = reason //required
                ReserveHold.ReleaseSchedule = releaseSchedule //required
                ReserveHold.ReservePlan = reservePlan //required
                ReserveHold.SourceCharge = sourceCharge //required
                ReserveHold.SourceType = sourceType //required
                ReserveHold.AmountReleasable = amountReleasable
                ReserveHold.IsReleasable = isReleasable
                ReserveHold.Metadata = metadata
            }

    and ReserveHoldReservePlan'AnyOf =
        | String of string
        | ReservePlan of ReservePlan

    and ReserveHoldSourceCharge'AnyOf =
        | String of string
        | Charge of Charge

    ///ReservePlans are used to automatically place holds on a merchant's funds until the plan expires. It takes a portion of each incoming Charge (including those resulting from a Transfer from a platform account).
    and ReservePlan = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Indicates which party created this ReservePlan.
        CreatedBy: ReservePlanCreatedBy
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). An unset currency indicates that the plan applies to all currencies.
        Currency: IsoTypes.IsoCurrencyCode option
        ///Time at which the ReservePlan was disabled.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]DisabledAt: DateTime option
        FixedRelease: ReservesReservePlansResourcesFixedRelease option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///The percent of each Charge to reserve.
        Percent: int
        RollingRelease: ReservesReservePlansResourcesRollingRelease option
        ///The current status of the ReservePlan. The ReservePlan only affects charges if it is `active`.
        Status: ReservePlanStatus
        ///The type of the ReservePlan.
        Type: ReservePlanType
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "reserve.plan"

        static member New (created: DateTime, createdBy: ReservePlanCreatedBy, currency: IsoTypes.IsoCurrencyCode option, disabledAt: DateTime option, id: string, livemode: bool, percent: int, status: ReservePlanStatus, ``type``: ReservePlanType, ?fixedRelease: ReservesReservePlansResourcesFixedRelease, ?metadata: Map<string, string>, ?rollingRelease: ReservesReservePlansResourcesRollingRelease) =
            {
                ReservePlan.Created = created //required
                ReservePlan.CreatedBy = createdBy //required
                ReservePlan.Currency = currency //required
                ReservePlan.DisabledAt = disabledAt //required
                ReservePlan.Id = id //required
                ReservePlan.Livemode = livemode //required
                ReservePlan.Percent = percent //required
                ReservePlan.Status = status //required
                ReservePlan.Type = ``type`` //required
                ReservePlan.FixedRelease = fixedRelease
                ReservePlan.Metadata = metadata
                ReservePlan.RollingRelease = rollingRelease
            }

    ///ReserveReleases represent the release of funds from a ReserveHold.
    and ReserveRelease = {
        ///Amount released. A positive integer representing how much is released in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
        Amount: int
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Indicates which party created this ReserveRelease.
        CreatedBy: ReserveReleaseCreatedBy
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///The reason for the ReserveRelease, indicating why the funds were released.
        Reason: ReserveReleaseReason
        ///The release timestamp of the funds.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ReleasedAt: DateTime
        ///The ReserveHold this ReserveRelease is associated with.
        ReserveHold: ReserveReleaseReserveHold'AnyOf option
        ///The ReservePlan ID this ReserveRelease is associated with. This field is only populated if a ReserveRelease is created by a ReservePlan disable operation, or from a scheduled ReservedHold expiry.
        ReservePlan: ReserveReleaseReservePlan'AnyOf option
        SourceTransaction: ReservesReserveReleasesResourcesSourceTransaction option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "reserve.release"

        static member New (amount: int, created: DateTime, createdBy: ReserveReleaseCreatedBy, currency: IsoTypes.IsoCurrencyCode, id: string, livemode: bool, reason: ReserveReleaseReason, releasedAt: DateTime, reserveHold: ReserveReleaseReserveHold'AnyOf option, reservePlan: ReserveReleaseReservePlan'AnyOf option, ?metadata: Map<string, string>, ?sourceTransaction: ReservesReserveReleasesResourcesSourceTransaction) =
            {
                ReserveRelease.Amount = amount //required
                ReserveRelease.Created = created //required
                ReserveRelease.CreatedBy = createdBy //required
                ReserveRelease.Currency = currency //required
                ReserveRelease.Id = id //required
                ReserveRelease.Livemode = livemode //required
                ReserveRelease.Reason = reason //required
                ReserveRelease.ReleasedAt = releasedAt //required
                ReserveRelease.ReserveHold = reserveHold //required
                ReserveRelease.ReservePlan = reservePlan //required
                ReserveRelease.Metadata = metadata
                ReserveRelease.SourceTransaction = sourceTransaction
            }

    and ReserveReleaseReserveHold'AnyOf =
        | String of string
        | ReserveHold of ReserveHold

    and ReserveReleaseReservePlan'AnyOf =
        | String of string
        | ReservePlan of ReservePlan

    and ReserveTransaction = {
        Amount: int
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///Unique identifier for the object.
        Id: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "reserve_transaction"

        static member New (amount: int, currency: IsoTypes.IsoCurrencyCode, description: string option, id: string) =
            {
                ReserveTransaction.Amount = amount //required
                ReserveTransaction.Currency = currency //required
                ReserveTransaction.Description = description //required
                ReserveTransaction.Id = id //required
            }

    and ReservesReserveReleasesResourcesSourceTransaction = {
        ///The ID of the dispute.
        Dispute: ReservesReserveReleasesResourcesSourceTransactionDispute'AnyOf option
        ///The ID of the refund.
        Refund: ReservesReserveReleasesResourcesSourceTransactionRefund'AnyOf option
        ///The type of source transaction.
        Type: ReservesReserveReleasesResourcesSourceTransactionType
    }
    with
        static member New (``type``: ReservesReserveReleasesResourcesSourceTransactionType, ?dispute: ReservesReserveReleasesResourcesSourceTransactionDispute'AnyOf, ?refund: ReservesReserveReleasesResourcesSourceTransactionRefund'AnyOf) =
            {
                ReservesReserveReleasesResourcesSourceTransaction.Type = ``type`` //required
                ReservesReserveReleasesResourcesSourceTransaction.Dispute = dispute
                ReservesReserveReleasesResourcesSourceTransaction.Refund = refund
            }

    and ReservesReserveReleasesResourcesSourceTransactionDispute'AnyOf =
        | String of string
        | Dispute of Dispute

    and ReservesReserveReleasesResourcesSourceTransactionRefund'AnyOf =
        | String of string
        | Refund of Refund

    ///Reviews can be used to supplement automated fraud detection with human expertise.
    ///Learn more about [Radar](/radar) and reviewing payments
    ///[here](https://docs.stripe.com/radar/reviews).
    and Review = {
        ///The ZIP or postal code of the card used, if applicable.
        BillingZip: string option
        ///The charge associated with this review.
        Charge: ReviewCharge'AnyOf option
        ///The reason the review was closed, or null if it has not yet been closed. One of `approved`, `refunded`, `refunded_as_fraud`, `disputed`, `redacted`, `canceled`, `payment_never_settled`, or `acknowledged`.
        ClosedReason: ReviewClosedReason option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Unique identifier for the object.
        Id: string
        ///The IP address where the payment originated.
        IpAddress: string option
        ///Information related to the location of the payment. Note that this information is an approximation and attempts to locate the nearest population center - it should not be used to determine a specific address.
        IpAddressLocation: RadarReviewResourceLocation option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///If `true`, the review needs action.
        Open: bool
        ///The reason the review was opened. One of `rule` or `manual`.
        OpenedReason: ReviewOpenedReason
        ///The PaymentIntent ID associated with this review, if one exists.
        PaymentIntent: ReviewPaymentIntent'AnyOf option
        ///The reason the review is currently open or closed. One of `rule`, `manual`, `approved`, `refunded`, `refunded_as_fraud`, `disputed`, `redacted`, `canceled`, `payment_never_settled`, or `acknowledged`.
        Reason: ReviewReason
        ///Information related to the browsing session of the user who initiated the payment.
        Session: RadarReviewResourceSession option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "review"

        static member New (billingZip: string option, charge: ReviewCharge'AnyOf option, closedReason: ReviewClosedReason option, created: DateTime, id: string, ipAddress: string option, ipAddressLocation: RadarReviewResourceLocation option, livemode: bool, ``open``: bool, openedReason: ReviewOpenedReason, reason: ReviewReason, session: RadarReviewResourceSession option, ?paymentIntent: ReviewPaymentIntent'AnyOf) =
            {
                Review.BillingZip = billingZip //required
                Review.Charge = charge //required
                Review.ClosedReason = closedReason //required
                Review.Created = created //required
                Review.Id = id //required
                Review.IpAddress = ipAddress //required
                Review.IpAddressLocation = ipAddressLocation //required
                Review.Livemode = livemode //required
                Review.Open = ``open`` //required
                Review.OpenedReason = openedReason //required
                Review.Reason = reason //required
                Review.Session = session //required
                Review.PaymentIntent = paymentIntent
            }

    and ReviewCharge'AnyOf =
        | String of string
        | Charge of Charge

    and ReviewPaymentIntent'AnyOf =
        | String of string
        | PaymentIntent of PaymentIntent

    and RevolutPayUnderlyingPaymentMethodFundingDetails = {
        Card: PaymentMethodDetailsPassthroughCard option
    }
    with
        ///funding type of the underlying payment method.
        member _.Type = "card"

        static member New (?card: PaymentMethodDetailsPassthroughCard) =
            {
                RevolutPayUnderlyingPaymentMethodFundingDetails.Card = card
            }

    ///If you have [scheduled a Sigma query](https://docs.stripe.com/sigma/scheduled-queries), you'll
    ///receive a `sigma.scheduled_query_run.created` webhook each time the query
    ///runs. The webhook contains a `ScheduledQueryRun` object, which you can use to
    ///retrieve the query results.
    and ScheduledQueryRun = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///When the query was run, Sigma contained a snapshot of your Stripe data at this time.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]DataLoadTime: DateTime
        Error: SigmaScheduledQueryRunError option
        ///The file object representing the results of the query.
        File: File option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Time at which the result expires and is no longer available for download.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ResultAvailableUntil: DateTime
        ///SQL for the query.
        Sql: string
        ///The query's execution status, which will be `completed` for successful runs, and `canceled`, `failed`, or `timed_out` otherwise.
        Status: string
        ///Title of the query.
        Title: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "scheduled_query_run"

        static member New (created: DateTime, dataLoadTime: DateTime, file: File option, id: string, livemode: bool, resultAvailableUntil: DateTime, sql: string, status: string, title: string, ?error: SigmaScheduledQueryRunError) =
            {
                ScheduledQueryRun.Created = created //required
                ScheduledQueryRun.DataLoadTime = dataLoadTime //required
                ScheduledQueryRun.File = file //required
                ScheduledQueryRun.Id = id //required
                ScheduledQueryRun.Livemode = livemode //required
                ScheduledQueryRun.ResultAvailableUntil = resultAvailableUntil //required
                ScheduledQueryRun.Sql = sql //required
                ScheduledQueryRun.Status = status //required
                ScheduledQueryRun.Title = title //required
                ScheduledQueryRun.Error = error
            }

    and SchedulesPhaseAutomaticTax = {
        ///Whether Stripe automatically computes tax on invoices created during this phase.
        Enabled: bool
        ///The account that's liable for tax. If set, the business address and tax registrations required to perform the tax calculation are loaded from this account. The tax transaction is returned in the report of the connected account.
        Liability: ConnectAccountReference option
    }
    with
        ///If Stripe disabled automatic tax, this enum describes why.
        member _.DisabledReason = "requires_location_inputs"

        static member New (enabled: bool, liability: ConnectAccountReference option) =
            {
                SchedulesPhaseAutomaticTax.Enabled = enabled //required
                SchedulesPhaseAutomaticTax.Liability = liability //required
            }

    and SecretServiceResourceScope = {
        ///The secret scope type.
        Type: SecretServiceResourceScopeType
        ///The user ID, if type is set to "user"
        User: string option
    }
    with
        static member New (``type``: SecretServiceResourceScopeType, ?user: string) =
            {
                SecretServiceResourceScope.Type = ``type`` //required
                SecretServiceResourceScope.User = user
            }

    and SepaDebitGeneratedFrom = {
        ///The ID of the Charge that generated this PaymentMethod, if any.
        Charge: SepaDebitGeneratedFromCharge'AnyOf option
        ///The ID of the SetupAttempt that generated this PaymentMethod, if any.
        SetupAttempt: SepaDebitGeneratedFromSetupAttempt'AnyOf option
    }
    with
        static member New (charge: SepaDebitGeneratedFromCharge'AnyOf option, setupAttempt: SepaDebitGeneratedFromSetupAttempt'AnyOf option) =
            {
                SepaDebitGeneratedFrom.Charge = charge //required
                SepaDebitGeneratedFrom.SetupAttempt = setupAttempt //required
            }

    and SepaDebitGeneratedFromCharge'AnyOf =
        | String of string
        | Charge of Charge

    and SepaDebitGeneratedFromSetupAttempt'AnyOf =
        | String of string
        | SetupAttempt of SetupAttempt

    ///A SetupAttempt describes one attempted confirmation of a SetupIntent,
    ///whether that confirmation is successful or unsuccessful. You can use
    ///SetupAttempts to inspect details of a specific attempt at setting up a
    ///payment method using a SetupIntent.
    and SetupAttempt = {
        ///The value of [application](https://docs.stripe.com/api/setup_intents/object#setup_intent_object-application) on the SetupIntent at the time of this confirmation.
        Application: SetupAttemptApplication'AnyOf option
        ///If present, the SetupIntent's payment method will be attached to the in-context Stripe Account.
        ///It can only be used for this Stripe Account’s own money movement flows like InboundTransfer and OutboundTransfers. It cannot be set to true when setting up a PaymentMethod for a Customer, and defaults to false when attaching a PaymentMethod to a Customer.
        AttachToSelf: bool option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The value of [customer](https://docs.stripe.com/api/setup_intents/object#setup_intent_object-customer) on the SetupIntent at the time of this confirmation.
        Customer: SetupAttemptCustomer'AnyOf option
        ///The value of [customer_account](https://docs.stripe.com/api/setup_intents/object#setup_intent_object-customer_account) on the SetupIntent at the time of this confirmation.
        CustomerAccount: string option
        ///Indicates the directions of money movement for which this payment method is intended to be used.
        ///Include `inbound` if you intend to use the payment method as the origin to pull funds from. Include `outbound` if you intend to use the payment method as the destination to send funds to. You can include both if you intend to use the payment method for both purposes.
        FlowDirections: SetupAttemptFlowDirections list option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The value of [on_behalf_of](https://docs.stripe.com/api/setup_intents/object#setup_intent_object-on_behalf_of) on the SetupIntent at the time of this confirmation.
        OnBehalfOf: SetupAttemptOnBehalfOf'AnyOf option
        ///ID of the payment method used with this SetupAttempt.
        PaymentMethod: SetupAttemptPaymentMethod'AnyOf
        PaymentMethodDetails: SetupAttemptPaymentMethodDetails
        ///The error encountered during this attempt to confirm the SetupIntent, if any.
        SetupError: ApiErrors option
        ///ID of the SetupIntent that this attempt belongs to.
        SetupIntent: SetupAttemptSetupIntent'AnyOf
        ///Status of this SetupAttempt, one of `requires_confirmation`, `requires_action`, `processing`, `succeeded`, `failed`, or `abandoned`.
        Status: SetupAttemptStatus
        ///The value of [usage](https://docs.stripe.com/api/setup_intents/object#setup_intent_object-usage) on the SetupIntent at the time of this confirmation, one of `off_session` or `on_session`.
        Usage: SetupAttemptUsage
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "setup_attempt"

        static member New (application: SetupAttemptApplication'AnyOf option, created: DateTime, customer: SetupAttemptCustomer'AnyOf option, customerAccount: string option, flowDirections: SetupAttemptFlowDirections list option, id: string, livemode: bool, onBehalfOf: SetupAttemptOnBehalfOf'AnyOf option, paymentMethod: SetupAttemptPaymentMethod'AnyOf, paymentMethodDetails: SetupAttemptPaymentMethodDetails, setupError: ApiErrors option, setupIntent: SetupAttemptSetupIntent'AnyOf, status: SetupAttemptStatus, usage: SetupAttemptUsage, ?attachToSelf: bool) =
            {
                SetupAttempt.Application = application //required
                SetupAttempt.Created = created //required
                SetupAttempt.Customer = customer //required
                SetupAttempt.CustomerAccount = customerAccount //required
                SetupAttempt.FlowDirections = flowDirections //required
                SetupAttempt.Id = id //required
                SetupAttempt.Livemode = livemode //required
                SetupAttempt.OnBehalfOf = onBehalfOf //required
                SetupAttempt.PaymentMethod = paymentMethod //required
                SetupAttempt.PaymentMethodDetails = paymentMethodDetails //required
                SetupAttempt.SetupError = setupError //required
                SetupAttempt.SetupIntent = setupIntent //required
                SetupAttempt.Status = status //required
                SetupAttempt.Usage = usage //required
                SetupAttempt.AttachToSelf = attachToSelf
            }

    and SetupAttemptApplication'AnyOf =
        | String of string
        | Application of Application

    and SetupAttemptCustomer'AnyOf =
        | String of string
        | Customer of Customer
        | DeletedCustomer of DeletedCustomer

    and SetupAttemptOnBehalfOf'AnyOf =
        | String of string
        | Account of Account

    and SetupAttemptPaymentMethod'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and SetupAttemptSetupIntent'AnyOf =
        | String of string
        | SetupIntent of SetupIntent

    and SetupAttemptPaymentMethodDetails = {
        AcssDebit: SetupAttemptPaymentMethodDetailsAcssDebit option
        AmazonPay: SetupAttemptPaymentMethodDetailsAmazonPay option
        AuBecsDebit: SetupAttemptPaymentMethodDetailsAuBecsDebit option
        BacsDebit: SetupAttemptPaymentMethodDetailsBacsDebit option
        Bancontact: SetupAttemptPaymentMethodDetailsBancontact option
        Boleto: SetupAttemptPaymentMethodDetailsBoleto option
        Card: SetupAttemptPaymentMethodDetailsCard option
        CardPresent: SetupAttemptPaymentMethodDetailsCardPresent option
        Cashapp: SetupAttemptPaymentMethodDetailsCashapp option
        Ideal: SetupAttemptPaymentMethodDetailsIdeal option
        KakaoPay: SetupAttemptPaymentMethodDetailsKakaoPay option
        Klarna: SetupAttemptPaymentMethodDetailsKlarna option
        KrCard: SetupAttemptPaymentMethodDetailsKrCard option
        Link: SetupAttemptPaymentMethodDetailsLink option
        NaverPay: SetupAttemptPaymentMethodDetailsNaverPay option
        NzBankAccount: SetupAttemptPaymentMethodDetailsNzBankAccount option
        Paypal: SetupAttemptPaymentMethodDetailsPaypal option
        Payto: SetupAttemptPaymentMethodDetailsPayto option
        Pix: SetupAttemptPaymentMethodDetailsPix option
        RevolutPay: SetupAttemptPaymentMethodDetailsRevolutPay option
        SepaDebit: SetupAttemptPaymentMethodDetailsSepaDebit option
        Sofort: SetupAttemptPaymentMethodDetailsSofort option
        ///The type of the payment method used in the SetupIntent (e.g., `card`). An additional hash is included on `payment_method_details` with a name matching this value. It contains confirmation-specific information for the payment method.
        Type: string
        Upi: SetupAttemptPaymentMethodDetailsUpi option
        UsBankAccount: SetupAttemptPaymentMethodDetailsUsBankAccount option
    }
    with
        static member New (``type``: string, ?acssDebit: SetupAttemptPaymentMethodDetailsAcssDebit, ?amazonPay: SetupAttemptPaymentMethodDetailsAmazonPay, ?auBecsDebit: SetupAttemptPaymentMethodDetailsAuBecsDebit, ?bacsDebit: SetupAttemptPaymentMethodDetailsBacsDebit, ?bancontact: SetupAttemptPaymentMethodDetailsBancontact, ?boleto: SetupAttemptPaymentMethodDetailsBoleto, ?card: SetupAttemptPaymentMethodDetailsCard, ?cardPresent: SetupAttemptPaymentMethodDetailsCardPresent, ?cashapp: SetupAttemptPaymentMethodDetailsCashapp, ?ideal: SetupAttemptPaymentMethodDetailsIdeal, ?kakaoPay: SetupAttemptPaymentMethodDetailsKakaoPay, ?klarna: SetupAttemptPaymentMethodDetailsKlarna, ?krCard: SetupAttemptPaymentMethodDetailsKrCard, ?link: SetupAttemptPaymentMethodDetailsLink, ?naverPay: SetupAttemptPaymentMethodDetailsNaverPay, ?nzBankAccount: SetupAttemptPaymentMethodDetailsNzBankAccount, ?paypal: SetupAttemptPaymentMethodDetailsPaypal, ?payto: SetupAttemptPaymentMethodDetailsPayto, ?pix: SetupAttemptPaymentMethodDetailsPix, ?revolutPay: SetupAttemptPaymentMethodDetailsRevolutPay, ?sepaDebit: SetupAttemptPaymentMethodDetailsSepaDebit, ?sofort: SetupAttemptPaymentMethodDetailsSofort, ?upi: SetupAttemptPaymentMethodDetailsUpi, ?usBankAccount: SetupAttemptPaymentMethodDetailsUsBankAccount) =
            {
                SetupAttemptPaymentMethodDetails.Type = ``type`` //required
                SetupAttemptPaymentMethodDetails.AcssDebit = acssDebit
                SetupAttemptPaymentMethodDetails.AmazonPay = amazonPay
                SetupAttemptPaymentMethodDetails.AuBecsDebit = auBecsDebit
                SetupAttemptPaymentMethodDetails.BacsDebit = bacsDebit
                SetupAttemptPaymentMethodDetails.Bancontact = bancontact
                SetupAttemptPaymentMethodDetails.Boleto = boleto
                SetupAttemptPaymentMethodDetails.Card = card
                SetupAttemptPaymentMethodDetails.CardPresent = cardPresent
                SetupAttemptPaymentMethodDetails.Cashapp = cashapp
                SetupAttemptPaymentMethodDetails.Ideal = ideal
                SetupAttemptPaymentMethodDetails.KakaoPay = kakaoPay
                SetupAttemptPaymentMethodDetails.Klarna = klarna
                SetupAttemptPaymentMethodDetails.KrCard = krCard
                SetupAttemptPaymentMethodDetails.Link = link
                SetupAttemptPaymentMethodDetails.NaverPay = naverPay
                SetupAttemptPaymentMethodDetails.NzBankAccount = nzBankAccount
                SetupAttemptPaymentMethodDetails.Paypal = paypal
                SetupAttemptPaymentMethodDetails.Payto = payto
                SetupAttemptPaymentMethodDetails.Pix = pix
                SetupAttemptPaymentMethodDetails.RevolutPay = revolutPay
                SetupAttemptPaymentMethodDetails.SepaDebit = sepaDebit
                SetupAttemptPaymentMethodDetails.Sofort = sofort
                SetupAttemptPaymentMethodDetails.Upi = upi
                SetupAttemptPaymentMethodDetails.UsBankAccount = usBankAccount
            }

    and SetupAttemptPaymentMethodDetailsBancontact = {
        ///Bank code of bank associated with the bank account.
        BankCode: string option
        ///Name of the bank associated with the bank account.
        BankName: string option
        ///Bank Identifier Code of the bank associated with the bank account.
        Bic: string option
        ///The ID of the SEPA Direct Debit PaymentMethod which was generated by this SetupAttempt.
        GeneratedSepaDebit: SetupAttemptPaymentMethodDetailsBancontactGeneratedSepaDebit'AnyOf option
        ///The mandate for the SEPA Direct Debit PaymentMethod which was generated by this SetupAttempt.
        GeneratedSepaDebitMandate: SetupAttemptPaymentMethodDetailsBancontactGeneratedSepaDebitMandate'AnyOf option
        ///Last four characters of the IBAN.
        [<JsonField(Name="iban_last4")>]IbanLast4: string option
        ///Preferred language of the Bancontact authorization page that the customer is redirected to.
        ///Can be one of `en`, `de`, `fr`, or `nl`
        PreferredLanguage: SetupAttemptPaymentMethodDetailsBancontactPreferredLanguage option
        ///Owner's verified full name. Values are verified or provided by Bancontact directly
        ///(if supported) at the time of authorization or settlement. They cannot be set or mutated.
        VerifiedName: string option
    }
    with
        static member New (bankCode: string option, bankName: string option, bic: string option, generatedSepaDebit: SetupAttemptPaymentMethodDetailsBancontactGeneratedSepaDebit'AnyOf option, generatedSepaDebitMandate: SetupAttemptPaymentMethodDetailsBancontactGeneratedSepaDebitMandate'AnyOf option, ibanLast4: string option, preferredLanguage: SetupAttemptPaymentMethodDetailsBancontactPreferredLanguage option, verifiedName: string option) =
            {
                SetupAttemptPaymentMethodDetailsBancontact.BankCode = bankCode //required
                SetupAttemptPaymentMethodDetailsBancontact.BankName = bankName //required
                SetupAttemptPaymentMethodDetailsBancontact.Bic = bic //required
                SetupAttemptPaymentMethodDetailsBancontact.GeneratedSepaDebit = generatedSepaDebit //required
                SetupAttemptPaymentMethodDetailsBancontact.GeneratedSepaDebitMandate = generatedSepaDebitMandate //required
                SetupAttemptPaymentMethodDetailsBancontact.IbanLast4 = ibanLast4 //required
                SetupAttemptPaymentMethodDetailsBancontact.PreferredLanguage = preferredLanguage //required
                SetupAttemptPaymentMethodDetailsBancontact.VerifiedName = verifiedName //required
            }

    and SetupAttemptPaymentMethodDetailsBancontactGeneratedSepaDebit'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and SetupAttemptPaymentMethodDetailsBancontactGeneratedSepaDebitMandate'AnyOf =
        | String of string
        | Mandate of Mandate

    and SetupAttemptPaymentMethodDetailsCard = {
        ///Card brand. Can be `amex`, `cartes_bancaires`, `diners`, `discover`, `eftpos_au`, `jcb`, `link`, `mastercard`, `unionpay`, `visa` or `unknown`.
        Brand: SetupAttemptPaymentMethodDetailsCardBrand option
        ///Check results by Card networks on Card address and CVC at the time of authorization
        Checks: SetupAttemptPaymentMethodDetailsCardChecks option
        ///Two-letter ISO code representing the country of the card. You could use this attribute to get a sense of the international breakdown of cards you've collected.
        Country: IsoTypes.IsoCountryCode option
        ///A high-level description of the type of cards issued in this range. (For internal use only and not typically available in standard API requests.)
        Description: string option
        ///Two-digit number representing the card's expiration month.
        ExpMonth: int option
        ///Four-digit number representing the card's expiration year.
        ExpYear: int option
        ///Uniquely identifies this particular card number. You can use this attribute to check whether two customers who’ve signed up with you are using the same card number, for example. For payment methods that tokenize card information (Apple Pay, Google Pay), the tokenized number might be provided instead of the underlying card number.
        ///*As of May 1, 2021, card fingerprint in India for Connect changed to allow two fingerprints for the same card---one for India and one for the rest of the world.*
        Fingerprint: string option
        ///Card funding type. Can be `credit`, `debit`, `prepaid`, or `unknown`.
        Funding: SetupAttemptPaymentMethodDetailsCardFunding option
        ///Issuer identification number of the card. (For internal use only and not typically available in standard API requests.)
        Iin: string option
        ///The name of the card's issuing bank. (For internal use only and not typically available in standard API requests.)
        Issuer: string option
        ///The last four digits of the card.
        [<JsonField(Name="last4")>]Last4: string option
        ///True if this payment was marked as MOTO and out of scope for SCA.
        Moto: bool option
        ///Identifies which network this charge was processed on. Can be `amex`, `cartes_bancaires`, `diners`, `discover`, `eftpos_au`, `interac`, `jcb`, `link`, `mastercard`, `unionpay`, `visa`, or `unknown`.
        Network: SetupAttemptPaymentMethodDetailsCardNetwork option
        ///Populated if this authorization used 3D Secure authentication.
        ThreeDSecure: ThreeDSecureDetails option
        ///If this Card is part of a card wallet, this contains the details of the card wallet.
        Wallet: SetupAttemptPaymentMethodDetailsCardWallet option
    }
    with
        static member New (brand: SetupAttemptPaymentMethodDetailsCardBrand option, checks: SetupAttemptPaymentMethodDetailsCardChecks option, country: IsoTypes.IsoCountryCode option, expMonth: int option, expYear: int option, funding: SetupAttemptPaymentMethodDetailsCardFunding option, last4: string option, network: SetupAttemptPaymentMethodDetailsCardNetwork option, threeDSecure: ThreeDSecureDetails option, wallet: SetupAttemptPaymentMethodDetailsCardWallet option, ?description: string option, ?fingerprint: string option, ?iin: string option, ?issuer: string option, ?moto: bool) =
            {
                SetupAttemptPaymentMethodDetailsCard.Brand = brand //required
                SetupAttemptPaymentMethodDetailsCard.Checks = checks //required
                SetupAttemptPaymentMethodDetailsCard.Country = country //required
                SetupAttemptPaymentMethodDetailsCard.ExpMonth = expMonth //required
                SetupAttemptPaymentMethodDetailsCard.ExpYear = expYear //required
                SetupAttemptPaymentMethodDetailsCard.Funding = funding //required
                SetupAttemptPaymentMethodDetailsCard.Last4 = last4 //required
                SetupAttemptPaymentMethodDetailsCard.Network = network //required
                SetupAttemptPaymentMethodDetailsCard.ThreeDSecure = threeDSecure //required
                SetupAttemptPaymentMethodDetailsCard.Wallet = wallet //required
                SetupAttemptPaymentMethodDetailsCard.Description = description |> Option.flatten
                SetupAttemptPaymentMethodDetailsCard.Fingerprint = fingerprint |> Option.flatten
                SetupAttemptPaymentMethodDetailsCard.Iin = iin |> Option.flatten
                SetupAttemptPaymentMethodDetailsCard.Issuer = issuer |> Option.flatten
                SetupAttemptPaymentMethodDetailsCard.Moto = moto
            }

    and SetupAttemptPaymentMethodDetailsCardChecks = {
        ///If a address line1 was provided, results of the check, one of `pass`, `fail`, `unavailable`, or `unchecked`.
        [<JsonField(Name="address_line1_check")>]AddressLine1Check: SetupAttemptPaymentMethodDetailsCardChecksAddressLine1Check option
        ///If a address postal code was provided, results of the check, one of `pass`, `fail`, `unavailable`, or `unchecked`.
        AddressPostalCodeCheck: SetupAttemptPaymentMethodDetailsCardChecksAddressPostalCodeCheck option
        ///If a CVC was provided, results of the check, one of `pass`, `fail`, `unavailable`, or `unchecked`.
        CvcCheck: SetupAttemptPaymentMethodDetailsCardChecksCvcCheck option
    }
    with
        static member New (addressLine1Check: SetupAttemptPaymentMethodDetailsCardChecksAddressLine1Check option, addressPostalCodeCheck: SetupAttemptPaymentMethodDetailsCardChecksAddressPostalCodeCheck option, cvcCheck: SetupAttemptPaymentMethodDetailsCardChecksCvcCheck option) =
            {
                SetupAttemptPaymentMethodDetailsCardChecks.AddressLine1Check = addressLine1Check //required
                SetupAttemptPaymentMethodDetailsCardChecks.AddressPostalCodeCheck = addressPostalCodeCheck //required
                SetupAttemptPaymentMethodDetailsCardChecks.CvcCheck = cvcCheck //required
            }

    and SetupAttemptPaymentMethodDetailsCardPresent = {
        ///The ID of the Card PaymentMethod which was generated by this SetupAttempt.
        GeneratedCard: SetupAttemptPaymentMethodDetailsCardPresentGeneratedCard'AnyOf option
        ///Details about payments collected offline.
        Offline: PaymentMethodDetailsCardPresentOffline option
    }
    with
        static member New (generatedCard: SetupAttemptPaymentMethodDetailsCardPresentGeneratedCard'AnyOf option, offline: PaymentMethodDetailsCardPresentOffline option) =
            {
                SetupAttemptPaymentMethodDetailsCardPresent.GeneratedCard = generatedCard //required
                SetupAttemptPaymentMethodDetailsCardPresent.Offline = offline //required
            }

    and SetupAttemptPaymentMethodDetailsCardPresentGeneratedCard'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and SetupAttemptPaymentMethodDetailsCardWallet = {
        ApplePay: PaymentMethodDetailsCardWalletApplePay option
        GooglePay: PaymentMethodDetailsCardWalletGooglePay option
        ///The type of the card wallet, one of `apple_pay`, `google_pay`, or `link`. An additional hash is included on the Wallet subhash with a name matching this value. It contains additional information specific to the card wallet type.
        Type: SetupAttemptPaymentMethodDetailsCardWalletType
    }
    with
        static member New (``type``: SetupAttemptPaymentMethodDetailsCardWalletType, ?applePay: PaymentMethodDetailsCardWalletApplePay, ?googlePay: PaymentMethodDetailsCardWalletGooglePay) =
            {
                SetupAttemptPaymentMethodDetailsCardWallet.Type = ``type`` //required
                SetupAttemptPaymentMethodDetailsCardWallet.ApplePay = applePay
                SetupAttemptPaymentMethodDetailsCardWallet.GooglePay = googlePay
            }

    and SetupAttemptPaymentMethodDetailsIdeal = {
        ///The customer's bank. Can be one of `abn_amro`, `adyen`, `asn_bank`, `bunq`, `buut`, `finom`, `handelsbanken`, `ing`, `knab`, `mollie`, `moneyou`, `n26`, `nn`, `rabobank`, `regiobank`, `revolut`, `sns_bank`, `triodos_bank`, `van_lanschot`, or `yoursafe`.
        Bank: SetupAttemptPaymentMethodDetailsIdealBank option
        ///The Bank Identifier Code of the customer's bank.
        Bic: SetupAttemptPaymentMethodDetailsIdealBic option
        ///The ID of the SEPA Direct Debit PaymentMethod which was generated by this SetupAttempt.
        GeneratedSepaDebit: SetupAttemptPaymentMethodDetailsIdealGeneratedSepaDebit'AnyOf option
        ///The mandate for the SEPA Direct Debit PaymentMethod which was generated by this SetupAttempt.
        GeneratedSepaDebitMandate: SetupAttemptPaymentMethodDetailsIdealGeneratedSepaDebitMandate'AnyOf option
        ///Last four characters of the IBAN.
        [<JsonField(Name="iban_last4")>]IbanLast4: string option
        ///Owner's verified full name. Values are verified or provided by iDEAL directly
        ///(if supported) at the time of authorization or settlement. They cannot be set or mutated.
        VerifiedName: string option
    }
    with
        static member New (bank: SetupAttemptPaymentMethodDetailsIdealBank option, bic: SetupAttemptPaymentMethodDetailsIdealBic option, generatedSepaDebit: SetupAttemptPaymentMethodDetailsIdealGeneratedSepaDebit'AnyOf option, generatedSepaDebitMandate: SetupAttemptPaymentMethodDetailsIdealGeneratedSepaDebitMandate'AnyOf option, ibanLast4: string option, verifiedName: string option) =
            {
                SetupAttemptPaymentMethodDetailsIdeal.Bank = bank //required
                SetupAttemptPaymentMethodDetailsIdeal.Bic = bic //required
                SetupAttemptPaymentMethodDetailsIdeal.GeneratedSepaDebit = generatedSepaDebit //required
                SetupAttemptPaymentMethodDetailsIdeal.GeneratedSepaDebitMandate = generatedSepaDebitMandate //required
                SetupAttemptPaymentMethodDetailsIdeal.IbanLast4 = ibanLast4 //required
                SetupAttemptPaymentMethodDetailsIdeal.VerifiedName = verifiedName //required
            }

    and SetupAttemptPaymentMethodDetailsIdealGeneratedSepaDebit'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and SetupAttemptPaymentMethodDetailsIdealGeneratedSepaDebitMandate'AnyOf =
        | String of string
        | Mandate of Mandate

    and SetupAttemptPaymentMethodDetailsSofort = {
        ///Bank code of bank associated with the bank account.
        BankCode: string option
        ///Name of the bank associated with the bank account.
        BankName: string option
        ///Bank Identifier Code of the bank associated with the bank account.
        Bic: string option
        ///The ID of the SEPA Direct Debit PaymentMethod which was generated by this SetupAttempt.
        GeneratedSepaDebit: SetupAttemptPaymentMethodDetailsSofortGeneratedSepaDebit'AnyOf option
        ///The mandate for the SEPA Direct Debit PaymentMethod which was generated by this SetupAttempt.
        GeneratedSepaDebitMandate: SetupAttemptPaymentMethodDetailsSofortGeneratedSepaDebitMandate'AnyOf option
        ///Last four characters of the IBAN.
        [<JsonField(Name="iban_last4")>]IbanLast4: string option
        ///Preferred language of the Sofort authorization page that the customer is redirected to.
        ///Can be one of `en`, `de`, `fr`, or `nl`
        PreferredLanguage: SetupAttemptPaymentMethodDetailsSofortPreferredLanguage option
        ///Owner's verified full name. Values are verified or provided by Sofort directly
        ///(if supported) at the time of authorization or settlement. They cannot be set or mutated.
        VerifiedName: string option
    }
    with
        static member New (bankCode: string option, bankName: string option, bic: string option, generatedSepaDebit: SetupAttemptPaymentMethodDetailsSofortGeneratedSepaDebit'AnyOf option, generatedSepaDebitMandate: SetupAttemptPaymentMethodDetailsSofortGeneratedSepaDebitMandate'AnyOf option, ibanLast4: string option, preferredLanguage: SetupAttemptPaymentMethodDetailsSofortPreferredLanguage option, verifiedName: string option) =
            {
                SetupAttemptPaymentMethodDetailsSofort.BankCode = bankCode //required
                SetupAttemptPaymentMethodDetailsSofort.BankName = bankName //required
                SetupAttemptPaymentMethodDetailsSofort.Bic = bic //required
                SetupAttemptPaymentMethodDetailsSofort.GeneratedSepaDebit = generatedSepaDebit //required
                SetupAttemptPaymentMethodDetailsSofort.GeneratedSepaDebitMandate = generatedSepaDebitMandate //required
                SetupAttemptPaymentMethodDetailsSofort.IbanLast4 = ibanLast4 //required
                SetupAttemptPaymentMethodDetailsSofort.PreferredLanguage = preferredLanguage //required
                SetupAttemptPaymentMethodDetailsSofort.VerifiedName = verifiedName //required
            }

    and SetupAttemptPaymentMethodDetailsSofortGeneratedSepaDebit'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and SetupAttemptPaymentMethodDetailsSofortGeneratedSepaDebitMandate'AnyOf =
        | String of string
        | Mandate of Mandate

    ///A SetupIntent guides you through the process of setting up and saving a customer's payment credentials for future payments.
    ///For example, you can use a SetupIntent to set up and save your customer's card without immediately collecting a payment.
    ///Later, you can use [PaymentIntents](https://api.stripe.com#payment_intents) to drive the payment flow.
    ///Create a SetupIntent when you're ready to collect your customer's payment credentials.
    ///Don't maintain long-lived, unconfirmed SetupIntents because they might not be valid.
    ///The SetupIntent transitions through multiple [statuses](https://docs.stripe.com/payments/intents#intent-statuses) as it guides
    ///you through the setup process.
    ///Successful SetupIntents result in payment credentials that are optimized for future payments.
    ///For example, cardholders in [certain regions](https://stripe.com/guides/strong-customer-authentication) might need to be run through
    ///[Strong Customer Authentication](https://docs.stripe.com/strong-customer-authentication) during payment method collection
    ///to streamline later [off-session payments](https://docs.stripe.com/payments/setup-intents).
    ///If you use the SetupIntent with a [Customer](https://api.stripe.com#setup_intent_object-customer),
    ///it automatically attaches the resulting payment method to that Customer after successful setup.
    ///We recommend using SetupIntents or [setup_future_usage](https://api.stripe.com#payment_intent_object-setup_future_usage) on
    ///PaymentIntents to save payment methods to prevent saving invalid or unoptimized payment methods.
    ///By using SetupIntents, you can reduce friction for your customers, even as regulations change over time.
    ///Related guide: [Setup Intents API](https://docs.stripe.com/payments/setup-intents)
    and SetupIntent = {
        ///ID of the Connect application that created the SetupIntent.
        Application: SetupIntentApplication'AnyOf option
        ///If present, the SetupIntent's payment method will be attached to the in-context Stripe Account.
        ///It can only be used for this Stripe Account’s own money movement flows like InboundTransfer and OutboundTransfers. It cannot be set to true when setting up a PaymentMethod for a Customer, and defaults to false when attaching a PaymentMethod to a Customer.
        AttachToSelf: bool option
        ///Settings for dynamic payment methods compatible with this Setup Intent
        AutomaticPaymentMethods: PaymentFlowsAutomaticPaymentMethodsSetupIntent option
        ///Reason for cancellation of this SetupIntent, one of `abandoned`, `requested_by_customer`, or `duplicate`.
        CancellationReason: SetupIntentCancellationReason option
        ///The client secret of this SetupIntent. Used for client-side retrieval using a publishable key.
        ///The client secret can be used to complete payment setup from your frontend. It should not be stored, logged, or exposed to anyone other than the customer. Make sure that you have TLS enabled on any page that includes the client secret.
        ClientSecret: string option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///ID of the Customer this SetupIntent belongs to, if one exists.
        ///If present, the SetupIntent's payment method will be attached to the Customer on successful setup. Payment methods attached to other Customers cannot be used with this SetupIntent.
        Customer: SetupIntentCustomer'AnyOf option
        ///ID of the Account this SetupIntent belongs to, if one exists.
        ///If present, the SetupIntent's payment method will be attached to the Account on successful setup. Payment methods attached to other Accounts cannot be used with this SetupIntent.
        CustomerAccount: string option
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///Payment method types that are excluded from this SetupIntent.
        ExcludedPaymentMethodTypes: SetupIntentExcludedPaymentMethodTypes list option
        ///Indicates the directions of money movement for which this payment method is intended to be used.
        ///Include `inbound` if you intend to use the payment method as the origin to pull funds from. Include `outbound` if you intend to use the payment method as the destination to send funds to. You can include both if you intend to use the payment method for both purposes.
        FlowDirections: SetupIntentFlowDirections list option
        ///Unique identifier for the object.
        Id: string
        ///The error encountered in the previous SetupIntent confirmation.
        LastSetupError: ApiErrors option
        ///The most recent SetupAttempt for this SetupIntent.
        LatestAttempt: SetupIntentLatestAttempt'AnyOf option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ManagedPayments: SmorResourceManagedPayments option
        ///ID of the multi use Mandate generated by the SetupIntent.
        Mandate: SetupIntentMandate'AnyOf option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///If present, this property tells you what actions you need to take in order for your customer to continue payment setup.
        NextAction: SetupIntentNextAction option
        ///The account (if any) for which the setup is intended.
        OnBehalfOf: SetupIntentOnBehalfOf'AnyOf option
        ///ID of the payment method used with this SetupIntent. If the payment method is `card_present` and isn't a digital wallet, then the [generated_card](https://docs.stripe.com/api/setup_attempts/object#setup_attempt_object-payment_method_details-card_present-generated_card) associated with the `latest_attempt` is attached to the Customer instead.
        PaymentMethod: SetupIntentPaymentMethod'AnyOf option
        ///Information about the [payment method configuration](https://docs.stripe.com/api/payment_method_configurations) used for this Setup Intent.
        PaymentMethodConfigurationDetails: PaymentMethodConfigBizPaymentMethodConfigurationDetails option
        ///Payment method-specific configuration for this SetupIntent.
        PaymentMethodOptions: SetupIntentPaymentMethodOptions option
        ///The list of payment method types (e.g. card) that this SetupIntent is allowed to set up. A list of valid payment method types can be found [here](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type).
        PaymentMethodTypes: string list
        ///ID of the single_use Mandate generated by the SetupIntent.
        SingleUseMandate: SetupIntentSingleUseMandate'AnyOf option
        ///[Status](https://docs.stripe.com/payments/intents#intent-statuses) of this SetupIntent, one of `requires_payment_method`, `requires_confirmation`, `requires_action`, `processing`, `canceled`, or `succeeded`.
        Status: SetupIntentStatus
        ///Indicates how the payment method is intended to be used in the future.
        ///Use `on_session` if you intend to only reuse the payment method when the customer is in your checkout flow. Use `off_session` if your customer may or may not be in your checkout flow. If not provided, this value defaults to `off_session`.
        Usage: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "setup_intent"

        static member New (application: SetupIntentApplication'AnyOf option, automaticPaymentMethods: PaymentFlowsAutomaticPaymentMethodsSetupIntent option, cancellationReason: SetupIntentCancellationReason option, clientSecret: string option, created: DateTime, customer: SetupIntentCustomer'AnyOf option, description: string option, excludedPaymentMethodTypes: SetupIntentExcludedPaymentMethodTypes list option, id: string, lastSetupError: ApiErrors option, latestAttempt: SetupIntentLatestAttempt'AnyOf option, livemode: bool, mandate: SetupIntentMandate'AnyOf option, metadata: Map<string, string> option, nextAction: SetupIntentNextAction option, onBehalfOf: SetupIntentOnBehalfOf'AnyOf option, paymentMethod: SetupIntentPaymentMethod'AnyOf option, paymentMethodConfigurationDetails: PaymentMethodConfigBizPaymentMethodConfigurationDetails option, paymentMethodOptions: SetupIntentPaymentMethodOptions option, paymentMethodTypes: string list, singleUseMandate: SetupIntentSingleUseMandate'AnyOf option, status: SetupIntentStatus, usage: string, ?attachToSelf: bool, ?customerAccount: string option, ?flowDirections: SetupIntentFlowDirections list option, ?managedPayments: SmorResourceManagedPayments option) =
            {
                SetupIntent.Application = application //required
                SetupIntent.AutomaticPaymentMethods = automaticPaymentMethods //required
                SetupIntent.CancellationReason = cancellationReason //required
                SetupIntent.ClientSecret = clientSecret //required
                SetupIntent.Created = created //required
                SetupIntent.Customer = customer //required
                SetupIntent.Description = description //required
                SetupIntent.ExcludedPaymentMethodTypes = excludedPaymentMethodTypes //required
                SetupIntent.Id = id //required
                SetupIntent.LastSetupError = lastSetupError //required
                SetupIntent.LatestAttempt = latestAttempt //required
                SetupIntent.Livemode = livemode //required
                SetupIntent.Mandate = mandate //required
                SetupIntent.Metadata = metadata //required
                SetupIntent.NextAction = nextAction //required
                SetupIntent.OnBehalfOf = onBehalfOf //required
                SetupIntent.PaymentMethod = paymentMethod //required
                SetupIntent.PaymentMethodConfigurationDetails = paymentMethodConfigurationDetails //required
                SetupIntent.PaymentMethodOptions = paymentMethodOptions //required
                SetupIntent.PaymentMethodTypes = paymentMethodTypes //required
                SetupIntent.SingleUseMandate = singleUseMandate //required
                SetupIntent.Status = status //required
                SetupIntent.Usage = usage //required
                SetupIntent.AttachToSelf = attachToSelf
                SetupIntent.CustomerAccount = customerAccount |> Option.flatten
                SetupIntent.FlowDirections = flowDirections |> Option.flatten
                SetupIntent.ManagedPayments = managedPayments |> Option.flatten
            }

    and SetupIntentApplication'AnyOf =
        | String of string
        | Application of Application

    and SetupIntentCustomer'AnyOf =
        | String of string
        | Customer of Customer
        | DeletedCustomer of DeletedCustomer

    and SetupIntentLatestAttempt'AnyOf =
        | String of string
        | SetupAttempt of SetupAttempt

    and SetupIntentMandate'AnyOf =
        | String of string
        | Mandate of Mandate

    and SetupIntentOnBehalfOf'AnyOf =
        | String of string
        | Account of Account

    and SetupIntentPaymentMethod'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and SetupIntentSingleUseMandate'AnyOf =
        | String of string
        | Mandate of Mandate

    and SetupIntentNextAction = {
        CashappHandleRedirectOrDisplayQrCode: PaymentIntentNextActionCashappHandleRedirectOrDisplayQrCode option
        PixDisplayQrCode: SetupIntentNextActionPixDisplayQrCode option
        RedirectToUrl: SetupIntentNextActionRedirectToUrl option
        ///Type of the next action to perform. Refer to the other child attributes under `next_action` for available values. Examples include: `redirect_to_url`, `use_stripe_sdk`, `alipay_handle_redirect`, `oxxo_display_details`, or `verify_with_microdeposits`.
        Type: SetupIntentNextActionType
        UpiHandleRedirectOrDisplayQrCode: PaymentIntentNextActionUpiHandleRedirectOrDisplayQrCode option
        ///When confirming a SetupIntent with Stripe.js, Stripe.js depends on the contents of this dictionary to invoke authentication flows. The shape of the contents is subject to change and is only intended to be used by Stripe.js.
        UseStripeSdk: string option
        VerifyWithMicrodeposits: SetupIntentNextActionVerifyWithMicrodeposits option
    }
    with
        static member New (``type``: SetupIntentNextActionType, ?cashappHandleRedirectOrDisplayQrCode: PaymentIntentNextActionCashappHandleRedirectOrDisplayQrCode, ?pixDisplayQrCode: SetupIntentNextActionPixDisplayQrCode, ?redirectToUrl: SetupIntentNextActionRedirectToUrl, ?upiHandleRedirectOrDisplayQrCode: PaymentIntentNextActionUpiHandleRedirectOrDisplayQrCode, ?useStripeSdk: string, ?verifyWithMicrodeposits: SetupIntentNextActionVerifyWithMicrodeposits) =
            {
                SetupIntentNextAction.Type = ``type`` //required
                SetupIntentNextAction.CashappHandleRedirectOrDisplayQrCode = cashappHandleRedirectOrDisplayQrCode
                SetupIntentNextAction.PixDisplayQrCode = pixDisplayQrCode
                SetupIntentNextAction.RedirectToUrl = redirectToUrl
                SetupIntentNextAction.UpiHandleRedirectOrDisplayQrCode = upiHandleRedirectOrDisplayQrCode
                SetupIntentNextAction.UseStripeSdk = useStripeSdk
                SetupIntentNextAction.VerifyWithMicrodeposits = verifyWithMicrodeposits
            }

    and SetupIntentNextActionVerifyWithMicrodeposits = {
        ///The timestamp when the microdeposits are expected to land.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ArrivalDate: DateTime
        ///The URL for the hosted verification page, which allows customers to verify their bank account.
        HostedVerificationUrl: string
        ///The type of the microdeposit sent to the customer. Used to distinguish between different verification methods.
        MicrodepositType: SetupIntentNextActionVerifyWithMicrodepositsMicrodepositType option
    }
    with
        static member New (arrivalDate: DateTime, hostedVerificationUrl: string, microdepositType: SetupIntentNextActionVerifyWithMicrodepositsMicrodepositType option) =
            {
                SetupIntentNextActionVerifyWithMicrodeposits.ArrivalDate = arrivalDate //required
                SetupIntentNextActionVerifyWithMicrodeposits.HostedVerificationUrl = hostedVerificationUrl //required
                SetupIntentNextActionVerifyWithMicrodeposits.MicrodepositType = microdepositType //required
            }

    and SetupIntentPaymentMethodOptions = {
        AcssDebit: SetupIntentPaymentMethodOptionsAcssDebit option
        AmazonPay: SetupIntentPaymentMethodOptionsAmazonPay option
        BacsDebit: SetupIntentPaymentMethodOptionsBacsDebit option
        Card: SetupIntentPaymentMethodOptionsCard option
        CardPresent: SetupIntentPaymentMethodOptionsCardPresent option
        Klarna: SetupIntentPaymentMethodOptionsKlarna option
        Link: SetupIntentPaymentMethodOptionsLink option
        Paypal: SetupIntentPaymentMethodOptionsPaypal option
        Payto: SetupIntentPaymentMethodOptionsPayto option
        Pix: SetupIntentPaymentMethodOptionsPix option
        SepaDebit: SetupIntentPaymentMethodOptionsSepaDebit option
        Upi: SetupIntentPaymentMethodOptionsUpi option
        UsBankAccount: SetupIntentPaymentMethodOptionsUsBankAccount option
    }
    with
        static member New (?acssDebit: SetupIntentPaymentMethodOptionsAcssDebit, ?amazonPay: SetupIntentPaymentMethodOptionsAmazonPay, ?bacsDebit: SetupIntentPaymentMethodOptionsBacsDebit, ?card: SetupIntentPaymentMethodOptionsCard, ?cardPresent: SetupIntentPaymentMethodOptionsCardPresent, ?klarna: SetupIntentPaymentMethodOptionsKlarna, ?link: SetupIntentPaymentMethodOptionsLink, ?paypal: SetupIntentPaymentMethodOptionsPaypal, ?payto: SetupIntentPaymentMethodOptionsPayto, ?pix: SetupIntentPaymentMethodOptionsPix, ?sepaDebit: SetupIntentPaymentMethodOptionsSepaDebit, ?upi: SetupIntentPaymentMethodOptionsUpi, ?usBankAccount: SetupIntentPaymentMethodOptionsUsBankAccount) =
            {
                SetupIntentPaymentMethodOptions.AcssDebit = acssDebit
                SetupIntentPaymentMethodOptions.AmazonPay = amazonPay
                SetupIntentPaymentMethodOptions.BacsDebit = bacsDebit
                SetupIntentPaymentMethodOptions.Card = card
                SetupIntentPaymentMethodOptions.CardPresent = cardPresent
                SetupIntentPaymentMethodOptions.Klarna = klarna
                SetupIntentPaymentMethodOptions.Link = link
                SetupIntentPaymentMethodOptions.Paypal = paypal
                SetupIntentPaymentMethodOptions.Payto = payto
                SetupIntentPaymentMethodOptions.Pix = pix
                SetupIntentPaymentMethodOptions.SepaDebit = sepaDebit
                SetupIntentPaymentMethodOptions.Upi = upi
                SetupIntentPaymentMethodOptions.UsBankAccount = usBankAccount
            }

    and SetupIntentPaymentMethodOptionsAcssDebit = {
        ///Currency supported by the bank account
        Currency: SetupIntentPaymentMethodOptionsAcssDebitCurrency option
        MandateOptions: SetupIntentPaymentMethodOptionsMandateOptionsAcssDebit option
        ///Bank account verification method. The default value is `automatic`.
        VerificationMethod: SetupIntentPaymentMethodOptionsAcssDebitVerificationMethod option
    }
    with
        static member New (currency: SetupIntentPaymentMethodOptionsAcssDebitCurrency option, ?mandateOptions: SetupIntentPaymentMethodOptionsMandateOptionsAcssDebit, ?verificationMethod: SetupIntentPaymentMethodOptionsAcssDebitVerificationMethod) =
            {
                SetupIntentPaymentMethodOptionsAcssDebit.Currency = currency //required
                SetupIntentPaymentMethodOptionsAcssDebit.MandateOptions = mandateOptions
                SetupIntentPaymentMethodOptionsAcssDebit.VerificationMethod = verificationMethod
            }

    and SetupIntentPaymentMethodOptionsBacsDebit = {
        MandateOptions: SetupIntentPaymentMethodOptionsMandateOptionsBacsDebit option
    }
    with
        static member New (?mandateOptions: SetupIntentPaymentMethodOptionsMandateOptionsBacsDebit) =
            {
                SetupIntentPaymentMethodOptionsBacsDebit.MandateOptions = mandateOptions
            }

    and SetupIntentPaymentMethodOptionsCard = {
        ///Configuration options for setting up an eMandate for cards issued in India.
        MandateOptions: SetupIntentPaymentMethodOptionsCardMandateOptions option
        ///Selected network to process this SetupIntent on. Depends on the available networks of the card attached to the setup intent. Can be only set confirm-time.
        Network: SetupIntentPaymentMethodOptionsCardNetwork option
        ///We strongly recommend that you rely on our SCA Engine to automatically prompt your customers for authentication based on risk level and [other requirements](https://docs.stripe.com/strong-customer-authentication). However, if you wish to request 3D Secure based on logic from your own fraud engine, provide this option. If not provided, this value defaults to `automatic`. Read our guide on [manually requesting 3D Secure](https://docs.stripe.com/payments/3d-secure/authentication-flow#manual-three-ds) for more information on how this configuration interacts with Radar and our SCA Engine.
        RequestThreeDSecure: SetupIntentPaymentMethodOptionsCardRequestThreeDSecure option
    }
    with
        static member New (mandateOptions: SetupIntentPaymentMethodOptionsCardMandateOptions option, network: SetupIntentPaymentMethodOptionsCardNetwork option, requestThreeDSecure: SetupIntentPaymentMethodOptionsCardRequestThreeDSecure option) =
            {
                SetupIntentPaymentMethodOptionsCard.MandateOptions = mandateOptions //required
                SetupIntentPaymentMethodOptionsCard.Network = network //required
                SetupIntentPaymentMethodOptionsCard.RequestThreeDSecure = requestThreeDSecure //required
            }

    and SetupIntentPaymentMethodOptionsCardMandateOptions = {
        ///Amount to be charged for future payments, specified in the presentment currency.
        Amount: int
        ///One of `fixed` or `maximum`. If `fixed`, the `amount` param refers to the exact amount to be charged in future payments. If `maximum`, the amount charged can be up to the value passed for the `amount` param.
        AmountType: SetupIntentPaymentMethodOptionsCardMandateOptionsAmountType
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///A description of the mandate or subscription that is meant to be displayed to the customer.
        Description: string option
        ///End date of the mandate or subscription. If not provided, the mandate will be active until canceled. If provided, end date should be after start date.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]EndDate: DateTime option
        ///Specifies payment frequency. One of `day`, `week`, `month`, `year`, or `sporadic`.
        Interval: SetupIntentPaymentMethodOptionsCardMandateOptionsInterval
        ///The number of intervals between payments. For example, `interval=month` and `interval_count=3` indicates one payment every three months. Maximum of one year interval allowed (1 year, 12 months, or 52 weeks). This parameter is optional when `interval=sporadic`.
        IntervalCount: int option
        ///Unique identifier for the mandate or subscription.
        Reference: string
        ///Start date of the mandate or subscription. Start date should not be lesser than yesterday.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]StartDate: DateTime
        ///Specifies the type of mandates supported. Possible values are `india`.
        SupportedTypes: string list option
    }
    with
        static member New (amount: int, amountType: SetupIntentPaymentMethodOptionsCardMandateOptionsAmountType, currency: IsoTypes.IsoCurrencyCode, description: string option, endDate: DateTime option, interval: SetupIntentPaymentMethodOptionsCardMandateOptionsInterval, intervalCount: int option, reference: string, startDate: DateTime, supportedTypes: string list option) =
            {
                SetupIntentPaymentMethodOptionsCardMandateOptions.Amount = amount //required
                SetupIntentPaymentMethodOptionsCardMandateOptions.AmountType = amountType //required
                SetupIntentPaymentMethodOptionsCardMandateOptions.Currency = currency //required
                SetupIntentPaymentMethodOptionsCardMandateOptions.Description = description //required
                SetupIntentPaymentMethodOptionsCardMandateOptions.EndDate = endDate //required
                SetupIntentPaymentMethodOptionsCardMandateOptions.Interval = interval //required
                SetupIntentPaymentMethodOptionsCardMandateOptions.IntervalCount = intervalCount //required
                SetupIntentPaymentMethodOptionsCardMandateOptions.Reference = reference //required
                SetupIntentPaymentMethodOptionsCardMandateOptions.StartDate = startDate //required
                SetupIntentPaymentMethodOptionsCardMandateOptions.SupportedTypes = supportedTypes //required
            }

    and SetupIntentPaymentMethodOptionsKlarna = {
        ///The currency of the setup intent. Three letter ISO currency code.
        Currency: IsoTypes.IsoCurrencyCode option
        ///Preferred locale of the Klarna checkout page that the customer is redirected to.
        PreferredLocale: string option
    }
    with
        static member New (currency: IsoTypes.IsoCurrencyCode option, preferredLocale: string option) =
            {
                SetupIntentPaymentMethodOptionsKlarna.Currency = currency //required
                SetupIntentPaymentMethodOptionsKlarna.PreferredLocale = preferredLocale //required
            }

    and SetupIntentPaymentMethodOptionsMandateOptionsAcssDebit = {
        ///A URL for custom mandate text
        CustomMandateUrl: string option
        ///List of Stripe products where this mandate can be selected automatically.
        DefaultFor: SetupIntentPaymentMethodOptionsMandateOptionsAcssDebitDefaultFor list option
        ///Description of the interval. Only required if the 'payment_schedule' parameter is 'interval' or 'combined'.
        IntervalDescription: string option
        ///Payment schedule for the mandate.
        PaymentSchedule: SetupIntentPaymentMethodOptionsMandateOptionsAcssDebitPaymentSchedule option
        ///Transaction type of the mandate.
        TransactionType: SetupIntentPaymentMethodOptionsMandateOptionsAcssDebitTransactionType option
    }
    with
        static member New (intervalDescription: string option, paymentSchedule: SetupIntentPaymentMethodOptionsMandateOptionsAcssDebitPaymentSchedule option, transactionType: SetupIntentPaymentMethodOptionsMandateOptionsAcssDebitTransactionType option, ?customMandateUrl: string, ?defaultFor: SetupIntentPaymentMethodOptionsMandateOptionsAcssDebitDefaultFor list) =
            {
                SetupIntentPaymentMethodOptionsMandateOptionsAcssDebit.IntervalDescription = intervalDescription //required
                SetupIntentPaymentMethodOptionsMandateOptionsAcssDebit.PaymentSchedule = paymentSchedule //required
                SetupIntentPaymentMethodOptionsMandateOptionsAcssDebit.TransactionType = transactionType //required
                SetupIntentPaymentMethodOptionsMandateOptionsAcssDebit.CustomMandateUrl = customMandateUrl
                SetupIntentPaymentMethodOptionsMandateOptionsAcssDebit.DefaultFor = defaultFor
            }

    and SetupIntentPaymentMethodOptionsMandateOptionsPayto = {
        ///Amount that will be collected. It is required when `amount_type` is `fixed`.
        Amount: int option
        ///The type of amount that will be collected. The amount charged must be exact or up to the value of `amount` param for `fixed` or `maximum` type respectively. Defaults to `maximum`.
        AmountType: SetupIntentPaymentMethodOptionsMandateOptionsPaytoAmountType option
        ///Date, in YYYY-MM-DD format, after which payments will not be collected. Defaults to no end date.
        EndDate: string option
        ///The periodicity at which payments will be collected. Defaults to `adhoc`.
        PaymentSchedule: SetupIntentPaymentMethodOptionsMandateOptionsPaytoPaymentSchedule option
        ///The number of payments that will be made during a payment period. Defaults to 1 except for when `payment_schedule` is `adhoc`. In that case, it defaults to no limit.
        PaymentsPerPeriod: int option
        ///The purpose for which payments are made. Has a default value based on your merchant category code.
        Purpose: SetupIntentPaymentMethodOptionsMandateOptionsPaytoPurpose option
        ///Date, in YYYY-MM-DD format, from which payments will be collected. Defaults to confirmation time.
        StartDate: string option
    }
    with
        static member New (amount: int option, amountType: SetupIntentPaymentMethodOptionsMandateOptionsPaytoAmountType option, endDate: string option, paymentSchedule: SetupIntentPaymentMethodOptionsMandateOptionsPaytoPaymentSchedule option, paymentsPerPeriod: int option, purpose: SetupIntentPaymentMethodOptionsMandateOptionsPaytoPurpose option, startDate: string option) =
            {
                SetupIntentPaymentMethodOptionsMandateOptionsPayto.Amount = amount //required
                SetupIntentPaymentMethodOptionsMandateOptionsPayto.AmountType = amountType //required
                SetupIntentPaymentMethodOptionsMandateOptionsPayto.EndDate = endDate //required
                SetupIntentPaymentMethodOptionsMandateOptionsPayto.PaymentSchedule = paymentSchedule //required
                SetupIntentPaymentMethodOptionsMandateOptionsPayto.PaymentsPerPeriod = paymentsPerPeriod //required
                SetupIntentPaymentMethodOptionsMandateOptionsPayto.Purpose = purpose //required
                SetupIntentPaymentMethodOptionsMandateOptionsPayto.StartDate = startDate //required
            }

    and SetupIntentPaymentMethodOptionsPayto = {
        MandateOptions: SetupIntentPaymentMethodOptionsMandateOptionsPayto option
    }
    with
        static member New (?mandateOptions: SetupIntentPaymentMethodOptionsMandateOptionsPayto) =
            {
                SetupIntentPaymentMethodOptionsPayto.MandateOptions = mandateOptions
            }

    and SetupIntentPaymentMethodOptionsPix = {
        MandateOptions: PaymentMethodOptionsMandateOptionsPix option
    }
    with
        static member New (?mandateOptions: PaymentMethodOptionsMandateOptionsPix) =
            {
                SetupIntentPaymentMethodOptionsPix.MandateOptions = mandateOptions
            }

    and SetupIntentPaymentMethodOptionsSepaDebit = {
        MandateOptions: SetupIntentPaymentMethodOptionsMandateOptionsSepaDebit option
    }
    with
        static member New (?mandateOptions: SetupIntentPaymentMethodOptionsMandateOptionsSepaDebit) =
            {
                SetupIntentPaymentMethodOptionsSepaDebit.MandateOptions = mandateOptions
            }

    and SetupIntentPaymentMethodOptionsUpi = {
        MandateOptions: PaymentMethodOptionsMandateOptionsUpi option
    }
    with
        static member New (?mandateOptions: PaymentMethodOptionsMandateOptionsUpi) =
            {
                SetupIntentPaymentMethodOptionsUpi.MandateOptions = mandateOptions
            }

    and SetupIntentPaymentMethodOptionsUsBankAccount = {
        FinancialConnections: LinkedAccountOptionsCommon option
        MandateOptions: PaymentMethodOptionsUsBankAccountMandateOptions option
        ///Bank account verification method. The default value is `automatic`.
        VerificationMethod: SetupIntentPaymentMethodOptionsUsBankAccountVerificationMethod option
    }
    with
        static member New (?financialConnections: LinkedAccountOptionsCommon, ?mandateOptions: PaymentMethodOptionsUsBankAccountMandateOptions, ?verificationMethod: SetupIntentPaymentMethodOptionsUsBankAccountVerificationMethod) =
            {
                SetupIntentPaymentMethodOptionsUsBankAccount.FinancialConnections = financialConnections
                SetupIntentPaymentMethodOptionsUsBankAccount.MandateOptions = mandateOptions
                SetupIntentPaymentMethodOptionsUsBankAccount.VerificationMethod = verificationMethod
            }

    and SetupIntentTypeSpecificPaymentMethodOptionsClient = {
        MandateOptions: SetupIntentPaymentMethodOptionsMandateOptionsPayto option
        ///Bank account verification method. The default value is `automatic`.
        VerificationMethod: SetupIntentTypeSpecificPaymentMethodOptionsClientVerificationMethod option
    }
    with
        static member New (?mandateOptions: SetupIntentPaymentMethodOptionsMandateOptionsPayto, ?verificationMethod: SetupIntentTypeSpecificPaymentMethodOptionsClientVerificationMethod) =
            {
                SetupIntentTypeSpecificPaymentMethodOptionsClient.MandateOptions = mandateOptions
                SetupIntentTypeSpecificPaymentMethodOptionsClient.VerificationMethod = verificationMethod
            }

    and Shipping = {
        Address: Address option
        ///The delivery service that shipped a physical product, such as Fedex, UPS, USPS, etc.
        Carrier: string option
        ///Recipient name.
        Name: string option
        ///Recipient phone (including extension).
        Phone: string option
        ///The tracking number for a physical product, obtained from the delivery service. If multiple tracking numbers were generated for this purchase, please separate them with commas.
        TrackingNumber: string option
    }
    with
        static member New (?address: Address, ?carrier: string option, ?name: string, ?phone: string option, ?trackingNumber: string option) =
            {
                Shipping.Address = address
                Shipping.Carrier = carrier |> Option.flatten
                Shipping.Name = name
                Shipping.Phone = phone |> Option.flatten
                Shipping.TrackingNumber = trackingNumber |> Option.flatten
            }

    ///Shipping rates describe the price of shipping presented to your customers and
    ///applied to a purchase. For more information, see [Charge for shipping](https://docs.stripe.com/payments/during-payment/charge-shipping).
    and ShippingRate = {
        ///Whether the shipping rate can be used for new purchases. Defaults to `true`.
        Active: bool
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///The estimated range for how long shipping will take, meant to be displayable to the customer. This will appear on CheckoutSessions.
        DeliveryEstimate: ShippingRateDeliveryEstimate option
        ///The name of the shipping rate, meant to be displayable to the customer. This will appear on CheckoutSessions.
        DisplayName: string option
        FixedAmount: ShippingRateFixedAmount option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///Specifies whether the rate is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`.
        TaxBehavior: ShippingRateTaxBehavior option
        ///A [tax code](https://docs.stripe.com/tax/tax-categories) ID. The Shipping tax code is `txcd_92010001`.
        TaxCode: ShippingRateTaxCode'AnyOf option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "shipping_rate"
        ///The type of calculation to use on the shipping rate.
        member _.Type = "fixed_amount"

        static member New (active: bool, created: DateTime, deliveryEstimate: ShippingRateDeliveryEstimate option, displayName: string option, id: string, livemode: bool, metadata: Map<string, string>, taxBehavior: ShippingRateTaxBehavior option, taxCode: ShippingRateTaxCode'AnyOf option, ?fixedAmount: ShippingRateFixedAmount) =
            {
                ShippingRate.Active = active //required
                ShippingRate.Created = created //required
                ShippingRate.DeliveryEstimate = deliveryEstimate //required
                ShippingRate.DisplayName = displayName //required
                ShippingRate.Id = id //required
                ShippingRate.Livemode = livemode //required
                ShippingRate.Metadata = metadata //required
                ShippingRate.TaxBehavior = taxBehavior //required
                ShippingRate.TaxCode = taxCode //required
                ShippingRate.FixedAmount = fixedAmount
            }

    and ShippingRateTaxCode'AnyOf =
        | String of string
        | TaxCode of TaxCode

    and ShippingRateCurrencyOption = {
        ///A non-negative integer in cents representing how much to charge.
        Amount: int
        ///Specifies whether the rate is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`.
        TaxBehavior: ShippingRateCurrencyOptionTaxBehavior
    }
    with
        static member New (amount: int, taxBehavior: ShippingRateCurrencyOptionTaxBehavior) =
            {
                ShippingRateCurrencyOption.Amount = amount //required
                ShippingRateCurrencyOption.TaxBehavior = taxBehavior //required
            }

    and ShippingRateDeliveryEstimate = {
        ///The upper bound of the estimated range. If empty, represents no upper bound i.e., infinite.
        Maximum: ShippingRateDeliveryEstimateBound option
        ///The lower bound of the estimated range. If empty, represents no lower bound.
        Minimum: ShippingRateDeliveryEstimateBound option
    }
    with
        static member New (maximum: ShippingRateDeliveryEstimateBound option, minimum: ShippingRateDeliveryEstimateBound option) =
            {
                ShippingRateDeliveryEstimate.Maximum = maximum //required
                ShippingRateDeliveryEstimate.Minimum = minimum //required
            }

    and ShippingRateDeliveryEstimateBound = {
        ///A unit of time.
        Unit: ShippingRateDeliveryEstimateBoundUnit
        ///Must be greater than 0.
        Value: int
    }
    with
        static member New (unit: ShippingRateDeliveryEstimateBoundUnit, value: int) =
            {
                ShippingRateDeliveryEstimateBound.Unit = unit //required
                ShippingRateDeliveryEstimateBound.Value = value //required
            }

    and ShippingRateFixedAmount = {
        ///A non-negative integer in cents representing how much to charge.
        Amount: int
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///Shipping rates defined in each available currency option. Each key must be a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html) and a [supported currency](https://stripe.com/docs/currencies).
        CurrencyOptions: Map<string, string list> option
    }
    with
        static member New (amount: int, currency: IsoTypes.IsoCurrencyCode, ?currencyOptions: Map<string, string list>) =
            {
                ShippingRateFixedAmount.Amount = amount //required
                ShippingRateFixedAmount.Currency = currency //required
                ShippingRateFixedAmount.CurrencyOptions = currencyOptions
            }

    ///`Source` objects allow you to accept a variety of payment methods. They
    ///represent a customer's payment instrument, and can be used with the Stripe API
    ///just like a `Card` object: once chargeable, they can be charged, or can be
    ///attached to customers.
    ///Stripe doesn't recommend using the deprecated [Sources API](https://docs.stripe.com/api/sources).
    ///We recommend that you adopt the [PaymentMethods API](https://docs.stripe.com/api/payment_methods).
    ///This newer API provides access to our latest features and payment method types.
    ///Related guides: [Sources API](https://docs.stripe.com/sources) and [Sources & Customers](https://docs.stripe.com/sources/customers).
    and Source = {
        AchCreditTransfer: SourceTypeAchCreditTransfer option
        AchDebit: SourceTypeAchDebit option
        AcssDebit: SourceTypeAcssDebit option
        Alipay: SourceTypeAlipay option
        ///This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment method in a checkout flow. The field defaults to “unspecified”.
        AllowRedisplay: SourceAllowRedisplay option
        ///A positive integer in the smallest currency unit (that is, 100 cents for $1.00, or 1 for ¥1, Japanese Yen being a zero-decimal currency) representing the total amount associated with the source. This is the amount for which the source will be chargeable once ready. Required for `single_use` sources.
        Amount: int option
        AuBecsDebit: SourceTypeAuBecsDebit option
        Bancontact: SourceTypeBancontact option
        Card: SourceTypeCard option
        CardPresent: SourceTypeCardPresent option
        ///The client secret of the source. Used for client-side retrieval using a publishable key.
        ClientSecret: string
        CodeVerification: SourceCodeVerificationFlow option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO code for the currency](https://stripe.com/docs/currencies) associated with the source. This is the currency for which the source will be chargeable once ready. Required for `single_use` sources.
        Currency: IsoTypes.IsoCurrencyCode option
        ///The ID of the customer to which this source is attached. This will not be present when the source has not been attached to a customer.
        Customer: string option
        Eps: SourceTypeEps option
        ///The authentication `flow` of the source. `flow` is one of `redirect`, `receiver`, `code_verification`, `none`.
        Flow: string
        Giropay: SourceTypeGiropay option
        ///Unique identifier for the object.
        Id: string
        Ideal: SourceTypeIdeal option
        Klarna: SourceTypeKlarna option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        Multibanco: SourceTypeMultibanco option
        ///Information about the owner of the payment instrument that may be used or required by particular source types.
        Owner: SourceOwner option
        [<JsonField(Name="p24")>]P24: SourceTypeP24 option
        Receiver: SourceReceiverFlow option
        Redirect: SourceRedirectFlow option
        SepaCreditTransfer: SourceTypeSepaCreditTransfer option
        SepaDebit: SourceTypeSepaDebit option
        Sofort: SourceTypeSofort option
        SourceOrder: SourceOrder option
        ///Extra information about a source. This will appear on your customer's statement every time you charge the source.
        StatementDescriptor: string option
        ///The status of the source, one of `canceled`, `chargeable`, `consumed`, `failed`, or `pending`. Only `chargeable` sources can be used to create a charge.
        Status: SourceStatus
        ThreeDSecure: SourceTypeThreeDSecure option
        ///The `type` of the source. The `type` is a payment method, one of `ach_credit_transfer`, `ach_debit`, `alipay`, `bancontact`, `card`, `card_present`, `eps`, `giropay`, `ideal`, `multibanco`, `klarna`, `p24`, `sepa_debit`, `sofort`, `three_d_secure`, or `wechat`. An additional hash is included on the source with a name matching this value. It contains additional information specific to the [payment method](https://docs.stripe.com/sources) used.
        Type: SourceType
        ///Either `reusable` or `single_use`. Whether this source should be reusable or not. Some source types may or may not be reusable by construction, while others may leave the option at creation. If an incompatible value is passed, an error will be returned.
        Usage: SourceUsage option
        Wechat: SourceTypeWechat option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "source"

        static member New (allowRedisplay: SourceAllowRedisplay option, amount: int option, clientSecret: string, created: DateTime, currency: IsoTypes.IsoCurrencyCode option, flow: string, id: string, livemode: bool, metadata: Map<string, string> option, owner: SourceOwner option, statementDescriptor: string option, status: SourceStatus, ``type``: SourceType, usage: SourceUsage option, ?achCreditTransfer: SourceTypeAchCreditTransfer, ?achDebit: SourceTypeAchDebit, ?acssDebit: SourceTypeAcssDebit, ?alipay: SourceTypeAlipay, ?auBecsDebit: SourceTypeAuBecsDebit, ?bancontact: SourceTypeBancontact, ?card: SourceTypeCard, ?cardPresent: SourceTypeCardPresent, ?codeVerification: SourceCodeVerificationFlow, ?customer: string, ?eps: SourceTypeEps, ?giropay: SourceTypeGiropay, ?ideal: SourceTypeIdeal, ?klarna: SourceTypeKlarna, ?multibanco: SourceTypeMultibanco, ?p24: SourceTypeP24, ?receiver: SourceReceiverFlow, ?redirect: SourceRedirectFlow, ?sepaCreditTransfer: SourceTypeSepaCreditTransfer, ?sepaDebit: SourceTypeSepaDebit, ?sofort: SourceTypeSofort, ?sourceOrder: SourceOrder, ?threeDSecure: SourceTypeThreeDSecure, ?wechat: SourceTypeWechat) =
            {
                Source.AllowRedisplay = allowRedisplay //required
                Source.Amount = amount //required
                Source.ClientSecret = clientSecret //required
                Source.Created = created //required
                Source.Currency = currency //required
                Source.Flow = flow //required
                Source.Id = id //required
                Source.Livemode = livemode //required
                Source.Metadata = metadata //required
                Source.Owner = owner //required
                Source.StatementDescriptor = statementDescriptor //required
                Source.Status = status //required
                Source.Type = ``type`` //required
                Source.Usage = usage //required
                Source.AchCreditTransfer = achCreditTransfer
                Source.AchDebit = achDebit
                Source.AcssDebit = acssDebit
                Source.Alipay = alipay
                Source.AuBecsDebit = auBecsDebit
                Source.Bancontact = bancontact
                Source.Card = card
                Source.CardPresent = cardPresent
                Source.CodeVerification = codeVerification
                Source.Customer = customer
                Source.Eps = eps
                Source.Giropay = giropay
                Source.Ideal = ideal
                Source.Klarna = klarna
                Source.Multibanco = multibanco
                Source.P24 = p24
                Source.Receiver = receiver
                Source.Redirect = redirect
                Source.SepaCreditTransfer = sepaCreditTransfer
                Source.SepaDebit = sepaDebit
                Source.Sofort = sofort
                Source.SourceOrder = sourceOrder
                Source.ThreeDSecure = threeDSecure
                Source.Wechat = wechat
            }

    ///Source mandate notifications should be created when a notification related to
    ///a source mandate must be sent to the payer. They will trigger a webhook or
    ///deliver an email to the customer.
    and SourceMandateNotification = {
        AcssDebit: SourceMandateNotificationAcssDebitData option
        ///A positive integer in the smallest currency unit (that is, 100 cents for $1.00, or 1 for ¥1, Japanese Yen being a zero-decimal currency) representing the amount associated with the mandate notification. The amount is expressed in the currency of the underlying source. Required if the notification type is `debit_initiated`.
        Amount: int option
        BacsDebit: SourceMandateNotificationBacsDebitData option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The reason of the mandate notification. Valid reasons are `mandate_confirmed` or `debit_initiated`.
        Reason: SourceMandateNotificationReason
        SepaDebit: SourceMandateNotificationSepaDebitData option
        Source: Source
        ///The status of the mandate notification. Valid statuses are `pending` or `submitted`.
        Status: SourceMandateNotificationStatus
        ///The type of source this mandate notification is attached to. Should be the source type identifier code for the payment method, such as `three_d_secure`.
        Type: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "source_mandate_notification"

        static member New (amount: int option, created: DateTime, id: string, livemode: bool, reason: SourceMandateNotificationReason, source: Source, status: SourceMandateNotificationStatus, ``type``: string, ?acssDebit: SourceMandateNotificationAcssDebitData, ?bacsDebit: SourceMandateNotificationBacsDebitData, ?sepaDebit: SourceMandateNotificationSepaDebitData) =
            {
                SourceMandateNotification.Amount = amount //required
                SourceMandateNotification.Created = created //required
                SourceMandateNotification.Id = id //required
                SourceMandateNotification.Livemode = livemode //required
                SourceMandateNotification.Reason = reason //required
                SourceMandateNotification.Source = source //required
                SourceMandateNotification.Status = status //required
                SourceMandateNotification.Type = ``type`` //required
                SourceMandateNotification.AcssDebit = acssDebit
                SourceMandateNotification.BacsDebit = bacsDebit
                SourceMandateNotification.SepaDebit = sepaDebit
            }

    and SourceOrder = {
        ///A positive integer in the smallest currency unit (that is, 100 cents for $1.00, or 1 for ¥1, Japanese Yen being a zero-decimal currency) representing the total amount for the order.
        Amount: int
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///The email address of the customer placing the order.
        Email: string option
        ///List of items constituting the order.
        Items: SourceOrderItem list option
        Shipping: Shipping option
    }
    with
        static member New (amount: int, currency: IsoTypes.IsoCurrencyCode, items: SourceOrderItem list option, ?email: string, ?shipping: Shipping) =
            {
                SourceOrder.Amount = amount //required
                SourceOrder.Currency = currency //required
                SourceOrder.Items = items //required
                SourceOrder.Email = email
                SourceOrder.Shipping = shipping
            }

    and SourceOrderItem = {
        ///The amount (price) for this order item.
        Amount: int option
        ///This currency of this order item. Required when `amount` is present.
        Currency: IsoTypes.IsoCurrencyCode option
        ///Human-readable description for this order item.
        Description: string option
        ///The ID of the associated object for this line item. Expandable if not null (e.g., expandable to a SKU).
        Parent: string option
        ///The quantity of this order item. When type is `sku`, this is the number of instances of the SKU to be ordered.
        Quantity: int option
        ///The type of this order item. Must be `sku`, `tax`, or `shipping`.
        Type: SourceOrderItemType option
    }
    with
        static member New (amount: int option, currency: IsoTypes.IsoCurrencyCode option, description: string option, parent: string option, ``type``: SourceOrderItemType option, ?quantity: int) =
            {
                SourceOrderItem.Amount = amount //required
                SourceOrderItem.Currency = currency //required
                SourceOrderItem.Description = description //required
                SourceOrderItem.Parent = parent //required
                SourceOrderItem.Type = ``type`` //required
                SourceOrderItem.Quantity = quantity
            }

    and SourceOwner = {
        ///Owner's address.
        Address: Address option
        ///Owner's email address.
        Email: string option
        ///Owner's full name.
        Name: string option
        ///Owner's phone number (including extension).
        Phone: string option
        ///Verified owner's address. Verified values are verified or provided by the payment method directly (and if supported) at the time of authorization or settlement. They cannot be set or mutated.
        VerifiedAddress: Address option
        ///Verified owner's email address. Verified values are verified or provided by the payment method directly (and if supported) at the time of authorization or settlement. They cannot be set or mutated.
        VerifiedEmail: string option
        ///Verified owner's full name. Verified values are verified or provided by the payment method directly (and if supported) at the time of authorization or settlement. They cannot be set or mutated.
        VerifiedName: string option
        ///Verified owner's phone number (including extension). Verified values are verified or provided by the payment method directly (and if supported) at the time of authorization or settlement. They cannot be set or mutated.
        VerifiedPhone: string option
    }
    with
        static member New (address: Address option, email: string option, name: string option, phone: string option, verifiedAddress: Address option, verifiedEmail: string option, verifiedName: string option, verifiedPhone: string option) =
            {
                SourceOwner.Address = address //required
                SourceOwner.Email = email //required
                SourceOwner.Name = name //required
                SourceOwner.Phone = phone //required
                SourceOwner.VerifiedAddress = verifiedAddress //required
                SourceOwner.VerifiedEmail = verifiedEmail //required
                SourceOwner.VerifiedName = verifiedName //required
                SourceOwner.VerifiedPhone = verifiedPhone //required
            }

    and SourceReceiverFlow = {
        ///The address of the receiver source. This is the value that should be communicated to the customer to send their funds to.
        Address: string option
        ///The total amount that was moved to your balance. This is almost always equal to the amount charged. In rare cases when customers deposit excess funds and we are unable to refund those, those funds get moved to your balance and show up in amount_charged as well. The amount charged is expressed in the source's currency.
        AmountCharged: int
        ///The total amount received by the receiver source. `amount_received = amount_returned + amount_charged` should be true for consumed sources unless customers deposit excess funds. The amount received is expressed in the source's currency.
        AmountReceived: int
        ///The total amount that was returned to the customer. The amount returned is expressed in the source's currency.
        AmountReturned: int
        ///Type of refund attribute method, one of `email`, `manual`, or `none`.
        RefundAttributesMethod: SourceReceiverFlowRefundAttributesMethod
        ///Type of refund attribute status, one of `missing`, `requested`, or `available`.
        RefundAttributesStatus: SourceReceiverFlowRefundAttributesStatus
    }
    with
        static member New (address: string option, amountCharged: int, amountReceived: int, amountReturned: int, refundAttributesMethod: SourceReceiverFlowRefundAttributesMethod, refundAttributesStatus: SourceReceiverFlowRefundAttributesStatus) =
            {
                SourceReceiverFlow.Address = address //required
                SourceReceiverFlow.AmountCharged = amountCharged //required
                SourceReceiverFlow.AmountReceived = amountReceived //required
                SourceReceiverFlow.AmountReturned = amountReturned //required
                SourceReceiverFlow.RefundAttributesMethod = refundAttributesMethod //required
                SourceReceiverFlow.RefundAttributesStatus = refundAttributesStatus //required
            }

    ///Some payment methods have no required amount that a customer must send.
    ///Customers can be instructed to send any amount, and it can be made up of
    ///multiple transactions. As such, sources can have multiple associated
    ///transactions.
    and SourceTransaction = {
        AchCreditTransfer: SourceTransactionAchCreditTransferData option
        ///A positive integer in the smallest currency unit (that is, 100 cents for $1.00, or 1 for ¥1, Japanese Yen being a zero-decimal currency) representing the amount your customer has pushed to the receiver.
        Amount: int
        ChfCreditTransfer: SourceTransactionChfCreditTransferData option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        GbpCreditTransfer: SourceTransactionGbpCreditTransferData option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        PaperCheck: SourceTransactionPaperCheckData option
        SepaCreditTransfer: SourceTransactionSepaCreditTransferData option
        ///The ID of the source this transaction is attached to.
        Source: string
        ///The status of the transaction, one of `succeeded`, `pending`, or `failed`.
        Status: SourceTransactionStatus
        ///The type of source this transaction is attached to.
        Type: SourceTransactionType
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "source_transaction"

        static member New (amount: int, created: DateTime, currency: IsoTypes.IsoCurrencyCode, id: string, livemode: bool, source: string, status: SourceTransactionStatus, ``type``: SourceTransactionType, ?achCreditTransfer: SourceTransactionAchCreditTransferData, ?chfCreditTransfer: SourceTransactionChfCreditTransferData, ?gbpCreditTransfer: SourceTransactionGbpCreditTransferData, ?paperCheck: SourceTransactionPaperCheckData, ?sepaCreditTransfer: SourceTransactionSepaCreditTransferData) =
            {
                SourceTransaction.Amount = amount //required
                SourceTransaction.Created = created //required
                SourceTransaction.Currency = currency //required
                SourceTransaction.Id = id //required
                SourceTransaction.Livemode = livemode //required
                SourceTransaction.Source = source //required
                SourceTransaction.Status = status //required
                SourceTransaction.Type = ``type`` //required
                SourceTransaction.AchCreditTransfer = achCreditTransfer
                SourceTransaction.ChfCreditTransfer = chfCreditTransfer
                SourceTransaction.GbpCreditTransfer = gbpCreditTransfer
                SourceTransaction.PaperCheck = paperCheck
                SourceTransaction.SepaCreditTransfer = sepaCreditTransfer
            }

    and SourceTransactionChfCreditTransferData = {
        ///Reference associated with the transfer.
        Reference: string option
        ///Sender's country address.
        SenderAddressCountry: IsoTypes.IsoCountryCode option
        ///Sender's line 1 address.
        [<JsonField(Name="sender_address_line1")>]SenderAddressLine1: string option
        ///Sender's bank account IBAN.
        SenderIban: string option
        ///Sender's name.
        SenderName: string option
    }
    with
        static member New (?reference: string, ?senderAddressCountry: IsoTypes.IsoCountryCode, ?senderAddressLine1: string, ?senderIban: string, ?senderName: string) =
            {
                SourceTransactionChfCreditTransferData.Reference = reference
                SourceTransactionChfCreditTransferData.SenderAddressCountry = senderAddressCountry
                SourceTransactionChfCreditTransferData.SenderAddressLine1 = senderAddressLine1
                SourceTransactionChfCreditTransferData.SenderIban = senderIban
                SourceTransactionChfCreditTransferData.SenderName = senderName
            }

    and SourceTypeAchDebit = {
        BankName: string option
        Country: IsoTypes.IsoCountryCode option
        Fingerprint: string option
        [<JsonField(Name="last4")>]Last4: string option
        RoutingNumber: string option
        Type: string option
    }
    with
        static member New (?bankName: string option, ?country: IsoTypes.IsoCountryCode option, ?fingerprint: string option, ?last4: string option, ?routingNumber: string option, ?``type``: string option) =
            {
                SourceTypeAchDebit.BankName = bankName |> Option.flatten
                SourceTypeAchDebit.Country = country |> Option.flatten
                SourceTypeAchDebit.Fingerprint = fingerprint |> Option.flatten
                SourceTypeAchDebit.Last4 = last4 |> Option.flatten
                SourceTypeAchDebit.RoutingNumber = routingNumber |> Option.flatten
                SourceTypeAchDebit.Type = ``type`` |> Option.flatten
            }

    and SourceTypeAcssDebit = {
        BankAddressCity: string option
        BankAddressLine1: string option
        BankAddressLine2: string option
        BankAddressPostalCode: string option
        BankName: string option
        Category: string option
        Country: IsoTypes.IsoCountryCode option
        Fingerprint: string option
        [<JsonField(Name="last4")>]Last4: string option
        RoutingNumber: string option
    }
    with
        static member New (?bankAddressCity: string option, ?bankAddressLine1: string option, ?bankAddressLine2: string option, ?bankAddressPostalCode: string option, ?bankName: string option, ?category: string option, ?country: IsoTypes.IsoCountryCode option, ?fingerprint: string option, ?last4: string option, ?routingNumber: string option) =
            {
                SourceTypeAcssDebit.BankAddressCity = bankAddressCity |> Option.flatten
                SourceTypeAcssDebit.BankAddressLine1 = bankAddressLine1 |> Option.flatten
                SourceTypeAcssDebit.BankAddressLine2 = bankAddressLine2 |> Option.flatten
                SourceTypeAcssDebit.BankAddressPostalCode = bankAddressPostalCode |> Option.flatten
                SourceTypeAcssDebit.BankName = bankName |> Option.flatten
                SourceTypeAcssDebit.Category = category |> Option.flatten
                SourceTypeAcssDebit.Country = country |> Option.flatten
                SourceTypeAcssDebit.Fingerprint = fingerprint |> Option.flatten
                SourceTypeAcssDebit.Last4 = last4 |> Option.flatten
                SourceTypeAcssDebit.RoutingNumber = routingNumber |> Option.flatten
            }

    and SourceTypeCard = {
        [<JsonField(Name="address_line1_check")>]AddressLine1Check: string option
        AddressZipCheck: string option
        Brand: string option
        Country: IsoTypes.IsoCountryCode option
        CvcCheck: string option
        Description: string option
        [<JsonField(Name="dynamic_last4")>]DynamicLast4: string option
        ExpMonth: int option
        ExpYear: int option
        Fingerprint: string option
        Funding: string option
        Iin: string option
        Issuer: string option
        [<JsonField(Name="last4")>]Last4: string option
        Name: string option
        ThreeDSecure: string option
        TokenizationMethod: string option
    }
    with
        static member New (?addressLine1Check: string option, ?addressZipCheck: string option, ?brand: string option, ?country: IsoTypes.IsoCountryCode option, ?cvcCheck: string option, ?description: string, ?dynamicLast4: string option, ?expMonth: int option, ?expYear: int option, ?fingerprint: string, ?funding: string option, ?iin: string, ?issuer: string, ?last4: string option, ?name: string option, ?threeDSecure: string, ?tokenizationMethod: string option) =
            {
                SourceTypeCard.AddressLine1Check = addressLine1Check |> Option.flatten
                SourceTypeCard.AddressZipCheck = addressZipCheck |> Option.flatten
                SourceTypeCard.Brand = brand |> Option.flatten
                SourceTypeCard.Country = country |> Option.flatten
                SourceTypeCard.CvcCheck = cvcCheck |> Option.flatten
                SourceTypeCard.Description = description
                SourceTypeCard.DynamicLast4 = dynamicLast4 |> Option.flatten
                SourceTypeCard.ExpMonth = expMonth |> Option.flatten
                SourceTypeCard.ExpYear = expYear |> Option.flatten
                SourceTypeCard.Fingerprint = fingerprint
                SourceTypeCard.Funding = funding |> Option.flatten
                SourceTypeCard.Iin = iin
                SourceTypeCard.Issuer = issuer
                SourceTypeCard.Last4 = last4 |> Option.flatten
                SourceTypeCard.Name = name |> Option.flatten
                SourceTypeCard.ThreeDSecure = threeDSecure
                SourceTypeCard.TokenizationMethod = tokenizationMethod |> Option.flatten
            }

    and SourceTypeCardPresent = {
        ApplicationCryptogram: string option
        ApplicationPreferredName: string option
        AuthorizationCode: string option
        AuthorizationResponseCode: string option
        Brand: string option
        Country: IsoTypes.IsoCountryCode option
        CvmType: string option
        DataType: string option
        DedicatedFileName: string option
        Description: string option
        EmvAuthData: string option
        EvidenceCustomerSignature: string option
        EvidenceTransactionCertificate: string option
        ExpMonth: int option
        ExpYear: int option
        Fingerprint: string option
        Funding: string option
        Iin: string option
        Issuer: string option
        [<JsonField(Name="last4")>]Last4: string option
        PosDeviceId: string option
        PosEntryMode: string option
        ReadMethod: string option
        Reader: string option
        TerminalVerificationResults: string option
        TransactionStatusInformation: string option
    }
    with
        static member New (?applicationCryptogram: string, ?applicationPreferredName: string, ?authorizationCode: string option, ?authorizationResponseCode: string, ?brand: string option, ?country: IsoTypes.IsoCountryCode option, ?cvmType: string, ?dataType: string option, ?dedicatedFileName: string, ?description: string, ?emvAuthData: string, ?evidenceCustomerSignature: string option, ?evidenceTransactionCertificate: string option, ?expMonth: int option, ?expYear: int option, ?fingerprint: string, ?funding: string option, ?iin: string, ?issuer: string, ?last4: string option, ?posDeviceId: string option, ?posEntryMode: string, ?readMethod: string option, ?reader: string option, ?terminalVerificationResults: string, ?transactionStatusInformation: string) =
            {
                SourceTypeCardPresent.ApplicationCryptogram = applicationCryptogram
                SourceTypeCardPresent.ApplicationPreferredName = applicationPreferredName
                SourceTypeCardPresent.AuthorizationCode = authorizationCode |> Option.flatten
                SourceTypeCardPresent.AuthorizationResponseCode = authorizationResponseCode
                SourceTypeCardPresent.Brand = brand |> Option.flatten
                SourceTypeCardPresent.Country = country |> Option.flatten
                SourceTypeCardPresent.CvmType = cvmType
                SourceTypeCardPresent.DataType = dataType |> Option.flatten
                SourceTypeCardPresent.DedicatedFileName = dedicatedFileName
                SourceTypeCardPresent.Description = description
                SourceTypeCardPresent.EmvAuthData = emvAuthData
                SourceTypeCardPresent.EvidenceCustomerSignature = evidenceCustomerSignature |> Option.flatten
                SourceTypeCardPresent.EvidenceTransactionCertificate = evidenceTransactionCertificate |> Option.flatten
                SourceTypeCardPresent.ExpMonth = expMonth |> Option.flatten
                SourceTypeCardPresent.ExpYear = expYear |> Option.flatten
                SourceTypeCardPresent.Fingerprint = fingerprint
                SourceTypeCardPresent.Funding = funding |> Option.flatten
                SourceTypeCardPresent.Iin = iin
                SourceTypeCardPresent.Issuer = issuer
                SourceTypeCardPresent.Last4 = last4 |> Option.flatten
                SourceTypeCardPresent.PosDeviceId = posDeviceId |> Option.flatten
                SourceTypeCardPresent.PosEntryMode = posEntryMode
                SourceTypeCardPresent.ReadMethod = readMethod |> Option.flatten
                SourceTypeCardPresent.Reader = reader |> Option.flatten
                SourceTypeCardPresent.TerminalVerificationResults = terminalVerificationResults
                SourceTypeCardPresent.TransactionStatusInformation = transactionStatusInformation
            }

    and SourceTypeKlarna = {
        BackgroundImageUrl: string option
        ClientToken: string option
        FirstName: string option
        LastName: string option
        Locale: string option
        LogoUrl: string option
        PageTitle: string option
        PayLaterAssetUrlsDescriptive: string option
        PayLaterAssetUrlsStandard: string option
        PayLaterName: string option
        PayLaterRedirectUrl: string option
        PayNowAssetUrlsDescriptive: string option
        PayNowAssetUrlsStandard: string option
        PayNowName: string option
        PayNowRedirectUrl: string option
        PayOverTimeAssetUrlsDescriptive: string option
        PayOverTimeAssetUrlsStandard: string option
        PayOverTimeName: string option
        PayOverTimeRedirectUrl: string option
        PaymentMethodCategories: string option
        PurchaseCountry: IsoTypes.IsoCountryCode option
        PurchaseType: string option
        RedirectUrl: string option
        ShippingDelay: int option
        ShippingFirstName: string option
        ShippingLastName: string option
    }
    with
        static member New (?backgroundImageUrl: string, ?clientToken: string option, ?firstName: string, ?lastName: string, ?locale: string, ?logoUrl: string, ?pageTitle: string, ?payLaterAssetUrlsDescriptive: string, ?payLaterAssetUrlsStandard: string, ?payLaterName: string, ?payLaterRedirectUrl: string, ?payNowAssetUrlsDescriptive: string, ?payNowAssetUrlsStandard: string, ?payNowName: string, ?payNowRedirectUrl: string, ?payOverTimeAssetUrlsDescriptive: string, ?payOverTimeAssetUrlsStandard: string, ?payOverTimeName: string, ?payOverTimeRedirectUrl: string, ?paymentMethodCategories: string, ?purchaseCountry: IsoTypes.IsoCountryCode, ?purchaseType: string, ?redirectUrl: string, ?shippingDelay: int, ?shippingFirstName: string, ?shippingLastName: string) =
            {
                SourceTypeKlarna.BackgroundImageUrl = backgroundImageUrl
                SourceTypeKlarna.ClientToken = clientToken |> Option.flatten
                SourceTypeKlarna.FirstName = firstName
                SourceTypeKlarna.LastName = lastName
                SourceTypeKlarna.Locale = locale
                SourceTypeKlarna.LogoUrl = logoUrl
                SourceTypeKlarna.PageTitle = pageTitle
                SourceTypeKlarna.PayLaterAssetUrlsDescriptive = payLaterAssetUrlsDescriptive
                SourceTypeKlarna.PayLaterAssetUrlsStandard = payLaterAssetUrlsStandard
                SourceTypeKlarna.PayLaterName = payLaterName
                SourceTypeKlarna.PayLaterRedirectUrl = payLaterRedirectUrl
                SourceTypeKlarna.PayNowAssetUrlsDescriptive = payNowAssetUrlsDescriptive
                SourceTypeKlarna.PayNowAssetUrlsStandard = payNowAssetUrlsStandard
                SourceTypeKlarna.PayNowName = payNowName
                SourceTypeKlarna.PayNowRedirectUrl = payNowRedirectUrl
                SourceTypeKlarna.PayOverTimeAssetUrlsDescriptive = payOverTimeAssetUrlsDescriptive
                SourceTypeKlarna.PayOverTimeAssetUrlsStandard = payOverTimeAssetUrlsStandard
                SourceTypeKlarna.PayOverTimeName = payOverTimeName
                SourceTypeKlarna.PayOverTimeRedirectUrl = payOverTimeRedirectUrl
                SourceTypeKlarna.PaymentMethodCategories = paymentMethodCategories
                SourceTypeKlarna.PurchaseCountry = purchaseCountry
                SourceTypeKlarna.PurchaseType = purchaseType
                SourceTypeKlarna.RedirectUrl = redirectUrl
                SourceTypeKlarna.ShippingDelay = shippingDelay
                SourceTypeKlarna.ShippingFirstName = shippingFirstName
                SourceTypeKlarna.ShippingLastName = shippingLastName
            }

    and SourceTypeMultibanco = {
        Entity: string option
        Reference: string option
        RefundAccountHolderAddressCity: string option
        RefundAccountHolderAddressCountry: IsoTypes.IsoCountryCode option
        [<JsonField(Name="refund_account_holder_address_line1")>]RefundAccountHolderAddressLine1: string option
        [<JsonField(Name="refund_account_holder_address_line2")>]RefundAccountHolderAddressLine2: string option
        RefundAccountHolderAddressPostalCode: string option
        RefundAccountHolderAddressState: string option
        RefundAccountHolderName: string option
        RefundIban: string option
    }
    with
        static member New (?entity: string option, ?reference: string option, ?refundAccountHolderAddressCity: string option, ?refundAccountHolderAddressCountry: IsoTypes.IsoCountryCode option, ?refundAccountHolderAddressLine1: string option, ?refundAccountHolderAddressLine2: string option, ?refundAccountHolderAddressPostalCode: string option, ?refundAccountHolderAddressState: string option, ?refundAccountHolderName: string option, ?refundIban: string option) =
            {
                SourceTypeMultibanco.Entity = entity |> Option.flatten
                SourceTypeMultibanco.Reference = reference |> Option.flatten
                SourceTypeMultibanco.RefundAccountHolderAddressCity = refundAccountHolderAddressCity |> Option.flatten
                SourceTypeMultibanco.RefundAccountHolderAddressCountry = refundAccountHolderAddressCountry |> Option.flatten
                SourceTypeMultibanco.RefundAccountHolderAddressLine1 = refundAccountHolderAddressLine1 |> Option.flatten
                SourceTypeMultibanco.RefundAccountHolderAddressLine2 = refundAccountHolderAddressLine2 |> Option.flatten
                SourceTypeMultibanco.RefundAccountHolderAddressPostalCode = refundAccountHolderAddressPostalCode |> Option.flatten
                SourceTypeMultibanco.RefundAccountHolderAddressState = refundAccountHolderAddressState |> Option.flatten
                SourceTypeMultibanco.RefundAccountHolderName = refundAccountHolderName |> Option.flatten
                SourceTypeMultibanco.RefundIban = refundIban |> Option.flatten
            }

    and SourceTypeSepaCreditTransfer = {
        BankName: string option
        Bic: string option
        Iban: string option
        RefundAccountHolderAddressCity: string option
        RefundAccountHolderAddressCountry: IsoTypes.IsoCountryCode option
        [<JsonField(Name="refund_account_holder_address_line1")>]RefundAccountHolderAddressLine1: string option
        [<JsonField(Name="refund_account_holder_address_line2")>]RefundAccountHolderAddressLine2: string option
        RefundAccountHolderAddressPostalCode: string option
        RefundAccountHolderAddressState: string option
        RefundAccountHolderName: string option
        RefundIban: string option
    }
    with
        static member New (?bankName: string option, ?bic: string option, ?iban: string option, ?refundAccountHolderAddressCity: string option, ?refundAccountHolderAddressCountry: IsoTypes.IsoCountryCode option, ?refundAccountHolderAddressLine1: string option, ?refundAccountHolderAddressLine2: string option, ?refundAccountHolderAddressPostalCode: string option, ?refundAccountHolderAddressState: string option, ?refundAccountHolderName: string option, ?refundIban: string option) =
            {
                SourceTypeSepaCreditTransfer.BankName = bankName |> Option.flatten
                SourceTypeSepaCreditTransfer.Bic = bic |> Option.flatten
                SourceTypeSepaCreditTransfer.Iban = iban |> Option.flatten
                SourceTypeSepaCreditTransfer.RefundAccountHolderAddressCity = refundAccountHolderAddressCity |> Option.flatten
                SourceTypeSepaCreditTransfer.RefundAccountHolderAddressCountry = refundAccountHolderAddressCountry |> Option.flatten
                SourceTypeSepaCreditTransfer.RefundAccountHolderAddressLine1 = refundAccountHolderAddressLine1 |> Option.flatten
                SourceTypeSepaCreditTransfer.RefundAccountHolderAddressLine2 = refundAccountHolderAddressLine2 |> Option.flatten
                SourceTypeSepaCreditTransfer.RefundAccountHolderAddressPostalCode = refundAccountHolderAddressPostalCode |> Option.flatten
                SourceTypeSepaCreditTransfer.RefundAccountHolderAddressState = refundAccountHolderAddressState |> Option.flatten
                SourceTypeSepaCreditTransfer.RefundAccountHolderName = refundAccountHolderName |> Option.flatten
                SourceTypeSepaCreditTransfer.RefundIban = refundIban |> Option.flatten
            }

    and SourceTypeSepaDebit = {
        BankCode: string option
        BranchCode: string option
        Country: IsoTypes.IsoCountryCode option
        Fingerprint: string option
        [<JsonField(Name="last4")>]Last4: string option
        MandateReference: string option
        MandateUrl: string option
    }
    with
        static member New (?bankCode: string option, ?branchCode: string option, ?country: IsoTypes.IsoCountryCode option, ?fingerprint: string option, ?last4: string option, ?mandateReference: string option, ?mandateUrl: string option) =
            {
                SourceTypeSepaDebit.BankCode = bankCode |> Option.flatten
                SourceTypeSepaDebit.BranchCode = branchCode |> Option.flatten
                SourceTypeSepaDebit.Country = country |> Option.flatten
                SourceTypeSepaDebit.Fingerprint = fingerprint |> Option.flatten
                SourceTypeSepaDebit.Last4 = last4 |> Option.flatten
                SourceTypeSepaDebit.MandateReference = mandateReference |> Option.flatten
                SourceTypeSepaDebit.MandateUrl = mandateUrl |> Option.flatten
            }

    and SourceTypeSofort = {
        BankCode: string option
        BankName: string option
        Bic: string option
        Country: IsoTypes.IsoCountryCode option
        [<JsonField(Name="iban_last4")>]IbanLast4: string option
        PreferredLanguage: string option
        StatementDescriptor: string option
    }
    with
        static member New (?bankCode: string option, ?bankName: string option, ?bic: string option, ?country: IsoTypes.IsoCountryCode option, ?ibanLast4: string option, ?preferredLanguage: string option, ?statementDescriptor: string option) =
            {
                SourceTypeSofort.BankCode = bankCode |> Option.flatten
                SourceTypeSofort.BankName = bankName |> Option.flatten
                SourceTypeSofort.Bic = bic |> Option.flatten
                SourceTypeSofort.Country = country |> Option.flatten
                SourceTypeSofort.IbanLast4 = ibanLast4 |> Option.flatten
                SourceTypeSofort.PreferredLanguage = preferredLanguage |> Option.flatten
                SourceTypeSofort.StatementDescriptor = statementDescriptor |> Option.flatten
            }

    and SourceTypeThreeDSecure = {
        [<JsonField(Name="address_line1_check")>]AddressLine1Check: string option
        AddressZipCheck: string option
        Authenticated: bool option
        Brand: string option
        Card: string option
        Country: IsoTypes.IsoCountryCode option
        Customer: string option
        CvcCheck: string option
        Description: string option
        [<JsonField(Name="dynamic_last4")>]DynamicLast4: string option
        ExpMonth: int option
        ExpYear: int option
        Fingerprint: string option
        Funding: string option
        Iin: string option
        Issuer: string option
        [<JsonField(Name="last4")>]Last4: string option
        Name: string option
        ThreeDSecure: string option
        TokenizationMethod: string option
    }
    with
        static member New (?addressLine1Check: string option, ?addressZipCheck: string option, ?authenticated: bool option, ?brand: string option, ?card: string option, ?country: IsoTypes.IsoCountryCode option, ?customer: string option, ?cvcCheck: string option, ?description: string, ?dynamicLast4: string option, ?expMonth: int option, ?expYear: int option, ?fingerprint: string, ?funding: string option, ?iin: string, ?issuer: string, ?last4: string option, ?name: string option, ?threeDSecure: string, ?tokenizationMethod: string option) =
            {
                SourceTypeThreeDSecure.AddressLine1Check = addressLine1Check |> Option.flatten
                SourceTypeThreeDSecure.AddressZipCheck = addressZipCheck |> Option.flatten
                SourceTypeThreeDSecure.Authenticated = authenticated |> Option.flatten
                SourceTypeThreeDSecure.Brand = brand |> Option.flatten
                SourceTypeThreeDSecure.Card = card |> Option.flatten
                SourceTypeThreeDSecure.Country = country |> Option.flatten
                SourceTypeThreeDSecure.Customer = customer |> Option.flatten
                SourceTypeThreeDSecure.CvcCheck = cvcCheck |> Option.flatten
                SourceTypeThreeDSecure.Description = description
                SourceTypeThreeDSecure.DynamicLast4 = dynamicLast4 |> Option.flatten
                SourceTypeThreeDSecure.ExpMonth = expMonth |> Option.flatten
                SourceTypeThreeDSecure.ExpYear = expYear |> Option.flatten
                SourceTypeThreeDSecure.Fingerprint = fingerprint
                SourceTypeThreeDSecure.Funding = funding |> Option.flatten
                SourceTypeThreeDSecure.Iin = iin
                SourceTypeThreeDSecure.Issuer = issuer
                SourceTypeThreeDSecure.Last4 = last4 |> Option.flatten
                SourceTypeThreeDSecure.Name = name |> Option.flatten
                SourceTypeThreeDSecure.ThreeDSecure = threeDSecure
                SourceTypeThreeDSecure.TokenizationMethod = tokenizationMethod |> Option.flatten
            }

    and StackableDiscountWithDiscountSettings = {
        ///ID of the coupon to create a new discount for.
        Coupon: StackableDiscountWithDiscountSettingsCoupon'AnyOf option
        ///ID of an existing discount on the object (or one of its ancestors) to reuse.
        Discount: StackableDiscountWithDiscountSettingsDiscount'AnyOf option
        ///ID of the promotion code to create a new discount for.
        PromotionCode: StackableDiscountWithDiscountSettingsPromotionCode'AnyOf option
    }
    with
        static member New (coupon: StackableDiscountWithDiscountSettingsCoupon'AnyOf option, discount: StackableDiscountWithDiscountSettingsDiscount'AnyOf option, promotionCode: StackableDiscountWithDiscountSettingsPromotionCode'AnyOf option) =
            {
                StackableDiscountWithDiscountSettings.Coupon = coupon //required
                StackableDiscountWithDiscountSettings.Discount = discount //required
                StackableDiscountWithDiscountSettings.PromotionCode = promotionCode //required
            }

    and StackableDiscountWithDiscountSettingsCoupon'AnyOf =
        | String of string
        | Coupon of Coupon

    and StackableDiscountWithDiscountSettingsDiscount'AnyOf =
        | String of string
        | Discount of Discount

    and StackableDiscountWithDiscountSettingsPromotionCode'AnyOf =
        | String of string
        | PromotionCode of PromotionCode

    and StackableDiscountWithDiscountSettingsAndDiscountEnd = {
        ///ID of the coupon to create a new discount for.
        Coupon: StackableDiscountWithDiscountSettingsAndDiscountEndCoupon'AnyOf option
        ///ID of an existing discount on the object (or one of its ancestors) to reuse.
        Discount: StackableDiscountWithDiscountSettingsAndDiscountEndDiscount'AnyOf option
        ///ID of the promotion code to create a new discount for.
        PromotionCode: StackableDiscountWithDiscountSettingsAndDiscountEndPromotionCode'AnyOf option
    }
    with
        static member New (coupon: StackableDiscountWithDiscountSettingsAndDiscountEndCoupon'AnyOf option, discount: StackableDiscountWithDiscountSettingsAndDiscountEndDiscount'AnyOf option, promotionCode: StackableDiscountWithDiscountSettingsAndDiscountEndPromotionCode'AnyOf option) =
            {
                StackableDiscountWithDiscountSettingsAndDiscountEnd.Coupon = coupon //required
                StackableDiscountWithDiscountSettingsAndDiscountEnd.Discount = discount //required
                StackableDiscountWithDiscountSettingsAndDiscountEnd.PromotionCode = promotionCode //required
            }

    and StackableDiscountWithDiscountSettingsAndDiscountEndCoupon'AnyOf =
        | String of string
        | Coupon of Coupon

    and StackableDiscountWithDiscountSettingsAndDiscountEndDiscount'AnyOf =
        | String of string
        | Discount of Discount

    and StackableDiscountWithDiscountSettingsAndDiscountEndPromotionCode'AnyOf =
        | String of string
        | PromotionCode of PromotionCode

    ///Subscriptions allow you to charge a customer on a recurring basis.
    ///Related guide: [Creating subscriptions](https://docs.stripe.com/billing/subscriptions/creating)
    and Subscription = {
        ///ID of the Connect Application that created the subscription.
        Application: SubscriptionApplication'AnyOf option
        ///A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account.
        ApplicationFeePercent: decimal option
        AutomaticTax: SubscriptionAutomaticTax
        ///The reference point that aligns future [billing cycle](https://docs.stripe.com/subscriptions/billing-cycle) dates. It sets the day of week for `week` intervals, the day of month for `month` and `year` intervals, and the month of year for `year` intervals. The timestamp is in UTC format.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]BillingCycleAnchor: DateTime
        ///The fixed values used to calculate the `billing_cycle_anchor`.
        BillingCycleAnchorConfig: SubscriptionsResourceBillingCycleAnchorConfig option
        BillingMode: SubscriptionsResourceBillingMode
        ///Define thresholds at which an invoice will be sent, and the subscription advanced to a new billing period
        BillingThresholds: SubscriptionBillingThresholds option
        ///A date in the future at which the subscription will automatically get canceled
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CancelAt: DateTime option
        ///Whether this subscription will (if `status=active`) or did (if `status=canceled`) cancel at the end of the current billing period.
        CancelAtPeriodEnd: bool
        ///If the subscription has been canceled, the date of that cancellation. If the subscription was canceled with `cancel_at_period_end`, `canceled_at` will reflect the time of the most recent update request, not the end of the subscription period when the subscription is automatically moved to a canceled state.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CanceledAt: DateTime option
        ///Details about why this subscription was cancelled
        CancellationDetails: CancellationDetails option
        ///Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this subscription at the end of the cycle using the default source attached to the customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`.
        CollectionMethod: SubscriptionCollectionMethod
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///ID of the customer who owns the subscription.
        Customer: SubscriptionCustomer'AnyOf
        ///ID of the account representing the customer who owns the subscription.
        CustomerAccount: string option
        ///Number of days a customer has to pay invoices generated by this subscription. This value will be `null` for subscriptions where `collection_method=charge_automatically`.
        DaysUntilDue: int option
        ///ID of the default payment method for the subscription. It must belong to the customer associated with the subscription. This takes precedence over `default_source`. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://docs.stripe.com/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://docs.stripe.com/api/customers/object#customer_object-default_source).
        DefaultPaymentMethod: SubscriptionDefaultPaymentMethod'AnyOf option
        ///ID of the default payment source for the subscription. It must belong to the customer associated with the subscription and be in a chargeable state. If `default_payment_method` is also set, `default_payment_method` will take precedence. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://docs.stripe.com/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://docs.stripe.com/api/customers/object#customer_object-default_source).
        DefaultSource: SubscriptionDefaultSource'AnyOf option
        ///The tax rates that will apply to any subscription item that does not have `tax_rates` set. Invoices created will have their `default_tax_rates` populated from the subscription.
        DefaultTaxRates: TaxRate list option
        ///The subscription's description, meant to be displayable to the customer. Use this field to optionally store an explanation of the subscription for rendering in Stripe surfaces and certain local payment methods UIs.
        Description: string option
        ///The discounts applied to the subscription. Subscription item discounts are applied before subscription discounts. Use `expand[]=discounts` to expand each discount.
        Discounts: SubscriptionDiscounts'AnyOf list
        ///If the subscription has ended, the date the subscription ended.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]EndedAt: DateTime option
        ///Unique identifier for the object.
        Id: string
        InvoiceSettings: SubscriptionsResourceSubscriptionInvoiceSettings
        ///List of subscription items, each with an attached price.
        Items: SubscriptionItems
        ///The most recent invoice this subscription has generated over its lifecycle (for example, when it cycles or is updated).
        LatestInvoice: SubscriptionLatestInvoice'AnyOf option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Settings for Managed Payments for this Subscription and resulting [Invoices](/api/invoices/object) and [PaymentIntents](/api/payment_intents/object).
        ManagedPayments: SmorResourceManagedPayments option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///Specifies the approximate timestamp on which any pending invoice items will be billed according to the schedule provided at `pending_invoice_item_interval`.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]NextPendingInvoiceItemInvoice: DateTime option
        ///The account (if any) the charge was made on behalf of for charges associated with this subscription. See the [Connect documentation](https://docs.stripe.com/connect/subscriptions#on-behalf-of) for details.
        OnBehalfOf: SubscriptionOnBehalfOf'AnyOf option
        ///If specified, payment collection for this subscription will be paused. Note that the subscription status will be unchanged and will not be updated to `paused`. Learn more about [pausing collection](https://docs.stripe.com/billing/subscriptions/pause-payment).
        PauseCollection: SubscriptionsResourcePauseCollection option
        ///Payment settings passed on to invoices created by the subscription.
        PaymentSettings: SubscriptionsResourcePaymentSettings option
        ///Specifies an interval for how often to bill for any pending invoice items. It is analogous to calling [Create an invoice](/api/invoices/create) for the given subscription at the specified interval.
        PendingInvoiceItemInterval: SubscriptionPendingInvoiceItemInterval option
        ///You can use this [SetupIntent](https://docs.stripe.com/api/setup_intents) to collect user authentication when creating a subscription without immediate payment or updating a subscription's payment method, allowing you to optimize for off-session payments. Learn more in the [SCA Migration Guide](https://docs.stripe.com/billing/migration/strong-customer-authentication#scenario-2).
        PendingSetupIntent: SubscriptionPendingSetupIntent'AnyOf option
        ///If specified, [pending updates](https://docs.stripe.com/billing/subscriptions/pending-updates) that will be applied to the subscription once the `latest_invoice` has been paid.
        PendingUpdate: SubscriptionsResourcePendingUpdate option
        PresentmentDetails: SubscriptionsResourceSubscriptionPresentmentDetails option
        ///The schedule attached to the subscription
        Schedule: SubscriptionSchedule'AnyOf option
        ///Date when the subscription was first created. The date might differ from the `created` date due to backdating.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]StartDate: DateTime
        ///Possible values are `incomplete`, `incomplete_expired`, `trialing`, `active`, `past_due`, `canceled`, `unpaid`, or `paused`. 
        ///For `collection_method=charge_automatically` a subscription moves into `incomplete` if the initial payment attempt fails. A subscription in this status can only have metadata and default_source updated. Once the first invoice is paid, the subscription moves into an `active` status. If the first invoice is not paid within 23 hours, the subscription transitions to `incomplete_expired`. This is a terminal status, the open invoice will be voided and no further invoices will be generated. 
        ///A subscription that is currently in a trial period is `trialing` and moves to `active` when the trial period is over. 
        ///A subscription can only enter a `paused` status [when a trial ends without a payment method](https://docs.stripe.com/billing/subscriptions/trials#create-free-trials-without-payment). A `paused` subscription doesn't generate invoices and can be resumed after your customer adds their payment method. The `paused` status is different from [pausing collection](https://docs.stripe.com/billing/subscriptions/pause-payment), which still generates invoices and leaves the subscription's status unchanged. 
        ///If subscription `collection_method=charge_automatically`, it becomes `past_due` when payment is required but cannot be paid (due to failed payment or awaiting additional user actions). Once Stripe has exhausted all payment retry attempts, the subscription will become `canceled` or `unpaid` (depending on your subscriptions settings). 
        ///If subscription `collection_method=send_invoice` it becomes `past_due` when its invoice is not paid by the due date, and `canceled` or `unpaid` if it is still not paid by an additional deadline after that. Note that when a subscription has a status of `unpaid`, no subsequent invoices will be attempted (invoices will be created, but then immediately automatically closed). After receiving updated payment information from a customer, you may choose to reopen and pay their closed invoices.
        Status: SubscriptionStatus
        ///ID of the test clock this subscription belongs to.
        TestClock: SubscriptionTestClock'AnyOf option
        ///The account (if any) the subscription's payments will be attributed to for tax reporting, and where funds from each payment will be transferred to for each of the subscription's invoices.
        TransferData: SubscriptionTransferData option
        ///If the subscription has a trial, the end of that trial.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]TrialEnd: DateTime option
        ///Settings related to subscription trials.
        TrialSettings: SubscriptionsResourceTrialSettingsTrialSettings option
        ///If the subscription has a trial, the beginning of that trial.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]TrialStart: DateTime option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "subscription"

        static member New (application: SubscriptionApplication'AnyOf option, applicationFeePercent: decimal option, automaticTax: SubscriptionAutomaticTax, billingCycleAnchor: DateTime, billingCycleAnchorConfig: SubscriptionsResourceBillingCycleAnchorConfig option, billingMode: SubscriptionsResourceBillingMode, billingThresholds: SubscriptionBillingThresholds option, cancelAt: DateTime option, cancelAtPeriodEnd: bool, canceledAt: DateTime option, cancellationDetails: CancellationDetails option, collectionMethod: SubscriptionCollectionMethod, created: DateTime, currency: IsoTypes.IsoCurrencyCode, customer: SubscriptionCustomer'AnyOf, customerAccount: string option, daysUntilDue: int option, defaultPaymentMethod: SubscriptionDefaultPaymentMethod'AnyOf option, defaultSource: SubscriptionDefaultSource'AnyOf option, description: string option, discounts: SubscriptionDiscounts'AnyOf list, endedAt: DateTime option, id: string, invoiceSettings: SubscriptionsResourceSubscriptionInvoiceSettings, items: SubscriptionItems, latestInvoice: SubscriptionLatestInvoice'AnyOf option, livemode: bool, managedPayments: SmorResourceManagedPayments option, metadata: Map<string, string>, nextPendingInvoiceItemInvoice: DateTime option, onBehalfOf: SubscriptionOnBehalfOf'AnyOf option, pauseCollection: SubscriptionsResourcePauseCollection option, paymentSettings: SubscriptionsResourcePaymentSettings option, pendingInvoiceItemInterval: SubscriptionPendingInvoiceItemInterval option, pendingSetupIntent: SubscriptionPendingSetupIntent'AnyOf option, pendingUpdate: SubscriptionsResourcePendingUpdate option, schedule: SubscriptionSchedule'AnyOf option, startDate: DateTime, status: SubscriptionStatus, testClock: SubscriptionTestClock'AnyOf option, transferData: SubscriptionTransferData option, trialEnd: DateTime option, trialSettings: SubscriptionsResourceTrialSettingsTrialSettings option, trialStart: DateTime option, ?defaultTaxRates: TaxRate list option, ?presentmentDetails: SubscriptionsResourceSubscriptionPresentmentDetails) =
            {
                Subscription.Application = application //required
                Subscription.ApplicationFeePercent = applicationFeePercent //required
                Subscription.AutomaticTax = automaticTax //required
                Subscription.BillingCycleAnchor = billingCycleAnchor //required
                Subscription.BillingCycleAnchorConfig = billingCycleAnchorConfig //required
                Subscription.BillingMode = billingMode //required
                Subscription.BillingThresholds = billingThresholds //required
                Subscription.CancelAt = cancelAt //required
                Subscription.CancelAtPeriodEnd = cancelAtPeriodEnd //required
                Subscription.CanceledAt = canceledAt //required
                Subscription.CancellationDetails = cancellationDetails //required
                Subscription.CollectionMethod = collectionMethod //required
                Subscription.Created = created //required
                Subscription.Currency = currency //required
                Subscription.Customer = customer //required
                Subscription.CustomerAccount = customerAccount //required
                Subscription.DaysUntilDue = daysUntilDue //required
                Subscription.DefaultPaymentMethod = defaultPaymentMethod //required
                Subscription.DefaultSource = defaultSource //required
                Subscription.Description = description //required
                Subscription.Discounts = discounts //required
                Subscription.EndedAt = endedAt //required
                Subscription.Id = id //required
                Subscription.InvoiceSettings = invoiceSettings //required
                Subscription.Items = items //required
                Subscription.LatestInvoice = latestInvoice //required
                Subscription.Livemode = livemode //required
                Subscription.ManagedPayments = managedPayments //required
                Subscription.Metadata = metadata //required
                Subscription.NextPendingInvoiceItemInvoice = nextPendingInvoiceItemInvoice //required
                Subscription.OnBehalfOf = onBehalfOf //required
                Subscription.PauseCollection = pauseCollection //required
                Subscription.PaymentSettings = paymentSettings //required
                Subscription.PendingInvoiceItemInterval = pendingInvoiceItemInterval //required
                Subscription.PendingSetupIntent = pendingSetupIntent //required
                Subscription.PendingUpdate = pendingUpdate //required
                Subscription.Schedule = schedule //required
                Subscription.StartDate = startDate //required
                Subscription.Status = status //required
                Subscription.TestClock = testClock //required
                Subscription.TransferData = transferData //required
                Subscription.TrialEnd = trialEnd //required
                Subscription.TrialSettings = trialSettings //required
                Subscription.TrialStart = trialStart //required
                Subscription.DefaultTaxRates = defaultTaxRates |> Option.flatten
                Subscription.PresentmentDetails = presentmentDetails
            }

    and SubscriptionApplication'AnyOf =
        | String of string
        | Application of Application
        | DeletedApplication of DeletedApplication

    and SubscriptionCustomer'AnyOf =
        | String of string
        | Customer of Customer
        | DeletedCustomer of DeletedCustomer

    and SubscriptionDefaultPaymentMethod'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and SubscriptionDefaultSource'AnyOf =
        | String of string
        | PaymentSource of PaymentSource

    and SubscriptionLatestInvoice'AnyOf =
        | String of string
        | Invoice of Invoice

    and SubscriptionOnBehalfOf'AnyOf =
        | String of string
        | Account of Account

    and SubscriptionPendingSetupIntent'AnyOf =
        | String of string
        | SetupIntent of SetupIntent

    and SubscriptionSchedule'AnyOf =
        | String of string
        | SubscriptionSchedule of SubscriptionSchedule

    and SubscriptionTestClock'AnyOf =
        | String of string
        | TestHelpersTestClock of TestHelpersTestClock

    and SubscriptionDiscounts'AnyOf =
        | String of string
        | Discount of Discount

    ///List of subscription items, each with an attached price.
    and SubscriptionItems = {
        ///Details about each object.
        Data: SubscriptionItem list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: SubscriptionItem list, hasMore: bool, url: string) =
            {
                SubscriptionItems.Data = data //required
                SubscriptionItems.HasMore = hasMore //required
                SubscriptionItems.Url = url //required
            }

    and SubscriptionAutomaticTax = {
        ///Whether Stripe automatically computes tax on this subscription.
        Enabled: bool
        ///The account that's liable for tax. If set, the business address and tax registrations required to perform the tax calculation are loaded from this account. The tax transaction is returned in the report of the connected account.
        Liability: ConnectAccountReference option
    }
    with
        ///If Stripe disabled automatic tax, this enum describes why.
        member _.DisabledReason = "requires_location_inputs"

        static member New (enabled: bool, liability: ConnectAccountReference option) =
            {
                SubscriptionAutomaticTax.Enabled = enabled //required
                SubscriptionAutomaticTax.Liability = liability //required
            }

    ///Subscription items allow you to create customer subscriptions with more than
    ///one plan, making it easy to represent complex billing relationships.
    and SubscriptionItem = {
        ///Define thresholds at which an invoice will be sent, and the related subscription advanced to a new billing period
        BillingThresholds: SubscriptionItemBillingThresholds option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        Created: int
        ///The end time of this subscription item's current billing period.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CurrentPeriodEnd: DateTime
        ///The start time of this subscription item's current billing period.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CurrentPeriodStart: DateTime
        ///The discounts applied to the subscription item. Subscription item discounts are applied before subscription discounts. Use `expand[]=discounts` to expand each discount.
        Discounts: SubscriptionItemDiscounts'AnyOf list
        ///Unique identifier for the object.
        Id: string
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        Plan: Plan
        Price: Price
        ///The [quantity](https://docs.stripe.com/subscriptions/quantities) of the plan to which the customer should be subscribed.
        Quantity: int option
        ///The `subscription` this `subscription_item` belongs to.
        Subscription: string
        ///The tax rates which apply to this `subscription_item`. When set, the `default_tax_rates` on the subscription do not apply to this `subscription_item`.
        TaxRates: TaxRate list option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "subscription_item"

        static member New (billingThresholds: SubscriptionItemBillingThresholds option, created: int, currentPeriodEnd: DateTime, currentPeriodStart: DateTime, discounts: SubscriptionItemDiscounts'AnyOf list, id: string, metadata: Map<string, string>, plan: Plan, price: Price, subscription: string, taxRates: TaxRate list option, ?quantity: int) =
            {
                SubscriptionItem.BillingThresholds = billingThresholds //required
                SubscriptionItem.Created = created //required
                SubscriptionItem.CurrentPeriodEnd = currentPeriodEnd //required
                SubscriptionItem.CurrentPeriodStart = currentPeriodStart //required
                SubscriptionItem.Discounts = discounts //required
                SubscriptionItem.Id = id //required
                SubscriptionItem.Metadata = metadata //required
                SubscriptionItem.Plan = plan //required
                SubscriptionItem.Price = price //required
                SubscriptionItem.Subscription = subscription //required
                SubscriptionItem.TaxRates = taxRates //required
                SubscriptionItem.Quantity = quantity
            }

    and SubscriptionItemDiscounts'AnyOf =
        | String of string
        | Discount of Discount

    and SubscriptionPaymentMethodOptionsCard = {
        MandateOptions: InvoiceMandateOptionsCard option
        ///Selected network to process this Subscription on. Depends on the available networks of the card attached to the Subscription. Can be only set confirm-time.
        Network: SubscriptionPaymentMethodOptionsCardNetwork option
        ///We strongly recommend that you rely on our SCA Engine to automatically prompt your customers for authentication based on risk level and [other requirements](https://docs.stripe.com/strong-customer-authentication). However, if you wish to request 3D Secure based on logic from your own fraud engine, provide this option. Read our guide on [manually requesting 3D Secure](https://docs.stripe.com/payments/3d-secure/authentication-flow#manual-three-ds) for more information on how this configuration interacts with Radar and our SCA Engine.
        RequestThreeDSecure: SubscriptionPaymentMethodOptionsCardRequestThreeDSecure option
    }
    with
        static member New (network: SubscriptionPaymentMethodOptionsCardNetwork option, requestThreeDSecure: SubscriptionPaymentMethodOptionsCardRequestThreeDSecure option, ?mandateOptions: InvoiceMandateOptionsCard) =
            {
                SubscriptionPaymentMethodOptionsCard.Network = network //required
                SubscriptionPaymentMethodOptionsCard.RequestThreeDSecure = requestThreeDSecure //required
                SubscriptionPaymentMethodOptionsCard.MandateOptions = mandateOptions
            }

    and SubscriptionPaymentMethodOptionsMandateOptionsPix = {
        ///Amount to be charged for future payments.
        Amount: int option
        ///Determines if the amount includes the IOF tax.
        AmountIncludesIof: SubscriptionPaymentMethodOptionsMandateOptionsPixAmountIncludesIof option
        ///Date when the mandate expires and no further payments will be charged, in `YYYY-MM-DD`.
        EndDate: string option
        ///Schedule at which the future payments will be charged.
        PaymentSchedule: SubscriptionPaymentMethodOptionsMandateOptionsPixPaymentSchedule option
    }
    with
        static member New (amount: int option, amountIncludesIof: SubscriptionPaymentMethodOptionsMandateOptionsPixAmountIncludesIof option, endDate: string option, paymentSchedule: SubscriptionPaymentMethodOptionsMandateOptionsPixPaymentSchedule option) =
            {
                SubscriptionPaymentMethodOptionsMandateOptionsPix.Amount = amount //required
                SubscriptionPaymentMethodOptionsMandateOptionsPix.AmountIncludesIof = amountIncludesIof //required
                SubscriptionPaymentMethodOptionsMandateOptionsPix.EndDate = endDate //required
                SubscriptionPaymentMethodOptionsMandateOptionsPix.PaymentSchedule = paymentSchedule //required
            }

    and SubscriptionPaymentMethodOptionsPix = {
        ///The number of seconds (between 10 and 1209600) after which Pix payment will expire. Defaults to 86400 seconds.
        ExpiresAfterSeconds: int option
        MandateOptions: SubscriptionPaymentMethodOptionsMandateOptionsPix option
    }
    with
        static member New (?expiresAfterSeconds: int, ?mandateOptions: SubscriptionPaymentMethodOptionsMandateOptionsPix) =
            {
                SubscriptionPaymentMethodOptionsPix.ExpiresAfterSeconds = expiresAfterSeconds
                SubscriptionPaymentMethodOptionsPix.MandateOptions = mandateOptions
            }

    and SubscriptionPendingInvoiceItemInterval = {
        ///Specifies invoicing frequency. Either `day`, `week`, `month` or `year`.
        Interval: SubscriptionPendingInvoiceItemIntervalInterval
        ///The number of intervals between invoices. For example, `interval=month` and `interval_count=3` bills every 3 months. Maximum of one year interval allowed (1 year, 12 months, or 52 weeks).
        IntervalCount: int
    }
    with
        static member New (interval: SubscriptionPendingInvoiceItemIntervalInterval, intervalCount: int) =
            {
                SubscriptionPendingInvoiceItemInterval.Interval = interval //required
                SubscriptionPendingInvoiceItemInterval.IntervalCount = intervalCount //required
            }

    ///A subscription schedule allows you to create and manage the lifecycle of a subscription by predefining expected changes.
    ///Related guide: [Subscription schedules](https://docs.stripe.com/billing/subscriptions/subscription-schedules)
    and SubscriptionSchedule = {
        ///ID of the Connect Application that created the schedule.
        Application: SubscriptionScheduleApplication'AnyOf option
        BillingMode: SubscriptionsResourceBillingMode
        ///Time at which the subscription schedule was canceled. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CanceledAt: DateTime option
        ///Time at which the subscription schedule was completed. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]CompletedAt: DateTime option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Object representing the start and end dates for the current phase of the subscription schedule, if it is `active`.
        CurrentPhase: SubscriptionScheduleCurrentPhase option
        ///ID of the customer who owns the subscription schedule.
        Customer: SubscriptionScheduleCustomer'AnyOf
        ///ID of the account who owns the subscription schedule.
        CustomerAccount: string option
        DefaultSettings: SubscriptionSchedulesResourceDefaultSettings
        ///Behavior of the subscription schedule and underlying subscription when it ends. Possible values are `release` or `cancel` with the default being `release`. `release` will end the subscription schedule and keep the underlying subscription running. `cancel` will end the subscription schedule and cancel the underlying subscription.
        EndBehavior: SubscriptionScheduleEndBehavior
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///Configuration for the subscription schedule's phases.
        Phases: SubscriptionSchedulePhaseConfiguration list
        ///Time at which the subscription schedule was released. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ReleasedAt: DateTime option
        ///ID of the subscription once managed by the subscription schedule (if it is released).
        ReleasedSubscription: string option
        ///The present status of the subscription schedule. Possible values are `not_started`, `active`, `completed`, `released`, and `canceled`. You can read more about the different states in our [behavior guide](https://docs.stripe.com/billing/subscriptions/subscription-schedules).
        Status: SubscriptionScheduleStatus
        ///ID of the subscription managed by the subscription schedule.
        Subscription: SubscriptionScheduleSubscription'AnyOf option
        ///ID of the test clock this subscription schedule belongs to.
        TestClock: SubscriptionScheduleTestClock'AnyOf option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "subscription_schedule"

        static member New (application: SubscriptionScheduleApplication'AnyOf option, billingMode: SubscriptionsResourceBillingMode, canceledAt: DateTime option, completedAt: DateTime option, created: DateTime, currentPhase: SubscriptionScheduleCurrentPhase option, customer: SubscriptionScheduleCustomer'AnyOf, customerAccount: string option, defaultSettings: SubscriptionSchedulesResourceDefaultSettings, endBehavior: SubscriptionScheduleEndBehavior, id: string, livemode: bool, metadata: Map<string, string> option, phases: SubscriptionSchedulePhaseConfiguration list, releasedAt: DateTime option, releasedSubscription: string option, status: SubscriptionScheduleStatus, subscription: SubscriptionScheduleSubscription'AnyOf option, testClock: SubscriptionScheduleTestClock'AnyOf option) =
            {
                SubscriptionSchedule.Application = application //required
                SubscriptionSchedule.BillingMode = billingMode //required
                SubscriptionSchedule.CanceledAt = canceledAt //required
                SubscriptionSchedule.CompletedAt = completedAt //required
                SubscriptionSchedule.Created = created //required
                SubscriptionSchedule.CurrentPhase = currentPhase //required
                SubscriptionSchedule.Customer = customer //required
                SubscriptionSchedule.CustomerAccount = customerAccount //required
                SubscriptionSchedule.DefaultSettings = defaultSettings //required
                SubscriptionSchedule.EndBehavior = endBehavior //required
                SubscriptionSchedule.Id = id //required
                SubscriptionSchedule.Livemode = livemode //required
                SubscriptionSchedule.Metadata = metadata //required
                SubscriptionSchedule.Phases = phases //required
                SubscriptionSchedule.ReleasedAt = releasedAt //required
                SubscriptionSchedule.ReleasedSubscription = releasedSubscription //required
                SubscriptionSchedule.Status = status //required
                SubscriptionSchedule.Subscription = subscription //required
                SubscriptionSchedule.TestClock = testClock //required
            }

    and SubscriptionScheduleApplication'AnyOf =
        | String of string
        | Application of Application
        | DeletedApplication of DeletedApplication

    and SubscriptionScheduleCustomer'AnyOf =
        | String of string
        | Customer of Customer
        | DeletedCustomer of DeletedCustomer

    and SubscriptionScheduleSubscription'AnyOf =
        | String of string
        | Subscription of Subscription

    and SubscriptionScheduleTestClock'AnyOf =
        | String of string
        | TestHelpersTestClock of TestHelpersTestClock

    ///An Add Invoice Item describes the prices and quantities that will be added as pending invoice items when entering a phase.
    and SubscriptionScheduleAddInvoiceItem = {
        ///The stackable discounts that will be applied to the item.
        Discounts: DiscountsResourceStackableDiscountWithDiscountEnd list
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        Period: SubscriptionScheduleAddInvoiceItemPeriod
        ///ID of the price used to generate the invoice item.
        Price: SubscriptionScheduleAddInvoiceItemPrice'AnyOf
        ///The quantity of the invoice item.
        Quantity: int option
        ///The tax rates which apply to the item. When set, the `default_tax_rates` do not apply to this item.
        TaxRates: TaxRate list option
    }
    with
        static member New (discounts: DiscountsResourceStackableDiscountWithDiscountEnd list, metadata: Map<string, string> option, period: SubscriptionScheduleAddInvoiceItemPeriod, price: SubscriptionScheduleAddInvoiceItemPrice'AnyOf, quantity: int option, ?taxRates: TaxRate list option) =
            {
                SubscriptionScheduleAddInvoiceItem.Discounts = discounts //required
                SubscriptionScheduleAddInvoiceItem.Metadata = metadata //required
                SubscriptionScheduleAddInvoiceItem.Period = period //required
                SubscriptionScheduleAddInvoiceItem.Price = price //required
                SubscriptionScheduleAddInvoiceItem.Quantity = quantity //required
                SubscriptionScheduleAddInvoiceItem.TaxRates = taxRates |> Option.flatten
            }

    and SubscriptionScheduleAddInvoiceItemPrice'AnyOf =
        | String of string
        | Price of Price
        | DeletedPrice of DeletedPrice

    and SubscriptionScheduleAddInvoiceItemPeriod = {
        End: SubscriptionSchedulesResourceInvoiceItemPeriodResourcePeriodEnd
        Start: SubscriptionSchedulesResourceInvoiceItemPeriodResourcePeriodStart
    }
    with
        static member New (``end``: SubscriptionSchedulesResourceInvoiceItemPeriodResourcePeriodEnd, start: SubscriptionSchedulesResourceInvoiceItemPeriodResourcePeriodStart) =
            {
                SubscriptionScheduleAddInvoiceItemPeriod.End = ``end`` //required
                SubscriptionScheduleAddInvoiceItemPeriod.Start = start //required
            }

    ///A phase item describes the price and quantity of a phase.
    and SubscriptionScheduleConfigurationItem = {
        ///Define thresholds at which an invoice will be sent, and the related subscription advanced to a new billing period
        BillingThresholds: SubscriptionItemBillingThresholds option
        ///The discounts applied to the subscription item. Subscription item discounts are applied before subscription discounts. Use `expand[]=discounts` to expand each discount.
        Discounts: StackableDiscountWithDiscountSettings list
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an item. Metadata on this item will update the underlying subscription item's `metadata` when the phase is entered.
        Metadata: Map<string, string> option
        ///ID of the plan to which the customer should be subscribed.
        Plan: SubscriptionScheduleConfigurationItemPlan'AnyOf
        ///ID of the price to which the customer should be subscribed.
        Price: SubscriptionScheduleConfigurationItemPrice'AnyOf
        ///Quantity of the plan to which the customer should be subscribed.
        Quantity: int option
        ///The tax rates which apply to this `phase_item`. When set, the `default_tax_rates` on the phase do not apply to this `phase_item`.
        TaxRates: TaxRate list option
    }
    with
        static member New (billingThresholds: SubscriptionItemBillingThresholds option, discounts: StackableDiscountWithDiscountSettings list, metadata: Map<string, string> option, plan: SubscriptionScheduleConfigurationItemPlan'AnyOf, price: SubscriptionScheduleConfigurationItemPrice'AnyOf, ?quantity: int, ?taxRates: TaxRate list option) =
            {
                SubscriptionScheduleConfigurationItem.BillingThresholds = billingThresholds //required
                SubscriptionScheduleConfigurationItem.Discounts = discounts //required
                SubscriptionScheduleConfigurationItem.Metadata = metadata //required
                SubscriptionScheduleConfigurationItem.Plan = plan //required
                SubscriptionScheduleConfigurationItem.Price = price //required
                SubscriptionScheduleConfigurationItem.Quantity = quantity
                SubscriptionScheduleConfigurationItem.TaxRates = taxRates |> Option.flatten
            }

    and SubscriptionScheduleConfigurationItemPlan'AnyOf =
        | String of string
        | Plan of Plan
        | DeletedPlan of DeletedPlan

    and SubscriptionScheduleConfigurationItemPrice'AnyOf =
        | String of string
        | Price of Price
        | DeletedPrice of DeletedPrice

    ///A phase describes the plans, coupon, and trialing status of a subscription for a predefined time period.
    and SubscriptionSchedulePhaseConfiguration = {
        ///A list of prices and quantities that will generate invoice items appended to the next invoice for this phase.
        AddInvoiceItems: SubscriptionScheduleAddInvoiceItem list
        ///A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account during this phase of the schedule.
        ApplicationFeePercent: decimal option
        AutomaticTax: SchedulesPhaseAutomaticTax option
        ///Possible values are `phase_start` or `automatic`. If `phase_start` then billing cycle anchor of the subscription is set to the start of the phase when entering the phase. If `automatic` then the billing cycle anchor is automatically modified as needed when entering the phase. For more information, see the billing cycle [documentation](https://docs.stripe.com/billing/subscriptions/billing-cycle).
        BillingCycleAnchor: SubscriptionSchedulePhaseConfigurationBillingCycleAnchor option
        ///Define thresholds at which an invoice will be sent, and the subscription advanced to a new billing period
        BillingThresholds: SubscriptionBillingThresholds option
        ///Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay the underlying subscription at the end of each billing cycle using the default source attached to the customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`.
        CollectionMethod: SubscriptionSchedulePhaseConfigurationCollectionMethod option
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///ID of the default payment method for the subscription schedule. It must belong to the customer associated with the subscription schedule. If not set, invoices will use the default payment method in the customer's invoice settings.
        DefaultPaymentMethod: SubscriptionSchedulePhaseConfigurationDefaultPaymentMethod'AnyOf option
        ///The default tax rates to apply to the subscription during this phase of the subscription schedule.
        DefaultTaxRates: TaxRate list option
        ///Subscription description, meant to be displayable to the customer. Use this field to optionally store an explanation of the subscription for rendering in Stripe surfaces and certain local payment methods UIs.
        Description: string option
        ///The stackable discounts that will be applied to the subscription on this phase. Subscription item discounts are applied before subscription discounts.
        Discounts: StackableDiscountWithDiscountSettingsAndDiscountEnd list
        ///The end of this phase of the subscription schedule.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]EndDate: DateTime
        ///The invoice settings applicable during this phase.
        InvoiceSettings: InvoiceSettingSubscriptionSchedulePhaseSetting option
        ///Subscription items to configure the subscription to during this phase of the subscription schedule.
        Items: SubscriptionScheduleConfigurationItem list
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to a phase. Metadata on a schedule's phase will update the underlying subscription's `metadata` when the phase is entered. Updating the underlying subscription's `metadata` directly will not affect the current phase's `metadata`.
        Metadata: Map<string, string> option
        ///The account (if any) the charge was made on behalf of for charges associated with the schedule's subscription. See the Connect documentation for details.
        OnBehalfOf: SubscriptionSchedulePhaseConfigurationOnBehalfOf'AnyOf option
        ///When transitioning phases, controls how prorations are handled (if any). Possible values are `create_prorations`, `none`, and `always_invoice`.
        ProrationBehavior: SubscriptionSchedulePhaseConfigurationProrationBehavior
        ///The start of this phase of the subscription schedule.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]StartDate: DateTime
        ///The account (if any) the associated subscription's payments will be attributed to for tax reporting, and where funds from each payment will be transferred to for each of the subscription's invoices.
        TransferData: SubscriptionTransferData option
        ///When the trial ends within the phase.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]TrialEnd: DateTime option
    }
    with
        static member New (addInvoiceItems: SubscriptionScheduleAddInvoiceItem list, applicationFeePercent: decimal option, billingCycleAnchor: SubscriptionSchedulePhaseConfigurationBillingCycleAnchor option, billingThresholds: SubscriptionBillingThresholds option, collectionMethod: SubscriptionSchedulePhaseConfigurationCollectionMethod option, currency: IsoTypes.IsoCurrencyCode, defaultPaymentMethod: SubscriptionSchedulePhaseConfigurationDefaultPaymentMethod'AnyOf option, description: string option, discounts: StackableDiscountWithDiscountSettingsAndDiscountEnd list, endDate: DateTime, invoiceSettings: InvoiceSettingSubscriptionSchedulePhaseSetting option, items: SubscriptionScheduleConfigurationItem list, metadata: Map<string, string> option, onBehalfOf: SubscriptionSchedulePhaseConfigurationOnBehalfOf'AnyOf option, prorationBehavior: SubscriptionSchedulePhaseConfigurationProrationBehavior, startDate: DateTime, transferData: SubscriptionTransferData option, trialEnd: DateTime option, ?automaticTax: SchedulesPhaseAutomaticTax, ?defaultTaxRates: TaxRate list option) =
            {
                SubscriptionSchedulePhaseConfiguration.AddInvoiceItems = addInvoiceItems //required
                SubscriptionSchedulePhaseConfiguration.ApplicationFeePercent = applicationFeePercent //required
                SubscriptionSchedulePhaseConfiguration.BillingCycleAnchor = billingCycleAnchor //required
                SubscriptionSchedulePhaseConfiguration.BillingThresholds = billingThresholds //required
                SubscriptionSchedulePhaseConfiguration.CollectionMethod = collectionMethod //required
                SubscriptionSchedulePhaseConfiguration.Currency = currency //required
                SubscriptionSchedulePhaseConfiguration.DefaultPaymentMethod = defaultPaymentMethod //required
                SubscriptionSchedulePhaseConfiguration.Description = description //required
                SubscriptionSchedulePhaseConfiguration.Discounts = discounts //required
                SubscriptionSchedulePhaseConfiguration.EndDate = endDate //required
                SubscriptionSchedulePhaseConfiguration.InvoiceSettings = invoiceSettings //required
                SubscriptionSchedulePhaseConfiguration.Items = items //required
                SubscriptionSchedulePhaseConfiguration.Metadata = metadata //required
                SubscriptionSchedulePhaseConfiguration.OnBehalfOf = onBehalfOf //required
                SubscriptionSchedulePhaseConfiguration.ProrationBehavior = prorationBehavior //required
                SubscriptionSchedulePhaseConfiguration.StartDate = startDate //required
                SubscriptionSchedulePhaseConfiguration.TransferData = transferData //required
                SubscriptionSchedulePhaseConfiguration.TrialEnd = trialEnd //required
                SubscriptionSchedulePhaseConfiguration.AutomaticTax = automaticTax
                SubscriptionSchedulePhaseConfiguration.DefaultTaxRates = defaultTaxRates |> Option.flatten
            }

    and SubscriptionSchedulePhaseConfigurationDefaultPaymentMethod'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and SubscriptionSchedulePhaseConfigurationOnBehalfOf'AnyOf =
        | String of string
        | Account of Account

    and SubscriptionSchedulesResourceDefaultSettings = {
        ///A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account during this phase of the schedule.
        ApplicationFeePercent: decimal option
        AutomaticTax: SubscriptionSchedulesResourceDefaultSettingsAutomaticTax option
        ///Possible values are `phase_start` or `automatic`. If `phase_start` then billing cycle anchor of the subscription is set to the start of the phase when entering the phase. If `automatic` then the billing cycle anchor is automatically modified as needed when entering the phase. For more information, see the billing cycle [documentation](https://docs.stripe.com/billing/subscriptions/billing-cycle).
        BillingCycleAnchor: SubscriptionSchedulesResourceDefaultSettingsBillingCycleAnchor
        ///Define thresholds at which an invoice will be sent, and the subscription advanced to a new billing period
        BillingThresholds: SubscriptionBillingThresholds option
        ///Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay the underlying subscription at the end of each billing cycle using the default source attached to the customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`.
        CollectionMethod: SubscriptionSchedulesResourceDefaultSettingsCollectionMethod option
        ///ID of the default payment method for the subscription schedule. If not set, invoices will use the default payment method in the customer's invoice settings.
        DefaultPaymentMethod: SubscriptionSchedulesResourceDefaultSettingsDefaultPaymentMethod'AnyOf option
        ///Subscription description, meant to be displayable to the customer. Use this field to optionally store an explanation of the subscription for rendering in Stripe surfaces and certain local payment methods UIs.
        Description: string option
        InvoiceSettings: InvoiceSettingSubscriptionScheduleSetting
        ///The account (if any) the charge was made on behalf of for charges associated with the schedule's subscription. See the Connect documentation for details.
        OnBehalfOf: SubscriptionSchedulesResourceDefaultSettingsOnBehalfOf'AnyOf option
        ///The account (if any) the associated subscription's payments will be attributed to for tax reporting, and where funds from each payment will be transferred to for each of the subscription's invoices.
        TransferData: SubscriptionTransferData option
    }
    with
        static member New (applicationFeePercent: decimal option, billingCycleAnchor: SubscriptionSchedulesResourceDefaultSettingsBillingCycleAnchor, billingThresholds: SubscriptionBillingThresholds option, collectionMethod: SubscriptionSchedulesResourceDefaultSettingsCollectionMethod option, defaultPaymentMethod: SubscriptionSchedulesResourceDefaultSettingsDefaultPaymentMethod'AnyOf option, description: string option, invoiceSettings: InvoiceSettingSubscriptionScheduleSetting, onBehalfOf: SubscriptionSchedulesResourceDefaultSettingsOnBehalfOf'AnyOf option, transferData: SubscriptionTransferData option, ?automaticTax: SubscriptionSchedulesResourceDefaultSettingsAutomaticTax) =
            {
                SubscriptionSchedulesResourceDefaultSettings.ApplicationFeePercent = applicationFeePercent //required
                SubscriptionSchedulesResourceDefaultSettings.BillingCycleAnchor = billingCycleAnchor //required
                SubscriptionSchedulesResourceDefaultSettings.BillingThresholds = billingThresholds //required
                SubscriptionSchedulesResourceDefaultSettings.CollectionMethod = collectionMethod //required
                SubscriptionSchedulesResourceDefaultSettings.DefaultPaymentMethod = defaultPaymentMethod //required
                SubscriptionSchedulesResourceDefaultSettings.Description = description //required
                SubscriptionSchedulesResourceDefaultSettings.InvoiceSettings = invoiceSettings //required
                SubscriptionSchedulesResourceDefaultSettings.OnBehalfOf = onBehalfOf //required
                SubscriptionSchedulesResourceDefaultSettings.TransferData = transferData //required
                SubscriptionSchedulesResourceDefaultSettings.AutomaticTax = automaticTax
            }

    and SubscriptionSchedulesResourceDefaultSettingsDefaultPaymentMethod'AnyOf =
        | String of string
        | PaymentMethod of PaymentMethod

    and SubscriptionSchedulesResourceDefaultSettingsOnBehalfOf'AnyOf =
        | String of string
        | Account of Account

    and SubscriptionSchedulesResourceDefaultSettingsAutomaticTax = {
        ///Whether Stripe automatically computes tax on invoices created during this phase.
        Enabled: bool
        ///The account that's liable for tax. If set, the business address and tax registrations required to perform the tax calculation are loaded from this account. The tax transaction is returned in the report of the connected account.
        Liability: ConnectAccountReference option
    }
    with
        ///If Stripe disabled automatic tax, this enum describes why.
        member _.DisabledReason = "requires_location_inputs"

        static member New (enabled: bool, liability: ConnectAccountReference option) =
            {
                SubscriptionSchedulesResourceDefaultSettingsAutomaticTax.Enabled = enabled //required
                SubscriptionSchedulesResourceDefaultSettingsAutomaticTax.Liability = liability //required
            }

    and SubscriptionSchedulesResourceInvoiceItemPeriodResourcePeriodEnd = {
        ///A precise Unix timestamp for the end of the invoice item period. Must be greater than or equal to `period.start`.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Timestamp: DateTime option
        ///Select how to calculate the end of the invoice item period.
        Type: SubscriptionSchedulesResourceInvoiceItemPeriodResourcePeriodEndType
    }
    with
        static member New (``type``: SubscriptionSchedulesResourceInvoiceItemPeriodResourcePeriodEndType, ?timestamp: DateTime) =
            {
                SubscriptionSchedulesResourceInvoiceItemPeriodResourcePeriodEnd.Type = ``type`` //required
                SubscriptionSchedulesResourceInvoiceItemPeriodResourcePeriodEnd.Timestamp = timestamp
            }

    and SubscriptionSchedulesResourceInvoiceItemPeriodResourcePeriodStart = {
        ///A precise Unix timestamp for the start of the invoice item period. Must be less than or equal to `period.end`.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Timestamp: DateTime option
        ///Select how to calculate the start of the invoice item period.
        Type: SubscriptionSchedulesResourceInvoiceItemPeriodResourcePeriodStartType
    }
    with
        static member New (``type``: SubscriptionSchedulesResourceInvoiceItemPeriodResourcePeriodStartType, ?timestamp: DateTime) =
            {
                SubscriptionSchedulesResourceInvoiceItemPeriodResourcePeriodStart.Type = ``type`` //required
                SubscriptionSchedulesResourceInvoiceItemPeriodResourcePeriodStart.Timestamp = timestamp
            }

    and SubscriptionTransferData = {
        ///A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the destination account. By default, the entire amount is transferred to the destination.
        AmountPercent: decimal option
        ///The account where funds from the payment will be transferred to upon payment success.
        Destination: SubscriptionTransferDataDestination'AnyOf
    }
    with
        static member New (amountPercent: decimal option, destination: SubscriptionTransferDataDestination'AnyOf) =
            {
                SubscriptionTransferData.AmountPercent = amountPercent //required
                SubscriptionTransferData.Destination = destination //required
            }

    and SubscriptionTransferDataDestination'AnyOf =
        | String of string
        | Account of Account

    ///The billing mode of the subscription.
    and SubscriptionsResourceBillingMode = {
        ///Configure behavior for flexible billing mode
        Flexible: SubscriptionsResourceBillingModeFlexible option
        ///Controls how prorations and invoices for subscriptions are calculated and orchestrated.
        Type: SubscriptionsResourceBillingModeType
        ///Details on when the current billing_mode was adopted.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]UpdatedAt: DateTime option
    }
    with
        static member New (flexible: SubscriptionsResourceBillingModeFlexible option, ``type``: SubscriptionsResourceBillingModeType, ?updatedAt: DateTime) =
            {
                SubscriptionsResourceBillingMode.Flexible = flexible //required
                SubscriptionsResourceBillingMode.Type = ``type`` //required
                SubscriptionsResourceBillingMode.UpdatedAt = updatedAt
            }

    and SubscriptionsResourceBillingModeFlexible = {
        ///Controls how invoices and invoice items display proration amounts and discount amounts.
        ProrationDiscounts: SubscriptionsResourceBillingModeFlexibleProrationDiscounts option
    }
    with
        static member New (?prorationDiscounts: SubscriptionsResourceBillingModeFlexibleProrationDiscounts) =
            {
                SubscriptionsResourceBillingModeFlexible.ProrationDiscounts = prorationDiscounts
            }

    ///The Pause Collection settings determine how we will pause collection for this subscription and for how long the subscription
    ///should be paused.
    and SubscriptionsResourcePauseCollection = {
        ///The payment collection behavior for this subscription while paused.
        Behavior: SubscriptionsResourcePauseCollectionBehavior
        ///The time after which the subscription will resume collecting payments.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ResumesAt: DateTime option
    }
    with
        static member New (behavior: SubscriptionsResourcePauseCollectionBehavior, resumesAt: DateTime option) =
            {
                SubscriptionsResourcePauseCollection.Behavior = behavior //required
                SubscriptionsResourcePauseCollection.ResumesAt = resumesAt //required
            }

    and SubscriptionsResourcePaymentMethodOptions = {
        ///This sub-hash contains details about the Canadian pre-authorized debit payment method options to pass to invoices created by the subscription.
        AcssDebit: InvoicePaymentMethodOptionsAcssDebit option
        ///This sub-hash contains details about the Bancontact payment method options to pass to invoices created by the subscription.
        Bancontact: InvoicePaymentMethodOptionsBancontact option
        ///This sub-hash contains details about the Card payment method options to pass to invoices created by the subscription.
        Card: SubscriptionPaymentMethodOptionsCard option
        ///This sub-hash contains details about the Bank transfer payment method options to pass to invoices created by the subscription.
        CustomerBalance: InvoicePaymentMethodOptionsCustomerBalance option
        ///This sub-hash contains details about the Konbini payment method options to pass to invoices created by the subscription.
        Konbini: InvoicePaymentMethodOptionsKonbini option
        ///This sub-hash contains details about the PayTo payment method options to pass to invoices created by the subscription.
        Payto: InvoicePaymentMethodOptionsPayto option
        ///This sub-hash contains details about the Pix payment method options to pass to invoices created by the subscription.
        Pix: SubscriptionPaymentMethodOptionsPix option
        ///This sub-hash contains details about the SEPA Direct Debit payment method options to pass to invoices created by the subscription.
        SepaDebit: InvoicePaymentMethodOptionsSepaDebit option
        ///This sub-hash contains details about the UPI payment method options to pass to invoices created by the subscription.
        Upi: InvoicePaymentMethodOptionsUpi option
        ///This sub-hash contains details about the ACH direct debit payment method options to pass to invoices created by the subscription.
        UsBankAccount: InvoicePaymentMethodOptionsUsBankAccount option
    }
    with
        static member New (acssDebit: InvoicePaymentMethodOptionsAcssDebit option, bancontact: InvoicePaymentMethodOptionsBancontact option, card: SubscriptionPaymentMethodOptionsCard option, customerBalance: InvoicePaymentMethodOptionsCustomerBalance option, konbini: InvoicePaymentMethodOptionsKonbini option, payto: InvoicePaymentMethodOptionsPayto option, pix: SubscriptionPaymentMethodOptionsPix option, sepaDebit: InvoicePaymentMethodOptionsSepaDebit option, upi: InvoicePaymentMethodOptionsUpi option, usBankAccount: InvoicePaymentMethodOptionsUsBankAccount option) =
            {
                SubscriptionsResourcePaymentMethodOptions.AcssDebit = acssDebit //required
                SubscriptionsResourcePaymentMethodOptions.Bancontact = bancontact //required
                SubscriptionsResourcePaymentMethodOptions.Card = card //required
                SubscriptionsResourcePaymentMethodOptions.CustomerBalance = customerBalance //required
                SubscriptionsResourcePaymentMethodOptions.Konbini = konbini //required
                SubscriptionsResourcePaymentMethodOptions.Payto = payto //required
                SubscriptionsResourcePaymentMethodOptions.Pix = pix //required
                SubscriptionsResourcePaymentMethodOptions.SepaDebit = sepaDebit //required
                SubscriptionsResourcePaymentMethodOptions.Upi = upi //required
                SubscriptionsResourcePaymentMethodOptions.UsBankAccount = usBankAccount //required
            }

    and SubscriptionsResourcePaymentSettings = {
        ///Payment-method-specific configuration to provide to invoices created by the subscription.
        PaymentMethodOptions: SubscriptionsResourcePaymentMethodOptions option
        ///The list of payment method types to provide to every invoice created by the subscription. If not set, Stripe attempts to automatically determine the types to use by looking at the invoice’s default payment method, the subscription’s default payment method, the customer’s default payment method, and your [invoice template settings](https://dashboard.stripe.com/settings/billing/invoice).
        PaymentMethodTypes: SubscriptionsResourcePaymentSettingsPaymentMethodTypes list option
        ///Configure whether Stripe updates `subscription.default_payment_method` when payment succeeds. Defaults to `off`.
        SaveDefaultPaymentMethod: SubscriptionsResourcePaymentSettingsSaveDefaultPaymentMethod option
    }
    with
        static member New (paymentMethodOptions: SubscriptionsResourcePaymentMethodOptions option, paymentMethodTypes: SubscriptionsResourcePaymentSettingsPaymentMethodTypes list option, saveDefaultPaymentMethod: SubscriptionsResourcePaymentSettingsSaveDefaultPaymentMethod option) =
            {
                SubscriptionsResourcePaymentSettings.PaymentMethodOptions = paymentMethodOptions //required
                SubscriptionsResourcePaymentSettings.PaymentMethodTypes = paymentMethodTypes //required
                SubscriptionsResourcePaymentSettings.SaveDefaultPaymentMethod = saveDefaultPaymentMethod //required
            }

    ///Pending Updates store the changes pending from a previous update that will be applied
    ///to the Subscription upon successful payment.
    and SubscriptionsResourcePendingUpdate = {
        ///If the update is applied, determines the date of the first full invoice, and, for plans with `month` or `year` intervals, the day of the month for subsequent invoices. The timestamp is in UTC format.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]BillingCycleAnchor: DateTime option
        ///The point after which the changes reflected by this update will be discarded and no longer applied.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime
        ///List of subscription items, each with an attached plan, that will be set if the update is applied.
        SubscriptionItems: SubscriptionItem list option
        ///Unix timestamp representing the end of the trial period the customer will get before being charged for the first time, if the update is applied.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]TrialEnd: DateTime option
        ///Indicates if a plan's `trial_period_days` should be applied to the subscription. Setting `trial_end` per subscription is preferred, and this defaults to `false`. Setting this flag to `true` together with `trial_end` is not allowed. See [Using trial periods on subscriptions](https://docs.stripe.com/billing/subscriptions/trials) to learn more.
        TrialFromPlan: bool option
    }
    with
        static member New (billingCycleAnchor: DateTime option, expiresAt: DateTime, subscriptionItems: SubscriptionItem list option, trialEnd: DateTime option, trialFromPlan: bool option) =
            {
                SubscriptionsResourcePendingUpdate.BillingCycleAnchor = billingCycleAnchor //required
                SubscriptionsResourcePendingUpdate.ExpiresAt = expiresAt //required
                SubscriptionsResourcePendingUpdate.SubscriptionItems = subscriptionItems //required
                SubscriptionsResourcePendingUpdate.TrialEnd = trialEnd //required
                SubscriptionsResourcePendingUpdate.TrialFromPlan = trialFromPlan //required
            }

    and SubscriptionsResourceSubscriptionInvoiceSettings = {
        ///The account tax IDs associated with the subscription. Will be set on invoices generated by the subscription.
        AccountTaxIds: SubscriptionsResourceSubscriptionInvoiceSettingsAccountTaxIds'AnyOf list option
        Issuer: ConnectAccountReference
    }
    with
        static member New (accountTaxIds: SubscriptionsResourceSubscriptionInvoiceSettingsAccountTaxIds'AnyOf list option, issuer: ConnectAccountReference) =
            {
                SubscriptionsResourceSubscriptionInvoiceSettings.AccountTaxIds = accountTaxIds //required
                SubscriptionsResourceSubscriptionInvoiceSettings.Issuer = issuer //required
            }

    and SubscriptionsResourceSubscriptionInvoiceSettingsAccountTaxIds'AnyOf =
        | String of string
        | TaxId of TaxId
        | DeletedTaxId of DeletedTaxId

    and SubscriptionsResourceSubscriptionPresentmentDetails = {
        ///Currency used for customer payments.
        PresentmentCurrency: IsoTypes.IsoCurrencyCode
    }
    with
        static member New (presentmentCurrency: IsoTypes.IsoCurrencyCode) =
            {
                SubscriptionsResourceSubscriptionPresentmentDetails.PresentmentCurrency = presentmentCurrency //required
            }

    ///Defines how a subscription behaves when a trial ends.
    and SubscriptionsResourceTrialSettingsEndBehavior = {
        ///Indicates how the subscription should change when the trial ends if the user did not provide a payment method.
        MissingPaymentMethod: SubscriptionsResourceTrialSettingsEndBehaviorMissingPaymentMethod
    }
    with
        static member New (missingPaymentMethod: SubscriptionsResourceTrialSettingsEndBehaviorMissingPaymentMethod) =
            {
                SubscriptionsResourceTrialSettingsEndBehavior.MissingPaymentMethod = missingPaymentMethod //required
            }

    ///Configures how this subscription behaves during the trial period.
    and SubscriptionsResourceTrialSettingsTrialSettings = {
        EndBehavior: SubscriptionsResourceTrialSettingsEndBehavior
    }
    with
        static member New (endBehavior: SubscriptionsResourceTrialSettingsEndBehavior) =
            {
                SubscriptionsResourceTrialSettingsTrialSettings.EndBehavior = endBehavior //required
            }

    ///Defines how a subscription behaves when a free trial ends.
    and SubscriptionsTrialsResourceEndBehavior = {
        ///Indicates how the subscription should change when the trial ends if the user did not provide a payment method.
        MissingPaymentMethod: SubscriptionsTrialsResourceEndBehaviorMissingPaymentMethod
    }
    with
        static member New (missingPaymentMethod: SubscriptionsTrialsResourceEndBehaviorMissingPaymentMethod) =
            {
                SubscriptionsTrialsResourceEndBehavior.MissingPaymentMethod = missingPaymentMethod //required
            }

    ///Configures how this subscription behaves during the trial period.
    and SubscriptionsTrialsResourceTrialSettings = {
        EndBehavior: SubscriptionsTrialsResourceEndBehavior
    }
    with
        static member New (endBehavior: SubscriptionsTrialsResourceEndBehavior) =
            {
                SubscriptionsTrialsResourceTrialSettings.EndBehavior = endBehavior //required
            }

    ///A Tax Association exposes the Tax Transactions that Stripe attempted to create on your behalf based on the PaymentIntent input
    and TaxAssociation = {
        ///The [Tax Calculation](https://docs.stripe.com/api/tax/calculations/object) that was included in PaymentIntent.
        Calculation: string
        ///Unique identifier for the object.
        Id: string
        ///The [PaymentIntent](https://docs.stripe.com/api/payment_intents/object) that this Tax Association is tracking.
        PaymentIntent: string
        ///Information about the tax transactions linked to this payment intent
        TaxTransactionAttempts: TaxProductResourceTaxAssociationTransactionAttempts list option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "tax.association"

        static member New (calculation: string, id: string, paymentIntent: string, taxTransactionAttempts: TaxProductResourceTaxAssociationTransactionAttempts list option) =
            {
                TaxAssociation.Calculation = calculation //required
                TaxAssociation.Id = id //required
                TaxAssociation.PaymentIntent = paymentIntent //required
                TaxAssociation.TaxTransactionAttempts = taxTransactionAttempts //required
            }

    ///A Tax Calculation allows you to calculate the tax to collect from your customer.
    ///Related guide: [Calculate tax in your custom payment flow](https://docs.stripe.com/tax/custom)
    and TaxCalculation = {
        ///Total amount after taxes in the [smallest currency unit](https://docs.stripe.com/currencies#minor-units).
        AmountTotal: int
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///The ID of an existing [Customer](https://docs.stripe.com/api/customers/object) used for the resource.
        Customer: string option
        CustomerDetails: TaxProductResourceCustomerDetails
        ///Timestamp of date at which the tax calculation will expire.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime option
        ///Unique identifier for the calculation.
        Id: string option
        ///The list of items the customer is purchasing.
        LineItems: TaxCalculationLineItems option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The details of the ship from location, such as the address.
        ShipFromDetails: TaxProductResourceShipFromDetails option
        ///The shipping cost details for the calculation.
        ShippingCost: TaxProductResourceTaxCalculationShippingCost option
        ///The amount of tax to be collected on top of the line item prices.
        TaxAmountExclusive: int
        ///The amount of tax already included in the line item prices.
        TaxAmountInclusive: int
        ///Breakdown of individual tax amounts that add up to the total.
        TaxBreakdown: TaxProductResourceTaxBreakdown list
        ///The calculation uses the tax rules and rates that are in effect at this timestamp. You can use a date up to 31 days in the past or up to 31 days in the future. If you use a future date, Stripe doesn't guarantee that the expected tax rules and rate being used match the actual rules and rate that will be in effect on that date. We deploy tax changes before their effective date, but not within a fixed window.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]TaxDate: DateTime
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "tax.calculation"

        static member New (amountTotal: int, currency: IsoTypes.IsoCurrencyCode, customer: string option, customerDetails: TaxProductResourceCustomerDetails, expiresAt: DateTime option, id: string option, livemode: bool, shipFromDetails: TaxProductResourceShipFromDetails option, shippingCost: TaxProductResourceTaxCalculationShippingCost option, taxAmountExclusive: int, taxAmountInclusive: int, taxBreakdown: TaxProductResourceTaxBreakdown list, taxDate: DateTime, ?lineItems: TaxCalculationLineItems option) =
            {
                TaxCalculation.AmountTotal = amountTotal //required
                TaxCalculation.Currency = currency //required
                TaxCalculation.Customer = customer //required
                TaxCalculation.CustomerDetails = customerDetails //required
                TaxCalculation.ExpiresAt = expiresAt //required
                TaxCalculation.Id = id //required
                TaxCalculation.Livemode = livemode //required
                TaxCalculation.ShipFromDetails = shipFromDetails //required
                TaxCalculation.ShippingCost = shippingCost //required
                TaxCalculation.TaxAmountExclusive = taxAmountExclusive //required
                TaxCalculation.TaxAmountInclusive = taxAmountInclusive //required
                TaxCalculation.TaxBreakdown = taxBreakdown //required
                TaxCalculation.TaxDate = taxDate //required
                TaxCalculation.LineItems = lineItems |> Option.flatten
            }

    ///The list of items the customer is purchasing.
    and TaxCalculationLineItems = {
        ///Details about each object.
        Data: TaxCalculationLineItem list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: TaxCalculationLineItem list, hasMore: bool, url: string) =
            {
                TaxCalculationLineItems.Data = data //required
                TaxCalculationLineItems.HasMore = hasMore //required
                TaxCalculationLineItems.Url = url //required
            }

    and TaxCalculationLineItem = {
        ///The line item amount in the [smallest currency unit](https://docs.stripe.com/currencies#minor-units). If `tax_behavior=inclusive`, then this amount includes taxes. Otherwise, taxes were calculated on top of this amount.
        Amount: int
        ///The amount of tax calculated for this line item, in the [smallest currency unit](https://docs.stripe.com/currencies#minor-units).
        AmountTax: int
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///The ID of an existing [Product](https://docs.stripe.com/api/products/object).
        Product: string option
        ///The number of units of the item being purchased. For reversals, this is the quantity reversed.
        Quantity: int
        ///A custom identifier for this line item.
        Reference: string
        ///Specifies whether the `amount` includes taxes. If `tax_behavior=inclusive`, then the amount includes taxes.
        TaxBehavior: TaxCalculationLineItemTaxBehavior
        ///Detailed account of taxes relevant to this line item.
        TaxBreakdown: TaxProductResourceLineItemTaxBreakdown list option
        ///The [tax code](https://docs.stripe.com/tax/tax-categories) ID used for this resource.
        TaxCode: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "tax.calculation_line_item"

        static member New (amount: int, amountTax: int, id: string, livemode: bool, metadata: Map<string, string> option, product: string option, quantity: int, reference: string, taxBehavior: TaxCalculationLineItemTaxBehavior, taxCode: string, ?taxBreakdown: TaxProductResourceLineItemTaxBreakdown list option) =
            {
                TaxCalculationLineItem.Amount = amount //required
                TaxCalculationLineItem.AmountTax = amountTax //required
                TaxCalculationLineItem.Id = id //required
                TaxCalculationLineItem.Livemode = livemode //required
                TaxCalculationLineItem.Metadata = metadata //required
                TaxCalculationLineItem.Product = product //required
                TaxCalculationLineItem.Quantity = quantity //required
                TaxCalculationLineItem.Reference = reference //required
                TaxCalculationLineItem.TaxBehavior = taxBehavior //required
                TaxCalculationLineItem.TaxCode = taxCode //required
                TaxCalculationLineItem.TaxBreakdown = taxBreakdown |> Option.flatten
            }

    ///A Tax `Registration` lets us know that your business is registered to collect tax on payments within a region, enabling you to [automatically collect tax](https://docs.stripe.com/tax).
    ///Stripe doesn't register on your behalf with the relevant authorities when you create a Tax `Registration` object. For more information on how to register to collect tax, see [our guide](https://docs.stripe.com/tax/registering).
    ///Related guide: [Using the Registrations API](https://docs.stripe.com/tax/registrations-api)
    and TaxRegistration = {
        ///Time at which the registration becomes active. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ActiveFrom: DateTime
        ///Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
        Country: IsoTypes.IsoCountryCode
        CountryOptions: TaxProductRegistrationsResourceCountryOptions
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///If set, the registration stops being active at this time. If not set, the registration will be active indefinitely. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpiresAt: DateTime option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The status of the registration. This field is present for convenience and can be deduced from `active_from` and `expires_at`.
        Status: TaxRegistrationStatus
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "tax.registration"

        static member New (activeFrom: DateTime, country: IsoTypes.IsoCountryCode, countryOptions: TaxProductRegistrationsResourceCountryOptions, created: DateTime, expiresAt: DateTime option, id: string, livemode: bool, status: TaxRegistrationStatus) =
            {
                TaxRegistration.ActiveFrom = activeFrom //required
                TaxRegistration.Country = country //required
                TaxRegistration.CountryOptions = countryOptions //required
                TaxRegistration.Created = created //required
                TaxRegistration.ExpiresAt = expiresAt //required
                TaxRegistration.Id = id //required
                TaxRegistration.Livemode = livemode //required
                TaxRegistration.Status = status //required
            }

    ///You can use Tax `Settings` to manage configurations used by Stripe Tax calculations.
    ///Related guide: [Using the Settings API](https://docs.stripe.com/tax/settings-api)
    and TaxSettings = {
        Defaults: TaxProductResourceTaxSettingsDefaults
        ///The place where your business is located.
        HeadOffice: TaxProductResourceTaxSettingsHeadOffice option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The status of the Tax `Settings`.
        Status: TaxSettingsStatus
        StatusDetails: TaxProductResourceTaxSettingsStatusDetails
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "tax.settings"

        static member New (defaults: TaxProductResourceTaxSettingsDefaults, headOffice: TaxProductResourceTaxSettingsHeadOffice option, livemode: bool, status: TaxSettingsStatus, statusDetails: TaxProductResourceTaxSettingsStatusDetails) =
            {
                TaxSettings.Defaults = defaults //required
                TaxSettings.HeadOffice = headOffice //required
                TaxSettings.Livemode = livemode //required
                TaxSettings.Status = status //required
                TaxSettings.StatusDetails = statusDetails //required
            }

    ///A Tax Transaction records the tax collected from or refunded to your customer.
    ///Related guide: [Calculate tax in your custom payment flow](https://docs.stripe.com/tax/custom#tax-transaction)
    and TaxTransaction = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///The ID of an existing [Customer](https://docs.stripe.com/api/customers/object) used for the resource.
        Customer: string option
        CustomerDetails: TaxProductResourceCustomerDetails
        ///Unique identifier for the transaction.
        Id: string
        ///The tax collected or refunded, by line item.
        LineItems: TaxTransactionLineItems option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///The Unix timestamp representing when the tax liability is assumed or reduced.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]PostedAt: DateTime
        ///A custom unique identifier, such as 'myOrder_123'.
        Reference: string
        ///If `type=reversal`, contains information about what was reversed.
        Reversal: TaxProductResourceTaxTransactionResourceReversal option
        ///The details of the ship from location, such as the address.
        ShipFromDetails: TaxProductResourceShipFromDetails option
        ///The shipping cost details for the transaction.
        ShippingCost: TaxProductResourceTaxTransactionShippingCost option
        ///The calculation uses the tax rules and rates that are in effect at this timestamp. You can use a date up to 31 days in the past or up to 31 days in the future. If you use a future date, Stripe doesn't guarantee that the expected tax rules and rate being used match the actual rules and rate that will be in effect on that date. We deploy tax changes before their effective date, but not within a fixed window.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]TaxDate: DateTime
        ///If `reversal`, this transaction reverses an earlier transaction.
        Type: TaxTransactionType
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "tax.transaction"

        static member New (created: DateTime, currency: IsoTypes.IsoCurrencyCode, customer: string option, customerDetails: TaxProductResourceCustomerDetails, id: string, livemode: bool, metadata: Map<string, string> option, postedAt: DateTime, reference: string, reversal: TaxProductResourceTaxTransactionResourceReversal option, shipFromDetails: TaxProductResourceShipFromDetails option, shippingCost: TaxProductResourceTaxTransactionShippingCost option, taxDate: DateTime, ``type``: TaxTransactionType, ?lineItems: TaxTransactionLineItems option) =
            {
                TaxTransaction.Created = created //required
                TaxTransaction.Currency = currency //required
                TaxTransaction.Customer = customer //required
                TaxTransaction.CustomerDetails = customerDetails //required
                TaxTransaction.Id = id //required
                TaxTransaction.Livemode = livemode //required
                TaxTransaction.Metadata = metadata //required
                TaxTransaction.PostedAt = postedAt //required
                TaxTransaction.Reference = reference //required
                TaxTransaction.Reversal = reversal //required
                TaxTransaction.ShipFromDetails = shipFromDetails //required
                TaxTransaction.ShippingCost = shippingCost //required
                TaxTransaction.TaxDate = taxDate //required
                TaxTransaction.Type = ``type`` //required
                TaxTransaction.LineItems = lineItems |> Option.flatten
            }

    ///The tax collected or refunded, by line item.
    and TaxTransactionLineItems = {
        ///Details about each object.
        Data: TaxTransactionLineItem list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: TaxTransactionLineItem list, hasMore: bool, url: string) =
            {
                TaxTransactionLineItems.Data = data //required
                TaxTransactionLineItems.HasMore = hasMore //required
                TaxTransactionLineItems.Url = url //required
            }

    and TaxTransactionLineItem = {
        ///The line item amount in the [smallest currency unit](https://docs.stripe.com/currencies#minor-units). If `tax_behavior=inclusive`, then this amount includes taxes. Otherwise, taxes were calculated on top of this amount.
        Amount: int
        ///The amount of tax calculated for this line item, in the [smallest currency unit](https://docs.stripe.com/currencies#minor-units).
        AmountTax: int
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///The ID of an existing [Product](https://docs.stripe.com/api/products/object).
        Product: string option
        ///The number of units of the item being purchased. For reversals, this is the quantity reversed.
        Quantity: int
        ///A custom identifier for this line item in the transaction.
        Reference: string
        ///If `type=reversal`, contains information about what was reversed.
        Reversal: TaxProductResourceTaxTransactionLineItemResourceReversal option
        ///Specifies whether the `amount` includes taxes. If `tax_behavior=inclusive`, then the amount includes taxes.
        TaxBehavior: TaxTransactionLineItemTaxBehavior
        ///The [tax code](https://docs.stripe.com/tax/tax-categories) ID used for this resource.
        TaxCode: string
        ///If `reversal`, this line item reverses an earlier transaction.
        Type: TaxTransactionLineItemType
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "tax.transaction_line_item"

        static member New (amount: int, amountTax: int, id: string, livemode: bool, metadata: Map<string, string> option, product: string option, quantity: int, reference: string, reversal: TaxProductResourceTaxTransactionLineItemResourceReversal option, taxBehavior: TaxTransactionLineItemTaxBehavior, taxCode: string, ``type``: TaxTransactionLineItemType) =
            {
                TaxTransactionLineItem.Amount = amount //required
                TaxTransactionLineItem.AmountTax = amountTax //required
                TaxTransactionLineItem.Id = id //required
                TaxTransactionLineItem.Livemode = livemode //required
                TaxTransactionLineItem.Metadata = metadata //required
                TaxTransactionLineItem.Product = product //required
                TaxTransactionLineItem.Quantity = quantity //required
                TaxTransactionLineItem.Reference = reference //required
                TaxTransactionLineItem.Reversal = reversal //required
                TaxTransactionLineItem.TaxBehavior = taxBehavior //required
                TaxTransactionLineItem.TaxCode = taxCode //required
                TaxTransactionLineItem.Type = ``type`` //required
            }

    and TaxIDsOwner = {
        ///The account being referenced when `type` is `account`.
        Account: TaxIDsOwnerAccount'AnyOf option
        ///The Connect Application being referenced when `type` is `application`.
        Application: TaxIDsOwnerApplication'AnyOf option
        ///The customer being referenced when `type` is `customer`.
        Customer: TaxIDsOwnerCustomer'AnyOf option
        ///The Account representing the customer being referenced when `type` is `customer`.
        CustomerAccount: string option
        ///Type of owner referenced.
        Type: TaxIDsOwnerType
    }
    with
        static member New (customerAccount: string option, ``type``: TaxIDsOwnerType, ?account: TaxIDsOwnerAccount'AnyOf, ?application: TaxIDsOwnerApplication'AnyOf, ?customer: TaxIDsOwnerCustomer'AnyOf) =
            {
                TaxIDsOwner.CustomerAccount = customerAccount //required
                TaxIDsOwner.Type = ``type`` //required
                TaxIDsOwner.Account = account
                TaxIDsOwner.Application = application
                TaxIDsOwner.Customer = customer
            }

    and TaxIDsOwnerAccount'AnyOf =
        | String of string
        | Account of Account

    and TaxIDsOwnerApplication'AnyOf =
        | String of string
        | Application of Application

    and TaxIDsOwnerCustomer'AnyOf =
        | String of string
        | Customer of Customer

    ///You can add one or multiple tax IDs to a [customer](https://docs.stripe.com/api/customers) or account.
    ///Customer and account tax IDs get displayed on related invoices and credit notes.
    ///Related guides: [Customer tax identification numbers](https://docs.stripe.com/billing/taxes/tax-ids), [Account tax IDs](https://docs.stripe.com/invoicing/connect#account-tax-ids)
    and TaxId = {
        ///Two-letter ISO code representing the country of the tax ID.
        Country: IsoTypes.IsoCountryCode option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///ID of the customer.
        Customer: TaxIdCustomer'AnyOf option
        ///ID of the Account representing the customer.
        CustomerAccount: string option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The account or customer the tax ID belongs to.
        Owner: TaxIDsOwner option
        ///Type of the tax ID, one of `ad_nrt`, `ae_trn`, `al_tin`, `am_tin`, `ao_tin`, `ar_cuit`, `au_abn`, `au_arn`, `aw_tin`, `az_tin`, `ba_tin`, `bb_tin`, `bd_bin`, `bf_ifu`, `bg_uic`, `bh_vat`, `bj_ifu`, `bo_tin`, `br_cnpj`, `br_cpf`, `bs_tin`, `by_tin`, `ca_bn`, `ca_gst_hst`, `ca_pst_bc`, `ca_pst_mb`, `ca_pst_sk`, `ca_qst`, `cd_nif`, `ch_uid`, `ch_vat`, `cl_tin`, `cm_niu`, `cn_tin`, `co_nit`, `cr_tin`, `cv_nif`, `de_stn`, `do_rcn`, `ec_ruc`, `eg_tin`, `es_cif`, `et_tin`, `eu_oss_vat`, `eu_vat`, `fo_vat`, `gb_vat`, `ge_vat`, `gi_tin`, `gn_nif`, `hk_br`, `hr_oib`, `hu_tin`, `id_npwp`, `il_vat`, `in_gst`, `is_vat`, `it_cf`, `jp_cn`, `jp_rn`, `jp_trn`, `ke_pin`, `kg_tin`, `kh_tin`, `kr_brn`, `kz_bin`, `la_tin`, `li_uid`, `li_vat`, `lk_vat`, `ma_vat`, `md_vat`, `me_pib`, `mk_vat`, `mr_nif`, `mx_rfc`, `my_frp`, `my_itn`, `my_sst`, `ng_tin`, `no_vat`, `no_voec`, `np_pan`, `nz_gst`, `om_vat`, `pe_ruc`, `ph_tin`, `pl_nip`, `py_ruc`, `ro_tin`, `rs_pib`, `ru_inn`, `ru_kpp`, `sa_vat`, `sg_gst`, `sg_uen`, `si_tin`, `sn_ninea`, `sr_fin`, `sv_nit`, `th_vat`, `tj_tin`, `tr_tin`, `tw_vat`, `tz_vat`, `ua_vat`, `ug_tin`, `us_ein`, `uy_ruc`, `uz_tin`, `uz_vat`, `ve_rif`, `vn_tin`, `za_vat`, `zm_tin`, or `zw_tin`. Note that some legacy tax IDs have type `unknown`
        Type: TaxIdType
        ///Value of the tax ID.
        Value: string
        ///Tax ID verification information.
        Verification: TaxIdVerification option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "tax_id"

        static member New (country: IsoTypes.IsoCountryCode option, created: DateTime, customer: TaxIdCustomer'AnyOf option, customerAccount: string option, id: string, livemode: bool, owner: TaxIDsOwner option, ``type``: TaxIdType, value: string, verification: TaxIdVerification option) =
            {
                TaxId.Country = country //required
                TaxId.Created = created //required
                TaxId.Customer = customer //required
                TaxId.CustomerAccount = customerAccount //required
                TaxId.Id = id //required
                TaxId.Livemode = livemode //required
                TaxId.Owner = owner //required
                TaxId.Type = ``type`` //required
                TaxId.Value = value //required
                TaxId.Verification = verification //required
            }

    and TaxIdCustomer'AnyOf =
        | String of string
        | Customer of Customer

    and TaxIdVerification = {
        ///Verification status, one of `pending`, `verified`, `unverified`, or `unavailable`.
        Status: TaxIdVerificationStatus
        ///Verified address.
        VerifiedAddress: string option
        ///Verified name.
        VerifiedName: string option
    }
    with
        static member New (status: TaxIdVerificationStatus, verifiedAddress: string option, verifiedName: string option) =
            {
                TaxIdVerification.Status = status //required
                TaxIdVerification.VerifiedAddress = verifiedAddress //required
                TaxIdVerification.VerifiedName = verifiedName //required
            }

    and TaxProductRegistrationsResourceCountryOptions = {
        Ae: TaxProductRegistrationsResourceCountryOptionsDefaultInboundGoods option
        Al: TaxProductRegistrationsResourceCountryOptionsDefault option
        Am: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Ao: TaxProductRegistrationsResourceCountryOptionsDefault option
        At: TaxProductRegistrationsResourceCountryOptionsEurope option
        Au: TaxProductRegistrationsResourceCountryOptionsDefaultInboundGoods option
        Aw: TaxProductRegistrationsResourceCountryOptionsDefault option
        Az: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Ba: TaxProductRegistrationsResourceCountryOptionsDefault option
        Bb: TaxProductRegistrationsResourceCountryOptionsDefault option
        Bd: TaxProductRegistrationsResourceCountryOptionsDefault option
        Be: TaxProductRegistrationsResourceCountryOptionsEurope option
        Bf: TaxProductRegistrationsResourceCountryOptionsDefault option
        Bg: TaxProductRegistrationsResourceCountryOptionsEurope option
        Bh: TaxProductRegistrationsResourceCountryOptionsDefault option
        Bj: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Bs: TaxProductRegistrationsResourceCountryOptionsDefault option
        By: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Ca: TaxProductRegistrationsResourceCountryOptionsCanada option
        Cd: TaxProductRegistrationsResourceCountryOptionsDefault option
        Ch: TaxProductRegistrationsResourceCountryOptionsDefaultInboundGoods option
        Cl: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Cm: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Co: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Cr: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Cv: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Cy: TaxProductRegistrationsResourceCountryOptionsEurope option
        Cz: TaxProductRegistrationsResourceCountryOptionsEurope option
        De: TaxProductRegistrationsResourceCountryOptionsEurope option
        Dk: TaxProductRegistrationsResourceCountryOptionsEurope option
        Ec: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Ee: TaxProductRegistrationsResourceCountryOptionsEurope option
        Eg: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Es: TaxProductRegistrationsResourceCountryOptionsEurope option
        Et: TaxProductRegistrationsResourceCountryOptionsDefault option
        Fi: TaxProductRegistrationsResourceCountryOptionsEurope option
        Fr: TaxProductRegistrationsResourceCountryOptionsEurope option
        Gb: TaxProductRegistrationsResourceCountryOptionsDefaultInboundGoods option
        Ge: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Gn: TaxProductRegistrationsResourceCountryOptionsDefault option
        Gr: TaxProductRegistrationsResourceCountryOptionsEurope option
        Hr: TaxProductRegistrationsResourceCountryOptionsEurope option
        Hu: TaxProductRegistrationsResourceCountryOptionsEurope option
        Id: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Ie: TaxProductRegistrationsResourceCountryOptionsEurope option
        In: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Is: TaxProductRegistrationsResourceCountryOptionsDefault option
        It: TaxProductRegistrationsResourceCountryOptionsEurope option
        Jp: TaxProductRegistrationsResourceCountryOptionsDefaultInboundGoods option
        Ke: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Kg: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Kh: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Kr: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Kz: TaxProductRegistrationsResourceCountryOptionsSimplified option
        La: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Lk: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Lt: TaxProductRegistrationsResourceCountryOptionsEurope option
        Lu: TaxProductRegistrationsResourceCountryOptionsEurope option
        Lv: TaxProductRegistrationsResourceCountryOptionsEurope option
        Ma: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Md: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Me: TaxProductRegistrationsResourceCountryOptionsDefault option
        Mk: TaxProductRegistrationsResourceCountryOptionsDefault option
        Mr: TaxProductRegistrationsResourceCountryOptionsDefault option
        Mt: TaxProductRegistrationsResourceCountryOptionsEurope option
        Mx: TaxProductRegistrationsResourceCountryOptionsSimplified option
        My: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Ng: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Nl: TaxProductRegistrationsResourceCountryOptionsEurope option
        No: TaxProductRegistrationsResourceCountryOptionsDefaultInboundGoods option
        Np: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Nz: TaxProductRegistrationsResourceCountryOptionsDefaultInboundGoods option
        Om: TaxProductRegistrationsResourceCountryOptionsDefault option
        Pe: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Ph: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Pl: TaxProductRegistrationsResourceCountryOptionsEurope option
        Pt: TaxProductRegistrationsResourceCountryOptionsEurope option
        Ro: TaxProductRegistrationsResourceCountryOptionsEurope option
        Rs: TaxProductRegistrationsResourceCountryOptionsDefault option
        Ru: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Sa: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Se: TaxProductRegistrationsResourceCountryOptionsEurope option
        Sg: TaxProductRegistrationsResourceCountryOptionsDefaultInboundGoods option
        Si: TaxProductRegistrationsResourceCountryOptionsEurope option
        Sk: TaxProductRegistrationsResourceCountryOptionsEurope option
        Sn: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Sr: TaxProductRegistrationsResourceCountryOptionsDefault option
        Th: TaxProductRegistrationsResourceCountryOptionsThailand option
        Tj: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Tr: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Tw: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Tz: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Ua: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Ug: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Us: TaxProductRegistrationsResourceCountryOptionsUnitedStates option
        Uy: TaxProductRegistrationsResourceCountryOptionsDefault option
        Uz: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Vn: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Za: TaxProductRegistrationsResourceCountryOptionsDefault option
        Zm: TaxProductRegistrationsResourceCountryOptionsSimplified option
        Zw: TaxProductRegistrationsResourceCountryOptionsDefault option
    }
    with
        static member New (?ae: TaxProductRegistrationsResourceCountryOptionsDefaultInboundGoods, ?al: TaxProductRegistrationsResourceCountryOptionsDefault, ?am: TaxProductRegistrationsResourceCountryOptionsSimplified, ?ao: TaxProductRegistrationsResourceCountryOptionsDefault, ?at: TaxProductRegistrationsResourceCountryOptionsEurope, ?au: TaxProductRegistrationsResourceCountryOptionsDefaultInboundGoods, ?aw: TaxProductRegistrationsResourceCountryOptionsDefault, ?az: TaxProductRegistrationsResourceCountryOptionsSimplified, ?ba: TaxProductRegistrationsResourceCountryOptionsDefault, ?bb: TaxProductRegistrationsResourceCountryOptionsDefault, ?bd: TaxProductRegistrationsResourceCountryOptionsDefault, ?be: TaxProductRegistrationsResourceCountryOptionsEurope, ?bf: TaxProductRegistrationsResourceCountryOptionsDefault, ?bg: TaxProductRegistrationsResourceCountryOptionsEurope, ?bh: TaxProductRegistrationsResourceCountryOptionsDefault, ?bj: TaxProductRegistrationsResourceCountryOptionsSimplified, ?bs: TaxProductRegistrationsResourceCountryOptionsDefault, ?by: TaxProductRegistrationsResourceCountryOptionsSimplified, ?ca: TaxProductRegistrationsResourceCountryOptionsCanada, ?cd: TaxProductRegistrationsResourceCountryOptionsDefault, ?ch: TaxProductRegistrationsResourceCountryOptionsDefaultInboundGoods, ?cl: TaxProductRegistrationsResourceCountryOptionsSimplified, ?cm: TaxProductRegistrationsResourceCountryOptionsSimplified, ?co: TaxProductRegistrationsResourceCountryOptionsSimplified, ?cr: TaxProductRegistrationsResourceCountryOptionsSimplified, ?cv: TaxProductRegistrationsResourceCountryOptionsSimplified, ?cy: TaxProductRegistrationsResourceCountryOptionsEurope, ?cz: TaxProductRegistrationsResourceCountryOptionsEurope, ?de: TaxProductRegistrationsResourceCountryOptionsEurope, ?dk: TaxProductRegistrationsResourceCountryOptionsEurope, ?ec: TaxProductRegistrationsResourceCountryOptionsSimplified, ?ee: TaxProductRegistrationsResourceCountryOptionsEurope, ?eg: TaxProductRegistrationsResourceCountryOptionsSimplified, ?es: TaxProductRegistrationsResourceCountryOptionsEurope, ?et: TaxProductRegistrationsResourceCountryOptionsDefault, ?fi: TaxProductRegistrationsResourceCountryOptionsEurope, ?fr: TaxProductRegistrationsResourceCountryOptionsEurope, ?gb: TaxProductRegistrationsResourceCountryOptionsDefaultInboundGoods, ?ge: TaxProductRegistrationsResourceCountryOptionsSimplified, ?gn: TaxProductRegistrationsResourceCountryOptionsDefault, ?gr: TaxProductRegistrationsResourceCountryOptionsEurope, ?hr: TaxProductRegistrationsResourceCountryOptionsEurope, ?hu: TaxProductRegistrationsResourceCountryOptionsEurope, ?id: TaxProductRegistrationsResourceCountryOptionsSimplified, ?ie: TaxProductRegistrationsResourceCountryOptionsEurope, ?``in``: TaxProductRegistrationsResourceCountryOptionsSimplified, ?is: TaxProductRegistrationsResourceCountryOptionsDefault, ?it: TaxProductRegistrationsResourceCountryOptionsEurope, ?jp: TaxProductRegistrationsResourceCountryOptionsDefaultInboundGoods, ?ke: TaxProductRegistrationsResourceCountryOptionsSimplified, ?kg: TaxProductRegistrationsResourceCountryOptionsSimplified, ?kh: TaxProductRegistrationsResourceCountryOptionsSimplified, ?kr: TaxProductRegistrationsResourceCountryOptionsSimplified, ?kz: TaxProductRegistrationsResourceCountryOptionsSimplified, ?la: TaxProductRegistrationsResourceCountryOptionsSimplified, ?lk: TaxProductRegistrationsResourceCountryOptionsSimplified, ?lt: TaxProductRegistrationsResourceCountryOptionsEurope, ?lu: TaxProductRegistrationsResourceCountryOptionsEurope, ?lv: TaxProductRegistrationsResourceCountryOptionsEurope, ?ma: TaxProductRegistrationsResourceCountryOptionsSimplified, ?md: TaxProductRegistrationsResourceCountryOptionsSimplified, ?me: TaxProductRegistrationsResourceCountryOptionsDefault, ?mk: TaxProductRegistrationsResourceCountryOptionsDefault, ?mr: TaxProductRegistrationsResourceCountryOptionsDefault, ?mt: TaxProductRegistrationsResourceCountryOptionsEurope, ?mx: TaxProductRegistrationsResourceCountryOptionsSimplified, ?my: TaxProductRegistrationsResourceCountryOptionsSimplified, ?ng: TaxProductRegistrationsResourceCountryOptionsSimplified, ?nl: TaxProductRegistrationsResourceCountryOptionsEurope, ?no: TaxProductRegistrationsResourceCountryOptionsDefaultInboundGoods, ?np: TaxProductRegistrationsResourceCountryOptionsSimplified, ?nz: TaxProductRegistrationsResourceCountryOptionsDefaultInboundGoods, ?om: TaxProductRegistrationsResourceCountryOptionsDefault, ?pe: TaxProductRegistrationsResourceCountryOptionsSimplified, ?ph: TaxProductRegistrationsResourceCountryOptionsSimplified, ?pl: TaxProductRegistrationsResourceCountryOptionsEurope, ?pt: TaxProductRegistrationsResourceCountryOptionsEurope, ?ro: TaxProductRegistrationsResourceCountryOptionsEurope, ?rs: TaxProductRegistrationsResourceCountryOptionsDefault, ?ru: TaxProductRegistrationsResourceCountryOptionsSimplified, ?sa: TaxProductRegistrationsResourceCountryOptionsSimplified, ?se: TaxProductRegistrationsResourceCountryOptionsEurope, ?sg: TaxProductRegistrationsResourceCountryOptionsDefaultInboundGoods, ?si: TaxProductRegistrationsResourceCountryOptionsEurope, ?sk: TaxProductRegistrationsResourceCountryOptionsEurope, ?sn: TaxProductRegistrationsResourceCountryOptionsSimplified, ?sr: TaxProductRegistrationsResourceCountryOptionsDefault, ?th: TaxProductRegistrationsResourceCountryOptionsThailand, ?tj: TaxProductRegistrationsResourceCountryOptionsSimplified, ?tr: TaxProductRegistrationsResourceCountryOptionsSimplified, ?tw: TaxProductRegistrationsResourceCountryOptionsSimplified, ?tz: TaxProductRegistrationsResourceCountryOptionsSimplified, ?ua: TaxProductRegistrationsResourceCountryOptionsSimplified, ?ug: TaxProductRegistrationsResourceCountryOptionsSimplified, ?us: TaxProductRegistrationsResourceCountryOptionsUnitedStates, ?uy: TaxProductRegistrationsResourceCountryOptionsDefault, ?uz: TaxProductRegistrationsResourceCountryOptionsSimplified, ?vn: TaxProductRegistrationsResourceCountryOptionsSimplified, ?za: TaxProductRegistrationsResourceCountryOptionsDefault, ?zm: TaxProductRegistrationsResourceCountryOptionsSimplified, ?zw: TaxProductRegistrationsResourceCountryOptionsDefault) =
            {
                TaxProductRegistrationsResourceCountryOptions.Ae = ae
                TaxProductRegistrationsResourceCountryOptions.Al = al
                TaxProductRegistrationsResourceCountryOptions.Am = am
                TaxProductRegistrationsResourceCountryOptions.Ao = ao
                TaxProductRegistrationsResourceCountryOptions.At = at
                TaxProductRegistrationsResourceCountryOptions.Au = au
                TaxProductRegistrationsResourceCountryOptions.Aw = aw
                TaxProductRegistrationsResourceCountryOptions.Az = az
                TaxProductRegistrationsResourceCountryOptions.Ba = ba
                TaxProductRegistrationsResourceCountryOptions.Bb = bb
                TaxProductRegistrationsResourceCountryOptions.Bd = bd
                TaxProductRegistrationsResourceCountryOptions.Be = be
                TaxProductRegistrationsResourceCountryOptions.Bf = bf
                TaxProductRegistrationsResourceCountryOptions.Bg = bg
                TaxProductRegistrationsResourceCountryOptions.Bh = bh
                TaxProductRegistrationsResourceCountryOptions.Bj = bj
                TaxProductRegistrationsResourceCountryOptions.Bs = bs
                TaxProductRegistrationsResourceCountryOptions.By = by
                TaxProductRegistrationsResourceCountryOptions.Ca = ca
                TaxProductRegistrationsResourceCountryOptions.Cd = cd
                TaxProductRegistrationsResourceCountryOptions.Ch = ch
                TaxProductRegistrationsResourceCountryOptions.Cl = cl
                TaxProductRegistrationsResourceCountryOptions.Cm = cm
                TaxProductRegistrationsResourceCountryOptions.Co = co
                TaxProductRegistrationsResourceCountryOptions.Cr = cr
                TaxProductRegistrationsResourceCountryOptions.Cv = cv
                TaxProductRegistrationsResourceCountryOptions.Cy = cy
                TaxProductRegistrationsResourceCountryOptions.Cz = cz
                TaxProductRegistrationsResourceCountryOptions.De = de
                TaxProductRegistrationsResourceCountryOptions.Dk = dk
                TaxProductRegistrationsResourceCountryOptions.Ec = ec
                TaxProductRegistrationsResourceCountryOptions.Ee = ee
                TaxProductRegistrationsResourceCountryOptions.Eg = eg
                TaxProductRegistrationsResourceCountryOptions.Es = es
                TaxProductRegistrationsResourceCountryOptions.Et = et
                TaxProductRegistrationsResourceCountryOptions.Fi = fi
                TaxProductRegistrationsResourceCountryOptions.Fr = fr
                TaxProductRegistrationsResourceCountryOptions.Gb = gb
                TaxProductRegistrationsResourceCountryOptions.Ge = ge
                TaxProductRegistrationsResourceCountryOptions.Gn = gn
                TaxProductRegistrationsResourceCountryOptions.Gr = gr
                TaxProductRegistrationsResourceCountryOptions.Hr = hr
                TaxProductRegistrationsResourceCountryOptions.Hu = hu
                TaxProductRegistrationsResourceCountryOptions.Id = id
                TaxProductRegistrationsResourceCountryOptions.Ie = ie
                TaxProductRegistrationsResourceCountryOptions.In = ``in``
                TaxProductRegistrationsResourceCountryOptions.Is = is
                TaxProductRegistrationsResourceCountryOptions.It = it
                TaxProductRegistrationsResourceCountryOptions.Jp = jp
                TaxProductRegistrationsResourceCountryOptions.Ke = ke
                TaxProductRegistrationsResourceCountryOptions.Kg = kg
                TaxProductRegistrationsResourceCountryOptions.Kh = kh
                TaxProductRegistrationsResourceCountryOptions.Kr = kr
                TaxProductRegistrationsResourceCountryOptions.Kz = kz
                TaxProductRegistrationsResourceCountryOptions.La = la
                TaxProductRegistrationsResourceCountryOptions.Lk = lk
                TaxProductRegistrationsResourceCountryOptions.Lt = lt
                TaxProductRegistrationsResourceCountryOptions.Lu = lu
                TaxProductRegistrationsResourceCountryOptions.Lv = lv
                TaxProductRegistrationsResourceCountryOptions.Ma = ma
                TaxProductRegistrationsResourceCountryOptions.Md = md
                TaxProductRegistrationsResourceCountryOptions.Me = me
                TaxProductRegistrationsResourceCountryOptions.Mk = mk
                TaxProductRegistrationsResourceCountryOptions.Mr = mr
                TaxProductRegistrationsResourceCountryOptions.Mt = mt
                TaxProductRegistrationsResourceCountryOptions.Mx = mx
                TaxProductRegistrationsResourceCountryOptions.My = my
                TaxProductRegistrationsResourceCountryOptions.Ng = ng
                TaxProductRegistrationsResourceCountryOptions.Nl = nl
                TaxProductRegistrationsResourceCountryOptions.No = no
                TaxProductRegistrationsResourceCountryOptions.Np = np
                TaxProductRegistrationsResourceCountryOptions.Nz = nz
                TaxProductRegistrationsResourceCountryOptions.Om = om
                TaxProductRegistrationsResourceCountryOptions.Pe = pe
                TaxProductRegistrationsResourceCountryOptions.Ph = ph
                TaxProductRegistrationsResourceCountryOptions.Pl = pl
                TaxProductRegistrationsResourceCountryOptions.Pt = pt
                TaxProductRegistrationsResourceCountryOptions.Ro = ro
                TaxProductRegistrationsResourceCountryOptions.Rs = rs
                TaxProductRegistrationsResourceCountryOptions.Ru = ru
                TaxProductRegistrationsResourceCountryOptions.Sa = sa
                TaxProductRegistrationsResourceCountryOptions.Se = se
                TaxProductRegistrationsResourceCountryOptions.Sg = sg
                TaxProductRegistrationsResourceCountryOptions.Si = si
                TaxProductRegistrationsResourceCountryOptions.Sk = sk
                TaxProductRegistrationsResourceCountryOptions.Sn = sn
                TaxProductRegistrationsResourceCountryOptions.Sr = sr
                TaxProductRegistrationsResourceCountryOptions.Th = th
                TaxProductRegistrationsResourceCountryOptions.Tj = tj
                TaxProductRegistrationsResourceCountryOptions.Tr = tr
                TaxProductRegistrationsResourceCountryOptions.Tw = tw
                TaxProductRegistrationsResourceCountryOptions.Tz = tz
                TaxProductRegistrationsResourceCountryOptions.Ua = ua
                TaxProductRegistrationsResourceCountryOptions.Ug = ug
                TaxProductRegistrationsResourceCountryOptions.Us = us
                TaxProductRegistrationsResourceCountryOptions.Uy = uy
                TaxProductRegistrationsResourceCountryOptions.Uz = uz
                TaxProductRegistrationsResourceCountryOptions.Vn = vn
                TaxProductRegistrationsResourceCountryOptions.Za = za
                TaxProductRegistrationsResourceCountryOptions.Zm = zm
                TaxProductRegistrationsResourceCountryOptions.Zw = zw
            }

    and TaxProductRegistrationsResourceCountryOptionsCanada = {
        ProvinceStandard: TaxProductRegistrationsResourceCountryOptionsCaProvinceStandard option
        ///Type of registration in Canada.
        Type: TaxProductRegistrationsResourceCountryOptionsCanadaType
    }
    with
        static member New (``type``: TaxProductRegistrationsResourceCountryOptionsCanadaType, ?provinceStandard: TaxProductRegistrationsResourceCountryOptionsCaProvinceStandard) =
            {
                TaxProductRegistrationsResourceCountryOptionsCanada.Type = ``type`` //required
                TaxProductRegistrationsResourceCountryOptionsCanada.ProvinceStandard = provinceStandard
            }

    and TaxProductRegistrationsResourceCountryOptionsDefaultInboundGoods = {
        Standard: TaxProductRegistrationsResourceCountryOptionsDefaultStandard option
    }
    with
        ///Type of registration in `country`.
        member _.Type = "standard"

        static member New (?standard: TaxProductRegistrationsResourceCountryOptionsDefaultStandard) =
            {
                TaxProductRegistrationsResourceCountryOptionsDefaultInboundGoods.Standard = standard
            }

    and TaxProductRegistrationsResourceCountryOptionsDefaultStandard = {
        ///Place of supply scheme used in an Default standard registration.
        PlaceOfSupplyScheme: TaxProductRegistrationsResourceCountryOptionsDefaultStandardPlaceOfSupplyScheme
    }
    with
        static member New (placeOfSupplyScheme: TaxProductRegistrationsResourceCountryOptionsDefaultStandardPlaceOfSupplyScheme) =
            {
                TaxProductRegistrationsResourceCountryOptionsDefaultStandard.PlaceOfSupplyScheme = placeOfSupplyScheme //required
            }

    and TaxProductRegistrationsResourceCountryOptionsEuStandard = {
        ///Place of supply scheme used in an EU standard registration.
        PlaceOfSupplyScheme: TaxProductRegistrationsResourceCountryOptionsEuStandardPlaceOfSupplyScheme
    }
    with
        static member New (placeOfSupplyScheme: TaxProductRegistrationsResourceCountryOptionsEuStandardPlaceOfSupplyScheme) =
            {
                TaxProductRegistrationsResourceCountryOptionsEuStandard.PlaceOfSupplyScheme = placeOfSupplyScheme //required
            }

    and TaxProductRegistrationsResourceCountryOptionsEurope = {
        Standard: TaxProductRegistrationsResourceCountryOptionsEuStandard option
        ///Type of registration in an EU country.
        Type: TaxProductRegistrationsResourceCountryOptionsEuropeType
    }
    with
        static member New (``type``: TaxProductRegistrationsResourceCountryOptionsEuropeType, ?standard: TaxProductRegistrationsResourceCountryOptionsEuStandard) =
            {
                TaxProductRegistrationsResourceCountryOptionsEurope.Type = ``type`` //required
                TaxProductRegistrationsResourceCountryOptionsEurope.Standard = standard
            }

    and TaxProductRegistrationsResourceCountryOptionsUnitedStates = {
        LocalAmusementTax: TaxProductRegistrationsResourceCountryOptionsUsLocalAmusementTax option
        LocalLeaseTax: TaxProductRegistrationsResourceCountryOptionsUsLocalLeaseTax option
        ///Two-letter US state code ([ISO 3166-2](https://en.wikipedia.org/wiki/ISO_3166-2)).
        State: string
        StateSalesTax: TaxProductRegistrationsResourceCountryOptionsUsStateSalesTax option
        ///Type of registration in the US.
        Type: TaxProductRegistrationsResourceCountryOptionsUnitedStatesType
    }
    with
        static member New (state: string, ``type``: TaxProductRegistrationsResourceCountryOptionsUnitedStatesType, ?localAmusementTax: TaxProductRegistrationsResourceCountryOptionsUsLocalAmusementTax, ?localLeaseTax: TaxProductRegistrationsResourceCountryOptionsUsLocalLeaseTax, ?stateSalesTax: TaxProductRegistrationsResourceCountryOptionsUsStateSalesTax) =
            {
                TaxProductRegistrationsResourceCountryOptionsUnitedStates.State = state //required
                TaxProductRegistrationsResourceCountryOptionsUnitedStates.Type = ``type`` //required
                TaxProductRegistrationsResourceCountryOptionsUnitedStates.LocalAmusementTax = localAmusementTax
                TaxProductRegistrationsResourceCountryOptionsUnitedStates.LocalLeaseTax = localLeaseTax
                TaxProductRegistrationsResourceCountryOptionsUnitedStates.StateSalesTax = stateSalesTax
            }

    and TaxProductRegistrationsResourceCountryOptionsUsStateSalesTax = {
        ///Elections for the state sales tax registration.
        Elections: TaxProductRegistrationsResourceCountryOptionsUsStateSalesTaxElection list option
    }
    with
        static member New (?elections: TaxProductRegistrationsResourceCountryOptionsUsStateSalesTaxElection list) =
            {
                TaxProductRegistrationsResourceCountryOptionsUsStateSalesTax.Elections = elections
            }

    and TaxProductRegistrationsResourceCountryOptionsUsStateSalesTaxElection = {
        ///A [FIPS code](https://www.census.gov/library/reference/code-lists/ansi.html) representing the local jurisdiction.
        Jurisdiction: string option
        ///The type of the election for the state sales tax registration.
        Type: TaxProductRegistrationsResourceCountryOptionsUsStateSalesTaxElectionType
    }
    with
        static member New (``type``: TaxProductRegistrationsResourceCountryOptionsUsStateSalesTaxElectionType, ?jurisdiction: string) =
            {
                TaxProductRegistrationsResourceCountryOptionsUsStateSalesTaxElection.Type = ``type`` //required
                TaxProductRegistrationsResourceCountryOptionsUsStateSalesTaxElection.Jurisdiction = jurisdiction
            }

    and TaxProductResourceCustomerDetails = {
        ///The customer's postal address (for example, home or business location).
        Address: TaxProductResourcePostalAddress option
        ///The type of customer address provided.
        AddressSource: TaxProductResourceCustomerDetailsAddressSource option
        ///The customer's IP address (IPv4 or IPv6).
        IpAddress: string option
        ///The customer's tax IDs (for example, EU VAT numbers).
        TaxIds: TaxProductResourceCustomerDetailsResourceTaxId list
        ///The taxability override used for taxation.
        TaxabilityOverride: TaxProductResourceCustomerDetailsTaxabilityOverride
    }
    with
        static member New (address: TaxProductResourcePostalAddress option, addressSource: TaxProductResourceCustomerDetailsAddressSource option, ipAddress: string option, taxIds: TaxProductResourceCustomerDetailsResourceTaxId list, taxabilityOverride: TaxProductResourceCustomerDetailsTaxabilityOverride) =
            {
                TaxProductResourceCustomerDetails.Address = address //required
                TaxProductResourceCustomerDetails.AddressSource = addressSource //required
                TaxProductResourceCustomerDetails.IpAddress = ipAddress //required
                TaxProductResourceCustomerDetails.TaxIds = taxIds //required
                TaxProductResourceCustomerDetails.TaxabilityOverride = taxabilityOverride //required
            }

    and TaxProductResourceCustomerDetailsResourceTaxId = {
        ///The type of the tax ID, one of `ad_nrt`, `ar_cuit`, `eu_vat`, `bo_tin`, `br_cnpj`, `br_cpf`, `cn_tin`, `co_nit`, `cr_tin`, `do_rcn`, `ec_ruc`, `eu_oss_vat`, `hr_oib`, `pe_ruc`, `ro_tin`, `rs_pib`, `sv_nit`, `uy_ruc`, `ve_rif`, `vn_tin`, `gb_vat`, `nz_gst`, `au_abn`, `au_arn`, `in_gst`, `no_vat`, `no_voec`, `za_vat`, `ch_vat`, `mx_rfc`, `sg_uen`, `ru_inn`, `ru_kpp`, `ca_bn`, `hk_br`, `es_cif`, `pl_nip`, `it_cf`, `fo_vat`, `gi_tin`, `py_ruc`, `tw_vat`, `th_vat`, `jp_cn`, `jp_rn`, `jp_trn`, `li_uid`, `li_vat`, `lk_vat`, `my_itn`, `us_ein`, `kr_brn`, `ca_qst`, `ca_gst_hst`, `ca_pst_bc`, `ca_pst_mb`, `ca_pst_sk`, `my_sst`, `sg_gst`, `ae_trn`, `cl_tin`, `sa_vat`, `id_npwp`, `my_frp`, `il_vat`, `ge_vat`, `ua_vat`, `is_vat`, `bg_uic`, `hu_tin`, `si_tin`, `ke_pin`, `tr_tin`, `eg_tin`, `ph_tin`, `al_tin`, `bh_vat`, `kz_bin`, `ng_tin`, `om_vat`, `de_stn`, `ch_uid`, `tz_vat`, `uz_vat`, `uz_tin`, `md_vat`, `ma_vat`, `by_tin`, `ao_tin`, `bs_tin`, `bb_tin`, `cd_nif`, `mr_nif`, `me_pib`, `zw_tin`, `ba_tin`, `gn_nif`, `mk_vat`, `sr_fin`, `sn_ninea`, `am_tin`, `np_pan`, `tj_tin`, `ug_tin`, `zm_tin`, `kh_tin`, `aw_tin`, `az_tin`, `bd_bin`, `bj_ifu`, `et_tin`, `kg_tin`, `la_tin`, `cm_niu`, `cv_nif`, `bf_ifu`, or `unknown`
        Type: TaxProductResourceCustomerDetailsResourceTaxIdType
        ///The value of the tax ID.
        Value: string
    }
    with
        static member New (``type``: TaxProductResourceCustomerDetailsResourceTaxIdType, value: string) =
            {
                TaxProductResourceCustomerDetailsResourceTaxId.Type = ``type`` //required
                TaxProductResourceCustomerDetailsResourceTaxId.Value = value //required
            }

    and TaxProductResourceJurisdiction = {
        ///Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
        Country: IsoTypes.IsoCountryCode
        ///A human-readable name for the jurisdiction imposing the tax.
        DisplayName: string
        ///Indicates the level of the jurisdiction imposing the tax.
        Level: TaxProductResourceJurisdictionLevel
        ///[ISO 3166-2 subdivision code](https://en.wikipedia.org/wiki/ISO_3166-2), without country prefix. For example, "NY" for New York, United States.
        State: string option
    }
    with
        static member New (country: IsoTypes.IsoCountryCode, displayName: string, level: TaxProductResourceJurisdictionLevel, state: string option) =
            {
                TaxProductResourceJurisdiction.Country = country //required
                TaxProductResourceJurisdiction.DisplayName = displayName //required
                TaxProductResourceJurisdiction.Level = level //required
                TaxProductResourceJurisdiction.State = state //required
            }

    and TaxProductResourceLineItemTaxBreakdown = {
        ///The amount of tax, in the [smallest currency unit](https://docs.stripe.com/currencies#minor-units).
        Amount: int
        Jurisdiction: TaxProductResourceJurisdiction
        ///Indicates whether the jurisdiction was determined by the origin (merchant's address) or destination (customer's address).
        Sourcing: TaxProductResourceLineItemTaxBreakdownSourcing
        ///Details regarding the rate for this tax. This field will be `null` when the tax is not imposed, for example if the product is exempt from tax.
        TaxRateDetails: TaxProductResourceLineItemTaxRateDetails option
        ///The reasoning behind this tax, for example, if the product is tax exempt. The possible values for this field may be extended as new tax rules are supported.
        TaxabilityReason: TaxProductResourceLineItemTaxBreakdownTaxabilityReason
        ///The amount on which tax is calculated, in the [smallest currency unit](https://docs.stripe.com/currencies#minor-units).
        TaxableAmount: int
    }
    with
        static member New (amount: int, jurisdiction: TaxProductResourceJurisdiction, sourcing: TaxProductResourceLineItemTaxBreakdownSourcing, taxRateDetails: TaxProductResourceLineItemTaxRateDetails option, taxabilityReason: TaxProductResourceLineItemTaxBreakdownTaxabilityReason, taxableAmount: int) =
            {
                TaxProductResourceLineItemTaxBreakdown.Amount = amount //required
                TaxProductResourceLineItemTaxBreakdown.Jurisdiction = jurisdiction //required
                TaxProductResourceLineItemTaxBreakdown.Sourcing = sourcing //required
                TaxProductResourceLineItemTaxBreakdown.TaxRateDetails = taxRateDetails //required
                TaxProductResourceLineItemTaxBreakdown.TaxabilityReason = taxabilityReason //required
                TaxProductResourceLineItemTaxBreakdown.TaxableAmount = taxableAmount //required
            }

    and TaxProductResourceLineItemTaxRateDetails = {
        ///A localized display name for tax type, intended to be human-readable. For example, "Local Sales and Use Tax", "Value-added tax (VAT)", or "Umsatzsteuer (USt.)".
        DisplayName: string
        ///The tax rate percentage as a string. For example, 8.5% is represented as "8.5".
        PercentageDecimal: string
        ///The tax type, such as `vat` or `sales_tax`.
        TaxType: TaxProductResourceLineItemTaxRateDetailsTaxType
    }
    with
        static member New (displayName: string, percentageDecimal: string, taxType: TaxProductResourceLineItemTaxRateDetailsTaxType) =
            {
                TaxProductResourceLineItemTaxRateDetails.DisplayName = displayName //required
                TaxProductResourceLineItemTaxRateDetails.PercentageDecimal = percentageDecimal //required
                TaxProductResourceLineItemTaxRateDetails.TaxType = taxType //required
            }

    and TaxProductResourcePostalAddress = {
        ///City, district, suburb, town, or village.
        City: string option
        ///Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
        Country: IsoTypes.IsoCountryCode
        ///Address line 1, such as the street, PO Box, or company name.
        [<JsonField(Name="line1")>]Line1: string option
        ///Address line 2, such as the apartment, suite, unit, or building.
        [<JsonField(Name="line2")>]Line2: string option
        ///ZIP or postal code.
        PostalCode: string option
        ///State/province as an [ISO 3166-2](https://en.wikipedia.org/wiki/ISO_3166-2) subdivision code, without country prefix, such as "NY" or "TX".
        State: string option
    }
    with
        static member New (city: string option, country: IsoTypes.IsoCountryCode, line1: string option, line2: string option, postalCode: string option, state: string option) =
            {
                TaxProductResourcePostalAddress.City = city //required
                TaxProductResourcePostalAddress.Country = country //required
                TaxProductResourcePostalAddress.Line1 = line1 //required
                TaxProductResourcePostalAddress.Line2 = line2 //required
                TaxProductResourcePostalAddress.PostalCode = postalCode //required
                TaxProductResourcePostalAddress.State = state //required
            }

    and TaxProductResourceShipFromDetails = {
        Address: TaxProductResourcePostalAddress
    }
    with
        static member New (address: TaxProductResourcePostalAddress) =
            {
                TaxProductResourceShipFromDetails.Address = address //required
            }

    and TaxProductResourceTaxAssociationTransactionAttempts = {
        Committed: TaxProductResourceTaxAssociationTransactionAttemptsResourceCommitted option
        Errored: TaxProductResourceTaxAssociationTransactionAttemptsResourceErrored option
        ///The source of the tax transaction attempt. This is either a refund or a payment intent.
        Source: string
        ///The status of the transaction attempt. This can be `errored` or `committed`.
        Status: TaxProductResourceTaxAssociationTransactionAttemptsStatus
    }
    with
        static member New (source: string, status: TaxProductResourceTaxAssociationTransactionAttemptsStatus, ?committed: TaxProductResourceTaxAssociationTransactionAttemptsResourceCommitted, ?errored: TaxProductResourceTaxAssociationTransactionAttemptsResourceErrored) =
            {
                TaxProductResourceTaxAssociationTransactionAttempts.Source = source //required
                TaxProductResourceTaxAssociationTransactionAttempts.Status = status //required
                TaxProductResourceTaxAssociationTransactionAttempts.Committed = committed
                TaxProductResourceTaxAssociationTransactionAttempts.Errored = errored
            }

    and TaxProductResourceTaxAssociationTransactionAttemptsResourceErrored = {
        ///Details on why we couldn't commit the tax transaction.
        Reason: TaxProductResourceTaxAssociationTransactionAttemptsResourceErroredReason
    }
    with
        static member New (reason: TaxProductResourceTaxAssociationTransactionAttemptsResourceErroredReason) =
            {
                TaxProductResourceTaxAssociationTransactionAttemptsResourceErrored.Reason = reason //required
            }

    and TaxProductResourceTaxBreakdown = {
        ///The amount of tax, in the [smallest currency unit](https://docs.stripe.com/currencies#minor-units).
        Amount: int
        ///Specifies whether the tax amount is included in the line item amount.
        Inclusive: bool
        TaxRateDetails: TaxProductResourceTaxRateDetails
        ///The reasoning behind this tax, for example, if the product is tax exempt. We might extend the possible values for this field to support new tax rules.
        TaxabilityReason: TaxProductResourceTaxBreakdownTaxabilityReason
        ///The amount on which tax is calculated, in the [smallest currency unit](https://docs.stripe.com/currencies#minor-units).
        TaxableAmount: int
    }
    with
        static member New (amount: int, inclusive: bool, taxRateDetails: TaxProductResourceTaxRateDetails, taxabilityReason: TaxProductResourceTaxBreakdownTaxabilityReason, taxableAmount: int) =
            {
                TaxProductResourceTaxBreakdown.Amount = amount //required
                TaxProductResourceTaxBreakdown.Inclusive = inclusive //required
                TaxProductResourceTaxBreakdown.TaxRateDetails = taxRateDetails //required
                TaxProductResourceTaxBreakdown.TaxabilityReason = taxabilityReason //required
                TaxProductResourceTaxBreakdown.TaxableAmount = taxableAmount //required
            }

    and TaxProductResourceTaxCalculationShippingCost = {
        ///The shipping amount in the [smallest currency unit](https://docs.stripe.com/currencies#minor-units). If `tax_behavior=inclusive`, then this amount includes taxes. Otherwise, taxes were calculated on top of this amount.
        Amount: int
        ///The amount of tax calculated for shipping, in the [smallest currency unit](https://docs.stripe.com/currencies#minor-units).
        AmountTax: int
        ///The ID of an existing [ShippingRate](https://docs.stripe.com/api/shipping_rates/object).
        ShippingRate: string option
        ///Specifies whether the `amount` includes taxes. If `tax_behavior=inclusive`, then the amount includes taxes.
        TaxBehavior: TaxProductResourceTaxCalculationShippingCostTaxBehavior
        ///Detailed account of taxes relevant to shipping cost.
        TaxBreakdown: TaxProductResourceLineItemTaxBreakdown list option
        ///The [tax code](https://docs.stripe.com/tax/tax-categories) ID used for shipping.
        TaxCode: string
    }
    with
        static member New (amount: int, amountTax: int, taxBehavior: TaxProductResourceTaxCalculationShippingCostTaxBehavior, taxCode: string, ?shippingRate: string, ?taxBreakdown: TaxProductResourceLineItemTaxBreakdown list) =
            {
                TaxProductResourceTaxCalculationShippingCost.Amount = amount //required
                TaxProductResourceTaxCalculationShippingCost.AmountTax = amountTax //required
                TaxProductResourceTaxCalculationShippingCost.TaxBehavior = taxBehavior //required
                TaxProductResourceTaxCalculationShippingCost.TaxCode = taxCode //required
                TaxProductResourceTaxCalculationShippingCost.ShippingRate = shippingRate
                TaxProductResourceTaxCalculationShippingCost.TaxBreakdown = taxBreakdown
            }

    and TaxProductResourceTaxRateDetails = {
        ///Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
        Country: IsoTypes.IsoCountryCode option
        ///The amount of the tax rate when the `rate_type` is `flat_amount`. Tax rates with `rate_type` `percentage` can vary based on the transaction, resulting in this field being `null`. This field exposes the amount and currency of the flat tax rate.
        FlatAmount: TaxRateFlatAmount option
        ///The tax rate percentage as a string. For example, 8.5% is represented as `"8.5"`.
        PercentageDecimal: string
        ///Indicates the type of tax rate applied to the taxable amount. This value can be `null` when no tax applies to the location. This field is only present for TaxRates created by Stripe Tax.
        RateType: TaxProductResourceTaxRateDetailsRateType option
        ///State, county, province, or region ([ISO 3166-2](https://en.wikipedia.org/wiki/ISO_3166-2)).
        State: string option
        ///The tax type, such as `vat` or `sales_tax`.
        TaxType: TaxProductResourceTaxRateDetailsTaxType option
    }
    with
        static member New (country: IsoTypes.IsoCountryCode option, flatAmount: TaxRateFlatAmount option, percentageDecimal: string, rateType: TaxProductResourceTaxRateDetailsRateType option, state: string option, taxType: TaxProductResourceTaxRateDetailsTaxType option) =
            {
                TaxProductResourceTaxRateDetails.Country = country //required
                TaxProductResourceTaxRateDetails.FlatAmount = flatAmount //required
                TaxProductResourceTaxRateDetails.PercentageDecimal = percentageDecimal //required
                TaxProductResourceTaxRateDetails.RateType = rateType //required
                TaxProductResourceTaxRateDetails.State = state //required
                TaxProductResourceTaxRateDetails.TaxType = taxType //required
            }

    and TaxProductResourceTaxSettingsDefaults = {
        ///The tax calculation provider this account uses. Defaults to `stripe` when not using a [third-party provider](/tax/third-party-apps).
        Provider: TaxProductResourceTaxSettingsDefaultsProvider
        ///Default [tax behavior](https://stripe.com/docs/tax/products-prices-tax-categories-tax-behavior#tax-behavior) used to specify whether the price is considered inclusive of taxes or exclusive of taxes. If the item's price has a tax behavior set, it will take precedence over the default tax behavior.
        TaxBehavior: TaxProductResourceTaxSettingsDefaultsTaxBehavior option
        ///Default [tax code](https://stripe.com/docs/tax/tax-categories) used to classify your products and prices.
        TaxCode: string option
    }
    with
        static member New (provider: TaxProductResourceTaxSettingsDefaultsProvider, taxBehavior: TaxProductResourceTaxSettingsDefaultsTaxBehavior option, taxCode: string option) =
            {
                TaxProductResourceTaxSettingsDefaults.Provider = provider //required
                TaxProductResourceTaxSettingsDefaults.TaxBehavior = taxBehavior //required
                TaxProductResourceTaxSettingsDefaults.TaxCode = taxCode //required
            }

    and TaxProductResourceTaxSettingsHeadOffice = {
        Address: Address
    }
    with
        static member New (address: Address) =
            {
                TaxProductResourceTaxSettingsHeadOffice.Address = address //required
            }

    and TaxProductResourceTaxSettingsStatusDetails = {
        Active: TaxProductResourceTaxSettingsStatusDetailsResourceActive option
        Pending: TaxProductResourceTaxSettingsStatusDetailsResourcePending option
    }
    with
        static member New (?active: TaxProductResourceTaxSettingsStatusDetailsResourceActive, ?pending: TaxProductResourceTaxSettingsStatusDetailsResourcePending) =
            {
                TaxProductResourceTaxSettingsStatusDetails.Active = active
                TaxProductResourceTaxSettingsStatusDetails.Pending = pending
            }

    and TaxProductResourceTaxSettingsStatusDetailsResourcePending = {
        ///The list of missing fields that are required to perform calculations. It includes the entry `head_office` when the status is `pending`. It is recommended to set the optional values even if they aren't listed as required for calculating taxes. Calculations can fail if missing fields aren't explicitly provided on every call.
        MissingFields: string list option
    }
    with
        static member New (missingFields: string list option) =
            {
                TaxProductResourceTaxSettingsStatusDetailsResourcePending.MissingFields = missingFields //required
            }

    and TaxProductResourceTaxTransactionShippingCost = {
        ///The shipping amount in the [smallest currency unit](https://docs.stripe.com/currencies#minor-units). If `tax_behavior=inclusive`, then this amount includes taxes. Otherwise, taxes were calculated on top of this amount.
        Amount: int
        ///The amount of tax calculated for shipping, in the [smallest currency unit](https://docs.stripe.com/currencies#minor-units).
        AmountTax: int
        ///The ID of an existing [ShippingRate](https://docs.stripe.com/api/shipping_rates/object).
        ShippingRate: string option
        ///Specifies whether the `amount` includes taxes. If `tax_behavior=inclusive`, then the amount includes taxes.
        TaxBehavior: TaxProductResourceTaxTransactionShippingCostTaxBehavior
        ///Detailed account of taxes relevant to shipping cost. (It is not populated for the transaction resource object and will be removed in the next API version.)
        TaxBreakdown: TaxProductResourceLineItemTaxBreakdown list option
        ///The [tax code](https://docs.stripe.com/tax/tax-categories) ID used for shipping.
        TaxCode: string
    }
    with
        static member New (amount: int, amountTax: int, taxBehavior: TaxProductResourceTaxTransactionShippingCostTaxBehavior, taxCode: string, ?shippingRate: string, ?taxBreakdown: TaxProductResourceLineItemTaxBreakdown list) =
            {
                TaxProductResourceTaxTransactionShippingCost.Amount = amount //required
                TaxProductResourceTaxTransactionShippingCost.AmountTax = amountTax //required
                TaxProductResourceTaxTransactionShippingCost.TaxBehavior = taxBehavior //required
                TaxProductResourceTaxTransactionShippingCost.TaxCode = taxCode //required
                TaxProductResourceTaxTransactionShippingCost.ShippingRate = shippingRate
                TaxProductResourceTaxTransactionShippingCost.TaxBreakdown = taxBreakdown
            }

    ///Tax rates can be applied to [invoices](/invoicing/taxes/tax-rates), [subscriptions](/billing/taxes/tax-rates) and [Checkout Sessions](/payments/checkout/use-manual-tax-rates) to collect tax.
    ///Related guide: [Tax rates](/billing/taxes/tax-rates)
    and TaxRate = {
        ///Defaults to `true`. When set to `false`, this tax rate cannot be used with new applications or Checkout Sessions, but will still work for subscriptions and invoices that already have it set.
        Active: bool
        ///Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
        Country: IsoTypes.IsoCountryCode option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///An arbitrary string attached to the tax rate for your internal use only. It will not be visible to your customers.
        Description: string option
        ///The display name of the tax rates as it will appear to your customer on their receipt email, PDF, and the hosted invoice page.
        DisplayName: string
        ///Actual/effective tax rate percentage out of 100. For tax calculations with automatic_tax[enabled]=true,
        ///this percentage reflects the rate actually used to calculate tax based on the product's taxability
        ///and whether the user is registered to collect taxes in the corresponding jurisdiction.
        EffectivePercentage: decimal option
        ///The amount of the tax rate when the `rate_type` is `flat_amount`. Tax rates with `rate_type` `percentage` can vary based on the transaction, resulting in this field being `null`. This field exposes the amount and currency of the flat tax rate.
        FlatAmount: TaxRateFlatAmount option
        ///Unique identifier for the object.
        Id: string
        ///This specifies if the tax rate is inclusive or exclusive.
        Inclusive: bool
        ///The jurisdiction for the tax rate. You can use this label field for tax reporting purposes. It also appears on your customer’s invoice.
        Jurisdiction: string option
        ///The level of the jurisdiction that imposes this tax rate. Will be `null` for manually defined tax rates.
        JurisdictionLevel: TaxRateJurisdictionLevel option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///Tax rate percentage out of 100. For tax calculations with automatic_tax[enabled]=true, this percentage includes the statutory tax rate of non-taxable jurisdictions.
        Percentage: decimal
        ///Indicates the type of tax rate applied to the taxable amount. This value can be `null` when no tax applies to the location. This field is only present for TaxRates created by Stripe Tax.
        RateType: TaxRateRateType option
        ///[ISO 3166-2 subdivision code](https://en.wikipedia.org/wiki/ISO_3166-2), without country prefix. For example, "NY" for New York, United States.
        State: string option
        ///The high-level tax type, such as `vat` or `sales_tax`.
        TaxType: TaxRateTaxType option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "tax_rate"

        static member New (active: bool, country: IsoTypes.IsoCountryCode option, created: DateTime, description: string option, displayName: string, effectivePercentage: decimal option, flatAmount: TaxRateFlatAmount option, id: string, inclusive: bool, jurisdiction: string option, jurisdictionLevel: TaxRateJurisdictionLevel option, livemode: bool, metadata: Map<string, string> option, percentage: decimal, rateType: TaxRateRateType option, state: string option, taxType: TaxRateTaxType option) =
            {
                TaxRate.Active = active //required
                TaxRate.Country = country //required
                TaxRate.Created = created //required
                TaxRate.Description = description //required
                TaxRate.DisplayName = displayName //required
                TaxRate.EffectivePercentage = effectivePercentage //required
                TaxRate.FlatAmount = flatAmount //required
                TaxRate.Id = id //required
                TaxRate.Inclusive = inclusive //required
                TaxRate.Jurisdiction = jurisdiction //required
                TaxRate.JurisdictionLevel = jurisdictionLevel //required
                TaxRate.Livemode = livemode //required
                TaxRate.Metadata = metadata //required
                TaxRate.Percentage = percentage //required
                TaxRate.RateType = rateType //required
                TaxRate.State = state //required
                TaxRate.TaxType = taxType //required
            }

    ///The amount of the tax rate when the `rate_type`` is `flat_amount`. Tax rates with `rate_type` `percentage` can vary based on the transaction, resulting in this field being `null`. This field exposes the amount and currency of the flat tax rate.
    and TaxRateFlatAmount = {
        ///Amount of the tax when the `rate_type` is `flat_amount`. This positive integer represents how much to charge in the smallest currency unit (e.g., 100 cents to charge $1.00 or 100 to charge ¥100, a zero-decimal currency). The amount value supports up to eight digits (e.g., a value of 99999999 for a USD charge of $999,999.99).
        Amount: int
        ///Three-letter ISO currency code, in lowercase.
        Currency: IsoTypes.IsoCurrencyCode
    }
    with
        static member New (amount: int, currency: IsoTypes.IsoCurrencyCode) =
            {
                TaxRateFlatAmount.Amount = amount //required
                TaxRateFlatAmount.Currency = currency //required
            }

    ///A Configurations object represents how features should be configured for terminal readers.
    ///For information about how to use it, see the [Terminal configurations documentation](https://docs.stripe.com/terminal/fleet/configurations-overview).
    and TerminalConfiguration = {
        [<JsonField(Name="bbpos_wisepad3")>]BbposWisepad3: TerminalConfigurationConfigurationResourceDeviceTypeSpecificConfig option
        BbposWiseposE: TerminalConfigurationConfigurationResourceDeviceTypeSpecificConfig option
        Cellular: TerminalConfigurationConfigurationResourceCellularConfig option
        ///Unique identifier for the object.
        Id: string
        ///Whether this Configuration is the default for your account
        IsAccountDefault: bool option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///String indicating the name of the Configuration object, set by the user
        Name: string option
        Offline: TerminalConfigurationConfigurationResourceOfflineConfig option
        RebootWindow: TerminalConfigurationConfigurationResourceRebootWindow option
        [<JsonField(Name="stripe_s700")>]StripeS700: TerminalConfigurationConfigurationResourceDeviceTypeSpecificConfig option
        [<JsonField(Name="stripe_s710")>]StripeS710: TerminalConfigurationConfigurationResourceDeviceTypeSpecificConfig option
        Tipping: TerminalConfigurationConfigurationResourceTipping option
        [<JsonField(Name="verifone_p400")>]VerifoneP400: TerminalConfigurationConfigurationResourceDeviceTypeSpecificConfig option
        Wifi: TerminalConfigurationConfigurationResourceWifiConfig option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "terminal.configuration"

        static member New (id: string, isAccountDefault: bool option, livemode: bool, name: string option, ?bbposWisepad3: TerminalConfigurationConfigurationResourceDeviceTypeSpecificConfig, ?bbposWiseposE: TerminalConfigurationConfigurationResourceDeviceTypeSpecificConfig, ?cellular: TerminalConfigurationConfigurationResourceCellularConfig, ?offline: TerminalConfigurationConfigurationResourceOfflineConfig, ?rebootWindow: TerminalConfigurationConfigurationResourceRebootWindow, ?stripeS700: TerminalConfigurationConfigurationResourceDeviceTypeSpecificConfig, ?stripeS710: TerminalConfigurationConfigurationResourceDeviceTypeSpecificConfig, ?tipping: TerminalConfigurationConfigurationResourceTipping, ?verifoneP400: TerminalConfigurationConfigurationResourceDeviceTypeSpecificConfig, ?wifi: TerminalConfigurationConfigurationResourceWifiConfig) =
            {
                TerminalConfiguration.Id = id //required
                TerminalConfiguration.IsAccountDefault = isAccountDefault //required
                TerminalConfiguration.Livemode = livemode //required
                TerminalConfiguration.Name = name //required
                TerminalConfiguration.BbposWisepad3 = bbposWisepad3
                TerminalConfiguration.BbposWiseposE = bbposWiseposE
                TerminalConfiguration.Cellular = cellular
                TerminalConfiguration.Offline = offline
                TerminalConfiguration.RebootWindow = rebootWindow
                TerminalConfiguration.StripeS700 = stripeS700
                TerminalConfiguration.StripeS710 = stripeS710
                TerminalConfiguration.Tipping = tipping
                TerminalConfiguration.VerifoneP400 = verifoneP400
                TerminalConfiguration.Wifi = wifi
            }

    ///A Location represents a grouping of readers.
    ///Related guide: [Fleet management](https://docs.stripe.com/terminal/fleet/locations)
    and TerminalLocation = {
        Address: Address
        AddressKana: LegalEntityJapanAddress option
        AddressKanji: LegalEntityJapanAddress option
        ///The ID of a configuration that will be used to customize all readers in this location.
        ConfigurationOverrides: string option
        ///The display name of the location.
        DisplayName: string
        ///The Kana variation of the display name of the location.
        DisplayNameKana: string option
        ///The Kanji variation of the display name of the location.
        DisplayNameKanji: string option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///The phone number of the location.
        Phone: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "terminal.location"

        static member New (address: Address, displayName: string, id: string, livemode: bool, metadata: Map<string, string>, ?addressKana: LegalEntityJapanAddress, ?addressKanji: LegalEntityJapanAddress, ?configurationOverrides: string, ?displayNameKana: string, ?displayNameKanji: string, ?phone: string) =
            {
                TerminalLocation.Address = address //required
                TerminalLocation.DisplayName = displayName //required
                TerminalLocation.Id = id //required
                TerminalLocation.Livemode = livemode //required
                TerminalLocation.Metadata = metadata //required
                TerminalLocation.AddressKana = addressKana
                TerminalLocation.AddressKanji = addressKanji
                TerminalLocation.ConfigurationOverrides = configurationOverrides
                TerminalLocation.DisplayNameKana = displayNameKana
                TerminalLocation.DisplayNameKanji = displayNameKanji
                TerminalLocation.Phone = phone
            }

    ///Returns redirect links used for onboarding onto Tap to Pay on iPhone.
    and TerminalOnboardingLink = {
        LinkOptions: TerminalOnboardingLinkLinkOptions
        ///Stripe account ID to generate the link for.
        OnBehalfOf: string option
        ///The link passed back to the user for their onboarding.
        RedirectUrl: string
    }
    with
        ///The type of link being generated.
        member _.LinkType = "apple_terms_and_conditions"
        member _.Object = "terminal.onboarding_link"

        static member New (linkOptions: TerminalOnboardingLinkLinkOptions, onBehalfOf: string option, redirectUrl: string) =
            {
                TerminalOnboardingLink.LinkOptions = linkOptions //required
                TerminalOnboardingLink.OnBehalfOf = onBehalfOf //required
                TerminalOnboardingLink.RedirectUrl = redirectUrl //required
            }

    ///A Reader represents a physical device for accepting payment details.
    ///Related guide: [Connecting to a reader](https://docs.stripe.com/terminal/payments/connect-reader)
    and TerminalReader = {
        ///The most recent action performed by the reader.
        Action: TerminalReaderReaderResourceReaderAction option
        ///The current software version of the reader.
        DeviceSwVersion: string option
        ///Device type of the reader.
        DeviceType: TerminalReaderDeviceType
        ///Unique identifier for the object.
        Id: string
        ///The local IP address of the reader.
        IpAddress: string option
        ///Custom label given to the reader for easier identification.
        Label: string
        ///The last time this reader reported to Stripe backend. Timestamp is measured in milliseconds since the Unix epoch. Unlike most other Stripe timestamp fields which use seconds, this field uses milliseconds.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]LastSeenAt: DateTime option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The location identifier of the reader.
        Location: TerminalReaderLocation'AnyOf option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///Serial number of the reader.
        SerialNumber: string
        ///The networking status of the reader. We do not recommend using this field in flows that may block taking payments.
        Status: TerminalReaderStatus option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "terminal.reader"

        static member New (action: TerminalReaderReaderResourceReaderAction option, deviceSwVersion: string option, deviceType: TerminalReaderDeviceType, id: string, ipAddress: string option, label: string, lastSeenAt: DateTime option, livemode: bool, location: TerminalReaderLocation'AnyOf option, metadata: Map<string, string>, serialNumber: string, status: TerminalReaderStatus option) =
            {
                TerminalReader.Action = action //required
                TerminalReader.DeviceSwVersion = deviceSwVersion //required
                TerminalReader.DeviceType = deviceType //required
                TerminalReader.Id = id //required
                TerminalReader.IpAddress = ipAddress //required
                TerminalReader.Label = label //required
                TerminalReader.LastSeenAt = lastSeenAt //required
                TerminalReader.Livemode = livemode //required
                TerminalReader.Location = location //required
                TerminalReader.Metadata = metadata //required
                TerminalReader.SerialNumber = serialNumber //required
                TerminalReader.Status = status //required
            }

    and TerminalReaderLocation'AnyOf =
        | String of string
        | TerminalLocation of TerminalLocation

    and TerminalConfigurationConfigurationResourceCurrencySpecificConfig = {
        ///Fixed amounts displayed when collecting a tip
        FixedAmounts: int list option
        ///Percentages displayed when collecting a tip
        Percentages: int list option
        ///Below this amount, fixed amounts will be displayed; above it, percentages will be displayed
        SmartTipThreshold: int option
    }
    with
        static member New (?fixedAmounts: int list option, ?percentages: int list option, ?smartTipThreshold: int) =
            {
                TerminalConfigurationConfigurationResourceCurrencySpecificConfig.FixedAmounts = fixedAmounts |> Option.flatten
                TerminalConfigurationConfigurationResourceCurrencySpecificConfig.Percentages = percentages |> Option.flatten
                TerminalConfigurationConfigurationResourceCurrencySpecificConfig.SmartTipThreshold = smartTipThreshold
            }

    and TerminalConfigurationConfigurationResourceDeviceTypeSpecificConfig = {
        ///A File ID representing an image to display on the reader
        Splashscreen: TerminalConfigurationConfigurationResourceDeviceTypeSpecificConfigSplashscreen'AnyOf option
    }
    with
        static member New (?splashscreen: TerminalConfigurationConfigurationResourceDeviceTypeSpecificConfigSplashscreen'AnyOf) =
            {
                TerminalConfigurationConfigurationResourceDeviceTypeSpecificConfig.Splashscreen = splashscreen
            }

    and TerminalConfigurationConfigurationResourceDeviceTypeSpecificConfigSplashscreen'AnyOf =
        | String of string
        | File of File

    and TerminalConfigurationConfigurationResourceTipping = {
        Aed: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Aud: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Cad: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Chf: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Czk: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Dkk: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Eur: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Gbp: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Gip: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Hkd: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Huf: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Jpy: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Mxn: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Myr: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Nok: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Nzd: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Pln: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Ron: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Sek: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Sgd: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
        Usd: TerminalConfigurationConfigurationResourceCurrencySpecificConfig option
    }
    with
        static member New (?aed: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?aud: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?cad: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?chf: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?czk: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?dkk: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?eur: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?gbp: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?gip: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?hkd: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?huf: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?jpy: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?mxn: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?myr: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?nok: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?nzd: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?pln: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?ron: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?sek: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?sgd: TerminalConfigurationConfigurationResourceCurrencySpecificConfig, ?usd: TerminalConfigurationConfigurationResourceCurrencySpecificConfig) =
            {
                TerminalConfigurationConfigurationResourceTipping.Aed = aed
                TerminalConfigurationConfigurationResourceTipping.Aud = aud
                TerminalConfigurationConfigurationResourceTipping.Cad = cad
                TerminalConfigurationConfigurationResourceTipping.Chf = chf
                TerminalConfigurationConfigurationResourceTipping.Czk = czk
                TerminalConfigurationConfigurationResourceTipping.Dkk = dkk
                TerminalConfigurationConfigurationResourceTipping.Eur = eur
                TerminalConfigurationConfigurationResourceTipping.Gbp = gbp
                TerminalConfigurationConfigurationResourceTipping.Gip = gip
                TerminalConfigurationConfigurationResourceTipping.Hkd = hkd
                TerminalConfigurationConfigurationResourceTipping.Huf = huf
                TerminalConfigurationConfigurationResourceTipping.Jpy = jpy
                TerminalConfigurationConfigurationResourceTipping.Mxn = mxn
                TerminalConfigurationConfigurationResourceTipping.Myr = myr
                TerminalConfigurationConfigurationResourceTipping.Nok = nok
                TerminalConfigurationConfigurationResourceTipping.Nzd = nzd
                TerminalConfigurationConfigurationResourceTipping.Pln = pln
                TerminalConfigurationConfigurationResourceTipping.Ron = ron
                TerminalConfigurationConfigurationResourceTipping.Sek = sek
                TerminalConfigurationConfigurationResourceTipping.Sgd = sgd
                TerminalConfigurationConfigurationResourceTipping.Usd = usd
            }

    and TerminalConfigurationConfigurationResourceWifiConfig = {
        EnterpriseEapPeap: TerminalConfigurationConfigurationResourceEnterprisePeapWifi option
        EnterpriseEapTls: TerminalConfigurationConfigurationResourceEnterpriseTlsWifi option
        PersonalPsk: TerminalConfigurationConfigurationResourcePersonalPskWifi option
        ///Security type of the WiFi network. The hash with the corresponding name contains the credentials for this security type.
        Type: TerminalConfigurationConfigurationResourceWifiConfigType
    }
    with
        static member New (``type``: TerminalConfigurationConfigurationResourceWifiConfigType, ?enterpriseEapPeap: TerminalConfigurationConfigurationResourceEnterprisePeapWifi, ?enterpriseEapTls: TerminalConfigurationConfigurationResourceEnterpriseTlsWifi, ?personalPsk: TerminalConfigurationConfigurationResourcePersonalPskWifi) =
            {
                TerminalConfigurationConfigurationResourceWifiConfig.Type = ``type`` //required
                TerminalConfigurationConfigurationResourceWifiConfig.EnterpriseEapPeap = enterpriseEapPeap
                TerminalConfigurationConfigurationResourceWifiConfig.EnterpriseEapTls = enterpriseEapTls
                TerminalConfigurationConfigurationResourceWifiConfig.PersonalPsk = personalPsk
            }

    ///Link type options associated with the current onboarding link object.
    and TerminalOnboardingLinkLinkOptions = {
        ///The options associated with the Apple Terms and Conditions link type.
        AppleTermsAndConditions: TerminalOnboardingLinkAppleTermsAndConditions option
    }
    with
        static member New (appleTermsAndConditions: TerminalOnboardingLinkAppleTermsAndConditions option) =
            {
                TerminalOnboardingLinkLinkOptions.AppleTermsAndConditions = appleTermsAndConditions //required
            }

    ///Represents a cart to be displayed on the reader
    and TerminalReaderReaderResourceCart = {
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///List of line items in the cart.
        LineItems: TerminalReaderReaderResourceLineItem list
        ///Tax amount for the entire cart. A positive integer in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
        Tax: int option
        ///Total amount for the entire cart, including tax. A positive integer in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal).
        Total: int
    }
    with
        static member New (currency: IsoTypes.IsoCurrencyCode, lineItems: TerminalReaderReaderResourceLineItem list, tax: int option, total: int) =
            {
                TerminalReaderReaderResourceCart.Currency = currency //required
                TerminalReaderReaderResourceCart.LineItems = lineItems //required
                TerminalReaderReaderResourceCart.Tax = tax //required
                TerminalReaderReaderResourceCart.Total = total //required
            }

    ///Choice to be selected on a Reader
    and TerminalReaderReaderResourceChoice = {
        ///The identifier for the selected choice. Maximum 50 characters.
        Id: string option
        ///The button style for the choice. Can be `primary` or `secondary`.
        Style: TerminalReaderReaderResourceChoiceStyle option
        ///The text to be selected. Maximum 30 characters.
        Text: string
    }
    with
        static member New (id: string option, style: TerminalReaderReaderResourceChoiceStyle option, text: string) =
            {
                TerminalReaderReaderResourceChoice.Id = id //required
                TerminalReaderReaderResourceChoice.Style = style //required
                TerminalReaderReaderResourceChoice.Text = text //required
            }

    ///Represents a per-transaction override of a reader configuration
    and TerminalReaderReaderResourceCollectConfig = {
        ///Enable customer-initiated cancellation when processing this payment.
        EnableCustomerCancellation: bool option
        ///Override showing a tipping selection screen on this transaction.
        SkipTipping: bool option
        Tipping: TerminalReaderReaderResourceTippingConfig option
    }
    with
        static member New (?enableCustomerCancellation: bool, ?skipTipping: bool, ?tipping: TerminalReaderReaderResourceTippingConfig) =
            {
                TerminalReaderReaderResourceCollectConfig.EnableCustomerCancellation = enableCustomerCancellation
                TerminalReaderReaderResourceCollectConfig.SkipTipping = skipTipping
                TerminalReaderReaderResourceCollectConfig.Tipping = tipping
            }

    ///Represents a reader action to collect customer inputs
    and TerminalReaderReaderResourceCollectInputsAction = {
        ///List of inputs to be collected.
        Inputs: TerminalReaderReaderResourceInput list
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
    }
    with
        static member New (inputs: TerminalReaderReaderResourceInput list, metadata: Map<string, string> option) =
            {
                TerminalReaderReaderResourceCollectInputsAction.Inputs = inputs //required
                TerminalReaderReaderResourceCollectInputsAction.Metadata = metadata //required
            }

    ///Represents a reader action to collect a payment method
    and TerminalReaderReaderResourceCollectPaymentMethodAction = {
        CollectConfig: TerminalReaderReaderResourceCollectConfig option
        ///Most recent PaymentIntent processed by the reader.
        PaymentIntent: TerminalReaderReaderResourceCollectPaymentMethodActionPaymentIntent'AnyOf
        PaymentMethod: PaymentMethod option
    }
    with
        static member New (paymentIntent: TerminalReaderReaderResourceCollectPaymentMethodActionPaymentIntent'AnyOf, ?collectConfig: TerminalReaderReaderResourceCollectConfig, ?paymentMethod: PaymentMethod) =
            {
                TerminalReaderReaderResourceCollectPaymentMethodAction.PaymentIntent = paymentIntent //required
                TerminalReaderReaderResourceCollectPaymentMethodAction.CollectConfig = collectConfig
                TerminalReaderReaderResourceCollectPaymentMethodAction.PaymentMethod = paymentMethod
            }

    and TerminalReaderReaderResourceCollectPaymentMethodActionPaymentIntent'AnyOf =
        | String of string
        | PaymentIntent of PaymentIntent

    ///Represents a reader action to confirm a payment
    and TerminalReaderReaderResourceConfirmPaymentIntentAction = {
        ConfirmConfig: TerminalReaderReaderResourceConfirmConfig option
        ///Most recent PaymentIntent processed by the reader.
        PaymentIntent: TerminalReaderReaderResourceConfirmPaymentIntentActionPaymentIntent'AnyOf
    }
    with
        static member New (paymentIntent: TerminalReaderReaderResourceConfirmPaymentIntentActionPaymentIntent'AnyOf, ?confirmConfig: TerminalReaderReaderResourceConfirmConfig) =
            {
                TerminalReaderReaderResourceConfirmPaymentIntentAction.PaymentIntent = paymentIntent //required
                TerminalReaderReaderResourceConfirmPaymentIntentAction.ConfirmConfig = confirmConfig
            }

    and TerminalReaderReaderResourceConfirmPaymentIntentActionPaymentIntent'AnyOf =
        | String of string
        | PaymentIntent of PaymentIntent

    ///Represents an input to be collected using the reader
    and TerminalReaderReaderResourceInput = {
        ///Default text of input being collected.
        CustomText: TerminalReaderReaderResourceCustomText option
        Email: TerminalReaderReaderResourceEmail option
        Numeric: TerminalReaderReaderResourceNumeric option
        Phone: TerminalReaderReaderResourcePhone option
        ///Indicate that this input is required, disabling the skip button.
        Required: bool option
        Selection: TerminalReaderReaderResourceSelection option
        Signature: TerminalReaderReaderResourceSignature option
        ///Indicate that this input was skipped by the user.
        Skipped: bool option
        Text: TerminalReaderReaderResourceText option
        ///List of toggles being collected. Values are present if collection is complete.
        Toggles: TerminalReaderReaderResourceToggle list option
        ///Type of input being collected.
        Type: TerminalReaderReaderResourceInputType
    }
    with
        static member New (customText: TerminalReaderReaderResourceCustomText option, required: bool option, toggles: TerminalReaderReaderResourceToggle list option, ``type``: TerminalReaderReaderResourceInputType, ?email: TerminalReaderReaderResourceEmail, ?numeric: TerminalReaderReaderResourceNumeric, ?phone: TerminalReaderReaderResourcePhone, ?selection: TerminalReaderReaderResourceSelection, ?signature: TerminalReaderReaderResourceSignature, ?skipped: bool, ?text: TerminalReaderReaderResourceText) =
            {
                TerminalReaderReaderResourceInput.CustomText = customText //required
                TerminalReaderReaderResourceInput.Required = required //required
                TerminalReaderReaderResourceInput.Toggles = toggles //required
                TerminalReaderReaderResourceInput.Type = ``type`` //required
                TerminalReaderReaderResourceInput.Email = email
                TerminalReaderReaderResourceInput.Numeric = numeric
                TerminalReaderReaderResourceInput.Phone = phone
                TerminalReaderReaderResourceInput.Selection = selection
                TerminalReaderReaderResourceInput.Signature = signature
                TerminalReaderReaderResourceInput.Skipped = skipped
                TerminalReaderReaderResourceInput.Text = text
            }

    ///Represents a per-transaction override of a reader configuration
    and TerminalReaderReaderResourceProcessConfig = {
        ///Enable customer-initiated cancellation when processing this payment.
        EnableCustomerCancellation: bool option
        ///If the customer doesn't abandon authenticating the payment, they're redirected to this URL after completion.
        ReturnUrl: string option
        ///Override showing a tipping selection screen on this transaction.
        SkipTipping: bool option
        Tipping: TerminalReaderReaderResourceTippingConfig option
    }
    with
        static member New (?enableCustomerCancellation: bool, ?returnUrl: string, ?skipTipping: bool, ?tipping: TerminalReaderReaderResourceTippingConfig) =
            {
                TerminalReaderReaderResourceProcessConfig.EnableCustomerCancellation = enableCustomerCancellation
                TerminalReaderReaderResourceProcessConfig.ReturnUrl = returnUrl
                TerminalReaderReaderResourceProcessConfig.SkipTipping = skipTipping
                TerminalReaderReaderResourceProcessConfig.Tipping = tipping
            }

    ///Represents a reader action to process a payment intent
    and TerminalReaderReaderResourceProcessPaymentIntentAction = {
        ///Most recent PaymentIntent processed by the reader.
        PaymentIntent: TerminalReaderReaderResourceProcessPaymentIntentActionPaymentIntent'AnyOf
        ProcessConfig: TerminalReaderReaderResourceProcessConfig option
    }
    with
        static member New (paymentIntent: TerminalReaderReaderResourceProcessPaymentIntentActionPaymentIntent'AnyOf, ?processConfig: TerminalReaderReaderResourceProcessConfig) =
            {
                TerminalReaderReaderResourceProcessPaymentIntentAction.PaymentIntent = paymentIntent //required
                TerminalReaderReaderResourceProcessPaymentIntentAction.ProcessConfig = processConfig
            }

    and TerminalReaderReaderResourceProcessPaymentIntentActionPaymentIntent'AnyOf =
        | String of string
        | PaymentIntent of PaymentIntent

    ///Represents a reader action to process a setup intent
    and TerminalReaderReaderResourceProcessSetupIntentAction = {
        ///ID of a card PaymentMethod generated from the card_present PaymentMethod that may be attached to a Customer for future transactions. Only present if it was possible to generate a card PaymentMethod.
        GeneratedCard: string option
        ProcessConfig: TerminalReaderReaderResourceProcessSetupConfig option
        ///Most recent SetupIntent processed by the reader.
        SetupIntent: TerminalReaderReaderResourceProcessSetupIntentActionSetupIntent'AnyOf
    }
    with
        static member New (setupIntent: TerminalReaderReaderResourceProcessSetupIntentActionSetupIntent'AnyOf, ?generatedCard: string, ?processConfig: TerminalReaderReaderResourceProcessSetupConfig) =
            {
                TerminalReaderReaderResourceProcessSetupIntentAction.SetupIntent = setupIntent //required
                TerminalReaderReaderResourceProcessSetupIntentAction.GeneratedCard = generatedCard
                TerminalReaderReaderResourceProcessSetupIntentAction.ProcessConfig = processConfig
            }

    and TerminalReaderReaderResourceProcessSetupIntentActionSetupIntent'AnyOf =
        | String of string
        | SetupIntent of SetupIntent

    ///Represents an action performed by the reader
    and TerminalReaderReaderResourceReaderAction = {
        CollectInputs: TerminalReaderReaderResourceCollectInputsAction option
        CollectPaymentMethod: TerminalReaderReaderResourceCollectPaymentMethodAction option
        ConfirmPaymentIntent: TerminalReaderReaderResourceConfirmPaymentIntentAction option
        ///Failure code, only set if status is `failed`.
        FailureCode: string option
        ///Detailed failure message, only set if status is `failed`.
        FailureMessage: string option
        ProcessPaymentIntent: TerminalReaderReaderResourceProcessPaymentIntentAction option
        ProcessSetupIntent: TerminalReaderReaderResourceProcessSetupIntentAction option
        RefundPayment: TerminalReaderReaderResourceRefundPaymentAction option
        SetReaderDisplay: TerminalReaderReaderResourceSetReaderDisplayAction option
        ///Status of the action performed by the reader.
        Status: TerminalReaderReaderResourceReaderActionStatus
        ///Type of action performed by the reader.
        Type: TerminalReaderReaderResourceReaderActionType
    }
    with
        static member New (failureCode: string option, failureMessage: string option, status: TerminalReaderReaderResourceReaderActionStatus, ``type``: TerminalReaderReaderResourceReaderActionType, ?collectInputs: TerminalReaderReaderResourceCollectInputsAction, ?collectPaymentMethod: TerminalReaderReaderResourceCollectPaymentMethodAction, ?confirmPaymentIntent: TerminalReaderReaderResourceConfirmPaymentIntentAction, ?processPaymentIntent: TerminalReaderReaderResourceProcessPaymentIntentAction, ?processSetupIntent: TerminalReaderReaderResourceProcessSetupIntentAction, ?refundPayment: TerminalReaderReaderResourceRefundPaymentAction, ?setReaderDisplay: TerminalReaderReaderResourceSetReaderDisplayAction) =
            {
                TerminalReaderReaderResourceReaderAction.FailureCode = failureCode //required
                TerminalReaderReaderResourceReaderAction.FailureMessage = failureMessage //required
                TerminalReaderReaderResourceReaderAction.Status = status //required
                TerminalReaderReaderResourceReaderAction.Type = ``type`` //required
                TerminalReaderReaderResourceReaderAction.CollectInputs = collectInputs
                TerminalReaderReaderResourceReaderAction.CollectPaymentMethod = collectPaymentMethod
                TerminalReaderReaderResourceReaderAction.ConfirmPaymentIntent = confirmPaymentIntent
                TerminalReaderReaderResourceReaderAction.ProcessPaymentIntent = processPaymentIntent
                TerminalReaderReaderResourceReaderAction.ProcessSetupIntent = processSetupIntent
                TerminalReaderReaderResourceReaderAction.RefundPayment = refundPayment
                TerminalReaderReaderResourceReaderAction.SetReaderDisplay = setReaderDisplay
            }

    ///Represents a reader action to refund a payment
    and TerminalReaderReaderResourceRefundPaymentAction = {
        ///The amount being refunded.
        Amount: int option
        ///Charge that is being refunded.
        Charge: TerminalReaderReaderResourceRefundPaymentActionCharge'AnyOf option
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///Payment intent that is being refunded.
        PaymentIntent: TerminalReaderReaderResourceRefundPaymentActionPaymentIntent'AnyOf option
        ///The reason for the refund.
        Reason: TerminalReaderReaderResourceRefundPaymentActionReason option
        ///Unique identifier for the refund object.
        Refund: TerminalReaderReaderResourceRefundPaymentActionRefund'AnyOf option
        ///Boolean indicating whether the application fee should be refunded when refunding this charge. If a full charge refund is given, the full application fee will be refunded. Otherwise, the application fee will be refunded in an amount proportional to the amount of the charge refunded. An application fee can be refunded only by the application that created the charge.
        RefundApplicationFee: bool option
        RefundPaymentConfig: TerminalReaderReaderResourceRefundPaymentConfig option
        ///Boolean indicating whether the transfer should be reversed when refunding this charge. The transfer will be reversed proportionally to the amount being refunded (either the entire or partial amount). A transfer can be reversed only by the application that created the charge.
        ReverseTransfer: bool option
    }
    with
        static member New (?amount: int, ?charge: TerminalReaderReaderResourceRefundPaymentActionCharge'AnyOf, ?metadata: Map<string, string>, ?paymentIntent: TerminalReaderReaderResourceRefundPaymentActionPaymentIntent'AnyOf, ?reason: TerminalReaderReaderResourceRefundPaymentActionReason, ?refund: TerminalReaderReaderResourceRefundPaymentActionRefund'AnyOf, ?refundApplicationFee: bool, ?refundPaymentConfig: TerminalReaderReaderResourceRefundPaymentConfig, ?reverseTransfer: bool) =
            {
                TerminalReaderReaderResourceRefundPaymentAction.Amount = amount
                TerminalReaderReaderResourceRefundPaymentAction.Charge = charge
                TerminalReaderReaderResourceRefundPaymentAction.Metadata = metadata
                TerminalReaderReaderResourceRefundPaymentAction.PaymentIntent = paymentIntent
                TerminalReaderReaderResourceRefundPaymentAction.Reason = reason
                TerminalReaderReaderResourceRefundPaymentAction.Refund = refund
                TerminalReaderReaderResourceRefundPaymentAction.RefundApplicationFee = refundApplicationFee
                TerminalReaderReaderResourceRefundPaymentAction.RefundPaymentConfig = refundPaymentConfig
                TerminalReaderReaderResourceRefundPaymentAction.ReverseTransfer = reverseTransfer
            }

    and TerminalReaderReaderResourceRefundPaymentActionCharge'AnyOf =
        | String of string
        | Charge of Charge

    and TerminalReaderReaderResourceRefundPaymentActionPaymentIntent'AnyOf =
        | String of string
        | PaymentIntent of PaymentIntent

    and TerminalReaderReaderResourceRefundPaymentActionRefund'AnyOf =
        | String of string
        | Refund of Refund

    ///Information about a selection being collected using a reader
    and TerminalReaderReaderResourceSelection = {
        ///List of possible choices to be selected
        Choices: TerminalReaderReaderResourceChoice list
        ///The id of the selected choice
        Id: string option
        ///The text of the selected choice
        Text: string option
    }
    with
        static member New (choices: TerminalReaderReaderResourceChoice list, id: string option, text: string option) =
            {
                TerminalReaderReaderResourceSelection.Choices = choices //required
                TerminalReaderReaderResourceSelection.Id = id //required
                TerminalReaderReaderResourceSelection.Text = text //required
            }

    ///Represents a reader action to set the reader display
    and TerminalReaderReaderResourceSetReaderDisplayAction = {
        ///Cart object to be displayed by the reader, including line items, amounts, and currency.
        Cart: TerminalReaderReaderResourceCart option
    }
    with
        ///Type of information to be displayed by the reader. Only `cart` is currently supported.
        member _.Type = "cart"

        static member New (cart: TerminalReaderReaderResourceCart option) =
            {
                TerminalReaderReaderResourceSetReaderDisplayAction.Cart = cart //required
            }

    ///Information about an input's toggle
    and TerminalReaderReaderResourceToggle = {
        ///The toggle's default value. Can be `enabled` or `disabled`.
        DefaultValue: TerminalReaderReaderResourceToggleDefaultValue option
        ///The toggle's description text. Maximum 50 characters.
        Description: string option
        ///The toggle's title text. Maximum 50 characters.
        Title: string option
        ///The toggle's collected value. Can be `enabled` or `disabled`.
        Value: TerminalReaderReaderResourceToggleValue option
    }
    with
        static member New (defaultValue: TerminalReaderReaderResourceToggleDefaultValue option, description: string option, title: string option, value: TerminalReaderReaderResourceToggleValue option) =
            {
                TerminalReaderReaderResourceToggle.DefaultValue = defaultValue //required
                TerminalReaderReaderResourceToggle.Description = description //required
                TerminalReaderReaderResourceToggle.Title = title //required
                TerminalReaderReaderResourceToggle.Value = value //required
            }

    ///A test clock enables deterministic control over objects in testmode. With a test clock, you can create
    ///objects at a frozen time in the past or future, and advance to a specific future time to observe webhooks and state changes. After the clock advances,
    ///you can either validate the current state of your scenario (and test your assumptions), change the current state of your scenario (and test more complex scenarios), or keep advancing forward in time.
    and TestHelpersTestClock = {
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Time at which this clock is scheduled to auto delete.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]DeletesAfter: DateTime
        ///Time at which all objects belonging to this clock are frozen.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]FrozenTime: DateTime
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The custom name supplied at creation.
        Name: string option
        ///The status of the Test Clock.
        Status: TestHelpersTestClockStatus
        StatusDetails: BillingClocksResourceStatusDetailsStatusDetails
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "test_helpers.test_clock"

        static member New (created: DateTime, deletesAfter: DateTime, frozenTime: DateTime, id: string, livemode: bool, name: string option, status: TestHelpersTestClockStatus, statusDetails: BillingClocksResourceStatusDetailsStatusDetails) =
            {
                TestHelpersTestClock.Created = created //required
                TestHelpersTestClock.DeletesAfter = deletesAfter //required
                TestHelpersTestClock.FrozenTime = frozenTime //required
                TestHelpersTestClock.Id = id //required
                TestHelpersTestClock.Livemode = livemode //required
                TestHelpersTestClock.Name = name //required
                TestHelpersTestClock.Status = status //required
                TestHelpersTestClock.StatusDetails = statusDetails //required
            }

    and ThreeDSecureDetails = {
        ///For authenticated transactions: how the customer was authenticated by
        ///the issuing bank.
        AuthenticationFlow: ThreeDSecureDetailsAuthenticationFlow option
        ///The Electronic Commerce Indicator (ECI). A protocol-level field
        ///indicating what degree of authentication was performed.
        ElectronicCommerceIndicator: ThreeDSecureDetailsElectronicCommerceIndicator option
        ///Indicates the outcome of 3D Secure authentication.
        Result: ThreeDSecureDetailsResult option
        ///Additional information about why 3D Secure succeeded or failed based
        ///on the `result`.
        ResultReason: ThreeDSecureDetailsResultReason option
        ///The 3D Secure 1 XID or 3D Secure 2 Directory Server Transaction ID
        ///(dsTransId) for this payment.
        TransactionId: string option
        ///The version of 3D Secure that was used.
        Version: ThreeDSecureDetailsVersion option
    }
    with
        static member New (authenticationFlow: ThreeDSecureDetailsAuthenticationFlow option, electronicCommerceIndicator: ThreeDSecureDetailsElectronicCommerceIndicator option, result: ThreeDSecureDetailsResult option, resultReason: ThreeDSecureDetailsResultReason option, transactionId: string option, version: ThreeDSecureDetailsVersion option) =
            {
                ThreeDSecureDetails.AuthenticationFlow = authenticationFlow //required
                ThreeDSecureDetails.ElectronicCommerceIndicator = electronicCommerceIndicator //required
                ThreeDSecureDetails.Result = result //required
                ThreeDSecureDetails.ResultReason = resultReason //required
                ThreeDSecureDetails.TransactionId = transactionId //required
                ThreeDSecureDetails.Version = version //required
            }

    and ThreeDSecureDetailsCharge = {
        ///For authenticated transactions: how the customer was authenticated by
        ///the issuing bank.
        AuthenticationFlow: ThreeDSecureDetailsChargeAuthenticationFlow option
        ///The Electronic Commerce Indicator (ECI). A protocol-level field
        ///indicating what degree of authentication was performed.
        ElectronicCommerceIndicator: ThreeDSecureDetailsChargeElectronicCommerceIndicator option
        ///The exemption requested via 3DS and accepted by the issuer at authentication time.
        ExemptionIndicator: ThreeDSecureDetailsChargeExemptionIndicator option
        ///Whether Stripe requested the value of `exemption_indicator` in the transaction. This will depend on
        ///the outcome of Stripe's internal risk assessment.
        ExemptionIndicatorApplied: bool option
        ///Indicates the outcome of 3D Secure authentication.
        Result: ThreeDSecureDetailsChargeResult option
        ///Additional information about why 3D Secure succeeded or failed based
        ///on the `result`.
        ResultReason: ThreeDSecureDetailsChargeResultReason option
        ///The 3D Secure 1 XID or 3D Secure 2 Directory Server Transaction ID
        ///(dsTransId) for this payment.
        TransactionId: string option
        ///The version of 3D Secure that was used.
        Version: ThreeDSecureDetailsChargeVersion option
    }
    with
        static member New (authenticationFlow: ThreeDSecureDetailsChargeAuthenticationFlow option, electronicCommerceIndicator: ThreeDSecureDetailsChargeElectronicCommerceIndicator option, exemptionIndicator: ThreeDSecureDetailsChargeExemptionIndicator option, result: ThreeDSecureDetailsChargeResult option, resultReason: ThreeDSecureDetailsChargeResultReason option, transactionId: string option, version: ThreeDSecureDetailsChargeVersion option, ?exemptionIndicatorApplied: bool) =
            {
                ThreeDSecureDetailsCharge.AuthenticationFlow = authenticationFlow //required
                ThreeDSecureDetailsCharge.ElectronicCommerceIndicator = electronicCommerceIndicator //required
                ThreeDSecureDetailsCharge.ExemptionIndicator = exemptionIndicator //required
                ThreeDSecureDetailsCharge.Result = result //required
                ThreeDSecureDetailsCharge.ResultReason = resultReason //required
                ThreeDSecureDetailsCharge.TransactionId = transactionId //required
                ThreeDSecureDetailsCharge.Version = version //required
                ThreeDSecureDetailsCharge.ExemptionIndicatorApplied = exemptionIndicatorApplied
            }

    and ThresholdsResourceUsageAlertFilter = {
        ///Limit the scope of the alert to this customer ID
        Customer: ThresholdsResourceUsageAlertFilterCustomer'AnyOf option
    }
    with
        member _.Type = "customer"

        static member New (customer: ThresholdsResourceUsageAlertFilterCustomer'AnyOf option) =
            {
                ThresholdsResourceUsageAlertFilter.Customer = customer //required
            }

    and ThresholdsResourceUsageAlertFilterCustomer'AnyOf =
        | String of string
        | Customer of Customer

    ///The usage threshold alert configuration enables setting up alerts for when a certain usage threshold on a specific meter is crossed.
    and ThresholdsResourceUsageThresholdConfig = {
        ///The filters allow limiting the scope of this usage alert. You can only specify up to one filter at this time.
        Filters: ThresholdsResourceUsageAlertFilter list option
        ///The value at which this alert will trigger.
        Gte: int
        ///The [Billing Meter](/api/billing/meter) ID whose usage is monitored.
        Meter: ThresholdsResourceUsageThresholdConfigMeter'AnyOf
    }
    with
        ///Defines how the alert will behave.
        member _.Recurrence = "one_time"

        static member New (filters: ThresholdsResourceUsageAlertFilter list option, gte: int, meter: ThresholdsResourceUsageThresholdConfigMeter'AnyOf) =
            {
                ThresholdsResourceUsageThresholdConfig.Filters = filters //required
                ThresholdsResourceUsageThresholdConfig.Gte = gte //required
                ThresholdsResourceUsageThresholdConfig.Meter = meter //required
            }

    and ThresholdsResourceUsageThresholdConfigMeter'AnyOf =
        | String of string
        | BillingMeter of BillingMeter

    ///Tokenization is the process Stripe uses to collect sensitive card or bank
    ///account details, or personally identifiable information (PII), directly from
    ///your customers in a secure manner. A token representing this information is
    ///returned to your server to use. Use our
    ///[recommended payments integrations](https://docs.stripe.com/payments) to perform this process
    ///on the client-side. This guarantees that no sensitive card data touches your server,
    ///and allows your integration to operate in a PCI-compliant way.
    ///If you can't use client-side tokenization, you can also create tokens using
    ///the API with either your publishable or secret API key. If
    ///your integration uses this method, you're responsible for any PCI compliance
    ///that it might require, and you must keep your secret API key safe. Unlike with
    ///client-side tokenization, your customer's information isn't sent directly to
    ///Stripe, so we can't determine how it's handled or stored.
    ///You can't store or use tokens more than once. To store card or bank account
    ///information for later use, create [Customer](https://docs.stripe.com/api#customers)
    ///objects or [External accounts](/api#external_accounts).
    ///[Radar](https://docs.stripe.com/radar), our integrated solution for automatic fraud protection,
    ///performs best with integrations that use client-side tokenization.
    and Token = {
        BankAccount: BankAccount option
        Card: Card option
        ///IP address of the client that generates the token.
        ClientIp: string option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Type of the token: `account`, `bank_account`, `card`, or `pii`.
        Type: TokenType
        ///Determines if you have already used this token (you can only use tokens once).
        Used: bool
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "token"

        static member New (clientIp: string option, created: DateTime, id: string, livemode: bool, ``type``: TokenType, used: bool, ?bankAccount: BankAccount, ?card: Card) =
            {
                Token.ClientIp = clientIp //required
                Token.Created = created //required
                Token.Id = id //required
                Token.Livemode = livemode //required
                Token.Type = ``type`` //required
                Token.Used = used //required
                Token.BankAccount = bankAccount
                Token.Card = card
            }

    ///To top up your Stripe balance, you create a top-up object. You can retrieve
    ///individual top-ups, as well as list all top-ups. Top-ups are identified by a
    ///unique, random ID.
    ///Related guide: [Topping up your platform account](https://docs.stripe.com/connect/top-ups)
    and Topup = {
        ///Amount transferred.
        Amount: int
        ///ID of the balance transaction that describes the impact of this top-up on your account balance. May not be specified depending on status of top-up.
        BalanceTransaction: TopupBalanceTransaction'AnyOf option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///Date the funds are expected to arrive in your Stripe account for payouts. This factors in delays like weekends or bank holidays. May not be specified depending on status of top-up.
        ExpectedAvailabilityDate: int option
        ///Error code explaining reason for top-up failure if available (see [the errors section](/api/errors) for a list of codes).
        FailureCode: string option
        ///Message to user further explaining reason for top-up failure if available.
        FailureMessage: string option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///The source field is deprecated. It might not always be present in the API response.
        Source: Source option
        ///Extra information about a top-up. This will appear on your source's bank statement. It must contain at least one letter.
        StatementDescriptor: string option
        ///The status of the top-up is either `canceled`, `failed`, `pending`, `reversed`, or `succeeded`.
        Status: TopupStatus
        ///A string that identifies this top-up as part of a group.
        TransferGroup: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "topup"

        static member New (amount: int, balanceTransaction: TopupBalanceTransaction'AnyOf option, created: DateTime, currency: IsoTypes.IsoCurrencyCode, description: string option, expectedAvailabilityDate: int option, failureCode: string option, failureMessage: string option, id: string, livemode: bool, metadata: Map<string, string>, source: Source option, statementDescriptor: string option, status: TopupStatus, transferGroup: string option) =
            {
                Topup.Amount = amount //required
                Topup.BalanceTransaction = balanceTransaction //required
                Topup.Created = created //required
                Topup.Currency = currency //required
                Topup.Description = description //required
                Topup.ExpectedAvailabilityDate = expectedAvailabilityDate //required
                Topup.FailureCode = failureCode //required
                Topup.FailureMessage = failureMessage //required
                Topup.Id = id //required
                Topup.Livemode = livemode //required
                Topup.Metadata = metadata //required
                Topup.Source = source //required
                Topup.StatementDescriptor = statementDescriptor //required
                Topup.Status = status //required
                Topup.TransferGroup = transferGroup //required
            }

    and TopupBalanceTransaction'AnyOf =
        | String of string
        | BalanceTransaction of BalanceTransaction

    ///A `Transfer` object is created when you move funds between Stripe accounts as
    ///part of Connect.
    ///Before April 6, 2017, transfers also represented movement of funds from a
    ///Stripe account to a card or bank account. This behavior has since been split
    ///out into a [Payout](https://api.stripe.com#payout_object) object, with corresponding payout endpoints. For more
    ///information, read about the
    ///[transfer/payout split](https://docs.stripe.com/transfer-payout-split).
    ///Related guide: [Creating separate charges and transfers](https://docs.stripe.com/connect/separate-charges-and-transfers)
    and Transfer = {
        ///Amount in cents (or local equivalent) to be transferred.
        Amount: int
        ///Amount in cents (or local equivalent) reversed (can be less than the amount attribute on the transfer if a partial reversal was issued).
        AmountReversed: int
        ///Balance transaction that describes the impact of this transfer on your account balance.
        BalanceTransaction: TransferBalanceTransaction'AnyOf option
        ///Time that this record of the transfer was first created.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///ID of the Stripe account the transfer was sent to.
        Destination: TransferDestination'AnyOf option
        ///If the destination is a Stripe account, this will be the ID of the payment that the destination account received for the transfer.
        DestinationPayment: TransferDestinationPayment'AnyOf option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///A list of reversals that have been applied to the transfer.
        Reversals: TransferReversals
        ///Whether the transfer has been fully reversed. If the transfer is only partially reversed, this attribute will still be false.
        Reversed: bool
        ///ID of the charge that was used to fund the transfer. If null, the transfer was funded from the available balance.
        SourceTransaction: TransferSourceTransaction'AnyOf option
        ///The source balance this transfer came from. One of `card`, `fpx`, or `bank_account`.
        SourceType: TransferSourceType option
        ///A string that identifies this transaction as part of a group. See the [Connect documentation](https://docs.stripe.com/connect/separate-charges-and-transfers#transfer-options) for details.
        TransferGroup: string option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "transfer"

        static member New (amount: int, amountReversed: int, balanceTransaction: TransferBalanceTransaction'AnyOf option, created: DateTime, currency: IsoTypes.IsoCurrencyCode, description: string option, destination: TransferDestination'AnyOf option, id: string, livemode: bool, metadata: Map<string, string>, reversals: TransferReversals, reversed: bool, sourceTransaction: TransferSourceTransaction'AnyOf option, transferGroup: string option, ?destinationPayment: TransferDestinationPayment'AnyOf, ?sourceType: TransferSourceType) =
            {
                Transfer.Amount = amount //required
                Transfer.AmountReversed = amountReversed //required
                Transfer.BalanceTransaction = balanceTransaction //required
                Transfer.Created = created //required
                Transfer.Currency = currency //required
                Transfer.Description = description //required
                Transfer.Destination = destination //required
                Transfer.Id = id //required
                Transfer.Livemode = livemode //required
                Transfer.Metadata = metadata //required
                Transfer.Reversals = reversals //required
                Transfer.Reversed = reversed //required
                Transfer.SourceTransaction = sourceTransaction //required
                Transfer.TransferGroup = transferGroup //required
                Transfer.DestinationPayment = destinationPayment
                Transfer.SourceType = sourceType
            }

    and TransferBalanceTransaction'AnyOf =
        | String of string
        | BalanceTransaction of BalanceTransaction

    and TransferDestination'AnyOf =
        | String of string
        | Account of Account

    and TransferDestinationPayment'AnyOf =
        | String of string
        | Charge of Charge

    and TransferSourceTransaction'AnyOf =
        | String of string
        | Charge of Charge

    ///A list of reversals that have been applied to the transfer.
    and TransferReversals = {
        ///Details about each object.
        Data: TransferReversal list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: TransferReversal list, hasMore: bool, url: string) =
            {
                TransferReversals.Data = data //required
                TransferReversals.HasMore = hasMore //required
                TransferReversals.Url = url //required
            }

    and TransferData = {
        ///The amount transferred to the destination account. This transfer will occur automatically after the payment succeeds. If no amount is specified, by default the entire payment amount is transferred to the destination account.
        /// The amount must be less than or equal to the [amount](https://docs.stripe.com/api/payment_intents/object#payment_intent_object-amount), and must be a positive integer
        /// representing how much to transfer in the smallest currency unit (e.g., 100 cents to charge $1.00).
        Amount: int option
        ///The account (if any) that the payment is attributed to for tax reporting, and where funds from the payment are transferred to after payment success.
        Destination: TransferDataDestination'AnyOf
    }
    with
        static member New (destination: TransferDataDestination'AnyOf, ?amount: int) =
            {
                TransferData.Destination = destination //required
                TransferData.Amount = amount
            }

    and TransferDataDestination'AnyOf =
        | String of string
        | Account of Account

    ///[Stripe Connect](https://docs.stripe.com/connect) platforms can reverse transfers made to a
    ///connected account, either entirely or partially, and can also specify whether
    ///to refund any related application fees. Transfer reversals add to the
    ///platform's balance and subtract from the destination account's balance.
    ///Reversing a transfer that was made for a [destination
    ///charge](/docs/connect/destination-charges) is allowed only up to the amount of
    ///the charge. It is possible to reverse a
    ///[transfer_group](https://docs.stripe.com/connect/separate-charges-and-transfers#transfer-options)
    ///transfer only if the destination account has enough balance to cover the
    ///reversal.
    ///Related guide: [Reverse transfers](https://docs.stripe.com/connect/separate-charges-and-transfers#reverse-transfers)
    and TransferReversal = {
        ///Amount, in cents (or local equivalent).
        Amount: int
        ///Balance transaction that describes the impact on your account balance.
        BalanceTransaction: TransferReversalBalanceTransaction'AnyOf option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///Linked payment refund for the transfer reversal.
        DestinationPaymentRefund: TransferReversalDestinationPaymentRefund'AnyOf option
        ///Unique identifier for the object.
        Id: string
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///ID of the refund responsible for the transfer reversal.
        SourceRefund: TransferReversalSourceRefund'AnyOf option
        ///ID of the transfer that was reversed.
        Transfer: TransferReversalTransfer'AnyOf
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "transfer_reversal"

        static member New (amount: int, balanceTransaction: TransferReversalBalanceTransaction'AnyOf option, created: DateTime, currency: IsoTypes.IsoCurrencyCode, destinationPaymentRefund: TransferReversalDestinationPaymentRefund'AnyOf option, id: string, metadata: Map<string, string> option, sourceRefund: TransferReversalSourceRefund'AnyOf option, transfer: TransferReversalTransfer'AnyOf) =
            {
                TransferReversal.Amount = amount //required
                TransferReversal.BalanceTransaction = balanceTransaction //required
                TransferReversal.Created = created //required
                TransferReversal.Currency = currency //required
                TransferReversal.DestinationPaymentRefund = destinationPaymentRefund //required
                TransferReversal.Id = id //required
                TransferReversal.Metadata = metadata //required
                TransferReversal.SourceRefund = sourceRefund //required
                TransferReversal.Transfer = transfer //required
            }

    and TransferReversalBalanceTransaction'AnyOf =
        | String of string
        | BalanceTransaction of BalanceTransaction

    and TransferReversalDestinationPaymentRefund'AnyOf =
        | String of string
        | Refund of Refund

    and TransferReversalSourceRefund'AnyOf =
        | String of string
        | Refund of Refund

    and TransferReversalTransfer'AnyOf =
        | String of string
        | Transfer of Transfer

    and TransferSchedule = {
        ///The number of days charges for the account will be held before being paid out.
        DelayDays: int
        ///How frequently funds will be paid out. One of `manual` (payouts only created via API call), `daily`, `weekly`, or `monthly`.
        Interval: TransferScheduleInterval
        ///The day of the month funds will be paid out. Only shown if `interval` is monthly. Payouts scheduled between the 29th and 31st of the month are sent on the last day of shorter months.
        MonthlyAnchor: int option
        ///The days of the month funds will be paid out. Only shown if `interval` is monthly. Payouts scheduled between the 29th and 31st of the month are sent on the last day of shorter months.
        MonthlyPayoutDays: int list option
        ///The day of the week funds will be paid out, of the style 'monday', 'tuesday', etc. Only shown if `interval` is weekly.
        WeeklyAnchor: string option
        ///The days of the week when available funds are paid out, specified as an array, for example, [`monday`, `tuesday`]. Only shown if `interval` is weekly.
        WeeklyPayoutDays: TransferScheduleWeeklyPayoutDays list option
    }
    with
        static member New (delayDays: int, interval: TransferScheduleInterval, ?monthlyAnchor: int, ?monthlyPayoutDays: int list, ?weeklyAnchor: string, ?weeklyPayoutDays: TransferScheduleWeeklyPayoutDays list) =
            {
                TransferSchedule.DelayDays = delayDays //required
                TransferSchedule.Interval = interval //required
                TransferSchedule.MonthlyAnchor = monthlyAnchor
                TransferSchedule.MonthlyPayoutDays = monthlyPayoutDays
                TransferSchedule.WeeklyAnchor = weeklyAnchor
                TransferSchedule.WeeklyPayoutDays = weeklyPayoutDays
            }

    and TransformQuantity = {
        ///Divide usage by this number.
        DivideBy: int
        ///After division, either round the result `up` or `down`.
        Round: TransformQuantityRound
    }
    with
        static member New (divideBy: int, round: TransformQuantityRound) =
            {
                TransformQuantity.DivideBy = divideBy //required
                TransformQuantity.Round = round //required
            }

    and TransformUsage = {
        ///Divide usage by this number.
        DivideBy: int
        ///After division, either round the result `up` or `down`.
        Round: TransformUsageRound
    }
    with
        static member New (divideBy: int, round: TransformUsageRound) =
            {
                TransformUsage.DivideBy = divideBy //required
                TransformUsage.Round = round //required
            }

    ///You can reverse some [ReceivedCredits](https://api.stripe.com#received_credits) depending on their network and source flow. Reversing a ReceivedCredit leads to the creation of a new object known as a CreditReversal.
    and TreasuryCreditReversal = {
        ///Amount (in cents) transferred.
        Amount: int
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///The FinancialAccount to reverse funds from.
        FinancialAccount: string
        ///A [hosted transaction receipt](https://docs.stripe.com/treasury/moving-money/regulatory-receipts) URL that is provided when money movement is considered regulated under Stripe's money transmission licenses.
        HostedRegulatoryReceiptUrl: string option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///The rails used to reverse the funds.
        Network: TreasuryCreditReversalNetwork
        ///The ReceivedCredit being reversed.
        ReceivedCredit: string
        ///Status of the CreditReversal
        Status: TreasuryCreditReversalStatus
        StatusTransitions: TreasuryReceivedCreditsResourceStatusTransitions
        ///The Transaction associated with this object.
        Transaction: TreasuryCreditReversalTransaction'AnyOf option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "treasury.credit_reversal"

        static member New (amount: int, created: DateTime, currency: IsoTypes.IsoCurrencyCode, financialAccount: string, hostedRegulatoryReceiptUrl: string option, id: string, livemode: bool, metadata: Map<string, string>, network: TreasuryCreditReversalNetwork, receivedCredit: string, status: TreasuryCreditReversalStatus, statusTransitions: TreasuryReceivedCreditsResourceStatusTransitions, transaction: TreasuryCreditReversalTransaction'AnyOf option) =
            {
                TreasuryCreditReversal.Amount = amount //required
                TreasuryCreditReversal.Created = created //required
                TreasuryCreditReversal.Currency = currency //required
                TreasuryCreditReversal.FinancialAccount = financialAccount //required
                TreasuryCreditReversal.HostedRegulatoryReceiptUrl = hostedRegulatoryReceiptUrl //required
                TreasuryCreditReversal.Id = id //required
                TreasuryCreditReversal.Livemode = livemode //required
                TreasuryCreditReversal.Metadata = metadata //required
                TreasuryCreditReversal.Network = network //required
                TreasuryCreditReversal.ReceivedCredit = receivedCredit //required
                TreasuryCreditReversal.Status = status //required
                TreasuryCreditReversal.StatusTransitions = statusTransitions //required
                TreasuryCreditReversal.Transaction = transaction //required
            }

    and TreasuryCreditReversalTransaction'AnyOf =
        | String of string
        | TreasuryTransaction of TreasuryTransaction

    ///You can reverse some [ReceivedDebits](https://api.stripe.com#received_debits) depending on their network and source flow. Reversing a ReceivedDebit leads to the creation of a new object known as a DebitReversal.
    and TreasuryDebitReversal = {
        ///Amount (in cents) transferred.
        Amount: int
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///The FinancialAccount to reverse funds from.
        FinancialAccount: string option
        ///A [hosted transaction receipt](https://docs.stripe.com/treasury/moving-money/regulatory-receipts) URL that is provided when money movement is considered regulated under Stripe's money transmission licenses.
        HostedRegulatoryReceiptUrl: string option
        ///Unique identifier for the object.
        Id: string
        ///Other flows linked to a DebitReversal.
        LinkedFlows: TreasuryReceivedDebitsResourceDebitReversalLinkedFlows option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///The rails used to reverse the funds.
        Network: TreasuryDebitReversalNetwork
        ///The ReceivedDebit being reversed.
        ReceivedDebit: string
        ///Status of the DebitReversal
        Status: TreasuryDebitReversalStatus
        StatusTransitions: TreasuryReceivedDebitsResourceStatusTransitions
        ///The Transaction associated with this object.
        Transaction: TreasuryDebitReversalTransaction'AnyOf option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "treasury.debit_reversal"

        static member New (amount: int, created: DateTime, currency: IsoTypes.IsoCurrencyCode, financialAccount: string option, hostedRegulatoryReceiptUrl: string option, id: string, linkedFlows: TreasuryReceivedDebitsResourceDebitReversalLinkedFlows option, livemode: bool, metadata: Map<string, string>, network: TreasuryDebitReversalNetwork, receivedDebit: string, status: TreasuryDebitReversalStatus, statusTransitions: TreasuryReceivedDebitsResourceStatusTransitions, transaction: TreasuryDebitReversalTransaction'AnyOf option) =
            {
                TreasuryDebitReversal.Amount = amount //required
                TreasuryDebitReversal.Created = created //required
                TreasuryDebitReversal.Currency = currency //required
                TreasuryDebitReversal.FinancialAccount = financialAccount //required
                TreasuryDebitReversal.HostedRegulatoryReceiptUrl = hostedRegulatoryReceiptUrl //required
                TreasuryDebitReversal.Id = id //required
                TreasuryDebitReversal.LinkedFlows = linkedFlows //required
                TreasuryDebitReversal.Livemode = livemode //required
                TreasuryDebitReversal.Metadata = metadata //required
                TreasuryDebitReversal.Network = network //required
                TreasuryDebitReversal.ReceivedDebit = receivedDebit //required
                TreasuryDebitReversal.Status = status //required
                TreasuryDebitReversal.StatusTransitions = statusTransitions //required
                TreasuryDebitReversal.Transaction = transaction //required
            }

    and TreasuryDebitReversalTransaction'AnyOf =
        | String of string
        | TreasuryTransaction of TreasuryTransaction

    ///Stripe Treasury provides users with a container for money called a FinancialAccount that is separate from their Payments balance.
    ///FinancialAccounts serve as the source and destination of Treasury’s money movement APIs.
    and TreasuryFinancialAccount = {
        ///The array of paths to active Features in the Features hash.
        ActiveFeatures: TreasuryFinancialAccountActiveFeatures list option
        Balance: TreasuryFinancialAccountsResourceBalance
        ///Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
        Country: IsoTypes.IsoCountryCode
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        Features: TreasuryFinancialAccountFeatures option
        ///The set of credentials that resolve to a FinancialAccount.
        FinancialAddresses: TreasuryFinancialAccountsResourceFinancialAddress list
        ///Unique identifier for the object.
        Id: string
        IsDefault: bool option
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string> option
        ///The nickname for the FinancialAccount.
        Nickname: string option
        ///The array of paths to pending Features in the Features hash.
        PendingFeatures: TreasuryFinancialAccountPendingFeatures list option
        ///The set of functionalities that the platform can restrict on the FinancialAccount.
        PlatformRestrictions: TreasuryFinancialAccountsResourcePlatformRestrictions option
        ///The array of paths to restricted Features in the Features hash.
        RestrictedFeatures: TreasuryFinancialAccountRestrictedFeatures list option
        ///Status of this FinancialAccount.
        Status: TreasuryFinancialAccountStatus
        StatusDetails: TreasuryFinancialAccountsResourceStatusDetails
        ///The currencies the FinancialAccount can hold a balance in. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase.
        SupportedCurrencies: string list
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "treasury.financial_account"

        static member New (balance: TreasuryFinancialAccountsResourceBalance, country: IsoTypes.IsoCountryCode, created: DateTime, financialAddresses: TreasuryFinancialAccountsResourceFinancialAddress list, id: string, livemode: bool, metadata: Map<string, string> option, status: TreasuryFinancialAccountStatus, statusDetails: TreasuryFinancialAccountsResourceStatusDetails, supportedCurrencies: string list, ?activeFeatures: TreasuryFinancialAccountActiveFeatures list, ?features: TreasuryFinancialAccountFeatures, ?isDefault: bool, ?nickname: string option, ?pendingFeatures: TreasuryFinancialAccountPendingFeatures list, ?platformRestrictions: TreasuryFinancialAccountsResourcePlatformRestrictions option, ?restrictedFeatures: TreasuryFinancialAccountRestrictedFeatures list) =
            {
                TreasuryFinancialAccount.Balance = balance //required
                TreasuryFinancialAccount.Country = country //required
                TreasuryFinancialAccount.Created = created //required
                TreasuryFinancialAccount.FinancialAddresses = financialAddresses //required
                TreasuryFinancialAccount.Id = id //required
                TreasuryFinancialAccount.Livemode = livemode //required
                TreasuryFinancialAccount.Metadata = metadata //required
                TreasuryFinancialAccount.Status = status //required
                TreasuryFinancialAccount.StatusDetails = statusDetails //required
                TreasuryFinancialAccount.SupportedCurrencies = supportedCurrencies //required
                TreasuryFinancialAccount.ActiveFeatures = activeFeatures
                TreasuryFinancialAccount.Features = features
                TreasuryFinancialAccount.IsDefault = isDefault
                TreasuryFinancialAccount.Nickname = nickname |> Option.flatten
                TreasuryFinancialAccount.PendingFeatures = pendingFeatures
                TreasuryFinancialAccount.PlatformRestrictions = platformRestrictions |> Option.flatten
                TreasuryFinancialAccount.RestrictedFeatures = restrictedFeatures
            }

    ///Encodes whether a FinancialAccount has access to a particular Feature, with a `status` enum and associated `status_details`.
    ///Stripe or the platform can control Features via the requested field.
    and TreasuryFinancialAccountFeatures = {
        CardIssuing: TreasuryFinancialAccountsResourceToggleSettings option
        DepositInsurance: TreasuryFinancialAccountsResourceToggleSettings option
        FinancialAddresses: TreasuryFinancialAccountsResourceFinancialAddressesFeatures option
        InboundTransfers: TreasuryFinancialAccountsResourceInboundTransfers option
        IntraStripeFlows: TreasuryFinancialAccountsResourceToggleSettings option
        OutboundPayments: TreasuryFinancialAccountsResourceOutboundPayments option
        OutboundTransfers: TreasuryFinancialAccountsResourceOutboundTransfers option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "treasury.financial_account_features"

        static member New (?cardIssuing: TreasuryFinancialAccountsResourceToggleSettings, ?depositInsurance: TreasuryFinancialAccountsResourceToggleSettings, ?financialAddresses: TreasuryFinancialAccountsResourceFinancialAddressesFeatures, ?inboundTransfers: TreasuryFinancialAccountsResourceInboundTransfers, ?intraStripeFlows: TreasuryFinancialAccountsResourceToggleSettings, ?outboundPayments: TreasuryFinancialAccountsResourceOutboundPayments, ?outboundTransfers: TreasuryFinancialAccountsResourceOutboundTransfers) =
            {
                TreasuryFinancialAccountFeatures.CardIssuing = cardIssuing
                TreasuryFinancialAccountFeatures.DepositInsurance = depositInsurance
                TreasuryFinancialAccountFeatures.FinancialAddresses = financialAddresses
                TreasuryFinancialAccountFeatures.InboundTransfers = inboundTransfers
                TreasuryFinancialAccountFeatures.IntraStripeFlows = intraStripeFlows
                TreasuryFinancialAccountFeatures.OutboundPayments = outboundPayments
                TreasuryFinancialAccountFeatures.OutboundTransfers = outboundTransfers
            }

    ///Use [InboundTransfers](https://docs.stripe.com/docs/treasury/moving-money/financial-accounts/into/inbound-transfers) to add funds to your [FinancialAccount](https://api.stripe.com#financial_accounts) via a PaymentMethod that is owned by you. The funds will be transferred via an ACH debit.
    ///Related guide: [Moving money with Treasury using InboundTransfer objects](https://docs.stripe.com/docs/treasury/moving-money/financial-accounts/into/inbound-transfers)
    and TreasuryInboundTransfer = {
        ///Amount (in cents) transferred.
        Amount: int
        ///Returns `true` if the InboundTransfer is able to be canceled.
        Cancelable: bool
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///Details about this InboundTransfer's failure. Only set when status is `failed`.
        FailureDetails: TreasuryInboundTransfersResourceFailureDetails option
        ///The FinancialAccount that received the funds.
        FinancialAccount: string
        ///A [hosted transaction receipt](https://docs.stripe.com/treasury/moving-money/regulatory-receipts) URL that is provided when money movement is considered regulated under Stripe's money transmission licenses.
        HostedRegulatoryReceiptUrl: string option
        ///Unique identifier for the object.
        Id: string
        LinkedFlows: TreasuryInboundTransfersResourceInboundTransferResourceLinkedFlows
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///The origin payment method to be debited for an InboundTransfer.
        OriginPaymentMethod: string option
        ///Details about the PaymentMethod for an InboundTransfer.
        OriginPaymentMethodDetails: InboundTransfers option
        ///Returns `true` if the funds for an InboundTransfer were returned after the InboundTransfer went to the `succeeded` state.
        Returned: bool option
        ///Statement descriptor shown when funds are debited from the source. Not all payment networks support `statement_descriptor`.
        StatementDescriptor: string
        ///Status of the InboundTransfer: `processing`, `succeeded`, `failed`, and `canceled`. An InboundTransfer is `processing` if it is created and pending. The status changes to `succeeded` once the funds have been "confirmed" and a `transaction` is created and posted. The status changes to `failed` if the transfer fails.
        Status: TreasuryInboundTransferStatus
        StatusTransitions: TreasuryInboundTransfersResourceInboundTransferResourceStatusTransitions
        ///The Transaction associated with this object.
        Transaction: TreasuryInboundTransferTransaction'AnyOf option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "treasury.inbound_transfer"

        static member New (amount: int, cancelable: bool, created: DateTime, currency: IsoTypes.IsoCurrencyCode, description: string option, failureDetails: TreasuryInboundTransfersResourceFailureDetails option, financialAccount: string, hostedRegulatoryReceiptUrl: string option, id: string, linkedFlows: TreasuryInboundTransfersResourceInboundTransferResourceLinkedFlows, livemode: bool, metadata: Map<string, string>, originPaymentMethod: string option, originPaymentMethodDetails: InboundTransfers option, returned: bool option, statementDescriptor: string, status: TreasuryInboundTransferStatus, statusTransitions: TreasuryInboundTransfersResourceInboundTransferResourceStatusTransitions, transaction: TreasuryInboundTransferTransaction'AnyOf option) =
            {
                TreasuryInboundTransfer.Amount = amount //required
                TreasuryInboundTransfer.Cancelable = cancelable //required
                TreasuryInboundTransfer.Created = created //required
                TreasuryInboundTransfer.Currency = currency //required
                TreasuryInboundTransfer.Description = description //required
                TreasuryInboundTransfer.FailureDetails = failureDetails //required
                TreasuryInboundTransfer.FinancialAccount = financialAccount //required
                TreasuryInboundTransfer.HostedRegulatoryReceiptUrl = hostedRegulatoryReceiptUrl //required
                TreasuryInboundTransfer.Id = id //required
                TreasuryInboundTransfer.LinkedFlows = linkedFlows //required
                TreasuryInboundTransfer.Livemode = livemode //required
                TreasuryInboundTransfer.Metadata = metadata //required
                TreasuryInboundTransfer.OriginPaymentMethod = originPaymentMethod //required
                TreasuryInboundTransfer.OriginPaymentMethodDetails = originPaymentMethodDetails //required
                TreasuryInboundTransfer.Returned = returned //required
                TreasuryInboundTransfer.StatementDescriptor = statementDescriptor //required
                TreasuryInboundTransfer.Status = status //required
                TreasuryInboundTransfer.StatusTransitions = statusTransitions //required
                TreasuryInboundTransfer.Transaction = transaction //required
            }

    and TreasuryInboundTransferTransaction'AnyOf =
        | String of string
        | TreasuryTransaction of TreasuryTransaction

    ///Use [OutboundPayments](https://docs.stripe.com/docs/treasury/moving-money/financial-accounts/out-of/outbound-payments) to send funds to another party's external bank account or [FinancialAccount](https://api.stripe.com#financial_accounts). To send money to an account belonging to the same user, use an [OutboundTransfer](https://api.stripe.com#outbound_transfers).
    ///Simulate OutboundPayment state changes with the `/v1/test_helpers/treasury/outbound_payments` endpoints. These methods can only be called on test mode objects.
    ///Related guide: [Moving money with Treasury using OutboundPayment objects](https://docs.stripe.com/docs/treasury/moving-money/financial-accounts/out-of/outbound-payments)
    and TreasuryOutboundPayment = {
        ///Amount (in cents) transferred.
        Amount: int
        ///Returns `true` if the object can be canceled, and `false` otherwise.
        Cancelable: bool
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///ID of the [customer](https://docs.stripe.com/api/customers) to whom an OutboundPayment is sent.
        Customer: string option
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///The PaymentMethod via which an OutboundPayment is sent. This field can be empty if the OutboundPayment was created using `destination_payment_method_data`.
        DestinationPaymentMethod: string option
        ///Details about the PaymentMethod for an OutboundPayment.
        DestinationPaymentMethodDetails: OutboundPaymentsPaymentMethodDetails option
        ///Details about the end user.
        EndUserDetails: TreasuryOutboundPaymentsResourceOutboundPaymentResourceEndUserDetails option
        ///The date when funds are expected to arrive in the destination account.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpectedArrivalDate: DateTime
        ///The FinancialAccount that funds were pulled from.
        FinancialAccount: string
        ///A [hosted transaction receipt](https://docs.stripe.com/treasury/moving-money/regulatory-receipts) URL that is provided when money movement is considered regulated under Stripe's money transmission licenses.
        HostedRegulatoryReceiptUrl: string option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///Details about a returned OutboundPayment. Only set when the status is `returned`.
        ReturnedDetails: TreasuryOutboundPaymentsResourceReturnedStatus option
        ///The description that appears on the receiving end for an OutboundPayment (for example, bank statement for external bank transfer).
        StatementDescriptor: string
        ///Current status of the OutboundPayment: `processing`, `failed`, `posted`, `returned`, `canceled`. An OutboundPayment is `processing` if it has been created and is pending. The status changes to `posted` once the OutboundPayment has been "confirmed" and funds have left the account, or to `failed` or `canceled`. If an OutboundPayment fails to arrive at its destination, its status will change to `returned`.
        Status: TreasuryOutboundPaymentStatus
        StatusTransitions: TreasuryOutboundPaymentsResourceOutboundPaymentResourceStatusTransitions
        ///Details about network-specific tracking information if available.
        TrackingDetails: TreasuryOutboundPaymentsResourceOutboundPaymentResourceTrackingDetails option
        ///The Transaction associated with this object.
        Transaction: TreasuryOutboundPaymentTransaction'AnyOf
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "treasury.outbound_payment"

        static member New (amount: int, cancelable: bool, created: DateTime, currency: IsoTypes.IsoCurrencyCode, customer: string option, description: string option, destinationPaymentMethod: string option, destinationPaymentMethodDetails: OutboundPaymentsPaymentMethodDetails option, endUserDetails: TreasuryOutboundPaymentsResourceOutboundPaymentResourceEndUserDetails option, expectedArrivalDate: DateTime, financialAccount: string, hostedRegulatoryReceiptUrl: string option, id: string, livemode: bool, metadata: Map<string, string>, returnedDetails: TreasuryOutboundPaymentsResourceReturnedStatus option, statementDescriptor: string, status: TreasuryOutboundPaymentStatus, statusTransitions: TreasuryOutboundPaymentsResourceOutboundPaymentResourceStatusTransitions, trackingDetails: TreasuryOutboundPaymentsResourceOutboundPaymentResourceTrackingDetails option, transaction: TreasuryOutboundPaymentTransaction'AnyOf) =
            {
                TreasuryOutboundPayment.Amount = amount //required
                TreasuryOutboundPayment.Cancelable = cancelable //required
                TreasuryOutboundPayment.Created = created //required
                TreasuryOutboundPayment.Currency = currency //required
                TreasuryOutboundPayment.Customer = customer //required
                TreasuryOutboundPayment.Description = description //required
                TreasuryOutboundPayment.DestinationPaymentMethod = destinationPaymentMethod //required
                TreasuryOutboundPayment.DestinationPaymentMethodDetails = destinationPaymentMethodDetails //required
                TreasuryOutboundPayment.EndUserDetails = endUserDetails //required
                TreasuryOutboundPayment.ExpectedArrivalDate = expectedArrivalDate //required
                TreasuryOutboundPayment.FinancialAccount = financialAccount //required
                TreasuryOutboundPayment.HostedRegulatoryReceiptUrl = hostedRegulatoryReceiptUrl //required
                TreasuryOutboundPayment.Id = id //required
                TreasuryOutboundPayment.Livemode = livemode //required
                TreasuryOutboundPayment.Metadata = metadata //required
                TreasuryOutboundPayment.ReturnedDetails = returnedDetails //required
                TreasuryOutboundPayment.StatementDescriptor = statementDescriptor //required
                TreasuryOutboundPayment.Status = status //required
                TreasuryOutboundPayment.StatusTransitions = statusTransitions //required
                TreasuryOutboundPayment.TrackingDetails = trackingDetails //required
                TreasuryOutboundPayment.Transaction = transaction //required
            }

    and TreasuryOutboundPaymentTransaction'AnyOf =
        | String of string
        | TreasuryTransaction of TreasuryTransaction

    ///Use [OutboundTransfers](https://docs.stripe.com/docs/treasury/moving-money/financial-accounts/out-of/outbound-transfers) to transfer funds from a [FinancialAccount](https://api.stripe.com#financial_accounts) to a PaymentMethod belonging to the same entity. To send funds to a different party, use [OutboundPayments](https://api.stripe.com#outbound_payments) instead. You can send funds over ACH rails or through a domestic wire transfer to a user's own external bank account.
    ///Simulate OutboundTransfer state changes with the `/v1/test_helpers/treasury/outbound_transfers` endpoints. These methods can only be called on test mode objects.
    ///Related guide: [Moving money with Treasury using OutboundTransfer objects](https://docs.stripe.com/docs/treasury/moving-money/financial-accounts/out-of/outbound-transfers)
    and TreasuryOutboundTransfer = {
        ///Amount (in cents) transferred.
        Amount: int
        ///Returns `true` if the object can be canceled, and `false` otherwise.
        Cancelable: bool
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string option
        ///The PaymentMethod used as the payment instrument for an OutboundTransfer.
        DestinationPaymentMethod: string option
        DestinationPaymentMethodDetails: OutboundTransfersPaymentMethodDetails
        ///The date when funds are expected to arrive in the destination account.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]ExpectedArrivalDate: DateTime
        ///The FinancialAccount that funds were pulled from.
        FinancialAccount: string
        ///A [hosted transaction receipt](https://docs.stripe.com/treasury/moving-money/regulatory-receipts) URL that is provided when money movement is considered regulated under Stripe's money transmission licenses.
        HostedRegulatoryReceiptUrl: string option
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///Details about a returned OutboundTransfer. Only set when the status is `returned`.
        ReturnedDetails: TreasuryOutboundTransfersResourceReturnedDetails option
        ///Information about the OutboundTransfer to be sent to the recipient account.
        StatementDescriptor: string
        ///Current status of the OutboundTransfer: `processing`, `failed`, `canceled`, `posted`, `returned`. An OutboundTransfer is `processing` if it has been created and is pending. The status changes to `posted` once the OutboundTransfer has been "confirmed" and funds have left the account, or to `failed` or `canceled`. If an OutboundTransfer fails to arrive at its destination, its status will change to `returned`.
        Status: TreasuryOutboundTransferStatus
        StatusTransitions: TreasuryOutboundTransfersResourceStatusTransitions
        ///Details about network-specific tracking information if available.
        TrackingDetails: TreasuryOutboundTransfersResourceOutboundTransferResourceTrackingDetails option
        ///The Transaction associated with this object.
        Transaction: TreasuryOutboundTransferTransaction'AnyOf
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "treasury.outbound_transfer"

        static member New (amount: int, cancelable: bool, created: DateTime, currency: IsoTypes.IsoCurrencyCode, description: string option, destinationPaymentMethod: string option, destinationPaymentMethodDetails: OutboundTransfersPaymentMethodDetails, expectedArrivalDate: DateTime, financialAccount: string, hostedRegulatoryReceiptUrl: string option, id: string, livemode: bool, metadata: Map<string, string>, returnedDetails: TreasuryOutboundTransfersResourceReturnedDetails option, statementDescriptor: string, status: TreasuryOutboundTransferStatus, statusTransitions: TreasuryOutboundTransfersResourceStatusTransitions, trackingDetails: TreasuryOutboundTransfersResourceOutboundTransferResourceTrackingDetails option, transaction: TreasuryOutboundTransferTransaction'AnyOf) =
            {
                TreasuryOutboundTransfer.Amount = amount //required
                TreasuryOutboundTransfer.Cancelable = cancelable //required
                TreasuryOutboundTransfer.Created = created //required
                TreasuryOutboundTransfer.Currency = currency //required
                TreasuryOutboundTransfer.Description = description //required
                TreasuryOutboundTransfer.DestinationPaymentMethod = destinationPaymentMethod //required
                TreasuryOutboundTransfer.DestinationPaymentMethodDetails = destinationPaymentMethodDetails //required
                TreasuryOutboundTransfer.ExpectedArrivalDate = expectedArrivalDate //required
                TreasuryOutboundTransfer.FinancialAccount = financialAccount //required
                TreasuryOutboundTransfer.HostedRegulatoryReceiptUrl = hostedRegulatoryReceiptUrl //required
                TreasuryOutboundTransfer.Id = id //required
                TreasuryOutboundTransfer.Livemode = livemode //required
                TreasuryOutboundTransfer.Metadata = metadata //required
                TreasuryOutboundTransfer.ReturnedDetails = returnedDetails //required
                TreasuryOutboundTransfer.StatementDescriptor = statementDescriptor //required
                TreasuryOutboundTransfer.Status = status //required
                TreasuryOutboundTransfer.StatusTransitions = statusTransitions //required
                TreasuryOutboundTransfer.TrackingDetails = trackingDetails //required
                TreasuryOutboundTransfer.Transaction = transaction //required
            }

    and TreasuryOutboundTransferTransaction'AnyOf =
        | String of string
        | TreasuryTransaction of TreasuryTransaction

    ///ReceivedCredits represent funds sent to a [FinancialAccount](https://api.stripe.com#financial_accounts) (for example, via ACH or wire). These money movements are not initiated from the FinancialAccount.
    and TreasuryReceivedCredit = {
        ///Amount (in cents) transferred.
        Amount: int
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string
        ///Reason for the failure. A ReceivedCredit might fail because the receiving FinancialAccount is closed or frozen.
        FailureCode: TreasuryReceivedCreditFailureCode option
        ///The FinancialAccount that received the funds.
        FinancialAccount: string option
        ///A [hosted transaction receipt](https://docs.stripe.com/treasury/moving-money/regulatory-receipts) URL that is provided when money movement is considered regulated under Stripe's money transmission licenses.
        HostedRegulatoryReceiptUrl: string option
        ///Unique identifier for the object.
        Id: string
        InitiatingPaymentMethodDetails: TreasurySharedResourceInitiatingPaymentMethodDetailsInitiatingPaymentMethodDetails
        LinkedFlows: TreasuryReceivedCreditsResourceLinkedFlows
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The rails used to send the funds.
        Network: TreasuryReceivedCreditNetwork
        ///Details describing when a ReceivedCredit may be reversed.
        ReversalDetails: TreasuryReceivedCreditsResourceReversalDetails option
        ///Status of the ReceivedCredit. ReceivedCredits are created either `succeeded` (approved) or `failed` (declined). If a ReceivedCredit is declined, the failure reason can be found in the `failure_code` field.
        Status: TreasuryReceivedCreditStatus
        ///The Transaction associated with this object.
        Transaction: TreasuryReceivedCreditTransaction'AnyOf option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "treasury.received_credit"

        static member New (amount: int, created: DateTime, currency: IsoTypes.IsoCurrencyCode, description: string, failureCode: TreasuryReceivedCreditFailureCode option, financialAccount: string option, hostedRegulatoryReceiptUrl: string option, id: string, initiatingPaymentMethodDetails: TreasurySharedResourceInitiatingPaymentMethodDetailsInitiatingPaymentMethodDetails, linkedFlows: TreasuryReceivedCreditsResourceLinkedFlows, livemode: bool, network: TreasuryReceivedCreditNetwork, reversalDetails: TreasuryReceivedCreditsResourceReversalDetails option, status: TreasuryReceivedCreditStatus, transaction: TreasuryReceivedCreditTransaction'AnyOf option) =
            {
                TreasuryReceivedCredit.Amount = amount //required
                TreasuryReceivedCredit.Created = created //required
                TreasuryReceivedCredit.Currency = currency //required
                TreasuryReceivedCredit.Description = description //required
                TreasuryReceivedCredit.FailureCode = failureCode //required
                TreasuryReceivedCredit.FinancialAccount = financialAccount //required
                TreasuryReceivedCredit.HostedRegulatoryReceiptUrl = hostedRegulatoryReceiptUrl //required
                TreasuryReceivedCredit.Id = id //required
                TreasuryReceivedCredit.InitiatingPaymentMethodDetails = initiatingPaymentMethodDetails //required
                TreasuryReceivedCredit.LinkedFlows = linkedFlows //required
                TreasuryReceivedCredit.Livemode = livemode //required
                TreasuryReceivedCredit.Network = network //required
                TreasuryReceivedCredit.ReversalDetails = reversalDetails //required
                TreasuryReceivedCredit.Status = status //required
                TreasuryReceivedCredit.Transaction = transaction //required
            }

    and TreasuryReceivedCreditTransaction'AnyOf =
        | String of string
        | TreasuryTransaction of TreasuryTransaction

    ///ReceivedDebits represent funds pulled from a [FinancialAccount](https://api.stripe.com#financial_accounts). These are not initiated from the FinancialAccount.
    and TreasuryReceivedDebit = {
        ///Amount (in cents) transferred.
        Amount: int
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string
        ///Reason for the failure. A ReceivedDebit might fail because the FinancialAccount doesn't have sufficient funds, is closed, or is frozen.
        FailureCode: TreasuryReceivedDebitFailureCode option
        ///The FinancialAccount that funds were pulled from.
        FinancialAccount: string option
        ///A [hosted transaction receipt](https://docs.stripe.com/treasury/moving-money/regulatory-receipts) URL that is provided when money movement is considered regulated under Stripe's money transmission licenses.
        HostedRegulatoryReceiptUrl: string option
        ///Unique identifier for the object.
        Id: string
        InitiatingPaymentMethodDetails: TreasurySharedResourceInitiatingPaymentMethodDetailsInitiatingPaymentMethodDetails option
        LinkedFlows: TreasuryReceivedDebitsResourceLinkedFlows
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The network used for the ReceivedDebit.
        Network: TreasuryReceivedDebitNetwork
        ///Details describing when a ReceivedDebit might be reversed.
        ReversalDetails: TreasuryReceivedDebitsResourceReversalDetails option
        ///Status of the ReceivedDebit. ReceivedDebits are created with a status of either `succeeded` (approved) or `failed` (declined). The failure reason can be found under the `failure_code`.
        Status: TreasuryReceivedDebitStatus
        ///The Transaction associated with this object.
        Transaction: TreasuryReceivedDebitTransaction'AnyOf option
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "treasury.received_debit"

        static member New (amount: int, created: DateTime, currency: IsoTypes.IsoCurrencyCode, description: string, failureCode: TreasuryReceivedDebitFailureCode option, financialAccount: string option, hostedRegulatoryReceiptUrl: string option, id: string, linkedFlows: TreasuryReceivedDebitsResourceLinkedFlows, livemode: bool, network: TreasuryReceivedDebitNetwork, reversalDetails: TreasuryReceivedDebitsResourceReversalDetails option, status: TreasuryReceivedDebitStatus, transaction: TreasuryReceivedDebitTransaction'AnyOf option, ?initiatingPaymentMethodDetails: TreasurySharedResourceInitiatingPaymentMethodDetailsInitiatingPaymentMethodDetails) =
            {
                TreasuryReceivedDebit.Amount = amount //required
                TreasuryReceivedDebit.Created = created //required
                TreasuryReceivedDebit.Currency = currency //required
                TreasuryReceivedDebit.Description = description //required
                TreasuryReceivedDebit.FailureCode = failureCode //required
                TreasuryReceivedDebit.FinancialAccount = financialAccount //required
                TreasuryReceivedDebit.HostedRegulatoryReceiptUrl = hostedRegulatoryReceiptUrl //required
                TreasuryReceivedDebit.Id = id //required
                TreasuryReceivedDebit.LinkedFlows = linkedFlows //required
                TreasuryReceivedDebit.Livemode = livemode //required
                TreasuryReceivedDebit.Network = network //required
                TreasuryReceivedDebit.ReversalDetails = reversalDetails //required
                TreasuryReceivedDebit.Status = status //required
                TreasuryReceivedDebit.Transaction = transaction //required
                TreasuryReceivedDebit.InitiatingPaymentMethodDetails = initiatingPaymentMethodDetails
            }

    and TreasuryReceivedDebitTransaction'AnyOf =
        | String of string
        | TreasuryTransaction of TreasuryTransaction

    ///Transactions represent changes to a [FinancialAccount's](https://api.stripe.com#financial_accounts) balance.
    and TreasuryTransaction = {
        ///Amount (in cents) transferred.
        Amount: int
        BalanceImpact: TreasuryTransactionsResourceBalanceImpact
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///An arbitrary string attached to the object. Often useful for displaying to users.
        Description: string
        ///A list of TransactionEntries that are part of this Transaction. This cannot be expanded in any list endpoints.
        Entries: TreasuryTransactionEntries option
        ///The FinancialAccount associated with this object.
        FinancialAccount: string
        ///ID of the flow that created the Transaction.
        Flow: string option
        ///Details of the flow that created the Transaction.
        FlowDetails: TreasuryTransactionsResourceFlowDetails option
        ///Type of the flow that created the Transaction.
        FlowType: TreasuryTransactionFlowType
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Status of the Transaction.
        Status: TreasuryTransactionStatus
        StatusTransitions: TreasuryTransactionsResourceAbstractTransactionResourceStatusTransitions
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "treasury.transaction"

        static member New (amount: int, balanceImpact: TreasuryTransactionsResourceBalanceImpact, created: DateTime, currency: IsoTypes.IsoCurrencyCode, description: string, financialAccount: string, flow: string option, flowType: TreasuryTransactionFlowType, id: string, livemode: bool, status: TreasuryTransactionStatus, statusTransitions: TreasuryTransactionsResourceAbstractTransactionResourceStatusTransitions, ?entries: TreasuryTransactionEntries option, ?flowDetails: TreasuryTransactionsResourceFlowDetails option) =
            {
                TreasuryTransaction.Amount = amount //required
                TreasuryTransaction.BalanceImpact = balanceImpact //required
                TreasuryTransaction.Created = created //required
                TreasuryTransaction.Currency = currency //required
                TreasuryTransaction.Description = description //required
                TreasuryTransaction.FinancialAccount = financialAccount //required
                TreasuryTransaction.Flow = flow //required
                TreasuryTransaction.FlowType = flowType //required
                TreasuryTransaction.Id = id //required
                TreasuryTransaction.Livemode = livemode //required
                TreasuryTransaction.Status = status //required
                TreasuryTransaction.StatusTransitions = statusTransitions //required
                TreasuryTransaction.Entries = entries |> Option.flatten
                TreasuryTransaction.FlowDetails = flowDetails |> Option.flatten
            }

    ///A list of TransactionEntries that are part of this Transaction. This cannot be expanded in any list endpoints.
    and TreasuryTransactionEntries = {
        ///Details about each object.
        Data: TreasuryTransactionEntry list
        ///True if this list has another page of items after this one that can be fetched.
        HasMore: bool
        ///The URL where this list can be accessed.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
        member _.Object = "list"

        static member New (data: TreasuryTransactionEntry list, hasMore: bool, url: string) =
            {
                TreasuryTransactionEntries.Data = data //required
                TreasuryTransactionEntries.HasMore = hasMore //required
                TreasuryTransactionEntries.Url = url //required
            }

    ///TransactionEntries represent individual units of money movements within a single [Transaction](https://api.stripe.com#transactions).
    and TreasuryTransactionEntry = {
        BalanceImpact: TreasuryTransactionsResourceBalanceImpact
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
        Currency: IsoTypes.IsoCurrencyCode
        ///When the TransactionEntry will impact the FinancialAccount's balance.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]EffectiveAt: DateTime
        ///The FinancialAccount associated with this object.
        FinancialAccount: string
        ///Token of the flow associated with the TransactionEntry.
        Flow: string option
        ///Details of the flow associated with the TransactionEntry.
        FlowDetails: TreasuryTransactionsResourceFlowDetails option
        ///Type of the flow associated with the TransactionEntry.
        FlowType: TreasuryTransactionEntryFlowType
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///The Transaction associated with this object.
        Transaction: TreasuryTransactionEntryTransaction'AnyOf
        ///The specific money movement that generated the TransactionEntry.
        Type: TreasuryTransactionEntryType
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "treasury.transaction_entry"

        static member New (balanceImpact: TreasuryTransactionsResourceBalanceImpact, created: DateTime, currency: IsoTypes.IsoCurrencyCode, effectiveAt: DateTime, financialAccount: string, flow: string option, flowType: TreasuryTransactionEntryFlowType, id: string, livemode: bool, transaction: TreasuryTransactionEntryTransaction'AnyOf, ``type``: TreasuryTransactionEntryType, ?flowDetails: TreasuryTransactionsResourceFlowDetails option) =
            {
                TreasuryTransactionEntry.BalanceImpact = balanceImpact //required
                TreasuryTransactionEntry.Created = created //required
                TreasuryTransactionEntry.Currency = currency //required
                TreasuryTransactionEntry.EffectiveAt = effectiveAt //required
                TreasuryTransactionEntry.FinancialAccount = financialAccount //required
                TreasuryTransactionEntry.Flow = flow //required
                TreasuryTransactionEntry.FlowType = flowType //required
                TreasuryTransactionEntry.Id = id //required
                TreasuryTransactionEntry.Livemode = livemode //required
                TreasuryTransactionEntry.Transaction = transaction //required
                TreasuryTransactionEntry.Type = ``type`` //required
                TreasuryTransactionEntry.FlowDetails = flowDetails |> Option.flatten
            }

    and TreasuryTransactionEntryTransaction'AnyOf =
        | String of string
        | TreasuryTransaction of TreasuryTransaction

    ///Toggle settings for enabling/disabling the ABA address feature
    and TreasuryFinancialAccountsResourceAbaToggleSettings = {
        ///Whether the FinancialAccount should have the Feature.
        Requested: bool
        ///Whether the Feature is operational.
        Status: TreasuryFinancialAccountsResourceAbaToggleSettingsStatus
        ///Additional details; includes at least one entry when the status is not `active`.
        StatusDetails: TreasuryFinancialAccountsResourceTogglesSettingStatusDetails list
    }
    with
        static member New (requested: bool, status: TreasuryFinancialAccountsResourceAbaToggleSettingsStatus, statusDetails: TreasuryFinancialAccountsResourceTogglesSettingStatusDetails list) =
            {
                TreasuryFinancialAccountsResourceAbaToggleSettings.Requested = requested //required
                TreasuryFinancialAccountsResourceAbaToggleSettings.Status = status //required
                TreasuryFinancialAccountsResourceAbaToggleSettings.StatusDetails = statusDetails //required
            }

    ///Balance information for the FinancialAccount
    and TreasuryFinancialAccountsResourceBalance = {
        ///Funds the user can spend right now.
        Cash: Map<string, string list>
        ///Funds not spendable yet, but will become available at a later time.
        InboundPending: Map<string, string list>
        ///Funds in the account, but not spendable because they are being held for pending outbound flows.
        OutboundPending: Map<string, string list>
    }
    with
        static member New (cash: Map<string, string list>, inboundPending: Map<string, string list>, outboundPending: Map<string, string list>) =
            {
                TreasuryFinancialAccountsResourceBalance.Cash = cash //required
                TreasuryFinancialAccountsResourceBalance.InboundPending = inboundPending //required
                TreasuryFinancialAccountsResourceBalance.OutboundPending = outboundPending //required
            }

    and TreasuryFinancialAccountsResourceClosedStatusDetails = {
        ///The array that contains reasons for a FinancialAccount closure.
        Reasons: TreasuryFinancialAccountsResourceClosedStatusDetailsReasons list
    }
    with
        static member New (reasons: TreasuryFinancialAccountsResourceClosedStatusDetailsReasons list) =
            {
                TreasuryFinancialAccountsResourceClosedStatusDetails.Reasons = reasons //required
            }

    ///FinancialAddresses contain identifying information that resolves to a FinancialAccount.
    and TreasuryFinancialAccountsResourceFinancialAddress = {
        Aba: TreasuryFinancialAccountsResourceAbaRecord option
        ///The list of networks that the address supports
        SupportedNetworks: TreasuryFinancialAccountsResourceFinancialAddressSupportedNetworks list option
    }
    with
        ///The type of financial address
        member _.Type = "aba"

        static member New (?aba: TreasuryFinancialAccountsResourceAbaRecord, ?supportedNetworks: TreasuryFinancialAccountsResourceFinancialAddressSupportedNetworks list) =
            {
                TreasuryFinancialAccountsResourceFinancialAddress.Aba = aba
                TreasuryFinancialAccountsResourceFinancialAddress.SupportedNetworks = supportedNetworks
            }

    ///Settings related to Financial Addresses features on a Financial Account
    and TreasuryFinancialAccountsResourceFinancialAddressesFeatures = {
        Aba: TreasuryFinancialAccountsResourceAbaToggleSettings option
    }
    with
        static member New (?aba: TreasuryFinancialAccountsResourceAbaToggleSettings) =
            {
                TreasuryFinancialAccountsResourceFinancialAddressesFeatures.Aba = aba
            }

    ///Toggle settings for enabling/disabling an inbound ACH specific feature
    and TreasuryFinancialAccountsResourceInboundAchToggleSettings = {
        ///Whether the FinancialAccount should have the Feature.
        Requested: bool
        ///Whether the Feature is operational.
        Status: TreasuryFinancialAccountsResourceInboundAchToggleSettingsStatus
        ///Additional details; includes at least one entry when the status is not `active`.
        StatusDetails: TreasuryFinancialAccountsResourceTogglesSettingStatusDetails list
    }
    with
        static member New (requested: bool, status: TreasuryFinancialAccountsResourceInboundAchToggleSettingsStatus, statusDetails: TreasuryFinancialAccountsResourceTogglesSettingStatusDetails list) =
            {
                TreasuryFinancialAccountsResourceInboundAchToggleSettings.Requested = requested //required
                TreasuryFinancialAccountsResourceInboundAchToggleSettings.Status = status //required
                TreasuryFinancialAccountsResourceInboundAchToggleSettings.StatusDetails = statusDetails //required
            }

    ///InboundTransfers contains inbound transfers features for a FinancialAccount.
    and TreasuryFinancialAccountsResourceInboundTransfers = {
        Ach: TreasuryFinancialAccountsResourceInboundAchToggleSettings option
    }
    with
        static member New (?ach: TreasuryFinancialAccountsResourceInboundAchToggleSettings) =
            {
                TreasuryFinancialAccountsResourceInboundTransfers.Ach = ach
            }

    ///Toggle settings for enabling/disabling an outbound ACH specific feature
    and TreasuryFinancialAccountsResourceOutboundAchToggleSettings = {
        ///Whether the FinancialAccount should have the Feature.
        Requested: bool
        ///Whether the Feature is operational.
        Status: TreasuryFinancialAccountsResourceOutboundAchToggleSettingsStatus
        ///Additional details; includes at least one entry when the status is not `active`.
        StatusDetails: TreasuryFinancialAccountsResourceTogglesSettingStatusDetails list
    }
    with
        static member New (requested: bool, status: TreasuryFinancialAccountsResourceOutboundAchToggleSettingsStatus, statusDetails: TreasuryFinancialAccountsResourceTogglesSettingStatusDetails list) =
            {
                TreasuryFinancialAccountsResourceOutboundAchToggleSettings.Requested = requested //required
                TreasuryFinancialAccountsResourceOutboundAchToggleSettings.Status = status //required
                TreasuryFinancialAccountsResourceOutboundAchToggleSettings.StatusDetails = statusDetails //required
            }

    ///Settings related to Outbound Payments features on a Financial Account
    and TreasuryFinancialAccountsResourceOutboundPayments = {
        Ach: TreasuryFinancialAccountsResourceOutboundAchToggleSettings option
        UsDomesticWire: TreasuryFinancialAccountsResourceToggleSettings option
    }
    with
        static member New (?ach: TreasuryFinancialAccountsResourceOutboundAchToggleSettings, ?usDomesticWire: TreasuryFinancialAccountsResourceToggleSettings) =
            {
                TreasuryFinancialAccountsResourceOutboundPayments.Ach = ach
                TreasuryFinancialAccountsResourceOutboundPayments.UsDomesticWire = usDomesticWire
            }

    ///OutboundTransfers contains outbound transfers features for a FinancialAccount.
    and TreasuryFinancialAccountsResourceOutboundTransfers = {
        Ach: TreasuryFinancialAccountsResourceOutboundAchToggleSettings option
        UsDomesticWire: TreasuryFinancialAccountsResourceToggleSettings option
    }
    with
        static member New (?ach: TreasuryFinancialAccountsResourceOutboundAchToggleSettings, ?usDomesticWire: TreasuryFinancialAccountsResourceToggleSettings) =
            {
                TreasuryFinancialAccountsResourceOutboundTransfers.Ach = ach
                TreasuryFinancialAccountsResourceOutboundTransfers.UsDomesticWire = usDomesticWire
            }

    ///Restrictions that a Connect Platform has placed on this FinancialAccount.
    and TreasuryFinancialAccountsResourcePlatformRestrictions = {
        ///Restricts all inbound money movement.
        InboundFlows: TreasuryFinancialAccountsResourcePlatformRestrictionsInboundFlows option
        ///Restricts all outbound money movement.
        OutboundFlows: TreasuryFinancialAccountsResourcePlatformRestrictionsOutboundFlows option
    }
    with
        static member New (inboundFlows: TreasuryFinancialAccountsResourcePlatformRestrictionsInboundFlows option, outboundFlows: TreasuryFinancialAccountsResourcePlatformRestrictionsOutboundFlows option) =
            {
                TreasuryFinancialAccountsResourcePlatformRestrictions.InboundFlows = inboundFlows //required
                TreasuryFinancialAccountsResourcePlatformRestrictions.OutboundFlows = outboundFlows //required
            }

    and TreasuryFinancialAccountsResourceStatusDetails = {
        ///Details related to the closure of this FinancialAccount
        Closed: TreasuryFinancialAccountsResourceClosedStatusDetails option
    }
    with
        static member New (closed: TreasuryFinancialAccountsResourceClosedStatusDetails option) =
            {
                TreasuryFinancialAccountsResourceStatusDetails.Closed = closed //required
            }

    ///Toggle settings for enabling/disabling a feature
    and TreasuryFinancialAccountsResourceToggleSettings = {
        ///Whether the FinancialAccount should have the Feature.
        Requested: bool
        ///Whether the Feature is operational.
        Status: TreasuryFinancialAccountsResourceToggleSettingsStatus
        ///Additional details; includes at least one entry when the status is not `active`.
        StatusDetails: TreasuryFinancialAccountsResourceTogglesSettingStatusDetails list
    }
    with
        static member New (requested: bool, status: TreasuryFinancialAccountsResourceToggleSettingsStatus, statusDetails: TreasuryFinancialAccountsResourceTogglesSettingStatusDetails list) =
            {
                TreasuryFinancialAccountsResourceToggleSettings.Requested = requested //required
                TreasuryFinancialAccountsResourceToggleSettings.Status = status //required
                TreasuryFinancialAccountsResourceToggleSettings.StatusDetails = statusDetails //required
            }

    ///Additional details on the FinancialAccount Features information.
    and TreasuryFinancialAccountsResourceTogglesSettingStatusDetails = {
        ///Represents the reason why the status is `pending` or `restricted`.
        Code: TreasuryFinancialAccountsResourceTogglesSettingStatusDetailsCode
        ///Represents what the user should do, if anything, to activate the Feature.
        Resolution: TreasuryFinancialAccountsResourceTogglesSettingStatusDetailsResolution option
        ///The `platform_restrictions` that are restricting this Feature.
        Restriction: TreasuryFinancialAccountsResourceTogglesSettingStatusDetailsRestriction option
    }
    with
        static member New (code: TreasuryFinancialAccountsResourceTogglesSettingStatusDetailsCode, resolution: TreasuryFinancialAccountsResourceTogglesSettingStatusDetailsResolution option, ?restriction: TreasuryFinancialAccountsResourceTogglesSettingStatusDetailsRestriction) =
            {
                TreasuryFinancialAccountsResourceTogglesSettingStatusDetails.Code = code //required
                TreasuryFinancialAccountsResourceTogglesSettingStatusDetails.Resolution = resolution //required
                TreasuryFinancialAccountsResourceTogglesSettingStatusDetails.Restriction = restriction
            }

    and TreasuryInboundTransfersResourceFailureDetails = {
        ///Reason for the failure.
        Code: TreasuryInboundTransfersResourceFailureDetailsCode
    }
    with
        static member New (code: TreasuryInboundTransfersResourceFailureDetailsCode) =
            {
                TreasuryInboundTransfersResourceFailureDetails.Code = code //required
            }

    and TreasuryOutboundPaymentsResourceOutboundPaymentResourceTrackingDetails = {
        Ach: TreasuryOutboundPaymentsResourceAchTrackingDetails option
        ///The US bank account network used to send funds.
        Type: TreasuryOutboundPaymentsResourceOutboundPaymentResourceTrackingDetailsType
        UsDomesticWire: TreasuryOutboundPaymentsResourceUsDomesticWireTrackingDetails option
    }
    with
        static member New (``type``: TreasuryOutboundPaymentsResourceOutboundPaymentResourceTrackingDetailsType, ?ach: TreasuryOutboundPaymentsResourceAchTrackingDetails, ?usDomesticWire: TreasuryOutboundPaymentsResourceUsDomesticWireTrackingDetails) =
            {
                TreasuryOutboundPaymentsResourceOutboundPaymentResourceTrackingDetails.Type = ``type`` //required
                TreasuryOutboundPaymentsResourceOutboundPaymentResourceTrackingDetails.Ach = ach
                TreasuryOutboundPaymentsResourceOutboundPaymentResourceTrackingDetails.UsDomesticWire = usDomesticWire
            }

    and TreasuryOutboundPaymentsResourceReturnedStatus = {
        ///Reason for the return.
        Code: TreasuryOutboundPaymentsResourceReturnedStatusCode
        ///The Transaction associated with this object.
        Transaction: TreasuryOutboundPaymentsResourceReturnedStatusTransaction'AnyOf
    }
    with
        static member New (code: TreasuryOutboundPaymentsResourceReturnedStatusCode, transaction: TreasuryOutboundPaymentsResourceReturnedStatusTransaction'AnyOf) =
            {
                TreasuryOutboundPaymentsResourceReturnedStatus.Code = code //required
                TreasuryOutboundPaymentsResourceReturnedStatus.Transaction = transaction //required
            }

    and TreasuryOutboundPaymentsResourceReturnedStatusTransaction'AnyOf =
        | String of string
        | TreasuryTransaction of TreasuryTransaction

    and TreasuryOutboundTransfersResourceOutboundTransferResourceTrackingDetails = {
        Ach: TreasuryOutboundTransfersResourceAchTrackingDetails option
        ///The US bank account network used to send funds.
        Type: TreasuryOutboundTransfersResourceOutboundTransferResourceTrackingDetailsType
        UsDomesticWire: TreasuryOutboundTransfersResourceUsDomesticWireTrackingDetails option
    }
    with
        static member New (``type``: TreasuryOutboundTransfersResourceOutboundTransferResourceTrackingDetailsType, ?ach: TreasuryOutboundTransfersResourceAchTrackingDetails, ?usDomesticWire: TreasuryOutboundTransfersResourceUsDomesticWireTrackingDetails) =
            {
                TreasuryOutboundTransfersResourceOutboundTransferResourceTrackingDetails.Type = ``type`` //required
                TreasuryOutboundTransfersResourceOutboundTransferResourceTrackingDetails.Ach = ach
                TreasuryOutboundTransfersResourceOutboundTransferResourceTrackingDetails.UsDomesticWire = usDomesticWire
            }

    and TreasuryOutboundTransfersResourceReturnedDetails = {
        ///Reason for the return.
        Code: TreasuryOutboundTransfersResourceReturnedDetailsCode
        ///The Transaction associated with this object.
        Transaction: TreasuryOutboundTransfersResourceReturnedDetailsTransaction'AnyOf
    }
    with
        static member New (code: TreasuryOutboundTransfersResourceReturnedDetailsCode, transaction: TreasuryOutboundTransfersResourceReturnedDetailsTransaction'AnyOf) =
            {
                TreasuryOutboundTransfersResourceReturnedDetails.Code = code //required
                TreasuryOutboundTransfersResourceReturnedDetails.Transaction = transaction //required
            }

    and TreasuryOutboundTransfersResourceReturnedDetailsTransaction'AnyOf =
        | String of string
        | TreasuryTransaction of TreasuryTransaction

    and TreasuryReceivedCreditsResourceLinkedFlows = {
        ///The CreditReversal created as a result of this ReceivedCredit being reversed.
        CreditReversal: string option
        ///Set if the ReceivedCredit was created due to an [Issuing Authorization](https://api.stripe.com#issuing_authorizations) object.
        IssuingAuthorization: string option
        ///Set if the ReceivedCredit is also viewable as an [Issuing transaction](https://api.stripe.com#issuing_transactions) object.
        IssuingTransaction: string option
        ///ID of the source flow. Set if `network` is `stripe` and the source flow is visible to the user. Examples of source flows include OutboundPayments, payouts, or CreditReversals.
        SourceFlow: string option
        ///The expandable object of the source flow.
        SourceFlowDetails: TreasuryReceivedCreditsResourceSourceFlowsDetails option
        ///The type of flow that originated the ReceivedCredit (for example, `outbound_payment`).
        SourceFlowType: string option
    }
    with
        static member New (creditReversal: string option, issuingAuthorization: string option, issuingTransaction: string option, sourceFlow: string option, sourceFlowType: string option, ?sourceFlowDetails: TreasuryReceivedCreditsResourceSourceFlowsDetails option) =
            {
                TreasuryReceivedCreditsResourceLinkedFlows.CreditReversal = creditReversal //required
                TreasuryReceivedCreditsResourceLinkedFlows.IssuingAuthorization = issuingAuthorization //required
                TreasuryReceivedCreditsResourceLinkedFlows.IssuingTransaction = issuingTransaction //required
                TreasuryReceivedCreditsResourceLinkedFlows.SourceFlow = sourceFlow //required
                TreasuryReceivedCreditsResourceLinkedFlows.SourceFlowType = sourceFlowType //required
                TreasuryReceivedCreditsResourceLinkedFlows.SourceFlowDetails = sourceFlowDetails |> Option.flatten
            }

    and TreasuryReceivedCreditsResourceReversalDetails = {
        ///Time before which a ReceivedCredit can be reversed.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Deadline: DateTime option
        ///Set if a ReceivedCredit cannot be reversed.
        RestrictedReason: TreasuryReceivedCreditsResourceReversalDetailsRestrictedReason option
    }
    with
        static member New (deadline: DateTime option, restrictedReason: TreasuryReceivedCreditsResourceReversalDetailsRestrictedReason option) =
            {
                TreasuryReceivedCreditsResourceReversalDetails.Deadline = deadline //required
                TreasuryReceivedCreditsResourceReversalDetails.RestrictedReason = restrictedReason //required
            }

    and TreasuryReceivedCreditsResourceSourceFlowsDetails = {
        CreditReversal: TreasuryCreditReversal option
        OutboundPayment: TreasuryOutboundPayment option
        OutboundTransfer: TreasuryOutboundTransfer option
        Payout: Payout option
        ///The type of the source flow that originated the ReceivedCredit.
        Type: TreasuryReceivedCreditsResourceSourceFlowsDetailsType
    }
    with
        static member New (``type``: TreasuryReceivedCreditsResourceSourceFlowsDetailsType, ?creditReversal: TreasuryCreditReversal, ?outboundPayment: TreasuryOutboundPayment, ?outboundTransfer: TreasuryOutboundTransfer, ?payout: Payout) =
            {
                TreasuryReceivedCreditsResourceSourceFlowsDetails.Type = ``type`` //required
                TreasuryReceivedCreditsResourceSourceFlowsDetails.CreditReversal = creditReversal
                TreasuryReceivedCreditsResourceSourceFlowsDetails.OutboundPayment = outboundPayment
                TreasuryReceivedCreditsResourceSourceFlowsDetails.OutboundTransfer = outboundTransfer
                TreasuryReceivedCreditsResourceSourceFlowsDetails.Payout = payout
            }

    and TreasuryReceivedDebitsResourceReversalDetails = {
        ///Time before which a ReceivedDebit can be reversed.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Deadline: DateTime option
        ///Set if a ReceivedDebit can't be reversed.
        RestrictedReason: TreasuryReceivedDebitsResourceReversalDetailsRestrictedReason option
    }
    with
        static member New (deadline: DateTime option, restrictedReason: TreasuryReceivedDebitsResourceReversalDetailsRestrictedReason option) =
            {
                TreasuryReceivedDebitsResourceReversalDetails.Deadline = deadline //required
                TreasuryReceivedDebitsResourceReversalDetails.RestrictedReason = restrictedReason //required
            }

    and TreasurySharedResourceBillingDetails = {
        Address: Address
        ///Email address.
        Email: string option
        ///Full name.
        Name: string option
    }
    with
        static member New (address: Address, email: string option, name: string option) =
            {
                TreasurySharedResourceBillingDetails.Address = address //required
                TreasurySharedResourceBillingDetails.Email = email //required
                TreasurySharedResourceBillingDetails.Name = name //required
            }

    and TreasurySharedResourceInitiatingPaymentMethodDetailsInitiatingPaymentMethodDetails = {
        BillingDetails: TreasurySharedResourceBillingDetails
        FinancialAccount: ReceivedPaymentMethodDetailsFinancialAccount option
        ///Set when `type` is `issuing_card`. This is an [Issuing Card](https://api.stripe.com#issuing_cards) ID.
        IssuingCard: string option
        ///Polymorphic type matching the originating money movement's source. This can be an external account, a Stripe balance, or a FinancialAccount.
        Type: TreasurySharedResourceInitiatingPaymentMethodDetailsInitiatingPaymentMethodDetailsType
        UsBankAccount: TreasurySharedResourceInitiatingPaymentMethodDetailsUsBankAccount option
    }
    with
        ///Set when `type` is `balance`.
        member _.Balance = "payments"

        static member New (billingDetails: TreasurySharedResourceBillingDetails, ``type``: TreasurySharedResourceInitiatingPaymentMethodDetailsInitiatingPaymentMethodDetailsType, ?financialAccount: ReceivedPaymentMethodDetailsFinancialAccount, ?issuingCard: string, ?usBankAccount: TreasurySharedResourceInitiatingPaymentMethodDetailsUsBankAccount) =
            {
                TreasurySharedResourceInitiatingPaymentMethodDetailsInitiatingPaymentMethodDetails.BillingDetails = billingDetails //required
                TreasurySharedResourceInitiatingPaymentMethodDetailsInitiatingPaymentMethodDetails.Type = ``type`` //required
                TreasurySharedResourceInitiatingPaymentMethodDetailsInitiatingPaymentMethodDetails.FinancialAccount = financialAccount
                TreasurySharedResourceInitiatingPaymentMethodDetailsInitiatingPaymentMethodDetails.IssuingCard = issuingCard
                TreasurySharedResourceInitiatingPaymentMethodDetailsInitiatingPaymentMethodDetails.UsBankAccount = usBankAccount
            }

    and TreasuryTransactionsResourceFlowDetails = {
        CreditReversal: TreasuryCreditReversal option
        DebitReversal: TreasuryDebitReversal option
        InboundTransfer: TreasuryInboundTransfer option
        IssuingAuthorization: IssuingAuthorization option
        OutboundPayment: TreasuryOutboundPayment option
        OutboundTransfer: TreasuryOutboundTransfer option
        ReceivedCredit: TreasuryReceivedCredit option
        ReceivedDebit: TreasuryReceivedDebit option
        ///Type of the flow that created the Transaction. Set to the same value as `flow_type`.
        Type: TreasuryTransactionsResourceFlowDetailsType
    }
    with
        static member New (``type``: TreasuryTransactionsResourceFlowDetailsType, ?creditReversal: TreasuryCreditReversal, ?debitReversal: TreasuryDebitReversal, ?inboundTransfer: TreasuryInboundTransfer, ?issuingAuthorization: IssuingAuthorization, ?outboundPayment: TreasuryOutboundPayment, ?outboundTransfer: TreasuryOutboundTransfer, ?receivedCredit: TreasuryReceivedCredit, ?receivedDebit: TreasuryReceivedDebit) =
            {
                TreasuryTransactionsResourceFlowDetails.Type = ``type`` //required
                TreasuryTransactionsResourceFlowDetails.CreditReversal = creditReversal
                TreasuryTransactionsResourceFlowDetails.DebitReversal = debitReversal
                TreasuryTransactionsResourceFlowDetails.InboundTransfer = inboundTransfer
                TreasuryTransactionsResourceFlowDetails.IssuingAuthorization = issuingAuthorization
                TreasuryTransactionsResourceFlowDetails.OutboundPayment = outboundPayment
                TreasuryTransactionsResourceFlowDetails.OutboundTransfer = outboundTransfer
                TreasuryTransactionsResourceFlowDetails.ReceivedCredit = receivedCredit
                TreasuryTransactionsResourceFlowDetails.ReceivedDebit = receivedDebit
            }

    and UsBankAccountNetworks = {
        ///The preferred network.
        Preferred: string option
        ///All supported networks.
        Supported: UsBankAccountNetworksSupported list
    }
    with
        static member New (preferred: string option, supported: UsBankAccountNetworksSupported list) =
            {
                UsBankAccountNetworks.Preferred = preferred //required
                UsBankAccountNetworks.Supported = supported //required
            }

    and VerificationSessionRedaction = {
        ///Indicates whether this object and its related objects have been redacted or not.
        Status: VerificationSessionRedactionStatus
    }
    with
        static member New (status: VerificationSessionRedactionStatus) =
            {
                VerificationSessionRedaction.Status = status //required
            }

    ///You can configure [webhook endpoints](https://docs.stripe.com/webhooks/) via the API to be
    ///notified about events that happen in your Stripe account or connected
    ///accounts.
    ///Most users configure webhooks from [the dashboard](https://dashboard.stripe.com/webhooks), which provides a user interface for registering and testing your webhook endpoints.
    ///Related guide: [Setting up webhooks](https://docs.stripe.com/webhooks/configure)
    and WebhookEndpoint = {
        ///The API version events are rendered as for this webhook endpoint.
        ApiVersion: string option
        ///The ID of the associated Connect application.
        Application: string option
        ///Time at which the object was created. Measured in seconds since the Unix epoch.
        [<JsonField(Transform=typeof<Transforms.DateTimeEpoch>)>]Created: DateTime
        ///An optional description of what the webhook is used for.
        Description: string option
        ///The list of events to enable for this endpoint. `['*']` indicates that all events are enabled, except those that require explicit selection.
        EnabledEvents: string list
        ///Unique identifier for the object.
        Id: string
        ///If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.
        Livemode: bool
        ///Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
        Metadata: Map<string, string>
        ///The endpoint's secret, used to generate [webhook signatures](https://docs.stripe.com/webhooks/signatures). Only returned at creation.
        Secret: string option
        ///The status of the webhook. It can be `enabled` or `disabled`.
        Status: WebhookEndpointStatus
        ///The URL of the webhook endpoint.
        Url: string
    }
    with
        ///String representing the object's type. Objects of the same type share the same value.
        member _.Object = "webhook_endpoint"

        static member New (apiVersion: string option, application: string option, created: DateTime, description: string option, enabledEvents: string list, id: string, livemode: bool, metadata: Map<string, string>, status: WebhookEndpointStatus, url: string, ?secret: string) =
            {
                WebhookEndpoint.ApiVersion = apiVersion //required
                WebhookEndpoint.Application = application //required
                WebhookEndpoint.Created = created //required
                WebhookEndpoint.Description = description //required
                WebhookEndpoint.EnabledEvents = enabledEvents //required
                WebhookEndpoint.Id = id //required
                WebhookEndpoint.Livemode = livemode //required
                WebhookEndpoint.Metadata = metadata //required
                WebhookEndpoint.Status = status //required
                WebhookEndpoint.Url = url //required
                WebhookEndpoint.Secret = secret
            }

